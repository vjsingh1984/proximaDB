# Runtime-resolved quality contract. Build qrels-backed corpora with
# --deduplicate none so every ranked vector has one unambiguous source.

[[models]]
model_id = "nomic-ai/nomic-embed-text-v1.5"
revision = "e5cf08aadaa33385f5990def41f7a23405aec398"
tokenizer_revision = "e5cf08aadaa33385f5990def41f7a23405aec398"
effective_context_limit = 8192
document_template = "search_document: {text}"
query_template = "search_query: {text}"
native_dimension = 768
output_dimension = 256
output_dimensions = [768, 512, 256, 128, 64]
runtime_provider = "open-weights"
# Established by a full pinned SentenceTransformers runtime resolution. Corpus
# variants reproduce this exact contract from tokenizer/config only; embedding
# reloads the weights and independently verifies the same fingerprint.
sealed_runtime_contract_fingerprint = "d4e37c1c58261e365d9c8f791302b56b6295447a01ff7f7b355cc97bd512eabd"
runtime_device = "cpu"
local_files_only = true
