# =============================================================================
# ProximaDB Shared Build Workflow (Reusable)
# =============================================================================
# This reusable workflow defines all build jobs that can be shared between
# prerelease-ci and release workflows.
#
# Usage:
#   jobs:
#     build:
#       uses: ./.github/workflows/_shared-build.yml
#       with:
#         version: "0.2.0"
#         cache-key: "build-${{ github.sha }}"
#         upload-artifacts: true
#   validate:
#     needs: build
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/download-artifact@v4
# =============================================================================

name: Shared Build

on:
  workflow_call:
    inputs:
      version:
        description: 'Version to build'
        required: true
        type: string
      cache-key:
        description: 'Unique cache key for artifact caching'
        required: true
        type: string
      upload-artifacts:
        description: 'Whether to upload as workflow artifacts'
        required: false
        type: boolean
        default: true
      skip-cache:
        description: 'Skip cache and always rebuild'
        required: false
        type: boolean
        default: false
    outputs:
      binaries-built:
        description: 'Number of binary artifacts built'
        value: ${{ jobs.build-binaries.outputs.built-count }}
      wheels-built:
        description: 'Number of wheel artifacts built'
        value: ${{ jobs.build-wheels.outputs.built-count }}
      sdist-built:
        description: 'Number of sdist artifacts built'
        value: ${{ jobs.build-sdist.outputs.built-count }}
      total-artifacts:
        description: 'Total artifacts built'
        value: ${{ jobs.summary.outputs.total-count }}

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  CARGO_INCREMENTAL: 0
  CARGO_NET_RETRY: 10
  RUSTUP_MAX_RETRIES: 10

permissions:
  contents: read
  actions: write  # For caching

jobs:
  # ============================================================================
  # CACHE CHECK - Check if we can reuse artifacts
  # ============================================================================
  cache-check:
    name: Check Build Cache
    runs-on: ubuntu-latest
    outputs:
      cache-hit: ${{ steps.cache.outputs.cache-hit }}
      cache-key: ${{ steps.cache.outputs.key }}
    steps:
      - name: Generate Cache Key
        id: cache
        run: |
          if [ "${{ inputs.skip-cache }}" = "true" ]; then
            echo "cache-hit=false" >> $GITHUB_OUTPUT
            echo "key=skip-${{ github.run_id }}" >> $GITHUB_OUTPUT
            echo "::notice::Cache skip requested, will rebuild"
          else
            KEY="artifacts-${{ inputs.cache-key }}"
            echo "key=$KEY" >> $GITHUB_OUTPUT
            echo "Checking for cache: $KEY"
          fi

      - name: Restore Cached Artifacts
        uses: actions/cache/restore@v4
        id: restore
        with:
          path: |
            build-cache/
            !build-cache/**/*.log
          key: ${{ steps.cache.outputs.key }}
          restore-keys: |
            artifacts-

      - name: Verify Cache
        run: |
          if [ "${{ steps.restore.outputs.cache-hit }}" = "true" ]; then
            echo "::notice::Cache hit! Found cached artifacts"
            ls -la build-cache/ || echo "Cache dir empty"
            echo "cache-hit=true" >> $GITHUB_STEP_SUMMARY
          else
            echo "::notice::Cache miss, will build"
            echo "cache-hit=false" >> $GITHUB_STEP_SUMMARY
          fi

  # ============================================================================
  # BUILD BINARIES - Skipped if cache hit
  # ============================================================================
  build-binaries:
    name: Build ${{ matrix.target }}
    needs: cache-check
    if: needs.cache-check.outputs.cache-hit != 'true'
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-22.04
            archive: tar.gz
          - target: x86_64-unknown-linux-musl
            os: ubuntu-22.04
            archive: tar.gz
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-22.04
            archive: tar.gz
            cross: true
          - target: x86_64-apple-darwin
            os: macos-13
            archive: tar.gz
          - target: aarch64-apple-darwin
            os: macos-14
            archive: tar.gz
          - target: x86_64-pc-windows-msvc
            os: windows-2022
            archive: zip
    outputs:
      built-count: ${{ steps.summary.outputs.count }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Cross
        if: matrix.cross
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Install Linux Dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libssl-dev

      - name: Setup Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          prefix-key: "${{ inputs.cache-key }}-${{ matrix.target }}"
          shared-key: "shared-build"

      - name: Build Binaries
        run: |
          if [ "${{ matrix.cross }}" = "true" ]; then
            cross build --release --target ${{ matrix.target }} \
              --bin proximadb-server \
              --bin proximadb-bench \
              --bin proximadb-migrate
          else
            cargo build --release --target ${{ matrix.target }} \
              --bin proximadb-server \
              --bin proximadb-bench \
              --bin proximadb-migrate
          fi
        shell: bash

      - name: Create Archive (Unix)
        if: runner.os != 'Windows'
        run: |
          VERSION="${{ inputs.version }}"
          TARGET="${{ matrix.target }}"
          ARCHIVE_NAME="proximadb-${VERSION}-${TARGET}"
          mkdir -p "${ARCHIVE_NAME}"
          cp target/${TARGET}/release/proximadb-server "${ARCHIVE_NAME}/"
          cp target/${TARGET}/release/proximadb-bench "${ARCHIVE_NAME}/"
          cp target/${TARGET}/release/proximadb-migrate "${ARCHIVE_NAME}/"
          cp -r config "${ARCHIVE_NAME}/" 2>/dev/null || true
          tar -czvf "${ARCHIVE_NAME}.tar.gz" "${ARCHIVE_NAME}"
          shasum -a 256 "${ARCHIVE_NAME}.tar.gz" > "${ARCHIVE_NAME}.tar.gz.sha256"
          echo "ARCHIVE_NAME=${ARCHIVE_NAME}.tar.gz" >> $GITHUB_ENV

      - name: Create Archive (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $VERSION = "${{ inputs.version }}"
          $TARGET = "${{ matrix.target }}"
          $ARCHIVE_NAME = "proximadb-${VERSION}-${TARGET}"
          New-Item -ItemType Directory -Path $ARCHIVE_NAME -Force
          Copy-Item "target/${TARGET}/release/proximadb-server.exe" "${ARCHIVE_NAME}/"
          Copy-Item "target/${TARGET}/release/proximadb-bench.exe" "${ARCHIVE_NAME}/"
          Copy-Item "target/${TARGET}/release/proximadb-migrate.exe" "${ARCHIVE_NAME}/"
          Copy-Item -Recurse "config" "${ARCHIVE_NAME}/" -ErrorAction SilentlyContinue
          Compress-Archive -Path $ARCHIVE_NAME -DestinationPath "${ARCHIVE_NAME}.zip"
          $hash = Get-FileHash "${ARCHIVE_NAME}.zip" -Algorithm SHA256
          "$($hash.Hash.ToLower())  ${ARCHIVE_NAME}.zip" | Out-File -Encoding ascii "${ARCHIVE_NAME}.zip.sha256"
          echo "ARCHIVE_NAME=${ARCHIVE_NAME}.zip" >> $env:GITHUB_ENV

      - name: Save to Build Cache
        uses: actions/cache/save@v4
        with:
          path: |
            proximadb-*.${{ matrix.archive }}
            proximadb-*.${{ matrix.archive }}.sha256
          key: binaries-${{ inputs.cache-key }}-${{ matrix.target }}-${{ github.run_id }}

      - name: Upload Binary Artifact
        if: inputs.upload-artifacts == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.target }}
          path: |
            proximadb-*.${{ matrix.archive }}
            proximadb-*.${{ matrix.archive }}.sha256
          retention-days: 7

      - name: Build Summary
        id: summary
        run: echo "count=1" >> $GITHUB_OUTPUT

  # ============================================================================
  # BUILD WHEELS - Skipped if cache hit
  # ============================================================================
  build-wheels:
    name: Build Wheels (${{ matrix.os }}, ${{ matrix.python }})
    needs: cache-check
    if: needs.cache-check.outputs.cache-hit != 'true'
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux x86_64
          - os: ubuntu-22.04
            target: x86_64
            python: "3.9"
          - os: ubuntu-22.04
            target: x86_64
            python: "3.10"
          - os: ubuntu-22.04
            target: x86_64
            python: "3.11"
          - os: ubuntu-22.04
            target: x86_64
            python: "3.12"
          # Linux aarch64
          - os: ubuntu-22.04
            target: aarch64
            python: "3.10"
          - os: ubuntu-22.04
            target: aarch64
            python: "3.11"
          - os: ubuntu-22.04
            target: aarch64
            python: "3.12"
          # macOS x86_64
          - os: macos-13
            target: x86_64
            python: "3.9"
          - os: macos-13
            target: x86_64
            python: "3.10"
          - os: macos-13
            target: x86_64
            python: "3.11"
          - os: macos-13
            target: x86_64
            python: "3.12"
          # macOS aarch64
          - os: macos-14
            target: aarch64
            python: "3.9"
          - os: macos-14
            target: aarch64
            python: "3.10"
          - os: macos-14
            target: aarch64
            python: "3.11"
          - os: macos-14
            target: aarch64
            python: "3.12"
          # Windows x64
          - os: windows-2022
            target: x64
            python: "3.9"
          - os: windows-2022
            target: x64
            python: "3.10"
          - os: windows-2022
            target: x64
            python: "3.11"
          - os: windows-2022
            target: x64
            python: "3.12"
    outputs:
      built-count: ${{ steps.summary.outputs.count }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          prefix-key: "${{ inputs.cache-key }}-wheel-${{ matrix.os }}-${{ matrix.target }}"
          shared-key: "shared-build-wheel"

      - name: Install Maturin
        run: pip install maturin

      - name: Build Wheel (Linux x86_64)
        if: matrix.os == 'ubuntu-22.04' && matrix.target == 'x86_64'
        uses: PyO3/maturin-action@v1
        with:
          target: x86_64
          manylinux: auto
          args: --release --out dist --features python

      - name: Build Wheel (Linux aarch64)
        if: matrix.os == 'ubuntu-22.04' && matrix.target == 'aarch64'
        uses: PyO3/maturin-action@v1
        with:
          target: aarch64
          manylinux: auto
          args: --release --out dist --features python

      - name: Build Wheel (macOS/Windows)
        if: runner.os != 'Linux'
        run: |
          if [ "${{ runner.os }}" = "macOS" ]; then
            if [ "${{ matrix.target }}" = "aarch64" ]; then
              maturin build --release --out dist --features python --target aarch64-apple-darwin
            else
              maturin build --release --out dist --features python --target x86_64-apple-darwin
            fi
          else
            maturin build --release --out dist --features python
          fi
        shell: bash

      - name: Save to Build Cache
        uses: actions/cache/save@v4
        with:
          path: dist/*.whl
          key: wheel-${{ inputs.cache-key }}-${{ matrix.os }}-${{ matrix.target }}-py${{ matrix.python }}-${{ github.run_id }}

      - name: Upload Wheel Artifact
        if: inputs.upload-artifacts == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.os }}-${{ matrix.target }}-py${{ matrix.python }}
          path: dist/*.whl
          retention-days: 7

      - name: Build Summary
        id: summary
        run: echo "count=1" >> $GITHUB_OUTPUT

  # ============================================================================
  # BUILD EMBEDDED WHEELS - Skipped if cache hit
  # ============================================================================
  # build-embedded-wheels:
  # NOTE: Temporarily disabled - clients/python-embedded is missing Cargo.toml
  # and Rust source code. The package structure exists but the actual embedded
  # Rust bindings haven't been implemented yet. This needs to be added back
  # once the embedded mode is fully implemented.
  # TODO: Re-enable when clients/python-embedded has proper Rust bindings
  #
  # build-embedded-wheels:
  #   name: Build Embedded Wheels (${{ matrix.os }}, ${{ matrix.target }})
  #   needs: cache-check
  #   if: needs.cache-check.outputs.cache-hit != 'true'
  #   runs-on: ${{ matrix.os }}
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       include:
  #         - os: ubuntu-22.04
  #           target: x86_64
  #         - os: ubuntu-22.04
  #           target: aarch64
  #         - os: macos-13
  #           target: x86_64
  #         - os: macos-14
  #           target: aarch64
  #         - os: windows-2022
  #           target: x64
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.11"
  #
  #     - name: Install Rust Toolchain
  #       uses: dtolnay/rust-toolchain@stable
  #
  #     - name: Setup Rust Cache
  #       uses: Swatinem/rust-cache@v2
  #       with:
  #         prefix-key: "${{ inputs.cache-key }}-embedded-${{ matrix.os }}-${{ matrix.target }}"
  #         shared-key: "shared-build-embedded"
  #
  #     - name: Install Maturin
  #       run: pip install maturin
  #
  #     - name: Build Embedded Wheels (Linux x86_64)
  #       if: matrix.os == 'ubuntu-22.04' && matrix.target == 'x86_64'
  #       uses: PyO3/maturin-action@v1
  #       with:
  #         working-directory: clients/python-embedded
  #         target: x86_64
  #         manylinux: auto
  #         args: --release --out dist --features python
  #
  #     - name: Build Embedded Wheels (Linux aarch64)
  #       if: matrix.os == 'ubuntu-22.04' && matrix.target == 'aarch64'
  #       uses: PyO3/maturin-action@v1
  #       with:
  #         working-directory: clients/python-embedded
  #         target: aarch64
  #         manylinux: auto
  #         args: --release --out dist --features python
  #
  #     - name: Build Embedded Wheels (macOS/Windows)
  #       if: runner.os != 'Linux'
  #       working-directory: clients/python-embedded
  #       run: |
  #         if [ "${{ runner.os }}" = "macOS" ]; then
  #           if [ "${{ matrix.target }}" = "aarch64" ]; then
  #             maturin build --release --out dist --features python --target aarch64-apple-darwin
  #           else
  #             maturin build --release --out dist --features python --target x86_64-apple-darwin
  #           fi
  #         else
  #           maturin build --release --out dist --features python
  #         fi
  #       shell: bash
  #
  #     - name: Save to Build Cache
  #       uses: actions/cache/save@v4
  #       with:
  #         path: clients/python-embedded/dist/*.whl
  #         key: embedded-wheel-${{ inputs.cache-key }}-${{ matrix.os }}-${{ matrix.target }}-${{ github.run_id }}
  #
  #     - name: Upload Embedded Wheel Artifact
  #       if: inputs.upload-artifacts == 'true'
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: embedded-wheel-${{ matrix.os }}-${{ matrix.target }}
  #         path: clients/python-embedded/dist/*.whl
  #         retention-days: 7

  # ============================================================================
  # BUILD SDIST - Skipped if cache hit
  # ============================================================================
  build-sdist:
    name: Build Source Distribution
    needs: cache-check
    if: needs.cache-check.outputs.cache-hit != 'true'
    runs-on: ubuntu-22.04
    outputs:
      built-count: ${{ steps.summary.outputs.count }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Maturin
        run: pip install maturin

      - name: Build sdist (main package)
        run: maturin sdist --out dist

      # NOTE: Temporarily disabled - clients/python-embedded is missing Cargo.toml
      # TODO: Re-enable when embedded package has proper Rust bindings
      # - name: Build sdist (embedded package)
      #   working-directory: clients/python-embedded
      #   run: maturin sdist --out dist

      - name: Save to Build Cache
        uses: actions/cache/save@v4
        with:
          # NOTE: embedded sdist temporarily disabled - see above
          path: dist/*.tar.gz
          key: sdist-${{ inputs.cache-key }}-${{ github.run_id }}

      - name: Upload sdist Artifact
        if: inputs.upload-artifacts == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: sdist
          # NOTE: embedded sdist temporarily disabled - see above
          path: dist/*.tar.gz
          retention-days: 7

      - name: Build Summary
        id: summary
        run: echo "count=2" >> $GITHUB_OUTPUT

  # ============================================================================
  # CACHE ARTIFACTS - Save final cache entry
  # ============================================================================
  save-cache:
    name: Save Build Cache
    # NOTE: build-embedded-wheels removed from dependencies - see above for TODO
    needs: [cache-check, build-binaries, build-wheels, build-sdist]
    if: always() && needs.cache-check.outputs.cache-hit != 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Create Cache Manifest
        run: |
          mkdir -p build-cache
          echo "version=${{ inputs.version }}" > build-cache/manifest.txt
          echo "cache-key=${{ inputs.cache-key }}" >> build-cache/manifest.txt
          echo "timestamp=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> build-cache/manifest.txt

      - name: Save Primary Cache Entry
        uses: actions/cache/save@v4
        with:
          path: build-cache/
          key: ${{ needs.cache-check.outputs.cache-key }}

  # ============================================================================
  # SUMMARY
  # ============================================================================
  summary:
    name: Build Summary
    # NOTE: build-embedded-wheels removed from dependencies - see above for TODO
    needs: [cache-check, build-binaries, build-wheels, build-sdist]
    if: always()
    runs-on: ubuntu-latest
    outputs:
      total-count: ${{ steps.count.outputs.total }}
    steps:
      - name: Count Artifacts
        id: count
        run: |
          if [ "${{ needs.cache-check.outputs.cache-hit }}" = "true" ]; then
            echo "::notice::Used cached artifacts"
            echo "total=cached" >> $GITHUB_OUTPUT
          else
            BINARIES=${{ needs.build-binaries.outputs.built-count }}
            WHEELS=${{ needs.build-wheels.outputs.built-count }}
            SDIST=${{ needs.build-sdist.outputs.built-count }}
            TOTAL=$((BINARIES + WHEELS + SDIST))
            echo "total=$TOTAL" >> $GITHUB_OUTPUT
          fi
