// collection_avro removed - using proto types directly
/// Document schema definitions for semi-structured data validation.
pub mod document;
/// Relational schema definitions for SQL-compatible table structures.
pub mod relational;
