//! Compatibility re-export for generic ordered concurrent map utilities.
//!
//! The implementation lives in `proximadb-runtime-common` because it is a
//! runtime data-structure helper, not root product wiring.

pub use proximadb_runtime_common::skiplist::*;
