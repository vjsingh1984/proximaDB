//! Column Projection Strategy Tests
//!
//! These tests are disabled because they test private internal APIs.
//! Column projection functionality should be tested through integration tests
//! that use the public search APIs which exercise these features indirectly.

#[cfg(disabled_private_api_tests)]
mod disabled_tests {
    // All tests moved to integration tests to avoid testing private APIs directly
}

#[test]
fn test_column_projection_through_public_api() {
    // This is a placeholder for a proper integration test that would
    // test column projection through the public search API
    // TODO: Implement integration test that exercises column projection
    // through two-stage search with different quantization levels
}