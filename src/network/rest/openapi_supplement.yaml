# OpenAPI supplement fragment (TD-126 Phase 1).
#
# This fragment carries the published REST surfaces that are NOT yet generated
# from `#[utoipa::path]` annotations on the live handlers. The generator
# (src/network/rest/openapi.rs) deep-merges this on top of the utoipa-generated
# core document to produce docs/openapi/proximadb-openapi.yaml, and the CI drift
# gate covers the merged result.
#
# Why these surfaces are here rather than annotated in code (tracked for later
# TD-126 increments):
#   * /health*, /api/v2/_meta/capabilities  — handlers return dynamic JSON
#     (serde_json::Value), no concrete DTO to derive ToSchema on yet.
#   * /api/v2/graphs/*                       — handlers + DTOs live in the
#     `proximadb-api` crate with private request types (RestNodeInput, ...).
#   * hybrid / document-collections /
#     observability                          — free-form passthrough endpoints.
#   * abac / pinning / affinity / primary-pod — operator & control-plane
#     routes excluded from the utoipa `ApiDoc` by convention (registered in
#     `operator_and_control_v2_routes` / its siblings in handlers.rs), not
#     because they lack concrete DTOs.
#
# The SDK contract gates (Python/Rust/Go/TS openapi_contract tests) exercise
# several of these (graphs, health probes), so they MUST stay in the published
# spec. Keep this fragment in sync with the live handlers until they are
# annotated; until then it is the source of truth for these operations only.
paths:
  /health:
    get:
      tags: [Health]
      operationId: getHealth
      summary: Get server health.
      security: []
      responses:
        "200":
          description: Health status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
  /health/live:
    get:
      tags: [Health]
      operationId: getLiveness
      summary: Kubernetes liveness probe.
      security: []
      responses:
        "200":
          description: Process is alive.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProbeResponse"
  /health/ready:
    get:
      tags: [Health]
      operationId: getReadiness
      summary: Kubernetes readiness probe.
      security: []
      responses:
        "200":
          description: Server is ready.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProbeResponse"
  /api/v2/_meta/capabilities:
    get:
      tags: [Meta]
      operationId: getCapabilities
      summary: Negotiate server capabilities.
      description: >
        Advertises API version, supported features, limits, and the error-
        envelope contract so SDKs can adapt instead of hard-coding assumptions.
      security: []
      responses:
        "200":
          description: Capability document.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CapabilitiesResponse"
  # ── Graph surface (v2) — handlers live in the proximadb-api crate
  # (crates/platform/proximadb-api/src/rest/canonical/graph.rs). Every
  # operation returns the graph envelope: {success, data?, error?,
  # metadata?} (schemas Graph*Response below) — this wave (6) corrected
  # the previously-published FLAT response models, which could not parse
  # the real wire format. Error statuses (400/404/500) carry the same
  # envelope with success=false and a SCREAMING_SNAKE_CASE error code.
  # Extractor rejections (malformed JSON) are plain text (axum default).
  # NOT exposed here, deliberately: /rag (501 until the port lands),
  # branch-merge (free-form response, TD deferral), and the legacy
  # 308-redirect shims (/api/v2/nodes|edges|stats — clients must use the
  # canonical graph-scoped paths).
  /api/v2/graphs:
    post:
      tags: [Graphs]
      operationId: createGraph
      summary: Create a graph collection.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateGraphRequest"
      responses:

        "201":
          description: Graph collection created (envelope; data = serialized collection).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphCollectionResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "500": { $ref: "#/components/responses/GraphInternal" }
    get:
      tags: [Graphs]
      operationId: listGraphs
      summary: List graph collections.
      responses:
        "200":
          description: Graph collection listing (envelope; data = array).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphCollectionListResponse"
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    get:
      tags: [Graphs]
      operationId: getGraph
      summary: Get a graph collection by id.
      responses:
        "200":
          description: Graph collection metadata (envelope; data = serialized collection).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphCollectionResponse"
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
    delete:
      tags: [Graphs]
      operationId: deleteGraph
      summary: Delete a graph collection.
      description: |
        204 on success (the handler's success body is not transmitted on
        a 204). Not-found maps to 404 with the envelope error.
      responses:
        "204": { description: Graph collection deleted. }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/schema:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    put:
      tags: [Graphs]
      operationId: updateGraphSchema
      summary: Update a graph collection's schema.
      description: |
        The body carries the schema as free-form JSON (the handler
        deserializes it into the port's GraphSchema). 200 returns the
        updated collection (envelope; data = serialized collection);
        invalid schema JSON maps to 400.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateGraphSchemaRequest"
      responses:

        "200":
          description: Updated collection (envelope).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphCollectionResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/nodes:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    post:
      tags: [Graphs]
      operationId: createNode
      summary: Create a node in a graph.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateNodeRequest"
      responses:

        "201":
          description: Created node (envelope).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphNodeResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/nodes/{node_id}:
    parameters:
      - $ref: "#/components/parameters/GraphId"
      - $ref: "#/components/parameters/NodeId"
    get:
      tags: [Graphs]
      operationId: getNode
      summary: Get a node by id.
      responses:
        "200":
          description: Node payload (envelope).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphNodeResponse"
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
    put:
      tags: [Graphs]
      operationId: updateNode
      summary: Replace a node.
      description: |
        Full replacement: the body is the complete node input (its `id`
        is overwritten by the path's `node_id`).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NodeInput"
      responses:

        "200":
          description: Updated node (envelope).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphNodeResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
    delete:
      tags: [Graphs]
      operationId: deleteNode
      summary: Delete a node by id.
      responses:
        "200":
          description: Deleted node (envelope — the deleted node is echoed).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphNodeResponse"
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/nodes/{node_id}/neighbors:
    parameters:
      - $ref: "#/components/parameters/GraphId"
      - $ref: "#/components/parameters/NodeId"
    get:
      tags: [Graphs]
      operationId: getNodeNeighbors
      summary: Get a node's neighbors.
      responses:
        "200":
          description: Neighbor nodes (envelope; data = node array).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphNodeListResponse"
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/edges:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    post:
      tags: [Graphs]
      operationId: createEdge
      summary: Create an edge in a graph.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEdgeRequest"
      responses:

        "201":
          description: Created edge (envelope).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphEdgeResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/edges/{edge_id}:
    parameters:
      - $ref: "#/components/parameters/GraphId"
      - $ref: "#/components/parameters/EdgeId"
    get:
      tags: [Graphs]
      operationId: getEdge
      summary: Get an edge by id.
      responses:
        "200":
          description: Edge payload (envelope).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphEdgeResponse"
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
    put:
      tags: [Graphs]
      operationId: updateEdge
      summary: Replace an edge.
      description: |
        Full replacement: the body is the complete edge input (its `id`
        is overwritten by the path's `edge_id`).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EdgeInput"
      responses:

        "200":
          description: Updated edge (envelope).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphEdgeResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
    delete:
      tags: [Graphs]
      operationId: deleteEdge
      summary: Delete an edge by id.
      responses:
        "200":
          description: Deleted edge (envelope — the deleted edge is echoed).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphEdgeResponse"
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/nodes/batch:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    post:
      tags: [Graphs]
      operationId: batchCreateNodes
      summary: Create multiple nodes in a single call.
      description: |
        Per-item rejections ride `failed_count`/`errors[]` while
        `results[]` carries what landed; the HTTP status stays 200.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchCreateNodesRequest"
      responses:

        "200":
          description: Batch outcome (envelope).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphBatchNodesResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/edges/batch:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    post:
      tags: [Graphs]
      operationId: batchCreateEdges
      summary: Create multiple edges in a single call.
      description: |
        Per-item rejections ride `failed_count`/`errors[]` while
        `results[]` carries what landed; the HTTP status stays 200.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchCreateEdgesRequest"
      responses:

        "200":
          description: Batch outcome (envelope).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphBatchEdgesResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/traverse:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    post:
      tags: [Graphs]
      operationId: traverseGraph
      summary: Traverse a graph from a start node.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TraverseRequest"
      responses:

        "200":
          description: Traversal result (envelope).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphTraversalResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/walk:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    post:
      tags: [Graphs]
      operationId: walkGraph
      summary: BFS walk from a start node.
      description: |
        A traverse with the algorithm pinned to BFS (defaults:
        max_depth 2, limit 100).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WalkRequest"
      responses:

        "200":
          description: Walk result (envelope; traversal shape).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphTraversalResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/step:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    post:
      tags: [Graphs]
      operationId: stepGraph
      summary: Single-step navigation (neighbors of one node).
      description: |
        Returns the neighbors of `node_id` (optionally restricted to one
        `edge_type`) in the traversal result shape.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WalkStepRequest"
      responses:

        "200":
          description: Step result (envelope).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphTraversalResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/shortest-path:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    post:
      tags: [Graphs]
      operationId: shortestPath
      summary: Shortest path between two nodes.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShortestPathRequest"
      responses:

        "200":
          description: Shortest-path result (envelope; found=false when unreachable).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphShortestPathResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/query:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    post:
      tags: [Graphs]
      operationId: executeGraphQuery
      summary: Execute a graph query.
      description: |
        Executes the query text through the port's JSON query path.
        `language` (default `native`) and `timeout_ms` are accepted but
        currently unused server-side.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GraphQueryRequest"
      responses:

        "200":
          description: Query result (envelope; data carries rows and row_count).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphQueryResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/query/nodes:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    post:
      tags: [Graphs]
      operationId: queryNodes
      summary: Query nodes by labels/properties.
      description: |
        Paginated: `limit` (default 100) + `offset`, or a
        `continuation_token` of the form `offset:<n>`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GraphNodeQuery"
      responses:

        "200":
          description: Matching nodes (envelope; QueryResults shape).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphNodeQueryResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/query/edges:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    post:
      tags: [Graphs]
      operationId: queryEdges
      summary: Query edges by type/endpoints/properties.
      description: |
        Paginated: `limit` (default 100) + `offset`, or a
        `continuation_token` of the form `offset:<n>`. `edge_type`
        defaults to the empty string (matches untyped edges).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GraphEdgeQuery"
      responses:

        "200":
          description: Matching edges (envelope; QueryResults shape).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphEdgeQueryResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/components:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    get:
      tags: [Graphs]
      operationId: getConnectedComponents
      summary: Get the graph's connected components.
      responses:
        "200":
          description: Components (envelope; data carries the node-id arrays).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphComponentsResponse"
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/cycles:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    get:
      tags: [Graphs]
      operationId: checkCycles
      summary: Check whether the graph contains a cycle.
      responses:
        "200":
          description: Cycle check (envelope; data carries has_cycle).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphCyclesResponse"
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/constraints/unique:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    post:
      tags: [Graphs]
      operationId: addUniqueConstraint
      summary: Add a unique constraint on (label, property).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UniqueConstraintRequest"
      responses:

        "200":
          description: DDL outcome (envelope; data carries success).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphDdlResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
    delete:
      tags: [Graphs]
      operationId: removeUniqueConstraint
      summary: Remove a unique constraint on (label, property).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UniqueConstraintRequest"
      responses:

        "200":
          description: DDL outcome (envelope; data carries success).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphDdlResponse"
        "400": { $ref: "#/components/responses/GraphBadRequest" }
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }
  /api/v2/graphs/{graph_id}/stats:
    parameters:
      - $ref: "#/components/parameters/GraphId"
    get:
      tags: [Graphs]
      operationId: getGraphStats
      summary: Get graph statistics.
      responses:
        "200":
          description: Graph statistics (envelope; data = GraphStats).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GraphStatsResponse"
        "404": { $ref: "#/components/responses/GraphNotFound" }
        "500": { $ref: "#/components/responses/GraphInternal" }

  # TD-SPECRAT-1 wave 7 — time-series surface (TD-TS-1; handlers in
  # src/network/rest/v2/timeseries.rs, mounted unconditionally in the v2
  # router; backed by the process-global TimeSeriesService over the
  # native TST engine — real, not stubs). UNLIKE the other supplement
  # surfaces, these handlers consume TenantContext from the middleware —
  # the optional X-Tenant-ID header IS consulted (per-tenant structural
  # isolation: each tenant gets its own engine rooted at
  # <data>/timeseries/<tenant>). Responses are FLAT (no envelope). All
  # handler errors map to 500 with the canonical error envelope (no 404
  # exists on this surface); an UNKNOWN collection is NOT an error —
  # query/aggregate return 200 with empty points/buckets, delete returns
  # 200 with success:false. Extractor rejections are plain text.
  # Aggregate buckets are free-form JSON (the aggregation result
  # objects).
  /api/v2/timeseries/collections:
    post:
      tags: [Timeseries]
      operationId: createTimeseriesCollection
      summary: Create a time-series collection.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TsCollectionConfig"
      responses:
        "200":
          description: Created (echoes the tenant-clean name).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TsCreateResponse"
        "500": { $ref: "#/components/responses/InternalError" }
    get:
      tags: [Timeseries]
      operationId: listTimeseriesCollections
      summary: List this tenant's time-series collections.
      responses:
        "200":
          description: Tenant-scoped collection configs.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TsListResponse"
        "500": { $ref: "#/components/responses/InternalError" }
  /api/v2/timeseries/collections/{collection_id}:
    parameters:
      - name: collection_id
        in: path
        required: true
        schema: { type: string }
    delete:
      tags: [Timeseries]
      operationId: deleteTimeseriesCollection
      summary: Delete a time-series collection.
      description: |
        Always 200; `success` distinguishes an actual deletion from a
        no-op (unknown collection).
      responses:
        "200":
          description: Deletion outcome.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TsDeleteResponse"
        "500": { $ref: "#/components/responses/InternalError" }
  /api/v2/timeseries/collections/{collection_id}/ingest:
    parameters:
      - name: collection_id
        in: path
        required: true
        schema: { type: string }
    post:
      tags: [Timeseries]
      operationId: ingestTimeseries
      summary: Ingest time-series points.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TsIngestRequest"
      responses:
        "200":
          description: Ingest count.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TsIngestResponse"
        "500": { $ref: "#/components/responses/InternalError" }
  /api/v2/timeseries/collections/{collection_id}/query:
    parameters:
      - name: collection_id
        in: path
        required: true
        schema: { type: string }
    post:
      tags: [Timeseries]
      operationId: queryTimeseries
      summary: Query points in a time range (epoch millis).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TsQueryRequest"
      responses:
        "200":
          description: Matching points.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TsQueryResponse"
        "500": { $ref: "#/components/responses/InternalError" }
  /api/v2/timeseries/collections/{collection_id}/aggregate:
    parameters:
      - name: collection_id
        in: path
        required: true
        schema: { type: string }
    post:
      tags: [Timeseries]
      operationId: aggregateTimeseries
      summary: Aggregate points into time buckets.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TsAggregateRequest"
      responses:
        "200":
          description: Aggregation buckets (free-form result objects).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TsAggregateResponse"
        "500": { $ref: "#/components/responses/InternalError" }
  # TD-SPECRAT-1 wave 10 — unified query surface (LIVE per the wave-8
  # round-2 correction; port impl src/query/unified_query_port_impl.rs).
  # Handlers: crates/platform/proximadb-api/src/rest/canonical/
  # multimodal_query.rs, mounted at /api/v2/unified (conditional on the
  # unified_query_port being wired — the default server wires it).
  # NOTE the error shape: a BARE {"error": "<string>"} object — NOT the
  # canonical envelope. Success responses are free-form JSON (the port
  # returns serde_json::Value) except prepare (201 {statement_id}) and
  # prepared delete (204). 501 fires only when the port returns
  # "not implemented"/UNIMPLEMENTED errors (non-default constructions).
  # Extractor rejections (malformed JSON, missing required field, wrong
  # content-type) are axum plain-text 400/415/422 — not the JSON shape.
  /api/v2/unified/execute:
    post:
      tags: [Query]
      operationId: executeUnifiedQuery
      summary: Execute a unified (UQL) query.
      description: |
        The optional `X-Tenant-ID` header is not consulted by this
        handler — unified queries run without tenant selection.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnifiedExecuteRequest"
      responses:
        "200":
          description: Query result (free-form — the port's JSON rendering).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedOpenValue"
        "400":
          description: Empty query — the BARE error shape (a plain string under error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
        "500":
          description: Port error — the BARE error shape (a plain string under error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
        "501":
          description: The port returned not-implemented (non-default constructions).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
  /api/v2/unified/multi-model:
    post:
      tags: [Query]
      operationId: executeMultiModelQuery
      summary: Execute a multi-model query (free-form body).
      description: |
        The optional `X-Tenant-ID` header is not consulted by this
        handler — unified queries run without tenant selection.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnifiedOpenValue"
      responses:
        "200":
          description: Query result (free-form).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedOpenValue"
        "500":
          description: Port error — the BARE error shape (a plain string under error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
        "501":
          description: The port returned not-implemented (non-default constructions).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
  /api/v2/unified/federated:
    post:
      tags: [Query]
      operationId: executeFederatedQuery
      summary: Execute a federated query.
      description: |
        The optional `X-Tenant-ID` header is not consulted by this
        handler — unified queries run without tenant selection.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnifiedFederatedRequest"
      responses:
        "200":
          description: Query result (free-form).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedOpenValue"
        "400":
          description: Empty query — the BARE error shape (a plain string under error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
        "500":
          description: Port error — the BARE error shape (a plain string under error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
        "501":
          description: The port returned not-implemented (non-default constructions).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
  /api/v2/unified/distributed:
    post:
      tags: [Query]
      operationId: executeDistributedQuery
      summary: Execute a distributed query.
      description: |
        The optional `X-Tenant-ID` header is not consulted by this
        handler — unified queries run without tenant selection.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnifiedDistributedRequest"
      responses:
        "200":
          description: Query result (free-form).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedOpenValue"
        "500":
          description: Port error — the BARE error shape (a plain string under error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
        "501":
          description: The port returned not-implemented (non-default constructions).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
  /api/v2/unified/explain:
    post:
      tags: [Query]
      operationId: explainUnifiedQuery
      summary: Explain a unified query (plan, no execution).
      description: |
        The optional `X-Tenant-ID` header is not consulted by this
        handler — unified queries run without tenant selection.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnifiedExplainRequest"
      responses:
        "200":
          description: The plan (free-form).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedOpenValue"
        "500":
          description: Port error — the BARE error shape (a plain string under error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
        "501":
          description: The port returned not-implemented (non-default constructions).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
  /api/v2/unified/prepare:
    post:
      tags: [Query]
      operationId: prepareStatement
      summary: Prepare a statement for repeated execution.
      description: |
        The optional `X-Tenant-ID` header is not consulted by this
        handler — unified queries run without tenant selection.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnifiedPrepareRequest"
      responses:
        "201":
          description: Prepared — the statement id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedPrepareResponse"
        "500":
          description: Port error — the BARE error shape (a plain string under error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
        "501":
          description: The port returned not-implemented (non-default constructions).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
  /api/v2/unified/execute/{statement_id}:
    parameters:
      - name: statement_id
        in: path
        required: true
        schema: { type: string }
    post:
      tags: [Query]
      operationId: executePreparedStatement
      summary: Execute a prepared statement with parameters.
      description: |
        The optional `X-Tenant-ID` header is not consulted by this
        handler — unified queries run without tenant selection.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnifiedExecutePreparedRequest"
      responses:
        "200":
          description: Query result (free-form).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedOpenValue"
        "500":
          description: Port error — the BARE error shape (a plain string under error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
        "501":
          description: The port returned not-implemented (non-default constructions).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
  /api/v2/unified/prepared/{statement_id}:
    parameters:
      - name: statement_id
        in: path
        required: true
        schema: { type: string }
    delete:
      tags: [Query]
      operationId: deletePreparedStatement
      summary: Delete a prepared statement.
      description: |
        The optional `X-Tenant-ID` header is not consulted by this
        handler — unified queries run without tenant selection.
      responses:
        "204":
          description: |
            Deleted. NOTE an unknown (or already-deleted) statement id
            is a 500 with the bare error shape — not a 204 and not a
            404; an expired-but-not-evicted id may still 204.
        "500":
          description: Port error — the BARE error shape (a plain string under error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
        "501":
          description: The port returned not-implemented (non-default constructions).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
  /api/v2/unified/prepared/stats:
    post:
      tags: [Query]
      operationId: getPreparedStats
      summary: Prepared-statement execution statistics.
      description: |
        The optional `X-Tenant-ID` header is not consulted by this
        handler — unified queries run without tenant selection.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnifiedPreparedStatsRequest"
      responses:
        "200":
          description: Stats (free-form).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedOpenValue"
        "500":
          description: Port error — the BARE error shape (a plain string under error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
        "501":
          description: The port returned not-implemented (non-default constructions).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnifiedBareError"
  # TD-SPECRAT-1 wave 8 — rank search + program census resolution.
  # POST /api/v2/rank/search: the multi-phase ranking pipeline (R-7c.1),
  # mounted unconditionally in handlers.rs; no per-route permission gate.
  # The DEFAULT server wires RankServices (the pipeline serves 200s);
  # 501 fires only in non-default constructions (embedded/test AppState
  # without RankServices). 404 = named rank profile not found; 400 =
  # invalid profile; 500 = pipeline failure. Responses carry per-hit
  # score vectors only when a rank profile was attached (retrieval-only
  # mode omits them).
  /api/v2/rank/search:
    post:
      tags: [Rank]
      operationId: rankSearch
      summary: Multi-phase ranked search (retrieval + optional rerank profile).
      description: |
        Runs the rank pipeline over the target collection: candidate
        retrieval (vector + optional BM25 text leg), then the global
        composition phase, then an optional profile-driven second phase.
        `rank_profile` selects a named profile from the server registry;
        when omitted, the response is retrieval-only (no score vectors —
        the zero-cost-when-unused contract). `rank_overrides` tweak
        per-phase knobs on top of the resolved profile. `query_vector`
        is the POST-embedding vector (the caller computed it).
        The optional `X-Tenant-ID` header is not consulted by this
        handler beyond the standard tenant middleware context.
        Extractor rejections (malformed JSON, missing required field,
        wrong content-type) are normalized to the canonical JSON error
        envelope with status 400/415/422.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RankSearchRequest"
      responses:
        "200":
          description: Ranked hits.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RankSearchResponse"
        "400":
          description: |
            Invalid rank profile or malformed JSON. Both handler and extractor
            errors use the canonical JSON envelope.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
        "415":
          description: Request body is missing the application/json content type.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }
        "422":
          description: JSON body has the wrong shape or omits a required field.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }
        "500": { $ref: "#/components/responses/InternalError" }
        "501":
          description: |
            The server was constructed without RankServices — NOT the
            default deployment (the default server wires the pipeline
            and returns 200s); reachable in embedded/test constructions.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  # ── Hybrid search (free-form passthrough). ──────────────────────────────
  /api/v2/hybrid/search:
    post:
      tags: [Hybrid]
      operationId: hybridSearch
      summary: BM25 + vector hybrid (fusion) search.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [collection]
              additionalProperties: true
              properties:
                collection: { type: string }
                vector: { type: array, items: { type: number } }
                text_query: { type: string }
                top_k: { type: integer }
                fusion_strategy: { type: string }
      responses:
        "200":
          description: Fused results.
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        "400": { $ref: "#/components/responses/BadRequest" }
  /api/v2/hybrid/index:
    post:
      tags: [Hybrid]
      operationId: hybridIndex
      summary: Index documents for BM25 full-text search.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [collection, documents]
              additionalProperties: true
      responses:
        "200":
          description: Index result.
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
  # ── Document collections (TD-SPECRAT-1 wave 9 — real shapes; the
  # pre-wave-9 free-form entries were reworked to the handler DTOs).
  # Handlers: crates/platform/proximadb-api/src/rest/canonical/
  # document.rs, mounted at /api/v2/document-collections. Errors carry
  # the canonical envelope (RestError → {error:{type,message,code}}).
  # No per-route permission gate. Extractor rejections are plain text.
  # create_index is an HONEST always-400 (indexes on existing
  # collections are unsupported — set them at creation); it fails
  # loudly, so it is exposable with that limitation documented.
  /api/v2/document-collections:
    post:
      tags: [Documents]
      operationId: createDocumentCollection
      summary: Create a document collection (with optional indexes).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDocumentCollectionRequest"
      responses:
        "200":
          description: Creation ack (success + collection).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocCreateCollectionResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "500": { $ref: "#/components/responses/InternalError" }
    get:
      tags: [Documents]
      operationId: listDocumentCollections
      summary: List document collections.
      responses:
        "200":
          description: Collections (an object carrying the array).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocCollectionListResponse"
        "500": { $ref: "#/components/responses/InternalError" }
  /api/v2/document-collections/{collection}:
    parameters:
      - name: collection
        in: path
        required: true
        schema: { type: string }
    get:
      tags: [Documents]
      operationId: getDocumentCollection
      summary: Get one collection's info.
      description: |
        404 when the collection does not exist. The body is an open
        object (the serialized collection info).
      responses:
        "200":
          description: Collection info.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocOpenObject"
        "404": { $ref: "#/components/responses/NotFound" }
        "500": { $ref: "#/components/responses/InternalError" }
    delete:
      tags: [Documents]
      operationId: deleteDocumentCollection
      summary: Delete a document collection.
      responses:
        "200":
          description: Deletion acknowledged (success flag).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocDeleteAck"
        "500": { $ref: "#/components/responses/InternalError" }
  /api/v2/document-collections/{collection}/documents:
    parameters:
      - name: collection
        in: path
        required: true
        schema: { type: string }
    post:
      tags: [Documents]
      operationId: insertDocument
      summary: Insert one document.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocInsertRequest"
      responses:
        "200":
          description: The stored document (with its version).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "500": { $ref: "#/components/responses/InternalError" }
    get:
      tags: [Documents]
      operationId: queryDocuments
      summary: Query documents (filter/projection/limit).
      parameters:
        - name: filter
          in: query
          required: false
          schema: { type: string }
          description: Filter expression (server-parsed).
        - name: projection
          in: query
          required: false
          schema: { type: string }
          description: Comma-separated field list.
        - name: limit
          in: query
          required: false
          schema: { type: integer, format: uint32 }
          description: Defaults to 100.
      responses:
        "200":
          description: Matching documents.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocQueryResponse"
        "400":
          description: |
            Invalid filter JSON (canonical envelope) — the only 400
            this op can emit; malformed query strings are axum plain
            text.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500": { $ref: "#/components/responses/InternalError" }
  /api/v2/document-collections/{collection}/documents/{id}:
    parameters:
      - name: collection
        in: path
        required: true
        schema: { type: string }
      - name: id
        in: path
        required: true
        schema: { type: string }
    get:
      tags: [Documents]
      operationId: getDocument
      summary: Get one document.
      parameters:
        - name: projection
          in: query
          required: false
          schema: { type: string }
          description: Comma-separated field list.
      responses:
        "200":
          description: The document.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocResponse"
        "404": { $ref: "#/components/responses/NotFound" }
        "500": { $ref: "#/components/responses/InternalError" }
    patch:
      tags: [Documents]
      operationId: updateDocument
      summary: Apply an update pipeline to a document.
      description: |
        Optimistic concurrency: `expected_version` rejects stale writes.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocUpdateRequest"
      responses:
        "200":
          description: Update outcome (success, id, new_version).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocUpdateResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "500": { $ref: "#/components/responses/InternalError" }
    delete:
      tags: [Documents]
      operationId: deleteDocument
      summary: Delete one document.
      responses:
        "200":
          description: Deletion acknowledged (success + id).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocDeleteAck"
        "500": { $ref: "#/components/responses/InternalError" }
  /api/v2/document-collections/{collection}/documents/batch:
    parameters:
      - name: collection
        in: path
        required: true
        schema: { type: string }
    post:
      tags: [Documents]
      operationId: batchInsertDocuments
      summary: Insert multiple documents (per-item partial success).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocBatchInsertRequest"
      responses:
        "200":
          description: Counts (inserted + failed; failures counted, not enumerated).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocBatchInsertResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "500": { $ref: "#/components/responses/InternalError" }
  /api/v2/document-collections/{collection}/documents/aggregate:
    parameters:
      - name: collection
        in: path
        required: true
        schema: { type: string }
    post:
      tags: [Documents]
      operationId: aggregateDocuments
      summary: Run an aggregation pipeline.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocAggregateRequest"
      responses:
        "200":
          description: Aggregation results (free-form) + timing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocAggregateResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "500": { $ref: "#/components/responses/InternalError" }
  /api/v2/document-collections/{collection}/indexes:
    parameters:
      - name: collection
        in: path
        required: true
        schema: { type: string }
    post:
      tags: [Documents]
      operationId: createDocumentIndex
      summary: Create an index on an existing collection.
      description: |
        HONEST ALWAYS-400: creating indexes on existing collections is
        not supported — specify `indexes` when creating the collection.
        The 400 body says exactly this.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocIndexDefinition"
      responses:
        "400": { $ref: "#/components/responses/BadRequest" }
    get:
      tags: [Documents]
      operationId: listDocumentIndexes
      summary: List the collection's indexes.
      responses:
        "200":
          description: Index definitions (name, path, unique).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocIndexListResponse"
        "404": { $ref: "#/components/responses/NotFound" }
        "500": { $ref: "#/components/responses/InternalError" }
  # ── Observability (free-form passthrough). ──────────────────────────────
  /api/v2/observability/namespaces/{namespace}/logs:
    parameters:
      - name: namespace
        in: path
        required: true
        schema: { type: string }
    post:
      tags: [Observability]
      operationId: ingestLog
      summary: Ingest a log entry.
      description: |
        Scope: path-namespace = isolation boundary (storage tenant key);
        X-Tenant-ID not consulted (TD-SPECRAT-2). Availability: mounted
        unconditionally on the unified server (the default, port 5678);
        legacy multi-port mode mounts it only with gRPC enabled; cluster-mode
        REST does not mount it.
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: true }
      responses:
        "200": { description: Ingested., content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        "500":
          description: Storage/processing failure (maps to internal_error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v2/observability/namespaces/{namespace}/logs/search:
    parameters:
      - name: namespace
        in: path
        required: true
        schema: { type: string }
    post:
      tags: [Observability]
      operationId: queryLogs
      summary: Query logs.
      description: |
        Reads are scoped by the path namespace — the same boundary the other
        observability ops use (any authenticated caller may query any
        namespace; TD-SPECRAT-2 tracks the ownership decision). Availability:
        mounted unconditionally on the unified server (the default, port
        5678); legacy multi-port mode mounts it only with gRPC enabled;
        cluster-mode REST does not mount it.
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: true }
      responses:
        "200": { description: Matching logs., content: { application/json: { schema: { type: object, additionalProperties: true } } } }
        "500":
          description: Storage/processing failure (maps to internal_error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v2/observability/namespaces:
    post:
      tags: [Observability]
      operationId: createObservabilityNamespace
      summary: Create an observability namespace.
      description: |
        Creates a namespace for log/metric/trace data with tiered retention.
        Retention days default to hot=1, warm=7, cold=30; archive is fixed at
        365 days server-side.

        Scope: the path namespace IS the isolation boundary — it maps 1:1 onto
        the storage tenant key. The request's X-Tenant-ID header is not
        consulted by this surface (TD-SPECRAT-2 tracks multi-tenant namespace
        ownership). Availability: mounted unconditionally on the unified
        server (the default, port 5678); legacy multi-port mode mounts it only
        with gRPC enabled; cluster-mode REST does not mount it.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateObservabilityNamespaceRequest"
      responses:
        "200":
          description: Namespace created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateObservabilityNamespaceResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "500":
          description: Storage/processing failure (maps to internal_error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v2/observability/namespaces/{namespace}/logs/bulk:
    parameters:
      - name: namespace
        in: path
        required: true
        schema: { type: string }
    post:
      tags: [Observability]
      operationId: ingestLogs
      summary: Ingest a batch of log entries.
      description: |
        Scope: path-namespace = isolation boundary (storage tenant key);
        X-Tenant-ID not consulted (TD-SPECRAT-2). Availability: mounted
        unconditionally on the unified server (the default, port 5678);
        legacy multi-port mode mounts it only with gRPC enabled; cluster-mode
        REST does not mount it.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkLogIngestRequest"
      responses:
        "200":
          description: Ingestion result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ObservabilityIngestResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "500":
          description: Storage/processing failure (maps to internal_error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v2/observability/namespaces/{namespace}/metrics:
    parameters:
      - name: namespace
        in: path
        required: true
        schema: { type: string }
    post:
      tags: [Observability]
      operationId: ingestMetric
      summary: Ingest a single metric sample.
      description: |
        Scope: path-namespace = isolation boundary (storage tenant key);
        X-Tenant-ID not consulted (TD-SPECRAT-2). Availability: mounted
        unconditionally on the unified server (the default, port 5678);
        legacy multi-port mode mounts it only with gRPC enabled; cluster-mode
        REST does not mount it.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetricSampleInput"
      responses:
        "200":
          description: Ingestion result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ObservabilityIngestResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "500":
          description: Storage/processing failure (maps to internal_error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v2/observability/namespaces/{namespace}/metrics/bulk:
    parameters:
      - name: namespace
        in: path
        required: true
        schema: { type: string }
    post:
      tags: [Observability]
      operationId: ingestMetrics
      summary: Ingest a batch of metric samples.
      description: |
        Scope: path-namespace = isolation boundary (storage tenant key);
        X-Tenant-ID not consulted (TD-SPECRAT-2). Availability: mounted
        unconditionally on the unified server (the default, port 5678);
        legacy multi-port mode mounts it only with gRPC enabled; cluster-mode
        REST does not mount it.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkMetricIngestRequest"
      responses:
        "200":
          description: Ingestion result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ObservabilityIngestResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "500":
          description: Storage/processing failure (maps to internal_error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v2/observability/namespaces/{namespace}/metrics/aggregate:
    parameters:
      - name: namespace
        in: path
        required: true
        schema: { type: string }
    post:
      tags: [Observability]
      operationId: aggregateMetrics
      summary: Aggregate a metric over a time range.
      description: |
        Aggregation defaults to `avg` with a 60-second step. Results are
        grouped by `group_by` label names and filtered by exact `labels` match.

        Scope: path-namespace = isolation boundary (storage tenant key);
        X-Tenant-ID not consulted (TD-SPECRAT-2). Availability: mounted
        unconditionally on the unified server (the default, port 5678);
        legacy multi-port mode mounts it only with gRPC enabled; cluster-mode
        REST does not mount it.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetricAggregationInput"
      responses:
        "200":
          description: Aggregated time series.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetricAggregationResult"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "500":
          description: Storage/processing failure (maps to internal_error).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v2/nl/translate:
    post:
      tags: [Query]
      operationId: translateNaturalLanguage
      summary: Translate natural language to a query (AV-SQL).
      description: |
        Runs the three-agent AV-SQL flow (rewrite → view generation → compose)
        over the configured LLM (`config.llm`) and returns the normalized
        query, the agent-selected views, and the final composed query string.
        Requires the LLM integration to be configured; the result is
        model-generated — validate before execution.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NlTranslateRequest"
      responses:
        "200":
          description: Translation result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NlTranslateResult"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "500":
          description: LLM unavailable or translation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  # TD-SPECRAT-1 wave 3 (TD-ABAC control-plane / ADR-090 grants / TD-SEC-2
  # posture — src/network/rest/canonical/abac_admin.rs). Every operation
  # below:
  #   * is an OPERATOR (cluster-scope SystemAdmin/ConfigureSystem) surface,
  #     never the calling tenant's own data-plane scope;
  #   * is registered ONLY in builds compiled with `--features abac-policy`
  #     (default OFF, TD-FOUNDATION-3). A default build does not mount these
  #     routes at all — an unmatched path returns a generic axum 404, not
  #     the typed AbacOperatorErrorResponse documented below;
  #   * returns AbacOperatorErrorResponse (a FLAT {error, message, code}
  #     shape distinct from the canonical nested ErrorResponse envelope —
  #     this is the handler's real wire format, carried verbatim, not
  #     normalized into the canonical shape);
  #   * resolves tenant path segments from a STRING to the durable stable
  #     u64 server-side at write/read time (never accepts a raw stable id).
  /api/v2/abac/policy-bindings/{tenant}/{object_id}:
    parameters:
      - name: tenant
        in: path
        required: true
        schema: { type: string }
      - name: object_id
        in: path
        required: true
        schema: { type: integer, format: uint64 }
    put:
      tags: [ABAC]
      operationId: putPolicyBinding
      summary: Upsert an ABAC policy binding.
      description: |
        Provisions (or replaces) the policy binding at `object_id` for
        `tenant` — a constraint-layer rule (`scope` + `effect`, optionally a
        `predicate_ref` row filter and a `field_mask`). Writes through the
        same store the live enforcer reads: visible on the next request, no
        restart. `Deny` wins during resolution; the default is deny.
        The optional `X-Tenant-ID` header is not consulted by this
        handler — the tenant path segment governs.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AbacPutPolicyBindingRequest"
      responses:
        "200":
          description: The stored binding (tenant string resolved to its stable id).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AbacPolicyBinding"
        "401": { $ref: "#/components/responses/AbacUnauthorized" }
        "403": { $ref: "#/components/responses/AbacForbidden" }
        "422": { $ref: "#/components/responses/AbacTenantUnresolved" }
        "503": { $ref: "#/components/responses/AbacUnavailable" }
    delete:
      tags: [ABAC]
      operationId: deletePolicyBinding
      summary: Remove an ABAC policy binding.
      description: |
        Idempotent — 204 whether or not a binding existed at `object_id`.
        The optional `X-Tenant-ID` header is not consulted by this
        handler — the tenant path segment governs.
      responses:
        "204": { description: Binding removed (or already absent). }
        "401": { $ref: "#/components/responses/AbacUnauthorized" }
        "403": { $ref: "#/components/responses/AbacForbidden" }
        "422": { $ref: "#/components/responses/AbacTenantUnresolved" }
        "503": { $ref: "#/components/responses/AbacUnavailable" }
  /api/v2/abac/policy-bindings/{tenant}:
    parameters:
      - name: tenant
        in: path
        required: true
        schema: { type: string }
    get:
      tags: [ABAC]
      operationId: listPolicyBindings
      summary: List a tenant's live ABAC policy bindings.
      description: |
        The exact binding set the enforcer composes for reads under
        `tenant` — an operator inspection endpoint, not a per-tenant
        self-service one.
        The optional `X-Tenant-ID` header is not consulted by this
        handler — the tenant path segment governs.
      responses:
        "200":
          description: The tenant's policy bindings.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AbacPolicyBindingsResponse"
        "401": { $ref: "#/components/responses/AbacUnauthorized" }
        "403": { $ref: "#/components/responses/AbacForbidden" }
        "422": { $ref: "#/components/responses/AbacTenantUnresolved" }
        "503": { $ref: "#/components/responses/AbacUnavailable" }
  /api/v2/abac/tenant-posture/{tenant}:
    parameters:
      - name: tenant
        in: path
        required: true
        schema: { type: string }
    get:
      tags: [ABAC]
      operationId: getTenantPosture
      summary: Get a tenant's explicit ABAC grant-enforcement posture.
      description: |
        404 means the tenant has NO explicit record — meaningfully
        different from `Off`, since an absent record means the tenant
        inherits the process default, which may itself be `Enforce`.
        The optional `X-Tenant-ID` header is not consulted by this
        handler — the tenant path segment governs.
      responses:
        "200":
          description: The tenant's explicit posture.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AbacTenantSecurityPosture"
        "401": { $ref: "#/components/responses/AbacUnauthorized" }
        "403": { $ref: "#/components/responses/AbacForbidden" }
        "404":
          description: No explicit posture record (tenant inherits the process default).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AbacOperatorErrorResponse"
        "422": { $ref: "#/components/responses/AbacTenantUnresolved" }
        "503": { $ref: "#/components/responses/AbacUnavailable" }
    put:
      tags: [ABAC]
      operationId: putTenantPosture
      summary: Set a tenant's ABAC grant-enforcement posture.
      description: |
        Intended rollout: `Off` -> `Audit` (watch would-be-denials fall to
        zero for this tenant) -> `Enforce`. Skipping the `Audit` rehearsal
        is how an operator breaks a customer's traffic.
        The optional `X-Tenant-ID` header is not consulted by this
        handler — the tenant path segment governs.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AbacPutTenantPostureRequest"
      responses:
        "200":
          description: The tenant's updated posture.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AbacTenantSecurityPosture"
        "401": { $ref: "#/components/responses/AbacUnauthorized" }
        "403": { $ref: "#/components/responses/AbacForbidden" }
        "422": { $ref: "#/components/responses/AbacTenantUnresolved" }
        "503": { $ref: "#/components/responses/AbacUnavailable" }
  /api/v2/abac/grants:
    post:
      tags: [ABAC]
      operationId: postGrant
      summary: Provision an ADR-090 grant (cross-tenant entitlement).
      description: |
        Grants are the entitlement layer: "this grantee may perform these
        actions on this resource", where the grantee MAY be a foreign
        tenant or a foreign tenant's user — the one place cross-tenant
        sharing is expressible. Both the owner and the grantee tenant must
        resolve (fail-closed on unminted tenants); the grantee subject is
        deliberately NOT validated against the principal registry, so a
        share may be provisioned before its recipient's first login.
        The optional `X-Tenant-ID` header is not consulted by this
        handler — the body `tenant` field governs.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AbacPostGrantRequest"
      responses:
        "200":
          description: The provisioned grant id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AbacPostGrantResponse"
        "401": { $ref: "#/components/responses/AbacUnauthorized" }
        "403": { $ref: "#/components/responses/AbacForbidden" }
        "422": { $ref: "#/components/responses/AbacTenantUnresolved" }
        "503": { $ref: "#/components/responses/AbacUnavailable" }
  /api/v2/abac/grants/{owner_tenant}:
    parameters:
      - name: owner_tenant
        in: path
        required: true
        schema: { type: string }
    get:
      tags: [ABAC]
      operationId: listGrants
      summary: List an owner tenant's grants.
      description: |
        Revoked grants stay listed with `revoked_at_ms` set — the audit
        trail is the point, not a live-only view.
        The optional `X-Tenant-ID` header is not consulted by this
        handler — the tenant path segment governs.
      responses:
        "200":
          description: The owner's grants (including revoked/expired ones).
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AbacGrantRecord"
        "401": { $ref: "#/components/responses/AbacUnauthorized" }
        "403": { $ref: "#/components/responses/AbacForbidden" }
        "422": { $ref: "#/components/responses/AbacTenantUnresolved" }
        "503": { $ref: "#/components/responses/AbacUnavailable" }
  /api/v2/abac/grants/{owner_tenant}/{grant_id}:
    parameters:
      - name: owner_tenant
        in: path
        required: true
        schema: { type: string }
      - name: grant_id
        in: path
        required: true
        schema: { type: string }
    delete:
      tags: [ABAC]
      operationId: deleteGrant
      summary: Revoke a grant.
      description: |
        Idempotent — 204 whether the grant existed (and was revoked) or was
        already unknown/revoked. Revocation under the wrong owner cannot
        even name the grant (the store is owner-partitioned).
        The optional `X-Tenant-ID` header is not consulted by this
        handler — the tenant path segment governs.
      responses:
        "204": { description: Grant revoked (or already unknown/revoked). }
        "401": { $ref: "#/components/responses/AbacUnauthorized" }
        "403": { $ref: "#/components/responses/AbacForbidden" }
        "422": { $ref: "#/components/responses/AbacTenantUnresolved" }
        "503": { $ref: "#/components/responses/AbacUnavailable" }
  /api/v2/abac/attribute-bindings:
    get:
      tags: [ABAC]
      operationId: listAttributeBindings
      summary: List every ABAC attribute binding.
      description: |
        Cluster-operator scope — every `(subject, tenant)` attribute
        binding across every tenant, by design (cross-tenant visibility is
        the point of an operator inspection endpoint).
        The optional `X-Tenant-ID` header is not consulted by this
        handler — this is a cross-tenant operator view.
      responses:
        "200":
          description: Every attribute binding.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AbacAttributeBindingsResponse"
        "401": { $ref: "#/components/responses/AbacUnauthorized" }
        "403": { $ref: "#/components/responses/AbacForbidden" }
        "503": { $ref: "#/components/responses/AbacUnavailable" }
    post:
      tags: [ABAC]
      operationId: postAttributeBinding
      summary: Upsert a subject's ABAC attribute binding.
      description: |
        The authority half of ABAC — a `(subject, tenant)`-keyed,
        multi-valued attribute set a policy's `predicate_ref` resolves
        against. Writes through the same store the live enforcer reads.
        The optional `X-Tenant-ID` header is not consulted by this
        handler — the body `tenant` field governs.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AbacPostAttributeBindingRequest"
      responses:
        "200":
          description: The stored attribute binding.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AbacAttributeBinding"
        "401": { $ref: "#/components/responses/AbacUnauthorized" }
        "403": { $ref: "#/components/responses/AbacForbidden" }
        "422": { $ref: "#/components/responses/AbacTenantUnresolved" }
        "503": { $ref: "#/components/responses/AbacUnavailable" }
  /api/v2/abac/predicate-objects:
    get:
      tags: [ABAC]
      operationId: listPredicateObjects
      summary: List every registered ABAC predicate object.
      description: |
        The optional `X-Tenant-ID` header is not consulted by this
        handler (predicate objects are global, not tenant-scoped).
      responses:
        "200":
          description: Every predicate object.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AbacPredicateObjectsResponse"
        "401": { $ref: "#/components/responses/AbacUnauthorized" }
        "403": { $ref: "#/components/responses/AbacForbidden" }
        "503": { $ref: "#/components/responses/AbacUnavailable" }
  /api/v2/abac/predicate-objects/{object_id}:
    parameters:
      - name: object_id
        in: path
        required: true
        schema: { type: integer, format: uint64 }
    get:
      tags: [ABAC]
      operationId: getPredicateObject
      summary: Get one ABAC predicate object.
      description: |
        404 if unknown. Note a dangling `predicate_ref` on a policy binding
        resolves fail-closed (safe) regardless of this endpoint.
        The optional `X-Tenant-ID` header is not consulted by this
        handler (predicate objects are global, not tenant-scoped).
      responses:
        "200":
          description: The predicate object.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AbacPredicateObjectResponse"
        "401": { $ref: "#/components/responses/AbacUnauthorized" }
        "403": { $ref: "#/components/responses/AbacForbidden" }
        "404":
          description: No predicate object registered under this object_id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AbacOperatorErrorResponse"
        "503": { $ref: "#/components/responses/AbacUnavailable" }
    put:
      tags: [ABAC]
      operationId: putPredicateObject
      summary: Register (or replace) an ABAC predicate object.
      description: |
        The request body IS the `FilterExpression` directly (no wrapper) —
        a stored row-filter predicate that a policy binding's
        `predicate_ref` can reference.
        The optional `X-Tenant-ID` header is not consulted by this
        handler (predicate objects are global, not tenant-scoped).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AbacFilterExpression"
      responses:
        "200":
          description: The registered predicate object.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AbacPredicateObjectResponse"
        "401": { $ref: "#/components/responses/AbacUnauthorized" }
        "403": { $ref: "#/components/responses/AbacForbidden" }
        "503": { $ref: "#/components/responses/AbacUnavailable" }
    delete:
      tags: [ABAC]
      operationId: deletePredicateObject
      summary: Revoke an ABAC predicate object.
      description: |
        Idempotent (204). Subsequent resolves of `object_id` by any policy
        binding fail-closed.
        The optional `X-Tenant-ID` header is not consulted by this
        handler (predicate objects are global, not tenant-scoped).
      responses:
        "204": { description: Predicate object revoked (or already absent). }
        "401": { $ref: "#/components/responses/AbacUnauthorized" }
        "403": { $ref: "#/components/responses/AbacForbidden" }
        "503": { $ref: "#/components/responses/AbacUnavailable" }
  # TD-SPECRAT-1 wave 4 — collections-admin operator surfaces
  # (src/network/rest/canonical/{pinning,affinity,primary_pod}.rs,
  # mounted unconditionally in handlers.rs). Notes:
  #   * pinning + affinity carry NO per-route permission gate — and the
  #     auth MIDDLEWARE itself only attaches when REST auth is enabled
  #     + a security coordinator exists (the shipped default config
  #     disables it): in an auth-disabled deployment these six
  #     cluster-wide, cross-tenant, state-mutating operations are
  #     reachable UNAUTHENTICATED. primary-pod IS operator-gated
  #     (SystemAdmin/ConfigureSystem) in each handler — and in an
  #     auth-disabled deployment every primary-pod call 401s
  #     (missing_auth_context), because the gate has no context.
  #   * pinning/affinity registries are collection-keyed and
  #     cross-tenant by construction (no tenant scoping); primary-pod
  #     is (tenant_id, collection_id)-scoped via the path.
  #   * plain-text error bodies on pinning/affinity (axum (StatusCode,
  #     String)); JSON flat error on primary-pod 401/403.
  /api/v2/collections/{collection_id}/pin:
    parameters:
      - name: collection_id
        in: path
        required: true
        schema: { type: string }
    patch:
      tags: [Collections]
      operationId: patchCollectionPin
      summary: Set or clear a collection pin.
      description: |
        Pins a collection to a physical medium (or unpins it). Returns
        immediately with the new pin state; physical data movement
        happens out of band — the access-pattern engine reads the
        registry on its next evaluation.
        The optional `X-Tenant-ID` header is not consulted by this
        handler (the pin registry is collection-keyed, not
        tenant-scoped).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PinRequest"
      responses:
        "200":
          description: The resulting pin state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PinResponse"
        "400":
          description: |
            `pinned: true` without `target`. The body is PLAIN TEXT
            (axum `(StatusCode, String)` error path), not JSON.
          content:
            text/plain:
              schema: { type: string }
    get:
      tags: [Collections]
      operationId: getCollectionPin
      summary: Read a collection's pin state.
      description: |
        200 with `status: "pinned"` + state when pinned; 200 with
        `status: "unpinned"` when not pinned.
        The optional `X-Tenant-ID` header is not consulted by this
        handler (the pin registry is collection-keyed, not
        tenant-scoped).
      responses:
        "200":
          description: Current pin state (pinned or unpinned).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PinResponse"
  /api/v2/collections/pinning:
    get:
      tags: [Collections]
      operationId: listCollectionPins
      summary: List every pinned collection.
      description: |
        Operator-dashboard view of all currently pinned collections
        (cross-tenant — the registry is collection-keyed).
        The optional `X-Tenant-ID` header is not consulted by this
        handler.
      responses:
        "200":
          description: All pin entries.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PinListResponse"
  /api/v2/collections/{collection_id}/affinity:
    parameters:
      - name: collection_id
        in: path
        required: true
        schema: { type: string }
    get:
      tags: [Collections]
      operationId: getCollectionAffinity
      summary: Read this node's cache-affinity entry for a collection.
      description: |
        Returns `status: "affinitized"` (with query count, age, and a
        `stale` flag) when an entry exists — including stale entries,
        so operators can see which collections went cold — and
        `status: "not_affinitized"` when there is no entry at all.
        Routing has already stopped using stale entries.
        The optional `X-Tenant-ID` header is not consulted by this
        handler (the affinity registry is per-node, collection-keyed).
      responses:
        "200":
          description: The affinity entry (or its absence).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AffinityResponse"
    delete:
      tags: [Collections]
      operationId: deleteCollectionAffinity
      summary: Drop this node's cache-affinity entry for a collection.
      description: |
        Invalidates the affinity hint so routing re-evaluates on the
        next query. 200 always; `dropped` distinguishes an actual
        removal from a no-op.
        The optional `X-Tenant-ID` header is not consulted by this
        handler.
      responses:
        "200":
          description: Drop outcome.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AffinityDeleteResponse"
  /api/v2/collections/affinity:
    get:
      tags: [Collections]
      operationId: listCollectionAffinities
      summary: List every cache-affinity entry on this node.
      description: |
        Sorted by collection_id; includes stale entries so operators
        can see which collections went cold.
        The optional `X-Tenant-ID` header is not consulted by this
        handler.
      responses:
        "200":
          description: All affinity entries on this node.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AffinityListResponse"
  /api/v2/primary-pod/{tenant_id}/{collection_id}:
    parameters:
      - name: tenant_id
        in: path
        required: true
        schema: { type: string }
      - name: collection_id
        in: path
        required: true
        schema: { type: string }
    get:
      tags: [Collections]
      operationId: getPrimaryPod
      summary: Look up a (tenant, collection)'s primary pod.
      description: |
        200 with `status: "bound"` + the assignment when one exists;
        200 with `status: "unbound"` when none does (never 404).
        The optional `X-Tenant-ID` header is not consulted by this
        handler — the `tenant_id` PATH segment governs.
        Requires REST auth enabled — in an auth-disabled
        deployment every call to this operation returns 401
        `missing_auth_context`.
      responses:
        "200":
          description: The binding (bound or unbound).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrimaryPodLookupResponse"
        "401": { $ref: "#/components/responses/PodUnauthorized" }
        "403": { $ref: "#/components/responses/PodForbidden" }
    put:
      tags: [Collections]
      operationId: putPrimaryPod
      summary: Assign a (tenant, collection)'s primary pod.
      description: |
        Operator-gated (SystemAdmin ∪ ConfigureSystem) — these expose
        cross-tenant placement and drive WAL write routing. The
        assignment is mirrored to the catalog; a mirror failure is
        logged and counted but does NOT fail the request (PUT-success
        means the binding is in effect for routing; the catalog
        reconciles on the next write).
        The optional `X-Tenant-ID` header is not consulted by this
        handler — the `tenant_id` PATH segment governs.
        Requires REST auth enabled — in an auth-disabled
        deployment every call to this operation returns 401
        `missing_auth_context`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrimaryPodAssignRequest"
      responses:
        "200":
          description: The assignment (with the previous binding when re-assigning).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrimaryPodAssignResponse"
        "401": { $ref: "#/components/responses/PodUnauthorized" }
        "403": { $ref: "#/components/responses/PodForbidden" }
    delete:
      tags: [Collections]
      operationId: deletePrimaryPod
      summary: Unassign a (tenant, collection)'s primary pod.
      description: |
        200 always; `removed` distinguishes an actual removal from a
        no-op. Catalog-mirror policy is the same as PUT (logged, not
        fatal).
        The optional `X-Tenant-ID` header is not consulted by this
        handler — the `tenant_id` PATH segment governs.
        Requires REST auth enabled — in an auth-disabled
        deployment every call to this operation returns 401
        `missing_auth_context`.
      responses:
        "200":
          description: Unassign outcome.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrimaryPodUnassignResponse"
        "401": { $ref: "#/components/responses/PodUnauthorized" }
        "403": { $ref: "#/components/responses/PodForbidden" }
  /api/v2/primary-pod:
    get:
      tags: [Collections]
      operationId: listPrimaryPods
      summary: List every primary-pod binding (all tenants).
      description: |
        Cross-tenant operator view — the registry is keyed by
        (tenant_id, collection_id).
        The optional `X-Tenant-ID` header is not consulted by this
        handler.
        Requires REST auth enabled — in an auth-disabled
        deployment every call to this operation returns 401
        `missing_auth_context`.
      responses:
        "200":
          description: All bindings.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrimaryPodListResponse"
        "401": { $ref: "#/components/responses/PodUnauthorized" }
        "403": { $ref: "#/components/responses/PodForbidden" }
  # TD-SPECRAT-1 wave 5 — catalog routing/explain operator surface
  # (handlers.rs: get_catalog_table_routing / explain_table_write_route;
  # mounted unconditionally, NO per-route permission gate — the global
  # auth middleware applies when REST auth is enabled). The enterprise
  # external-catalog surface (/api/v2/catalogs/*, enterprise-catalogs
  # feature gate) is a separate area, explicitly deferred (TD).
  /api/v2/catalog/table-routing:
    get:
      tags: [Catalog]
      operationId: getCatalogTableRouting
      summary: Table-level xCatalog routing metadata.
      description: |
        Serves `information_schema.table_routing` as a table-shaped
        result — the same projection pgwire/SQL clients see. All cell
        values are strings.
        The optional `X-Tenant-ID` header is not consulted by this
        handler (the introspection projection is cluster-scope).
        The filter dispatch is LEXICAL over the composed SQL text, so a
        `table_name` containing SQL fragments (e.g. ` from
        xcatalog.namespaces`) can be misread as a different view — do
        not treat this endpoint as a structured query interface
        (TD-SPECRAT-3 tracks the structured-filter fix). Names match
        case-insensitively; a name containing a single quote silently
        truncates the filter at the quote.
      parameters:
        - name: table_name
          in: query
          required: false
          schema: { type: string }
          description: Filter to one table (case-insensitive name match).
      responses:
        "200":
          description: Routing rows (columns/column_types/rows, all strings).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CatalogIntrospectionResult"
        "500": { $ref: "#/components/responses/InternalError" }
  /api/v2/catalog/table-write/explain:
    post:
      tags: [Catalog]
      operationId: explainTableWriteRoute
      summary: Explain table-write route selection WITHOUT executing.
      description: |
        Runs the DML write planner in explain-only mode: resolves the
        target (and source) tables, plans the copy/write, and returns
        the full route decision — selected backend + access method,
        write lane (+ rejected lanes with reasons), candidate/rejected
        paths, estimated cost and data movement, required guards, and
        the write intent summary. Nothing is written.
        Provide EITHER `source_table` OR `source_sql` (400 otherwise).
        Extractor rejections (malformed JSON body, duplicate query
        params) bypass the JSON error envelope and return axum's
        plain-text 400/415/422 bodies — the typed envelope below covers
        handler-emitted errors only.
        The optional `X-Tenant-ID` header is not consulted — tenant
        context, if any, rides the body's `tenant_id`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TableWriteExplainRequest"
      responses:
        "200":
          description: The route explanation (no write executed).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TableWriteRouteExplanation"
        "400": { $ref: "#/components/responses/BadRequest" }
        "404": { $ref: "#/components/responses/NotFound" }
        "500": { $ref: "#/components/responses/InternalError" }
components:
  parameters:
    GraphId:
      name: graph_id
      in: path
      required: true
      schema:
        type: string
        minLength: 1
    NodeId:
      name: node_id
      in: path
      required: true
      schema:
        type: string
        minLength: 1
    EdgeId:
      name: edge_id
      in: path
      required: true
      schema:
        type: string
        minLength: 1
  responses:
    BadRequest:
      description: Invalid request.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: Resource not found.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Unauthorized:
      description: Authentication required or insufficient permissions.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    AbacUnauthorized:
      description: |
        Missing auth context (401) — the request reached the handler with
        no resolved principal, indicating the auth middleware itself is
        misconfigured for this deployment (`error: "missing_auth_context"`).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/AbacOperatorErrorResponse"
    AbacForbidden:
      description: |
        Authenticated but lacks `SystemAdmin` or `ConfigureSystem`
        (`error: "operator_permission_required"`) — this is a
        cluster-scope operator surface; a tenant's own admin permissions
        do not satisfy it.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/AbacOperatorErrorResponse"
    AbacTenantUnresolved:
      description: |
        The tenant path/body segment has no durable stable id and the
        configured catalog could not mint one (`error:
        "tenant_unresolved"`).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/AbacOperatorErrorResponse"
    AbacUnavailable:
      description: |
        ABAC is compiled in (`abac-policy`) but no durable store is open
        for this process (no `data_dir`), or the underlying store returned
        an error (`error: "abac_unavailable"` / `"grant_store_error"` /
        `"posture_store_error"` / `"tenant_mint_failed"`).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/AbacOperatorErrorResponse"
    PodUnauthorized:
      description: |
        Missing auth context (401) — the request reached the handler
        with no resolved principal (`error: "missing_auth_context"`).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/PrimaryPodOperatorErrorResponse"
    PodForbidden:
      description: |
        Authenticated but lacks `SystemAdmin` or `ConfigureSystem`
        (`error: "operator_permission_required"`) — a cluster-scope
        operator gate.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/PrimaryPodOperatorErrorResponse"
    InternalError:
      description: Internal server error (canonical error envelope).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    GraphBadRequest:
      description: |
        Invalid argument — the FULL graph envelope with success=false
        and error.code INVALID_ARGUMENT (a genuine handler-emitted JSON
        400 exists only on updateGraphSchema; axum extractor rejections
        — malformed JSON, wrong content-type, missing required field —
        are 400/415/422 with PLAIN-TEXT bodies and no envelope).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/GraphErrorResponse"
    GraphNotFound:
      description: |
        Not found — the FULL graph envelope with success=false and
        error.code NOT_FOUND.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/GraphErrorResponse"
    GraphInternal:
      description: |
        Internal error — the FULL graph envelope with success=false and
        error.code INTERNAL_ERROR.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/GraphErrorResponse"
  schemas:
    HealthResponse:
      type: object
      additionalProperties: true
      properties:
        status:
          type: string
        version:
          type: string
        uptime_seconds:
          type: number
    ProbeResponse:
      type: object
      required: [status]
      properties:
        status:
          type: string
    CapabilitiesResponse:
      type: object
      additionalProperties: true
      properties:
        api_version:
          type: string
        surface:
          type: string
        features:
          type: array
          items:
            type: string
        limits:
          type: object
          additionalProperties: true
        error_envelope:
          type: object
          additionalProperties: true
    # ── Graph schemas (v2). Reconciled against the proximadb-api graph
    # handler; server is authoritative for the wire contract. ───────────
    # ── Graph surface schemas (wave 6: envelope-corrected). ──────────────
    # The graph handlers all return {success, data?, error?, metadata?};
    # each Graph*Response below is that envelope around its payload type.
    GraphErrorBody:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
          enum: [NOT_FOUND, ALREADY_EXISTS, INVALID_ARGUMENT, CONSTRAINT_VIOLATION, INTERNAL_ERROR, TIMEOUT, PERMISSION_DENIED]
        message: { type: string }
        details: {}
    GraphErrorResponse:
      type: object
      required: [success, error]
      description: |
        The graph envelope in its error form — what every graph error
        status actually carries on the wire ({success: false, error:
        {code, message, details?}, metadata?}; `data` is absent.
      properties:
        success:
          type: boolean
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphResponseMetadata:
      type: object
      properties:
        request_id: { type: string }
        execution_time_ms:
          type: integer
          format: uint64
    GraphEmbedding:
      type: object
      required: [model_id, model_version, vector, dimension]
      properties:
        model_id: { type: string }
        model_version: { type: string }
        vector:
          type: array
          items: { type: number }
        dimension:
          type: integer
          format: uint32
    CanonicalNode:
      type: object
      required: [id, labels, properties, created_at, updated_at]
      properties:
        id: { type: string }
        labels:
          type: array
          items: { type: string }
        properties:
          type: object
          additionalProperties: true
        embedding: { $ref: "#/components/schemas/GraphEmbedding" }
        created_at: { type: string }
        updated_at: { type: string }
    CanonicalEdge:
      type: object
      required: [id, from_node_id, to_node_id, edge_type, properties, created_at, updated_at]
      properties:
        id: { type: string }
        from_node_id: { type: string }
        to_node_id: { type: string }
        edge_type: { type: string }
        properties:
          type: object
          additionalProperties: true
        weight: { type: number }
        created_at: { type: string }
        updated_at: { type: string }
    GraphNodeResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        data: { $ref: "#/components/schemas/CanonicalNode" }
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphEdgeResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        data: { $ref: "#/components/schemas/CanonicalEdge" }
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphNodeListResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        data:
          type: array
          items: { $ref: "#/components/schemas/CanonicalNode" }
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphCollectionResponse:
      type: object
      required: [success]
      description: |
        Envelope around the serialized graph-collection object. The
        payload is the port's collection record rendered as JSON — an
        open object (the field set is the proto collection's, subject to
        port evolution).
      properties:
        success: { type: boolean }
        data:
          type: object
          additionalProperties: true
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphCollectionListResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        data:
          type: array
          items:
            type: object
            additionalProperties: true
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphTraversalStats:
      type: object
      required: [nodes_visited, edges_traversed, max_depth_reached]
      properties:
        nodes_visited:
          type: integer
          format: uint64
        edges_traversed:
          type: integer
          format: uint64
        max_depth_reached:
          type: integer
          format: uint32
        execution_time_ms:
          type: integer
          format: uint64
    GraphTraversalData:
      type: object
      required: [nodes, edges]
      properties:
        nodes:
          type: array
          items: { $ref: "#/components/schemas/CanonicalNode" }
        edges:
          type: array
          items: { $ref: "#/components/schemas/CanonicalEdge" }
        paths:
          type: array
          items:
            type: array
            items: { type: string }
        stats: { $ref: "#/components/schemas/GraphTraversalStats" }
    GraphTraversalResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        data: { $ref: "#/components/schemas/GraphTraversalData" }
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphStats:
      type: object
      required:
        - total_nodes
        - total_edges
        - label_stats
        - edge_type_stats
        - total_properties
        - memory_usage_bytes
        - average_degree
        - max_degree
        - connected_components
      properties:
        total_nodes:
          type: integer
          format: uint64
        total_edges:
          type: integer
          format: uint64
        label_stats:
          type: array
          items: {}
        edge_type_stats:
          type: array
          items: {}
        total_properties:
          type: integer
          format: uint64
        memory_usage_bytes:
          type: integer
          format: uint64
        average_degree: { type: number }
        max_degree:
          type: integer
          format: uint32
        connected_components:
          type: integer
          format: uint32
    GraphStatsResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        data: { $ref: "#/components/schemas/GraphStats" }
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphNodeBatchResults:
      type: object
      required: [created_count, updated_count, failed_count, results, errors]
      properties:
        created_count:
          type: integer
          format: uint64
        updated_count:
          type: integer
          format: uint64
        failed_count:
          type: integer
          format: uint64
        results:
          type: array
          items: { $ref: "#/components/schemas/CanonicalNode" }
        errors:
          type: array
          items: {}
    GraphEdgeBatchResults:
      type: object
      required: [created_count, updated_count, failed_count, results, errors]
      properties:
        created_count:
          type: integer
          format: uint64
        updated_count:
          type: integer
          format: uint64
        failed_count:
          type: integer
          format: uint64
        results:
          type: array
          items: { $ref: "#/components/schemas/CanonicalEdge" }
        errors:
          type: array
          items: {}
    GraphBatchNodesResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        data: { $ref: "#/components/schemas/GraphNodeBatchResults" }
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphBatchEdgesResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        data: { $ref: "#/components/schemas/GraphEdgeBatchResults" }
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphNodeQueryResults:
      type: object
      required: [items, has_more]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/CanonicalNode" }
        total_count:
          type: integer
          format: uint64
        has_more: { type: boolean }
        next_token: { type: string }
    GraphEdgeQueryResults:
      type: object
      required: [items, has_more]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/CanonicalEdge" }
        total_count:
          type: integer
          format: uint64
        has_more: { type: boolean }
        next_token: { type: string }
    GraphNodeQueryResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        data: { $ref: "#/components/schemas/GraphNodeQueryResults" }
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphEdgeQueryResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        data: { $ref: "#/components/schemas/GraphEdgeQueryResults" }
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphShortestPathData:
      type: object
      required: [path, found]
      properties:
        path:
          type: array
          items: { type: string }
        total_weight: { type: number }
        found: { type: boolean }
    GraphShortestPathResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        data: { $ref: "#/components/schemas/GraphShortestPathData" }
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphQueryData:
      type: object
      required: [rows, row_count]
      properties:
        rows:
          type: array
          items: {}
        row_count:
          type: integer
          format: uint64
    GraphQueryResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        data: { $ref: "#/components/schemas/GraphQueryData" }
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphComponentsData:
      type: object
      required: [components]
      properties:
        components:
          type: array
          items:
            type: array
            items: { type: string }
    GraphComponentsResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        data: { $ref: "#/components/schemas/GraphComponentsData" }
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphCyclesData:
      type: object
      required: [has_cycle]
      properties:
        has_cycle: { type: boolean }
    GraphCyclesResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        data: { $ref: "#/components/schemas/GraphCyclesData" }
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    GraphDdlData:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
    GraphDdlResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        data: { $ref: "#/components/schemas/GraphDdlData" }
        error: { $ref: "#/components/schemas/GraphErrorBody" }
        metadata: { $ref: "#/components/schemas/GraphResponseMetadata" }
    CreateGraphRequest:
      type: object
      required: [graph_id]
      properties:
        graph_id: { type: string }
        name: { type: string }
        description: { type: string }
    UpdateGraphSchemaRequest:
      type: object
      required: [schema]
      properties:
        schema:
          type: object
          additionalProperties: true
    NodeInput:
      type: object
      required: [id]
      properties:
        id: { type: string }
        labels:
          type: array
          items: { type: string }
        properties:
          type: object
          additionalProperties: true
        embedding:
          type: object
          required: [vector]
          properties:
            vector:
              type: array
              items: { type: number }
            version: { type: string }
            model_id: { type: string }
    CreateNodeRequest:
      type: object
      required: [node]
      properties:
        node: { $ref: "#/components/schemas/NodeInput" }
    EdgeInput:
      type: object
      required: [id, from_node_id, to_node_id, edge_type]
      properties:
        id: { type: string }
        from_node_id: { type: string }
        to_node_id: { type: string }
        edge_type: { type: string }
        properties:
          type: object
          additionalProperties: true
        weight: { type: number }
    CreateEdgeRequest:
      type: object
      required: [edge]
      properties:
        edge: { $ref: "#/components/schemas/EdgeInput" }
    BatchCreateNodesRequest:
      type: object
      required: [nodes]
      properties:
        nodes:
          type: array
          items: { $ref: "#/components/schemas/NodeInput" }
    BatchCreateEdgesRequest:
      type: object
      required: [edges]
      properties:
        edges:
          type: array
          items: { $ref: "#/components/schemas/EdgeInput" }
    TraverseRequest:
      type: object
      required: [start_node_id]
      properties:
        start_node_id: { type: string }
        max_depth:
          type: integer
          format: uint32
          default: 5
        edge_types:
          type: array
          items: { type: string }
        node_labels:
          type: array
          items: { type: string }
        algorithm:
          type: string
          default: bfs
        limit:
          type: [integer, "null"]
          format: uint32
    WalkRequest:
      type: object
      required: [start_node_id]
      properties:
        start_node_id: { type: string }
        max_depth:
          type: integer
          format: uint32
          default: 2
        limit:
          type: integer
          format: uint32
          default: 100
    WalkStepRequest:
      type: object
      required: [node_id]
      properties:
        node_id: { type: string }
        edge_type: { type: string }
        limit:
          type: integer
          format: uint32
          default: 50
          description: Accepted but currently unused server-side.
    ShortestPathRequest:
      type: object
      required: [start_node_id, target_node_id]
      properties:
        start_node_id: { type: string }
        target_node_id: { type: string }
        max_depth:
          type: [integer, "null"]
          format: uint32
        edge_types:
          type: array
          items: { type: string }
        algorithm: { type: string }
        k:
          type: [integer, "null"]
          format: uint32
    GraphNodeQuery:
      type: object
      properties:
        labels:
          type: array
          items: { type: string }
        properties:
          type: object
          additionalProperties: true
        limit:
          type: integer
          format: uint32
          default: 100
        offset:
          type: [integer, "null"]
          format: uint32
        continuation_token:
          type: [string, "null"]
          description: '"offset:<n>" form; decodes to the offset.'
    GraphEdgeQuery:
      type: object
      properties:
        edge_type:
          type: string
          default: ""
        from_node_id: { type: string }
        to_node_id: { type: string }
        properties:
          type: object
          additionalProperties: true
        limit:
          type: integer
          format: uint32
          default: 100
        offset:
          type: [integer, "null"]
          format: uint32
        continuation_token:
          type: [string, "null"]
          description: '"offset:<n>" form; decodes to the offset.'
    UniqueConstraintRequest:
      type: object
      required: [label, property]
      properties:
        label: { type: string }
        property: { type: string }
    GraphQueryRequest:
      type: object
      required: [query]
      properties:
        query: { type: string }
        language:
          type: string
          default: native
          description: Accepted but currently unused server-side.
        timeout_ms:
          type: [integer, "null"]
          format: uint32
          description: Accepted but currently unused server-side.

    # ── Observability (wave-1 spec rationalization: TD-SPECRAT-1) ──────────────
    CreateObservabilityNamespaceRequest:
      type: object
      required: [name]
      description: |
        Body for `POST /api/v2/observability/namespaces`. Retention days
        default to hot=1, warm=7, cold=30.
      properties:
        name:
          type: string
          minLength: 1
        hot_retention_days:
          type: integer
          format: uint32
          default: 1
        warm_retention_days:
          type: integer
          format: uint32
          default: 7
        cold_retention_days:
          type: integer
          format: uint32
          default: 30
    CreateObservabilityNamespaceResponse:
      type: object
      required: [success, namespace]
      properties:
        success:
          type: boolean
        namespace:
          type: string
    LogEntryInput:
      type: object
      required: [message]
      description: |
        A single log entry for ingestion. `severity` defaults to `info`;
        `fields` carries arbitrary structured attributes.
      properties:
        timestamp_ns:
          type: integer
          format: int64
          nullable: true
        message:
          type: string
        severity:
          type: string
          default: info
          description: "Canonical: trace, debug, info, warn, error, fatal. Aliases accepted (case-insensitive): verbose->trace, information->info, warning->warn, err->error, critical->fatal. Unknown values fall back to info."
        source:
          type: string
          nullable: true
        service:
          type: string
          nullable: true
        fields:
          type: object
          additionalProperties: true
    BulkLogIngestRequest:
      type: object
      required: [logs]
      properties:
        logs:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/LogEntryInput"
    MetricSampleInput:
      type: object
      required: [name, value]
      properties:
        name:
          type: string
          minLength: 1
        value:
          type: number
          format: double
        timestamp_ns:
          type: integer
          format: int64
          nullable: true
          description: Defaults to server receive time.
        labels:
          type: object
          additionalProperties: { type: string }
    BulkMetricIngestRequest:
      type: object
      required: [metrics]
      properties:
        metrics:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/MetricSampleInput"
    ObservabilityIngestResponse:
      type: object
      required: [ingested, failed, success]
      properties:
        ingested:
          type: integer
          format: uint64
        failed:
          type: integer
          format: uint64
        success:
          type: boolean
    MetricAggregationInput:
      type: object
      required: [metric_name, start_time_ns, end_time_ns]
      properties:
        metric_name:
          type: string
          minLength: 1
        start_time_ns:
          type: integer
          format: int64
        end_time_ns:
          type: integer
          format: int64
        aggregation:
          type: string
          default: avg
          description: "Canonical: min, max, avg, sum, count. Percentile/rate forms also accepted: p50, p90, p95, p99, rate. Unknown values fall back to avg."
        step_seconds:
          type: integer
          format: uint32
          default: 60
        group_by:
          type: array
          items: { type: string }
        labels:
          type: object
          additionalProperties: { type: string }
    MetricDataPoint:
      type: object
      required: [timestamp_ns, value]
      properties:
        timestamp_ns:
          type: integer
          format: int64
        value:
          type: number
          format: double
    MetricTimeSeries:
      type: object
      required: [labels, points]
      properties:
        labels:
          type: object
          additionalProperties: { type: string }
        points:
          type: array
          items:
            $ref: "#/components/schemas/MetricDataPoint"
    MetricAggregationResult:
      type: object
      required: [series, query_time_ms]
      properties:
        series:
          type: array
          items:
            $ref: "#/components/schemas/MetricTimeSeries"
        query_time_ms:
          type: integer
          format: uint64
    NlTranslateRequest:
      type: object
      required: [query]
      description: |
        Body for `POST /api/v2/nl/translate`.
      properties:
        query:
          type: string
          minLength: 1
    NlTranslateResult:
      type: object
      required: [normalized_query, views, final_query]
      description: |
        Model-generated translation output — the final query string is not
        validated SQL/AQL; validate before executing it.
      properties:
        normalized_query:
          type: string
        views:
          type: array
          items: { type: string }
        final_query:
          type: string
    AbacOperatorErrorResponse:
      type: object
      required: [error, message, code]
      description: |
        The FLAT error shape every ABAC operator endpoint returns —
        `{error, message, code}` where `error` is a short machine-readable
        slug (e.g. `"tenant_unresolved"`). This is deliberately DISTINCT
        from the canonical nested `ErrorResponse` (`{error: {type,
        message, code}}`) used elsewhere in this spec; it is the actual
        wire format of `abac_admin::OperatorErrorResponse`, carried
        verbatim rather than normalized.
      properties:
        error:
          type: string
          description: Machine-readable error slug.
        message:
          type: string
        code:
          type: integer
          description: The HTTP status code, repeated in the body.
    AbacScope:
      description: |
        The catalog container a policy/grant governs — a stable-id-keyed,
        externally-tagged Rust enum. Exactly one of `Namespace`, `Table`,
        or `Column` is present.
      oneOf:
        - type: object
          required: [Namespace]
          properties:
            Namespace: { type: integer, minimum: 0, maximum: 65535 }
        - type: object
          required: [Table]
          properties:
            Table: { type: integer, format: uint32 }
        - type: object
          required: [Column]
          properties:
            Column: { $ref: "#/components/schemas/AbacColumnScope" }
    AbacColumnScope:
      type: object
      required: [table, column]
      description: The `Column` variant payload of [AbacScope](#/components/schemas/AbacScope).
      properties:
        table: { type: integer, format: uint32 }
        column: { type: integer, format: uint32 }
    AbacEffect:
      type: string
      enum: [Permit, Deny]
      description: Permit or deny; deny wins during policy resolution.
    AbacFieldMask:
      type: string
      enum: ["Null", Redact, Forbid]
      description: |
        Column-level masking. `Forbid` rejects the read (existence must
        not leak); `Null`/`Redact` are projection rewrites.
    AbacAttrValue:
      description: |
        An ABAC subject attribute value — an externally-tagged Rust enum;
        exactly one of `Str`, `Int`, `Bool`, or `List` is present.
      oneOf:
        - type: object
          required: [Str]
          properties: { Str: { type: string } }
        - type: object
          required: [Int]
          properties: { Int: { type: integer, format: int64 } }
        - type: object
          required: [Bool]
          properties: { Bool: { type: boolean } }
        - type: object
          required: [List]
          properties:
            List: { type: array, items: { type: string } }
    AbacComparisonOperator:
      type: string
      enum:
        - Equals
        - NotEquals
        - GreaterThan
        - GreaterThanOrEqual
        - LessThan
        - LessThanOrEqual
        - In
        - NotIn
        - Contains
        - StartsWith
        - EndsWith
        - Between
        - IsNull
        - IsNotNull
        - Like
    AbacFilterExpression:
      type: object
      additionalProperties: true
      description: |
        A predicate-object row filter — a RECURSIVE, externally-tagged Rust
        enum (`proximadb_filter_expression::FilterExpression`). Modeled
        here as free-form JSON (rather than a typed `oneOf`) because the
        self-referential union is not representable in a form the SDK
        codegen tooling can process (openapi-python-client cannot resolve
        a `oneOf` branch whose `items` `$ref`s the union being defined);
        this is the one schema in the ABAC surface where the generated
        client falls back to an untyped object. Construct exactly one of:

        * `{"Comparison": {"field": <string>, "operator": <one of
          "Equals","NotEquals","GreaterThan","GreaterThanOrEqual",
          "LessThan","LessThanOrEqual","In","NotIn","Contains",
          "StartsWith","EndsWith","Between","IsNull","IsNotNull","Like">,
          "value": <any JSON value>}}`
        * `{"And": [<AbacFilterExpression>, ...]}`
        * `{"Or": [<AbacFilterExpression>, ...]}`
        * `{"Not": <AbacFilterExpression>}`

        See [AbacComparisonOperator](#/components/schemas/AbacComparisonOperator)
        for the authoritative operator enum (referenced here for docs only;
        not structurally, for the same recursion reason).
    AbacPutPolicyBindingRequest:
      type: object
      required: [scope, effect]
      description: Body for `PUT /api/v2/abac/policy-bindings/{tenant}/{object_id}`.
      properties:
        scope: { $ref: "#/components/schemas/AbacScope" }
        effect: { $ref: "#/components/schemas/AbacEffect" }
        predicate_ref:
          type: [integer, "null"]
          format: uint64
          description: |
            The predicate object this binding carries (a row-level rule).
            Omit for a predicate-free table-level grant. A dangling ref
            resolves fail-closed at read time.
        field_mask:
          oneOf:
            - $ref: "#/components/schemas/AbacFieldMask"
            - type: "null"
    AbacPolicyBinding:
      type: object
      required: [object_id, tenant_stable_id, scope, effect]
      properties:
        object_id: { type: integer, format: uint64 }
        tenant_stable_id: { type: integer, format: uint64 }
        scope: { $ref: "#/components/schemas/AbacScope" }
        effect: { $ref: "#/components/schemas/AbacEffect" }
        predicate_ref:
          type: [integer, "null"]
          format: uint64
        field_mask:
          oneOf:
            - $ref: "#/components/schemas/AbacFieldMask"
            - type: "null"
    AbacPolicyBindingsResponse:
      type: object
      required: [tenant, tenant_stable_id, count, bindings]
      properties:
        tenant: { type: string }
        tenant_stable_id: { type: integer, format: uint64 }
        count: { type: integer, format: uint64 }
        bindings:
          type: array
          items: { $ref: "#/components/schemas/AbacPolicyBinding" }
    AbacPostAttributeBindingRequest:
      type: object
      required: [subject_id, tenant, attrs]
      description: Body for `POST /api/v2/abac/attribute-bindings`.
      properties:
        subject_id: { type: string }
        tenant:
          type: string
          description: Tenant display name; resolved to the stable u64 server-side.
        attrs:
          type: object
          additionalProperties: { $ref: "#/components/schemas/AbacAttrValue" }
    AbacAttributeBinding:
      type: object
      required: [subject_id, tenant_stable_id, attrs]
      properties:
        subject_id: { type: string }
        tenant_stable_id: { type: integer, format: uint64 }
        attrs:
          type: object
          additionalProperties: { $ref: "#/components/schemas/AbacAttrValue" }
    AbacAttributeBindingsResponse:
      type: object
      required: [count, bindings]
      properties:
        count: { type: integer, format: uint64 }
        bindings:
          type: array
          items: { $ref: "#/components/schemas/AbacAttributeBinding" }
    AbacPredicateObjectResponse:
      type: object
      required: [object_id, expression]
      properties:
        object_id: { type: integer, format: uint64 }
        expression: { $ref: "#/components/schemas/AbacFilterExpression" }
    AbacPredicateObjectsResponse:
      type: object
      required: [count, objects]
      properties:
        count: { type: integer, format: uint64 }
        objects:
          type: array
          items: { $ref: "#/components/schemas/AbacPredicateObjectResponse" }
    AbacGrantee:
      description: |
        Who a grant admits — an externally-tagged Rust enum. Exactly one
        of `Tenant` (every subject of that tenant) or `User` (one specific
        subject, possibly of a foreign tenant) is present.
      oneOf:
        - type: object
          required: [Tenant]
          properties:
            Tenant: { type: integer, format: uint64 }
        - type: object
          required: [User]
          properties:
            User: { $ref: "#/components/schemas/AbacGranteeUser" }
    AbacGranteeUser:
      type: object
      required: [tenant_stable_id, subject]
      description: The `User` variant payload of [AbacGrantee](#/components/schemas/AbacGrantee).
      properties:
        tenant_stable_id: { type: integer, format: uint64 }
        subject: { type: string }
    AbacGrantAction:
      type: string
      enum: [Read, Write, Ddl, Grant]
      description: |
        `Grant` is delegation authority (further re-grant); it is not
        consulted for data access, only by the admin surface.
    AbacGrantGranteeRequest:
      type: object
      required: [tenant]
      description: |
        Grantee as provisioned over the wire: a tenant-wide share when
        `subject` is omitted/empty, else one user of (possibly foreign)
        `tenant`. Clients supply tenant/subject strings; stable ids are
        resolved server-side.
      properties:
        tenant: { type: string }
        subject:
          type: [string, "null"]
    AbacPostGrantRequest:
      type: object
      required: [owner_tenant, resource, grantee, actions]
      description: Body for `POST /api/v2/abac/grants`.
      properties:
        owner_tenant:
          type: string
          description: The resource-owner tenant (string; resolved and must be minted).
        resource: { $ref: "#/components/schemas/AbacScope" }
        grantee: { $ref: "#/components/schemas/AbacGrantGranteeRequest" }
        actions:
          type: array
          items: { $ref: "#/components/schemas/AbacGrantAction" }
          uniqueItems: true
        predicate_ref:
          type: [integer, "null"]
          format: uint64
        field_mask:
          oneOf:
            - $ref: "#/components/schemas/AbacFieldMask"
            - type: "null"
        expires_at_ms:
          type: [integer, "null"]
          format: int64
    AbacPostGrantResponse:
      type: object
      required: [grant_id, owner_tenant_stable_id]
      properties:
        grant_id: { type: string }
        owner_tenant_stable_id: { type: integer, format: uint64 }
    AbacGrantRecord:
      type: object
      required:
        - grant_id
        - owner_tenant_stable_id
        - resource
        - grantee
        - actions
        - created_at_ms
      properties:
        grant_id: { type: string }
        owner_tenant_stable_id: { type: integer, format: uint64 }
        resource: { $ref: "#/components/schemas/AbacScope" }
        grantee: { $ref: "#/components/schemas/AbacGrantee" }
        actions:
          type: array
          items: { $ref: "#/components/schemas/AbacGrantAction" }
        predicate_ref:
          type: [integer, "null"]
          format: uint64
        field_mask:
          oneOf:
            - $ref: "#/components/schemas/AbacFieldMask"
            - type: "null"
        created_at_ms: { type: integer, format: int64 }
        expires_at_ms:
          type: [integer, "null"]
          format: int64
        revoked_at_ms:
          type: [integer, "null"]
          format: int64
          description: Set once revoked; the record stays listed (audit trail).
    AbacGrantEnforcement:
      type: string
      enum: ["Off", Audit, Enforce]
      description: |
        Rollout is intended `Off` -> `Audit` (would-be-denials logged, read
        still admitted) -> `Enforce` (no applicable grant denies).
    AbacPutTenantPostureRequest:
      type: object
      required: [grant_enforcement]
      properties:
        grant_enforcement: { $ref: "#/components/schemas/AbacGrantEnforcement" }
    AbacTenantSecurityPosture:
      type: object
      required: [tenant_stable_id, grant_enforcement, updated_at_ms]
      properties:
        tenant_stable_id: { type: integer, format: uint64 }
        grant_enforcement: { $ref: "#/components/schemas/AbacGrantEnforcement" }
        updated_at_ms: { type: integer, format: int64 }
    PinTarget:
      type: string
      enum: [memory, nvme_ssd, cloud]
      description: |
        Physical medium to pin to. `cloud` effectively means "do not
        promote this collection" (an explicit unpin-in-spirit).
    PinRequest:
      type: object
      required: [pinned]
      description: |
        Body for `PATCH /api/v2/collections/{collection_id}/pin`. Either
        `{pinned: true, target: ..., replicas?: ...}` or `{pinned: false}`
        (target/replicas ignored when unpinning; `target` is REQUIRED
        when pinning — a plain-text 400 otherwise). `replicas` defaults
        to 1.
      properties:
        pinned: { type: boolean }
        target:
          oneOf:
            - $ref: "#/components/schemas/PinTarget"
            - type: "null"
        replicas:
          type: [integer, "null"]
          format: uint32
    PinResponse:
      type: object
      required: [status, collection_id]
      description: |
        Internally tagged by `status` (snake_case). `status: "pinned"`
        carries `target`/`replicas`/`pinned_at_ns`; `status: "unpinned"`
        carries `was_pinned` (true when the request removed an existing
        pin — useful for audit logs; false on a no-op or read of an
        unpinned collection).
      properties:
        status:
          type: string
          enum: [pinned, unpinned]
        collection_id: { type: string }
        target: { $ref: "#/components/schemas/PinTarget" }
        replicas:
          type: integer
          format: uint32
        pinned_at_ns:
          type: integer
          format: int64
        was_pinned: { type: boolean }
    PinListItem:
      type: object
      required: [collection_id, target, replicas, pinned_at_ns]
      properties:
        collection_id: { type: string }
        target: { $ref: "#/components/schemas/PinTarget" }
        replicas: { type: integer, format: uint32 }
        pinned_at_ns: { type: integer, format: int64 }
    PinListResponse:
      type: object
      required: [count, items]
      properties:
        count: { type: integer, format: uint64 }
        items:
          type: array
          items: { $ref: "#/components/schemas/PinListItem" }
    AffinityResponse:
      type: object
      required: [status, collection_id]
      description: |
        Internally tagged by `status` (snake_case). `status:
        "affinitized"` carries the entry — including STALE entries
        (routing already ignores them; `stale: true` flags them for the
        operator) — with `query_count` (monotonic queries served while
        this node held affinity) and `age_seconds` (seconds since the
        last recorded query). `status: "not_affinitized"` means no
        entry exists at all.
      properties:
        status:
          type: string
          enum: [affinitized, not_affinitized]
        collection_id: { type: string }
        node_id: { type: string }
        query_count:
          type: integer
          format: uint64
        age_seconds:
          type: integer
          format: uint64
        stale: { type: boolean }
    AffinityDeleteResponse:
      type: object
      required: [status, collection_id, dropped]
      description: |
        `status` is always `"dropped"`; the `dropped` flag
        distinguishes an actual removal from a no-op.
      properties:
        status:
          type: string
          enum: [dropped]
        collection_id: { type: string }
        dropped: { type: boolean }
    AffinityListItem:
      type: object
      required: [collection_id, node_id, query_count, age_seconds, stale]
      properties:
        collection_id: { type: string }
        node_id: { type: string }
        query_count: { type: integer, format: uint64 }
        age_seconds: { type: integer, format: uint64 }
        stale: { type: boolean }
    AffinityListResponse:
      type: object
      required: [count, items]
      properties:
        count: { type: integer, format: uint64 }
        items:
          type: array
          items: { $ref: "#/components/schemas/AffinityListItem" }
    AssignmentReason:
      type: string
      enum: [create, operator, failover, rebalance, catalog_replay]
      description: |
        Why an assignment was made (locked vocabulary for dashboards /
        EXPLAIN). `catalog_replay` = loaded from durable state at
        restart, not freshly assigned.
    PrimaryPod:
      type: object
      required: [pod, assigned_at_ns, reason]
      properties:
        pod:
          type: string
          description: Pod identifier (typically a k8s pod name); opaque.
        assigned_at_ns:
          type: integer
          format: int64
          description: Wall-clock nanoseconds when last set; reassignments advance it.
        reason: { $ref: "#/components/schemas/AssignmentReason" }
    PrimaryPodLookupResponse:
      type: object
      required: [status, tenant_id, collection_id]
      description: |
        Internally tagged by `status` (snake_case). `status: "bound"`
        carries `primary`; `status: "unbound"` means no binding (never
        a 404).
      properties:
        status:
          type: string
          enum: [bound, unbound]
        tenant_id: { type: string }
        collection_id: { type: string }
        primary: { $ref: "#/components/schemas/PrimaryPod" }
    PrimaryPodAssignRequest:
      type: object
      required: [pod]
      description: |
        Body for `PUT /api/v2/primary-pod/{tenant_id}/{collection_id}`.
        `reason` defaults to `"operator"` when omitted.
      properties:
        pod: { type: string }
        reason: { $ref: "#/components/schemas/AssignmentReason" }
    PrimaryPodAssignResponse:
      type: object
      required: [tenant_id, collection_id, primary]
      properties:
        tenant_id: { type: string }
        collection_id: { type: string }
        primary: { $ref: "#/components/schemas/PrimaryPod" }
        previous:
          oneOf:
            - $ref: "#/components/schemas/PrimaryPod"
            - type: "null"
          description: The prior binding on re-assignment; null on first assignment.
    PrimaryPodUnassignResponse:
      type: object
      required: [tenant_id, collection_id, removed]
      properties:
        tenant_id: { type: string }
        collection_id: { type: string }
        removed:
          type: boolean
          description: True when a binding was actually removed; false when nothing was bound.
    PrimaryPodListItem:
      type: object
      required: [tenant_id, collection_id, primary]
      properties:
        tenant_id: { type: string }
        collection_id: { type: string }
        primary: { $ref: "#/components/schemas/PrimaryPod" }
    PrimaryPodListResponse:
      type: object
      required: [count, items]
      properties:
        count: { type: integer, format: uint64 }
        items:
          type: array
          items: { $ref: "#/components/schemas/PrimaryPodListItem" }
    PrimaryPodOperatorErrorResponse:
      type: object
      required: [error, message, code]
      description: |
        The FLAT error shape the primary-pod operator endpoints return
        (`{error, message, code}`, `error` a short machine-readable
        slug). Same convention as `AbacOperatorErrorResponse`; carried
        as its own schema because the two handler families define the
        type independently.
      properties:
        error: { type: string }
        message: { type: string }
        code: { type: integer }
    CatalogIntrospectionResult:
      type: object
      required: [columns, column_types, rows]
      description: |
        Table-shaped catalog metadata (the projection pgwire/SQL clients
        see). Every cell value is a string.
      properties:
        columns:
          type: array
          items: { type: string }
        column_types:
          type: array
          items: { type: string }
        rows:
          type: array
          items:
            type: array
            items: { type: string }
    TableWriteExplainRequest:
      type: object
      required: [target_table]
      description: |
        Body for `POST /api/v2/catalog/table-write/explain`. Provide
        EITHER `source_table` OR `source_sql` — both, or neither, is a
        400. `write_mode` (default `append`) accepts, case-insensitive:
        append | insert | insert_only | insert-only | upsert | overwrite
        | insert_overwrite | insert-overwrite | overwrite_table |
        overwrite-table | merge. `distribution` (default `auto`) accepts:
        auto | local | local_only | local-only | pseudo |
        pseudo_distributed | pseudo-distributed | distributed. Upsert/
        merge imply upsert conflict policy; everything else errors on
        conflict.
      properties:
        target_table:
          type: string
          description: Dotted name (`[namespace.]…table`); last segment is the table.
        source_table:
          type: [string, "null"]
        source_sql:
          type: [string, "null"]
        write_mode:
          type: [string, "null"]
        distribution:
          type: [string, "null"]
        target_columns:
          type: [array, "null"]
          items: { type: string }
        tenant_id:
          type: [string, "null"]
        actor:
          type: [string, "null"]
        idempotency_key:
          type: [string, "null"]
        row_count_hint:
          type: [integer, "null"]
          format: uint64
        estimated_bytes:
          type: [integer, "null"]
          format: uint64
        requires_row_level_semantics:
          type: [boolean, "null"]
        batch_local_constraints_sufficient:
          type: [boolean, "null"]
    TableWriteRouteExplanation:
      type: object
      required:
        - target_table
        - source
        - write_mode
        - distribution
        - write_intent
        - write_lane
        - write_lane_reason
        - write_lane_required_guards
        - rejected_write_lanes
        - selected_backend
        - selected_access_method
        - estimated_cost
        - data_movement
        - required_guards
        - route_metadata
        - candidate_paths
        - rejected_paths
      description: |
        The full DML write-planner route decision (explain-only; nothing
        was written). `write_mode`/`distribution` echo the resolved
        canonical enum names (PascalCase, e.g. `Append`,
        `PseudoDistributed`). `execution_*` fields appear only for
        EXPLAIN ANALYZE (never set by this REST explain path).
      properties:
        target_table: { type: string }
        source: { type: string }
        write_mode: { type: string }
        distribution: { type: string }
        write_intent: { $ref: "#/components/schemas/TableWriteIntentExplanation" }
        write_lane: { type: string }
        write_lane_reason: { type: string }
        write_lane_required_guards:
          type: array
          items: { type: string }
        rejected_write_lanes:
          type: array
          items: { $ref: "#/components/schemas/TableWriteRejectedLaneExplanation" }
        selected_backend: { type: string }
        selected_access_method: { type: string }
        estimated_cost: { $ref: "#/components/schemas/TableWriteCostExplanation" }
        data_movement: { $ref: "#/components/schemas/TableWriteDataMovementExplanation" }
        required_guards:
          type: array
          items: { type: string }
        route_metadata: { $ref: "#/components/schemas/TableWriteRouteMetadataExplanation" }
        candidate_paths:
          type: array
          items: { $ref: "#/components/schemas/TableWriteCandidateExplanation" }
        rejected_paths:
          type: array
          items: { $ref: "#/components/schemas/TableWriteRejectedPathExplanation" }
        execution_elapsed_us:
          type: integer
          format: uint64
        execution_rows_written:
          type: integer
          format: uint64
    TableWriteIntentExplanation:
      type: object
      required:
        - target_table
        - operation_kind
        - durability
        - isolation
        - projection_freshness
        - requires_row_level_semantics
        - batch_local_constraints_sufficient
      properties:
        target_table: { type: string }
        operation_kind: { type: string }
        durability: { type: string }
        isolation: { type: string }
        projection_freshness: { type: string }
        tenant_id:
          type: [string, "null"]
        actor:
          type: [string, "null"]
        idempotency_key:
          type: [string, "null"]
        catalog_schema_version:
          type: [integer, "null"]
          format: uint64
        row_count_hint:
          type: [integer, "null"]
          format: uint64
        estimated_bytes:
          type: [integer, "null"]
          format: uint64
        requires_row_level_semantics: { type: boolean }
        batch_local_constraints_sufficient: { type: boolean }
    TableWriteRejectedLaneExplanation:
      type: object
      required: [lane, reason]
      properties:
        lane: { type: string }
        reason: { type: string }
    TableWriteCostExplanation:
      type: object
      required: [relative_cost, reason]
      properties:
        rows:
          type: [integer, "null"]
          format: uint64
        bytes:
          type: [integer, "null"]
          format: uint64
        relative_cost:
          type: number
          description: Relative cost (unitless planner estimate).
        reason: { type: string }
    TableWriteDataMovementExplanation:
      type: object
      required: [estimate_source, stats_freshness]
      properties:
        source_rows:
          type: [integer, "null"]
          format: uint64
        source_bytes:
          type: [integer, "null"]
          format: uint64
        target_rows_before_write:
          type: [integer, "null"]
          format: uint64
        target_bytes_before_write:
          type: [integer, "null"]
          format: uint64
        estimated_read_bytes:
          type: [integer, "null"]
          format: uint64
        estimated_write_bytes:
          type: [integer, "null"]
          format: uint64
        estimated_rewrite_bytes:
          type: [integer, "null"]
          format: uint64
        estimate_source: { type: string }
        source_last_analyzed_ms:
          type: [integer, "null"]
          format: int64
        target_last_analyzed_ms:
          type: [integer, "null"]
          format: int64
        source_stats_age_ms:
          type: [integer, "null"]
          format: uint64
        target_stats_age_ms:
          type: [integer, "null"]
          format: uint64
        freshness_sla_ms:
          type: [integer, "null"]
          format: uint64
        stats_freshness: { type: string }
    TableWriteRouteMetadataExplanation:
      type: object
      required:
        - authority_mode
        - workload_profile
        - storage_specialization
        - projection_metadata
        - policy_boundary
        - constraint_enforcement
        - constraint_gaps
      properties:
        authority_mode: { type: string }
        workload_profile: { type: string }
        storage_specialization: { type: string }
        primary_format:
          type: [string, "null"]
        preferred_compute_route:
          type: [string, "null"]
        partitioning:
          type: [string, "null"]
        isolation_profile:
          type: [string, "null"]
        freshness_sla:
          type: [string, "null"]
        projection_freshness_state:
          type: [string, "null"]
        projection_metadata:
          type: array
          items: { $ref: "#/components/schemas/ProjectionRouteMetadataExplanation" }
        policy_boundary: { type: string }
        constraint_enforcement: { type: string }
        constraint_gaps:
          type: array
          items: { type: string }
    ProjectionRouteMetadataExplanation:
      type: object
      required:
        - name
        - kind
        - physical_format
        - rebuild_source
        - freshness
        - freshness_state
        - rebuildable
        - lossy
        - support_status
      properties:
        name: { type: string }
        kind: { type: string }
        physical_format: { type: string }
        rebuild_source: { type: string }
        freshness: { type: string }
        freshness_state: { type: string }
        max_lag_ms:
          type: [integer, "null"]
          format: int64
        source_range:
          type: [string, "null"]
        last_included_position:
          type: [string, "null"]
        rebuildable: { type: boolean }
        invalidation_policy:
          type: [string, "null"]
        policy_boundary:
          type: [string, "null"]
        lossy: { type: boolean }
        support_status: { type: string }
        benchmark_gate:
          type: [string, "null"]
    TableWriteCandidateExplanation:
      type: object
      required: [backend, access_method, estimated_cost, required_guards, pushdown]
      properties:
        backend: { type: string }
        access_method: { type: string }
        estimated_cost: { $ref: "#/components/schemas/TableWriteCostExplanation" }
        required_guards:
          type: array
          items: { type: string }
        pushdown:
          type: array
          items: { type: string }
    TableWriteRejectedPathExplanation:
      type: object
      required: [backend, access_method, reason, required_guards]
      properties:
        backend: { type: string }
        access_method: { type: string }
        reason: { type: string }
        required_guards:
          type: array
          items: { type: string }
    TsValueColumn:
      type: object
      required: [name]
      properties:
        name: { type: string }
        unit:
          type: [string, "null"]
          description: Serialized as an explicit null when unset.
        aggregation:
          type: [string, "null"]
          description: Serialized as an explicit null when unset.
    TsCollectionConfig:
      type: object
      required: [name]
      properties:
        name: { type: string }
        timestamp_column:
          type: string
          default: timestamp
        value_columns:
          type: array
          items: { $ref: "#/components/schemas/TsValueColumn" }
        tag_columns:
          type: array
          items: { type: string }
        retention_ms:
          type: [integer, "null"]
          format: int64
    TsPoint:
      type: object
      required: [timestamp]
      properties:
        timestamp:
          type: integer
          format: int64
          description: Epoch milliseconds.
        values:
          type: object
          additionalProperties: { type: number, format: double }
        tags:
          type: object
          additionalProperties: { type: string }
    TsCreateResponse:
      type: object
      required: [name]
      properties:
        name: { type: string }
    TsListResponse:
      type: object
      required: [collections]
      properties:
        collections:
          type: array
          items: { $ref: "#/components/schemas/TsCollectionConfig" }
    TsDeleteResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
    TsIngestRequest:
      type: object
      required: [points]
      properties:
        points:
          type: array
          items: { $ref: "#/components/schemas/TsPoint" }
    TsIngestResponse:
      type: object
      required: [ingested]
      properties:
        ingested:
          type: integer
          format: uint64
    TsQueryRequest:
      type: object
      required: [start_time, end_time]
      properties:
        start_time:
          type: integer
          format: int64
        end_time:
          type: integer
          format: int64
        limit:
          type: [integer, "null"]
          format: uint64
    TsQueryResponse:
      type: object
      required: [points]
      properties:
        points:
          type: array
          items: { $ref: "#/components/schemas/TsPoint" }
    TsAggregateRequest:
      type: object
      required: [start_time, end_time]
      properties:
        start_time:
          type: integer
          format: int64
        end_time:
          type: integer
          format: int64
        aggregation:
          type: string
          default: avg
        bucket_ms:
          type: integer
          format: int64
          default: 60000
    TsAggregateResponse:
      type: object
      required: [buckets]
      properties:
        buckets:
          type: array
          items: {}
    RankPhaseOverride:
      type: object
      properties:
        rerank_count:
          type: [integer, "null"]
          format: uint32
        batch_size:
          type: [integer, "null"]
          format: uint32
    RankOverrides:
      type: object
      properties:
        second_phase: { $ref: "#/components/schemas/RankPhaseOverride" }
        global_phase: { $ref: "#/components/schemas/RankPhaseOverride" }
    RankSearchRequest:
      type: object
      required: [collection]
      properties:
        collection: { type: string }
        query_vector:
          type: array
          items:
            type: number
            format: float
          description: Post-embedding query vector (caller-computed).
        query_text:
          type: [string, "null"]
          description: Optional BM25/full-text leg; absent/empty = vector-only.
        k:
          type: integer
          format: uint64
          default: 10
        rank_profile:
          type: [string, "null"]
          description: Named profile; omitted = retrieval-only output.
        rank_overrides: { $ref: "#/components/schemas/RankOverrides" }
    ScoreComponent:
      type: object
      required: [name, value, weight, contribution]
      properties:
        name:
          type: string
          description: "e.g. bm25(title), closeness(embedding), model(rerank-v3)."
        value:
          type: number
          format: double
        weight:
          type: number
          format: double
        contribution:
          type: number
          format: double
          description: Typically value * weight.
    RankScoreVector:
      type: object
      required: [primary, phase]
      properties:
        primary:
          type: number
          format: float
        phase:
          type: integer
          description: JSON-friendly phase ordinal (not the kernel newtype).
        components:
          type: array
          items: { $ref: "#/components/schemas/ScoreComponent" }
    RankScoredHit:
      type: object
      required: [id, score]
      properties:
        id: { type: string }
        score:
          type: number
          format: float
        score_vector: { $ref: "#/components/schemas/RankScoreVector" }
        match_features:
          type: object
          additionalProperties: { type: number, format: double }
        summary_features:
          type: object
          additionalProperties: { type: number, format: double }
    RankSearchResponse:
      type: object
      required: [hits, phase_truncated]
      properties:
        hits:
          type: array
          items: { $ref: "#/components/schemas/RankScoredHit" }
        phase_truncated: { type: boolean }
        rank_profile:
          type: [string, "null"]
        rank_profile_version:
          type: [integer, "null"]
          format: uint32
    DocOpenObject:
      type: object
      additionalProperties: true
      description: Serialized collection info (open field set).
    DocCollectionListResponse:
      type: object
      required: [collections]
      properties:
        collections:
          type: array
          items: { $ref: "#/components/schemas/DocOpenObject" }
    DocCreateCollectionResponse:
      type: object
      required: [success, collection]
      properties:
        success: { type: boolean }
        collection: { $ref: "#/components/schemas/DocOpenObject" }
    DocDeleteAck:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        id:
          type: string
          description: Present on document deletes; absent on collection deletes.
    DocIndexDefinition:
      type: object
      required: [path]
      properties:
        name: { type: [string, "null"] }
        path: { type: string }
        index_type:
          type: string
          default: btree
          description: |
            btree | hash | inverted | fulltext | geo (unknown values
            fall back to btree).
        unique:
          type: boolean
          default: false
        sparse:
          type: boolean
          default: false
    CreateDocumentCollectionRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }
        indexes:
          type: array
          items: { $ref: "#/components/schemas/DocIndexDefinition" }
    DocInsertRequest:
      type: object
      required: [document]
      properties:
        id:
          type: [string, "null"]
          description: Server-assigned when omitted.
        document:
          description: The document body (free-form JSON).
    DocResponse:
      type: object
      required: [id, document, version]
      properties:
        id: { type: string }
        document: {}
        version:
          type: integer
          format: uint64
    DocQueryResponse:
      type: object
      required: [documents, has_more]
      properties:
        documents:
          type: array
          items: { $ref: "#/components/schemas/DocResponse" }
        total_count:
          type: [integer, "null"]
          format: uint64
        has_more: { type: boolean }
    DocUpdateRequest:
      type: object
      required: [updates]
      properties:
        updates:
          type: array
          items: {}
          description: Update pipeline steps ({operation, path, value} objects).
        expected_version:
          type: [integer, "null"]
          format: uint64
          description: Optimistic-concurrency guard; rejects stale writes.
    DocUpdateResponse:
      type: object
      required: [success, id, new_version]
      properties:
        success: { type: boolean }
        id: { type: string }
        new_version:
          type: integer
          format: uint64
    DocBatchInsertRequest:
      type: object
      required: [documents]
      properties:
        documents:
          type: array
          items: { $ref: "#/components/schemas/DocInsertRequest" }
    DocBatchInsertResponse:
      type: object
      required: [inserted, failed]
      properties:
        inserted:
          type: integer
          format: uint64
        failed:
          type: integer
          format: uint64
          description: Count only — per-item failures are not enumerated.
    DocAggregateRequest:
      type: object
      required: [pipeline]
      properties:
        pipeline:
          type: array
          items: {}
          description: Aggregation pipeline stages (free-form).
    DocAggregateResponse:
      type: object
      required: [results, query_time_ms]
      properties:
        results:
          type: array
          items: {}
        query_time_ms:
          type: integer
          format: uint64
    DocIndexListResponse:
      type: object
      required: [indexes]
      properties:
        indexes:
          type: array
          items:
            type: object
            required: [name, path, unique]
            properties:
              name:
                type: [string, "null"]
              path: { type: string }
              unique: { type: boolean }
    UnifiedOpenValue:
      description: Free-form JSON (the port's serde_json::Value rendering).
    UnifiedBareError:
      type: object
      required: [error]
      description: |
        The unified surface's BARE error shape — a plain string under
        `error`, NOT the canonical {error:{type,message,code}} envelope
        other surfaces carry.
      properties:
        error: { type: string }
    UnifiedExecuteRequest:
      type: object
      required: [query]
      properties:
        query: { type: string }
        parameters:
          type: [array, "null"]
          items: {}
        collection: { type: string }
        limit:
          type: [integer, "null"]
          format: uint32
    UnifiedFederatedRequest:
      type: object
      required: [query]
      properties:
        query: { type: string }
        parameters:
          type: [array, "null"]
          items: {}
    UnifiedExplainRequest:
      type: object
      required: [query]
      properties:
        query: { type: string }
        collection: { type: string }
    UnifiedPrepareRequest:
      type: object
      required: [query]
      properties:
        query: { type: string }
        name: { type: string }
        cache_results:
          type: boolean
          default: false
        ttl_seconds:
          type: [integer, "null"]
          format: uint64
    UnifiedPrepareResponse:
      type: object
      required: [statement_id]
      properties:
        statement_id: { type: string }
    UnifiedExecutePreparedRequest:
      type: object
      properties:
        parameters:
          type: [array, "null"]
          items: {}
        collection: { type: string }
    UnifiedPreparedStatsRequest:
      type: object
      properties:
        statement_ids:
          type: array
          items: { type: string }
    UnifiedDistributedRequest:
      type: object
      required: [query]
      description: |
        Like the federated request, plus an optional row limit the impl
        applies to the distributed result.
      properties:
        query: { type: string }
        parameters:
          type: [array, "null"]
          items: {}
        limit:
          type: [integer, "null"]
          format: uint32
