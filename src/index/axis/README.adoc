= AXIS - Adaptive eXtensible Indexing System
:toc: left
:toclevels: 3
:icons: font

[abstract]
AXIS is ProximaDB's intelligent indexing layer providing high-performance vector similarity search through multiple adaptive indexing algorithms. It automatically selects optimal indexes based on data characteristics and query patterns, with zero-downtime migration as workloads evolve.

== Overview

AXIS (Adaptive eXtensible Indexing System) serves as ProximaDB's primary indexing layer for vector similarity search. Unlike traditional static indexing systems, AXIS dynamically adapts to your data and query patterns, automatically selecting and migrating between indexing strategies to maintain optimal performance.

**Key Features**:

* Six specialized indexing algorithms (HNSW, IVF, LSH, Annoy, PQ, Flat)
* Automatic index selection based on collection characteristics
* Zero-downtime migration between index types
* Query pattern analysis and optimization
* Event-driven updates via EventLog integration
* Seamless integration with all storage engines
* Hardware-accelerated distance computation

**Status**: ✅ PRODUCTION-READY

== Architecture

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
    subgraph CLIENT["Query Flow"]
        A[Search Request]
        B[Query Analysis]
        C[Index Selection]
        D[Result Ranking]
    end

    subgraph AXIS["AXIS Core"]
        E[Strategy Selector<br/>Analyze & recommend]
        F[Index Factory<br/>Create indexes]
        G[Migration Engine<br/>Zero-downtime switch]
        H[Monitor<br/>Track performance]
    end

    subgraph INDEXES["Index Algorithms"]
        I[HNSW<br/>High recall graphs]
        J[IVF<br/>Clustered search]
        K[LSH<br/>Approximate hash]
        L[Annoy<br/>Tree-based]
        M[PQ<br/>Compression]
        N[Flat<br/>Exact search]
    end

    subgraph STORAGE["Storage Integration"]
        O[SST Engine]
        P[VIPER Engine]
        Q[SWIFT Engine]
        R[HELIX Engine]
        S[NOVA Engine]
        T[RAPTOR Engine]
    end

    subgraph INTEGRATION["Integration Layer"]
        U[EventLog Consumer<br/>Real-time updates]
        V[Memory Tracker<br/>Resource management]
        W[Tiering Manager<br/>Hot/cold data]
        X[Collection State<br/>Multi-tenant]
    end

    A --> B
    B --> C
    C --> D
    B --> E
    E --> F
    E --> G
    F --> I
    F --> J
    F --> K
    F --> L
    F --> M
    F --> N
    I --> D
    J --> D
    K --> D
    L --> D
    M --> D
    N --> D
    H --> E
    D --> O
    D --> P
    D --> Q
    D --> R
    D --> S
    D --> T
    U --> F
    V --> E
    W --> E
    X --> E

    style A fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#000
    style B fill:#5ba3f5,stroke:#3d7ab8,stroke-width:2px,color:#000
    style C fill:#7db8f7,stroke:#5090d3,stroke-width:2px,color:#000
    style D fill:#93c47d,stroke:#5a8047,stroke-width:2px,color:#000
    style E fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#000
    style F fill:#5ba3f5,stroke:#3d7ab8,stroke-width:2px,color:#000
    style G fill:#7db8f7,stroke:#5090d3,stroke-width:2px,color:#000
    style H fill:#ffd966,stroke:#cc9900,stroke-width:2px,color:#000
    style I fill:#8fc4f9,stroke:#6ba4d8,stroke-width:2px,color:#000
    style J fill:#8fc4f9,stroke:#6ba4d8,stroke-width:2px,color:#000
    style K fill:#8fc4f9,stroke:#6ba4d8,stroke-width:2px,color:#000
    style L fill:#8fc4f9,stroke:#6ba4d8,stroke-width:2px,color:#000
    style M fill:#8fc4f9,stroke:#6ba4d8,stroke-width:2px,color:#000
    style N fill:#8fc4f9,stroke:#6ba4d8,stroke-width:2px,color:#000
    style O fill:#a0d0fa,stroke:#7eb5de,stroke-width:2px,color:#000
    style P fill:#a0d0fa,stroke:#7eb5de,stroke-width:2px,color:#000
    style Q fill:#a0d0fa,stroke:#7eb5de,stroke-width:2px,color:#000
    style R fill:#a0d0fa,stroke:#7eb5de,stroke-width:2px,color:#000
    style S fill:#a0d0fa,stroke:#7eb5de,stroke-width:2px,color:#000
    style T fill:#a0d0fa,stroke:#7eb5de,stroke-width:2px,color:#000
    style U fill:#b0dcfb,stroke:#8fc6e4,stroke-width:2px,color:#000
    style V fill:#b0dcfb,stroke:#8fc6e4,stroke-width:2px,color:#000
    style W fill:#b0dcfb,stroke:#8fc6e4,stroke-width:2px,color:#000
    style X fill:#b0dcfb,stroke:#8fc6e4,stroke-width:2px,color:#000

    style CLIENT fill:#f0f4f8,stroke:#5a5a5a,stroke-width:1px,color:#000
    style AXIS fill:#e8f0fa,stroke:#5a5a5a,stroke-width:1px,color:#000
    style INDEXES fill:#dde9f5,stroke:#5a5a5a,stroke-width:1px,color:#000
    style STORAGE fill:#d2e4f0,stroke:#5a5a5a,stroke-width:1px,color:#000
    style INTEGRATION fill:#c7dfe8,stroke:#5a5a5a,stroke-width:1px,color:#000
----

== Module Structure

[source]
----
src/index/axis/
├── mod.rs                          # Public API and exports
├── types.rs                        # Core type definitions
│
├── indexes/                        # Index Implementations
│   ├── mod.rs                      # Index exports
│   ├── hnsw_index.rs               # Hierarchical Navigable Small World
│   ├── ivf_unified.rs              # Inverted File Index
│   ├── lsh_index.rs                # Locality Sensitive Hashing
│   ├── annoy_index.rs              # Approximate Nearest Neighbors
│   └── global_id_index.rs          # O(1) ID to storage mapping
│
├── management/                     # Index Lifecycle Management
│   ├── mod.rs                      # Management exports
│   ├── strategy.rs                 # Index selection strategy
│   ├── analyzer.rs                 # Collection characteristics analysis
│   ├── monitor.rs                  # Performance monitoring
│   ├── migration_engine.rs         # Zero-downtime migration
│   ├── adaptive_engine.rs          # Adaptive optimization
│   └── manager.rs                  # AXIS orchestration
│
├── storage/                        # Index Persistence
│   ├── mod.rs                      # Storage exports
│   ├── serialization.rs            # Index serialization
│   ├── recovery.rs                 # Index recovery
│   ├── format_strategy.rs          # Format selection
│   ├── universal_index_storage.rs  # Unified storage backend
│   └── ivf_posting_list_storage.rs # IVF-specific storage
│
├── integration/                    # System Integration
│   ├── mod.rs                      # Integration exports
│   ├── eventlog_consumer.rs        # EventLog integration
│   ├── flush_integration.rs        # WAL flush coordination
│   ├── memory_tracker.rs           # Memory management
│   ├── tiering_manager.rs          # Hot/cold tiering
│   └── collection_state.rs         # Multi-tenant state
│
├── eventlog/                       # Event-Driven Updates
│   ├── mod.rs                      # EventLog exports
│   ├── event_log.rs                # Event log implementation
│   ├── event_log_manager.rs        # Event log lifecycle
│   ├── service_adapter.rs          # Service integration
│   └── service_interface.rs        # Service abstraction
│
└── utilities/                      # Shared Utilities
    ├── clustering.rs               # Vector clustering
    ├── pattern_analyzer.rs         # Query pattern analysis
    ├── index_factory.rs            # Index creation factory
    ├── compact_vector.rs           # Memory-efficient vectors
    └── utils.rs                    # Common utilities
----

== Index Algorithms

AXIS supports six specialized indexing algorithms, each optimized for different use cases:

=== Algorithm Comparison Table

[cols="2,2,2,2,2,3"]
|===
|Algorithm |Best For |Build Time |Search Speed |Memory Usage |Recall

|**HNSW**
|High-accuracy search
|Medium
|Very Fast
|High
|95-99%

|**IVF**
|Large-scale datasets
|Fast
|Fast
|Medium
|85-95%

|**LSH**
|Approximate search
|Very Fast
|Very Fast
|Low
|70-85%

|**Annoy**
|Static datasets
|Medium
|Fast
|Low
|80-90%

|**PQ**
|Memory-constrained
|Slow
|Fast
|Very Low
|75-90%

|**Flat**
|Exact results
|N/A
|Slow
|Low
|100%
|===

=== 1. HNSW (Hierarchical Navigable Small World)

**Description**: Graph-based indexing using hierarchical layers for navigable small-world graphs.

**When to Use**:

* High recall requirements (95%+)
* Real-time search applications
* Collections with 10K-10M vectors
* Low-latency requirements (< 10ms)

**Configuration**:

[source,rust]
----
IndexAlgorithm::HNSW {
    m: 16,                    // Bi-directional links (typical: 12-48)
    ef_construction: 200,     // Build quality (higher = better)
    ef_search: 50,            // Search quality (higher = better)
    max_elements: 1000000,    // Maximum vectors
}
----

**Performance Characteristics**:

* Build: O(n log n)
* Search: O(log n)
* Memory: ~200-300 bytes per vector
* Recall: 95-99% at ef_search=100

**Pros**:

* Excellent recall with low latency
* Good for dynamic updates
* Predictable performance
* Well-suited for real-time systems

**Cons**:

* High memory usage
* Parameter tuning required
* Not ideal for very large datasets (>100M)

=== 2. IVF (Inverted File Index)

**Description**: Clustering-based indexing that groups similar vectors into partitions.

**When to Use**:

* Large-scale datasets (10M-1B vectors)
* Batch query workloads
* Memory-efficient search
* Acceptable recall tradeoff (85-95%)

**Configuration**:

[source,rust]
----
IndexAlgorithm::IVF {
    nlist: 1000,              // Number of clusters (sqrt(n) typical)
    nprobe: 10,               // Clusters to search (higher = better recall)
    quantizer: None,          // Optional PQ compression
}
----

**Performance Characteristics**:

* Build: O(n * k * iterations) - requires training
* Search: O(nprobe * (n/nlist))
* Memory: ~50-100 bytes per vector + centroids
* Recall: 85-95% at nprobe=10

**Pros**:

* Scales to billions of vectors
* Memory-efficient
* Good balance of speed and recall
* Can combine with PQ for compression

**Cons**:

* Requires training phase
* Recall depends on clustering quality
* Not ideal for frequently changing data
* Static nlist parameter

=== 3. LSH (Locality Sensitive Hashing)

**Description**: Hash-based approximate search using random projections.

**When to Use**:

* Approximate search acceptable
* Very large datasets (100M+ vectors)
* Low memory budgets
* High throughput requirements

**Configuration**:

[source,rust]
----
IndexAlgorithm::LSH {
    n_projections: 10,        // Hash functions per table
    n_hash_tables: 5,         // Independent hash tables
    hash_width: 4.0,          // Bucket width
}
----

**Performance Characteristics**:

* Build: O(n * L * k) - L tables, k hashes
* Search: O(L * bucket_size)
* Memory: ~20-50 bytes per vector
* Recall: 70-85% (tunable)

**Pros**:

* Very low memory usage
* Fast build and search
* No training required
* Simple to implement and tune

**Cons**:

* Lower recall than graph methods
* Parameter tuning affects quality
* Hash collision tradeoffs
* Not suitable for high-precision search

=== 4. Annoy (Approximate Nearest Neighbors Oh Yeah)

**Description**: Tree-based indexing using random projection trees.

**When to Use**:

* Static or infrequently updated datasets
* Read-heavy workloads
* Disk-based indexing
* Predictable memory usage

**Configuration**:

[source,rust]
----
IndexAlgorithm::Annoy {
    n_trees: 10,              // Number of trees (more = better recall)
    search_k: -1,             // Nodes to inspect (-1 = auto)
    max_leaf_size: 8,         // Vectors per leaf
}
----

**Performance Characteristics**:

* Build: O(n log n * n_trees)
* Search: O(log n * n_trees)
* Memory: ~100-150 bytes per vector
* Recall: 80-90% at n_trees=10

**Pros**:

* Good for static datasets
* Memory-mappable (disk-friendly)
* Simple tree structure
* Predictable performance

**Cons**:

* Not optimized for updates
* Requires rebuild for major changes
* Recall lower than HNSW
* More memory than LSH

=== 5. PQ (Product Quantization)

**Description**: Vector compression through subspace quantization.

**When to Use**:

* Memory-constrained environments
* Large vectors (1536D+)
* Acceptable recall loss
* Batch processing workflows

**Configuration**:

[source,rust]
----
IndexAlgorithm::PQ {
    m: 8,                     // Number of subquantizers
    nbits: 8,                 // Bits per subquantizer (256 centroids)
    train_size: 10000,        // Training vectors
}
----

**Performance Characteristics**:

* Build: O(train_size * m * 2^nbits * iterations)
* Search: O(n) - but with compressed vectors
* Memory: m bytes per vector (typically 8-16)
* Recall: 75-90% (depends on m and nbits)

**Pros**:

* Extreme memory compression (10-100x)
* Fast distance computation with lookup tables
* Scalable to billions of vectors
* Can combine with IVF

**Cons**:

* Requires significant training data
* Recall loss from quantization
* Not suitable for low-dimensional data
* Complex parameter tuning

=== 6. Flat (Brute Force)

**Description**: Exact search through exhaustive comparison.

**When to Use**:

* Small datasets (< 10K vectors)
* 100% recall required
* Benchmarking and validation
* Simple baseline

**Configuration**:

[source,rust]
----
// Flat index has no configuration - always exact
----

**Performance Characteristics**:

* Build: O(1) - just store vectors
* Search: O(n * d) - check all vectors
* Memory: ~4 * dimension bytes per vector
* Recall: 100% (exact)

**Pros**:

* Perfect recall
* No training required
* Simple implementation
* Good for small datasets

**Cons**:

* Does not scale
* Slow for large datasets
* No optimization opportunities

== Adaptive Strategy Selection

AXIS automatically selects the optimal index based on collection characteristics and query patterns:

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
flowchart TD
    A[Collection Created] --> B{Analyze Characteristics}
    B --> C[Vector Count]
    B --> D[Dimensionality]
    B --> E[Query Patterns]
    B --> F[Memory Budget]

    C --> G{Size Decision}
    G -->|< 10K| H[Flat Index]
    G -->|10K-1M| I[HNSW Index]
    G -->|1M-100M| J{Recall Requirement}
    G -->|> 100M| K{Memory Budget}

    J -->|High 95%+| L[HNSW with partitioning]
    J -->|Medium 85%+| M[IVF Index]
    J -->|Low 70%+| N[LSH Index]

    K -->|High| O[IVF + HNSW Hybrid]
    K -->|Medium| P[IVF + PQ]
    K -->|Low| Q[LSH or Annoy]

    E --> R{Query Pattern}
    R -->|Static| S[Annoy Index]
    R -->|Dynamic| T[HNSW Index]
    R -->|Filtered| U[IVF + Metadata]

    style A fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#000
    style B fill:#5ba3f5,stroke:#3d7ab8,stroke-width:2px,color:#000
    style G fill:#ffd966,stroke:#cc9900,stroke-width:2px,color:#000
    style J fill:#ffd966,stroke:#cc9900,stroke-width:2px,color:#000
    style K fill:#ffd966,stroke:#cc9900,stroke-width:2px,color:#000
    style R fill:#ffd966,stroke:#cc9900,stroke-width:2px,color:#000
    style H fill:#93c47d,stroke:#5a8047,stroke-width:2px,color:#000
    style I fill:#93c47d,stroke:#5a8047,stroke-width:2px,color:#000
    style L fill:#93c47d,stroke:#5a8047,stroke-width:2px,color:#000
    style M fill:#93c47d,stroke:#5a8047,stroke-width:2px,color:#000
    style N fill:#93c47d,stroke:#5a8047,stroke-width:2px,color:#000
    style O fill:#93c47d,stroke:#5a8047,stroke-width:2px,color:#000
    style P fill:#93c47d,stroke:#5a8047,stroke-width:2px,color:#000
    style Q fill:#93c47d,stroke:#5a8047,stroke-width:2px,color:#000
    style S fill:#93c47d,stroke:#5a8047,stroke-width:2px,color:#000
    style T fill:#93c47d,stroke:#5a8047,stroke-width:2px,color:#000
    style U fill:#93c47d,stroke:#5a8047,stroke-width:2px,color:#000
----

=== Selection Criteria

**Collection Size**:

* **< 10K vectors**: Flat (exact search is fast enough)
* **10K-1M vectors**: HNSW (optimal for medium scale)
* **1M-100M vectors**: IVF or HNSW with partitioning
* **> 100M vectors**: IVF + PQ or distributed HNSW

**Dimensionality**:

* **< 128D**: HNSW or IVF (lower memory overhead)
* **128D-768D**: HNSW, IVF, or Annoy
* **> 768D**: IVF + PQ (compression beneficial)

**Query Patterns**:

* **High recall (95%+)**: HNSW or IVF with high nprobe
* **Balanced (85-95%)**: IVF with moderate nprobe
* **Fast approximate (70-85%)**: LSH or Annoy
* **Filtered queries**: IVF + metadata indexes

**Memory Budget**:

* **Unlimited**: HNSW (best quality)
* **Limited**: IVF or Annoy
* **Very Limited**: LSH or PQ
* **Disk-based**: Annoy (memory-mappable)

**Update Frequency**:

* **Static**: Annoy (optimized for reads)
* **Occasional updates**: HNSW (handles updates)
* **Frequent updates**: IVF with incremental rebuild
* **Real-time streams**: HNSW with background optimization

== Zero-Downtime Migration

AXIS enables seamless migration between index types as your data and query patterns evolve:

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
sequenceDiagram
    participant Monitor as AXIS Monitor
    participant Engine as Migration Engine
    participant Old as Old Index
    participant New as New Index
    participant Query as Query Router

    Note over Monitor: Detects degraded performance
    Monitor->>Engine: Trigger migration evaluation
    Engine->>Engine: Analyze current vs optimal
    Engine->>New: Build new index in background
    Note over New: Index build (non-blocking)
    New-->>Engine: Build complete
    Engine->>Engine: Validation phase
    Engine->>Query: Shift 10% traffic to new index
    Query->>New: Test queries
    New-->>Query: Results
    Query-->>Engine: Performance metrics
    Engine->>Query: Shift 50% traffic
    Query->>Old: 50% queries
    Query->>New: 50% queries
    Engine->>Query: Shift 100% traffic
    Note over Old: No longer serving queries
    Engine->>Old: Cleanup old index
    Old-->>Engine: Cleanup complete
----

=== Migration Workflow

**1. Detection Phase**:

* Monitor tracks query performance metrics
* Detects degradation or suboptimal patterns
* Analyzes collection growth and changes

**2. Planning Phase**:

* Evaluate current index performance
* Recommend optimal index type
* Estimate migration duration and resources
* Create rollback plan

**3. Build Phase**:

* Construct new index in background
* Parallel build using available CPU cores
* No impact on existing queries
* Progress tracking

**4. Validation Phase**:

* Verify index consistency
* Sample query testing
* Performance validation
* Quality checks

**5. Traffic Shifting**:

* Gradually shift query traffic (10% → 50% → 100%)
* Monitor performance at each stage
* Automatic rollback on degradation
* Minimal user impact

**6. Cleanup Phase**:

* Remove old index after stability period
* Free resources
* Update monitoring baselines

=== Migration Example

[source,rust]
----
use proximadb::index::axis::{AxisManager, MigrationEngine};

// AXIS detects collection growth
let collection_stats = axis_manager.analyze_collection("my_collection").await?;

// Recommend migration from HNSW to IVF
if collection_stats.total_vectors > 5_000_000 {
    let migration_plan = MigrationEngine::create_plan(
        "my_collection",
        current_strategy,
        recommended_strategy
    ).await?;

    // Execute migration with automatic rollback
    migration_engine.execute_plan(
        migration_plan,
        MigrationConfig {
            max_duration: Duration::from_hours(2),
            traffic_shift_steps: vec![0.1, 0.5, 1.0],
            auto_rollback: true,
            ..Default::default()
        }
    ).await?;
}
----

== Integration with Storage Engines

AXIS seamlessly integrates with all ProximaDB storage engines:

=== Storage Engine Coordination

[cols="2,3,3"]
|===
|Storage Engine |AXIS Integration |Optimal Index

|**SST**
|Real-time EventLog updates, WAL coordination
|HNSW (dynamic updates)

|**VIPER**
|Batch operations, columnar scans
|IVF (large scale)

|**SWIFT**
|Hierarchical indexing, tenant isolation
|IVF + BTree (filtered)

|**HELIX**
|Hilbert curve locality, spatial clustering
|HNSW (spatial aware)

|**NOVA**
|Progressive reads, mixed workloads
|HNSW or IVF (adaptive)

|**RAPTOR**
|Adaptive row-groups, dynamic access
|HNSW (flexible)
|===

=== EventLog Integration

AXIS consumes events from ProximaDB's global EventLog for real-time index updates:

[source,rust]
----
use proximadb::index::axis::integration::EventLogConsumer;

// Create EventLog consumer
let consumer = EventLogConsumer::new(
    collection_id,
    event_log_service,
    ConsumerConfig {
        batch_size: 1000,
        flush_interval: Duration::from_secs(5),
        ..Default::default()
    }
).await?;

// Start consuming events
consumer.start().await?;

// Events automatically update indexes:
// - VectorInserted -> add to index
// - VectorUpdated -> update in index
// - VectorDeleted -> remove from index
// - CollectionFlushed -> rebuild if needed
----

=== WAL Flush Coordination

AXIS coordinates with the global WAL system for consistent index updates:

[source,rust]
----
use proximadb::index::axis::integration::FlushIntegration;

// Coordinate flush with WAL
let flush_integration = FlushIntegration::new(
    collection_id,
    wal_manifest,
    FlushConfig {
        enable_background_rebuild: true,
        rebuild_threshold: 0.3, // Rebuild when 30% changed
        ..Default::default()
    }
);

// On WAL flush, update indexes
wal_manifest.on_flush(|flushed_records| {
    flush_integration.update_indexes(flushed_records).await?;
}).await?;
----

== Performance Characteristics

=== Query Latency Benchmarks

**Test Setup**: 10M vectors, 1536 dimensions, Euclidean distance

[cols="2,2,2,2,2"]
|===
|Index |p50 Latency |p99 Latency |Recall |Memory

|**HNSW**
|2.3ms
|8.1ms
|97.5%
|3.2GB

|**IVF**
|4.7ms
|12.3ms
|91.2%
|1.8GB

|**LSH**
|1.8ms
|5.6ms
|78.5%
|800MB

|**Annoy**
|3.5ms
|9.8ms
|87.3%
|1.4GB

|**PQ (8 subvectors)**
|6.2ms
|15.7ms
|82.1%
|80MB

|**Flat**
|245ms
|312ms
|100%
|61GB
|===

=== Index Build Performance

**Test Setup**: 1M vectors, 768 dimensions

[cols="2,2,2,2"]
|===
|Index |Build Time |Throughput |Notes

|**HNSW**
|3m 20s
|5,000/sec
|Parallel build

|**IVF**
|1m 45s
|9,500/sec
|Includes training

|**LSH**
|45s
|22,000/sec
|No training

|**Annoy**
|2m 10s
|7,700/sec
|10 trees

|**PQ**
|8m 30s
|2,000/sec
|Training heavy

|**Flat**
|5s
|200,000/sec
|Just storage
|===

=== Memory Efficiency

**Per-Vector Memory Overhead**:

* **HNSW**: 200-300 bytes (graph structure)
* **IVF**: 50-100 bytes (cluster assignment)
* **LSH**: 20-50 bytes (hash tables)
* **Annoy**: 100-150 bytes (tree nodes)
* **PQ**: 8-16 bytes (quantized codes)
* **Flat**: 0 bytes (raw storage only)

== Configuration

=== Collection-Level Configuration

Specify index type when creating a collection:

[source,json]
----
{
  "name": "my_vectors",
  "dimension": 1536,
  "distance_metric": "L2",
  "index_config": {
    "algorithm": "HNSW",
    "parameters": {
      "m": 16,
      "ef_construction": 200,
      "ef_search": 50
    },
    "adaptive": {
      "enabled": true,
      "migration_threshold": 0.15,
      "evaluation_interval_hours": 24
    }
  }
}
----

=== Automatic Index Selection

Let AXIS choose the optimal index:

[source,json]
----
{
  "name": "my_vectors",
  "dimension": 768,
  "distance_metric": "Cosine",
  "index_config": {
    "algorithm": "auto",
    "optimization_goal": "balanced",
    "constraints": {
      "max_memory_gb": 8.0,
      "target_latency_ms": 10.0,
      "min_recall": 0.90
    }
  }
}
----

=== Global Configuration

In `config.toml`:

[source,toml]
----
[index.axis]
# Enable adaptive indexing
adaptive_indexing = true

# Migration settings
migration_enabled = true
migration_evaluation_interval_hours = 24
migration_threshold = 0.15  # 15% performance improvement

# Memory management
max_memory_per_index_gb = 4.0
enable_tiering = true

# Monitoring
enable_performance_tracking = true
query_sampling_rate = 0.1  # Sample 10% of queries
----

== Use Cases and Examples

=== Use Case 1: E-Commerce Product Search

**Scenario**: 50M product embeddings, high-recall search, fast queries

**Recommended Index**: HNSW with partitioning

[source,rust]
----
use proximadb::index::axis::{IndexAlgorithm, AxisManager};

// Create collection with HNSW
let config = CollectionConfig {
    name: "products".to_string(),
    dimension: 512,
    distance_metric: DistanceMetric::Cosine,
    index_config: IndexConfig {
        algorithm: IndexAlgorithm::HNSW {
            m: 24,
            ef_construction: 300,
            ef_search: 100,
            max_elements: 50_000_000,
        },
        adaptive: AdaptiveConfig {
            enabled: true,
            migration_threshold: 0.2,
            evaluation_interval: Duration::from_hours(12),
        },
    },
};

axis_manager.create_index(config).await?;

// Search for similar products
let results = axis_manager.search(
    "products",
    query_embedding,
    SearchParams {
        top_k: 20,
        filter: Some(json!({
            "category": "electronics",
            "price_range": {"min": 100, "max": 500}
        })),
        ..Default::default()
    }
).await?;
----

=== Use Case 2: Document Similarity at Scale

**Scenario**: 500M document embeddings, memory-constrained, batch queries

**Recommended Index**: IVF + PQ

[source,rust]
----
// Create collection with IVF + PQ
let config = CollectionConfig {
    name: "documents".to_string(),
    dimension: 1536,
    distance_metric: DistanceMetric::L2,
    index_config: IndexConfig {
        algorithm: IndexAlgorithm::IVF {
            nlist: 10000,  // sqrt(500M) ≈ 22K, use 10K
            nprobe: 20,
            quantizer: Some(Box::new(IndexAlgorithm::PQ {
                m: 16,       // 16 subvectors
                nbits: 8,    // 256 centroids per subvector
                train_size: 100000,
            })),
        },
        adaptive: AdaptiveConfig {
            enabled: true,
            migration_threshold: 0.15,
            evaluation_interval: Duration::from_hours(24),
        },
    },
};

axis_manager.create_index(config).await?;

// Train index on representative sample
let training_vectors = load_sample_vectors(100_000);
axis_manager.train_index("documents", training_vectors).await?;

// Insert documents
axis_manager.insert_batch("documents", document_embeddings).await?;

// Batch search
let results = axis_manager.batch_search(
    "documents",
    query_embeddings,  // Vec<Vec<f32>>
    BatchSearchParams {
        top_k: 10,
        parallel: true,
        ..Default::default()
    }
).await?;
----

=== Use Case 3: Real-Time Image Similarity

**Scenario**: 10M image embeddings, real-time updates, low latency

**Recommended Index**: HNSW

[source,rust]
----
// Create collection with HNSW optimized for updates
let config = CollectionConfig {
    name: "images".to_string(),
    dimension: 2048,
    distance_metric: DistanceMetric::Cosine,
    index_config: IndexConfig {
        algorithm: IndexAlgorithm::HNSW {
            m: 16,
            ef_construction: 200,
            ef_search: 50,
            max_elements: 20_000_000,  // Room for growth
        },
        adaptive: AdaptiveConfig::default(),
    },
};

axis_manager.create_index(config).await?;

// Real-time insertion
for image in image_stream {
    axis_manager.insert(
        "images",
        image.id,
        image.embedding,
        image.metadata
    ).await?;
}

// Low-latency search
let similar_images = axis_manager.search(
    "images",
    query_embedding,
    SearchParams {
        top_k: 10,
        timeout_ms: 50,  // 50ms timeout
        ..Default::default()
    }
).await?;
----

=== Use Case 4: Approximate Search on Device

**Scenario**: Mobile device, 1M vectors, limited memory, offline

**Recommended Index**: LSH or Annoy

[source,rust]
----
// Create collection with LSH for low memory
let config = CollectionConfig {
    name: "mobile_vectors".to_string(),
    dimension: 384,
    distance_metric: DistanceMetric::Cosine,
    index_config: IndexConfig {
        algorithm: IndexAlgorithm::LSH {
            n_projections: 12,
            n_hash_tables: 8,
            hash_width: 4.0,
        },
        adaptive: AdaptiveConfig {
            enabled: false,  // Disable for mobile
            ..Default::default()
        },
    },
};

axis_manager.create_index(config).await?;

// Build and serialize for mobile
axis_manager.build("mobile_vectors").await?;
let index_data = axis_manager.serialize("mobile_vectors").await?;

// Deploy to mobile device
deploy_to_device(index_data)?;

// On device: load and search
let mobile_index = MobileIndex::load(index_data)?;
let results = mobile_index.search(query, 10)?;
----

== Monitoring and Metrics

=== Performance Metrics

AXIS tracks comprehensive performance metrics:

[source,rust]
----
use proximadb::index::axis::management::AxisMonitor;

// Create monitor
let monitor = AxisMonitor::new(collection_id);

// Get metrics
let metrics = monitor.collect_metrics().await?;

println!("Query Performance:");
println!("  Average latency: {:.2}ms", metrics.avg_query_latency_ms);
println!("  p99 latency: {:.2}ms", metrics.p99_query_latency_ms);
println!("  Throughput: {:.0} qps", metrics.queries_per_second);
println!("  Average recall: {:.2}%", metrics.avg_recall * 100.0);

println!("\nIndex Status:");
println!("  Algorithm: {:?}", metrics.current_algorithm);
println!("  Vector count: {}", metrics.vector_count);
println!("  Memory usage: {:.2}GB", metrics.memory_usage_bytes as f64 / 1e9);
println!("  Index quality: {:.2}%", metrics.index_quality * 100.0);

println!("\nQuery Patterns:");
println!("  Filtered queries: {:.1}%", metrics.filter_usage_ratio * 100.0);
println!("  Average k: {}", metrics.avg_top_k);
println!("  Cache hit rate: {:.1}%", metrics.cache_hit_rate * 100.0);
----

=== Dashboard Integration

Monitor AXIS performance in ProximaDB dashboard:

* Real-time query latency graphs
* Index type distribution
* Migration status and history
* Memory usage trends
* Recall quality over time

=== Alerts and Thresholds

Configure alerts for performance degradation:

[source,toml]
----
[index.axis.alerts]
# Latency alerts
latency_p99_threshold_ms = 100.0
latency_degradation_percent = 20.0

# Recall alerts
recall_threshold = 0.85
recall_degradation_percent = 5.0

# Memory alerts
memory_usage_threshold_gb = 10.0
memory_growth_rate_percent_per_day = 10.0

# Query pattern alerts
query_failure_rate_threshold = 0.01  # 1%
----

== Troubleshooting

=== Common Issues

**Issue**: Poor search recall

**Solutions**:

1. Increase index quality parameters:
   * HNSW: Increase `ef_search` (e.g., 50 → 100)
   * IVF: Increase `nprobe` (e.g., 10 → 20)
   * LSH: Add more hash tables
   * Annoy: Increase `n_trees`

2. Check index training:
   * IVF: Ensure sufficient training data (10K+ vectors)
   * PQ: Use representative training sample

3. Verify distance metric matches use case

**Issue**: High query latency

**Solutions**:

1. Reduce search scope:
   * HNSW: Decrease `ef_search`
   * IVF: Decrease `nprobe`
   * Use metadata filtering to prune candidates

2. Consider index migration:
   * HNSW → IVF (if recall can be reduced)
   * IVF → LSH (for approximate results)

3. Enable caching and prefetching

4. Check hardware acceleration (SIMD, GPU)

**Issue**: High memory usage

**Solutions**:

1. Enable compression:
   * Add PQ quantization to IVF
   * Use INT8 quantization
   * Enable memory tiering

2. Reduce index parameters:
   * HNSW: Decrease `m` (e.g., 16 → 12)
   * IVF: Decrease `nlist`

3. Consider memory-efficient alternatives:
   * HNSW → IVF
   * IVF → LSH
   * Add PQ compression

**Issue**: Slow index builds

**Solutions**:

1. Increase parallel build threads
2. Reduce training iterations (IVF, PQ)
3. Use fast approximation mode
4. Build incrementally with smaller batches

=== Debugging

**Enable detailed logging**:

[source,bash]
----
RUST_LOG=proximadb::index::axis=debug cargo run --bin proximadb-server
----

**Check index health**:

[source,rust]
----
let health = axis_manager.check_index_health("collection_id").await?;

println!("Index Health Report:");
println!("  Status: {:?}", health.status);
println!("  Warnings: {:?}", health.warnings);
println!("  Recommendations: {:?}", health.recommendations);
----

**Analyze query patterns**:

[source,rust]
----
let pattern_analysis = axis_manager.analyze_query_patterns(
    "collection_id",
    Duration::from_hours(24)  // Last 24 hours
).await?;

println!("Query Pattern Analysis:");
println!("  Total queries: {}", pattern_analysis.total_queries);
println!("  Average k: {}", pattern_analysis.avg_k);
println!("  Filter usage: {:.1}%", pattern_analysis.filter_ratio * 100.0);
println!("  Recommended index: {:?}", pattern_analysis.recommended_algorithm);
----

== Performance Tuning Guidelines

=== For High Recall (95%+)

[source,toml]
----
[index.axis.hnsw]
m = 24                     # More connections
ef_construction = 300      # Better build quality
ef_search = 100            # More thorough search
----

=== For Low Latency (< 5ms)

[source,toml]
----
[index.axis.hnsw]
ef_search = 30             # Faster search
enable_caching = true      # Cache frequent queries
prefetch_neighbors = true  # Prefetch graph nodes
----

=== For Large Scale (100M+ vectors)

[source,toml]
----
[index.axis.ivf]
nlist = 10000              # More clusters
nprobe = 10                # Moderate recall
enable_pq = true           # Compression
pq_subvectors = 16         # 16-byte codes
----

=== For Memory Constraints

[source,toml]
----
[index.axis]
default_algorithm = "LSH"
enable_tiering = true      # Move cold indexes to disk

[index.axis.lsh]
n_hash_tables = 5
n_projections = 10

# Or use IVF + PQ
[index.axis.ivf]
nlist = 5000
enable_pq = true
pq_subvectors = 8          # 8-byte codes
----

=== For Dynamic Workloads

[source,toml]
----
[index.axis]
adaptive_indexing = true
migration_threshold = 0.1  # Migrate if 10% improvement
evaluation_interval_hours = 6  # Check every 6 hours

[index.axis.hnsw]
m = 16                     # Good for updates
ef_construction = 200
----

== API Reference

=== Core API

[source,rust]
----
// Create AXIS manager
let axis_manager = AxisManager::new(config).await?;

// Create index
axis_manager.create_index(collection_config).await?;

// Insert vectors
axis_manager.insert(collection_id, id, vector, metadata).await?;
axis_manager.insert_batch(collection_id, batch).await?;

// Search
let results = axis_manager.search(
    collection_id,
    query_vector,
    search_params
).await?;

// Update
axis_manager.update(collection_id, id, vector, metadata).await?;

// Delete
axis_manager.delete(collection_id, id).await?;

// Analyze and optimize
let analysis = axis_manager.analyze_collection(collection_id).await?;
let recommendation = axis_manager.recommend_index(collection_id).await?;

// Migrate index
axis_manager.migrate_index(
    collection_id,
    target_algorithm,
    migration_config
).await?;

// Monitor
let metrics = axis_manager.get_metrics(collection_id).await?;
----

=== Strategy Selection API

[source,rust]
----
use proximadb::index::axis::management::{
    StrategySelector, CollectionStatistics, QueryPatterns, OptimizationConfig
};

// Build strategy selector
let strategy = StrategySelector::new(
    CollectionStatistics {
        total_vectors: 10_000_000,
        vector_dimension: 768,
        avg_vector_sparsity: 0.0,
        has_metadata: true,
        metadata_cardinality: hashmap!{
            "category".to_string() => 50,
            "user_id".to_string() => 100_000,
        },
        has_text_fields: false,
        update_frequency: 100.0,  // 100 updates/sec
    },
    QueryPatterns {
        avg_queries_per_second: 1000.0,
        filter_usage_ratio: 0.3,
        text_search_ratio: 0.0,
        typical_k: 10,
        recall_requirement: 0.95,
    }
).with_optimization(OptimizationConfig {
    goal: OptimizationGoal::MinLatency,
    max_memory_gb: Some(8.0),
    target_latency_ms: Some(10.0),
    min_accuracy: Some(0.95),
}).build()?;
----

=== Migration API

[source,rust]
----
use proximadb::index::axis::management::{MigrationEngine, MigrationPlan};

// Create migration engine
let migration_engine = MigrationEngine::new(config).await?;

// Evaluate migration need
let decision = migration_engine.evaluate_migration(
    collection_id,
    current_strategy,
    performance_metrics
).await?;

// Create migration plan
let plan = migration_engine.create_plan(
    collection_id,
    current_strategy,
    target_strategy
).await?;

// Execute migration
migration_engine.execute_plan(
    plan,
    MigrationConfig {
        max_duration: Duration::from_hours(4),
        traffic_shift_steps: vec![0.1, 0.25, 0.5, 0.75, 1.0],
        auto_rollback: true,
        validation_sample_size: 1000,
        ..Default::default()
    }
).await?;

// Monitor migration
let status = migration_engine.get_migration_status(collection_id).await?;
----

== Future Enhancements

**Planned Features** (see link:../../../docs/09-roadmap/[Roadmap]):

=== Short-Term (Q1-Q2 2025)

* **GPU-Accelerated Indexes**: CUDA/ROCm support for HNSW and IVF
* **Distributed Indexes**: Shard indexes across multiple nodes
* **Advanced PQ**: Optimized Product Quantization with polysemous codes
* **Hybrid Indexes**: Combine multiple algorithms (e.g., IVF + HNSW)
* **Index Compression**: On-disk compression for Annoy and HNSW

=== Mid-Term (Q3-Q4 2025)

* **Neural Indexes**: Learned indexes using neural networks
* **Approximate Graph Indexes**: ScaNN, DiskANN integration
* **Multi-Index Queries**: Query multiple indexes in parallel
* **Index Versioning**: Maintain multiple index versions
* **Smart Prefetching**: ML-based query prediction

=== Long-Term (2026+)

* **Quantum-Inspired Indexes**: Quantum annealing for optimization
* **Neuromorphic Computing**: Spiking neural networks for search
* **AutoML Index Selection**: Automated hyperparameter tuning
* **Federated Indexes**: Cross-organization index federation
* **Real-Time Index Evolution**: Dynamic index restructuring

== References

**AXIS Documentation**:

* link:../../../docs/index/axis-deep-dive.adoc[AXIS Deep Dive] (to be created)
* link:../../../docs/index/index-selection-guide.adoc[Index Selection Guide] (to be created)
* link:../../../docs/index/migration-guide.adoc[Migration Guide] (to be created)

**Algorithm Papers**:

* **HNSW**: "Efficient and robust approximate nearest neighbor search using Hierarchical Navigable Small World graphs" (Malkov & Yashunin, 2018)
* **IVF**: "Product quantization for nearest neighbor search" (Jegou et al., 2011)
* **LSH**: "Locality-sensitive hashing scheme based on p-stable distributions" (Datar et al., 2004)
* **Annoy**: "Annoy: Approximate Nearest Neighbors in C++/Python" (Bernhardsson, 2013)
* **PQ**: "Optimized Product Quantization" (Ge et al., 2013)

**ProximaDB Architecture**:

* link:../../../docs/architecture/overview.adoc[Architecture Overview]
* link:../../../docs/storage/engines/[Storage Engines Documentation]
* link:../../../docs/09-roadmap/[Development Roadmap]

**External Resources**:

* https://github.com/facebookresearch/faiss[FAISS: Facebook AI Similarity Search]
* https://github.com/nmslib/hnswlib[HNSWlib: Fast approximate nearest neighbor search]
* https://github.com/spotify/annoy[Annoy: Approximate Nearest Neighbors library]
* https://ann-benchmarks.com/[ANN-Benchmarks: Performance comparison]

---
_Last updated: January 2025 (v0.1.5)_
