# ProximaDB Default Configuration
# This file contains all default values for ProximaDB configuration
# Users only need to specify values they want to override

[server]
node_id = "proximadb-node-1"
bind_address = "0.0.0.0"
port = 5678
data_dir = "./data"

[storage]
data_dirs = ["./data/storage"]
write_buffer_dir = "./data/write_buffer"
mmap_enabled = true
cache_size_mb = 512
bloom_filter_bits = 10

[storage.write_buffer_config]
write_buffer_size_mb = 8192  # 8GB total across all collections
memory_flush_size_bytes = 16777216  # 16MB per collection (aggregate of all unflushed batches)
memtable_type = "BTree"
sync_mode = "PerBatch"
write_buffer_directory = "./data/write_buffer"
enable_wal = true

[storage.sst_config]
level_count = 7
compaction_threshold = 5  # Minimum 5 files before compaction
compaction_strategy = "leveled"
compression = "snappy"
block_size_kb = 8192  # SSTable block size (KB). 8MB optimized for ZSTD compression and modern storage. Range: 4KB-16MB. Backward compatible.
cache_size_mb = 128
max_files_per_level = 10
level_size_multiplier = 10.0
max_levels = 7
background_thread_count = 4
data_directory = "./data/sst_data"
mmap_enabled = true
prefetch_enabled = true
prefetch_size_kb = 64

[storage.viper_config]
row_group_size = 100000
compression = "snappy"
enable_statistics = true
data_directory = "./data/viper_data"
cache_size_mb = 512

[storage.bloom_filter_config]
bits_per_key = 10
enabled = true

[storage.storage_layout]
node_instance = 1
assignment_strategy = "RoundRobin"

[[storage.storage_layout.base_paths]]
base_dir = "./data/storage"
instance_id = 1
mount_point = "./"
disk_type = { Ssd = { max_iops = 50000 } }

[storage.storage_layout.base_paths.capacity_config]
max_write_buffer_size_mb = 1024
metadata_reserved_mb = 256
warning_threshold_percent = 85.0

[storage.storage_layout.temp_config]
use_same_directory = true
temp_suffix = "___temp"
compaction_suffix = "___compaction"
flush_suffix = "___flushed"
cleanup_on_startup = true

[storage.filesystem_config]
enable_write_strategy_cache = true
temp_strategy = "SameDirectory"

[storage.filesystem_config.atomic_config]
enable_local_atomic = true
enable_object_store_atomic = false
cleanup_temp_on_startup = true

[storage.metadata_backend]
backend_type = "filestore"
storage_url = "file://./data/metadata"
cache_size_mb = 64
flush_interval_secs = 30
enable_compression = true
enable_snapshots = true
snapshot_threshold = 100
keep_snapshots = 3

[api]
grpc_port = 5679
rest_port = 5678
max_request_size_mb = 10
timeout_seconds = 30
enable_tls = false

[monitoring]
metrics_enabled = true
log_level = "info"

[consensus]
node_id = 1
cluster_peers = []
election_timeout_ms = 5000
heartbeat_interval_ms = 1000
snapshot_threshold = 1000

[tls]
enabled = false