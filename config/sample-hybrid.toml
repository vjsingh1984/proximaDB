# Sample ProximaDB hybrid and graph runtime configuration

[graph]
# Select graph engine: ORION | PULSAR | QUASAR (future)
engine = "ORION"

[hybrid]
# Default seeding strategy for hybrid (Average | Per_Seed | None)
seeding_strategy = "PER_SEED"

# Fusion weights for [vector, graph]
fusion_weights = [0.7, 0.3]

