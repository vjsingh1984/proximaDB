# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from __future__ import annotations

from collections.abc import Mapping
from typing import TYPE_CHECKING, Any, BinaryIO, Generator, TextIO, TypeVar, cast

from attrs import define as _attrs_define
from attrs import field as _attrs_field

from ..types import UNSET, Unset

T = TypeVar("T", bound="GraphStats")


@_attrs_define
class GraphStats:
    """
    Attributes:
        total_nodes (int):
        total_edges (int):
        label_stats (list[Any]):
        edge_type_stats (list[Any]):
        total_properties (int):
        memory_usage_bytes (int):
        average_degree (float):
        max_degree (int):
        connected_components (int):
    """

    total_nodes: int
    total_edges: int
    label_stats: list[Any]
    edge_type_stats: list[Any]
    total_properties: int
    memory_usage_bytes: int
    average_degree: float
    max_degree: int
    connected_components: int
    additional_properties: dict[str, Any] = _attrs_field(init=False, factory=dict)

    def to_dict(self) -> dict[str, Any]:
        total_nodes = self.total_nodes

        total_edges = self.total_edges

        label_stats = self.label_stats

        edge_type_stats = self.edge_type_stats

        total_properties = self.total_properties

        memory_usage_bytes = self.memory_usage_bytes

        average_degree = self.average_degree

        max_degree = self.max_degree

        connected_components = self.connected_components

        field_dict: dict[str, Any] = {}
        field_dict.update(self.additional_properties)
        field_dict.update(
            {
                "total_nodes": total_nodes,
                "total_edges": total_edges,
                "label_stats": label_stats,
                "edge_type_stats": edge_type_stats,
                "total_properties": total_properties,
                "memory_usage_bytes": memory_usage_bytes,
                "average_degree": average_degree,
                "max_degree": max_degree,
                "connected_components": connected_components,
            }
        )

        return field_dict

    @classmethod
    def from_dict(cls: type[T], src_dict: Mapping[str, Any]) -> T:
        d = dict(src_dict)
        total_nodes = d.pop("total_nodes")

        total_edges = d.pop("total_edges")

        label_stats = cast(list[Any], d.pop("label_stats"))

        edge_type_stats = cast(list[Any], d.pop("edge_type_stats"))

        total_properties = d.pop("total_properties")

        memory_usage_bytes = d.pop("memory_usage_bytes")

        average_degree = d.pop("average_degree")

        max_degree = d.pop("max_degree")

        connected_components = d.pop("connected_components")

        graph_stats = cls(
            total_nodes=total_nodes,
            total_edges=total_edges,
            label_stats=label_stats,
            edge_type_stats=edge_type_stats,
            total_properties=total_properties,
            memory_usage_bytes=memory_usage_bytes,
            average_degree=average_degree,
            max_degree=max_degree,
            connected_components=connected_components,
        )

        graph_stats.additional_properties = d
        return graph_stats

    @property
    def additional_keys(self) -> list[str]:
        return list(self.additional_properties.keys())

    def __getitem__(self, key: str) -> Any:
        return self.additional_properties[key]

    def __setitem__(self, key: str, value: Any) -> None:
        self.additional_properties[key] = value

    def __delitem__(self, key: str) -> None:
        del self.additional_properties[key]

    def __contains__(self, key: str) -> bool:
        return key in self.additional_properties
