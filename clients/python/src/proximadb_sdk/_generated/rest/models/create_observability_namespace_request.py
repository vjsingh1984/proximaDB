# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from __future__ import annotations

from collections.abc import Mapping
from typing import TYPE_CHECKING, Any, BinaryIO, Generator, TextIO, TypeVar

from attrs import define as _attrs_define
from attrs import field as _attrs_field

from ..types import UNSET, Unset

T = TypeVar("T", bound="CreateObservabilityNamespaceRequest")


@_attrs_define
class CreateObservabilityNamespaceRequest:
    """Body for `POST /api/v2/observability/namespaces`. Retention days
    default to hot=1, warm=7, cold=30.

        Attributes:
            name (str):
            hot_retention_days (int | Unset):  Default: 1.
            warm_retention_days (int | Unset):  Default: 7.
            cold_retention_days (int | Unset):  Default: 30.
    """

    name: str
    hot_retention_days: int | Unset = 1
    warm_retention_days: int | Unset = 7
    cold_retention_days: int | Unset = 30
    additional_properties: dict[str, Any] = _attrs_field(init=False, factory=dict)

    def to_dict(self) -> dict[str, Any]:
        name = self.name

        hot_retention_days = self.hot_retention_days

        warm_retention_days = self.warm_retention_days

        cold_retention_days = self.cold_retention_days

        field_dict: dict[str, Any] = {}
        field_dict.update(self.additional_properties)
        field_dict.update(
            {
                "name": name,
            }
        )
        if hot_retention_days is not UNSET:
            field_dict["hot_retention_days"] = hot_retention_days
        if warm_retention_days is not UNSET:
            field_dict["warm_retention_days"] = warm_retention_days
        if cold_retention_days is not UNSET:
            field_dict["cold_retention_days"] = cold_retention_days

        return field_dict

    @classmethod
    def from_dict(cls: type[T], src_dict: Mapping[str, Any]) -> T:
        d = dict(src_dict)
        name = d.pop("name")

        hot_retention_days = d.pop("hot_retention_days", UNSET)

        warm_retention_days = d.pop("warm_retention_days", UNSET)

        cold_retention_days = d.pop("cold_retention_days", UNSET)

        create_observability_namespace_request = cls(
            name=name,
            hot_retention_days=hot_retention_days,
            warm_retention_days=warm_retention_days,
            cold_retention_days=cold_retention_days,
        )

        create_observability_namespace_request.additional_properties = d
        return create_observability_namespace_request

    @property
    def additional_keys(self) -> list[str]:
        return list(self.additional_properties.keys())

    def __getitem__(self, key: str) -> Any:
        return self.additional_properties[key]

    def __setitem__(self, key: str, value: Any) -> None:
        self.additional_properties[key] = value

    def __delitem__(self, key: str) -> None:
        del self.additional_properties[key]

    def __contains__(self, key: str) -> bool:
        return key in self.additional_properties
