# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from __future__ import annotations

from collections.abc import Mapping
from typing import TYPE_CHECKING, Any, BinaryIO, Generator, TextIO, TypeVar, cast

from attrs import define as _attrs_define
from attrs import field as _attrs_field

from ..models.primary_pod_lookup_response_status import PrimaryPodLookupResponseStatus
from ..types import UNSET, Unset

if TYPE_CHECKING:
    from ..models.primary_pod import PrimaryPod


T = TypeVar("T", bound="PrimaryPodLookupResponse")


@_attrs_define
class PrimaryPodLookupResponse:
    """Internally tagged by `status` (snake_case). `status: "bound"`
    carries `primary`; `status: "unbound"` means no binding (never
    a 404).

        Attributes:
            status (PrimaryPodLookupResponseStatus):
            tenant_id (str):
            collection_id (str):
            primary (None | PrimaryPod | Unset):
    """

    status: PrimaryPodLookupResponseStatus
    tenant_id: str
    collection_id: str
    primary: None | PrimaryPod | Unset = UNSET
    additional_properties: dict[str, Any] = _attrs_field(init=False, factory=dict)

    def to_dict(self) -> dict[str, Any]:
        from ..models.primary_pod import PrimaryPod

        status = self.status.value

        tenant_id = self.tenant_id

        collection_id = self.collection_id

        primary: dict[str, Any] | None | Unset
        if isinstance(self.primary, Unset):
            primary = UNSET
        elif isinstance(self.primary, PrimaryPod):
            primary = self.primary.to_dict()
        else:
            primary = self.primary

        field_dict: dict[str, Any] = {}
        field_dict.update(self.additional_properties)
        field_dict.update(
            {
                "status": status,
                "tenant_id": tenant_id,
                "collection_id": collection_id,
            }
        )
        if primary is not UNSET:
            field_dict["primary"] = primary

        return field_dict

    @classmethod
    def from_dict(cls: type[T], src_dict: Mapping[str, Any]) -> T:
        from ..models.primary_pod import PrimaryPod

        d = dict(src_dict)
        status = PrimaryPodLookupResponseStatus(d.pop("status"))

        tenant_id = d.pop("tenant_id")

        collection_id = d.pop("collection_id")

        def _parse_primary(data: object) -> None | PrimaryPod | Unset:
            if data is None:
                return data
            if isinstance(data, Unset):
                return data
            try:
                if not isinstance(data, dict):
                    raise TypeError()
                primary_type_0 = PrimaryPod.from_dict(data)

                return primary_type_0
            except (TypeError, ValueError, AttributeError, KeyError):
                pass
            return cast(None | PrimaryPod | Unset, data)

        primary = _parse_primary(d.pop("primary", UNSET))

        primary_pod_lookup_response = cls(
            status=status,
            tenant_id=tenant_id,
            collection_id=collection_id,
            primary=primary,
        )

        primary_pod_lookup_response.additional_properties = d
        return primary_pod_lookup_response

    @property
    def additional_keys(self) -> list[str]:
        return list(self.additional_properties.keys())

    def __getitem__(self, key: str) -> Any:
        return self.additional_properties[key]

    def __setitem__(self, key: str, value: Any) -> None:
        self.additional_properties[key] = value

    def __delitem__(self, key: str) -> None:
        del self.additional_properties[key]

    def __contains__(self, key: str) -> bool:
        return key in self.additional_properties
