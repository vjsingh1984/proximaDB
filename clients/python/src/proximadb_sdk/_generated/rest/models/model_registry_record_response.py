# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from __future__ import annotations

from collections.abc import Mapping
from typing import TYPE_CHECKING, Any, BinaryIO, Generator, TextIO, TypeVar, cast

from attrs import define as _attrs_define
from attrs import field as _attrs_field

from ..types import UNSET, Unset

if TYPE_CHECKING:
    from ..models.catalog_embedding_model_registry import CatalogEmbeddingModelRegistry


T = TypeVar("T", bound="ModelRegistryRecordResponse")


@_attrs_define
class ModelRegistryRecordResponse:
    """REST uses a decimal string for stable catalog IDs so JavaScript clients do
    not lose precision. gRPC exposes the same value as `uint64`.

        Attributes:
            asset_id (str):
            registry (CatalogEmbeddingModelRegistry): Registered embedding model: immutable versions plus separately mutable
                or
                append-only lifecycle records.
            tenant_id (str):
    """

    asset_id: str
    registry: CatalogEmbeddingModelRegistry
    tenant_id: str
    additional_properties: dict[str, Any] = _attrs_field(init=False, factory=dict)

    def to_dict(self) -> dict[str, Any]:
        from ..models.catalog_embedding_model_registry import (
            CatalogEmbeddingModelRegistry,
        )

        asset_id = self.asset_id

        registry = self.registry.to_dict()

        tenant_id = self.tenant_id

        field_dict: dict[str, Any] = {}
        field_dict.update(self.additional_properties)
        field_dict.update(
            {
                "asset_id": asset_id,
                "registry": registry,
                "tenant_id": tenant_id,
            }
        )

        return field_dict

    @classmethod
    def from_dict(cls: type[T], src_dict: Mapping[str, Any]) -> T:
        from ..models.catalog_embedding_model_registry import (
            CatalogEmbeddingModelRegistry,
        )

        d = dict(src_dict)
        asset_id = d.pop("asset_id")

        registry = CatalogEmbeddingModelRegistry.from_dict(d.pop("registry"))

        tenant_id = d.pop("tenant_id")

        model_registry_record_response = cls(
            asset_id=asset_id,
            registry=registry,
            tenant_id=tenant_id,
        )

        model_registry_record_response.additional_properties = d
        return model_registry_record_response

    @property
    def additional_keys(self) -> list[str]:
        return list(self.additional_properties.keys())

    def __getitem__(self, key: str) -> Any:
        return self.additional_properties[key]

    def __setitem__(self, key: str, value: Any) -> None:
        self.additional_properties[key] = value

    def __delitem__(self, key: str) -> None:
        del self.additional_properties[key]

    def __contains__(self, key: str) -> bool:
        return key in self.additional_properties
