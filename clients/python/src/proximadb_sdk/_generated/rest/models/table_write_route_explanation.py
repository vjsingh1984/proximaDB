# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from __future__ import annotations

from collections.abc import Mapping
from typing import TYPE_CHECKING, Any, BinaryIO, Generator, TextIO, TypeVar, cast

from attrs import define as _attrs_define
from attrs import field as _attrs_field

from ..types import UNSET, Unset

if TYPE_CHECKING:
    from ..models.table_write_candidate_explanation import (
        TableWriteCandidateExplanation,
    )
    from ..models.table_write_cost_explanation import TableWriteCostExplanation
    from ..models.table_write_data_movement_explanation import (
        TableWriteDataMovementExplanation,
    )
    from ..models.table_write_intent_explanation import TableWriteIntentExplanation
    from ..models.table_write_rejected_lane_explanation import (
        TableWriteRejectedLaneExplanation,
    )
    from ..models.table_write_rejected_path_explanation import (
        TableWriteRejectedPathExplanation,
    )
    from ..models.table_write_route_metadata_explanation import (
        TableWriteRouteMetadataExplanation,
    )


T = TypeVar("T", bound="TableWriteRouteExplanation")


@_attrs_define
class TableWriteRouteExplanation:
    """The full DML write-planner route decision (explain-only; nothing
    was written). `write_mode`/`distribution` echo the resolved
    canonical enum names (PascalCase, e.g. `Append`,
    `PseudoDistributed`). `execution_*` fields appear only for
    EXPLAIN ANALYZE (never set by this REST explain path).

        Attributes:
            target_table (str):
            source (str):
            write_mode (str):
            distribution (str):
            write_intent (TableWriteIntentExplanation):
            write_lane (str):
            write_lane_reason (str):
            write_lane_required_guards (list[str]):
            rejected_write_lanes (list[TableWriteRejectedLaneExplanation]):
            selected_backend (str):
            selected_access_method (str):
            estimated_cost (TableWriteCostExplanation):
            data_movement (TableWriteDataMovementExplanation):
            required_guards (list[str]):
            route_metadata (TableWriteRouteMetadataExplanation):
            candidate_paths (list[TableWriteCandidateExplanation]):
            rejected_paths (list[TableWriteRejectedPathExplanation]):
            execution_elapsed_us (int | Unset):
            execution_rows_written (int | Unset):
    """

    target_table: str
    source: str
    write_mode: str
    distribution: str
    write_intent: TableWriteIntentExplanation
    write_lane: str
    write_lane_reason: str
    write_lane_required_guards: list[str]
    rejected_write_lanes: list[TableWriteRejectedLaneExplanation]
    selected_backend: str
    selected_access_method: str
    estimated_cost: TableWriteCostExplanation
    data_movement: TableWriteDataMovementExplanation
    required_guards: list[str]
    route_metadata: TableWriteRouteMetadataExplanation
    candidate_paths: list[TableWriteCandidateExplanation]
    rejected_paths: list[TableWriteRejectedPathExplanation]
    execution_elapsed_us: int | Unset = UNSET
    execution_rows_written: int | Unset = UNSET
    additional_properties: dict[str, Any] = _attrs_field(init=False, factory=dict)

    def to_dict(self) -> dict[str, Any]:
        from ..models.table_write_candidate_explanation import (
            TableWriteCandidateExplanation,
        )
        from ..models.table_write_cost_explanation import TableWriteCostExplanation
        from ..models.table_write_data_movement_explanation import (
            TableWriteDataMovementExplanation,
        )
        from ..models.table_write_intent_explanation import TableWriteIntentExplanation
        from ..models.table_write_rejected_lane_explanation import (
            TableWriteRejectedLaneExplanation,
        )
        from ..models.table_write_rejected_path_explanation import (
            TableWriteRejectedPathExplanation,
        )
        from ..models.table_write_route_metadata_explanation import (
            TableWriteRouteMetadataExplanation,
        )

        target_table = self.target_table

        source = self.source

        write_mode = self.write_mode

        distribution = self.distribution

        write_intent = self.write_intent.to_dict()

        write_lane = self.write_lane

        write_lane_reason = self.write_lane_reason

        write_lane_required_guards = self.write_lane_required_guards

        rejected_write_lanes = []
        for rejected_write_lanes_item_data in self.rejected_write_lanes:
            rejected_write_lanes_item = rejected_write_lanes_item_data.to_dict()
            rejected_write_lanes.append(rejected_write_lanes_item)

        selected_backend = self.selected_backend

        selected_access_method = self.selected_access_method

        estimated_cost = self.estimated_cost.to_dict()

        data_movement = self.data_movement.to_dict()

        required_guards = self.required_guards

        route_metadata = self.route_metadata.to_dict()

        candidate_paths = []
        for candidate_paths_item_data in self.candidate_paths:
            candidate_paths_item = candidate_paths_item_data.to_dict()
            candidate_paths.append(candidate_paths_item)

        rejected_paths = []
        for rejected_paths_item_data in self.rejected_paths:
            rejected_paths_item = rejected_paths_item_data.to_dict()
            rejected_paths.append(rejected_paths_item)

        execution_elapsed_us = self.execution_elapsed_us

        execution_rows_written = self.execution_rows_written

        field_dict: dict[str, Any] = {}
        field_dict.update(self.additional_properties)
        field_dict.update(
            {
                "target_table": target_table,
                "source": source,
                "write_mode": write_mode,
                "distribution": distribution,
                "write_intent": write_intent,
                "write_lane": write_lane,
                "write_lane_reason": write_lane_reason,
                "write_lane_required_guards": write_lane_required_guards,
                "rejected_write_lanes": rejected_write_lanes,
                "selected_backend": selected_backend,
                "selected_access_method": selected_access_method,
                "estimated_cost": estimated_cost,
                "data_movement": data_movement,
                "required_guards": required_guards,
                "route_metadata": route_metadata,
                "candidate_paths": candidate_paths,
                "rejected_paths": rejected_paths,
            }
        )
        if execution_elapsed_us is not UNSET:
            field_dict["execution_elapsed_us"] = execution_elapsed_us
        if execution_rows_written is not UNSET:
            field_dict["execution_rows_written"] = execution_rows_written

        return field_dict

    @classmethod
    def from_dict(cls: type[T], src_dict: Mapping[str, Any]) -> T:
        from ..models.table_write_candidate_explanation import (
            TableWriteCandidateExplanation,
        )
        from ..models.table_write_cost_explanation import TableWriteCostExplanation
        from ..models.table_write_data_movement_explanation import (
            TableWriteDataMovementExplanation,
        )
        from ..models.table_write_intent_explanation import TableWriteIntentExplanation
        from ..models.table_write_rejected_lane_explanation import (
            TableWriteRejectedLaneExplanation,
        )
        from ..models.table_write_rejected_path_explanation import (
            TableWriteRejectedPathExplanation,
        )
        from ..models.table_write_route_metadata_explanation import (
            TableWriteRouteMetadataExplanation,
        )

        d = dict(src_dict)
        target_table = d.pop("target_table")

        source = d.pop("source")

        write_mode = d.pop("write_mode")

        distribution = d.pop("distribution")

        write_intent = TableWriteIntentExplanation.from_dict(d.pop("write_intent"))

        write_lane = d.pop("write_lane")

        write_lane_reason = d.pop("write_lane_reason")

        write_lane_required_guards = cast(
            list[str], d.pop("write_lane_required_guards")
        )

        rejected_write_lanes = []
        _rejected_write_lanes = d.pop("rejected_write_lanes")
        for rejected_write_lanes_item_data in _rejected_write_lanes:
            rejected_write_lanes_item = TableWriteRejectedLaneExplanation.from_dict(
                rejected_write_lanes_item_data
            )

            rejected_write_lanes.append(rejected_write_lanes_item)

        selected_backend = d.pop("selected_backend")

        selected_access_method = d.pop("selected_access_method")

        estimated_cost = TableWriteCostExplanation.from_dict(d.pop("estimated_cost"))

        data_movement = TableWriteDataMovementExplanation.from_dict(
            d.pop("data_movement")
        )

        required_guards = cast(list[str], d.pop("required_guards"))

        route_metadata = TableWriteRouteMetadataExplanation.from_dict(
            d.pop("route_metadata")
        )

        candidate_paths = []
        _candidate_paths = d.pop("candidate_paths")
        for candidate_paths_item_data in _candidate_paths:
            candidate_paths_item = TableWriteCandidateExplanation.from_dict(
                candidate_paths_item_data
            )

            candidate_paths.append(candidate_paths_item)

        rejected_paths = []
        _rejected_paths = d.pop("rejected_paths")
        for rejected_paths_item_data in _rejected_paths:
            rejected_paths_item = TableWriteRejectedPathExplanation.from_dict(
                rejected_paths_item_data
            )

            rejected_paths.append(rejected_paths_item)

        execution_elapsed_us = d.pop("execution_elapsed_us", UNSET)

        execution_rows_written = d.pop("execution_rows_written", UNSET)

        table_write_route_explanation = cls(
            target_table=target_table,
            source=source,
            write_mode=write_mode,
            distribution=distribution,
            write_intent=write_intent,
            write_lane=write_lane,
            write_lane_reason=write_lane_reason,
            write_lane_required_guards=write_lane_required_guards,
            rejected_write_lanes=rejected_write_lanes,
            selected_backend=selected_backend,
            selected_access_method=selected_access_method,
            estimated_cost=estimated_cost,
            data_movement=data_movement,
            required_guards=required_guards,
            route_metadata=route_metadata,
            candidate_paths=candidate_paths,
            rejected_paths=rejected_paths,
            execution_elapsed_us=execution_elapsed_us,
            execution_rows_written=execution_rows_written,
        )

        table_write_route_explanation.additional_properties = d
        return table_write_route_explanation

    @property
    def additional_keys(self) -> list[str]:
        return list(self.additional_properties.keys())

    def __getitem__(self, key: str) -> Any:
        return self.additional_properties[key]

    def __setitem__(self, key: str, value: Any) -> None:
        self.additional_properties[key] = value

    def __delitem__(self, key: str) -> None:
        del self.additional_properties[key]

    def __contains__(self, key: str) -> bool:
        return key in self.additional_properties
