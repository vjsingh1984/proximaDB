# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from __future__ import annotations

from collections.abc import Mapping
from typing import TYPE_CHECKING, Any, BinaryIO, Generator, TextIO, TypeVar, cast

from attrs import define as _attrs_define
from attrs import field as _attrs_field

from ..types import UNSET, Unset

if TYPE_CHECKING:
    from ..models.canonical_edge import CanonicalEdge


T = TypeVar("T", bound="GraphEdgeBatchResults")


@_attrs_define
class GraphEdgeBatchResults:
    """
    Attributes:
        created_count (int):
        updated_count (int):
        failed_count (int):
        results (list[CanonicalEdge]):
        errors (list[Any]):
    """

    created_count: int
    updated_count: int
    failed_count: int
    results: list[CanonicalEdge]
    errors: list[Any]
    additional_properties: dict[str, Any] = _attrs_field(init=False, factory=dict)

    def to_dict(self) -> dict[str, Any]:
        from ..models.canonical_edge import CanonicalEdge

        created_count = self.created_count

        updated_count = self.updated_count

        failed_count = self.failed_count

        results = []
        for results_item_data in self.results:
            results_item = results_item_data.to_dict()
            results.append(results_item)

        errors = self.errors

        field_dict: dict[str, Any] = {}
        field_dict.update(self.additional_properties)
        field_dict.update(
            {
                "created_count": created_count,
                "updated_count": updated_count,
                "failed_count": failed_count,
                "results": results,
                "errors": errors,
            }
        )

        return field_dict

    @classmethod
    def from_dict(cls: type[T], src_dict: Mapping[str, Any]) -> T:
        from ..models.canonical_edge import CanonicalEdge

        d = dict(src_dict)
        created_count = d.pop("created_count")

        updated_count = d.pop("updated_count")

        failed_count = d.pop("failed_count")

        results = []
        _results = d.pop("results")
        for results_item_data in _results:
            results_item = CanonicalEdge.from_dict(results_item_data)

            results.append(results_item)

        errors = cast(list[Any], d.pop("errors"))

        graph_edge_batch_results = cls(
            created_count=created_count,
            updated_count=updated_count,
            failed_count=failed_count,
            results=results,
            errors=errors,
        )

        graph_edge_batch_results.additional_properties = d
        return graph_edge_batch_results

    @property
    def additional_keys(self) -> list[str]:
        return list(self.additional_properties.keys())

    def __getitem__(self, key: str) -> Any:
        return self.additional_properties[key]

    def __setitem__(self, key: str, value: Any) -> None:
        self.additional_properties[key] = value

    def __delitem__(self, key: str) -> None:
        del self.additional_properties[key]

    def __contains__(self, key: str) -> bool:
        return key in self.additional_properties
