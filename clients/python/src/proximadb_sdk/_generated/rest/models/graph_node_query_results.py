# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from __future__ import annotations

from collections.abc import Mapping
from typing import TYPE_CHECKING, Any, BinaryIO, Generator, TextIO, TypeVar, cast

from attrs import define as _attrs_define
from attrs import field as _attrs_field

from ..types import UNSET, Unset

if TYPE_CHECKING:
    from ..models.canonical_node import CanonicalNode


T = TypeVar("T", bound="GraphNodeQueryResults")


@_attrs_define
class GraphNodeQueryResults:
    """
    Attributes:
        items (list[CanonicalNode]):
        has_more (bool):
        total_count (int | Unset):
        next_token (str | Unset):
    """

    items: list[CanonicalNode]
    has_more: bool
    total_count: int | Unset = UNSET
    next_token: str | Unset = UNSET
    additional_properties: dict[str, Any] = _attrs_field(init=False, factory=dict)

    def to_dict(self) -> dict[str, Any]:
        from ..models.canonical_node import CanonicalNode

        items = []
        for items_item_data in self.items:
            items_item = items_item_data.to_dict()
            items.append(items_item)

        has_more = self.has_more

        total_count = self.total_count

        next_token = self.next_token

        field_dict: dict[str, Any] = {}
        field_dict.update(self.additional_properties)
        field_dict.update(
            {
                "items": items,
                "has_more": has_more,
            }
        )
        if total_count is not UNSET:
            field_dict["total_count"] = total_count
        if next_token is not UNSET:
            field_dict["next_token"] = next_token

        return field_dict

    @classmethod
    def from_dict(cls: type[T], src_dict: Mapping[str, Any]) -> T:
        from ..models.canonical_node import CanonicalNode

        d = dict(src_dict)
        items = []
        _items = d.pop("items")
        for items_item_data in _items:
            items_item = CanonicalNode.from_dict(items_item_data)

            items.append(items_item)

        has_more = d.pop("has_more")

        total_count = d.pop("total_count", UNSET)

        next_token = d.pop("next_token", UNSET)

        graph_node_query_results = cls(
            items=items,
            has_more=has_more,
            total_count=total_count,
            next_token=next_token,
        )

        graph_node_query_results.additional_properties = d
        return graph_node_query_results

    @property
    def additional_keys(self) -> list[str]:
        return list(self.additional_properties.keys())

    def __getitem__(self, key: str) -> Any:
        return self.additional_properties[key]

    def __setitem__(self, key: str, value: Any) -> None:
        self.additional_properties[key] = value

    def __delitem__(self, key: str) -> None:
        del self.additional_properties[key]

    def __contains__(self, key: str) -> bool:
        return key in self.additional_properties
