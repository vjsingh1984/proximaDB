# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from __future__ import annotations

from collections.abc import Mapping
from typing import TYPE_CHECKING, Any, BinaryIO, Generator, TextIO, TypeVar, cast

from attrs import define as _attrs_define
from attrs import field as _attrs_field

from ..types import UNSET, Unset

if TYPE_CHECKING:
    from ..models.primary_pod import PrimaryPod


T = TypeVar("T", bound="PrimaryPodAssignResponse")


@_attrs_define
class PrimaryPodAssignResponse:
    """
    Attributes:
        tenant_id (str):
        collection_id (str):
        primary (PrimaryPod):
        previous (None | PrimaryPod | Unset): The prior binding on re-assignment; null on first assignment.
    """

    tenant_id: str
    collection_id: str
    primary: PrimaryPod
    previous: None | PrimaryPod | Unset = UNSET
    additional_properties: dict[str, Any] = _attrs_field(init=False, factory=dict)

    def to_dict(self) -> dict[str, Any]:
        from ..models.primary_pod import PrimaryPod

        tenant_id = self.tenant_id

        collection_id = self.collection_id

        primary = self.primary.to_dict()

        previous: dict[str, Any] | None | Unset
        if isinstance(self.previous, Unset):
            previous = UNSET
        elif isinstance(self.previous, PrimaryPod):
            previous = self.previous.to_dict()
        else:
            previous = self.previous

        field_dict: dict[str, Any] = {}
        field_dict.update(self.additional_properties)
        field_dict.update(
            {
                "tenant_id": tenant_id,
                "collection_id": collection_id,
                "primary": primary,
            }
        )
        if previous is not UNSET:
            field_dict["previous"] = previous

        return field_dict

    @classmethod
    def from_dict(cls: type[T], src_dict: Mapping[str, Any]) -> T:
        from ..models.primary_pod import PrimaryPod

        d = dict(src_dict)
        tenant_id = d.pop("tenant_id")

        collection_id = d.pop("collection_id")

        primary = PrimaryPod.from_dict(d.pop("primary"))

        def _parse_previous(data: object) -> None | PrimaryPod | Unset:
            if data is None:
                return data
            if isinstance(data, Unset):
                return data
            try:
                if not isinstance(data, dict):
                    raise TypeError()
                previous_type_0 = PrimaryPod.from_dict(data)

                return previous_type_0
            except (TypeError, ValueError, AttributeError, KeyError):
                pass
            return cast(None | PrimaryPod | Unset, data)

        previous = _parse_previous(d.pop("previous", UNSET))

        primary_pod_assign_response = cls(
            tenant_id=tenant_id,
            collection_id=collection_id,
            primary=primary,
            previous=previous,
        )

        primary_pod_assign_response.additional_properties = d
        return primary_pod_assign_response

    @property
    def additional_keys(self) -> list[str]:
        return list(self.additional_properties.keys())

    def __getitem__(self, key: str) -> Any:
        return self.additional_properties[key]

    def __setitem__(self, key: str, value: Any) -> None:
        self.additional_properties[key] = value

    def __delitem__(self, key: str) -> None:
        del self.additional_properties[key]

    def __contains__(self, key: str) -> bool:
        return key in self.additional_properties
