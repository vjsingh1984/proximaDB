# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from __future__ import annotations

from collections.abc import Mapping
from typing import TYPE_CHECKING, Any, BinaryIO, Generator, TextIO, TypeVar, cast

from attrs import define as _attrs_define
from attrs import field as _attrs_field

from ..types import UNSET, Unset

if TYPE_CHECKING:
    from ..models.canonical_edge_properties import CanonicalEdgeProperties


T = TypeVar("T", bound="CanonicalEdge")


@_attrs_define
class CanonicalEdge:
    """
    Attributes:
        id (str):
        from_node_id (str):
        to_node_id (str):
        edge_type (str):
        properties (CanonicalEdgeProperties):
        created_at (str):
        updated_at (str):
        weight (float | Unset):
    """

    id: str
    from_node_id: str
    to_node_id: str
    edge_type: str
    properties: CanonicalEdgeProperties
    created_at: str
    updated_at: str
    weight: float | Unset = UNSET
    additional_properties: dict[str, Any] = _attrs_field(init=False, factory=dict)

    def to_dict(self) -> dict[str, Any]:
        from ..models.canonical_edge_properties import CanonicalEdgeProperties

        id = self.id

        from_node_id = self.from_node_id

        to_node_id = self.to_node_id

        edge_type = self.edge_type

        properties = self.properties.to_dict()

        created_at = self.created_at

        updated_at = self.updated_at

        weight = self.weight

        field_dict: dict[str, Any] = {}
        field_dict.update(self.additional_properties)
        field_dict.update(
            {
                "id": id,
                "from_node_id": from_node_id,
                "to_node_id": to_node_id,
                "edge_type": edge_type,
                "properties": properties,
                "created_at": created_at,
                "updated_at": updated_at,
            }
        )
        if weight is not UNSET:
            field_dict["weight"] = weight

        return field_dict

    @classmethod
    def from_dict(cls: type[T], src_dict: Mapping[str, Any]) -> T:
        from ..models.canonical_edge_properties import CanonicalEdgeProperties

        d = dict(src_dict)
        id = d.pop("id")

        from_node_id = d.pop("from_node_id")

        to_node_id = d.pop("to_node_id")

        edge_type = d.pop("edge_type")

        properties = CanonicalEdgeProperties.from_dict(d.pop("properties"))

        created_at = d.pop("created_at")

        updated_at = d.pop("updated_at")

        weight = d.pop("weight", UNSET)

        canonical_edge = cls(
            id=id,
            from_node_id=from_node_id,
            to_node_id=to_node_id,
            edge_type=edge_type,
            properties=properties,
            created_at=created_at,
            updated_at=updated_at,
            weight=weight,
        )

        canonical_edge.additional_properties = d
        return canonical_edge

    @property
    def additional_keys(self) -> list[str]:
        return list(self.additional_properties.keys())

    def __getitem__(self, key: str) -> Any:
        return self.additional_properties[key]

    def __setitem__(self, key: str, value: Any) -> None:
        self.additional_properties[key] = value

    def __delitem__(self, key: str) -> None:
        del self.additional_properties[key]

    def __contains__(self, key: str) -> bool:
        return key in self.additional_properties
