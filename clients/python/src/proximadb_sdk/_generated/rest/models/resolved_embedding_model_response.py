# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from __future__ import annotations

from collections.abc import Mapping
from typing import TYPE_CHECKING, Any, BinaryIO, Generator, TextIO, TypeVar, cast

from attrs import define as _attrs_define
from attrs import field as _attrs_field

from ..types import UNSET, Unset

if TYPE_CHECKING:
    from ..models.catalog_embedding_model_version import CatalogEmbeddingModelVersion


T = TypeVar("T", bound="ResolvedEmbeddingModelResponse")


@_attrs_define
class ResolvedEmbeddingModelResponse:
    """
    Attributes:
        asset_id (str):
        contract_sha256 (str):
        model (CatalogEmbeddingModelVersion): Immutable executable contract for one registered model version.
        registry_name (str):
        registry_revision (int):
    """

    asset_id: str
    contract_sha256: str
    model: CatalogEmbeddingModelVersion
    registry_name: str
    registry_revision: int
    additional_properties: dict[str, Any] = _attrs_field(init=False, factory=dict)

    def to_dict(self) -> dict[str, Any]:
        from ..models.catalog_embedding_model_version import (
            CatalogEmbeddingModelVersion,
        )

        asset_id = self.asset_id

        contract_sha256 = self.contract_sha256

        model = self.model.to_dict()

        registry_name = self.registry_name

        registry_revision = self.registry_revision

        field_dict: dict[str, Any] = {}
        field_dict.update(self.additional_properties)
        field_dict.update(
            {
                "asset_id": asset_id,
                "contract_sha256": contract_sha256,
                "model": model,
                "registry_name": registry_name,
                "registry_revision": registry_revision,
            }
        )

        return field_dict

    @classmethod
    def from_dict(cls: type[T], src_dict: Mapping[str, Any]) -> T:
        from ..models.catalog_embedding_model_version import (
            CatalogEmbeddingModelVersion,
        )

        d = dict(src_dict)
        asset_id = d.pop("asset_id")

        contract_sha256 = d.pop("contract_sha256")

        model = CatalogEmbeddingModelVersion.from_dict(d.pop("model"))

        registry_name = d.pop("registry_name")

        registry_revision = d.pop("registry_revision")

        resolved_embedding_model_response = cls(
            asset_id=asset_id,
            contract_sha256=contract_sha256,
            model=model,
            registry_name=registry_name,
            registry_revision=registry_revision,
        )

        resolved_embedding_model_response.additional_properties = d
        return resolved_embedding_model_response

    @property
    def additional_keys(self) -> list[str]:
        return list(self.additional_properties.keys())

    def __getitem__(self, key: str) -> Any:
        return self.additional_properties[key]

    def __setitem__(self, key: str, value: Any) -> None:
        self.additional_properties[key] = value

    def __delitem__(self, key: str) -> None:
        del self.additional_properties[key]

    def __contains__(self, key: str) -> bool:
        return key in self.additional_properties
