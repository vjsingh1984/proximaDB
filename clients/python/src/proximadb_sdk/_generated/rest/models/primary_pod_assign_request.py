# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from __future__ import annotations

from collections.abc import Mapping
from typing import TYPE_CHECKING, Any, BinaryIO, Generator, TextIO, TypeVar

from attrs import define as _attrs_define
from attrs import field as _attrs_field

from ..models.assignment_reason import AssignmentReason
from ..types import UNSET, Unset

T = TypeVar("T", bound="PrimaryPodAssignRequest")


@_attrs_define
class PrimaryPodAssignRequest:
    """Body for `PUT /api/v2/primary-pod/{tenant_id}/{collection_id}`.
    `reason` defaults to `"operator"` when omitted.

        Attributes:
            pod (str):
            reason (AssignmentReason | Unset): Why an assignment was made (locked vocabulary for dashboards /
                EXPLAIN). `catalog_replay` = loaded from durable state at
                restart, not freshly assigned.
    """

    pod: str
    reason: AssignmentReason | Unset = UNSET
    additional_properties: dict[str, Any] = _attrs_field(init=False, factory=dict)

    def to_dict(self) -> dict[str, Any]:
        pod = self.pod

        reason: str | Unset = UNSET
        if not isinstance(self.reason, Unset):
            reason = self.reason.value

        field_dict: dict[str, Any] = {}
        field_dict.update(self.additional_properties)
        field_dict.update(
            {
                "pod": pod,
            }
        )
        if reason is not UNSET:
            field_dict["reason"] = reason

        return field_dict

    @classmethod
    def from_dict(cls: type[T], src_dict: Mapping[str, Any]) -> T:
        d = dict(src_dict)
        pod = d.pop("pod")

        _reason = d.pop("reason", UNSET)
        reason: AssignmentReason | Unset
        if isinstance(_reason, Unset):
            reason = UNSET
        else:
            reason = AssignmentReason(_reason)

        primary_pod_assign_request = cls(
            pod=pod,
            reason=reason,
        )

        primary_pod_assign_request.additional_properties = d
        return primary_pod_assign_request

    @property
    def additional_keys(self) -> list[str]:
        return list(self.additional_properties.keys())

    def __getitem__(self, key: str) -> Any:
        return self.additional_properties[key]

    def __setitem__(self, key: str, value: Any) -> None:
        self.additional_properties[key] = value

    def __delitem__(self, key: str) -> None:
        del self.additional_properties[key]

    def __contains__(self, key: str) -> bool:
        return key in self.additional_properties
