# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from __future__ import annotations

from collections.abc import Mapping
from typing import TYPE_CHECKING, Any, BinaryIO, Generator, TextIO, TypeVar

from attrs import define as _attrs_define
from attrs import field as _attrs_field

from ..types import UNSET, Unset

T = TypeVar("T", bound="AbacOperatorErrorResponse")


@_attrs_define
class AbacOperatorErrorResponse:
    """The FLAT error shape every ABAC operator endpoint returns —
    `{error, message, code}` where `error` is a short machine-readable
    slug (e.g. `"tenant_unresolved"`). This is deliberately DISTINCT
    from the canonical nested `ErrorResponse` (`{error: {type,
    message, code}}`) used elsewhere in this spec; it is the actual
    wire format of `abac_admin::OperatorErrorResponse`, carried
    verbatim rather than normalized.

        Attributes:
            error (str): Machine-readable error slug.
            message (str):
            code (int): The HTTP status code, repeated in the body.
    """

    error: str
    message: str
    code: int
    additional_properties: dict[str, Any] = _attrs_field(init=False, factory=dict)

    def to_dict(self) -> dict[str, Any]:
        error = self.error

        message = self.message

        code = self.code

        field_dict: dict[str, Any] = {}
        field_dict.update(self.additional_properties)
        field_dict.update(
            {
                "error": error,
                "message": message,
                "code": code,
            }
        )

        return field_dict

    @classmethod
    def from_dict(cls: type[T], src_dict: Mapping[str, Any]) -> T:
        d = dict(src_dict)
        error = d.pop("error")

        message = d.pop("message")

        code = d.pop("code")

        abac_operator_error_response = cls(
            error=error,
            message=message,
            code=code,
        )

        abac_operator_error_response.additional_properties = d
        return abac_operator_error_response

    @property
    def additional_keys(self) -> list[str]:
        return list(self.additional_properties.keys())

    def __getitem__(self, key: str) -> Any:
        return self.additional_properties[key]

    def __setitem__(self, key: str, value: Any) -> None:
        self.additional_properties[key] = value

    def __delitem__(self, key: str) -> None:
        del self.additional_properties[key]

    def __contains__(self, key: str) -> bool:
        return key in self.additional_properties
