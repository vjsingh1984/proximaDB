# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from http import HTTPStatus
from typing import Any, cast
from urllib.parse import quote

import httpx

from ... import errors
from ...client import AuthenticatedClient, Client
from ...models.primary_pod_assign_request import PrimaryPodAssignRequest
from ...models.primary_pod_assign_response import PrimaryPodAssignResponse
from ...models.primary_pod_operator_error_response import (
    PrimaryPodOperatorErrorResponse,
)
from ...types import UNSET, Response, Unset


def _get_kwargs(
    tenant_id: str,
    collection_id: str,
    *,
    body: PrimaryPodAssignRequest,
    x_tenant_id: str | Unset = UNSET,
) -> dict[str, Any]:
    headers: dict[str, Any] = {}
    if not isinstance(x_tenant_id, Unset):
        headers["X-Tenant-ID"] = x_tenant_id

    _kwargs: dict[str, Any] = {
        "method": "put",
        "url": "/api/v2/primary-pod/{tenant_id}/{collection_id}".format(
            tenant_id=quote(str(tenant_id), safe=""),
            collection_id=quote(str(collection_id), safe=""),
        ),
    }

    _kwargs["json"] = body.to_dict()

    headers["Content-Type"] = "application/json"

    _kwargs["headers"] = headers
    return _kwargs


def _parse_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> PrimaryPodAssignResponse | PrimaryPodOperatorErrorResponse | None:
    if response.status_code == 200:
        response_200 = PrimaryPodAssignResponse.from_dict(response.json())

        return response_200

    if response.status_code == 401:
        response_401 = PrimaryPodOperatorErrorResponse.from_dict(response.json())

        return response_401

    if response.status_code == 403:
        response_403 = PrimaryPodOperatorErrorResponse.from_dict(response.json())

        return response_403

    if client.raise_on_unexpected_status:
        raise errors.UnexpectedStatus(response.status_code, response.content)
    else:
        return None


def _build_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> Response[PrimaryPodAssignResponse | PrimaryPodOperatorErrorResponse]:
    return Response(
        status_code=HTTPStatus(response.status_code),
        content=response.content,
        headers=response.headers,
        parsed=_parse_response(client=client, response=response),
    )


def sync_detailed(
    tenant_id: str,
    collection_id: str,
    *,
    client: AuthenticatedClient | Client,
    body: PrimaryPodAssignRequest,
    x_tenant_id: str | Unset = UNSET,
) -> Response[PrimaryPodAssignResponse | PrimaryPodOperatorErrorResponse]:
    """Assign a (tenant, collection)'s primary pod.

     Operator-gated (SystemAdmin ∪ ConfigureSystem) — these expose
    cross-tenant placement and drive WAL write routing. The
    assignment is mirrored to the catalog; a mirror failure is
    logged and counted but does NOT fail the request (PUT-success
    means the binding is in effect for routing; the catalog
    reconciles on the next write).
    The optional `X-Tenant-ID` header is not consulted by this
    handler — the `tenant_id` PATH segment governs.
    Requires REST auth enabled — in an auth-disabled
    deployment every call to this operation returns 401
    `missing_auth_context`.

    Args:
        tenant_id (str):
        collection_id (str):
        x_tenant_id (str | Unset):
        body (PrimaryPodAssignRequest): Body for `PUT /api/v2/primary-
            pod/{tenant_id}/{collection_id}`.
            `reason` defaults to `"operator"` when omitted.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[PrimaryPodAssignResponse | PrimaryPodOperatorErrorResponse]
    """

    kwargs = _get_kwargs(
        tenant_id=tenant_id,
        collection_id=collection_id,
        body=body,
        x_tenant_id=x_tenant_id,
    )

    response = client.get_httpx_client().request(
        **kwargs,
    )

    return _build_response(client=client, response=response)


def sync(
    tenant_id: str,
    collection_id: str,
    *,
    client: AuthenticatedClient | Client,
    body: PrimaryPodAssignRequest,
    x_tenant_id: str | Unset = UNSET,
) -> PrimaryPodAssignResponse | PrimaryPodOperatorErrorResponse | None:
    """Assign a (tenant, collection)'s primary pod.

     Operator-gated (SystemAdmin ∪ ConfigureSystem) — these expose
    cross-tenant placement and drive WAL write routing. The
    assignment is mirrored to the catalog; a mirror failure is
    logged and counted but does NOT fail the request (PUT-success
    means the binding is in effect for routing; the catalog
    reconciles on the next write).
    The optional `X-Tenant-ID` header is not consulted by this
    handler — the `tenant_id` PATH segment governs.
    Requires REST auth enabled — in an auth-disabled
    deployment every call to this operation returns 401
    `missing_auth_context`.

    Args:
        tenant_id (str):
        collection_id (str):
        x_tenant_id (str | Unset):
        body (PrimaryPodAssignRequest): Body for `PUT /api/v2/primary-
            pod/{tenant_id}/{collection_id}`.
            `reason` defaults to `"operator"` when omitted.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        PrimaryPodAssignResponse | PrimaryPodOperatorErrorResponse
    """

    return sync_detailed(
        tenant_id=tenant_id,
        collection_id=collection_id,
        client=client,
        body=body,
        x_tenant_id=x_tenant_id,
    ).parsed


async def asyncio_detailed(
    tenant_id: str,
    collection_id: str,
    *,
    client: AuthenticatedClient | Client,
    body: PrimaryPodAssignRequest,
    x_tenant_id: str | Unset = UNSET,
) -> Response[PrimaryPodAssignResponse | PrimaryPodOperatorErrorResponse]:
    """Assign a (tenant, collection)'s primary pod.

     Operator-gated (SystemAdmin ∪ ConfigureSystem) — these expose
    cross-tenant placement and drive WAL write routing. The
    assignment is mirrored to the catalog; a mirror failure is
    logged and counted but does NOT fail the request (PUT-success
    means the binding is in effect for routing; the catalog
    reconciles on the next write).
    The optional `X-Tenant-ID` header is not consulted by this
    handler — the `tenant_id` PATH segment governs.
    Requires REST auth enabled — in an auth-disabled
    deployment every call to this operation returns 401
    `missing_auth_context`.

    Args:
        tenant_id (str):
        collection_id (str):
        x_tenant_id (str | Unset):
        body (PrimaryPodAssignRequest): Body for `PUT /api/v2/primary-
            pod/{tenant_id}/{collection_id}`.
            `reason` defaults to `"operator"` when omitted.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[PrimaryPodAssignResponse | PrimaryPodOperatorErrorResponse]
    """

    kwargs = _get_kwargs(
        tenant_id=tenant_id,
        collection_id=collection_id,
        body=body,
        x_tenant_id=x_tenant_id,
    )

    response = await client.get_async_httpx_client().request(**kwargs)

    return _build_response(client=client, response=response)


async def asyncio(
    tenant_id: str,
    collection_id: str,
    *,
    client: AuthenticatedClient | Client,
    body: PrimaryPodAssignRequest,
    x_tenant_id: str | Unset = UNSET,
) -> PrimaryPodAssignResponse | PrimaryPodOperatorErrorResponse | None:
    """Assign a (tenant, collection)'s primary pod.

     Operator-gated (SystemAdmin ∪ ConfigureSystem) — these expose
    cross-tenant placement and drive WAL write routing. The
    assignment is mirrored to the catalog; a mirror failure is
    logged and counted but does NOT fail the request (PUT-success
    means the binding is in effect for routing; the catalog
    reconciles on the next write).
    The optional `X-Tenant-ID` header is not consulted by this
    handler — the `tenant_id` PATH segment governs.
    Requires REST auth enabled — in an auth-disabled
    deployment every call to this operation returns 401
    `missing_auth_context`.

    Args:
        tenant_id (str):
        collection_id (str):
        x_tenant_id (str | Unset):
        body (PrimaryPodAssignRequest): Body for `PUT /api/v2/primary-
            pod/{tenant_id}/{collection_id}`.
            `reason` defaults to `"operator"` when omitted.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        PrimaryPodAssignResponse | PrimaryPodOperatorErrorResponse
    """

    return (
        await asyncio_detailed(
            tenant_id=tenant_id,
            collection_id=collection_id,
            client=client,
            body=body,
            x_tenant_id=x_tenant_id,
        )
    ).parsed
