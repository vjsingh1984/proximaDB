# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from http import HTTPStatus
from typing import Any, cast
from urllib.parse import quote

import httpx

from ... import errors
from ...client import AuthenticatedClient, Client
from ...models.error_response import ErrorResponse
from ...models.table_write_explain_request import TableWriteExplainRequest
from ...models.table_write_route_explanation import TableWriteRouteExplanation
from ...types import UNSET, Response, Unset


def _get_kwargs(
    *,
    body: TableWriteExplainRequest,
    x_tenant_id: str | Unset = UNSET,
) -> dict[str, Any]:
    headers: dict[str, Any] = {}
    if not isinstance(x_tenant_id, Unset):
        headers["X-Tenant-ID"] = x_tenant_id

    _kwargs: dict[str, Any] = {
        "method": "post",
        "url": "/api/v2/catalog/table-write/explain",
    }

    _kwargs["json"] = body.to_dict()

    headers["Content-Type"] = "application/json"

    _kwargs["headers"] = headers
    return _kwargs


def _parse_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> ErrorResponse | TableWriteRouteExplanation | None:
    if response.status_code == 200:
        response_200 = TableWriteRouteExplanation.from_dict(response.json())

        return response_200

    if response.status_code == 400:
        response_400 = ErrorResponse.from_dict(response.json())

        return response_400

    if response.status_code == 404:
        response_404 = ErrorResponse.from_dict(response.json())

        return response_404

    if response.status_code == 500:
        response_500 = ErrorResponse.from_dict(response.json())

        return response_500

    if client.raise_on_unexpected_status:
        raise errors.UnexpectedStatus(response.status_code, response.content)
    else:
        return None


def _build_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> Response[ErrorResponse | TableWriteRouteExplanation]:
    return Response(
        status_code=HTTPStatus(response.status_code),
        content=response.content,
        headers=response.headers,
        parsed=_parse_response(client=client, response=response),
    )


def sync_detailed(
    *,
    client: AuthenticatedClient | Client,
    body: TableWriteExplainRequest,
    x_tenant_id: str | Unset = UNSET,
) -> Response[ErrorResponse | TableWriteRouteExplanation]:
    """Explain table-write route selection WITHOUT executing.

     Runs the DML write planner in explain-only mode: resolves the
    target (and source) tables, plans the copy/write, and returns
    the full route decision — selected backend + access method,
    write lane (+ rejected lanes with reasons), candidate/rejected
    paths, estimated cost and data movement, required guards, and
    the write intent summary. Nothing is written.
    Provide EITHER `source_table` OR `source_sql` (400 otherwise).
    Extractor rejections (malformed JSON body, duplicate query
    params) bypass the JSON error envelope and return axum's
    plain-text 400/415/422 bodies — the typed envelope below covers
    handler-emitted errors only.
    The optional `X-Tenant-ID` header is not consulted — tenant
    context, if any, rides the body's `tenant_id`.

    Args:
        x_tenant_id (str | Unset):
        body (TableWriteExplainRequest): Body for `POST /api/v2/catalog/table-write/explain`.
            Provide
            EITHER `source_table` OR `source_sql` — both, or neither, is a
            400. `write_mode` (default `append`) accepts, case-insensitive:
            append | insert | insert_only | insert-only | upsert | overwrite
            | insert_overwrite | insert-overwrite | overwrite_table |
            overwrite-table | merge. `distribution` (default `auto`) accepts:
            auto | local | local_only | local-only | pseudo |
            pseudo_distributed | pseudo-distributed | distributed. Upsert/
            merge imply upsert conflict policy; everything else errors on
            conflict.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[ErrorResponse | TableWriteRouteExplanation]
    """

    kwargs = _get_kwargs(
        body=body,
        x_tenant_id=x_tenant_id,
    )

    response = client.get_httpx_client().request(
        **kwargs,
    )

    return _build_response(client=client, response=response)


def sync(
    *,
    client: AuthenticatedClient | Client,
    body: TableWriteExplainRequest,
    x_tenant_id: str | Unset = UNSET,
) -> ErrorResponse | TableWriteRouteExplanation | None:
    """Explain table-write route selection WITHOUT executing.

     Runs the DML write planner in explain-only mode: resolves the
    target (and source) tables, plans the copy/write, and returns
    the full route decision — selected backend + access method,
    write lane (+ rejected lanes with reasons), candidate/rejected
    paths, estimated cost and data movement, required guards, and
    the write intent summary. Nothing is written.
    Provide EITHER `source_table` OR `source_sql` (400 otherwise).
    Extractor rejections (malformed JSON body, duplicate query
    params) bypass the JSON error envelope and return axum's
    plain-text 400/415/422 bodies — the typed envelope below covers
    handler-emitted errors only.
    The optional `X-Tenant-ID` header is not consulted — tenant
    context, if any, rides the body's `tenant_id`.

    Args:
        x_tenant_id (str | Unset):
        body (TableWriteExplainRequest): Body for `POST /api/v2/catalog/table-write/explain`.
            Provide
            EITHER `source_table` OR `source_sql` — both, or neither, is a
            400. `write_mode` (default `append`) accepts, case-insensitive:
            append | insert | insert_only | insert-only | upsert | overwrite
            | insert_overwrite | insert-overwrite | overwrite_table |
            overwrite-table | merge. `distribution` (default `auto`) accepts:
            auto | local | local_only | local-only | pseudo |
            pseudo_distributed | pseudo-distributed | distributed. Upsert/
            merge imply upsert conflict policy; everything else errors on
            conflict.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        ErrorResponse | TableWriteRouteExplanation
    """

    return sync_detailed(
        client=client,
        body=body,
        x_tenant_id=x_tenant_id,
    ).parsed


async def asyncio_detailed(
    *,
    client: AuthenticatedClient | Client,
    body: TableWriteExplainRequest,
    x_tenant_id: str | Unset = UNSET,
) -> Response[ErrorResponse | TableWriteRouteExplanation]:
    """Explain table-write route selection WITHOUT executing.

     Runs the DML write planner in explain-only mode: resolves the
    target (and source) tables, plans the copy/write, and returns
    the full route decision — selected backend + access method,
    write lane (+ rejected lanes with reasons), candidate/rejected
    paths, estimated cost and data movement, required guards, and
    the write intent summary. Nothing is written.
    Provide EITHER `source_table` OR `source_sql` (400 otherwise).
    Extractor rejections (malformed JSON body, duplicate query
    params) bypass the JSON error envelope and return axum's
    plain-text 400/415/422 bodies — the typed envelope below covers
    handler-emitted errors only.
    The optional `X-Tenant-ID` header is not consulted — tenant
    context, if any, rides the body's `tenant_id`.

    Args:
        x_tenant_id (str | Unset):
        body (TableWriteExplainRequest): Body for `POST /api/v2/catalog/table-write/explain`.
            Provide
            EITHER `source_table` OR `source_sql` — both, or neither, is a
            400. `write_mode` (default `append`) accepts, case-insensitive:
            append | insert | insert_only | insert-only | upsert | overwrite
            | insert_overwrite | insert-overwrite | overwrite_table |
            overwrite-table | merge. `distribution` (default `auto`) accepts:
            auto | local | local_only | local-only | pseudo |
            pseudo_distributed | pseudo-distributed | distributed. Upsert/
            merge imply upsert conflict policy; everything else errors on
            conflict.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[ErrorResponse | TableWriteRouteExplanation]
    """

    kwargs = _get_kwargs(
        body=body,
        x_tenant_id=x_tenant_id,
    )

    response = await client.get_async_httpx_client().request(**kwargs)

    return _build_response(client=client, response=response)


async def asyncio(
    *,
    client: AuthenticatedClient | Client,
    body: TableWriteExplainRequest,
    x_tenant_id: str | Unset = UNSET,
) -> ErrorResponse | TableWriteRouteExplanation | None:
    """Explain table-write route selection WITHOUT executing.

     Runs the DML write planner in explain-only mode: resolves the
    target (and source) tables, plans the copy/write, and returns
    the full route decision — selected backend + access method,
    write lane (+ rejected lanes with reasons), candidate/rejected
    paths, estimated cost and data movement, required guards, and
    the write intent summary. Nothing is written.
    Provide EITHER `source_table` OR `source_sql` (400 otherwise).
    Extractor rejections (malformed JSON body, duplicate query
    params) bypass the JSON error envelope and return axum's
    plain-text 400/415/422 bodies — the typed envelope below covers
    handler-emitted errors only.
    The optional `X-Tenant-ID` header is not consulted — tenant
    context, if any, rides the body's `tenant_id`.

    Args:
        x_tenant_id (str | Unset):
        body (TableWriteExplainRequest): Body for `POST /api/v2/catalog/table-write/explain`.
            Provide
            EITHER `source_table` OR `source_sql` — both, or neither, is a
            400. `write_mode` (default `append`) accepts, case-insensitive:
            append | insert | insert_only | insert-only | upsert | overwrite
            | insert_overwrite | insert-overwrite | overwrite_table |
            overwrite-table | merge. `distribution` (default `auto`) accepts:
            auto | local | local_only | local-only | pseudo |
            pseudo_distributed | pseudo-distributed | distributed. Upsert/
            merge imply upsert conflict policy; everything else errors on
            conflict.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        ErrorResponse | TableWriteRouteExplanation
    """

    return (
        await asyncio_detailed(
            client=client,
            body=body,
            x_tenant_id=x_tenant_id,
        )
    ).parsed
