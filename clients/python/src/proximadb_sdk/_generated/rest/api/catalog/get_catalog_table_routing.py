# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from http import HTTPStatus
from typing import Any, cast
from urllib.parse import quote

import httpx

from ... import errors
from ...client import AuthenticatedClient, Client
from ...models.catalog_introspection_result import CatalogIntrospectionResult
from ...models.error_response import ErrorResponse
from ...types import UNSET, Response, Unset


def _get_kwargs(
    *,
    table_name: str | Unset = UNSET,
    x_tenant_id: str | Unset = UNSET,
) -> dict[str, Any]:
    headers: dict[str, Any] = {}
    if not isinstance(x_tenant_id, Unset):
        headers["X-Tenant-ID"] = x_tenant_id

    params: dict[str, Any] = {}

    params["table_name"] = table_name

    params = {k: v for k, v in params.items() if v is not UNSET and v is not None}

    _kwargs: dict[str, Any] = {
        "method": "get",
        "url": "/api/v2/catalog/table-routing",
        "params": params,
    }

    _kwargs["headers"] = headers
    return _kwargs


def _parse_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> CatalogIntrospectionResult | ErrorResponse | None:
    if response.status_code == 200:
        response_200 = CatalogIntrospectionResult.from_dict(response.json())

        return response_200

    if response.status_code == 500:
        response_500 = ErrorResponse.from_dict(response.json())

        return response_500

    if client.raise_on_unexpected_status:
        raise errors.UnexpectedStatus(response.status_code, response.content)
    else:
        return None


def _build_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> Response[CatalogIntrospectionResult | ErrorResponse]:
    return Response(
        status_code=HTTPStatus(response.status_code),
        content=response.content,
        headers=response.headers,
        parsed=_parse_response(client=client, response=response),
    )


def sync_detailed(
    *,
    client: AuthenticatedClient | Client,
    table_name: str | Unset = UNSET,
    x_tenant_id: str | Unset = UNSET,
) -> Response[CatalogIntrospectionResult | ErrorResponse]:
    """Table-level xCatalog routing metadata.

     Serves `information_schema.table_routing` as a table-shaped
    result — the same projection pgwire/SQL clients see. All cell
    values are strings.
    The optional `X-Tenant-ID` header is not consulted by this
    handler (the introspection projection is cluster-scope).
    The filter dispatch is LEXICAL over the composed SQL text, so a
    `table_name` containing SQL fragments (e.g. ` from
    xcatalog.namespaces`) can be misread as a different view — do
    not treat this endpoint as a structured query interface
    (TD-SPECRAT-3 tracks the structured-filter fix). Names match
    case-insensitively; a name containing a single quote silently
    truncates the filter at the quote.

    Args:
        table_name (str | Unset):
        x_tenant_id (str | Unset):

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[CatalogIntrospectionResult | ErrorResponse]
    """

    kwargs = _get_kwargs(
        table_name=table_name,
        x_tenant_id=x_tenant_id,
    )

    response = client.get_httpx_client().request(
        **kwargs,
    )

    return _build_response(client=client, response=response)


def sync(
    *,
    client: AuthenticatedClient | Client,
    table_name: str | Unset = UNSET,
    x_tenant_id: str | Unset = UNSET,
) -> CatalogIntrospectionResult | ErrorResponse | None:
    """Table-level xCatalog routing metadata.

     Serves `information_schema.table_routing` as a table-shaped
    result — the same projection pgwire/SQL clients see. All cell
    values are strings.
    The optional `X-Tenant-ID` header is not consulted by this
    handler (the introspection projection is cluster-scope).
    The filter dispatch is LEXICAL over the composed SQL text, so a
    `table_name` containing SQL fragments (e.g. ` from
    xcatalog.namespaces`) can be misread as a different view — do
    not treat this endpoint as a structured query interface
    (TD-SPECRAT-3 tracks the structured-filter fix). Names match
    case-insensitively; a name containing a single quote silently
    truncates the filter at the quote.

    Args:
        table_name (str | Unset):
        x_tenant_id (str | Unset):

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        CatalogIntrospectionResult | ErrorResponse
    """

    return sync_detailed(
        client=client,
        table_name=table_name,
        x_tenant_id=x_tenant_id,
    ).parsed


async def asyncio_detailed(
    *,
    client: AuthenticatedClient | Client,
    table_name: str | Unset = UNSET,
    x_tenant_id: str | Unset = UNSET,
) -> Response[CatalogIntrospectionResult | ErrorResponse]:
    """Table-level xCatalog routing metadata.

     Serves `information_schema.table_routing` as a table-shaped
    result — the same projection pgwire/SQL clients see. All cell
    values are strings.
    The optional `X-Tenant-ID` header is not consulted by this
    handler (the introspection projection is cluster-scope).
    The filter dispatch is LEXICAL over the composed SQL text, so a
    `table_name` containing SQL fragments (e.g. ` from
    xcatalog.namespaces`) can be misread as a different view — do
    not treat this endpoint as a structured query interface
    (TD-SPECRAT-3 tracks the structured-filter fix). Names match
    case-insensitively; a name containing a single quote silently
    truncates the filter at the quote.

    Args:
        table_name (str | Unset):
        x_tenant_id (str | Unset):

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[CatalogIntrospectionResult | ErrorResponse]
    """

    kwargs = _get_kwargs(
        table_name=table_name,
        x_tenant_id=x_tenant_id,
    )

    response = await client.get_async_httpx_client().request(**kwargs)

    return _build_response(client=client, response=response)


async def asyncio(
    *,
    client: AuthenticatedClient | Client,
    table_name: str | Unset = UNSET,
    x_tenant_id: str | Unset = UNSET,
) -> CatalogIntrospectionResult | ErrorResponse | None:
    """Table-level xCatalog routing metadata.

     Serves `information_schema.table_routing` as a table-shaped
    result — the same projection pgwire/SQL clients see. All cell
    values are strings.
    The optional `X-Tenant-ID` header is not consulted by this
    handler (the introspection projection is cluster-scope).
    The filter dispatch is LEXICAL over the composed SQL text, so a
    `table_name` containing SQL fragments (e.g. ` from
    xcatalog.namespaces`) can be misread as a different view — do
    not treat this endpoint as a structured query interface
    (TD-SPECRAT-3 tracks the structured-filter fix). Names match
    case-insensitively; a name containing a single quote silently
    truncates the filter at the quote.

    Args:
        table_name (str | Unset):
        x_tenant_id (str | Unset):

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        CatalogIntrospectionResult | ErrorResponse
    """

    return (
        await asyncio_detailed(
            client=client,
            table_name=table_name,
            x_tenant_id=x_tenant_id,
        )
    ).parsed
