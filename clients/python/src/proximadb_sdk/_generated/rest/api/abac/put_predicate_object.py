# @generated by openapi-python-client (TD-126 Phase 4) — DO NOT EDIT.
#
# Regenerate with `make gen-python-sdk`. Source of truth:
# docs/openapi/proximadb-openapi.yaml. The CI gate `python-sdk-codegen-drift`
# fails if this directory drifts from a fresh regeneration.
from http import HTTPStatus
from typing import Any, cast
from urllib.parse import quote

import httpx

from ... import errors
from ...client import AuthenticatedClient, Client
from ...models.abac_filter_expression import AbacFilterExpression
from ...models.abac_operator_error_response import AbacOperatorErrorResponse
from ...models.abac_predicate_object_response import AbacPredicateObjectResponse
from ...types import UNSET, Response, Unset


def _get_kwargs(
    object_id: int,
    *,
    body: AbacFilterExpression,
    x_tenant_id: str | Unset = UNSET,
) -> dict[str, Any]:
    headers: dict[str, Any] = {}
    if not isinstance(x_tenant_id, Unset):
        headers["X-Tenant-ID"] = x_tenant_id

    _kwargs: dict[str, Any] = {
        "method": "put",
        "url": "/api/v2/abac/predicate-objects/{object_id}".format(
            object_id=quote(str(object_id), safe=""),
        ),
    }

    _kwargs["json"] = body.to_dict()

    headers["Content-Type"] = "application/json"

    _kwargs["headers"] = headers
    return _kwargs


def _parse_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> AbacOperatorErrorResponse | AbacPredicateObjectResponse | None:
    if response.status_code == 200:
        response_200 = AbacPredicateObjectResponse.from_dict(response.json())

        return response_200

    if response.status_code == 401:
        response_401 = AbacOperatorErrorResponse.from_dict(response.json())

        return response_401

    if response.status_code == 403:
        response_403 = AbacOperatorErrorResponse.from_dict(response.json())

        return response_403

    if response.status_code == 503:
        response_503 = AbacOperatorErrorResponse.from_dict(response.json())

        return response_503

    if client.raise_on_unexpected_status:
        raise errors.UnexpectedStatus(response.status_code, response.content)
    else:
        return None


def _build_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> Response[AbacOperatorErrorResponse | AbacPredicateObjectResponse]:
    return Response(
        status_code=HTTPStatus(response.status_code),
        content=response.content,
        headers=response.headers,
        parsed=_parse_response(client=client, response=response),
    )


def sync_detailed(
    object_id: int,
    *,
    client: AuthenticatedClient | Client,
    body: AbacFilterExpression,
    x_tenant_id: str | Unset = UNSET,
) -> Response[AbacOperatorErrorResponse | AbacPredicateObjectResponse]:
    """Register (or replace) an ABAC predicate object.

     The request body IS the `FilterExpression` directly (no wrapper) —
    a stored row-filter predicate that a policy binding's
    `predicate_ref` can reference.
    The optional `X-Tenant-ID` header is not consulted by this
    handler (predicate objects are global, not tenant-scoped).

    Args:
        object_id (int):
        x_tenant_id (str | Unset):
        body (AbacFilterExpression): A predicate-object row filter — a RECURSIVE, externally-
            tagged Rust
            enum (`proximadb_filter_expression::FilterExpression`). Modeled
            here as free-form JSON (rather than a typed `oneOf`) because the
            self-referential union is not representable in a form the SDK
            codegen tooling can process (openapi-python-client cannot resolve
            a `oneOf` branch whose `items` `$ref`s the union being defined);
            this is the one schema in the ABAC surface where the generated
            client falls back to an untyped object. Construct exactly one of:

            * `{"Comparison": {"field": <string>, "operator": <one of
              "Equals","NotEquals","GreaterThan","GreaterThanOrEqual",
              "LessThan","LessThanOrEqual","In","NotIn","Contains",
              "StartsWith","EndsWith","Between","IsNull","IsNotNull","Like">,
              "value": <any JSON value>}}`
            * `{"And": [<AbacFilterExpression>, ...]}`
            * `{"Or": [<AbacFilterExpression>, ...]}`
            * `{"Not": <AbacFilterExpression>}`

            See [AbacComparisonOperator](#/components/schemas/AbacComparisonOperator)
            for the authoritative operator enum (referenced here for docs only;
            not structurally, for the same recursion reason).

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[AbacOperatorErrorResponse | AbacPredicateObjectResponse]
    """

    kwargs = _get_kwargs(
        object_id=object_id,
        body=body,
        x_tenant_id=x_tenant_id,
    )

    response = client.get_httpx_client().request(
        **kwargs,
    )

    return _build_response(client=client, response=response)


def sync(
    object_id: int,
    *,
    client: AuthenticatedClient | Client,
    body: AbacFilterExpression,
    x_tenant_id: str | Unset = UNSET,
) -> AbacOperatorErrorResponse | AbacPredicateObjectResponse | None:
    """Register (or replace) an ABAC predicate object.

     The request body IS the `FilterExpression` directly (no wrapper) —
    a stored row-filter predicate that a policy binding's
    `predicate_ref` can reference.
    The optional `X-Tenant-ID` header is not consulted by this
    handler (predicate objects are global, not tenant-scoped).

    Args:
        object_id (int):
        x_tenant_id (str | Unset):
        body (AbacFilterExpression): A predicate-object row filter — a RECURSIVE, externally-
            tagged Rust
            enum (`proximadb_filter_expression::FilterExpression`). Modeled
            here as free-form JSON (rather than a typed `oneOf`) because the
            self-referential union is not representable in a form the SDK
            codegen tooling can process (openapi-python-client cannot resolve
            a `oneOf` branch whose `items` `$ref`s the union being defined);
            this is the one schema in the ABAC surface where the generated
            client falls back to an untyped object. Construct exactly one of:

            * `{"Comparison": {"field": <string>, "operator": <one of
              "Equals","NotEquals","GreaterThan","GreaterThanOrEqual",
              "LessThan","LessThanOrEqual","In","NotIn","Contains",
              "StartsWith","EndsWith","Between","IsNull","IsNotNull","Like">,
              "value": <any JSON value>}}`
            * `{"And": [<AbacFilterExpression>, ...]}`
            * `{"Or": [<AbacFilterExpression>, ...]}`
            * `{"Not": <AbacFilterExpression>}`

            See [AbacComparisonOperator](#/components/schemas/AbacComparisonOperator)
            for the authoritative operator enum (referenced here for docs only;
            not structurally, for the same recursion reason).

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        AbacOperatorErrorResponse | AbacPredicateObjectResponse
    """

    return sync_detailed(
        object_id=object_id,
        client=client,
        body=body,
        x_tenant_id=x_tenant_id,
    ).parsed


async def asyncio_detailed(
    object_id: int,
    *,
    client: AuthenticatedClient | Client,
    body: AbacFilterExpression,
    x_tenant_id: str | Unset = UNSET,
) -> Response[AbacOperatorErrorResponse | AbacPredicateObjectResponse]:
    """Register (or replace) an ABAC predicate object.

     The request body IS the `FilterExpression` directly (no wrapper) —
    a stored row-filter predicate that a policy binding's
    `predicate_ref` can reference.
    The optional `X-Tenant-ID` header is not consulted by this
    handler (predicate objects are global, not tenant-scoped).

    Args:
        object_id (int):
        x_tenant_id (str | Unset):
        body (AbacFilterExpression): A predicate-object row filter — a RECURSIVE, externally-
            tagged Rust
            enum (`proximadb_filter_expression::FilterExpression`). Modeled
            here as free-form JSON (rather than a typed `oneOf`) because the
            self-referential union is not representable in a form the SDK
            codegen tooling can process (openapi-python-client cannot resolve
            a `oneOf` branch whose `items` `$ref`s the union being defined);
            this is the one schema in the ABAC surface where the generated
            client falls back to an untyped object. Construct exactly one of:

            * `{"Comparison": {"field": <string>, "operator": <one of
              "Equals","NotEquals","GreaterThan","GreaterThanOrEqual",
              "LessThan","LessThanOrEqual","In","NotIn","Contains",
              "StartsWith","EndsWith","Between","IsNull","IsNotNull","Like">,
              "value": <any JSON value>}}`
            * `{"And": [<AbacFilterExpression>, ...]}`
            * `{"Or": [<AbacFilterExpression>, ...]}`
            * `{"Not": <AbacFilterExpression>}`

            See [AbacComparisonOperator](#/components/schemas/AbacComparisonOperator)
            for the authoritative operator enum (referenced here for docs only;
            not structurally, for the same recursion reason).

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[AbacOperatorErrorResponse | AbacPredicateObjectResponse]
    """

    kwargs = _get_kwargs(
        object_id=object_id,
        body=body,
        x_tenant_id=x_tenant_id,
    )

    response = await client.get_async_httpx_client().request(**kwargs)

    return _build_response(client=client, response=response)


async def asyncio(
    object_id: int,
    *,
    client: AuthenticatedClient | Client,
    body: AbacFilterExpression,
    x_tenant_id: str | Unset = UNSET,
) -> AbacOperatorErrorResponse | AbacPredicateObjectResponse | None:
    """Register (or replace) an ABAC predicate object.

     The request body IS the `FilterExpression` directly (no wrapper) —
    a stored row-filter predicate that a policy binding's
    `predicate_ref` can reference.
    The optional `X-Tenant-ID` header is not consulted by this
    handler (predicate objects are global, not tenant-scoped).

    Args:
        object_id (int):
        x_tenant_id (str | Unset):
        body (AbacFilterExpression): A predicate-object row filter — a RECURSIVE, externally-
            tagged Rust
            enum (`proximadb_filter_expression::FilterExpression`). Modeled
            here as free-form JSON (rather than a typed `oneOf`) because the
            self-referential union is not representable in a form the SDK
            codegen tooling can process (openapi-python-client cannot resolve
            a `oneOf` branch whose `items` `$ref`s the union being defined);
            this is the one schema in the ABAC surface where the generated
            client falls back to an untyped object. Construct exactly one of:

            * `{"Comparison": {"field": <string>, "operator": <one of
              "Equals","NotEquals","GreaterThan","GreaterThanOrEqual",
              "LessThan","LessThanOrEqual","In","NotIn","Contains",
              "StartsWith","EndsWith","Between","IsNull","IsNotNull","Like">,
              "value": <any JSON value>}}`
            * `{"And": [<AbacFilterExpression>, ...]}`
            * `{"Or": [<AbacFilterExpression>, ...]}`
            * `{"Not": <AbacFilterExpression>}`

            See [AbacComparisonOperator](#/components/schemas/AbacComparisonOperator)
            for the authoritative operator enum (referenced here for docs only;
            not structurally, for the same recursion reason).

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        AbacOperatorErrorResponse | AbacPredicateObjectResponse
    """

    return (
        await asyncio_detailed(
            object_id=object_id,
            client=client,
            body=body,
            x_tenant_id=x_tenant_id,
        )
    ).parsed
