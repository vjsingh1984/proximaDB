diff --git a/src/audit/logger.rs b/src/audit/logger.rs
index 1234567..abcdefg 100644
--- a/src/audit/logger.rs
+++ b/src/audit/logger.rs
@@ -482,7 +482,7 @@ impl AuditLogger {
         // Check user's recent activity pattern
         // (Placeholder - real implementation would analyze user behavior)

-        Some(risk_score.min(1.0f64))
+        Some(risk_score.min(1.0))
     }

     /// Check if IP address is suspicious

diff --git a/src/ai/natural_language/translator.rs b/src/ai/natural_language/translator.rs
index 1234567..abcdefg 100644
--- a/src/ai/natural_language/translator.rs
+++ b/src/ai/natural_language/translator.rs
@@ -212,8 +212,11 @@ impl NaturalLanguageTranslator {
     ) -> Result<String, TranslationError> {
         let template = PromptTemplate::SecureTranslation;

+        let accessible_tables_str = user_context.accessible_tables.iter()
+            .map(|s| s.as_str())
+            .collect::<Vec<&str>>()
+            .join(", ");
         let prompt = self.prompt_builder.build_prompt(template, &[
-            ("user_accessible_tables", &user_context.accessible_tables.join(", ")),
+            ("user_accessible_tables", &accessible_tables_str),
             ("schema_context", schema_context),
             ("natural_language_query", query),
             ("tenant_id", &user_context.tenant_id.clone().unwrap_or_default()),

diff --git a/src/ai/natural_language/schema_context.rs b/src/ai/natural_language/schema_context.rs
index 1234567..abcdefg 100644
--- a/src/ai/natural_language/schema_context.rs
+++ b/src/ai/natural_language/schema_context.rs
@@ -162,8 +162,8 @@ impl SchemaContext {

             // Add sample values if configured and available
             if self.config.include_sample_values && !column.sample_values.is_empty() {
-                let samples: Vec<&String> = column.sample_values.iter()
-                    .take(self.config.max_sample_values_per_column)
+                let samples: Vec<&str> = column.sample_values.iter()
+                    .map(|s| s.as_str())
+                    .take(self.config.max_sample_values_per_column)
                     .collect();
                 description.push_str(&format!(" [Examples: {}]", samples.join(", ")));
             }

diff --git a/src/ai/llm_integration/engine.rs b/src/ai/llm_integration/engine.rs
index 1234567..abcdefg 100644
--- a/src/ai/llm_integration/engine.rs
+++ b/src/ai/llm_integration/engine.rs
@@ -380,8 +380,15 @@ impl std::fmt::Display for LLMProvider {
     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
         match self {
             LLMProvider::OpenAI => write!(f, "OpenAI"),
             LLMProvider::Anthropic => write!(f, "Anthropic"),
+            LLMProvider::Cohere => write!(f, "Cohere"),
+            LLMProvider::AWSBedrock => write!(f, "AWS Bedrock"),
+            LLMProvider::AzureOpenAI => write!(f, "Azure OpenAI"),
+            LLMProvider::GoogleVertexAI => write!(f, "Google Vertex AI"),
+            LLMProvider::Ollama => write!(f, "Ollama"),
+            LLMProvider::VLLM => write!(f, "VLLM"),
+            LLMProvider::HuggingFace => write!(f, "HuggingFace"),
         }
     }
 }

diff --git a/src/ai/llm_integration/types.rs b/src/ai/llm_integration/types.rs
index 1234567..abcdefg 100644
--- a/src/ai/llm_integration/types.rs
+++ b/src/ai/llm_integration/types.rs
@@ -263,8 +263,15 @@ impl LLMRequestContext {
     fn get_provider_specific_config(&self) -> String {
         match self.provider {
             LLMProvider::OpenAI => "openai_config".to_string(),
             LLMProvider::Anthropic => "anthropic_config".to_string(),
+            LLMProvider::Cohere => "cohere_config".to_string(),
+            LLMProvider::AWSBedrock => "aws_bedrock_config".to_string(),
+            LLMProvider::AzureOpenAI => "azure_openai_config".to_string(),
+            LLMProvider::GoogleVertexAI => "google_vertex_config".to_string(),
+            LLMProvider::Ollama => "ollama_config".to_string(),
+            LLMProvider::VLLM => "vllm_config".to_string(),
+            LLMProvider::HuggingFace => "huggingface_config".to_string(),
         }
     }
 }