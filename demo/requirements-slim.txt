# Minimal requirements for production demo
# ~2GB total vs 10GB with full requirements

# Core dependencies only
numpy>=1.24.0
requests>=2.34.2

# BERT embeddings - smallest model only
sentence-transformers>=6.0.0
# CPU-only version. Floor is >=2.13.0 for CVE-2025-3000 (memory corruption via
# torch.jit.script): the previous `>=2.0.0,<2.1.0` capped BELOW the fix, so the
# range could only ever resolve to a vulnerable build. The demo uses only
# manual_seed / cuda.manual_seed_all / no_grad, which are stable across 2.x, and
# never calls torch.jit.script, so raising the floor is behaviour-preserving here.
torch>=2.13.0
--extra-index-url https://download.pytorch.org/whl/cpu

# Essential for demos
nltk>=3.10.2
aiohttp>=3.8.0
aiohttp-cors>=0.8.1

# No visualization, no dev tools, no testing
# No transformers library (use sentence-transformers only)
# No pandas, scikit-learn unless absolutely needed