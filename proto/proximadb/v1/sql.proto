syntax = "proto3";

package proximadb.v1;

import "proximadb.proto"; // For ExecuteSqlRequest/Response types

// SQL execution service (v1, versioned namespace)
service SqlService {
  rpc ExecuteSql(proximadb.ExecuteSqlRequest) returns (proximadb.ExecuteSqlResponse);
}

