= Storage Engine Internals
:toc: left
:toclevels: 2
:imagesdir: ../diagrams/images

**Deep dive into ProximaDB's dual storage engine architecture.**

== Overview

image::storage-engine-abstract.svg[Storage Engine Architecture, width=2700]

ProximaDB implements five storage engines, each optimized for specific workloads and performance requirements:

[cols="1,2,2,1", options="header"]
|===
| Engine | Architecture | Best For | Status

| **SST**
| Row-based LSM-tree storage
| General purpose, proven performance, streaming data
| ✅ Production

| **VIPER**
| Columnar Parquet-based storage
| Analytics, batch processing, high compression needs
| ✅ Production

| **PRISM**
| Memory-optimized hierarchical engine
| Ultra-low latency, memory-constrained environments
| 🚧 Beta

| **SWIFT**
| Dual-mode SST with instant navigation
| Fast lookups, tree-based navigation, ID-heavy workloads
| 🚧 Beta

| **NOVA**
| Advanced columnar with hierarchical stats
| Complex analytics, advanced optimizations, research
| 🔬 Alpha
|===

== SST Engine

=== Hierarchical Architecture

image::sst-format-detail-modular.svg[SST Format, width=900]

The SST engine implements a sophisticated three-stage filtering system:

**Stage 1: File-Level Filtering**
* Global bloom filter (2KB): 85% file skip rate
* Min/max keys and timestamp ranges
* SST1 magic bytes: `0x535354310A1A0A0D`

**Stage 2: Block-Level Filtering**
* Per-block bloom filters (256B): 95% block skip rate
* Metadata statistics for predicate pushdown
* Compression metadata for adaptive decompression

**Stage 3: Record-Level Access**
* Zero-copy deserialization via bytemuck
* Memory-mapped I/O for hot blocks
* MVCC resolution at read time

=== Zero-Copy Compaction

[source,rust]
----
// Direct SST operations without VectorRecord conversion
pub struct SstCompactor {
    pub async fn compact_with_streaming(
        &self,
        input_files: Vec<String>,
        target_level: u8,
    ) -> Result<ZeroCopyCompactionStats> {
        // Phase 1: Create streaming iterators
        let iterators = self.create_sst_direct_readers(input_files).await?;
        
        // Phase 2: K-way merge with MVCC
        let merged = self.k_way_merge_streaming(iterators).await?;
        
        // Phase 3: Direct write with hierarchical blooms
        writer.write_streaming_sst_records(merged).await
    }
}
----

**Performance Benefits:**
* Eliminates double copying for 2000+ vectors per block
* 60% reduction in memory pressure
* 40-60% faster than traditional compaction

=== Adaptive Sorting

image::sst-sorting-detail-modular.svg[SST Sorting, width=900]

The SST engine automatically selects optimal sorting algorithms:

[cols="2,1,1,2", options="header"]
|===
| Strategy | Threshold | Complexity | Use Case

| Vec + Sort
| <10K records
| O(n log n)
| Small datasets, single batch

| K-Way Merge
| >10K records
| O(n log k)
| Large datasets, multiple batches
|===

=== Cloud-Optimized Block Sizing

Default 3MB blocks align with cloud storage patterns:

[cols="2,1,1,1", options="header"]
|===
| Storage | Optimal I/O | SST Alignment | API Reduction

| AWS S3 | 5MB | 2 blocks/part | 85%
| Azure Blob | 4MB | Perfect | 90%
| GCS | 8MB | 3 blocks/chunk | 60%
| EBS gp3 | 256KB | 12 parallel | 4x throughput
|===

== VIPER Engine

=== Columnar Architecture

VIPER leverages Apache Parquet for columnar storage:

* **Compression**: 50-80% storage reduction
* **Predicate Pushdown**: Filter at storage layer
* **Quantization**: Optional INT8/PQ compression
* **Batch Processing**: Optimized for large scans

== PRISM Engine

=== Memory-Optimized Architecture

PRISM delivers ultra-low latency through hierarchical memory optimization:

* **Zero-Overhead Storage**: 75-96% memory savings over traditional approaches
* **Hierarchical Caching**: Multi-tier memory management
* **Progressive Compression**: Adaptive compression based on access patterns
* **Memory-Mapped I/O**: Efficient large dataset handling

=== Key Features

**Memory Efficiency:**
* Progressive data structure sizing
* Automatic memory pool management
* Cache-aware data placement
* Zero-copy operations

**Performance:**
* Sub-millisecond query latency
* 20K+ vectors/second write throughput
* Automatic NUMA awareness
* CPU cache optimization

== SWIFT Engine

=== Tree-Based Navigation Architecture

SWIFT provides instant vector traversal through optimized tree structures:

* **B+ Tree Indexing**: O(log n) ID-based lookups
* **Dual-Mode Operation**: Index-driven and index-free modes
* **Progressive Search**: Binary → INT8 → PQ → Full precision
* **Zero-Overhead Integration**: Seamless AXIS compatibility

=== Navigation Features

**Tree Operations:**
* Fast prefix searches
* Range-based queries
* Batch tree operations
* Automatic rebalancing

**Search Optimization:**
* Four-stage progressive refinement
* Adaptive quantization levels
* Memory-efficient tree traversal
* Cache-friendly access patterns

== NOVA Engine

=== Advanced Columnar Analytics

NOVA represents the next generation of columnar vector storage:

* **Hierarchical Statistics**: Zone maps and bloom filters
* **Streaming Processors**: Real-time analytics pipelines
* **Advanced Quantization**: Multi-tier compression strategies
* **Progressive Search**: Intelligent query optimization

=== Analytics Features

**Advanced Compression:**
* Multi-algorithm compression chains
* Adaptive compression selection
* Context-aware quantization
* Predictive compression tuning

**Query Optimization:**
* Cost-based query planning
* Predicate pushdown optimization
* Column projection intelligence
* Parallel execution pipelines

=== Engine Quantization Strategy

[cols="1,2,2", options="header"]
|===
| Engine | Quantization | Strategy

| **SST**
| ❌ Not Recommended
| Row format doesn't benefit, adds complexity

| **VIPER**
| ✅ Recommended
| Columnar format benefits from uniform types

| **PRISM**
| ✅ Essential
| Memory optimization requires quantization

| **SWIFT**
| ✅ Progressive
| Multi-tier quantization for tree navigation

| **NOVA**
| ✅ Advanced
| Hierarchical quantization with intelligent selection
|===

== Engine Ranking & Selection Guide

=== Engine Ranking by Use Case

**📊 Performance Ranking (General Purpose)**
1. **SST** (Primary/Default) - Battle-tested, proven LSM-tree architecture
2. **VIPER** (Specialized) - Columnar analytics with excellent compression
3. **PRISM** (High Performance) - Ultra-low latency, memory-optimized
4. **SWIFT** (Fast Access) - Tree-based navigation, instant lookups
5. **NOVA** (Research) - Cutting-edge analytics, experimental features

**🔧 Production Readiness Ranking**
1. **SST** - Fully production-ready, 2,700+ passing tests
2. **VIPER** - Production-ready for analytics workloads
3. **PRISM** - Beta testing, memory-optimized deployments
4. **SWIFT** - Beta testing, ID-lookup optimization
5. **NOVA** - Alpha stage, research and development

**💰 Cost Efficiency Ranking**
1. **PRISM** - 75-96% memory savings, zero-overhead architecture
2. **NOVA** - 50-70% storage reduction through advanced compression
3. **VIPER** - 50-80% compression, columnar efficiency
4. **SST** - 40-50% compression, efficient writes
5. **SWIFT** - Memory-efficient with tree-based indexing

image::engine-selection-tree.svg[Engine Selection Decision Tree, width=900, align=center]

.Performance Characteristics: All Engines
[%autowidth.stretch, cols="^1h,^1,^1,^1,^1,^1", options="header", frame="none", grid="rows", stripes="even"]
|===
| Metric | SST | VIPER | PRISM | SWIFT | NOVA

| Write Speed 
| *17K/sec* 🚀
| 5K/sec
| *20K/sec* ⚡
| 12K/sec
| 4K/sec

| Query Latency 
| *0.9ms* ⚡
| 2-3ms
| *0.3ms* 🚀
| 0.7ms
| 1-2ms

| Compression 
| 40-50%
| *70-80%* 📦
| 60-70%
| 45-55%
| *80-90%* 🗜️

| Memory Usage 
| Medium
| Lower ✓
| *Very Low* 🎯
| Medium
| Lower

| Maturity
| Production ✅
| Production ✅
| Beta 🚧
| Beta 🚧
| Alpha 🔬

| Best For 
| General Purpose
| Analytics
| Ultra-Low Latency
| Fast Lookups
| Research
|===

=== Comprehensive Decision Matrix

[cols="2,1,1,1,1,1", options="header"]
|===
| Criteria | SST | VIPER | PRISM | SWIFT | NOVA

| **Dataset Size**
| Any size
| >100K vectors
| Memory-bound
| Any size
| >1M vectors

| **Write Pattern**
| Real-time
| Batch inserts
| High-frequency
| Mixed
| Batch analytics

| **Query Pattern**
| General
| Range scans
| Point lookups
| ID-based
| Complex analytics

| **Memory Budget**
| Medium
| Low
| Very Low
| Medium
| Low

| **Latency Priority**
| Medium
| Low
| Critical
| High
| Medium

| **Compression**
| Medium
| High
| Medium
| Medium
| Very High
|===

=== Configuration Examples

**SST (General Purpose):**
[source,python]
----
config = CollectionConfig(
    name="general_vectors",
    dimension=768,
    storage_engine="sst",
    compression=CompressionConfig(
        algorithm="lz4",
        level=1,
        enable_quantization=False
    )
)
----

**VIPER (Analytics):**
[source,python]
----
config = CollectionConfig(
    name="analytics_warehouse",
    dimension=1536,
    storage_engine="viper",
    compression=CompressionConfig(
        algorithm="zstd",
        level=6,
        enable_quantization=True,
        quantization_type="pq8"
    )
)
----

**PRISM (Ultra-Low Latency):**
[source,python]
----
config = CollectionConfig(
    name="realtime_recommendations",
    dimension=384,
    storage_engine="prism",
    compression=CompressionConfig(
        algorithm="lz4",
        level=1,
        enable_quantization=True,
        memory_optimization=True
    )
)
----

**SWIFT (Fast Lookups):**
[source,python]
----
config = CollectionConfig(
    name="user_embeddings",
    dimension=512,
    storage_engine="swift",
    compression=CompressionConfig(
        algorithm="zstd",
        level=3,
        enable_tree_indexing=True
    )
)
----

**NOVA (Advanced Analytics):**
[source,python]
----
config = CollectionConfig(
    name="research_dataset",
    dimension=2048,
    storage_engine="nova",
    compression=CompressionConfig(
        algorithm="zstd",
        level=9,
        enable_quantization=True,
        hierarchical_stats=True
    )
)
----

== Performance Characteristics

=== Write Performance
[cols="2,1,1", options="header"]
|===
| Metric | SST | VIPER
| Single insert | 0.5ms | 2ms
| Batch (1000) | 50ms | 100ms
| Flush time | 100ms/MB | 500ms/MB
|===

=== Read Performance
[cols="2,1,1", options="header"]
|===
| Metric | SST | VIPER
| Point query | 0.3ms | 1ms
| Range scan (1000) | 10ms | 5ms
| Compression overhead | 10% | 5%
|===

== Implementation Details

For detailed implementation and advanced features, see:

* link:hierarchical_sst_implementation.adoc[Hierarchical SST Implementation]
* link:../compression_guide.adoc[Compression Guide]
* link:technical_reference.adoc[Technical Reference]

---
**Version 0.1.4** | Last Updated: 2025-01