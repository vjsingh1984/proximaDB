= Convergence Audit — Divergent Seams & Reconcile Directions
:toc: left
:toclevels: 3
:icons: font
:source-highlighter: rouge
:sectnums:
:last-updated: 2026-05-30

Status: Active audit (no code changes in this pass — reconcile work is queued as TD items) +
Date: 2026-05-30 +
Apex map: link:SYSTEM_MAP_2026_05_30.adoc[SYSTEM_MAP_2026_05_30] +
Prior audits: `project_proliferation_audit_2026_05_20`, `project_proliferation_audit_2026_05_25` (memory)

[.lead]
Rapid AI-assisted ("vibe-coded") development produced parallel implementations across this codebase. Two
prior audits resolved most of them. This audit (a) confirms what is already converged or intentionally
separate, (b) catalogs the *live* divergent seams with a directionally-aligned reconcile direction each,
and (c) installs a **Convergence Gate** governance rule so future agentic edits stay aligned.

[IMPORTANT]
====
**Scope:** documentation + queued TD items only. No source files are reconciled in this pass. The point is
to make the seams explicit and pointable so they get fixed deliberately, not to churn code while documenting
how to reduce churn.

**Release target:** all reconcile recommendations below (TD-104…108) and the agent-memory items
(TD-100…103) target the **v0.2** release currently in development — none are deferred to a later release.
One item (S2 gRPC shims) carries existing code markers that say `0.3.0`; landing it in v0.2 means pulling
that removal forward and updating those markers (called out in the S2 row).
====

== Method & a lesson from prior audits

The 2026-05-25 audit found ~40% false positives when seams were identified by *symbol name* alone
(same-named structs that are actually different shapes, scoped test mocks, or re-export aliases). The
durable lesson: **verify the shape and the live caller chain before declaring a consolidation target.**
Every seam below is classified as one of:

* **LIVE** — genuine divergence; reconcile direction given.
* **BY-DESIGN** — looks like duplication, is intentional; documented so it is not "re-fixed".
* **RESOLVED** — already converged; listed for the record.

== Convergence invariant (what must never diverge)

Per `CLAUDE.md` and `RELATIONAL_DOCUMENT_GRAPH_CONVERGENCE_2026_05_14.adoc`: **durable authority is
single.** All protocol facades lower into one chain — `ProximaRecord` + canonical WAL + xCatalog (see
the convergence diagram in the apex map). Proliferation is tolerated at the *edge* (facades, adapters,
projections); it is a regression anywhere in the durable core. Every seam below is judged against this
invariant: edge-divergence is a cleanup; core-divergence is a defect.

== Live divergent seams

[cols="1,3,3,1", options="header"]
|===
| Seam | Current state | Convergent target & reconcile direction | Tracker

| S1 Phase 9 handler/service extraction
| Real `UnifiedHandlers` impl (~250 methods) in `src/api_handlers/request_handlers.rs`; `crates/platform/proximadb-runtime/src/handlers.rs` is a 50-line composition stub explicitly noting the real impl still lives in the root crate.
| Finish the extraction: move handler bodies into `proximadb-api`/`proximadb-runtime`, leave the root crate composing ports only. Do it endpoint-group at a time (commit each group immediately — linter displaces unstaged files). Target: one home per handler.
| TD-104

| S2 Deprecated gRPC shims — RESOLVED (2026-06-03)
| RESOLVED. The `*_service.rs` files were NOT shims — they were the live `*Port` backends the `crates/platform/proximadb-api/src/grpc/v1/` tonic adapters delegate to; the dead code was the never-served tonic `impl *Service` blocks. Phase A deleted the 3 truly-dead files (entity/document/hybrid, `a6c80f90f`); Phase B converged the 4 live backends (observability/streaming/graph/security) off their dead tonic surfaces and removed the stale `0.3.0` headers.
| Done: no `*_service.rs` carries a tonic `impl *Service`; all consumers bind `Arc<dyn *Port>`; build + unit tests green.
| TD-105 (DONE)

| S3 Proto-v1 internal mirrors
| `VectorRecord`, `SqlValue`, `SqlObject` (proto v1) are still used as *internal* types; canonical is `ProximaRecord` / `ProximaValue` / `ProximaType`. Handlers parse proto v1 then convert.
| Keep proto v1 for the *wire* only. Migrate internal usage to canonical types one handler file at a time. Edge adapters (parse → convert) stay; internal mirrors go.
| TD-106

| S4 Scattered configs — RESOLVED (2026-06-05)
| RESOLVED at the right seam. Cluster configs fully converged into `proximadb-config/src/cluster_config.rs` (Consensus/Routing/Shard/Partition/ClusterReplication/NodeRegistry/MetadataService/DistributedOps/ConnectionPool/Cluster), re-exported from `src/cluster/*.rs` (8 sites); the only local one, `CachedPartitionConfig`, is a non-serializable in-memory cache and stays. CDC: `src/cdc/config.rs` re-exports the canonical `proximadb_config` CDC surface; the structs left in `src/cdc/` are connector-specific (Kafka/Webhook/Postgres/MySql/Mongo, `experimental-cdc-connectors`-gated) or subscription-runtime — legitimate specialization the seam says to preserve; moving them would add cross-module entanglement for ~zero benefit.
| Done: app-level CDC + cluster config in `proximadb-config`, re-exported; domain/connector/runtime specialization stays local; build green.
| TD-107 (Resolved)

| S6 Residual catalog re-export shims — RESOLVED (2026-06-05)
| RESOLVED: no pure re-export shim remains under `src/catalog/`. The local `types` shim (`pub use proximadb_catalog::*`) was deleted; `src/catalog/mod.rs` is the module root (re-exports the canonical `proximadb_catalog` surface + carries real submodules), and every other `src/catalog/*.rs` is a real implementation module. Build green.
| Done: shims removed; importers use the crate via the module root; files with real glue retained.
| TD-108 (Resolved)

| S7 Scattered `ProximaRecord`↔Arrow/Parquet converters
| Three lossy forward converters predate a canonical home: `src/connectors/spark.rs::proxima_records_to_record_batch` (id + `List<Float32>` vector only — props dropped), `src/storage/formats/arrow_conversion.rs` (legacy `VectorRecord`-based), `src/network/arrow_ipc/codec.rs::vector_records_to_batch` (collapses all props into one `StructArray`). Separately, every Parquet helper (`columnar_io::write_parquet`, `file_export::read_parquet_file`, `open/iceberg.rs`) is FileSystem-**path**-based — no in-memory `RecordBatch`↔`Bytes` layer existed for the object-store base tier.
| Canonical home is `crates/storage/proximadb-storage-common`: `proxima_arrow::proxima_records_to_record_batch(records, &ProximaSchema)` (F1.5, `6dfd51661` — schema-driven via `ProximaSchema::to_arrow_schema`/`to_arrow_type`, props-complete, correct null masking, vector→`FixedSizeBinary(dim*4)`) + `proxima_parquet` (F1.6, `2f6c74c41` — in-memory `record_batches_to_parquet_bytes`/`parquet_bytes_to_record_batches`/`proxima_records_to_parquet_bytes`, `object_store`-version-independent). New readers/writers/projections REUSE these; the three legacy converters delegate as a `ProximaSchema` becomes available at each call site (spark.rs's `List<Float32>` shape is a wire change — gate behind the JNI path). The F2 `ObjectStoreBridge` impl composes F1.5 + F1.6. (`proximadb-codec` is vector/quantization columnar encoding — NOT the Parquet seam.)
| F-series

| S8 Three parallel data-type enums + two schema structs
| Three logical "data type" enums — `ProximaType` (`proximadb-data-model`, canonical/richest, has `pgwire_oid`), `ProximaDataType` (`proximadb-storage-common`, used by `ProximaSchema`; also carries *physical* quantized-vector variants), `CatalogDataType` (`proximadb-catalog`, durable in `CatalogTableSchema`, proto-v1 codes) — with scattered Arrow/proto/pgwire bridges and no `ProximaType`↔`ProximaDataType` link. Plus two schema structs (`CatalogTableSchema` durable authority vs in-memory `ProximaSchema`) with no converter. REST v2 types via bespoke strings; proto v1/v2 are separate wire enums.
| Per *ADR-024*: `ProximaType` is the single canonical *logical* type (physical encoding/quantization is an orthogonal storage attribute, not a type variant); `ProximaDataType` + `CatalogDataType` collapse into it; the two schema structs merge into one (`ProximaType` columns). Arrow/pgwire/proto bridges centralize on `ProximaType` (Step 1: `to_arrow_type`/`from_arrow_type` + extended `pgwire_oid` + `VectorElement::BFloat16` landed additively). Proto enums stay as versioned wire edges with one bridge each (advances S3). Phased rollout (Steps 1–5) with serde deserialize-compat shims for persisted catalog schemas; proto wire codes never renumbered.
| ADR-024 / TD-111
|===

== By-design separations (do NOT "re-converge")

[cols="1,3", options="header"]
|===
| Seam | Why it stays separate

| S5 PlanCache B/C/D
| Four impls; A consolidated onto `proximadb_runtime_common::ThreadSafeLruCache` (commit `b7bd2ae74`). B (per-entry EMA mutation), C (async RwLock, tenant scoping, mass flush), D (DashMap lock-free) have distinct concurrency/async requirements. Documented as intentional; consolidation gains do not exceed separation cost.

| Vector query 2-shape (ADR-014)
| Control-plane vs orchestration-plane `VectorSearchRequest`/plan shapes are an intentional boundary; the dead `proximadb-proto` dep was removed to *enforce* the separation. See ADR-014.

| Multiple `StorageError` homes
| `storage_common::StorageError` is canonical; `persistence.rs` / `transaction/` uses are semantically correct (serialization vs commit-failure). Orphan rule prevents a single workspace-wide `From` impl. Not a defect.

| `HardwareCapabilities` / `QuantizationHardwareDescriptor`
| Foundation detector vs root-crate GPU/topology extension vs contract param shape — three roles, not three copies. Resolved 2026-05-25.

| Quantization layering (types → contracts → vector impls → compute orchestration + turboquant)
| Clean layered specialization, not duplication. TurboQuant (ADR-021) is additive under `experimental-turboquant`.

| Multi-engine read routing (Volcano / Polars / DataFusion / Specialized)
| Course-correction §5: one shared logical plane (`proximadb-relational-frontend` → `-algebra::LogicalNode`), split physical plane. The READ router is `src/query/compute_scheduler.rs::ComputeScheduler::route_select` (canonical) — the read analog of the WRITE router `table_write_plan::RoutedExecutionPlan`/`RouteDecisionMetadata`; both REUSE the one `ComputeBackend` enum and `CatalogWorkloadProfile` (no parallel vocabulary). P0 always selects `Native(Volcano)` (additive, observable via `EXPLAIN SELECT`); P1+ flip the OLAP arm to `DataFusionLocal` for Parquet-backed tables; §5.2 Phase 2 feeds the decision to the existing `RLPlanner`. Distinct physical engines are an intentional boundary, not duplication; the single decision point prevents fragmentation.
|===

== Governance — the Convergence Gate

To keep agentic-AI / vibe-coded additions directionally aligned *without* a human re-auditing every PR,
adopt a lightweight, mandatory gate. This extends the "Reuse-First Architecture Rules" in `CLAUDE.md`.

[NOTE]
====
**Convergence Gate (landed in CLAUDE.md Architecture Mandates, v0.2).**

Any new `struct` / `enum` / endpoint / handler / planner / config / storage path MUST, in its PR
description or a doc comment, either:

. **Name the canonical surface it converges on** (the existing service, engine, router, cache, proto,
  record, or config it extends/reuses), OR
. **Cite the ADR** that justifies introducing a new seam (with `authority_mode`, `policy_boundary`, and
  `freshness` declared per the route-declaration mandate).

A new whole-system architecture diagram MUST live in `SYSTEM_MAP_2026_05_30.adoc` or a feeder linked
from it — never in a new competing top-level map.

Reviewers reject additions that do neither. Treat a duplicate abstraction as a regression.
====

The gate is the durable mechanism; this audit is a point-in-time snapshot. As seams are reconciled,
update the trackers below and the apex map's "Where the seams are" section.

== Tracker

New TD items queued in link:../10-quality/TECHNICAL_DEBT.adoc[TECHNICAL_DEBT.adoc]: TD-104 (S1),
TD-105 (S2), TD-106 (S3), TD-107 (S4), TD-108 (S6). Agent-memory items TD-100…TD-103 are tracked via
link:adr/ADR-022-agent-memory-layer.adoc[ADR-022] + link:AGENT_MEMORY_LAYER_HLD_2026_05_30.adoc[the HLD].

S7 (Arrow/Parquet converter convergence) is tracked under the warehouse base-tier **F-series**
(`DATA_WAREHOUSE_AND_ENGINEERING_COURSE_CORRECTION_2026_06_04.adoc` §6): F1 `proximadb-object-store`
plumbing → F1.5 `proxima_arrow` → F1.6 `proxima_parquet` (the in-memory serialization stack, landed) →
F2 `ObjectStoreBridge` impl (composes F1.5+F1.6) → F5 DataFusion reads the base tier through the bridge.
The three legacy converters are converged opportunistically as each call site gains a `ProximaSchema`.
