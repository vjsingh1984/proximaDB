= Data Warehouse & Enterprise Data Engineering Course Correction
:toc: left
:toclevels: 3
:icons: font
:sectnums:
:revdate: 2026-06-04

[.lead]
This document defines a strategic course correction for ProximaDB's architecture, shifting focus towards direct competition with enterprise data warehouses (Snowflake, Databricks, BigQuery, Trino) and modern distributed data engineering engines (Spark, Ray). While the vector and multi-modal capabilities remain important, the foundational MVP and initial product wedge will now explicitly prioritize relational semantics, object-storage-first decoupled architecture, and Rust-native distributed execution.

== 1. Strategic Shifts

=== 1.1. Postgres Wire Compatibility as the Primary Wedge
To directly compete for SQL data warehouse workloads against Trino, Snowflake, and Databricks, ProximaDB must present a frictionless adoption path. The primary ingestion, DDL, DML, and query surface will be **100% PostgreSQL wire protocol compatible**. 
* **Why:** BI tools (Tableau, Looker, Metabase), dbt, and existing enterprise SQL workloads expect a Postgres or MySQL dialect. By prioritizing full Postgres compatibility, ProximaDB can be a drop-in replacement for existing analytical query engines.

=== 1.2. Relational Foundation First
While ProximaDB supports documents, graphs, and vectors, the core execution and storage models must optimize for the relational algebra first. 
* **Why:** The enterprise data engineering landscape runs on tables, columns, joins, and aggregates. The multi-modal features (vector search, graph traversal) will be implemented as extensions to the relational algebra, not completely separate paths.

=== 1.3. Object Storage First (Decoupled Storage & Compute)
The storage layer must be fully decoupled from the execution layer from day one, utilizing local filesystem API-driven object storage abstractions (e.g., `object_store` crate) that can seamlessly scale to S3/GCS/Azure Blob.
* **Why:** Snowflake and Databricks won by scaling compute independently from storage. ProximaDB's storage layout will prioritize open table formats (Parquet/Arrow/Iceberg) layered over object storage, utilizing advanced optimizations like header/filter-driven caches and late materialization to minimize I/O overhead.

=== 1.4. Rust-Native Distributed Execution (Spark-like)
Instead of purely delegating large-scale transformations to external Spark clusters, ProximaDB will build a **Rust-native distributed execution engine** close to the data, capable of PySpark-style API execution.
* **MVP:** The initial implementation will leverage **DataFusion** running in a single-node or pseudo-distributed seam (e.g., multi-process or multi-threaded worker model).
* **Future-proofing:** The execution architecture will remain flexible via a pluggable engine trait, allowing us to swap or enhance DataFusion with custom Rust-based distributed workers for CPU-intensive (Monte Carlo style) jobs and async I/O heavy workloads.
* **Why:** A unified Rust engine removes the JVM overhead of Spark, drastically lowers memory pressure, and simplifies the deployment topology while providing the same distributed data engineering capabilities.

== 2. Blueprint for the MVP

=== 2.1. Pseudo-Distributed DataFusion Seam
The MVP will construct a single-node platform that simulates distributed execution. 
1. **Coordinator/Worker Abstraction:** A master coordinator process (or thread pool) handles Postgres wire connections, parses SQL/DataFrame logic, and creates logical plans.
2. **Pluggable Execution:** The logical plan is lowered into a physical plan executed by DataFusion workers. The design must isolate DataFusion behind an `ExecutionEngine` trait so it can be swapped.
3. **Async I/O Efficiency:** Workers will use Tokio async patterns to fetch Parquet/Arrow blocks from the object storage layer concurrently.

=== 2.2. Object Storage & Caching Layer
* **Storage Interface:** All data reads/writes go through an `ObjectStore` trait implementation. For MVP, this points to a local directory structure mimicking an S3 bucket layout.
* **Header/Filter Caching:** To achieve high performance, the system will implement aggressive caching of Parquet footers/headers and Iceberg manifests. 
* **Predicate Pushdown:** Query planning will utilize these caches to perform partition pruning and row-group skipping before any network/disk read occurs.

=== 2.3. Data Engineering Experience
* Provide a Python DataFrame API (via PyO3) that mimics PySpark's lazy execution model, compiling down into the same DataFusion/Rust logical plan that the SQL engine uses.
* Allow users to write UDFs in Rust or Python, executed safely within the worker processes.

== 3. Impact on Existing Architecture

* **Update to `DATA_AI_PLATFORM_ARCHITECTURE_ANCHOR`:** The anchor must reflect that local Rust execution is not just for HTAP, but the foundation for a distributed MPP warehouse.
* **Update to `COMPETITIVE_OLTP_OLAP_MPP_TRAJECTORY`:** Shift from "Delegate MPP to Spark/Trino" to "Build Rust-native MPP using DataFusion/Ballista concepts, outcompeting Spark/Trino on efficiency."
* **Storage Layer:** Deprioritize highly specialized proprietary file formats in favor of optimized Parquet/Iceberg execution over object storage to match Lakehouse architectures.

== 4. Grounded Reality vs Vision (code audit 2026-06-04)

The course correction above is the target. This section records what is *actually
built today* so the roadmap (§6) closes a real gap, not an imagined one. Audited
against the live tree on branch `proximadb-embedding`.

[cols="3,1,1,4",options="header"]
|===
| Component | Exists | Wired live | Notes (file)
| pgwire relational algebra — joins / GROUP BY / aggregates / multi-col ORDER BY /
  WHERE+OR pushdown | YES | **LIVE** | Hand-rolled **Volcano** push-executor, NOT
  DataFusion. `network/postgres/relational_pipeline.rs` (`try_run_select`); engages
  only for joins/agg, simple SELECTs stay on legacy. ADR-018 Phase 2 / Slice 8.
| `proximadb-relational-{frontend,algebra,planner,executor,reader,engine}` | YES |
  **LIVE** | L1 SQL→LogicalNode (sqlparser) · L2 algebra · L3 physical planner · L4
  Volcano executor · L5 storage-reader trait. Reads real data via
  `DmlTableReader`→`DmlService::scan_table_relational`→`RecordStorage`.
| DML writes (INSERT/UPDATE/DELETE) | YES | **LIVE** | `services/dml/mod.rs`
  →`TableRecordStore`→`DirectWalTableRecordStore` (WAL + `RecordStorage` DashMap) →
  engine layouts. **No Parquet/Iceberg write.**
| DataFusion (`mc_price` UDF + `FilesystemParquetSplitReader`) | YES | **UNWIRED** |
  `src/datafusion/**` behind optional `datafusion-integration` feature; test-only
  (financial workload). Zero pgwire/REST/gRPC coupling. (`19a2688fa`)
| `object_store` crate | YES | **read-only** | ADR-023 cold-tier IVF byte-RANGE
  reads (`filesystem/aws_s3.rs`,`azure_blob.rs`). Not a write authority.
| `ObjectStoreIcebergRecordStore` / `ObjectStoreVectorRecordStore` | NO | — | Design
  targets in the pgwire blueprint; **no code**.
| `ComputeProvider` / `LocalComputeProvider::execute` | PARTIAL | **DEAD STUB** |
  `compute/provider/local.rs` — `execute()` `bail!`s; every node returns
  `empty_stream()`. The pluggable `ExecutionEngine` seam is a shell.
| Python DataFrame API (PySpark-like → logical plan) | NO | — | Not built.
|===

**Headline:** the relational *wedge is already live* — but on a hand-rolled Volcano
executor over WAL+`RecordStorage`, while DataFusion, Polars, and object-storage-write
are unwired. The roadmap is therefore *gap closure + a deliberate multi-engine seam*,
not a green-field build.

**Target storage decomposition (per the revised LLD + storage-format docs,
2026-06-04 — aspirational, not yet coded):** the relational durable format moves from
PAX block-modes to **Parquet/Iceberg over object storage**, via two NEW crates —
`proximadb-object-store` (an `object_store`-crate wrapper) and `proximadb-iceberg-engine`
(the Parquet/Iceberg primary store for relational/document/graph/observability). **PAX
and the SST/VIPER/NOVA/HELIX/RAPTOR/SWIFT engines are reserved EXCLUSIVELY for the
vector/ANN modality.** WAL+`RecordStorage` is NOT removed: it becomes the **OLTP
write-ahead + freshness/delta tier** in front of the Iceberg durable tier — Volcano
serves strong-freshness reads from the delta, a background flush/compaction promotes it
to Parquet/Iceberg, and DataFusion/Polars read the base snapshots (optionally unioning
the delta per the §5 freshness boundary). This doc (not the now-[SUPERSEDED]
`MULTIMODAL_OVERHAUL_SPEC`) is the storage-model authority.

== 5. Intelligent Multi-Engine Routing (Policy to RL Evolution)

DECISION (2026-06-04): The architecture is not a binary choice between Volcano and DataFusion. The system utilizes an **Intelligent Multi-Engine Routing** multiplexer. To prevent fragmentation, all engines consume the same logical plane:

* **Shared logical plane, split physical plane.** All queries pass through `proximadb-relational-frontend` → `proximadb-relational-algebra` (`LogicalNode`). They diverge ONLY at the physical planner/executor. 

### 5.1. The Routing Destinations
The Query Router evaluates cardinality, partition counts, and freshness SLAs to select the optimal physical engine:
* **Volcano (OLTP):** Selected for point lookups, high-concurrency single-row DML (INSERT/UPDATE/DELETE), and queries demanding immediate, strong freshness against the WAL/Memtable.
* **Polars (Local Analytics):** Selected for single-partition queries, lightweight aggregations, and local data transformations. Bypasses the overhead of DataFusion's distributed worker context, acting as the high-speed local execution seam.
* **DataFusion (MPP/OLAP):** Selected for multi-table joins, distributed processing, large-scale Parquet/Iceberg object storage scans, and complex window functions.
* **Specialized Engines (Vector/ANN):** Selected for similarity search, routing execution down to SST, VIPER, NOVA, HELIX, RAPTOR, SWIFT via the PAX block layout.

### 5.2. Routing Evolution: From Policy to Reinforcement Learning
* **Phase 1: Policy & Heuristic-Driven (MVP)**
  For the SaaS MVP, routing must be predictable and explicitly tied to SLAs and budgets. The `ComputeScheduler` relies on strict, rule-based policies. For example: if `partitions == 1` and `join_count == 0`, route to Polars. If `point_lookup == true`, route to Volcano.
* **Phase 2: RL-Driven Autonomous Routing**
  Leveraging the existing `RLPlanner` (Thompson sampling, Contextual Bandits), the system observes the execution of the policy-driven routes. It continually learns cost models based on observed latency, throughput, and I/O. As the cluster matures, the RL engine autonomously tunes the routing logic to find the perfect equilibrium between Polars' speed, DataFusion's scale, and Volcano's freshness for complex HTAP workloads.

* **Convergence-Gate registration.** This multi-engine seam is a deliberate by-design separation; record it in `CONVERGENCE_AUDIT_2026_05_30.adoc`.

== 6. Sequenced Roadmap (reality → vision)

Each phase is independently shippable + verifiable. The phases stand up the §5
`ComputeScheduler` and its four routing destinations one engine at a time, behind a
contract that is made explicit BEFORE any engine is added — so the live Volcano
path never destabilizes. Maturity tags reflect the §4 audit (NEW = not in the tree
yet; WIRE = code exists but unwired; LIVE = extend an existing live path).

. **P0 — `ComputeScheduler` skeleton + route contract + EXPLAIN (no behavior
  change).** Introduce `ComputeRoute { Volcano | Polars | DataFusion | Specialized }`
  and a policy/heuristic `ComputeScheduler` (the §5.2 Phase-1 rule engine) at the
  pgwire planner boundary. For now the rules ALWAYS pick `Volcano` for relational and
  `Specialized` for vector (today's behavior), but the decision is materialized: emit
  `compute_route`/`workload_profile`/`freshness_sla` + an `EXPLAIN` line, and register
  the multi-engine seam in `CONVERGENCE_AUDIT_2026_05_30.adoc`. Pure additive; locks
  the contract before any second executor exists. _(scheduler = NEW; route fields reuse
  the existing typed route surface.)_
. **P1 — First LIVE DataFusion OLAP read.** Teach the scheduler one real OLAP rule
  (e.g. multi-table join / large aggregate over a Parquet-backed table) and route it to
  a DataFusion `SessionContext` over the existing `FilesystemParquetSplitReader` —
  turning `src/datafusion/**` into a real query path for the first time. pgwire
  smoke-test; default stays Volcano, opt-in by table/feature. _(WIRE.)_
. **P2 — Polars local-analytics seam.** Add the §5.1 Polars destination: a
  single-partition / lightweight-aggregation rule (`partitions == 1 && join_count ==
  0`) routes to a Polars adapter that bypasses DataFusion's worker context. _(Polars =
  NEW dependency + adapter; gate behind a feature like `datafusion-integration`.)_
. **P3 — Object-store Parquet/Iceberg write seam (the relational durable target).**
  Stand up the two new crates — `proximadb-object-store` (`object_store` wrapper) and
  `proximadb-iceberg-engine` (Parquet/Iceberg primary store) — and wire
  `proximadb-iceberg-engine` as a `TableRecordStore` impl behind
  `CatalogRoutingTableRecordStore`, selectable for relational/OLAP-profile tables. This
  is the durable BASE tier; WAL+`RecordStorage` stays as the OLTP write-ahead +
  freshness DELTA in front of it, with a background flush/compaction promoting
  delta→Parquet/Iceberg (the staging/durable tiering from §4). Vector tables keep
  PAX/SST/VIPER/etc. over `proximadb-object-store` (`ObjectStoreVectorRecordStore`
  later). _(both crates NEW; object_store is read-only today; phase the cutover —
  OLAP-profile tables first, OLTP/HTAP once delta-union read is proven.)_
. **P4 — Shared logical lowering.** Lower `proximadb-relational-algebra::LogicalNode`
  into a DataFusion `LogicalPlan` (and a Polars frame), so every destination consumes
  the SAME frontend/algebra the Volcano path uses — discharging the "shared logical
  plane, split physical plane" guarantee of §5 in code. _(NEW lowering.)_
. **P5 — Pseudo-distributed worker seam.** Replace the dead
  `LocalComputeProvider::execute` stub with a real DataFusion-backed `ExecutionEngine`
  + the notebook blueprint's coordinator/worker model (multi-process/thread). Async
  Tokio object-store fetch + footer/manifest caches + predicate pushdown. _(replaces a
  STUB.)_
. **P6 — Python DataFrame API (PySpark-like).** PyO3 DataFrame that lowers to the SAME
  logical plan as SQL, scheduled by the same `ComputeScheduler`. _(NEW.)_
. **P7 — RL-driven routing (§5.2 Phase 2).** Feed the policy scheduler's observed
  latency/throughput/I-O into the existing `RLPlanner` (Thompson sampling / contextual
  bandits) so it learns cost models and autonomously tunes the Volcano/Polars/
  DataFusion/Specialized equilibrium for HTAP. _(extends an existing component;
  policy rules remain the safe fallback + the RL action space.)_

**SaaS MVP cut (§9):** P0–P3 + P5 deliver the §9.1 MVP scope — single-node
pseudo-distributed execution, object-store persistence, Volcano/Polars/DataFusion +
Specialized vector over pgwire/REST/gRPC, multi-tenant RLS, billing/quota. P4/P6/P7
harden + extend post-MVP; full distribution (Raft/sharding) is §9.2 Phase 6, funded
by MVP revenue.

Secondary track (vector-era cleanup, not on the warehouse critical path):
**TD-104 ROOT deletion** — the `UnifiedHandlers` monolith is vector-DB-era; the
relational/pgwire wedge already bypasses it (pgwire never used the monolith). Continue
per the S3-a..f plan in `CONVERGENCE_SESSION_HANDOFF_2026_06_03.adoc` §3bis when
warehouse phases permit; it does not block P0–P7.

== 7. Anchoring

This document is a feeder to the apex `SYSTEM_MAP_2026_05_30.adoc` and refines, not
replaces, `COMPETITIVE_OLTP_OLAP_MPP_TRAJECTORY_2026_05_20.adoc` (route map),
`RELATIONAL_PGWIRE_DML_COMPUTE_BLUEPRINT_2026_05_20.adoc` (active DML/compute
tracker), and the compute spec `docs/_internal/roadmap/specifications/
NATIVE_COMPUTE_ENGINE_SPECIFICATION.adoc` (which must gain the §5 `ComputeScheduler` /
four-destination routing terminology as it is revised). The multi-engine routing
contract (§5) must be reflected in the Convergence Audit before P1 lands a second
physical executor.

As of 2026-06-04 this doc is also the **storage-model authority**: it supersedes
`roadmap/MULTIMODAL_OVERHAUL_SPEC_2026_05_08.adoc` (now marked [SUPERSEDED] — its
unified `ProximaRecord` envelope is retained, its single-physical-execution mandate is
not), and `RELATIONAL_DOCUMENT_GRAPH_CONVERGENCE_2026_05_14.adoc` re-points its durable
convergence target here. The revised `MODULE_COMPONENT_LLD_2026_05_22.adoc` adds the
`proximadb-object-store` + `proximadb-iceberg-engine` crates and reserves
`proximadb-block-format`/PAX + the SST-family engines for the vector modality; the
revised `RELATIONAL_STORAGE_FORMAT_AND_INTEROPERABILITY_2026_05_19.adoc` makes
Parquet/Iceberg-over-object_store the relational internal format. The roadmap §6 (esp.
P3) is the executable bridge from the live WAL+`RecordStorage` path to that target.

== 8. Stacked Modalities over the Relational Spine (The Multi-Modal Moat)

ProximaDB’s foundational "moat" is providing zero-ETL multimodal capabilities over a single **canonical relational spine**. Instead of running three separate databases for SQL, Document, and Graph data, ProximaDB stacks all modalities on the same durable foundation.

* **The Relational Spine:** Every piece of data is durably stored as a canonical `ProximaRecord` containing a `ProximaValue` tuple, aligned with a standard tabular schema.
* **Document Modality (JSON):** Documents are stored as `JSONB`/`msgpack` typed columns within standard relational tables. `xCatalog` can automatically shred highly-structured, recurring JSON paths into virtual relational columns (using Iceberg path-level metadata and Parquet structural stripes) for vectorized DataFusion/Polars scans.
* **Graph Modality:** Graph topology is not a distinct durable silo. Nodes and Edges are standard relational tables. Specialized Graph algorithms (Cypher traversals, PageRank) operate over **derived, rebuildable projections** (e.g., compressed CSR/CSC neighbor lists stored in PAX/Parquet). If the graph projection is stale or lost, it is deterministically rebuilt from the canonical edge table.
* **Vector/ANN Modality:** Vector embeddings are stored as `List<Float32>` columns. The high-performance search indices (HNSW, LSH, IVF) are projections managed by specialized execution engines (SST, VIPER, NOVA, HELIX, RAPTOR, SWIFT). These engines handle SIMD acceleration and spatial co-location via the specialized PAX block layout, operating alongside the relational pathways.
* **Observability Modality:** Logs, metrics, and traces are time-partitioned relational rows, natively pushed into DataFusion/Polars for aggregations and rollups.

This architecture ensures that a single pgwire or REST request can join a Graph traversal result with a relational aggregation, filtered by a Vector similarity score—all without ever moving data outside the cluster.

== 9. SaaS MVP Rollout and Commercial Strategy

To sustain development and fund the complex, fully distributed MPP roadmap, ProximaDB will be brought to market quickly via a focused **SaaS MVP offering**. This establishes a revenue generation loop and real-world operational feedback.

### 9.1. The SaaS MVP Scope
* **Deployment:** Single-node robust execution (pseudo-distributed worker seam) running in a managed Kubernetes or ECS environment.
* **Storage:** Fully decoupled Object Storage (S3/GCS) utilizing the `object_store` crate.
* **Workloads Supported:** 
  1. **Transactional (Volcano/Polars):** Low-latency point lookups, single-row DML, and Vector Search (via SST/VIPER/HELIX) exposed over REST, gRPC, and Postgres wire.
  2. **Analytical (DataFusion):** Core aggregation, filtering, and projection via Postgres wire over Parquet/Iceberg.
* **Multi-Tenancy:** Hardened tenant isolation via `xCatalog` policy boundaries, RLS (Row-Level Security), and tenant-aware metrics.
* **Billing/Quota:** Built-in budget guards, rate limiting, and exact request/byte-level metering.

### 9.2. Commercial Go-to-Market
* **Primary Wedge:** Position ProximaDB as a **Postgres-compatible AI Database**. It functions as a drop-in replacement for standard analytics and vector search, eliminating the need to sync data between Postgres and a standalone Vector DB like Pinecone or Qdrant.
* **Revenue Engine:** The MVP provides the core capabilities needed for Agentic Intelligence (RAG, memory) and data warehousing on a consumption-based pricing model. The revenue generated will fund Phase 6 (Active Memory & Collective Reasoning) and the fully distributed cluster consensus mechanisms (Raft, distributed sharding) required for petabyte-scale MPP.
