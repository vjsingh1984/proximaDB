= Unified Resource Lease And DML Locking Review
:toc: left
:toclevels: 3
:icons: font
:date: 2026-06-24
:status: Approved correction plan

== Status

Approved on 2026-06-24 as the tracking snapshot for the `feat/unified-resource-key`
branch.

This document governs the follow-up implementation for
`src/cluster/partition_lease.rs` and the planned DML/protocol lock wiring. It is
intentionally correction-first: the unified `ResourceKey` direction is retained,
but the lock semantics and object-store path contracts must be tightened before
the feature is exposed through `DmlService` or protocol handlers.

[IMPORTANT]
====
Do not wire DML locks into REST, gRPC, pgwire, or Arrow Flight until the
foundation gates in <<approved-foundation-gates>> are complete. A protocol-level
lock that is not tenant-correct, path-compatible, hierarchically enforced, and
fenced at the storage writer is only advisory and can create split-brain writes.
====

== First-principles position

Object-store CAS is a strong primitive for coarse ownership, manifest publication,
and generation fencing. It is not, by itself, a high-frequency distributed lock
manager.

Use object-store CAS for:

* resource ownership and leaseholder election;
* fenced manifest and metadata commits;
* WAL/table-shard authority handoff;
* durable operation epochs where the write path validates the epoch.

Do not use object-store CAS as the hot path for every row-level DML lock unless
the write is low-frequency and the storage layer validates the acquired fencing
token before committing.

The target shape is:

* `ResourceKey` is the canonical identity for all lease-protected resources.
* Every durable lease path is tenant-isolated and path-safe.
* Legacy collection leases remain mixed-read-safe during migration.
* DML lock guards are scoped to real tenant/schema/table identity.
* Locks that claim hierarchy enforce ancestor and descendant conflicts.
* A writer carries a fencing generation or operation epoch into the storage layer.
* Protocol handlers map lock outcomes, but do not become the source of
  correctness.

== Approved findings

[cols="1,1,3,4,3", options="header"]
|===
| ID | Priority | Finding | Impact | Approved correction

| F1
| P0
| `std::sync::RwLock` was imported while the code awaits lock operations.
| The branch does not compile; all later design work is untestable.
| Use `tokio::sync::RwLock` for async DML lock registry operations, or make the
section synchronous. Prefer `tokio::sync::RwLock`.

| F2
| P0
| Legacy collection leases and new `ResourceKey::legacy_collection` leases use
different object-store paths.
| Mixed-read-safe migration is not actually safe: old and new pods can create
independent leases for the same collection.
| Define a compatibility path policy. Legacy collection keys must read/write the
old `{prefix}/{tenant}/{collection}/_manifests` path until a separate migration
finalizes the new layout.

| F3
| P0
| DML lock keys use pod id as `tenant_id`.
| Different pods do not contend for the same durable lock, defeating the lock.
| DML acquisition must receive the real `TenantContext` and resolved table
identity. Pod id is only the holder, never the tenant.

| F4
| P0
| The durable lease primitive is single-holder but the API advertises shared
read locks.
| Cross-pod `Read + Read` cannot work with the current durable state. Same-pod
read locks also collapse into one `HashMap` entry without ref counting.
| Choose explicit semantics before wiring: either exclusive-only DML locks for
MVP, or implement a true multi-holder durable read lock record.

| F5
| P0
| ~~Hierarchical locking is declared but not enforced.~~ **FIXED**: enforced.
| ~~A schema lock does not block table/partition/record locks. Tests currently
allow the unsafe behavior.~~ Now blocked: schema/table lock fences descendants.
| ~~Enforce ancestor and descendant compatibility checks.~~ **DONE**:
`DmlLockScope::is_ancestor_of`/`is_descendant_of` wired into `acquire_dml_lock`
ahead of the intent check; verified by `dml_lock_hierarchy`.

| F6
| P0
| DML lock TTL can expire while the mutation continues.
| A stale writer can keep writing after another pod takes the lock.
| ~~Return an RAII guard + pass the fencing generation into the storage write
path.~~ **SEAM DONE (staged)**: guard carries `lease_generation()`; the request
carries it on `WriteIntent::fencing_generation`; the storage writer contract is
the pure, unit-tested `validate_fencing`. Engine-level enforcement (threading
the token to the flush boundary + calling `validate_fencing`) is the follow-up
TD — see <<phase-2-gated>>. (`FlushParameters` deliberately NOT extended: ~150
test construction sites make a required field break the test build for a field
with zero current consumers.)

| F7
| P1
| ~~Release only clears local memory and waits for durable TTL expiry.~~ **FIXED**.
| ~~Elastic handoff and conflict recovery are delayed by TTL.~~
| ~~Add fenced durable release/tombstone.~~ **DONE**:
`PartitionLeaseStore::release_with_key` publishes a **released tombstone at
generation+1** (preserves the monotonic fence; object-store *delete* was
rejected — it would reset generation to 1). `DmlLockGuard` releases durably
via `DmlLockService::release_full`. TTL stays the crash fallback. Verified by
`release_publishes_higher_generation_tombstone`,
`acquire_after_release_keeps_monotonic_generation`,
`release_by_non_holder_is_noop`.

| F8
| P1
| `ResourceKey` path encoding joins raw strings with `/`.
| Tenant, namespace, and resource identifiers can collide or escape path
boundaries.
| Route through `DrPathBuilder` where applicable and encode every component with
a canonical path-safe encoder.

| F9
| P1
| Key-based leases are not renewed by full `ResourceKey`.
| Non-collection resources lose type/namespace identity during renewal.
| Track held leases by canonical `ResourceKey`; renew by key.

| F10
| P1
| Registry reconciliation flattens keys to tenant plus resource id.
| Different resource types and namespaces can collide in the routing registry.
| Registry bindings for generalized resources must include full canonical
`ResourceKey` identity.

| F11
| P1
| CAS per DML lock is expensive for hot serverless paths.
| High-frequency DML can become object-store metadata-bound.
| Keep object-store CAS for coarse ownership and fencing. Co-design hot DML
around WAL shard leases, storage-level conflict checks, idempotent writes, and
batching.

| F12
| P2
| ~~Design text says 12 resource types while code has 13.~~ **FIXED**: Documentation now correctly reflects 13 types.
| Reviewers and future implementers will reason from stale counts.
| ~~Fix documentation and separate resource domain from granularity where useful.~~ **COMPLETED**: Count corrected.
|===

== Approved foundation gates

[cols="1,1,5,3", options="header"]
|===
| Gate | Priority | Required outcome | Blocks

| A1
| P0
| Branch compiles and focused lease tests use the current constructor/API shape.
| All later work.

| A2
| P0
| Legacy collection lease path compatibility is explicit and tested.
| DML/collection protocol wiring.

| A3
| P0
| `ResourceKey` path encoding is canonical, path-safe, and tenant-isolated.
| Generalized resource leases.

| A4
| P1
| `PartitionLeaseManager` renews and reconciles generalized leases by full
`ResourceKey` identity.
| Background renewal for non-collection resources.

| A5
| P0
| DML lock semantics are explicit. MVP uses exclusive write/schema locks unless
true multi-reader durable state is implemented.
| `DmlService` integration.

| A6
| P0
| DML lock acquisition returns a guard that owns renewal/release and carries the
fencing generation to the writer.
| Protocol exposure.

| A7
| P1
| `DmlService::execute_scoped` acquires locks after catalog resolution, using
real tenant/schema/table identity.
| REST/gRPC/pgwire/Flight wiring.

| A8
| P1
| Protocol handlers map lock outcomes consistently.
| User-visible protocol behavior.

| A9
| P1
| Prometheus metrics expose lock contention, wait/fail counts, renewal failures,
and object-store CAS volume.
| Production tuning and billing.
|===

== DML lock semantic decision

MVP DML locking should start with exclusive write/schema locks. Shared read
locks should not be exposed until the durable representation supports multiple
holders and release/reference semantics.

Rationale:

* Most DML mutations need write exclusion, not read locks.
* A single-owner lease maps cleanly to object-store CAS.
* Claiming shared reads on top of a single-holder lease misleads protocol
  integrations.
* Read consistency should primarily come from MVCC/WAL snapshot semantics and
  storage-level visibility, not a distributed read lock on every select.

If shared read locks are required later, introduce a separate durable lock record
shape with:

* holder ids and per-holder expirations;
* monotonically fenced write epoch;
* compare-and-swap update of the holder set;
* bounded holder cardinality or sharded reader tickets;
* explicit garbage collection of expired holders.

== DML integration rules

`DmlService` integration must happen below protocol handlers and above
`TableRecordStore::write_mutations`, after catalog resolution:

1. Resolve tenant/table with `resolve_table_scoped`.
2. Build a DML lock scope from the resolved table identity, not from raw SQL
   text alone.
3. Acquire a write lock guard for INSERT/UPDATE/DELETE/UPSERT and table-write
   plans.
4. Pass the guard fencing generation into the write request once the storage
   trait supports it.
5. Release/complete the guard after success or failure; natural TTL expiry is
   only for crash recovery.

For statement shapes:

* INSERT/UPSERT: table-level write lock for MVP; later row/partition locks only
  after key extraction and deadlock ordering are defined.
* UPDATE/DELETE: table-level write lock for MVP. Row locks can follow once
  predicate-to-key extraction is complete and ordered.
* INSERT SELECT / INSERT OVERWRITE: table-level write lock on the target; source
  reads rely on snapshot semantics.

== Protocol mapping

Protocol handlers should not acquire their own independent locks once
`DmlService` owns lock acquisition. They should only translate lock errors:

[cols="1,3", options="header"]
|===
| Surface | Conflict mapping

| REST
| `409 Conflict` for fail-fast conflict, or `423 Locked` if the API surface
documents lock semantics.

| gRPC
| Prefer `ABORTED` for concurrency conflicts. Use `FAILED_PRECONDITION` when
the request cannot proceed because a prerequisite lock state is absent.

| pgwire
| SQLSTATE `55P03` (`lock_not_available`) for fail-fast lock acquisition.

| Arrow Flight
| Return a structured Flight error that maps to concurrency conflict and carries
retry-after metadata when available.
|===

== Tracking checklist

[cols="1,1,4,2", options="header"]
|===
| Item | Status | Work | Notes

| A1
| ✅ Complete
| Compile and focused test cleanup.
| Branch compiles successfully.

| A2
| ✅ Complete
| Legacy path compatibility.
| Mixed-read-safe migration implemented via `committer_for_key()`.

| A3
| ✅ Complete
| Canonical path encoding.
| `encode_path_component()` URL-encodes all path components.

| A4
| ✅ Complete
| Full-key held lease renewal.
| `held_resource_keys: DashMap<String, ResourceKey>` stores full keys.

| A5
| ✅ Complete
| Enforced DML hierarchy and explicit exclusive-MVP semantics.
| `is_ancestor_of()`/`is_descendant_of()` implemented and wired into acquisition.

| A6
| ⚠️ Staged
| Guard, renewal, release, fencing token.
| Guard carries `lease_generation()`; request carries it on
`WriteIntent::fencing_generation`; writer contract is the unit-tested
`validate_fencing`. Engine-level enforcement + `FlushParameters` threading =
follow-up TD (see <<phase-2-gated>>). F7 durable release also done.

| A7
| Pending (gated)
| `DmlService` integration.
| After the A6-enforcement TD; see <<phase-2-gated>>.

| A8
| Pending (gated)
| Protocol mapping.
| Should not duplicate lock acquisition.

| A9
| ✅ Complete
| Metrics and object-store operation accounting.
| `src/metrics/dml_lock_metrics.rs`: acquisitions_total{outcome,resource_type,tenant_id}
+ duration histogram + held_current gauge + renewal_failures counter; wired into
`acquire_dml_lock` / `release_full` / `renew_held`.
|===

[[phase-2-gated]]
== Phase 2 (gated) — not in this branch

The foundation gates A1-A5 + A9 are complete; A6 is landed as the non-breaking
seam + contract; F7 durable release is done. The remaining work is **gated**
behind the A6-enforcement TD and must NOT be wired into protocol handlers until
it lands (a protocol-level lock that is not fenced at the storage writer is only
advisory — see the IMPORTANT block at the top):

* **A6-enforcement TD:** thread the fencing token from `WriteIntent` through the
  WAL intermediate to the flush boundary, and have each storage engine call
  `validate_fencing(write_gen, current_durable_gen)` before committing. This is
  the split-brain defense that makes DML locks non-advisory. (`FlushParameters`
  was deliberately NOT extended with the field: ~150 test construction sites are
  full literals, so a required field would break the test build for a field with
  zero current consumers — the enforcement TD will thread via `..Default::default()`
  or a dedicated write context.)
* **A7:** `DmlService::execute_scoped` acquires locks after catalog resolution,
  using real tenant/schema/table identity.
* **A8:** protocol error mapping (REST 409/423, gRPC ABORTED, pgwire
  SQLSTATE 55P03, Arrow Flight structured conflict).

Round-2 verification: `cargo check --lib` green; 46 `partition_lease` +
`dml_lock_metrics` + `validate_fencing` nextest cases pass (F5 hierarchy, F7
tombstone, A6 validator, A9 metrics — no regressions).

== Code anchors

* `src/cluster/partition_lease.rs` - unified resource leases and DML lock service.
* `src/cluster/primary_pod_registry.rs` - current collection write-routing registry.
* `src/services/dml/mod.rs` - DML execution boundary where locks should be acquired.
* `src/services/record_store/` - canonical record mutation boundary that must eventually validate fencing.

== Relationship to existing docs

This document refines and constrains:

* `TENANT_COLLECTION_POD_AFFINITY_2026_05_27.adoc`
* `CODESIGN_DIMENSIONAL_ARCHITECTURE_2026_06_19.adoc`
* `CATALOG_OBJECT_MODEL_2026_06_21.adoc`
* `RELATIONAL_PGWIRE_DML_COMPUTE_BLUEPRINT_2026_05_20.adoc`
* `roadmap/techdebt/TD_110_OLTP_OLAP_HTAP_STORAGE_AND_CONSTRAINTS_PLAN_2026_06_04.adoc`

The design remains aligned with the SaaS mandate: tenant isolation must flow
through real `TenantContext`, and control-plane/object-store paths must be
constructed through approved path builders or canonical encoders rather than raw
string joins.
