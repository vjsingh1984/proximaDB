= TD-064: Predicate-Aware Vector Search Correctness And Fallback Reporting
:toc: auto
:toclevels: 3
:date: 2026-05-27

[NOTE]
====
**v0.2 release status: Beta.** Predicate-aware HNSW traversal is wired
(`AxisManager::query_hnsw_with_predicate`, `src/index/axis/management/manager.rs:934`); the planner
emits ADR-011 mode and selectivity; REST/gRPC surfaces predicate shortfall. **`RecallProbeGate` is
diagnostic only in v0.2 — route selection does not consult the gate.** Quantized-route enforcement
is deferred to TD-075. v0.2 ships without a published recall envelope by selectivity band; the
differential test (`tests/filtered_ann_recall_bands.rs`) documents observed recall but does not
gate the release. See link:../SUPPORTED_SURFACE.adoc[`docs/SUPPORTED_SURFACE.adoc`] and
link:../_internal/status/MVP_RELEASE_READINESS_RECONCILIATION_2026_05_28.adoc[MVP Release
Readiness Reconciliation — 2026-05-28].
====

[source,mermaid]
%%{init: {"theme": "neutral"}}%%
flowchart TB
    subgraph Current["Current State (INCORRECT)"]
        A[Query with k=10<br/>+ metadata filter] --> B[HNSW Search<br/>Returns 10 candidates]
        B --> C[Post-filter<br/>Applies metadata filter]
        C --> D[Returns < 10 results<br/>e.g., only 3 match]
        D --> E[Client receives<br/>INCOMPLETE results]
        E -.->|SECURITY ISSUE| F[User sees incomplete<br/>top-k for their tenant]
    end

    subgraph Proposed["Proposed State (CORRECT)"]
        G[Query with k=10<br/>+ metadata filter] --> H{Predicate Planning}
        H --> I[Prefilter-Exact<br/>Full table scan]
        H --> J[Predicate-Aware ANN<br/>HNSW with oversampling]
        H --> K[Post-Filter Fallback<br/>With explicit warning]
        I --> L[Guarantees k results<br/>if they exist]
        J --> M[Uses predicate-aware<br/>candidate budget]
        K --> N[Returns results<br/>+ shortfall warning]
        L --> O[Client receives<br/>COMPLETE results]
        M --> O
        N --> O
    end

== Executive Summary

TD-064 addresses a **critical correctness and security surface** where post-filtered ANN (Approximate Nearest Neighbor) search can return fewer than the requested matching top-k results under tenant/RLS, time, branch, and typed metadata filters.

**Problem**: Legacy and partially-wired paths can perform vector search first, then apply metadata filters as a residual post-filter. If the filter eliminates most candidates, the client can receive fewer results than requested without a route-level indication that the selected plan was degraded.

**Solution**: Complete the existing ADR-011 filtered-ANN routing contract so `VectorTopK` planning distinguishes three execution strategies:
1. **PreFilter** — evaluate scalar predicates first, then run exact or restricted vector scoring over the candidate set
2. **Inline** — predicate-aware ANN via HNSW/IVF predicate callbacks
3. **PostFilter** — overfetch from ANN, apply residual predicates, and disclose degraded/shortfall risk in `EXPLAIN`

This design coordinates with Phase C of the multimodal overhaul spec (`roadmap/MULTIMODAL_OVERHAUL_SPEC_2026_05_08.adoc`).

---

## Current State Analysis

### Verified Existing Infrastructure

=== HNSW Predicate API
`src/index/axis/indexes/hnsw_index.rs:555-606` already implements `search_with_predicate()`:

[source, rust]
----
pub async fn search_with_predicate<P>(
    &self,
    query: &[f32],
    k: usize,
    predicate: P,
) -> Result<Vec<(String, f32)>>
where
    P: Fn(&str) -> bool + Send + Sync,
----

The predicate receives external string IDs and returns true if the record should be included. Internally, it delegates to `search_layer_predicate` at layer 0 using a skip-through traversal: non-passing nodes can still be traversed, but do not enter the result candidate set.

**Gap**: The current public callback shape is `Fn(&str) -> bool`. The Phase C target in `roadmap/MULTIMODAL_OVERHAUL_SPEC_2026_05_08.adoc` calls for predicate evaluation over `ProximaRecord` semantics, including tenant/RLS, branch/time, and typed props. The bridge from ID to visible record metadata is not complete.

=== Filter Contract Layer
`src/core/search/filter_contract.rs` provides:

* `FilterContract` trait — normalized filter evaluation interface
* `NormalizedFilter` — `FilterExpression`-based implementation
* `estimated_selectivity()` — for planning heuristics
* `can_pushdown()` — engine capability detection

=== ADR-011 Routing Policy
`docs/12-design/adr/ADR-011-filtered-ann-routing-modes.adoc` and `crates/control/proximadb-catalog/src/lib.rs` already define `AnnFilteringPolicy` and `AnnFilteringMode`:

* `PreFilter` for selectivity below `pre_filter_threshold` (default `0.05`)
* `Inline` for selectivity between `0.05` and `0.50`
* `PostFilter` for selectivity above `post_filter_threshold` (default `0.50`)
* post-filter overfetch via `effective_top_k_for_post_filter()` (default `2x`)

**Gap**: The policy is not consistently the single route-selection boundary for every `VectorTopK`/AXIS query path, and the selected mode is not consistently surfaced through EXPLAIN.

=== AXIS Filtered Search
`src/index/axis/management/filtered_search.rs` and `src/index/axis/management/manager.rs` already contain scaffolding:

* `make_id_predicate()` converts ID-only `FilterExpression` clauses to HNSW predicates
* `query_hnsw_with_predicate()` calls `search_with_predicate()`
* `execute_exact_filtered_query()` can evaluate metadata filters against in-memory `ProximaRecord` copies

**Gap**: Metadata field predicates currently pass through `make_id_predicate()` and are not evaluated inside HNSW traversal. `AxisMetadataLookup` is a placeholder returning `None`, and exact fallback is backed by `collection_vectors`, not a cataloged record/PAX scanner.

=== Metadata Filter Pushdown
`src/core/search/metadata_filter_pushdown.rs` implements:

* Bloom filters for quick existence checks
* Column statistics for selective filtering
* Index structures (inverted, range, trie)

**Gap**: These data structures are not yet the canonical candidate-set producer for ADR-011 `PreFilter` or the record metadata bridge for `Inline`.

=== Catalog Integration
`crates/control/proximadb-catalog/src/lib.rs` provides:

* `Catalog` trait with schema and metadata access
* `CatalogTableSchema` with column statistics
* `CatalogNamespace` with tenant/DR authority

**Gap**: Catalog policy and statistics exist, but the query planner still needs one typed route decision carrying `ann_filtering_mode`, selectivity source, projection freshness, residual predicates, and rejected/degraded route reasons.

---

## Architecture Design

### Three Execution Strategies

The routing thresholds must reuse ADR-011 `AnnFilteringPolicy`, not introduce a second policy with different constants.

==== 1. PreFilter / Exact Candidate Scoring (Very Low Selectivity)

When metadata filter selectivity is below `pre_filter_threshold` (default `< 5%`):

[source, text]
----
┌─────────────────────────────────────────────────────────────┐
│  Metadata Scan (Parquet/Columnar)                           │
│  - Apply filter first                                      │
│  - Extract matching vectors                                │
│  - Compute distances in batch                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │  Top-K Sort     │
                    │  (Exact k)      │
                    └─────────────────┘
----

*Use case*: Very selective metadata filters (e.g., `tenant_id = 'X' AND status = 'active'` when fewer than 5% of records match)
*Guarantee*: Returns exactly k results if enough records match the filter
*Trade-off**: Higher latency, lower compute efficiency

==== 2. Inline Predicate-Aware ANN (Moderate Selectivity)

When metadata filter selectivity is between `pre_filter_threshold` and `post_filter_threshold` (default `5%-50%`) and a predicate-aware ANN index is fresh enough for the requested snapshot:

[source, text]
----
┌─────────────────────────────────────────────────────────────┐
│  HNSW/IVF predicate-aware traversal                         │
│  - Predicate callback evaluates visible record metadata     │
│  - Skip-through traversal keeps graph connectivity          │
│  - Residual filter verifies final candidates                │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │  Post-Filter    │
                    │  (should be rare)│
                    └─────────────────┘
----

*Use case*: Moderate selectivity where predicate-aware traversal avoids materializing a large prefilter candidate set
*Guarantee*: Approximate ANN recall; residual predicates prevent false positives, but recall risk must be visible when the predicate bridge or projection freshness is degraded
*Trade-off**: Approximate but fast

*Candidate Budget Formula*:
[source, rust]
----
let selectivity = selectivity.max(MIN_SELECTIVITY);
let requested_k = ((k as f64) / selectivity)
    .ceil()
    .min((k as f64) * MAX_OVERSAMPLE) as usize;
// e.g., k=100, selectivity=0.2 → request_k=500
----

This is a candidate budget, not a correctness guarantee. The previous `k * (k / selectivity)` formula was incorrect because it scaled by `k` twice.

==== 3. PostFilter Fallback (High Selectivity)

When metadata filter selectivity is above `post_filter_threshold` (default `> 50%`):

[source, text]
----
┌─────────────────────────────────────────────────────────────┐
│  Traditional ANN Search                                     │
│  - Request k × post_filter_oversample_factor candidates     │
│  - Post-filter metadata                                     │
│  - Return whatever matches (may be < k)                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │  EXPLAIN        │
                    │  Disclosure     │
                    │  " shortfall=87"│
                    └─────────────────┘
----

*Use case*: Non-selective filters where most ANN neighbors are expected to survive the filter
*Guarantee*: Best-effort k results
*Trade-off**: Fastest but may return incomplete results (explicitly disclosed)

---

## Component Design

### 1. VectorTopK / ADR-011 Route Planning

Do not create a second `PredicatePlanningPolicy` with different thresholds. The route decision must extend the existing catalog contract:

* `CatalogTableSchema.ann_filtering_policy`
* `AnnFilteringPolicy::routing_mode(estimated_selectivity)`
* `AnnFilteringPolicy::effective_top_k_for_post_filter(top_k)`
* ADR-004 `FilterDiagnostics` and route/rejection metadata

The planner output should be a typed `VectorTopK` route descriptor carrying:

* selected `AnnFilteringMode`
* selectivity estimate and source (`catalog_stats`, `sample`, `heuristic`, or `forced`)
* pushed predicates and residual predicates
* projection/index freshness state
* candidate budget or post-filter overfetch factor
* degraded/rejected-route reasons

### 2. Mandatory Tenant/RLS Predicate Compilation

Tenant, RLS, branch/time, and typed metadata predicates must be compiled into the same normalized predicate expression before route selection. A standalone placeholder builder that checks only `rls_policy_id` is not sufficient.

The correct boundary is:

1. Resolve tenant/session/policy context from catalog and request context.
2. Compile mandatory policy predicates into `FilterExpression` / normalized filter form.
3. `AND` mandatory predicates with user predicates.
4. Validate against xCatalog schema and supported-surface maturity.
5. Pass the combined expression into `VectorTopK` planning.

### 3. HNSW Predicate Bridge

Extend the existing AXIS bridge rather than adding a parallel one:

* `src/index/axis/management/filtered_search.rs::make_id_predicate()` currently handles ID-only clauses.
* `src/index/axis/management/manager.rs::query_hnsw_with_predicate()` currently calls `search_with_predicate()`.
* `src/core/search/filter_contract.rs::MetadataLookup` already defines single and batch lookup hooks.

TD-064 should replace ID-only pass-through behavior with a record-aware bridge:

[source, rust]
----
fn predicate_for_filter(
    filter: Arc<dyn FilterContract>,
    lookup: Arc<dyn MetadataLookup>,
) -> impl Fn(&str) -> bool + Send + Sync {
    move |id| {
        let metadata = match lookup.get_metadata(id) {
            Ok(Some(metadata)) => metadata,
            _ => return false, // fail closed for policy-bearing predicates
        };
        filter.evaluate_row(&metadata).unwrap_or(false)
    }
}
----

This bridge must distinguish mandatory policy predicates from optional user filters. Missing metadata may safely degrade optional filters to residual evaluation only when the route explicitly records that degradation; missing metadata for tenant/RLS must reject or route to an exact canonical scan.

### 4. Record Metadata Lookup

Do not invent a non-existent `ZeroCopyMetadataCache` API or describe the WAL as the normal point-read source. The authority chain is WAL/log/manifest plus canonical record storage; runtime lookups should use cataloged record/PAX/SST/columnar access methods and their existing cache/filesystem layers.

The implementation target is a `MetadataLookup` backed by the same record source used for canonical visibility:

* hot current-state/memtable or `collection_vectors` during the migration period
* PAX/SST/columnar row-directory or ID lookup for flushed records
* cataloged secondary/path indexes for `PreFilter` candidate generation
* cache/filesystem zero-copy helpers as an optimization, not a separate authority

Key design points:

1. **No hidden authority** — metadata values come from visible `ProximaRecord` state.
2. **Secondary indexes are allowed** — JSON/path/inverted/range indexes are Layer 2 projections and can be used for `PreFilter`; they are not durable truth.
3. **Batch first** — HNSW predicate callbacks are per-ID, but the bridge should prefetch/batch candidates when the executor can expose the frontier or candidate set.
4. **Freshness explicit** — bounded-lag projections can be used only when the requested snapshot and policy boundary allow them.

### 5. EXPLAIN Integration

File: `src/observability/search_plan_trace.rs` (extend existing)

[source, rust]
----
/// Extended search plan trace with predicate-aware information
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PredicateAwarePlanTrace {
    /// Chosen execution strategy
    pub strategy: ExecutionStrategy,

    /// Estimated filter selectivity
    pub selectivity_estimate: f64,

    /// Oversample factor (if predicate-aware ANN)
    pub oversample_factor: Option<f64>,

    /// Expected shortfall percentage (if post-filter fallback)
    pub expected_shortfall_pct: Option<f64>,

    /// Actual results returned vs requested
    pub results_returned: usize,
    pub results_requested: usize,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub enum ExecutionStrategy {
    /// Full metadata scan before vector search
    PrefilterExact,

    /// HNSW with predicate pushdown
    PredicateAwareAnn { oversample_k: usize },

    /// Post-filter with possible shortfall
    PostFilterFallback,
}
----

---

## Implementation Plan

### Phase 1: Foundation (Week 1-2)

==== P1.1 Complete ADR-011 Route Selection
* [ ] Thread `CatalogTableSchema.ann_filtering_policy` into `VectorTopK` / AXIS query planning
* [ ] Route through existing `AnnFilteringPolicy::routing_mode()`
* [ ] Carry selected mode, selectivity source, projection freshness, and rejected/degraded reasons
* [ ] Add tests for `PreFilter`, `Inline`, forced mode, and `PostFilter`

==== P1.2 Bridge FilterContract to HNSW Predicate
* [ ] Extend `src/index/axis/management/filtered_search.rs` beyond ID-only predicates
* [ ] Implement `FilterContract` + `MetadataLookup` conversion for `search_with_predicate()`
* [ ] Preserve residual predicates for approximate or unsupported inline evaluation
* [ ] Add tests for ID, tenant, typed metadata, missing metadata, and fail-closed behavior

==== P1.3 Canonical Record Metadata Lookup
* [ ] Implement `MetadataLookup` backed by visible `ProximaRecord` state, not catalog schema rows
* [ ] Use hot current-state during migration and record/PAX/SST ID lookup for flushed records
* [ ] Add batch lookup/prefetch support where executor shape permits it
* [ ] Add tests for snapshot visibility, tombstones, tenant fields, and props conversion

**Deliverable**: Foundation components with TDD tests passing.

### Phase 2: Tenant/RLS Integration (Week 3-4)

==== P2.1 Tenant/RLS Predicate Compiler
* [ ] Compile tenant, RLS, branch/time, and supported typed predicates into the normalized filter contract
* [ ] Reject or exact-route predicates that cannot safely run through the selected projection
* [ ] Add inline tests for mandatory predicate composition and schema validation

==== P2.2 Predicate Injection in Search Path
* [ ] Modify the shared `VectorTopK` / AXIS planning boundary
* [ ] Inject tenant/RLS filters before planning
* [ ] Ensure filters are never bypassed
* [ ] Add security tests for filter injection

==== P2.3 Security Validation
* [ ] Add tests that verify tenant isolation
* [ ] Add tests that verify RLS enforcement
* [ ] Add tests for cross-tenant access attempts
* [ ] Document security guarantees

**Deliverable**: Tenant/RLS filters guaranteed to be applied in all paths.

### Phase 3: Execution Integration (Week 5-6)

==== P3.1 Prefilter-Exact Execution
* [ ] Implement metadata-first candidate generation from canonical record scans and cataloged secondary indexes
* [ ] Add SIMD distance computation on filtered vectors
* [ ] Integrate with PAX/SST/columnar row group pruning and projection freshness checks
* [ ] Add performance benchmarks

==== P3.2 Predicate-Aware ANN Execution
* [ ] Wire record-aware predicates into `search_with_predicate()` calls
* [ ] Implement adaptive candidate budgets / ef_search scaling; do not mutate construction-time `ef_construction` per query
* [ ] Add exact-route fallback when mandatory predicates cannot be evaluated inline
* [ ] Add recall tests

==== P3.3 Post-Filter Fallback with Disclosure
* [ ] Use `AnnFilteringPolicy::effective_top_k_for_post_filter()` for overfetch
* [ ] Implement explicit shortfall tracking
* [ ] Add warning emission when results < k
* [ ] Update EXPLAIN output with shortfall information
* [ ] Add telemetry for shortfall events

**Deliverable**: All three execution strategies wired end-to-end.

### Phase 4: EXPLAIN and Observability (Week 7-8)

==== P4.1 Extended EXPLAIN Output
* [ ] Add `PredicateAwarePlanTrace` to search traces
* [ ] Include strategy choice reasoning
* [ ] Include selectivity estimates
* [ ] Include oversample factors and shortfall warnings

==== P4.2 Telemetry and Metrics
* [ ] Emit metric for each execution strategy
* [ ] Emit metric for selectivity estimation accuracy
* [ ] Emit metric for shortfall events
* [ ] Add dashboard panels

==== P4.3 Validation Tests
* [ ] Add integration tests for correctness
* [ ] Add tests for recall guarantees
* [ ] Add security penetration tests
* [ ] Document migration path for existing queries

**Deliverable**: Full observability and validation.

---

## Security Considerations

=== Tenant Isolation
* **Tenant filter MUST be injected at the earliest possible point** — before any storage or index access
* **Tenant filter CANNOT be bypassed by user query** — always ANDed with user-provided filters
* **Cross-tenant access MUST return empty results, not error** — prevents tenant enumeration

=== RLS Enforcement
* **RLS policy lookup MUST happen at query time, not insert time** — policies can change
* **RLS filter CANNOT be overridden by user query** — always applied regardless of user filters
* **RLS violations MUST be logged** — for security audit trails

=== Filter Injection Safety
* **User-provided filters MUST be validated against schema** — prevent injection attacks
* **Filter evaluation MUST be sandboxed** — prevent code execution via malicious JSON
* **Filter errors MUST fail closed** — exclude record on error rather than include

---

## Performance Considerations

=== Selectivity Estimation
* **Use catalog statistics when available** — accurate estimates from histograms
* **Fall back to heuristics when not** — default selectivity per operator type
* **Cache estimates per collection** — avoid repeated catalog lookups

=== Oversampling Trade-offs
* **Candidate budget = min(k / selectivity, k × MAX_OVERSAMPLE)** — balances recall vs latency without double-scaling by `k`
* **Monitor actual vs expected shortfall** — adjust thresholds based on real data
* **Cap oversampling at 10× by default** — prevent pathological cases

=== Caching Strategy
* **Cache metadata lookups during predicate execution** — reuse across candidates
* **Treat cache as projection state** — visibility, freshness, and invalidation must be explicit
* **Size cache by collection and policy boundary** — prevent memory bloat and cross-tenant reuse

---

## Testing Strategy

=== Unit Tests (TDD)
* `test_prefilter_exact_strategy()` — low selectivity routing
* `test_inline_predicate_aware_strategy()` — moderate selectivity with record-aware predicate evaluation
* `test_post_filter_fallback_strategy()` — high selectivity with overfetch and disclosure
* `test_tenant_filter_injection()` — tenant isolation guarantees
* `test_rls_filter_enforcement()` — RLS policy application
* `test_selectivity_estimation()` — catalog statistics usage

=== Integration Tests
* `test_end_to_end_prefilter_exact()` — full path with real PAX/SST/columnar data
* `test_end_to_end_predicate_aware()` — HNSW with real predicate
* `test_cross_tenant_isolation()` — verify no cross-tenant leakage
* `test_rls_policy_changes()` — verify policy updates are respected
* `test_shortfall_disclosure()` — verify warnings are emitted

=== Security Tests
* `test_tenant_enumeration_prevention()` — verify no info leak on missing tenant
* `test_rls_bypass_attempts()` — verify RLS cannot be bypassed
* `test_filter_injection_attacks()` — verify malicious filters are handled safely
* `test_cross_tenant_filter_injection()` — verify users can't filter on other tenants

=== Performance Tests
* `bench_prefilter_exact_latency()` — measure scan+compute latency
* `bench_predicate_aware_recall()` — measure ANN recall with oversampling
* `bench_selectivity_estimation_accuracy()` — measure estimate vs actual
* `bench_oversample_factor_impact()` — measure latency vs recall trade-off

---

## Migration Path

=== Phase 1: Opt-In (Week 1-2)
* Add feature flag `PREDICATE_AWARE_SEARCH=1`
* New behavior only applies when flag is set
* Existing queries unaffected

=== Phase 2: Monitor (Week 3-4)
* Enable for 10% of traffic
* Monitor latency, recall, shortfall metrics
* Adjust thresholds based on real data

=== Phase 3: Default (Week 5-6)
* Change default to enabled (flag can still disable)
* All new queries get predicate-aware planning
* Document migration for any query changes needed

=== Phase 4: Remove Fallback (Week 7-8)
* Feature flag remains for emergency disable
* Post-filter fallback still available with disclosure
* Plan deprecation of legacy path for future version

---

## Open Questions

=== Q1: Selectivity Estimation Accuracy
*Question*: How accurate are catalog statistics for selectivity estimation?

*Investigation needed*:
* Measure estimate vs actual for real workloads
* Identify common patterns where estimates fail
* Consider histogram vs sampling approaches

=== Q2: Oversample Factor Tuning
*Question*: What is the optimal oversample factor for different selectivities?

*Investigation needed*:
* Benchmark oversample factors 2×, 5×, 10×
* Measure recall vs latency trade-off
* Consider dynamic adjustment based on historical performance

=== Q3: Predicate Callback Overhead
*Question*: How much overhead does the predicate callback add to HNSW traversal?

*Investigation needed*:
* Profile `search_with_predicate()` vs vanilla search
* Measure impact of different filter complexities
* Consider JIT compilation for hot predicates

=== Q4: Cache Sizing and Eviction
*Question*: How large should the metadata cache be, and what eviction policy?

*Investigation needed*:
* Measure cache hit rates for different workloads
* Consider LRU vs size-based eviction
* Consider per-collection cache limits

---

## Coordination Points

=== With ADR-002 / ADR-019 (Filter Contract And Query Architecture)
* Reuse `FilterExpression` from relational frontend
* Align predicate pushdown with relational predicate pushdown
* Share selectivity estimation logic

=== With TD-040 (Quantization-Aware Pruning)
* Use PAX/SST/columnar row group statistics for prefilter decisions
* Share vector bounds metadata for selectivity estimation
* Coordinate pruning strategies

=== With xCatalog (Route And Schema Authority)
* Reuse `AnnFilteringPolicy` and `FilterDiagnostics`
* Align selectivity/statistics format
* Record projection freshness and policy-boundary requirements

=== With Multimodal Overhaul Phase C
* Follow `ProximaRecord` + `ProximaValue` patterns
* Align with stacked durability architecture
* Support time-travel and branch predicates

---

## Success Criteria

=== Functional
* [ ] All three execution strategies implemented and tested
* [ ] Tenant/RLS filters guaranteed in all paths
* [ ] EXPLAIN shows strategy choice and shortfall warnings
* [ ] No regression in existing functionality

=== Performance
* [ ] Prefilter-exact latency within 2× of current exact scan for low-selectivity queries
* [ ] Predicate-aware ANN maintains >90% recall at ADR-011 inline selectivities
* [ ] Post-filter fallback latency unchanged (disclosure only)
* [ ] Selectivity estimation error <20% for common patterns

=== Security
* [ ] Zero cross-tenant leakage in penetration tests
* [ ] Zero RLS bypass in penetration tests
* [ ] All filter injection attacks safely handled
* [ ] Security audit passes

=== Observability
* [ ] Every query emits strategy choice metric
* [ ] Every shortfall event emits warning metric
* [ ] EXPLAIN shows full predicate-aware plan
* [ ] Dashboard panels show strategy distribution

---

## References

* `docs/10-quality/TECHNICAL_DEBT.adoc` — TD-064 definition
* `roadmap/MULTIMODAL_OVERHAUL_SPEC_2026_05_08.adoc` — Phase C predicate pushdown
* `docs/12-design/RELATIONAL_DOCUMENT_GRAPH_CONVERGENCE_2026_05_14.adoc` — Stacked durability
* `docs/12-design/OPEN_FORMAT_CATALOG_2026_05_17.adoc` — xCatalog integration
* `src/core/search/filter_contract.rs` — Filter contract interface
* `src/index/axis/indexes/hnsw_index.rs` — HNSW predicate API

== Appendix: Existing Architecture Patterns

=== Target Canonical Storage Pattern

The target architecture is canonical, but the current AXIS implementation is still transitional. TD-064 must follow this target without claiming it is already fully implemented:

[source, text]
----
┌─────────────────────────────────────────────────────────────┐
│              AXIS / Storage Target Pattern                   │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐    │
│  │      Canonical authority                              │    │
│  │      xCatalog + WAL/log/manifest + ProximaRecord      │    │
│  │  - Single durable authority                          │    │
│  │  - Vectors in embedding cells                         │    │
│  │  - Metadata in typed props                            │    │
│  │  - Tenant/RLS/time/provenance fields                  │    │
│  └─────────────────────────────────────────────────────┘    │
│                          │                                  │
│           ┌──────────────┴──────────────┐                 │
│           ▼                              ▼                 │
│  ┌────────────────┐          ┌────────────────┐           │
│  │ ID lookup      │          │ secondary idxs │           │
│  │ (ID → record) │          │ (Value → IDs)  │           │
│  │ - Rebuildable  │          │ - Rebuildable  │           │
│  │ - ID-only      │          │ - Cache only   │           │
│  └────────────────┘          └────────────────┘           │
│           │                              │                  │
│           ▼                              ▼                  │
│  ┌────────────────┐          ┌────────────────┐           │
│  │ HNSW/IVF/PQ    │          │ Cache/fs tiers │           │
│  │ (ANN fragment) │          │ (optimization) │           │
│  │ - Memory       │          │ - Memory/NVMe  │           │
│  │ - NVMe reload  │          │ - Footer/Header│           │
│  └────────────────┘          └────────────────┘           │
│                                                               │
│  Projection freshness and rebuild source are cataloged       │
│  Cache/filesystem tiers never become semantic authority      │
└─────────────────────────────────────────────────────────────┘
----

=== Current Implementation Delta

Current code does not yet fully match the target:

* `AxisHnswIndex` stores vector payloads in `ZeroOverheadCollection` and quantized side maps, not IDs only.
* `AxisManager` keeps `collection_vectors` as an in-memory `ProximaRecord` map for exact filtered fallback and MVCC expiration lookup.
* `crate::index::MetadataIndex`, `GlobalIdIndex`, `DenseVectorIndex`, and `SparseVectorIndex` in `src/index/mod.rs` are placeholders, while storage-engine-specific metadata indexes also exist.
* `make_id_predicate()` evaluates only ID clauses; non-ID metadata predicates pass through and are handled, if at all, as residual/exact fallback.
* `AxisMetadataLookup` currently returns `None`; it is not a production record metadata bridge.

This is acceptable as a migration state, but TD-064 should be explicit: the work is to converge AXIS and storage lookup onto the canonical authority/projection contract, not to remove all metadata indexes. Secondary indexes are valid Layer 2 projections when cataloged, rebuildable, freshness-aware, and never treated as durable truth.

=== Key Patterns TD-064 Must Follow

1. **Canonical authority** — xCatalog + WAL/log/manifest + visible `ProximaRecord` state owns semantics.
2. **Cataloged projections** — HNSW/IVF/PQ, JSON/path/range indexes, row-group stats, and caches are rebuildable access methods.
3. **Freshness-aware routing** — route to projections only when their freshness satisfies the request and policy boundary.
4. **Record-aware predicates** — tenant/RLS/time/typed props must be available to candidate generation or force an exact canonical route.
5. **Explicit degradation** — post-filter, stale projection, missing metadata, and unsupported predicate cases must surface in EXPLAIN.
