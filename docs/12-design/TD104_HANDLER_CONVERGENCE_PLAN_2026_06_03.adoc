= TD-104 / Seam S1 — UnifiedHandlers Convergence Plan
:toc: left
:toclevels: 2
Date: 2026-06-03
Status: S1 `f141dcc5d` + S2 `7e18a483b` + S3 `20e41ad95`/`<S3a>` SHIPPED — all 4
protocols off the *concrete* root handler. S4/S5 + ROOT deletion pending.

[NOTE]
====
**S3 SHIPPED 2026-06-03** (Arrow Flight off the concrete `UnifiedHandlers`):
* **S3a** (`feat(runtime): RecordOpsPort + relocate BatchOperationResult`): moved
  `BatchOperationResult`/`BatchOperationMetrics` down to `proximadb-runtime` (root
  is a re-export shim; 8 consumers unchanged), added `proximadb-records` dep,
  defined `RecordOpsPort`, and impl'd it on the root `UnifiedHandlers` (delegates
  to `handle_record_*_for_tenant`).
* **S3b** (`20e41ad95`): `ProximaFlightService` now holds `Arc<dyn ApiHandlersPort>`
  + `Arc<dyn RecordOpsPort>` + the vector-ops/collection services directly, not
  `Arc<UnifiedHandlers>`. The two type-mapping risks were eliminated upfront
  (`CollectionPort`/`CollectionService` return the **same** `proximadb_v1::Collection`;
  the record result is the relocated `BatchOperationResult`), so it was a
  same-instance swap — compile-clean ⇒ mechanically correct, behavior identical by
  construction. TDD: the 131 Arrow service/codec tests pass unchanged (the 5
  failures are pre-existing onnx-off + arrow `StructArray`, verified foreign).
  `new()` stays the boot adapter (extracts ports/services from the passed
  `UnifiedHandlers`), so `ArrowFlightServer`/`multi_server` are unchanged.

**What still ties to ROOT** (for the terminal deletion): Arrow's ports are still
*backed by* the root `UnifiedHandlers` via the `new()` boot adapter (the runtime
handler doesn't impl `RecordOpsPort` and doesn't expose the concrete vector/
collection services); the `rest/v1/handlers.rs:1337` `ports=None` fallback
(legacy/cluster); and the build-time AQL/hybrid/memory field-extraction (S5).
ROOT deletion = impl `RecordOpsPort` on a runtime-accessible target + inject the
vector/collection services at boot + S5 + remove the fallback.
====

[NOTE]
====
**S1 partial SHIPPED 2026-06-03** (`f141dcc5d`): `RestServerPorts` now carries
`api_handlers` unconditionally (doc/graph/obs route ports became `Option`), so the
multi-port REST server wires the runtime port-based handler even when gRPC is
disabled — closing the one divergent normal-mode case. The root-handler fallback
(`rest/v1/handlers.rs:1337`) was **intentionally kept** because it is still
load-bearing for the legacy/cluster constructors that pass `ports=None`
(`with_security_and_config`). Removing the fallback is deferred until those paths
also wire `api_handlers`. **Needs a server smoke-test** (unified / multi-port+gRPC /
multi-port+no-gRPC) — not covered by `cargo test --lib`.
====
Seam: CONVERGENCE_AUDIT S1 / TD-104
Branch: proximadb-embedding

[NOTE]
====
Planning artifact for collapsing the two `UnifiedHandlers` onto the runtime
port-based one. Based on a full repo map (2026-06-03). No code changed yet.
Companion to the apex map (`SYSTEM_MAP_2026_05_30.adoc`) and the Convergence Gate.
====

== 1. The seam

Two `UnifiedHandlers` implement `ApiHandlersPort`
(`crates/platform/proximadb-runtime/src/port.rs:26`):

* **ROOT (inherent-logic)** — `src/api_handlers/request_handlers.rs:232`. Holds **12
  concrete services** (`CollectionService`, `VectorOperationsService`, `DocumentService`,
  `ObservabilityService`, `EventLogEngine`, 3× graph services, `MetricsQueryService`,
  `QueryFacadeAdapter`, `DmlService`, `CanonicalPrecisionResolver`). Implements the 7
  `ApiHandlersPort` methods **plus ~30 inherent-only methods** (metrics/admin, record
  APIs, `execute_sql_frontend`, cache/setters).
* **RUNTIME (port-based)** — `crates/platform/proximadb-runtime/src/handlers.rs:129`. Holds
  **3 port trait objects** (`CollectionPort`, `VectorOpsPort`, `QueryAdapterPort`) + cache.
  Implements exactly the 7 `ApiHandlersPort` methods by delegation; **zero inherent logic**.

`ApiHandlersPort` (7 methods): `handle_collection_operation_for_tenant`,
`handle_vector_search_v1{,_for_tenant}`, `handle_vector_batch_v1_for_tenant`,
`handle_vector_v1_for_tenant`, `execute_hybrid_query`, `execute_sql_v1`.

== 2. Current wiring (the good news)

* **REST already prefers RUNTIME.** `src/network/rest/v1/handlers.rs:1337` casts
  `state.api_handlers` (RUNTIME, built in `shared_services.rs:1529`) and only **falls back**
  to `state.request_handlers` (ROOT) when `api_handlers` is `None`. In normal boot both exist
  and REST collection/vector dispatch already goes through RUNTIME.
* **gRPC / Arrow Flight / pgwire are locked to ROOT** — `multi_server.rs` passes
  `services.request_handlers` (ROOT) directly to those servers (many sites ~445–1509).
* **Router-build field extractions are locked to ROOT** — AQL source registration, the
  hybrid backend, bulk-load coordinator, and memory store read concrete fields off ROOT:
  `.vector_operations_service` (rest/v1/handlers.rs:1207/1399/1462/1482/1511/1521/1547),
  `.document_service` (:1492), `.observability_service` (:1498), `.event_log`
  (:1474/1554/1567), `.graph_execution_service` (rest/server.rs:308). These are **build-time
  wiring**, separate from per-request dispatch.

== 3. Blockers to full convergence (missing port surface)

The RUNTIME handler can't yet replace ROOT everywhere because the ports don't cover ROOT's
inherent methods. Ports live in `crates/platform/proximadb-runtime/src/service_ports.rs`.

[cols="2,2,1",options="header"]
|===
| ROOT capability | Port today | Gap
| Collection CRUD / resolve | `CollectionPort` (6 methods) | covered
| Vector search / batch / get | `VectorOpsPort::{search,batch_upsert,get_vector}` | covered
| Hybrid + SQL v1 | `QueryAdapterPort::{execute_hybrid,execute_sql}` | covered
| `metrics` / `collection_metrics` / `query_hints` | — | **missing**
| `force_flush_collection` (port has `flush_all` only) | — | **missing**
| `list_unflushed_vectors` | — | **missing**
| Record APIs (`handle_record_*_for_tenant`) | — | **missing (no DocumentPort)**
| `execute_sql_frontend` (legacy SQL) | — | **missing**
| Build-time service field extraction | — | **no port; structural**
|===

== 4. Recommended slicing

* **S1 — always wire RUNTIME into REST, then remove the ROOT fallback (CORRECTED — NOT a
  trivial cast-removal).** Verified 2026-06-03: the `unwrap_or_else(ROOT)` fallback at
  `rest/v1/handlers.rs:1337` is **load-bearing for multi-port mode with gRPC disabled**, not
  dead. The handlers.rs `AppState.api_handlers` is `Option` (set only via `with_api_handlers`,
  called only when `ports: Some`). In `multi_server.rs`:
** **Unified-port mode (default)** already builds `RestServerPorts` unconditionally with
   `api_handlers: services.api_handlers.clone()` (multi_server.rs:843–848) → REST **already
   uses RUNTIME** there. This half of the seam is effectively closed in the default config.
** **Multi-port mode** builds `rest_ports_opt` **inside the `if enable_grpc` block**
   (multi_server.rs:302/466) → when gRPC is off, `ports == None`, `api_handlers` stays `None`,
   and REST dispatch falls back to ROOT. Replacing 1337 with `.expect(...)` would **panic** in
   that config.
+
**Corrected S1:** hoist the `api_handlers` wiring (and the `doc_port`/`graph_port`/`obs_port`
that `RestServerPorts` bundles) **out of the gRPC-gated block** so the multi-port REST server
always receives `services.api_handlers` (always built at `shared_services.rs:1733`). Then the
1337 fallback is truly dead and can be removed. **Risk: moderate** — touches server-boot wiring
across unified + multi-port (+ gRPC on/off) modes; must be exercised in each mode (can't be
unit-tested from `cargo test --lib`). Est. ~1d incl. mode verification, not 0.5d.
* **S2 — `MetricsPort` (or extend `VectorOpsPort`) (~2d).** Add `metrics`,
  `collection_metrics`, `query_hints`, `force_flush_collection`, `list_unflushed_vectors`;
  implement on the concrete services; surface on RUNTIME.
* **S3 — `DocumentPort`/`RecordPort` (~3d).** Cover `handle_record_{search,get,scan,
  scan_paginated,delete_batch,batch,insert_batch}_for_tenant`; cut gRPC record paths over.
* **S4 — SQL frontend (~2d).** Fold `execute_sql_frontend` into `QueryAdapterPort` (or
  deprecate the legacy path) so the multiplex/pgwire SQL route leaves ROOT.
* **S5 — build-time field extraction (~3d, the hard part).** Re-wire AQL source
  registration / hybrid backend / bulk-load / memory store / graph AQL source to receive
  injected services (or ports) at boot instead of reaching into ROOT's concrete fields.
  Only after S2–S5 can gRPC/Arrow/pgwire move off ROOT and ROOT be deleted.

Full convergence ≈ 10 engineering days. **S1 is independently shippable now** and removes
the live REST dependence on ROOT without touching the other protocols.

=== 4bis. Per-protocol ROOT-consumption (verified 2026-06-03) — REST/gRPC/pgwire OFF ROOT

A handler-consumption audit corrected the picture: of the four protocols, **only Arrow
Flight still consumes the ROOT handler.**

* **REST** — converged in **S1** (`f141dcc5d`).
* **gRPC** — converged in **S2** (`7e18a483b`): gRPC v1 (`crates/platform/proximadb-api/src/grpc/v1/{vector,collection,sql}.rs`) already stored the handler as `Arc<dyn ApiHandlersPort>` and called only the 7 trait methods; document/graph/observability RPCs already use their own ports. Switched the 3 boot sites (`multi_server.rs` start/unified/cluster) from `services.request_handlers` (ROOT) to `services.api_handlers` (RUNTIME). **Needs gRPC smoke-test.**
* **pgwire** — **already independent**: `PostgresServer` (`network/postgres/protocol.rs:50–76`) was built from decomposed ports/services (`CollectionPort`, `VectorOperationsService`, `CatalogManager`, `DocumentService`, `GraphService`, `ObservabilityService`) — never the monolithic handler. No change.
* **Arrow Flight** — **still on ROOT** (the last consumer). `ArrowFlightServer`/`ProximaFlightService` (`network/arrow_ipc/{server,service}.rs`) hold the **concrete** `Arc<UnifiedHandlers>` and use:
** the record-batch methods `handle_record_{insert_batch,batch,delete_batch}_for_tenant` (service.rs:913/917/941) — **ROOT inherent, not in any port**;
** concrete-field access `.collection_service.{collection,list_collections}` (service.rs:1028/1049) — coverable by the existing `CollectionPort`;
** the inherent `.storage_config()` (server.rs:162) — should become flight-server state;
** `handle_vector_search_v1` (service.rs:966) — already `ApiHandlersPort`.

=== 4ter. Arrow Flight migration (the real S3) — design-first, blocked by crate layering

The record-batch methods are the blocker. Their request/response types live in the **root crate**:
`RichRecordBatchRequest`/`RichRecordDeleteBatchRequest` (`src/services/operations/vectors/legacy.rs:85/92`),
`BatchOperationResult` (`src/services/operations/batch_result.rs:48`), and `handle_record_*_for_tenant`
(`src/api_handlers/request_handlers.rs:1021/1069/1145`). A `RecordOpsPort` must live in
`proximadb-runtime` (or a contract crate) which sits **below** the root crate, so it **cannot reference
those root types** as-is. S3 therefore requires, in order:
. **Relocate** `RichRecordBatchRequest`/`RichRecordDeleteBatchRequest`/`BatchOperationResult` (and the
  `Record` payload they carry) down into a shared crate the port can depend on (candidate:
  `proximadb-runtime` or a new `proximadb-record-ops` contract). Mind the dependency direction.
. **Define `RecordOpsPort`** there: `handle_record_insert_batch_for_tenant`, `handle_record_batch_for_tenant`,
  `handle_record_delete_batch_for_tenant`.
. **Impl it** on the concrete service (the bodies move from `request_handlers.rs`, delegating to
  `vector_operations_service`/`document_service`); surface it on RUNTIME (or wire it standalone).
. **Re-type `ArrowFlightServer`/`ProximaFlightService`** to hold `Arc<dyn ApiHandlersPort>` +
  `Arc<dyn RecordOpsPort>` + `Arc<dyn CollectionPort>` instead of `Arc<UnifiedHandlers>`; pass
  `StorageConfig` to the flight server as state to drop `.storage_config()`.
. Then Arrow is off ROOT. With S5 (build-time AQL/hybrid/memory field-extraction injection) and the
  `ports=None` legacy/cluster paths wired, the `rest/v1/handlers.rs:1337` fallback and ROOT itself can be deleted.

**Not a mechanical switch** — it's a cross-crate type relocation + a new port + un-verifiable Arrow
runtime behavior. Do it design-first, in its own session, with a Flight smoke-test.

**Additional wrinkles found (2026-06-03), why this is gated on a running Flight loop:**

* **Result type** — `RecordOpsPort` in `proximadb-runtime` cannot return root-crate
  `BatchOperationResult`. Either move `BatchOperationResult` down (8 files import it) or define a
  port-local `RecordBatchOutcome` and adapt at Arrow's `do_put` result-consumption site. The
  records input is fine: `RichRecordBatchRequest.records` is already `Vec<ProximaRecord>` and
  `proximadb-runtime` can add a `proximadb-records` dep (valid foundation→platform direction, no cycle).
* **Collection type identity** — Arrow `list_flights` (service.rs:1028/1049) reads the
  `CollectionService::collection`/`list_collections` result to build `FlightInfo`. `CollectionPort`
  returns `Result<Option<Collection>>` / `Result<Vec<Collection>>`, but it must be confirmed that the
  port's `Collection` is the **same** type the `FlightInfo` builder consumes (vs a runtime DTO needing
  a field-by-field conversion). This is a correctness question that a compile check alone won't fully
  settle and that no `cargo test --lib` covers.

Because these are correctness unknowns on a **wire-protocol ingest/list path** with no local
verification loop, S3 should be executed where a Flight client smoke-test (DoPut insert/upsert/delete +
DoGet search + ListFlights) can confirm it — not landed blind on a shared branch. S1/S2 were safe to
land compile-only because they were same-trait reference swaps with no type-mapping uncertainty; S3 is
not in that category.

== 5. Convergence Gate notes

* New ports (`MetricsPort`, `DocumentPort`) must declare authority mode / policy boundary in
  their doc headers per the Gate; they are facades over the same concrete services (no new
  durable authority).
* Do **not** expose concrete services as public fields to "fix" S5 — that re-introduces the
  hidden coupling the seam exists to remove. Inject at boot.
* ROOT deletion is the terminal step; keep both handlers until gRPC/Arrow/pgwire + build-time
  wiring are all on ports.
