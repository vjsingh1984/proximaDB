= Agent Memory Layer — High-Level Design (HLD)
:toc: left
:toclevels: 3
:icons: font
:source-highlighter: rouge
:sectnums:
:last-updated: 2026-05-30

Status: Draft, paired with link:adr/ADR-022-agent-memory-layer.adoc[ADR-022] +
Date: 2026-05-30 +
Scope: Architecture, layering, capability-to-primitive mapping, and a phased plan for bringing mem0 /
agentmemory in as first-class citizens of ProximaDB. +
Apex map: link:SYSTEM_MAP_2026_05_30.adoc[SYSTEM_MAP_2026_05_30].

== Problem & Goals

AI agents need long-term, cross-session memory: a place to write distilled facts and read back the
salient ones, with provenance and dedup. The ecosystem solves the *orchestration* of this (mem0,
agentmemory); ProximaDB owns the *substrate* (durable multi-model storage, retrieval, fusion, ranking,
audit). The goal of this HLD is to make ProximaDB a first-class agent-memory backend by **reusing** its
existing surfaces and positioning mem0/agentmemory as compatible consumers — per ADR-022.

=== Non-goals

* **Not** a new storage engine, WAL path, or catalog contract — memories are `ProximaRecord`s.
* **Not** a re-implementation of mem0's pipeline or a vendoring of agentmemory's code.
* **Not** a new LLM-provider abstraction — extraction/consolidation reuse `LLMIntegrationEngine`.

== Layering — orchestration vs storage

The single most important framing: mem0 and agentmemory are *orchestration libraries*; ProximaDB is the
*storage/infrastructure layer* they sit on top of. They are complementary, not competitive.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
flowchart TB
  classDef app fill:#cfe2f7,stroke:#2e5c8a,color:#000;
  classDef orch fill:#9dc3ec,stroke:#2e5c8a,color:#000;
  classDef store fill:#4a90e2,stroke:#2e5c8a,color:#000;

  A["AI Agent / LLM app"]:::app
  subgraph ORCH["Orchestration layer (extract · consolidate · retrieve)"]
    M0["mem0<br/>(LLM 2-phase pipeline)"]:::orch
    AM["agentmemory<br/>(CRUD over categories)"]:::orch
    NAT["ProximaDB native<br/>Agent Memory facade (optional)"]:::orch
  end
  subgraph STORE["Storage / infrastructure layer"]
    PDB["<b>ProximaDB</b><br/>vector · graph · document · relational<br/>quantization · federated query · audit"]:::store
    ALT["(commodity peers it replaces:<br/>Qdrant · Chroma · pgvector · Neo4j)"]:::store
  end

  A --> ORCH
  M0 -->|pgwire 5433 / VectorStoreBase| PDB
  AM -->|CLIENT_TYPE=POSTGRES| PDB
  NAT -->|in-process / AQL| PDB
----

== What mem0 and agentmemory are (cited)

[cols="1,2,2", options="header"]
|===
| | mem0 (`mem0ai`) | agentmemory (Moon, PyPI)

| Kind
| LLM-driven memory orchestration layer
| Thin CRUD + semantic-search function library

| Pipeline
| Two-phase: *extraction* (salient facts per message pair) + *update/consolidation* (retrieve top-_s_ similar → LLM picks `ADD`/`UPDATE`/`DELETE`/`NOOP`)
| None — manual `create_memory` / `search_memory`; similarity-threshold dedup only

| Retrieval
| Hybrid: semantic + BM25 + entity match
| Semantic embedding search

| Storage backend
| Pluggable, 20–24+ vector stores (Qdrant default) + optional graph store (Neo4j default, `Mem0g`)
| ChromaDB default, or Postgres/pgvector via `CLIENT_TYPE`

| Own storage engine?
| No (bundles a SQLite history/audit log only)
| No

| Source
| arXiv:2504.19413; `docs.mem0.ai`; `mem0/utils/factory.py`, `mem0/vector_stores/base.py`
| PyPI `agentmemory`; README function list
|===

== Capability-to-primitive mapping (reuse vs extend vs new)

[cols="2,3,1", options="header"]
|===
| Agent-memory primitive | ProximaDB surface (file) | Action

| Auditable retrieval / provenance
| AQL + RUBICON audit trail — `src/query/aql/{mod.rs,executor.rs}` (TD-050)
| Reuse

| Salience / re-ranking
| RERANK SRF — `crates/modalities/proximadb-rank-*`; `ProductionHybridBackend` (`src/network/rest/v1/rank_backend.rs`)
| Reuse

| Entity-linking / graph memory
| Graph RAG (RGL) — `src/graph/rag/{mod.rs,engine_impls.rs}`; ORION/PULSAR/QUASAR
| Reuse

| Hybrid retrieval (semantic + lexical)
| RRF + 11 strategies — `src/core/search/hybrid/fusion.rs`; BM25 leg `crates/modalities/proximadb-rank-features/src/bm25.rs`
| Reuse (inline BM25 = TD-092)

| Semantic similarity retrieval
| Vector search + AXIS/DiskANN — `src/index/`, `src/compute/distance/`
| Reuse

| Typed memory (short vs long, categories)
| `ProximaRecord.memory_type` (Memanto 13-cat, TD-055) + temporal `created_at_ns`/`updated_at_ns`
| Reuse

| Scope: user / tenant / actor
| `ProximaRecord.tenant_id` / `actor` / `permitted_principals` + RLS — `src/security/rls/`
| Reuse

| Scope: session
| `props["session_id"]` (first); first-class field deferred
| Extend

| Extraction (salient facts)
| `LLMIntegrationEngine` (`src/ai/llm_integration/engine.rs`) + AV-SQL agent pattern (`src/query/nl/`)
| New wrapper

| Consolidation (`ADD/UPDATE/DELETE/NOOP`)
| Graph RAG retrieve + fusion + LLM decision via `LLMIntegrationEngine`
| New wrapper

| Encoding gates (Novelty/Salience/Error)
| Foundation in Entanglement Index (TD-043); gate trait
| New (TD-053)

| Entropy-based lazy loading (L-RAG)
| SDP foundation (`src/storage/document/sdp.rs`); gated `Find` in AQL
| New (TD-054)

| External-framework integration
| pgvector-compatible pgwire — `src/network/postgres/{mod.rs,types.rs,translator.rs}`
| Reuse
|===

== Memory write path (extract → consolidate → store)

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
flowchart LR
  classDef n fill:#4a90e2,stroke:#2e5c8a,color:#000;
  MSG["agent turn /<br/>message pair"]:::n --> EX["Extraction<br/>(LLMIntegrationEngine)"]:::n
  EX --> CAND["salient candidate facts"]:::n
  CAND --> RET["retrieve top-s similar<br/>(vector + Graph RAG)"]:::n
  RET --> GATE["Consolidation decision<br/>ADD/UPDATE/DELETE/NOOP (LLM)"]:::n
  GATE --> REC["ProximaRecord<br/>(memory_type, tenant_id, actor, session_id)"]:::n
  REC --> WAL["canonical WAL → engine"]:::n
----

== Memory read path (retrieve → fuse → rerank → audit)

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
flowchart LR
  classDef n fill:#4a90e2,stroke:#2e5c8a,color:#000;
  Q["agent query /<br/>context need"]:::n --> AQL["AQL Find/From/Where<br/>(scope: tenant·session·memory_type)"]:::n
  AQL --> SEM["semantic search<br/>(AXIS/DiskANN)"]:::n
  AQL --> LEX["lexical / BM25"]:::n
  AQL --> ENT["entity expand<br/>(Graph RAG)"]:::n
  SEM --> FU["fusion (RRF)"]:::n
  LEX --> FU
  ENT --> FU
  FU --> RK["RERANK (salience)"]:::n
  RK --> AUD["RUBICON audit trail"]:::n
  AUD --> Q
----

== Phased plan

All phases below target the **v0.2** release in development (P1→P4 sequenced within v0.2, not deferred).
The Phase-6 active-memory gates (TD-052/053/054) are pre-existing roadmap items that P4 aligns to.

[cols="1,3,1", options="header"]
|===
| Phase | Work | Tracker

| P1 — Read surface
| Wire AQL memory queries with tenant/session/`memory_type` scope over existing fusion + RERANK; emit RUBICON audit per query. Validate mem0 PGVector provider against pgwire 5433.
| TD-100

| P2 — Write surface
| Extraction wrapper (`LLMIntegrationEngine` + AV-SQL pattern) → consolidation decision (retrieve + LLM `ADD/UPDATE/DELETE/NOOP`) → `ProximaRecord` write. `props["session_id"]` scope.
| TD-101

| P3 — Framework adapters
| mem0 `VectorStoreBase` provider + (optional) graph-store mapping to native graph; agentmemory pgvector path notes; conformance tests.
| TD-102

| P4 — Active memory (Phase 6 align)
| Encoding gates (Novelty/Salience/Error, TD-053), entropy-based lazy loading (TD-054), proactive retrieval (TD-052).
| TD-053/054/052
|===

== Open questions

* Does ProximaDB's pgwire implement enough of the pgvector surface (operators, index types, ANN query
  syntax) that mem0's PGVector provider works unmodified? (Validate in P1.)
* Can one ProximaDB collection back *both* mem0's vector store and mem0g's graph store, and do mem0g's
  graph operations map onto the native graph engine? (P3.)
* Is a first-class `ProximaRecord.session_id` field warranted, or is `props["session_id"]` sufficient for
  the query paths agents actually use? (Revisit after P2.)

== References

* ADR-022 (decision) — `adr/ADR-022-agent-memory-layer.adoc`
* RUBICON / AQL — `docs/_internal/roadmap/specifications/RUBICON_ALIGNMENT.adoc`, `src/query/aql/`
* Graph RAG (RGL) — TD-045; `src/graph/rag/`
* Memanto typed memory — TD-055; `ProximaRecord.memory_type`
* mem0 — arXiv:2504.19413; `github.com/mem0ai/mem0`
* agentmemory — PyPI `agentmemory`
