= ProximaDB Architecture Atlas
:toc:
:toclevels: 3

Status: active baseline +
Date: 2026-05-22 +
Evidence: Cargo workspace metadata, repository layout, `docs/12-design/README.adoc`, and
`.victor/project.db`

This atlas is the top-down map for ProximaDB as an OSS core database and as the data plane behind
a hosted SaaS product. It is intentionally navigation-first: it shows ownership boundaries,
authority boundaries, and where lower-level module maps live.

== Reading Order

1. This atlas for system shape and connectedness.
2. `MODULE_COMPONENT_LLD_2026_05_22.adoc` for per-module component breakdown.
3. `DOCUMENTATION_CONSOLIDATION_AND_ARCHIVE_PLAN_2026_05_22.adoc` for doc ownership and archive
   rules.
4. Existing active blueprints and ADRs in `docs/12-design/README.adoc`.

== Non-Negotiable Architecture Invariants

* Durable authority is `xCatalog + WAL/log/manifest + ProximaRecord + policy/RLS + version/time`.
* Protocols are adapters: REST, gRPC, pgwire, Arrow Flight, SDKs, and embedded bindings lower into
  canonical records, catalog metadata, and shared query/write contracts.
* Open formats are interoperability contracts. Iceberg, Delta, Hudi, Parquet, and object stores are
  publications, imports, federated reads, or explicit external-authoritative assets.
* Storage engines and modality-specific indexes are layouts, projections, and access methods. They
  do not own independent durable truth unless xCatalog declares that authority mode explicitly.
* The router is a control-plane planner boundary: route once per plan/fragment/split, emit typed
  route/explain metadata, and dispatch to leaf executors/readers.

== System Context

[source,mermaid]
----
flowchart TB
  Users["OSS users / SaaS tenants / operators"]
  SDK["SDKs and embedded bindings"]
  API["REST / gRPC / pgwire / Arrow Flight"]
  Auth["Auth, tenant context, RLS, budget guard"]
  Handlers["API handlers and protocol adapters"]
  Planner["Unified query/write planner and router"]
  Services["Service layer: DDL, DML, vector, document, graph, observability, queue"]
  Authority["xCatalog + WAL/log/manifest + policy + provenance"]
  Storage["PAX, LSM, columnar, ANN, graph, JSON, observability, queue disk/object tiers"]
  External["Open formats and external engines: Parquet, Iceberg, Delta, Hudi, DataFusion"]
  Ops["Telemetry, metering, audit, security, deployment"]

  Users --> SDK
  Users --> API
  SDK --> API
  API --> Auth
  Auth --> Handlers
  Handlers --> Planner
  Planner --> Services
  Services --> Authority
  Authority --> Storage
  Storage --> External
  Services --> Ops
  Auth --> Ops
----

== Workspace Layer Map

[source,text]
----
apps/ and clients/
    |
    v
crates/platform/        crates/platform/
  proximadb-api           proximadb-runtime
    |                       |
    +-----------+-----------+
                v
root crate `proximadb` (compatibility and integration shell while migration continues)
                |
                v
crates/query/  <---->  crates/modalities/
                |
                v
crates/control/ and crates/storage/
                |
                v
crates/horizontal/
                |
                v
crates/foundation/ and crates/contracts/
----

The long-term map remains the May 2026 workspace refactor plan:

`Foundation -> Contracts -> Modality Runtime -> Cross-Model Query Runtime -> Platform Runtime -> Apps/Bindings`

== Workspace Crate Inventory

[cols="1,2,4",options="header"]
|===
| Layer | Crates | Role

| Application
| `proximadb-server`, `proximadb-migrate`, `proximadb-bench`
| Operator and benchmark entry points. They should depend on platform/runtime/root integration
  crates, not on leaf storage/query internals.

| Binding
| `proximadb-sdk`
| Rust client SDK. Other SDKs live outside the Cargo workspace under `clients/`.

| Contract
| `proximadb-quantization-contracts`
| Shared contract surface for quantization behavior and compatibility gates.

| Control
| `proximadb-catalog`
| xCatalog schema, statistics, relational metadata, authority declarations, catalog cache, and
  planner-visible knobs.

| Foundation
| `proximadb-kernel`, `proximadb-data-model`, `proximadb-proto`, `proximadb-records`,
  `proximadb-filter-expression`, `proximadb-distance-types`, `proximadb-compression-types`,
  `proximadb-encoding-types`, `proximadb-quantization-types`, `proximadb-config`,
  `proximadb-hardware`, `proximadb-pipeline-operator`
| Stable lowest-level types, generated protobufs, canonical records, expression contracts, errors,
  config, hardware capability, and common operator abstractions. High centrality; changes need
  narrow tests and migration notes.

| Horizontal
| `proximadb-codec`, `proximadb-compression`, `proximadb-security`, `proximadb-telemetry`,
  `proximadb-runtime-common`, `proximadb-embedded-common`
| Cross-cutting services reused by multiple modalities and platform paths.

| Modality
| `proximadb-vector`, `proximadb-document`, `proximadb-graph`, `proximadb-observability`,
  `proximadb-embedding`, `proximadb-queue`
| Domain-specific runtime capabilities that must lower into canonical authority and shared query
  contracts.

| Platform
| `proximadb-api`, `proximadb-runtime`
| Public protocol surfaces and runtime composition. SaaS product control plane should integrate
  here rather than inside storage/query internals.

| Query
| `proximadb-query`, `proximadb-uql`, `proximadb-multimodel-query`,
  `proximadb-multimodel-plan`, `proximadb-query-capability`, `proximadb-query-filter`,
  `proximadb-query-clauses`, `proximadb-query-fusion`, `proximadb-vector-query`,
  `proximadb-document-query`, `proximadb-graph-query`, `proximadb-graph-arrow`,
  `proximadb-graph-subset`, `proximadb-observability-query`
| Cross-model algebra, query clauses, planning, fusion, modality query contracts, and query
  adapters.

| Storage
| `proximadb-block-format`, `proximadb-storage-common`
| Shared block/PAX/record storage format and common storage interfaces.
|===

== SaaS/Core Database Split

[source,mermaid]
----
flowchart LR
  subgraph SaaS["Hosted SaaS control plane"]
    Tenant["tenant/org/project"]
    Billing["metering and budget"]
    Policy["policy, auth, RLS"]
    Ops["audit, support, rollout flags"]
  end

  subgraph Core["OSS core data plane"]
    API["protocol adapters"]
    Catalog["xCatalog"]
    WAL["WAL/log/manifest"]
    Query["query and write planner"]
    Store["storage engines and projections"]
    Obs["observability events"]
  end

  Tenant --> Policy
  Billing --> Policy
  Policy --> API
  API --> Catalog
  API --> Query
  Query --> WAL
  WAL --> Store
  Store --> Obs
  Obs --> Billing
  Ops --> Catalog
----

SaaS-specific functions should remain policy/control-plane concerns around the OSS data plane:
tenant hierarchy, usage metering, billing, operator audit, feature rollout, support tooling, and
fleet orchestration. The OSS data plane should keep deterministic protocol behavior, storage
semantics, query semantics, and explainable routing.

== Runtime Data Flow

[source,text]
----
Client request
  -> protocol decode and tenant/auth context
  -> schema/type coercion at protocol boundary
  -> write/query intent
  -> xCatalog route/profile lookup
  -> planner/router emits executable plan + EXPLAIN metadata
  -> service layer executes against canonical record/storage contracts
  -> WAL/log/manifest updates authoritative state
  -> projections/indexes/cache/observability update as derived state
  -> protocol response encode
----

== Victor Graph Findings

Victor's Rust CCG construction previously resolved CALLS edges per-file only — `name_to_ids` was
built from a single file's nodes during `_build_calls_edges`, so any callee defined in another
file resolved to the empty set and no edge was emitted. With cross-file CALLS empty,
`ModuleAnalyzer._load_module_graph` (which only loads cross-file `graph_edge` rows) returned an
empty module set and `graph_module_metric` stayed at zero rows.

Resolution now proceeds in three coordinated layers:

1. *Project-wide leaf-name index* in `_resolve_cross_file_calls` runs after all nodes are
   persisted and emits CALLS edges via UPSERT. A fanout cap
   (`cross_file_call_max_fanout`, default 25) keeps common names (`new`, `default`, `from`) from
   inflating the graph.
2. *Impl-type index* keyed by `(impl_type, method_name)`, populated from the existing
   ``parent_id`` linkage (methods inside ``impl T`` carry ``parent_id`` pointing at the impl
   node, whose name is ``T``). When a call record carries ``receiver_type``, the typed pass
   binds against this index and bypasses the fanout cap — typed dispatch is unambiguous.
3. *Receiver-type inference in the Rust plugin* covers four expression shapes:
+
[loweralpha]
.. ``self`` against the enclosing ``impl T`` (including ``Self::method()`` path calls).
.. ``identifier`` against the current function's scope (parameter annotations and
   ``let x: T = ...`` / ``let x = T::new()`` bindings).
.. ``field_expression`` chains like ``self.a.b.method()`` walk through a per-file
   ``struct_item`` field map.
.. ``call_expression`` chains like ``a.foo().bar()`` walk through a per-file impl-method
   return-type map.
+
Cross-file struct fields and return types are out of scope for this round; method chains and
field receivers that cross file boundaries fall back to name-only.

Victor database scale after the 2026-05-22 resolver, audit, and quality fixes
(10/10 random spot-check samples manually verified against source):

[cols="1,1",options="header"]
|===
| Metric | Value
| `graph_node` rows | 864,367
| `graph_edge` rows | 2,645,534
| `graph_module_metric` rows | 1,716
| `DDG_DEF_USE` edges | ~1,571,000
| `CFG_SUCCESSOR` edges | ~523,000
| `CALLS` edges | 124,018 (58,962 same-file, 65,056 cross-file)
| `CDG` edges | ~101,000
| `CONTAINS` edges | ~22,400
|===

Evolution across the fix:

[cols="2,1,1,1",options="header"]
|===
| Snapshot | CALLS total | CALLS cross-file | module metrics
| Pre-fix (per-file resolution) | 103,255 | **0** | **0**
| Name-only project-wide resolution | 480,632 | 428,791 | 1,675
| Receiver-typed dispatch (basic) | 467,982 | 415,788 | 1,684
| Plus struct-field, method-chain, ``Self::`` | 1,262,182 | 1,203,169 | 1,706
| Strict semantics + macro skip + method-drop | 1,034,444 | 979,795 | 1,707
| Plus impl_item-as-implementing-type | 239,097 | 183,520 | 1,713
| Plus same-file preference for plain calls | 116,910 | 57,636 | 1,714
| Plus trait-impl exclusion from name-only | **124,018** | **65,056** | 1,716
|===

The dramatic drop in the final snapshot is **noise removal, not coverage loss**. Audit
of the round-4 graph (sampling top callees) surfaced three distinct quality issues that
were each fixed under TDD:

1. *Strict receiver-typed semantics* (~30k spurious edges removed). When the plugin
   inferred a receiver type like ``Vec`` (stdlib) and the impl-type lookup missed, the
   resolver was falling back to name-only and fanning out to all 12 user-defined ``iter``
   methods. Now a typed miss means external/stdlib -- drop the call.
2. *Macro invocations excluded* (~5k removed). ``println!``, ``format!``, ``vec!``,
   ``write!`` are not function calls; they were being emitted as CALLS edges with the
   macro name as callee and binding to user-defined functions of the same leaf name.
3. *``impl_item`` name uses implementing type, not trait* (~795k removed). The
   codingagent extractor was naming ``impl Default for Foo`` as ``Default`` (first
   identifier scanned) instead of ``Foo`` (the ``type`` field). Every
   ``Default::default()`` call across the codebase had ``receiver_type='Default'``, and
   the impl-method index keyed by ``(Default, default)`` returned all 800,078 user
   ``fn default`` methods. Fixed in ``_extract_name_from_node`` by trying the ``type``
   field after ``name`` but before the first-identifier fallback.

Method-syntax dispatch policy was also tightened: ``obj.method()`` calls with no
inferable receiver type are now dropped instead of falling back to name-only matching
(plain function calls ``func()`` and path calls ``Mod::func()`` still use name-only with
the fanout cap, because those are globally unambiguous in well-named codebases). This
removed ~140k speculative bindings on common leaf names like ``collect``, ``clone``,
``len`` that were fanning out to every user impl with the same leaf.

A subsequent spot-check audit (5 random src->src CALLS samples) caught a fourth
quality issue: plain function calls to local test helpers like ``inputs(&t)`` were
fanning out across all 11 same-leaf-name definitions in the codebase. The fix --
*same-file preference for plain function calls* -- restricts the name-only candidate
set to caller-file matches when any exist (Rust function name resolution is
module-scoped; cross-file binding only makes sense when there's no local match).
Removed 122k cross-file fanout edges while increasing same-file edges by 3.7k
(the legitimate local bindings that were getting buried in the fanout).

A second-round spot-check on the post-same-file graph (10 random samples) caught
a fifth quality issue: plain ``drop(deps)`` calls (the stdlib ``std::mem::drop``)
were binding to all 16 user ``impl Drop for T { fn drop }`` impls. Same pattern
applied for any trait method whose name collides with stdlib functions or that
the user might invoke as a plain function. The fix -- *exclude trait-impl methods
from the name-only candidate list* -- detects trait impls via the impl node's
signature (``impl <Trait> for <Type>`` always contains `` for `` between
identifiers; inherent impls don't). Typed dispatch via the impl-method index
still has full access to trait methods, so ``x.clone()`` and ``x.fmt(f)``
continue to bind correctly via receiver-type lookup. After the fix, a fresh
10-sample audit hit 10/10 verified correct (vs 8/10 in the prior round).

Cross-file ratio settles at 49% (57,636 / 116,910) -- intentionally lower than the
inflated 95% in earlier snapshots. The earlier ratios were dominated by cross-file
fanout to wrong files; the new ratio reflects real cross-module dispatch (typed methods
binding to ``impl T::method`` in another file, and plain calls where no local match
exists). Treat the new total as a tight lower bound on the true call graph; the
speculative edges that previously inflated it are gone, and the gaps that remain
(method chains crossing file boundaries, generics, trait objects) fall back gracefully
rather than producing wrong edges.

Top node kinds from Victor:

[cols="1,1",options="header"]
|===
| Node kind | Count
| `statement` | 804,928
| `function` | 35,071
| `struct` | 5,101
| `impl` | 4,071
| `class` | 1,238
| `trait` | 231
|===

Top path prefixes by Victor node count:

[cols="1,1",options="header"]
|===
| Prefix | Nodes
| `src` | 435,258
| `clients` | 132,243
| `crates` | 112,328
| `tests` | 80,994
| `benches` | 51,582
| `demo` | 14,490
| `examples` | 8,034
| `scripts` | 6,475
| `apps` | 5,588
| `ui` | 2,078
|===

Top node-dense files from Victor:

[cols="3,1",options="header"]
|===
| File | Nodes
| `crates/foundation/proximadb-proto/src/proto/proximadb.v1.rs` | 13,234
| `src/network/postgres/protocol.rs` | 5,514
| `src/query/federated/execution/mod.rs` | 5,453
| `src/services/dml/mod.rs` | 5,305
| `clients/python/tests/embedded_consolidated_benchmark.py` | 5,028
| `clients/python/src/proximadb_sdk/chunking_strategies/code.py` | 5,022
| `clients/python/src/proximadb_sdk/unified_client.py` | 4,948
| `apps/proximadb-bench/src/main.rs` | 4,850
| `src/query/federated/optimizer/mod.rs` | 4,468
| `src/embedded/mod.rs` | 4,267
|===

Top workspace crates by Cargo dependency PageRank:

[cols="2,1,1,1",options="header"]
|===
| Crate | PageRank | In | Out
| `proximadb-kernel` | 0.130951 | 18 | 0
| `proximadb-data-model` | 0.073740 | 15 | 0
| `proximadb-proto` | 0.053290 | 10 | 1
| `proximadb-query-filter` | 0.049835 | 12 | 0
| `proximadb` | 0.043111 | 4 | 42
| `proximadb-records` | 0.039766 | 11 | 2
| `proximadb-filter-expression` | 0.030123 | 4 | 0
| `proximadb-graph-query` | 0.025395 | 7 | 3
| `proximadb-compression-types` | 0.023671 | 3 | 0
| `proximadb-query` | 0.023101 | 3 | 13
|===

Interpretation:

* Foundation crates are correctly central, but they are blast-radius sensitive.
* The root `proximadb` crate still has high efferent coupling (`out=42`). That is acceptable as a
  migration shell but should continue shrinking as platform/runtime/query/modality crates mature.
* `proximadb-query`, `proximadb-uql`, and `proximadb-multimodel-query` are expected integration
  hubs. Their public contracts should stay small and test-heavy.
* Generated proto files dominate raw node counts. Treat them as generated artifacts, not
  refactor targets.
* `src/query/federated/optimizer`, `src/api_handlers/request_handlers.rs`,
  `src/network/postgres/protocol.rs`, `src/services/dml/mod.rs`, and `proximadb-catalog` are real
  human-authored hotspots and should be first-class consolidation targets.

== Victor Reproducibility Queries

Use read-only SQLite queries against `.victor/project.db` after each expansion/consolidation cycle.
The schema columns used here are `graph_node.file`, `graph_node.type`, and `graph_edge.type`.

[source,sql]
----
select count(*) from graph_module_metric;
select count(*) from graph_node;
select count(*) from graph_edge;

select type, count(*)
from graph_edge
group by type
order by count(*) desc;

select type, count(*)
from graph_node
group by type
order by count(*) desc
limit 20;

select substr(file, 1, instr(file || '/', '/') - 1) as prefix, count(*)
from graph_node
group by prefix
order by count(*) desc
limit 20;

select file, count(*) as nodes
from graph_node
group by file
order by nodes desc
limit 20;

select
  sum(case when src_node.file = dst_node.file then 1 else 0 end) as same_file_calls,
  sum(case when src_node.file <> dst_node.file then 1 else 0 end) as cross_file_calls
from graph_edge edge
join graph_node src_node on edge.src = src_node.node_id
join graph_node dst_node on edge.dst = dst_node.node_id
where edge.type = 'CALLS';
----

Current interpretation:

* `graph_module_metric` contains 1,714 rows. Module-level PageRank, instability, cohesion,
  hotspot, and TDD priority metrics are first-class signals; use them as gates alongside Cargo
  dependency centrality. Hotspot rankings remain stable across the audit-fix rebuilds (top
  files: `src/network/rest/v1/handlers.rs`, `src/network/multi_server.rs`,
  `src/network/arrow_ipc/service.rs`, `src/services/operations/vectors/legacy.rs`,
  `src/services/dml/mod.rs`) -- the noise removal didn't change which modules are central,
  only the absolute Ce values.
* `CALLS` resolves cross-file at 95% (1.2M of 1.26M total). The resolver applies receiver-typed
  dispatch whenever the Rust plugin can infer the receiver from any of: ``self`` inside
  ``impl T``, ``Self::method()`` path calls, scope identifiers (parameter type, ``let`` binding),
  ``struct_item`` field chains (``self.field.method()`` and longer paths), and impl-method
  return-type chains (``a.foo().bar()``).
* Falls back to name-only resolution (with fanout cap 25) when the receiver-type chain hits
  something unsupported: a method call on a stdlib type (``Vec``, ``HashMap``) whose impl lives
  outside the codebase, a generic parameter with no concrete bound resolved at parse time, a
  trait object (``Box<dyn Trait>``), or a cross-file struct field whose type is declared in
  another module. None of these are wrong — they're just imprecise; the fanout cap keeps the
  noise bounded.
* Treat call-edge counts as a tight upper bound: typed dispatch is exact; the name-only tail
  is fanout-bounded; the recovered edges in the third snapshot are real calls that the
  earlier resolvers were dropping silently.
* Statement-level nodes still dominate the graph. Use file/module aggregation first, then
  inspect human-authored hotspot files manually.

== Consolidation / Expansion Zigzag

Use this cycle for feature growth:

1. Expand behind existing contracts, feature flags, and xCatalog-declared authority.
2. Refresh Victor and Cargo coupling metrics.
3. Identify hotspots, duplicated authority, and root-crate fanout.
4. Consolidate one boundary at a time into the existing crate/module owner.
5. Add or update the module LLD section before enabling by default.
6. Run focused tests plus workspace layering validation.
7. Repeat.

This keeps the system from drifting into parallel protocol, storage, or modality paths while still
allowing rapid feature expansion.

== Use Case Map

Actor-to-entry-point map. Ports verified against `config/config.toml` (5678 unified REST,
5679 gRPC, 5680 Arrow Flight) and `CLAUDE.md` (5433 pgwire). Entry surfaces verified against
`src/network/` (rest, grpc, postgres, arrow_ipc) and `src/embedded/` (PyO3/JNI/NAPI/C-FFI).

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
flowchart LR
  subgraph Actors["Actors"]
    WebApp["Web app (JS/TS)"]
    SqlClient["SQL / psql client"]
    AppDev["App developer (Python/Rust/Go/Java SDK)"]
    DataPipeline["Data pipeline (bulk insert)"]
    AIAgent["AI agent (embedded)"]
    OpMonitor["Operator (monitoring)"]
    ExtConsumer["External format consumer (Parquet/Delta/Iceberg)"]
  end

  subgraph Entry["Entry surfaces"]
    REST["REST API :5678 (HTTP/1.1)"]
    GRPC["gRPC API :5679 (HTTP/2)"]
    ARROW["Arrow Flight :5680"]
    PGWIRE["PostgreSQL wire :5433 (pgvector ops)"]
    EMBEDDED["Embedded (in-process)"]
  end

  WebApp -->|vector search, collection mgmt| REST
  SqlClient -->|SQL, pgvector distance ops| PGWIRE
  AppDev -->|create collection, insert/upsert, search| GRPC
  AppDev -->|JSON REST calls| REST
  DataPipeline -->|bulk insert via Arrow| ARROW
  DataPipeline -->|high-throughput streaming| GRPC
  AIAgent -->|in-process multi-model search, Graph RAG| EMBEDDED
  OpMonitor -->|scrape /metrics/prometheus, /health| REST
  ExtConsumer -->|federated / open-format read| REST
  ExtConsumer -->|Arrow export of multi-modal records| ARROW
----

== Deployment Topology

Verified against `deploy/docker/docker-compose.yml`, `deploy/kubernetes/`, `Dockerfile`,
`config/config.toml` (`[distributed]` disabled by default), and `src/network/multi_server.rs`.
Distributed mode is gated by the `cluster` feature + `distributed.enabled = true`.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TD
  subgraph ClientTier["Client tier"]
    Web["Browser / dashboard"]
    CLI["CLI / psql"]
    SDK["SDK client"]
    ETL["ETL (Spark/Kafka)"]
  end

  subgraph SingleNode["Single-node (default: dev/test)"]
    PDB["ProximaDB server\n:5678 REST · :5679 gRPC\n:5680 Arrow Flight · :5433 pgwire"]
    STORAGE["Local storage /tmp/proximadb\n(data/ metadata/ manifest/)"]
    HEALTH["GET /health"]
  end

  subgraph K8s["Kubernetes (staging/prod)"]
    SVC["Service / load balancer"]
    POD["Pod: proximadb\n(ports as above + :9090 metrics)"]
    PVC["PersistentVolume /data"]
    CM["ConfigMap config.toml"]
  end

  subgraph Cluster["Distributed cluster (feature=cluster)"]
    N1["Node 1 — Raft leader"]
    N2["Node 2 — follower"]
    N3["Node 3 — follower"]
    RAFT["Raft consensus over :5679"]
    SHARED["Shared object store (S3/Azure/GCS)"]
  end

  subgraph Obs["Observability (optional sidecars)"]
    PROM["Prometheus :9091"]
    GRAF["Grafana :3000"]
  end

  subgraph Cloud["Cloud storage (feature=cloud-full)"]
    S3["S3"]
    AZ["Azure Blob"]
    GCS["GCS"]
  end

  Web --> SingleNode
  CLI --> SingleNode
  SDK --> SingleNode
  ETL --> SingleNode
  SingleNode --> STORAGE
  STORAGE -.-> Cloud
  SingleNode --> HEALTH
  SingleNode -.-> Obs

  SDK --> SVC
  SVC --> POD
  POD --> PVC
  POD --> CM
  POD -.-> Obs

  N1 -.-> RAFT
  N2 -.-> RAFT
  N3 -.-> RAFT
  N1 --> SHARED
  N2 --> SHARED
  N3 --> SHARED
  SHARED --> S3
  SHARED --> AZ
  SHARED --> GCS

  PROM --> GRAF
----

NOTE: These two system-level views complement the recursive per-`src/`-module decomposition in
`MODULE_COMPONENT_LLD_2026_05_22.adoc` ("Recursive src Module Decomposition"). Read this atlas for
shape and entry points; read the LLD for component-internal structure and critical-path flows.
