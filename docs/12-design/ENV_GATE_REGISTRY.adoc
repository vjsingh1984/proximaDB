= ENV GATE REGISTRY — the authoritative catalog of `PROXIMADB_*` environment gates
:status: Authoritative (CI-enforced by `scripts/check_env_gates.py`)
:updated: 2026-07-25

== Why this exists

221 unique `PROXIMADB_*` variables exist in the tree. Unregistered
proliferation is a cognitive-load tax and produces near-miss names — the
canonical incident: `PROXIMADB_IVF2` (WRITE: train A0 at compaction) vs
`PROXIMADB_IVF2_PROBE` (READ: use A0 at search), two default-OFF halves of
one feature whose confusion cost a full measurement cycle on 2026-07-25.
This file is the single source of truth: **a `PROXIMADB_*` name that is not
listed here fails CI** (`check_env_gates.py`, wired into
`make work-commit-check`).

== Naming rules (new gates MUST follow; existing names are frozen aliases)

1. `PROXIMADB_<AREA>_<SEMANTIC>` — area = subsystem (PAX, CACHE, SEARCH,
   WAL, QUEUE, TRACE, TEST…). Semantic says what it DOES, not the feature
   codename.
2. **Paired write/read gates carry `WRITE_`/`READ_` (or `EMIT_`/`USE_`)
   explicitly** — never two near-identical names. (Had the rule existed:
   `PROXIMADB_PAX_WRITE_A0_TRAIN` / `PROXIMADB_PAX_READ_COARSE_PROBE`.)
3. Booleans: presence-style gates accept `1|true|on|yes`; kill-switches are
   named for what they DISABLE (`…_DISABLE`) so unset = shipped default.
4. State the compile-time default and the flip-precondition in this table at
   introduction time; `TODO` is only legal for pre-registry legacy rows.
5. Renaming a shipped gate = alias forever (operational API, mixed-safety):
   support both names, document the alias here, never repurpose a name.

== Lifecycle tiers

*eval* (diagnostics/benchmarks; may change anytime) · *opt-in* (default-OFF
feature, flip-precondition documented) · *kill-switch* (default-ON, env is
the emergency off) · *config-mirror* (env overrides a config field) ·
*test-only* (never read in production paths).

== Catalog (195 live gates)

[cols="3,4,2,2,3", options="header"]
|===
| Variable | Semantic | Default | Tier; Owner | Declared in

| `PROXIMADB_ADVISOR_OBS_DISK_SIDECAR` | Path to append advisor search observations as JSONL for offline analysis. | OFF (unset = no-op) | opt-in; — | src/services/advisor_observations.rs
| `PROXIMADB_AZURE_EMULATOR` | Point the Azure blob backend at the Azurite emulator. | OFF (false) | test-only; — | src/storage/persistence/filesystem/mod.rs
| `PROXIMADB_AZURE_TEST` | Gate that enables the `#[ignore]`d Azure dual-store integration test. | test skipped unless set | test-only; — | src/index/axis/indexes/dual_store_ivf.rs
| `PROXIMADB_CACHE_PREFILL` | restart warming replay (0=off) | ON | TD-CACHE-1 | `src/database.rs (+1)`
| `PROXIMADB_CACHE_LOCAL_DISK_MAX_GB` | Shared persistent PAX warm-tier budget in GiB; only active when the local-disk path is configured. Zero disables the tier. | 100 | config-mirror; ADR-082/TD-CACHE-9 | src/storage/engines/sst/core.rs
| `PROXIMADB_CACHE_LOCAL_DISK_PATH` | Local filesystem root for the optional persistent PAX warm tier. HDD is valid; SSD/NVMe is recommended for latency. URLs are invalid. | unset = L2 disabled | config-mirror; ADR-082/TD-CACHE-9 | src/storage/engines/sst/core.rs
| `PROXIMADB_CACHE_ON_WRITE` | Post-publication PAX cache population policy: `none`, `invariant`, or `all`. | invariant | config-mirror; ADR-082/TD-CACHE-9 | src/core/config.rs; src/storage/engines/sst/flush/mod.rs; src/storage/engines/sst/compaction.rs
| `PROXIMADB_CACHE_TIERS_PATH` | operator tier policy JSON (floors/weights) | unset = uniform | TD-CACHE-3 S1 | `src/network/shared_services.rs (+3)`
| `PROXIMADB_CATALOG_DRPATH` | Route the system-catalog WAL under the DR-path operator control-plane prefix. | OFF (falls back to `{base}/system-catalog.wal`) | opt-in; — (mirrors PROXIMADB_WAREHOUSE_DRPATH) | src/network/shared_services.rs
| `PROXIMADB_CATALOG_FOLLOWER_POLL_SECS` | Enable + interval (secs) for catalog follower snapshot polling (cross-pod coherence). | OFF (unset/0 = disabled) | opt-in; Phase 6b | src/network/shared_services.rs
| `PROXIMADB_CATALOG_OBJECT_ID_PATHS` | Dual-write object_id-keyed catalog metadata layout alongside the legacy name path. | OFF | opt-in; TD-181 P3 | crates/control/proximadb-catalog/src/native.rs
| `PROXIMADB_CATALOG_SNAPSHOT_KEEP` | Number of newest catalog snapshots to keep (committer clamps ≥2). | 3 | opt-in; TD-MANIFEST-1 | src/services/catalog_snapshot_store.rs
| `PROXIMADB_CATALOG_SNAPSHOT_MIN_AGE_SECS` | Min age (secs) a superseded snapshot must reach before pruning. | 900 | opt-in; TD-MANIFEST-1 | src/services/catalog_snapshot_store.rs
| `PROXIMADB_CATALOG_SNAPSHOT_RETENTION` | Enable/disable catalog snapshot manifest pruning. | ON (true); set `=0` to disable | kill-switch; TD-MANIFEST-1 | src/services/catalog_snapshot_store.rs
| `PROXIMADB_CATALOG_SNAPSHOT_THRESHOLD` | WAL-append count that triggers a new catalog snapshot. | 1000 (DEFAULT_SNAPSHOT_THRESHOLD; 0 ignored) | opt-in; — | src/services/system_catalog.rs
| `PROXIMADB_CATALOG_TENANT_NAMESPACES` | Store collection catalog assets under a tenant-prefixed namespace. | OFF | opt-in; TD-CAT-2b S3a | src/services/collection/manager.rs
| `PROXIMADB_CHURN_WORKING_SET_MB` | Soft ceiling (MB) for a Churn collection's in-memory working set; breach logs a dedup warn. | None = no ceiling (unset) | opt-in; TD-WLP-8 | crates/storage/proximadb-storage-common/src/storage_profile.rs
| `PROXIMADB_COMPUTE_REGION` | Control-plane-stamped compute-region label for consumption/usage metrics. | "" (unwrap_or_default; warns on cloud) | config-mirror; — | src/metrics/consumption_metrics.rs
| `PROXIMADB_CORPUS_VERSION_PATH` | Filesystem path for plan-cache corpus_version durability store. | unset = in-memory only (restart resets versions to 1) | opt-in; — | src/database.rs
| `PROXIMADB_COUNT_FS_IO` | counting-FS physical GET/byte meters | OFF (eval) | ADR-027/030 | `crates/storage/proximadb-storage-filesystem-types/src/counting.rs (+1)`
| `PROXIMADB_CROSS_MODEL_TX_ENABLED` | Enable the cross-model (graph+vector) transaction coordinator. | OFF (false) | opt-in; — | src/services/transaction/cross_model_coordinator.rs
| `PROXIMADB_CRYPTO_MASTER_KEY` | Names the env var holding the WAL/file master encryption key | config-mirror default of `master_key_env_var`; key itself unset ⇒ no key material | config-mirror; — | `src/storage/persistence/write_ahead_log/config.rs`
| `PROXIMADB_DATA_DIR` | Base data directory for the native engine / pgwire protocol handler. | "./data" | config-mirror; — | src/network/postgres/protocol.rs
| `PROXIMADB_DF_BLOOM_SEMIJOIN` | Enable bloom-filter semijoin pushdown (fact-scan pruning vs dimension in-lists). | ON (true); `=0` opts out | kill-switch; TD-OLAP-3 | src/datafusion/engine_adapters/object_store_parquet_reader.rs
| `PROXIMADB_DF_BLOOM_SEMIJOIN_MIN_KEYS` | Min in-list cardinality above which the bloom semijoin engages. | 256 | opt-in; TD-OLAP-3 | src/datafusion/engine_adapters/object_store_parquet_reader.rs
| `PROXIMADB_DF_PAX_RANGED` | PAX-native reader fetches surviving blocks via ranged reads instead of whole-file. | OFF | opt-in; TD-DOC-PUSHDOWN-1 / ADR-052 | src/datafusion/engine_adapters/pax_adapter.rs
| `PROXIMADB_DF_PAX_READER` | Opt in to the PAX-native OLAP scan reader. | OFF | opt-in; TD-OLAP-1 | src/datafusion/engine_adapters/pax_adapter.rs
| `PROXIMADB_DF_RUNTIME_FILTER_PRUNE` | Absorb the hash-join runtime/dynamic filter into the fact scan for row-group pruning. | ON (true); `=0` opts out | kill-switch; ADR-056 AQE-S11 | src/datafusion/proxima_scan_exec.rs
| `PROXIMADB_DF_RUNTIME_FILTER_WAIT_MS` | Probe-scan wait budget (ms) for the build-side runtime-filter rendezvous. | 1500 | opt-in; ADR-056 AQE-S11 | src/datafusion/proxima_scan_exec.rs
| `PROXIMADB_DF_STATS_VALUE_BOUNDS` | Project typed min/max value bounds into DF ColumnStatistics (interval selectivity + MIN/MAX elision). | OFF | opt-in; — (default-OFF planner mandate) | src/datafusion/schema_inference.rs
| `PROXIMADB_DISABLE_GRAPH_FUSION_CALIBRATION` | Kill-switch: skip PIT calibration in graph fusion search, return raw seed+expand union. | calibration ON (unset) | kill-switch; TD-131 | src/services/fusion_service.rs
| `PROXIMADB_DISABLE_PAX_RANGE_COALESCE` | Kill-switch: fall back to per-block reads instead of coalesced PAX range reads. | coalesce ON (unset) | kill-switch; TD-151 | src/storage/engines/core/formats/proximablocks/sst_io_layer.rs
| `PROXIMADB_DISABLE_SST_SCAN_COALESCE` | Kill-switch: disable coalesced SST scan block reads. | coalesce ON (unset) | kill-switch; — | src/storage/engines/sst/readers/sst_query_engine.rs
| `PROXIMADB_DISABLE_SYSTEM_CATALOG` | Kill-switch: revert default catalog from WAL-backed SystemCatalog to file-per-object NativeCatalog. | SystemCatalog ON (unset) | kill-switch; — | src/network/shared_services.rs
| `PROXIMADB_DISABLE_WAL` | Kill-switch (TEST MODE): skip WAL writes in the orion engine for benchmarking. | WAL ON (unset; only `=1` disables) | kill-switch (test-only); — | crates/modalities/proximadb-orion-engine/src/lib.rs
| `PROXIMADB_DISK_CACHE_MAX_GB` | Byte cap (GB) for the local disk cache (whole-file read-through). | 10 | config-mirror; ADR-073 | crates/storage/proximadb-storage-common/src/unified_cache_config.rs
| `PROXIMADB_DISK_CACHE_PATH` | Local disk-cache directory (point at instance NVMe). Overrides the TOML/default. | `/tmp/proximadb_cache` | config-mirror; ADR-073 | crates/storage/proximadb-storage-common/src/unified_cache_config.rs
| `PROXIMADB_DISK_CLASS` | hdd => CLOUD coalescing profile for file:// paths | unset (LOCAL profile) | ADR-073 | `crates/storage/proximadb-storage-common/src/iops_budget.rs`
| `PROXIMADB_DOC_CANONICAL_VECTOR` | Route documents through the canonical vector path; doubles as per-collection allowlist + global kill-switch. | ON (unset=ON); `0/off/none`=OFF all; CSV list=only those | kill-switch; — (cutover) | src/storage/document/service.rs
| `PROXIMADB_DRIFT_INTERVAL_SECS` | Discovery drift-watcher poll interval (secs). | 60 (DEFAULT_DRIFT_INTERVAL) | opt-in; — | src/services/discovery/drift.rs
| `PROXIMADB_DRIFT_THRESHOLD_WRITES` | Writes-since-recluster count that signals drift. | 10000 (DEFAULT_DRIFT_THRESHOLD_WRITES) | opt-in; — | src/services/discovery/drift.rs
| `PROXIMADB_DUCKDB_ROUTE` | Force DuckDB-local as PRIMARY engine for join/agg parquet SELECTs (DataFusion stays floor). | OFF | opt-in; ADR-054 | src/query/execution/duckdb_engine.rs
| `PROXIMADB_EMBED_ASYNC_QUEUE_CAP` | Async embedding pool queue capacity. | 4096 | config-mirror; — | crates/modalities/proximadb-embedding/src/scheduler.rs
| `PROXIMADB_EMBED_ASYNC_WORKERS` | Async embedding pool worker-thread count. | 8 | config-mirror; — | crates/modalities/proximadb-embedding/src/scheduler.rs
| `PROXIMADB_EMBED_DRAINER_PARTITIONS` | Partition range this pod's embedding drainer owns (e.g. "0..16", "8,3..6"). | 0..16 (all) | config-mirror; — | src/database.rs
| `PROXIMADB_EMBED_INTRA_OP_THREADS` | ONNX intra-op thread count for BGE inference. | auto (ORT/suggested when unset) | opt-in; — | crates/modalities/proximadb-embedding/src/models/bge.rs
| `PROXIMADB_EMBED_MODEL_DIR` | Root directory for BGE ONNX model + tokenizer files. | "/var/lib/proximadb/models" | config-mirror; — | crates/modalities/proximadb-embedding/src/models/bge.rs
| `PROXIMADB_EMBED_PRECISION_SCHEMA_V2` | Allow WAL writer to emit precision-aware schema-v2 embedding records (fp16 etc.). | OFF (schema-v1 only) | config-mirror; — | crates/foundation/proximadb-config/src/lib.rs
| `PROXIMADB_EMBED_PROVIDER` | Select the ONNX execution provider (cpu/coreml/cuda/tensorrt/…). | cpu (None/unknown ⇒ Cpu) | config-mirror; — | crates/modalities/proximadb-embedding/src/models/bge.rs
| `PROXIMADB_EMBED_SESSIONS` | ONNX session-pool size for embedding. | 1 (clamped 1..32) | config-mirror; — | crates/modalities/proximadb-embedding/src/models/bge.rs
| `PROXIMADB_EMBED_SYNC_QUEUE_CAP` | Sync embedding pool queue capacity. | 8 | config-mirror; — | crates/modalities/proximadb-embedding/src/scheduler.rs
| `PROXIMADB_EMBED_SYNC_WORKERS` | Sync embedding pool worker-thread count. | 4 | config-mirror; — | crates/modalities/proximadb-embedding/src/scheduler.rs
| `PROXIMADB_EVENTLOG_ENTITY_KEYED` | Store audit events entity-keyed (`{base}/{entity}/event_{seq}.bin`) so restart recovery rebuilds the index from a LIST (no per-event GET) — O(N)→O(entities) round-trips (TD-EVENTLOG-1 §3). Mixed-read-safe: legacy `partition_*` events still recovered via GET. | OFF (legacy sequence-keyed layout) | opt-in; TD-EVENTLOG-1 | src/storage/engines/eventlog/mod.rs
| `PROXIMADB_EMBED_VARIANT` | BGE model variant (small/large/m3). | small | config-mirror; — | apps/proximadb-server/src/main.rs
| `PROXIMADB_EMIT_USAGE_EVENTS` | Enable emission of usage/billing events (default-inert egress hook). | OFF (unless truthy) | opt-in; — | src/metrics/usage_event.rs
| `PROXIMADB_EVICTION_WATCH` | Spot IMDS watch (azure|aws|gcp) | OFF | TD-CACHE-1 S4 | `src/database.rs (+1)`
| `PROXIMADB_EXTERNAL_TABLE_ROOTS` | Comma-separated allowlist of URI-prefix roots permitted for CREATE EXTERNAL TABLE (fail-closed). | unset = no roots allowed (feature rejected) | opt-in; — | src/services/ddl/mod.rs
| `PROXIMADB_FILTER_FAIL_LOUD` | Metadata filter eval fails loud on error (vs silent drop) in the vectors legacy path. | ON (true unless 0/false/off/no) | kill-switch; — | src/services/operations/vectors/legacy.rs
| `PROXIMADB_FILTER_STRICT` | Metadata filtering is fail-loud (strict) in the progressive search pipeline. | ON (true unless 0/false/off/no) | kill-switch; TD-FILT-1 | src/core/search/progressive_search_pipeline.rs
| `PROXIMADB_FS_READ_RANGES_PARALLEL` | Bound on concurrent ranged reads in `FileSystem::read_ranges`. | `0`/1 → sequential (the default) | opt-in; — | crates/storage/proximadb-storage-filesystem-types/src/lib.rs:551
| `PROXIMADB_FUSION_POOL_CAP` | Per-source candidate cap (top-N by raw score) bounding cross-modal fusion fan-out RAM. | unset = unbounded | opt-in; TD-XMODAL-9 | src/services/fusion_service.rs
| `PROXIMADB_GCS_ANONYMOUS` | Use anonymous GCS auth (fake-gcs-server). | false | test-only; — | src/storage/persistence/filesystem/mod.rs
| `PROXIMADB_GCS_ENDPOINT` | Override the GCS endpoint URL (fake-gcs / custom). | None (real GCS) | config-mirror; — | src/storage/persistence/filesystem/mod.rs
| `PROXIMADB_GCS_TEST` | Gate that enables the `#[ignore]`d GCS dual-store integration test. | test skipped unless set | test-only; — | src/index/axis/indexes/dual_store_ivf.rs
| `PROXIMADB_GCS_TEST_BUCKET` | Bucket name for GCS integration tests. | "proximadb-test" | test-only; — | src/storage/persistence/filesystem/gcs_store.rs
| `PROXIMADB_GCS_TEST_ENDPOINT` | fake-gcs-server endpoint for GCS integration tests; also gates them. | test skipped if unset | test-only; TD-OBJSTORE-5 | src/storage/persistence/filesystem/gcs_store.rs
| `PROXIMADB_GRAPH_CANONICAL_RECOVERY` | Enable the buffered graph segment store (required as its crash-recovery backstop; paired with PROXIMADB_GRAPH_SEGMENT_STORE). | OFF (only `=1` enables) | opt-in; — | src/storage/persistence/write_ahead_log/wal_operations.rs
| `PROXIMADB_GRAPH_CANONICAL_REPLAY_SCOPE` | Emits `CanonicalEmission` WAL markers and truncates graph WAL replay at the recovered canonical checkpoint LSN | OFF (full replay, no markers) | opt-in; TD-066(c) | `src/storage/persistence/write_ahead_log/wal_operations.rs`
| `PROXIMADB_GRAPH_COLD_PAYLOADS` | Enables the cold graph-payload tier (byte-budgeted cache over canonical record store on RAM miss) | OFF (all-RAM path) | opt-in; TD-168 | `src/graph/service.rs`
| `PROXIMADB_GRAPH_SEGMENT_STORE` | Backs the cold-payload tier with the batched `ColdGraphSegmentStore` instead of per-record store | OFF (per-record store); refused unless canonical-recovery backstop is on | opt-in; #52 | `src/graph/service.rs`
| `PROXIMADB_GRAPH_SEQUENTIAL_VALIDATION` | Test-mode toggle to run graph edge validation sequentially. | OFF (parallel) | test-only; — | src/graph/service.rs:2719
| `PROXIMADB_INDEX_CATALOG_PATHS` | Opts fleet into DrPathBuilder `indexes/<projection>/` catalog-resolved index layout | OFF (present-or-absent, `var_os`) | opt-in; — | `src/network/shared_services.rs`
| `PROXIMADB_INDEX_PERSIST_URL` | Object-store URI (s3/adls/gs) for AXIS index persistence + byte-range cold-load | falls back to `<data_dir>/axis_indexes` local dir | config-mirror; TD-087 | `src/network/shared_services.rs`
| `PROXIMADB_INTERNAL_MUX_PORT` | internal gRPC/Flight upstream port override | derived | TD-NET-1 | `crates/foundation/proximadb-config/src/lib.rs (+1)`
| `PROXIMADB_IO_TRACE_SINK` | Master enable for the durable io_trace ETL sink observer | falls back to TOML `io_trace_sink.enabled` (effectively OFF) | opt-in/config-mirror; TD-TRACE-2/ADR-066 | `src/core/config.rs`
| `PROXIMADB_IO_TRACE_SINK_ACCESS_TIER` | Object-store at-rest access tier for sealed trace segments | falls back to TOML, then `Cold`; invalid value errors | config-mirror; ADR-066 | `src/core/config.rs`
| `PROXIMADB_IO_TRACE_SINK_COMPRESSION` | Trace segment compression codec | falls back to TOML, then `zstd` (only zstd supported) | config-mirror; ADR-066 | `src/core/config.rs`
| `PROXIMADB_IO_TRACE_SINK_FLUSH_INTERVAL_S` | Trace sink flush cadence (seconds) | falls back to TOML, then 60 (min 1) | config-mirror; ADR-066 | `src/core/config.rs`
| `PROXIMADB_IO_TRACE_SINK_FORMAT` | Trace segment serialization format | falls back to TOML, then `jsonl` (only jsonl supported) | config-mirror; ADR-066 | `src/core/config.rs`
| `PROXIMADB_IO_TRACE_SINK_LOCAL_DIR` | Local spool directory for trace segments | falls back to TOML, then `./log/io_trace` | config-mirror; ADR-066 | `src/core/config.rs`
| `PROXIMADB_IO_TRACE_SINK_OBJECT_STORE_URI` | Optional object-store dispatch target for sealed trace segments (S2) | falls back to TOML, then None (local-only S1) | config-mirror; ADR-066 D4 | `src/core/config.rs`
| `PROXIMADB_IO_TRACE_SINK_PENDING_MAX_BYTES` | In-flight pending byte cap before backpressure | falls back to TOML, then 1 GiB | config-mirror; ADR-066 | `src/core/config.rs`
| `PROXIMADB_IO_TRACE_SINK_SEGMENT_BYTES` | Sealed-segment size threshold | falls back to TOML, then 4 MiB | config-mirror; ADR-066 | `src/core/config.rs`
| `PROXIMADB_IO_TRACE_SINK_SPOOL_MAX_BYTES` | On-disk spool byte cap | falls back to TOML, then 256 MiB | config-mirror; ADR-066 | `src/core/config.rs`
| `PROXIMADB_IO_TRACE_SINK_WAREHOUSE_COMPACTION_INTERVAL_S` | Background warehouse-compaction interval; 0 disables | falls back to TOML, then None/off | config-mirror; ADR-066 S4b | `src/core/config.rs`
| `PROXIMADB_IVF_BINARY_TIER` | Gates the 1-bit binary IVF tier (`add_vector` populates binary_codes) | OFF | opt-in; TD-087/F2 | `src/index/axis/indexes/dual_store_ivf.rs`
| `PROXIMADB_IVF_K` | Overrides fine IVF cell count (≈ block count) at compaction | computed `(n·dim)/iop_target` clamped 2..4096 | eval; TD-COMPACT-1/ADR-065 | `src/storage/engines/sst/block_cluster.rs`
| `PROXIMADB_IVF_NCOMP` | Overrides PCA projection dimensionality for IVF | computed `max(1.5·log2 dim, 1.5·log2 k)` clamped 1..dim | eval; TD-WLP-4b | `src/storage/engines/sst/block_cluster.rs`
| `PROXIMADB_IVF_NCOMP_A` | Coefficient `a` (intrinsic-dim term) in the NCOMP formula | 1.5 | eval; TD-WLP-4b | `src/storage/engines/sst/block_cluster.rs`
| `PROXIMADB_IVF_NCOMP_B` | Coefficient `b` (partition-granularity term) in the NCOMP formula | 1.5 | eval; TD-WLP-4b | `src/storage/engines/sst/block_cluster.rs`
| `PROXIMADB_IVF_TRAIN_SAMPLE` | Overrides IVF/PCA k-means train-sample size | computed `default_train_sample(k)` = 50k scaled by k, capped 200k | eval; TD-COMPACT-1 | `src/storage/engines/sst/block_cluster.rs`
| `PROXIMADB_L0_COMPACTION_ENABLED` | arm L0 compaction (count arm + TD-COMPACT-5 training arm) | OFF (or collection tag compaction:on) | TD-WLP-7 | `crates/storage/proximadb-storage-common/src/compaction_override.rs (+1)`
| `PROXIMADB_L0_SLOWDOWN_TRIGGER` | L0 admission soft watermark — when the live L0 file count reaches this the flush yields ~1ms so async compaction (TD-COMPACT-6) can drain L0. Overrides `[storage.sst_config] l0_slowdown_trigger`. | 6 (0 disables) | opt-in tuning; ADR-076 D2 | `src/storage/engines/sst/flush/mod.rs`
| `PROXIMADB_L0_STOP_TRIGGER` | L0 admission hard ceiling — when the live L0 file count reaches this the flush defers (returns Err; WAL retained) until compaction drains L0. Overrides `[storage.sst_config] l0_stop_trigger`; must be ≤ `max_files_per_level`. | 10 (= `max_files_per_level`; 0 disables) | opt-in tuning; ADR-076 D2 | `src/storage/engines/sst/flush/mod.rs`
| `PROXIMADB_LEASE_MANIFEST_KEEP` | Newest manifests to keep per lease log during prune | 32 (committer clamps ≥2) | opt-in tuning; — | `src/network/shared_services.rs`
| `PROXIMADB_LEASE_MANIFEST_MIN_AGE_SECS` | Grace age a superseded manifest must reach before prune | 900 | opt-in tuning; — | `src/network/shared_services.rs`
| `PROXIMADB_LEASE_MANIFEST_PRUNE_INTERVAL_SECS` | Prune loop sweep cadence (seconds) | 300 | opt-in tuning; — | `src/network/shared_services.rs`
| `PROXIMADB_LEASE_MANIFEST_RETENTION` | Master switch for the lease-manifest prune loop | ON (kill-switch; set `0` to disable) | kill-switch; — | `src/network/shared_services.rs`
| `PROXIMADB_MATERIALIZE_OBJECT_ID_PATHS` | Keys materialized table object path by stable object_id instead of table name | OFF | opt-in; ADR-031 | `src/services/dml/mod.rs`
| `PROXIMADB_MATERIALIZE_ROW_GROUP_ROWS` | Iceberg warehouse writer max row-group row count | 65,536 | opt-in tuning; — | `crates/storage/proximadb-iceberg-engine/src/lib.rs`
| `PROXIMADB_MEMORY_POOL_SIZE_MB` | Global shared memory-pool size (MB) | smart default from system memory | opt-in tuning; — | `src/storage/engines/core/ops/performance_optimization.rs`
| `PROXIMADB_METERING_FLUSH_INTERVAL_SECS` | Durable `_metering` sink flush cadence (seconds) | 3600 (1 h) | opt-in tuning; TD-161 | `src/database.rs`
| `PROXIMADB_METERING_KSU_GAUGE_INTERVAL_SECS` | Prometheus resident-storage KSU gauge poll interval (seconds) | 300 (5 min) | opt-in tuning; ADR-030 | `src/database.rs`
| `PROXIMADB_NATIVE_ALLOW_GROUPED_AGG` | Opts grouped-aggregate plans into native routing (else routed to DataFusion) | OFF (any value set ⇒ allow) | opt-in; TD-OLAP-11 | `src/network/postgres/relational_pipeline.rs`
| `PROXIMADB_NATIVE_JOIN` | Enables native hash-join operator path | OFF | opt-in; ADR-054/TD-OLAP-11 | `src/query/execution/native_engine.rs`
| `PROXIMADB_NATIVE_JOIN_SHADOW` | Runs native join as a shadow probe alongside DataFusion (measurement only) | OFF | eval; TD-OLAP-11 | `src/query/execution/native_shadow.rs`
| `PROXIMADB_NATIVE_MORSEL` | Enables TD-OLAP-12 morsel-parallel scheduler for native pipelines | OFF | opt-in; TD-OLAP-12 | `src/query/execution/native_engine.rs`
| `PROXIMADB_NATIVE_ROUTE` | Routes native-winning op classes to native engine as PRIMARY over parquet | OFF | opt-in; TD-OLAP-4 | `src/query/execution/native_engine.rs`
| `PROXIMADB_NATIVE_SHADOW` | Runs native vectorized engine as a shadow alongside DataFusion (doubles work) | OFF | eval; TD-OLAP-4 | `src/query/execution/native_engine.rs`
| `PROXIMADB_NATIVE_VECTORIZED` | Enables native-over-parquet vectorized engine (FilterProject/HashAgg) | OFF | opt-in; ADR-054 | `src/query/execution/native_engine.rs`
| `PROXIMADB_NETWORK_SCOPE_MAP` | JSON file path for CIDR→client-locality scope map (result-egress) | default-empty (clients classify Unknown) | config-mirror; Co-design KOU | `src/metrics/consumption_metrics.rs`
| `PROXIMADB_NOVA_MAX_ROW_GROUP_SIZE` | Forces smaller NOVA parquet row-group size (exercise row-group pruning) | 50,000 | test-only/opt-in; TD-040 | `src/storage/engines/nova/operations/flush.rs`
| `PROXIMADB_OBJECT_STORE_LOCALITY` | Overrides derived object-store read-egress locality | derived locality (invalid ⇒ WARN + derived) | opt-in override; Co-design C3/KOU | `src/metrics/consumption_metrics.rs`
| `PROXIMADB_WARM_TEST_BASE` | Cloud base URL for the warm-manifest round-trip test (falls back to `PROXIMADB_OBJECT_STORE_URL`) | none (test `#[ignore]` without it) | test-only; TD-CACHE-7 | `src/storage/engines/sst/warming.rs`
| `PROXIMADB_OBJECT_STORE_URL` | Object-store base URL (s3/adls/gs) for restart-recovery integration tests | none (tests `#[ignore]` without it) | test-only; TD-OBJSTORE-5 | `apps/proximadb-server/tests/object_store_restart_recovery.rs`
| `PROXIMADB_OID_RESOLVER_CACHE_MB` | Override the shared OID→position resolver-cache budget in MiB; zero disables the in-memory cache. | `[storage.sst_config].oid_resolver_cache_mb` (default 64) | config-mirror; TD-DELVEC-1 WI-3c | `src/storage/engines/sst/core.rs`
| `PROXIMADB_OLAP_DELTA_MERGE` | Engine-wide read-merge (delta-over-parquet) toggle; tables can re-opt-in | ON (falsy token `0/false/off/no` disables) | kill-switch; ADR-025 | `src/network/postgres/relational_pipeline.rs`
| `PROXIMADB_OLAP_TABLE_CACHE` | Enables the table-OPEN cache | OFF (default-OFF-until-baked) | opt-in; TD-OLAP | `src/datafusion/engine_adapters/table_open_cache.rs`
| `PROXIMADB_OTLP_ENDPOINT` | OTLP gRPC endpoint that arms the external metering push | unset ⇒ metering push inert (no-op) | opt-in; TD-161/ADR-027 | `crates/modalities/proximadb-observability-engine/src/metering_otlp.rs`
| `PROXIMADB_PARQUET_FOOTER_HINT_KB` | Parquet footer prefetch size (KB) | 512 KiB | opt-in tuning; TD-OLAP-4 | `src/datafusion/engine_adapters/object_store_parquet_reader.rs`
| `PROXIMADB_PAX_ENCODE_THREADS` | flush encode-pool width (bounded scoped rayon pool; 1 = sequential) | cores/2, min 2 (query headroom) | opt-in tuning; TD-FLUSH-5 | `crates/storage/proximadb-block-format/src/coalesced_rabitq.rs`
| `PROXIMADB_PAX_BLOCK_CLUSTER` | Sign-Gray block reclustering at flush/compaction | ON (kill-switch; `0/false/off/no` ⇒ insertion order) | kill-switch; TD-WLP | `src/storage/engines/sst/block_cluster.rs`
| `PROXIMADB_PAX_BLOCK_SIZE` | Overrides PAX target block geometry (vectors/block) | None ⇒ writer default (`SST_DEFAULT_VECTORS_PER_BLOCK`) | opt-in tuning; TD-156/ADR-026 | `src/storage/engines/sst/flush/mod.rs`
| `PROXIMADB_PAX_COALESCED_RABITQ` | coalesced Region-A segment layout (writer+reader) | ON (env = kill-switch) | ADR-065 | `src/storage/engines/sst/segment_format.rs`
| `PROXIMADB_PAX_COALESCE_GAP` | coalesce max_gap override (bytes) | backend profile | ADR-065/073 | `src/storage/engines/sst/segment_format.rs`
| `PROXIMADB_PAX_COALESCE_RANGE` | coalesce max_range override (bytes) | backend profile | ADR-065/073 | `src/storage/engines/sst/segment_format.rs`
| `PROXIMADB_PAX_DROP_TENANT_COL` | Drops the tenant column from the on-disk PAX block | OFF (`var_os`) | opt-in; — | `crates/storage/proximadb-block-format/src/writer.rs`
| `PROXIMADB_PAX_F32_TIER` | Emits exact-f32 stripe (Phase D) for exact rerank / include_vectors | OFF (per-collection tag overrides) | opt-in; P3 Phase D | `src/storage/engines/sst/flush/mod.rs`
| `PROXIMADB_PAX_FLUSH_CLUSTER` | Upgrades L0 flush ordering to full PCA+IVF recluster | OFF (`=ivf/pca_ivf/1/...` enables) | eval; TD-WLP-4/WLP-9 | `src/storage/engines/sst/block_cluster.rs`
| `PROXIMADB_PAX_LOSSLESS_CLUSTERED` | Enables lossless clustered PAX codec path | OFF | eval; — | `src/storage/engines/sst/segment_format.rs`
| `PROXIMADB_PAX_LOSSLESS_SCALAR` | Enables lossless scalar PAX codec path | OFF | eval; — | `src/storage/engines/sst/segment_format.rs`
| `PROXIMADB_PAX_PREFIX_PREFETCH_BYTES` | Bounded PAX prefix read covering the header and, when it fits, A0/RaBitQ control plus early probed-cell bytes. Clamped to 8 MiB. | 1 MiB (kill-switch: `0`) | opt-in tuning; TD-SEARCH-3 | `src/storage/engines/sst/segment_format.rs`
| `PROXIMADB_PAX_QUANT_DISABLE` | Global kill-switch: forces recall-exact RawF32 quant (disables RaBitQ/SQ8 coding) for every collection. | OFF (kill-switch; default RaBitQ) | kill-switch; ADR-049 M1-3 | src/storage/engines/sst/flush/mod.rs:1080
| `PROXIMADB_PAX_RABITQ_POOL` | Forces fixed RaBitQ survivor-pool M for the recall sweep | unset ⇒ computed `max(k·mult, ceil(n·rate), min)` | eval; — | `src/storage/engines/sst/segment_format.rs`
| `PROXIMADB_PAX_RABITQ_POOL_MIN` | Floor for the RaBitQ survivor-pool size M in PAX top-k search. | `1000` | eval; — | src/storage/engines/sst/segment_format.rs:861
| `PROXIMADB_PAX_RABITQ_POOL_MULT` | Per-k multiplier for the RaBitQ survivor pool (M ≥ k·mult). | `100` | eval; — | src/storage/engines/sst/segment_format.rs:857
| `PROXIMADB_PAX_RABITQ_POOL_RATE` | Corpus-fraction term for the pool (M ≥ ceil(n·rate)). | `0.01` (1%) | eval; — | src/storage/engines/sst/segment_format.rs:865
| `PROXIMADB_PAX_READ_COARSE_NPROBE` | READ knob: coarse-probe cell count (nprobe). Falls back to TOML `[storage.sst_config.coarse_probe] nprobe_multiplier/min/max` → geometric `ceil(sqrt(ncells) × multiplier)` | geometric `ceil(sqrt(ncells) × 1.0)` (was `k_c/4`; ~56× fewer ranks at 1M) | config-mirror; TD-RDSTRAT-8 PR-B; retired alias `PROXIMADB_IVF2_NPROBE` | `src/storage/engines/sst/segment_format.rs`
| `PROXIMADB_PAX_READ_COARSE_PROBE` | READ kill-switch: coarse probe via A0 on trained v3 segments (`0/off/false/no` disables). Falls back to TOML `[storage.sst_config.coarse_probe] enable_read_probe` | **ON** (flipped 2026-07-26; sweep: recall 0.986-0.987 ≥ ratchet at default nprobe, 81 GETs/92ms vs 108/144ms unprobed) | config-mirror; TD-RDSTRAT-8 PR-B | `src/storage/engines/sst/segment_format.rs`
| `PROXIMADB_PAX_RERANK_QUANT` | Tier-2 rerank quant strategy when tier-1 is RaBitQ (fp16/f32/sq8). | `Sq8` (after per-collection tag) | opt-in; TD-123 | src/storage/engines/sst/flush/mod.rs:1239
| `PROXIMADB_PAX_SPLIT_PROBE_META_CACHE` | Compatibility kill-switch for caching immutable header/A0/RaBitQ-parameter/footer bytes on the coarse-probe path; explicit `0/off/false/no` disables. | ON (unset) | kill-switch; TD-CACHE-9 | src/storage/engines/sst/segment_format.rs
| `PROXIMADB_PAX_VECTOR_COALESCE_GAP` | Max byte gap to bridge when coalescing adjacent A-region (vector-stripe) probe-cell ranged GETs | 0 (no gap coalescing) | eval; — | `src/storage/engines/sst/segment_format.rs`
| `PROXIMADB_PAX_VECTOR_COALESCE_RANGE` | Max coalesced byte range for A-region probe-cell GETs | 0 (off) | eval; — | `src/storage/engines/sst/segment_format.rs`
| `PROXIMADB_PAX_VECTOR_QUANT` | flush vector quant codec (rabitq|sq8|...) | rabitq | Phase F | `crates/foundation/proximadb-proto/src/proto/proximadb.v1.rs (+1)`
| `PROXIMADB_PAX_WRITE_A0_TRAIN` | WRITE gate: compaction trains + emits the v3/A0 coarse directory. Falls back to TOML `[storage.sst_config.coarse_probe] enable_write_train` | **ON** (flipped 2026-07-26; COGS-arc unblock — IVF cuts GETs/q ~4×, recall/GET gates passed, ledger `nprobe_sweep_trained_1m`) | config-mirror; TD-RDSTRAT-8 PR-A | `src/storage/engines/sst/block_cluster.rs`
| `PROXIMADB_PGWIRE_RATE_LIMIT_RPM` | Per-IP request/min rate cap on the pgwire query path. | OFF (unset/0 → no limit) | opt-in; — | src/network/multi_server.rs:897
| `PROXIMADB_PGWIRE_RELATIONAL_PIPELINE` | Toggles the new pgwire relational executor pipeline | ON (only `=0` forces legacy path) | kill-switch; ADR-018 Ph2 | `src/network/postgres/relational_pipeline.rs`
| `PROXIMADB_POD_ID` | Supplies this pod's identity (k8s metadata.name) for cache/primary-pod affinity. | falls back to explicit override, then `"self"` | config-mirror; — | src/cluster/primary_pod_registry.rs:628
| `PROXIMADB_PRIMARY_POD_PERSISTENCE_MODE` | Boot-time primary-pod persistence mode (catalog_primary vs sidecar_only). | falls back to `sidecar_only` (unknown → warn + sidecar_only) | config-mirror; Slice 5d.2 | src/cluster/primary_pod_registry.rs:652
| `PROXIMADB_PROTO_REGEN` | Build-time flag forcing proto regeneration in `build.rs`. | unset → skip regen | opt-in (build); — | crates/foundation/proximadb-proto/build.rs:15
| `PROXIMADB_QUERY_RESULT_CACHE` | pgwire OLAP result cache | OFF | #708/ADR-051 | `src/network/postgres/protocol.rs (+2)`
| `PROXIMADB_QUEUE_OBJECT_ARCHIVE` | Archive URL for the async-ingest queue's object store. | falls back to toml `object_archive`, else `None` | config-mirror; — | src/core/config.rs:347
| `PROXIMADB_QUEUE_ROOT` | Filesystem/object root that enables async ingest; unset keeps it off. | falls back to toml `root`, else `None` (async ingest off) | config-mirror; — | src/core/config.rs:341
| `PROXIMADB_QUEUE_SYNC_MODE` | Durability/sync mode for the ingest queue. | falls back to toml, else `"strict"` | config-mirror; — | src/core/config.rs:350
| `PROXIMADB_RELATIONAL_BOOTSTRAP_DEMO_TABLE` | Opt-in to bootstrap a demo table in the relational pipeline. | OFF | opt-in; — | src/network/postgres/relational_pipeline.rs:76
| `PROXIMADB_REST_V1_COMPAT` | Re-enables the deprecated `/api/v1/*` REST surface (else 410 Gone). | OFF (v1 disabled) — note: const doc at v1_sunset.rs:40 wrongly says "Default ON"; parser returns false on unset | kill-switch; ADR-049 / TD-121 / TD-V1SUNSET-1 | src/network/middleware/v1_sunset.rs:52
| `PROXIMADB_REST_V1_SUNSET` | RFC 8594 `Sunset` HTTP-date advertised when v1 is disabled. | unset → no Sunset header | opt-in; TD-V1SUNSET-1 | src/network/middleware/v1_sunset.rs:48
| `PROXIMADB_ROUTER_QUANT_HELIX` | Under `index_policy.mode=auto` + quantization, route the collection to the HELIX engine instead of SST cold path. | OFF | opt-in; ADR-028 | src/services/collection/engine_selector.rs
| `PROXIMADB_ROUTE_COST_OVERRIDE` | Enables the live route-cost override (global bool or comma-list of shape-class prefixes). | OFF (observe-only; unparseable → OFF) | eval; ADR-058 / TD-EXEC-2 | src/query/route_cost_model.rs:70
| `PROXIMADB_ROUTE_RTT_BUDGET` | Hard round-trip (range_gets) budget for route costing. | `None` (disabled) | opt-in; TD-170 | src/query/route_cost_model.rs:854
| `PROXIMADB_S3_BUCKET` | S3 bucket that enables the cloud index-storage tier. | `None` (cloud tier off) | config-mirror; — | src/index/axis/storage/universal_index_storage.rs:137
| `PROXIMADB_S3_ENDPOINT` | Custom S3 endpoint URL (e.g. MinIO). | `None` (AWS default endpoint) | config-mirror; — | src/storage/persistence/filesystem/mod.rs:486
| `PROXIMADB_S3_FORCE_PATH_STYLE` | Forces path-style S3 addressing (MinIO/compat). | `false` | config-mirror; — | src/storage/persistence/filesystem/mod.rs:487
| `PROXIMADB_S3_REGION` | S3 region override for the backend. | falls back to `AWS_REGION`/`AWS_DEFAULT_REGION`, else `us-east-1` | config-mirror; — | src/storage/persistence/filesystem/mod.rs:482
| `PROXIMADB_S3_TEST_ACCESS` | MinIO access key for the `#[ignore]` S3 integration test. | `"minioadmin"` | test-only; — | src/index/axis/indexes/dual_store_ivf.rs:4259
| `PROXIMADB_S3_TEST_BUCKET` | Bucket name for the S3 integration test. | `"proximadb-test"` | test-only; — | src/index/axis/indexes/dual_store_ivf.rs:4257
| `PROXIMADB_S3_TEST_ENDPOINT` | MinIO endpoint that gates (enables) the S3 integration test. | unset → test skips | test-only; — | src/index/axis/indexes/dual_store_ivf.rs:4252
| `PROXIMADB_S3_TEST_SECRET` | MinIO secret key for the S3 integration test. | `"minioadmin"` | test-only; — | src/index/axis/indexes/dual_store_ivf.rs:4261
| `PROXIMADB_SCAN_INDEX_DISABLE` | Kill-switch bypassing the cached scan index (memory-pressure escape hatch). | OFF (index on) | kill-switch; TD-099(3d) | src/storage/memtable/implementations/global_partitioned.rs:127
| `PROXIMADB_SEARCH_MAX_TOP_K` | Server cap on request `top_k` | 10,000 | opt-in tuning; — | `src/network/rest/v2/records.rs`
| `PROXIMADB_SEARCH_MORSEL_DEGREE` | intra-query rank parallelism (0=adaptive,1=off,n) | 0 = adaptive (all cores only when scan is alone) | TD-SEARCH-2 S2 | `src/storage/engines/sst/search/mod.rs`
| `PROXIMADB_SEARCH_PARALLEL_FILES` | inter-file search workers (0=50% cores) | 0 | TD-SEARCH-2 S1 | `src/core/config.rs (+1)`
| `PROXIMADB_SECONDARY_INDEX_DISABLE` | Kill-switch forcing OLTP secondary-index off (scan fallback). | OFF (index on) | kill-switch; TD-127 | src/services/record_store.rs:943
| `PROXIMADB_SEGMENT_INVARIANTS_CACHE_MB` | segment invariants cache pool (0=off) | 256 (config-first) | TD-CACHE-4 | `src/core/config.rs (+1)`
| `PROXIMADB_SERVERLESS_CERTIFIED` | Enables certified serverless recovery mode (fails hard on uninitialized manifest). | OFF | kill-switch; — | src/storage/persistence/write_ahead_log/recovery_token.rs:130
| `PROXIMADB_SIMD_CONFIG` | Path to the SIMD config TOML (only controls simd_config, NOT global config). | "config/simd.toml" | config-mirror; — | src/storage/engines/core/ops/simd_config.rs
| `PROXIMADB_SKIP_AXIS_INDEXING` | Global override to skip AXIS indexing at flush. | OFF (indexing on) | kill-switch; — | src/storage/engines/sst/flush/mod.rs:86
| `PROXIMADB_STORAGE_PROFILE` | Workload storage profile (append/bulk/churn) driving compaction. | falls back to collection tag, else `AppendBulk` | config-mirror; — | crates/storage/proximadb-storage-common/src/storage_profile.rs:24
| `PROXIMADB_STORAGE_READ_STRATEGY_CHOOSER` | Engages the cost-driven per-block read-strategy chooser (observe mode). | OFF (always-coalesce baseline) | eval; TD-RDSTRAT-1 / ADR-052 | src/storage/engines/core/coalesce_strategy.rs:86
| `PROXIMADB_STORE_REGION` | Storage region stamped into the placement context (billing/locality). | empty → falls back to S3/AWS region envs, else derives `Local` | config-mirror; — | src/metrics/consumption_metrics.rs:406
| `PROXIMADB_SURVIVOR_CACHE_BUDGET_MB` | survivor range cache pool (0=off) | 1024 (config-first) | TD-CACHE-4/ADR-065 | `src/core/config.rs (+2)`
| `PROXIMADB_SURVIVOR_OID_CEILING_FRAC` | CacheKind::Other admission ceiling | 0.3 | TD-CACHE-2 S2c | `src/storage/engines/sst/survivor_range_cache.rs`
| `PROXIMADB_SURVIVOR_PIN_RESERVE_FRAC` | true-pin reserve fraction (needs tiers policy) | 0 = OFF | TD-CACHE-3 S2 | `src/storage/engines/sst/survivor_range_cache.rs`
| `PROXIMADB_SYSCAT_WARM_DIR` | On-disk warm-tier dir inserted between hot and canonical system-catalog cache. | falls back to builder field, else `None` (hot→canonical) | opt-in; TD-SC-2b | src/services/collection/manager.rs:228
| `PROXIMADB_TENANT_HEADER_TRUST` | Tenant header-trust policy override (open/authenticated-only/gateway-only). | env>config>preset; unset → `Open`; unparseable → tightens to AuthenticatedOnly | config-mirror; — | crates/foundation/proximadb-tenant/src/identity_trust.rs:83
| `PROXIMADB_TEST_BERT_ONNX_PATH` | Path to a cross-encoder ONNX model for a manual ORT integration test. | unset → test skips | test-only; — | crates/modalities/proximadb-rank-onnx/src/ort_tokenized_scorer_session.rs:306
| `PROXIMADB_TEST_LOG_LEVEL` | Sample env used to test TOML `${...}` placeholder expansion (log_level). | unset → placeholder test n/a | test-only; — | src/core/config_loader_tests.rs:958
| `PROXIMADB_TEST_NODE_ID` | Sample env used to test TOML `${...}` placeholder expansion (node_id). | unset → placeholder test n/a | test-only; — | src/core/config_loader_tests.rs:957
| `PROXIMADB_TEST_ONNX_PATH` | Path to an ONNX model for a manual ORT scorer integration test. | unset → test skips | test-only; — | crates/modalities/proximadb-rank-onnx/src/ort_scorer_session.rs:245
| `PROXIMADB_TEST_REAL_CORPUS` | Path to a real-vector corpus file for an `#[ignore]` IVF recall test. | unset → test skips | test-only; — | src/index/axis/indexes/dual_store_ivf.rs:3520
| `PROXIMADB_TEST_RECOVERY_CRASH_POINT` | Test seam injecting a crash at `after_materialize`/`after_mark_flushed` in WAL recovery. | unset → normal recovery path | test-only; TD-OBJSTORE-4 S3 | src/storage/persistence/write_ahead_log/recovery_manager.rs:651
| `PROXIMADB_TEST_SECRET` | Throwaway credential for CDC auth-wiring tests (avoids hard-coded secret). | falls back to `"test-secret-{pid}"` | test-only; — | src/cdc/sinks/webhook.rs:534
| `PROXIMADB_TIER_CONFIG_PATH` | Path to the runtime tier-config overlay JSON. | falls back to `/config/tier-config.json` | config-mirror; Phase B-5 | crates/foundation/proximadb-tenant/src/tenant_tier_types.rs:101
| `PROXIMADB_TOKENIZER_PATH` | Override path for the BGE tokenizer.json. | falls back to `/var/lib/proximadb/models/tokenizer.json` (then whitespace fallback) | config-mirror; — | crates/modalities/proximadb-embedding/src/tokenizer.rs:23
| `PROXIMADB_TRACE_COMPACTION_MEM` | Emits per-stage RSS/mem trace lines during compaction. | OFF | eval; — | src/storage/engines/sst/compaction.rs:92
| `PROXIMADB_TRACE_GETS` | per-GET size trace on the PAX read path | OFF (eval) | ADR-065 | `src/storage/engines/sst/segment_format.rs (+1)`
| `PROXIMADB_TRACE_IVF_FLUSH` | Emits IVF flush tracing. | OFF | eval; — | src/storage/engines/sst/block_cluster.rs:383
| `PROXIMADB_TRACE_PAX_STAGES` | Emits PAX write-stage boundary tracing. | OFF | eval; — | src/storage/engines/sst/segment_format.rs:1654
| `PROXIMADB_TRACE_PAX_WRITE` | Arms the PAX-writer phase timer/bucket histogram (read at writer construction). | OFF | eval; TD-COMPACT-1 | src/storage/engines/sst/segment_format.rs:459
| `PROXIMADB_TRAINING_COMPACTION_MIN_MB` | min untrained-L0 size for the training arm | 32 | TD-COMPACT-5 | `src/storage/engines/sst/flush/mod.rs`
| `PROXIMADB_TYPED_PATHS` | Enables typed (base62-id) storage/WAL path resolution. | OFF (legacy string paths; read once/process) | opt-in; ADR-031 | src/storage/trait_components/path_resolver.rs:464
| `PROXIMADB_VECTOR_BOUNDS_PRUNE_DISABLE` | Kill-switch forcing full-read (disables vector-bounds row-group pruning). | OFF (prune on) | kill-switch; TD-040 | src/storage/engines/nova/operations/search.rs:25
| `PROXIMADB_VECTOR_RABITQ_ENABLE` | Opt-in: encode vector stripes with RaBitQ binary quantization instead of SQ8. | OFF (SQ8) | opt-in; — | crates/storage/proximadb-block-format/src/writer.rs:1828
| `PROXIMADB_VECTOR_SQ8_DISABLE` | Kill-switch: vector stripes fall back to raw f32 instead of SQ8. | OFF (SQ8 on) | kill-switch; — | crates/storage/proximadb-block-format/src/writer.rs:1821
| `PROXIMADB_WAL_LOCAL_DIR` | Local WAL root (e.g. `file:///waldisk`); routes per-collection WAL to `{dir}/{id}/wal/` on local disk, decoupled from the object-store data assignment (ADR-069 S1). Governs write + Bincode replay. Env override of TOML `[storage.wal_config].wal_local_dir`. | OFF (WAL co-located with data) | opt-in; ADR-069 S1 | src/storage/persistence/write_ahead_log/mod.rs
| `PROXIMADB_WAL_OBJECT_ID_KEY` | Master gate keying WAL `collection_id` by stable object_id (dual-read safe). | OFF (name-keyed) | opt-in; ADR-031 O2 | src/services/record_store.rs:705
| `PROXIMADB_WRITE_FENCING` | storage-write fence enforcement | OFF (fail-open) | A6 | `src/database.rs (+6)`
|===

== Misregistered / dead entries (26 — the TD-ENVGATE-1 S3 prune worklist)

Rows below have NO functional Rust env reader (compile-time consts,
prefix-harvest stubs, template-emitted names, or vars whose own tests assert
they are unwired). Listed so the guard passes while the strings remain in
source; each S3 prune PR deletes the source reference AND its row here.

[cols="3,7", options="header"]
|===
| Name | Detail

| `PROXIMADB_API_GRPC_PORT` | DEAD (no rust reader): only set in a test that asserts it is IGNORED; no production override wiring exists. | intended config-mirror of `api.grpc_port` (5679) but not wired | config-mirror; — | src/core/config_loader_tests.rs
| `PROXIMADB_DESCRIPTION` | DEAD (no runtime env reader): a Rust `const` bound to `env!("CARGO_PKG_DESCRIPTION")` at compile time, not a runtime env var. | n/a (build constant) | config-mirror; — | src/version.rs
| `PROXIMADB_ENABLE_AI` | DEAD (no rust reader): emitted as a literal into the generated docker-compose YAML in the provisioner; no code reads it back. | n/a (deployment template) | config-mirror; — | src/deployment/automation/provisioner.rs
| `PROXIMADB_ENABLE_MONITORING` | DEAD (no rust reader): emitted into generated docker-compose YAML; no code reads it back. | n/a (deployment template) | config-mirror; — | src/deployment/automation/provisioner.rs
| `PROXIMADB_FS_COPY_KEY` | DEAD (no rust reader for this name): the actual test env var is `TEST_PROXIMADB_FS_COPY_KEY`, an encryption master-key holder used only in a copy-fallback test. | n/a (real var TEST_-prefixed) | test-only; — | src/storage/persistence/filesystem/mod.rs
| `PROXIMADB_FS_FACTORY_KEY` | DEAD (no rust reader for this name): the actual test env var is `TEST_PROXIMADB_FS_FACTORY_KEY`, an encryption master-key holder used only in a factory test. | n/a (real var TEST_-prefixed) | test-only; — | src/storage/persistence/filesystem/mod.rs
| `PROXIMADB_MEMORY_MB` | DEAD (no rust reader) — only emitted into generated deployment manifests by the provisioner; nothing reads it back | n/a | prune candidate; — | `src/deployment/automation/provisioner.rs`
| `PROXIMADB_NAME` | DEAD (not an env var) — a compile-time Rust const = `CARGO_PKG_NAME`, never read from env | n/a | prune candidate; — | `src/version.rs`
| `PROXIMADB_OPERATION` | DEAD (not an env var) — only a substring of the secret-detection regex `SSO_AUTH_APP_TOKEN_PROXIMADB_OPERATION`, never read from env | n/a | prune candidate; — | `src/audit/correlation.rs`
| `PROXIMADB_PSEUDO_QUERY_FIELD` | NOT an env var — a Rust `pub const` holding the metadata key `"proximadb.pseudo_query"`. | n/a | DEAD (no rust reader; misregistered constant, not env) | src/services/operations/vectors/validation/metadata.rs:10
| `PROXIMADB_PSEUDO_QUERY_SOURCE_FIELD` | NOT an env var — Rust `pub const` = `"proximadb.pseudo_query_source_fields"` metadata key. | n/a | DEAD (no rust reader; misregistered constant) | src/services/operations/vectors/validation/metadata.rs:11
| `PROXIMADB_SERVER_PORT` | Intended server-port override — but the test proves env is NOT applied to config. | DEAD (test asserts default `5678` used; no wiring) | test-only; — | src/core/config_loader_tests.rs:794
| `PROXIMADB_STORAGE_CACHE_SIZE_MB` | Intended storage cache-size override — test proves env is NOT applied. | DEAD (test asserts default `512` used; no wiring) | test-only; — | src/core/config_loader_tests.rs:796
| `PROXIMADB_STORAGE_ENGINE` | Emitted into a generated k8s manifest template; no reader in this codebase. | DEAD (no rust reader; manifest output only) | config-mirror; — | src/deployment/automation/provisioner.rs:756
| `PROXIMADB_TENANT_ID` | Emitted into a generated k8s manifest template; no reader in this codebase. | DEAD (no rust reader; manifest output only) | config-mirror; — | src/deployment/automation/provisioner.rs:758
| `PROXIMADB_VERSION` | NOT an env var — Rust `pub const` = `env!("CARGO_PKG_VERSION")` (compile-time crate version). | n/a | DEAD (no runtime env reader; misregistered) | src/version.rs:10
|===

== Pruned (S3, 2026-07-26 first pass)

Deleted from source + registry: `PROXIMADB_PAX_VECTOR_SEGMENTS` (vestigial
no-op reader), 4 stale doc-comment-only names (`PARTITION_LEASE_ON`,
`NATIVE_MORSEL_NUMA`, `WAREHOUSE_DRPATH`, `BENCH_HNSW_EF` mention), and 5
prefix-stub harvest artifacts (guard now ignores names ending in `_`).
Intentionally KEPT in the dead section: template-emitted names (deployment
manifests may feed container entrypoints), test-asserted-unwired vars (the
tests deliberately prove env does NOT leak into config), and compile-time
consts that merely share the prefix.

== Retired names (reserved — never repurpose)

Pre-GA clean renames (TD-ENVGATE-1: no external operators yet, so no alias
layer; at GA, renames become alias-forever):

* `PROXIMADB_IVF2` → `PROXIMADB_PAX_WRITE_A0_TRAIN` (2026-07-25)
* `PROXIMADB_IVF2_PROBE` → `PROXIMADB_PAX_READ_COARSE_PROBE` (2026-07-25)
* `PROXIMADB_IVF2_NPROBE` → `PROXIMADB_PAX_READ_COARSE_NPROBE` (2026-07-25)
* `PROXIMADB_CACHE_NVME_PATH` → `PROXIMADB_CACHE_LOCAL_DISK_PATH` (2026-07-29; pre-GA clean break)
* `PROXIMADB_CACHE_NVME_MAX_GB` → `PROXIMADB_CACHE_LOCAL_DISK_MAX_GB` (2026-07-29; pre-GA clean break)
