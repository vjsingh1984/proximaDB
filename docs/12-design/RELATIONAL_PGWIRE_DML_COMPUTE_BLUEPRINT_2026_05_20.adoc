= Relational Pgwire, DML, Legacy Routing, And Compute Blueprint
:toc:
:toclevels: 4
:sectnums:

Status: Active implementation blueprint +
Date: 2026-05-20 +
Scope: PostgreSQL wire SQL DML, relational catalog validation, legacy VectorOps compatibility,
table-record writes, table-to-table write planning, compute routing, and source-reader execution.

== Purpose

This document is the high-level and low-level blueprint for the current SQL/pgwire relational
write and compute-routing architecture. It records both the target shape and the transitional
implementation currently in the repository, so development can continue without losing the
architectural thread.

Authoritative context:

* `docs/12-design/RELATIONAL_DOCUMENT_GRAPH_CONVERGENCE_2026_05_14.adoc`
* `docs/12-design/RELATIONAL_STORAGE_FORMAT_AND_INTEROPERABILITY_2026_05_19.adoc`
* `docs/12-design/COMPETITIVE_OLTP_OLAP_MPP_TRAJECTORY_2026_05_20.adoc`
* `docs/12-design/adr/ADR-009-schema-modes-and-proximarecord-surfaces.adoc`
* `docs/12-design/adr/ADR-010-pax-block-format.adoc`
* `roadmap/MULTIMODAL_OVERHAUL_SPEC_2026_05_08.adoc`

Key principle: pgwire, REST/gRPC, Arrow Flight, embedded APIs, document APIs, graph APIs, and vector
APIs are facades over xCatalog plus canonical `ProximaRecord`/`ProximaValue` semantics. Legacy
VectorOps can remain as a compatibility route, but it must not be the semantic owner of new
relational DML.

Competitive trajectory principle: ProximaDB adopts OLTP correctness from PostgreSQL/Oracle,
storage pushdown from Exadata/Netezza, projection/vectorization from Vertica/ClickHouse, MPP
routing discipline from Teradata/Redshift, and open compute delegation from Spark/Trino without
moving semantic authority out of xCatalog, canonical WAL, and `ProximaRecord`.

== Current Repository Shape

[cols="1,2,2,2"]
|===
| Area | Current modules | Current role | Migration target

| SQL parser and DML shape
| `src/query/sql_frontend/parser.rs`
| Parses SQL DDL/DML. `INSERT ... VALUES`, `UPSERT`, `UPDATE`, `DELETE` lower to `DmlStatement`.
`INSERT ... SELECT` and `INSERT OVERWRITE ... SELECT` lower to syntax-level `CopyIntoPlan`.
| Parser remains syntax-only. It never chooses storage, compute, WAL, or catalog authority.

| DML service
| `src/services/dml/mod.rs`
| Resolves xCatalog tables, validates relational rows with `CatalogRow`, creates canonical
mutation records, and calls `TableRecordStore`. For table-to-table writes it invokes
`DmlWritePlanner` and `TableWriteExecutor`.
| Keep as DML coordinator, not optimizer and not physical storage owner.

| Catalog row validation
| `crates/control/proximadb-catalog/src/relational.rs`
| Validates columns, nullability, primary-key shape, and converts relational rows to
`ProximaRecord` mutation records.
| Extend to richer `ProximaType`, generated/default columns, check constraints, FK/unique checks,
and source/target coercion.

| Table-record write boundary
| `src/services/record_store.rs`
| Canonical DML write/read trait. `CatalogRoutingTableRecordStore` selects canonical or legacy
route from xCatalog table metadata. `VectorOpsTableRecordStore` adapts to existing VectorOps for
mutation, point lookup, and current-state scan compatibility.
| Replace compatibility adapter for relational/PAX tables with direct WAL/table-record writer.
Keep VectorOps route only for vector-specialized legacy tables during migration.

| Table write planner/router
| `src/query/table_write_plan.rs`
| Models `CopyIntoPlan`, `DmlWritePlanner`, `TableWriteRouter`, candidate paths, cost hints, and
execution guards.
| Add source schema inference, real stats, richer CBO, rejected-path reasons, EXPLAIN integration,
and CTAS/MERGE contracts.

| Table write executor
| `src/query/table_write_executor.rs`
| Defines `TableWriteExecutor`, `TableRecordSourceReader`, planned-only executor, and native
executor that commits `ProximaRecord` batches through `TableRecordStore`.
| Add catalog scan reader, DataFusion reader, Arrow Flight reader, external-file reader, and
open-table commit executors.

| Compute engines
| `src/datafusion/**`, future Polars/DuckDB adapters
| DataFusion integration exists for scan/provider work and is the primary Rust SQL/Arrow substrate.
Polars and DuckDB are optional compatibility/local transform paths.
| DataFusion local/distributed selected by router. Polars/DuckDB remain adapters; never semantic
authorities.
|===

== HLD: End-To-End System Shape

[source,mermaid]
----
flowchart TB
  subgraph Facades["Layer 4: Protocol and API Facades"]
    Pgwire["PostgreSQL wire / SQL"]
    RestGrpc["REST / gRPC"]
    ArrowFlight["Arrow Flight / Flight SQL"]
    Embedded["Embedded SDKs"]
    VectorApi["Legacy vector APIs"]
    DocGraph["Document / Graph APIs"]
  end

  subgraph Planning["Layer 3: Parsing, DML, Query, Planning"]
    SqlParser["SQL parser\nsyntax only"]
    DmlSvc["DmlService\ncatalog-aware DML coordinator"]
    RowValidator["CatalogRow validation\nProximaValue coercion"]
    CopyPlan["CopyIntoPlan\nINSERT SELECT / OVERWRITE"]
    WritePlanner["DmlWritePlanner\nxCatalog target validation"]
    Router["TableWriteRouter\ncandidate paths + guards"]
    Executor["TableWriteExecutor\nnative / DataFusion / external"]
  end

  subgraph Catalog["Control Plane"]
    XCatalog["xCatalog\nschema, workload, layout,\nauthority, stats, snapshots"]
  end

  subgraph Writes["Layer 1/0: Canonical Writes"]
    TableStore["TableRecordStore\ncanonical mutation boundary"]
    Compat["VectorOpsTableRecordStore\ncompatibility adapter"]
    VectorOps["VectorOps\nlegacy vector path"]
    DirectWriter["Direct WAL/table-record writer\n(target)"]
    WAL["WAL / manifest / snapshot commit"]
  end

  subgraph Physical["Layer 2: Physical Layouts and Projections"]
    PAX["PAX / row-family / OLTP"]
    Columnar["Columnar / OLAP / Parquet"]
    LSM["Legacy SST / LSM"]
    ANN["ANN indexes"]
    JsonGraph["JSON, graph topology,\nobservability projections"]
    OpenTables["Iceberg / Delta / Hudi\nexternal-authoritative modes"]
  end

  Pgwire --> SqlParser
  SqlParser --> DmlSvc
  RestGrpc --> DmlSvc
  ArrowFlight --> DmlSvc
  Embedded --> DmlSvc
  VectorApi --> VectorOps
  DocGraph --> DmlSvc

  DmlSvc --> XCatalog
  DmlSvc --> RowValidator
  RowValidator --> TableStore
  SqlParser --> CopyPlan
  CopyPlan --> WritePlanner
  WritePlanner --> XCatalog
  WritePlanner --> Router
  Router --> Executor
  Executor --> TableStore

  TableStore --> DirectWriter
  TableStore --> Compat
  Compat --> VectorOps
  DirectWriter --> WAL
  VectorOps --> WAL

  WAL --> PAX
  WAL --> Columnar
  WAL --> LSM
  WAL --> ANN
  WAL --> JsonGraph
  Executor --> OpenTables
----

== LLD: Pgwire DML Control Flow

=== INSERT / UPSERT / UPDATE / DELETE

[source,plantuml]
----
@startuml
title Pgwire VALUES DML through xCatalog and TableRecordStore

actor Client
participant "pgwire SQL handler" as Pgwire
participant "SqlFrontend parser" as Parser
participant "DmlService" as DML
participant "CatalogManager/xCatalog" as Catalog
participant "CatalogRow" as Row
participant "TableRecordStore" as Store
participant "VectorOpsTableRecordStore\n(compat route)" as Compat
participant "VectorOps\n(legacy route)" as VectorOps
participant "Direct WAL/table writer\n(target route)" as Direct
database "WAL / manifest" as WAL

Client -> Pgwire: INSERT/UPSERT/UPDATE/DELETE
Pgwire -> Parser: parse_dml(sql)
Parser -> DML: DmlStatement
DML -> Catalog: resolve_table(table)
Catalog --> DML: CatalogTableSchema
DML -> Row: validate/coerce values
Row --> DML: CatalogRow
DML -> Row: to_mutation_record(kind)
Row --> DML: ProximaRecord
DML -> Store: write_mutations(schema, mutations)
Store -> Catalog: inspect workload/storage specialization
alt vector or legacy LSM/SST route during migration
  Store -> Compat: write_mutations
  Compat -> VectorOps: insert_records_with_tenant_context
  VectorOps -> WAL: legacy-compatible canonical write
else relational/PAX/modern route target
  Store -> Direct: write_mutations
  Direct -> WAL: RecordMutation / snapshot commit
end
Store --> DML: TableRecordWriteResult
DML --> Pgwire: DmlResult
Pgwire --> Client: command tag / rows affected
@enduml
----

Rules:

* `DmlService` owns catalog-aware DML coordination.
* `CatalogRow` owns relational validation and conversion into canonical records.
* `TableRecordStore` owns mutation semantics below protocol facades.
* `VectorOpsTableRecordStore` is only a compatibility bridge for legacy/vector-specialized tables.
* The direct WAL/table-record writer is the required target for relational/PAX/OLTP/OLAP/HTAP
  tables.

=== INSERT SELECT / INSERT OVERWRITE

[source,plantuml]
----
@startuml
title Table-to-table write planning and execution

actor Client
participant "pgwire SQL handler" as Pgwire
participant "SqlFrontend parser" as Parser
participant "DmlService" as DML
participant "CatalogManager/xCatalog" as Catalog
participant "DmlWritePlanner" as Planner
participant "TableWriteRouter" as Router
participant "TableWriteExecutor" as Executor
participant "TableRecordSourceReader" as Reader
participant "TableRecordStore" as Store
database "WAL / snapshot catalog" as WAL
collections "Open table commit\nIceberg/Delta/Hudi" as Open

Client -> Pgwire: INSERT INTO target SELECT ...\nor INSERT OVERWRITE target SELECT ...
Pgwire -> Parser: parse_dml(sql)
Parser --> DML: DmlStatement::{InsertSelect|InsertOverwrite}\nwith CopyIntoPlan
DML -> Catalog: resolve target table
Catalog --> DML: CatalogTableSchema
DML -> Planner: plan(target_schema, CopyIntoPlan, target_columns)
Planner -> Router: route(RoutingContext)
Router --> Planner: RoutedExecutionPlan\nbackend + guards + candidates
Planner --> DML: RoutedExecutionPlan
DML -> Executor: execute(routed_plan)
alt Native route
  Executor -> Reader: next_batch(source)
  Reader --> Executor: Vec<ProximaRecord>
  Executor -> Store: write_mutations(kind, records)
  Store -> WAL: append/overwrite mutation commit
else DataFusion route
  Executor -> Reader: DataFusion query output -> ProximaRecord batches
  Reader --> Executor: Vec<ProximaRecord>
  Executor -> Store: write_mutations(kind, records)
  Store -> WAL: commit
else External authoritative route
  Executor -> Open: stage data + atomic table commit
  Open --> Executor: snapshot id / commit metadata
end
Executor --> DML: TableWriteExecutionResult
DML --> Pgwire: DmlResult or explicit unsupported gap
@enduml
----

Current status:

* Parser lowering exists for `INSERT INTO ... SELECT` and `INSERT OVERWRITE ... SELECT`.
* `DmlWritePlanner` validates target table/columns and calls `TableWriteRouter`.
* `TableWriteRouter` selects native, DataFusion local/distributed, or external delegated routes.
* `TableWriteExecutor` exists.
* `NativeTableWriteExecutor` can commit `ProximaRecord` batches through `TableRecordStore`.
* Missing: concrete source readers for catalog scans, DataFusion query output, Arrow Flight tickets,
  and external file/table scans.

== Route Selection Matrix

[cols="1,2,2,2,2"]
|===
| Workload / table shape | xCatalog signal | Preferred backend | Write target | Notes

| OLTP relational / PAX row-family
| `workload_profile = Oltp`, `storage_specialization = PaxOltp` or `PaxRowFamily`
| `Native`
| `TableRecordStore` -> direct WAL/table writer
| Point DML, row-level MVCC, constraints, idempotent writes, and prior-version closure belong here.

| OLAP / columnar
| `workload_profile = Olap`, `storage_specialization = PaxOlap` or `ColumnarAnalytics`
| `DataFusionLocal` or `DataFusionDistributed`
| `TableRecordStore` for Proxima-owned tables; open-table writer for external-authoritative tables
| DataFusion handles vectorized scans/joins/aggregates; ProximaDB owns commit semantics.

| HTAP / mixed
| `workload_profile = Htap` or `Mixed`
| Cost router chooses `Native` for write-heavy/point writes, DataFusion for analytical transforms
| `TableRecordStore`
| Requires stats-backed CBO and freshness-aware projection selection.

| Vector ANN / legacy vector tables
| `workload_profile = Vector` or `storage_specialization = VectorAnn`
| Native vector/query facade for search; compatibility write route during migration
| `VectorOpsTableRecordStore` -> `VectorOps`
| Transitional only. Vector search behavior can remain facade-specific, but durable record writes
must converge on table-record semantics.

| Legacy SST/LSM write-optimized
| `storage_specialization = LsmWriteOptimized`
| Native compatibility route initially
| `VectorOpsTableRecordStore` -> `VectorOps`
| Keeps old behavior working while direct WAL/table-record writer and PAX/LSM convergence land.

| Document / graph / observability
| `DocumentJson`, `GraphTopology`, `ObservabilityTimeSeries`
| Native facade operators plus DataFusion where relational/Arrow-compatible
| `TableRecordStore` plus rebuildable projections
| Durable truth remains canonical records. JSON path indexes, topology, rollups, and trace indexes
are projections/access methods.

| External authoritative Iceberg/Delta/Hudi
| primary layout authority is external and format is open-table
| `ExternalDelegated(format)` or DataFusion plus external commit writer
| External atomic commit protocol
| xCatalog records ownership, snapshot, type mapping, policy boundary, refresh/repair source.

| Polars compatibility
| local dataframe transform wins on cost and operator coverage
| `PolarsLocal`
| staged Arrow/record batches then `TableRecordStore`
| Optional backend. It never owns SQL semantics, WAL, catalog, RLS, or record identity.

| DuckDB compatibility
| dialect/file interoperability or user-requested DuckDB path
| `DuckDbCompat`
| staged Arrow/record batches then `TableRecordStore` or external commit
| Optional adapter for compatibility/conformance, not ProximaDB's internal durable engine.
|===

== LLM Implementation Alignment Contract

This section is intentionally prescriptive. It exists so generated implementation work does not
drift into protocol-owned semantics, duplicate durable paths, or hidden engine selection.

=== Non-Negotiable Invariants

Generated code must preserve these invariants:

* Protocol handlers parse, authenticate, shape responses, and attach session context. They do not
  own catalog authority, record identity, WAL semantics, MVCC, RLS, or compute routing.
* `DmlService` coordinates DML after catalog resolution. It validates through catalog contracts and
  delegates writes to `TableRecordStore`; it does not choose storage engines directly.
* `DmlWritePlanner` and `TableWriteRouter` own table-to-table route selection. Any new backend must
  appear as a `ComputeBackend`/candidate route with guards and rejected-route reasons.
* `TableRecordStore` is the only Proxima-owned mutation boundary below services. New relational,
  document, graph, observability, or table-like writes must pass through it unless a doc/ADR marks
  the asset as external-authoritative.
* `VectorOpsTableRecordStore` is a compatibility bridge, not a template for new table semantics.
* `ProximaRecord`, `ProximaType`, and `ProximaValue` are the internal data contract. Protocol DTOs,
  legacy `VectorRecord`, `SqlValue`, Arrow arrays, JSON objects, and open-format rows are adapters.
* xCatalog metadata must be read before route selection and written when capabilities, layout,
  projection freshness, external snapshots, or supported-surface status change.
* RLS, tenant, masking, branch/snapshot, and retention checks must be applied before data leaves a
  ProximaDB policy boundary.

=== Ownership Matrix

[cols="1,3,3"]
|===
| Component | Owns | Must not own

| `src/network/postgres/**`
| pgwire protocol frames, SQL text handoff, command tags, session context
| catalog mutation rules, durable rows, route selection, storage writes

| `src/query/sql_frontend/parser.rs`
| syntax parsing and syntax-level DML/query shape
| xCatalog resolution, type coercion, storage layout, compute backend choice

| `src/services/ddl/**`
| catalog-backed lifecycle operations and schema capability registration
| independent protocol-specific catalogs or physical write paths

| `src/services/dml/**`
| catalog-aware DML coordination, row validation handoff, table-write planning handoff
| direct VectorOps calls, direct file writes, DataFusion/Spark/Trino selection logic

| `crates/control/proximadb-catalog/**`
| schema, type, constraint, capability, authority, freshness, and route metadata
| record data payloads or engine-private state that cannot be rebuilt/validated

| `src/services/record_store.rs`
| canonical mutation/read boundary for table-like assets
| protocol response shaping or frontend syntax behavior

| `src/query/table_write_plan.rs`
| candidate paths, guard requirements, cost hints, selected route metadata
| physical execution loops or WAL commits

| `src/query/table_write_executor.rs`
| executing a routed plan and committing through `TableRecordStore` or external commit protocol
| selecting a backend without `RoutedExecutionPlan`

| Storage/projection readers
| policy-aware scans, snapshot visibility, pruning, Arrow/record batch production
| bypassing xCatalog policy/freshness/authority metadata
|===

=== Required Shape For New Routes

Every new compute or source route must add or update these concepts together:

[source,text]
----
ComputeBackend variant
CandidateWritePath or CandidateReadPath
ExecutionGuard list
PolicyBoundary declaration
AuthorityMode compatibility
Freshness requirement
RejectedRouteReason values
EXPLAIN fields
Focused smoke test
----

The route must stay disabled or planned-only until:

* source snapshot pinning is implemented or explicitly unnecessary;
* target write capability checks are implemented;
* tenant/RLS behavior is clear for every input row;
* lossy type conversions are recorded or rejected;
* projection freshness is updated, preserved, or marked stale;
* retries are idempotent or explicitly non-retriable.

=== Router/Multiplexer Boundary

`DmlWritePlanner` + `TableWriteRouter` are the route-selection boundary. A future standalone router
or router crate may own this responsibility, but the ownership rules stay the same:

* route selection happens once per plan, fragment, split set, or write intent;
* the result is a typed `RoutedExecutionPlan`, not a handler-local callback or string route;
* `TableWriteExecutor` is the runtime multiplexer for table writes: it validates guards and
  dispatches to native, DataFusion, external, or planned-only execution;
* leaf executors/readers own tight-loop performance and must receive typed plans/cursors, not
  protocol DTOs;
* no leaf executor may select itself without a routed plan and no protocol handler may bypass the
  router for a non-trivial route decision.

Codegen rule: if a change adds a new leaf executor, it must add the route candidate and rejected
route reason first. If it adds a new route candidate, it must update `EXPLAIN` before the route is
enabled by default.

=== Required Shape For New Source Readers

Every `TableRecordSourceReader` implementation must document and test:

* source authority mode: Proxima-owned, imported snapshot, federated read, or external authority;
* snapshot semantics: current, pinned catalog snapshot, file manifest snapshot, stream offset, or
  unsupported;
* type mapping into `ProximaType`/`ProximaValue`;
* policy behavior: Proxima-enforced before read, connector-enforced, external-policy, or rejected;
* null/default/generated-column behavior;
* batch ordering and idempotency expectations;
* how errors surface as retryable, permanent, policy violation, type violation, or stale snapshot.

Reader output is `ProximaRecord` or Arrow batches with a deterministic conversion into
`ProximaRecord`. A reader must not return protocol DTOs as internal write payloads.

=== Required Shape For Projection Updates

Whenever a write path updates or invalidates PAX, columnar, ANN, JSON, graph, time-series, open
table, materialized view, or cache projections, generated code must choose one of these outcomes:

* synchronous projection update before commit is visible;
* asynchronous projection directive enqueued with xCatalog freshness watermark;
* projection marked stale with repair source and rejected for current-snapshot queries;
* external-authoritative snapshot registered through the external format commit protocol.

Silent best-effort projection updates are not allowed for supported paths.

=== Anti-Patterns To Reject In Review

Reject generated code that:

* adds a new service-local `HashMap`/file/table as durable metadata without xCatalog registration;
* writes records directly from pgwire, REST/gRPC, Flight, or SDK handlers;
* calls `VectorOps` for new relational, document, graph, or observability semantics;
* chooses DataFusion, Spark, Trino, Polars, DuckDB, or a storage engine inside a parser or protocol
  handler;
* exposes external compute for tenant/RLS data without a policy-boundary guard;
* treats Parquet, Arrow, Delta, Iceberg, or Hudi as canonical record authority for Proxima-owned
  hot tables;
* introduces a new DTO/type envelope when `ProximaRecord`/`ProximaValue` can represent the data;
* reports success when WAL commit succeeded but required synchronous projections failed;
* documents a feature as supported without a supported-surface entry and a benchmark or smoke gate.

== Core Contracts

=== TableRecordStore

[source,rust]
----
trait TableRecordStore {
    async fn write_mutations(
        &self,
        table_schema: &CatalogTableSchema,
        mutations: Vec<TableRecordMutation>,
        tenant_context: Option<&TenantContext>,
    ) -> Result<TableRecordWriteResult>;

    async fn get_by_key(
        &self,
        table_schema: &CatalogTableSchema,
        request: TableRecordGetRequest,
        tenant_context: Option<&TenantContext>,
    ) -> Result<TableRecordGetResponse>;
}
----

Current routing:

[source,text]
----
CatalogRoutingTableRecordStore::store_for_schema(schema):
  VectorAnn or Vector workload       -> LegacyVectorCompatibility
  LsmWriteOptimized                  -> LegacyVectorCompatibility
  everything else                    -> CanonicalRecordStore

Current migration construction:
  canonical_store      = VectorOpsTableRecordStore
  legacy_vector_store  = VectorOpsTableRecordStore

Target construction:
  canonical_store      = DirectWalTableRecordStore
  legacy_vector_store  = VectorOpsTableRecordStore
----

=== DmlService

`DmlService` is the catalog-aware DML coordinator:

* resolves target table with `CatalogManager::resolve_table`;
* verifies table existence;
* validates and coerces values through `CatalogRow`;
* constructs `ProximaRecord` mutation envelopes;
* calls `TableRecordStore` for VALUES DML;
* calls `DmlWritePlanner` and `TableWriteExecutor` for table-to-table writes.

`DmlService` must not:

* choose compute engines directly;
* own physical layout decisions;
* call VectorOps directly except through injected compatibility store construction;
* become the source of truth for catalog, WAL, MVCC, RLS, or storage semantics.

=== CopyIntoPlan And RoutedExecutionPlan

[source,rust]
----
struct CopyIntoPlan {
    source: ReadSource,
    target: LogicalTableRef,
    write_mode: WriteMode,
    conflict_policy: ConflictPolicy,
    distribution: DistributionMode,
}

struct RoutedExecutionPlan {
    backend: ComputeBackend,
    plan: CopyIntoPlan,
    estimated_cost: CostEstimate,
    required_guards: Vec<ExecutionGuard>,
    selected_path: CandidateWritePath,
    candidate_paths: Vec<CandidateWritePath>,
}
----

The parser may construct `CopyIntoPlan`; only the DML planner/router can validate it against
xCatalog and select a route.

=== TableWriteExecutor And Source Readers

[source,rust]
----
trait TableWriteExecutor {
    async fn execute(
        &self,
        request: TableWriteExecutionRequest<'_>,
    ) -> Result<TableWriteExecutionResult>;
}

trait TableRecordSourceReader {
    async fn next_batch(
        &self,
        source: &ReadSource,
        target_schema: &CatalogTableSchema,
    ) -> Result<Option<Vec<ProximaRecord>>>;
}
----

`NativeTableWriteExecutor` already has the write loop:

[source,text]
----
validate execution guards
if backend != Native:
  return PlannedOnly
map WriteMode -> TableRecordMutationKind
while source_reader.next_batch(...) returns records:
  create TableRecordMutation records
  TableRecordStore::write_mutations(...)
return Completed(rows_written)
----

Next readers:

* `CatalogTableSourceReader`: reads Proxima-owned tables at a pinned snapshot.
* `DataFusionRecordSourceReader`: executes DataFusion logical/physical plans and converts Arrow
  batches into `ProximaRecord` batches via xCatalog type mapping.
* `ArrowFlightSourceReader`: consumes Flight tickets into Arrow batches, then records.
* `ExternalFileSourceReader`: reads CSV/JSON/Parquet/Avro/ORC through DataFusion or format readers.
* `OpenTableSourceReader`: reads Iceberg/Delta/Hudi snapshots with xCatalog ownership metadata.

== Execution Guards

Every routed plan must carry guards. Guards are not advisory; they define what must be true before
execution can safely commit.

[cols="1,2,2"]
|===
| Guard | Meaning | Current status

| `PinSourceSnapshot`
| Source table/file/open-table snapshot is stable for the read.
| Planned and validated as required. Concrete readers must enforce it.

| `CheckTargetWriteCapabilities`
| Target supports append/upsert/overwrite/replace/merge mode.
| Planned and validated as required. Capability enforcement needs richer xCatalog flags.

| `EnforceRlsInProxima`
| Tenant/RLS predicates are applied before rows leave the policy boundary.
| Planned. Requires source readers to include policy context.

| `RequireExternalAtomicCommit`
| External authoritative tables must commit through their format protocol.
| Planned. External commit executor is pending.

| `RequireIdempotencyKey`
| Retriable overwrite/replace writes need deterministic idempotency.
| Planned for overwrite/replace. Key plumbing is pending.

| `PreservePreviousSnapshot`
| Old data remains recoverable until retention/branch/CDC rules allow cleanup.
| Planned for overwrite/replace. Snapshot catalog mutation is pending.
|===

== Route Explanation Contract

The route explanation is the mechanical bridge between the planner, `EXPLAIN`, tests, and LLM/code
review. Every routed table write must be able to produce a stable, protocol-neutral explanation.

Current implementation anchor: `src/query/table_write_plan.rs` exposes
`RoutedExecutionPlan::route_explanation()` and the stable explanation structs:

* `TableWriteRouteExplanation`
* `TableWriteIntentExplanation`
* `TableWriteRouteMetadataExplanation`
* `TableWriteCostExplanation`
* `TableWriteDataMovementExplanation`
* `TableWriteCandidateExplanation`
* `TableWriteRejectedPathExplanation`
* `TableWriteRejectedLaneExplanation`

Required fields:

[cols="1,3,3"]
|===
| Field group | Must include | Why it matters

| Target/source
| target table, source kind, write mode, distribution mode
| Prevents parser-level plans from hiding source/target semantics.

| Write intent
| operation kind, durability, isolation, projection freshness, tenant, actor, idempotency key,
schema version, row/byte hints, row-level semantics
| Makes bulk-vs-row-lane decisions and retry safety visible.

| Write lane
| selected lane, reason, required guards, rejected lanes and reasons
| Prevents silent fallback from WAL-current-state semantics to unsafe bulk/file behavior.

| Selected route
| selected backend, selected access method, estimated cost, required guards
| Shows the actual compute/storage route selected by xCatalog-aware planning.

| Route metadata
| authority mode, workload profile, storage specialization, physical format, preferred route,
partitioning, isolation profile, freshness SLA, policy boundary
| Keeps all decisions tied to catalog metadata rather than protocol or handler heuristics.

| Data movement
| source/target rows and bytes, estimated read/write/rewrite bytes, stats source, stats age,
freshness SLA, stats freshness
| Makes distributed and overwrite costs visible before MPP or external routes are enabled.

| Candidates
| every viable backend/access method, cost, guards, pushdown capabilities
| Allows tests to assert that the desired route was considered, not merely selected accidentally.

| Rejections
| every rejected backend/access method, reason, guards that would be required
| Required for safe planned-only behavior and actionable unsupported errors.
|===

The explanation is part of the contract, not debug trivia. Any new route, write lane, guard,
authority mode, or access method must extend the explanation before it becomes default-enabled.

Minimum `EXPLAIN` output for table writes:

[source,text]
----
target=<namespace.table>
source=<catalog-table|query-sql|external-location|arrow-flight-ticket>
write_mode=<append|insert-only|upsert|overwrite-table|replace-partitions|merge>
write_lane=<WalCurrentState|BulkAppendCommit|ExternalAtomicCommit|...>
selected_backend=<Native|DataFusionLocal|DataFusionDistributed|ExternalDelegated(...)>
selected_access_method=<NativeRecord|Pax|Columnar|ExternalOpenTable|...>
authority_mode=<ProximaAuthoritative|ExternalAuthoritative|ImportedSnapshot|...>
policy_boundary=<engine-enforced|connector-enforced|external-policy|unsupported>
required_guards=[...]
rejected_routes=[backend/access_method: reason]
estimated_rows=<n|unknown>
estimated_bytes=<n|unknown>
projection_freshness=<Synchronous|BoundedAsync|StaleOk|RebuildOnDemand>
----

== Source Reader LLD

`TableRecordSourceReader` is the stable boundary for native source reads. DataFusion, Arrow Flight,
external files, and open-table readers should adapt into this boundary or into Arrow batches with a
deterministic `ProximaRecord` conversion.

Current implementation anchor:

* `TableRecordStoreSourceReader` reads `ReadSource::CatalogTable` through
  `TableRecordStore::scan_records`.
* It is valid only for simple catalog-table source reads after route selection chooses the native
  path.
* General `QuerySql`, joins, aggregates, file scans, and Flight tickets must use a compute adapter
  and then yield canonical records.

Required reader metadata:

[cols="1,3"]
|===
| Reader | Required semantics before default use

| `TableRecordStoreSourceReader`
| pinned source snapshot, tenant/RLS context, branch visibility, scan limit behavior, current-state
visibility, and table/schema match validation

| `DataFusionRecordSourceReader`
| logical/physical DataFusion plan id, Arrow schema to `ProximaType` mapping, pushed/residual
predicates, spill behavior, lossy conversion rejection, and deterministic row identity mapping

| `ArrowFlightSourceReader`
| ticket authority, endpoint trust boundary, tenant/RLS enforcement location, schema fingerprint,
batch ordering, retry semantics, and backpressure behavior

| `ExternalFileSourceReader`
| file format, location, schema hint/inference result, split boundaries, CSV line-boundary behavior,
null/default coercion, lossy conversion handling, and object-store consistency assumption

| `OpenTableSourceReader`
| Iceberg/Delta/Hudi snapshot id, manifest/version metadata, delete-file handling, partition
evolution mapping, schema evolution mapping, refresh mode, and external authority mode
|===

Reader failure classes:

* `RetryableReadFailure`: transient I/O, throttling, network, or retry-safe compute failure.
* `PermanentReadFailure`: missing object, unsupported format, corrupt file, unsupported operator.
* `PolicyViolation`: tenant/RLS/masking cannot be enforced before data leaves the boundary.
* `TypeViolation`: source values cannot be losslessly coerced into target `ProximaType`.
* `StaleSnapshot`: requested snapshot/freshness cannot satisfy the route.

The executor should surface these classes as structured errors over time. Until structured errors
exist, error strings must include the class name.

== Projection Freshness State Machine

Layer-2 structures are useful only when freshness is explicit. Writes that affect PAX, columnar,
ANN, JSON, graph, observability, materialized view, cache, or open-format projections must move the
projection through a cataloged state.

[source,text]
----
Fresh
  Projection includes all committed records required by its declared snapshot/freshness SLA.

Updating
  Synchronous or bounded-async update is in progress. Current-snapshot queries may wait, route to
  canonical records, or reject depending on freshness_sla.

Stale
  Projection is known behind the canonical WAL/record state. Current-snapshot queries must not use
  it unless the query accepts stale reads.

RebuildRequired
  Projection cannot be incrementally repaired and must be rebuilt from WAL/record storage.

ExternalSnapshotRegistered
  External authoritative snapshot or Proxima publication has been atomically registered in xCatalog.

Unavailable
  Projection metadata exists but the physical projection cannot be used.
----

Required xCatalog fields for each projection:

* projection id and owning asset id;
* projection kind and access-method family;
* source WAL range or source snapshot;
* last included commit/snapshot;
* freshness state and freshness SLA;
* rebuild source and invalidation policy;
* policy boundary and tenant/RLS pushdown capability;
* checksum or manifest fingerprint where applicable;
* support/maturity status and benchmark gate.

Freshness rules:

* A route requiring current snapshot semantics may use only `Fresh` projections or canonical
  records.
* A `BoundedAsync` projection must expose lag and reject routes when lag exceeds the SLA.
* `StaleOk` must be a query/session/catalog choice, not a silent fallback.
* Projection update failure after WAL commit must either leave the projection `Stale` with repair
  metadata or fail the write if the projection was declared synchronous.
* Open-format publications must register a snapshot/manifest atomically before external readers see
  the new version.

== Data And Mutation Semantics

=== INSERT

* Validates target columns and values against `CatalogTableSchema`.
* Constructs a new `ProximaRecord`.
* Commits `TableRecordMutationKind::Insert`.
* Duplicate handling depends on primary key/unique enforcement in the direct writer; current
  compatibility route inherits legacy behavior.

=== UPSERT

* Validates insert row and conflict metadata.
* Constructs canonical upsert record.
* Commits `TableRecordMutationKind::Upsert`.
* Direct writer must close prior visible version or preserve it according to snapshot/MVCC policy.

=== UPDATE

* Requires primary-key targeted predicates in the current implementation.
* Reads existing visible row with `TableRecordStore::get_by_key`.
* Reconstructs full row shape, applies validated assignments, and writes a new update record.
* Direct writer must close prior visible version and reject stale expected versions when OCC is
  enabled.

=== DELETE

* Requires primary-key targeted predicates in the current implementation.
* Builds a catalog-shaped tombstone record.
* Direct writer must make the tombstone visible at the mutation snapshot and close prior visible
  row versions.

=== INSERT SELECT

* Parser lowers syntax to `CopyIntoPlan`.
* `DmlWritePlanner` resolves target catalog metadata.
* `TableWriteRouter` selects native/DataFusion/external route.
* `NativeTableWriteExecutor` can already commit record batches once a source reader exists.
* DataFusion/external execution remains pending.

=== INSERT OVERWRITE

* Uses `WriteMode::OverwriteTable`.
* Requires previous snapshot preservation and idempotency.
* Native executor maps to `TableRecordMutationKind::OverwriteSnapshot`; direct writer still needs
  atomic snapshot replacement semantics.
* External authoritative targets must use Iceberg/Delta/Hudi atomic commit protocols.

=== MERGE

* Planned in contract only.
* Current native executor rejects MERGE until join predicate, conflict policy, matched/not matched
  actions, and prior-version closure are specified and implemented.

== HLD: Compute Engine Boundaries

[source,mermaid]
----
flowchart LR
  Plan["RoutedExecutionPlan"]
  Native["Native\nOLTP/PAX row-family\nrecord mutation"]
  DataFusion["DataFusion\nSQL/Arrow scans, joins,\naggregates, distributed"]
  Polars["Polars\nlocal dataframe transforms"]
  DuckDB["DuckDB\ncompatibility / file conformance"]
  External["ExternalDelegated\nIceberg/Delta/Hudi commit"]
  Records["ProximaRecord batches"]
  Store["TableRecordStore"]
  Commit["WAL / xCatalog snapshot\nor external atomic commit"]

  Plan --> Native
  Plan --> DataFusion
  Plan --> Polars
  Plan --> DuckDB
  Plan --> External

  Native --> Records
  DataFusion --> Records
  Polars --> Records
  DuckDB --> Records
  Records --> Store
  Store --> Commit
  External --> Commit
----

Boundary rules:

* DataFusion, Polars, DuckDB, Spark, Trino, Flink, and Ray may execute scans/transforms.
* They do not own ProximaDB identity, catalog, policy, WAL, snapshots, MVCC, RLS, or record typing.
* All Proxima-authoritative writes cross back into `TableRecordStore`.
* External-authoritative open tables commit through their format protocol, with xCatalog recording
  authority and refresh/repair semantics.

== HLD: Write Path Fast Lanes

Verification snapshot:

* REST and gRPC record/vector writes already skip SQL parsing. They lower protocol payloads into
  `ProximaRecord` batches and call `UnifiedHandlers` / `VectorOperationsService`.
* Arrow Flight `DoPut` / `DoExchange` skips SQL parsing and accepts Arrow `RecordBatch` payloads.
  Relational descriptors still validate through xCatalog via `CatalogBulkWriteService` before
  records are written.
* `VectorOperationsService` has a bulk-write router, but the current large-batch branch still writes
  through `wal_manager.write_vector_batch_native_arc(...)`. The code deliberately preserves WAL
  durability until WAL-skip safety is resolved; comments and metrics now describe this as a
  WAL-backed bulk lane.
* gRPC v2 record writes can also emit a fire-and-forget PAX projection after successful engine
  insert. That projection is not authoritative and must remain rebuildable.

Recommended shape:

[cols="1,2,3,3"]
|===
| Surface | Parsing/lowering | Default write lane | Fast-lane rule

| REST / gRPC
| Typed API payload -> `ProximaRecord` batch.
| Catalog validation -> canonical WAL/current-state writer.
| May skip SQL planner. Must not skip xCatalog validation, RLS/tenant coercion, constraints, or
  WAL unless the request is explicitly a bulk-load direct-commit mode.

| Arrow Flight
| Arrow `RecordBatch` -> `ProximaRecord` batch, with optional table descriptor -> xCatalog schema.
| Batch validation -> WAL/current-state writer; optional rebuildable PAX/columnar projection.
| Best bulk path. May use a direct segment commit only for append/overwrite snapshot loads where
  ACK waits for durable segment+manifest commit. Upsert/update/delete/merge must use WAL/MVCC
  because they need conflict checks, prior-version closure, tombstones, and replay ordering.

| pgwire SQL
| SQL parser -> `DmlStatement` / `CopyIntoPlan` -> `DmlService` / `TableWriteRouter`.
| Same canonical write lane as REST/gRPC after parsing.
| Should use fast lanes only after SQL lowering proves the operation is batch append/overwrite and
  xCatalog route guards allow it. SQL should not call the slow general optimizer for simple
  `INSERT VALUES`, PK `UPDATE`, PK `DELETE`, or safe `INSERT INTO ... SELECT * FROM table`; it should
  lower to the same record-batch/direct-source executor used by typed APIs.

| External open-table engines
| Format metadata/splits -> Arrow/record batches or external commit protocol.
| External-authoritative mode commits through Iceberg/Delta/Hudi with xCatalog authority metadata.
| Proxima-authoritative mode must return through canonical WAL/current-state writer; external files
  are read sources or projections.
|===

Write-mode contract:

* `WalCurrentState`: default OLTP/HTAP path. Append canonical WAL, update current-state/PAX row
  family, return after WAL durability. Required for insert/upsert/update/delete/merge.
* `BulkAppendCommit`: large append-only load. Convert Arrow/record batches, validate catalog and
  constraints that can be checked batch-locally, write durable PAX/columnar/LSM segment and manifest,
  then return. No WAL skip unless the segment+manifest commit itself is the durable journal entry or
  an equivalent canonical checkpoint/barrier is written.
* `OverwriteSnapshotCommit`: replace table or partition snapshot. Requires xCatalog snapshot
  transaction, atomic manifest swap, and rollback/repair source. Not valid for row-level update
  semantics.
* `ProjectionOnly`: rebuildable PAX/ANN/JSON/graph/observability projections after an authoritative
  write. Fire-and-forget is acceptable only when freshness metadata records lag/staleness.

The optimization target is therefore not “SQL versus API.” The target is to keep protocol adapters
thin and route every surface into a shared write planner with explicit operation semantics,
durability mode, catalog policy, and projection freshness.

Plan refinement:

* Add a shared `WriteIntent` / `WriteLane` contract below protocol adapters. It should carry
  operation kind, idempotency key, tenant context, actor, table identity, catalog schema version,
  durability mode, isolation requirement, projection freshness requirement, and whether batch-local
  constraint checks are sufficient.
* Keep SQL parsing as a pgwire-only syntax adapter. Once parsed, pgwire must call the same
  record-batch and copy-plan executor used by REST/gRPC/Arrow Flight instead of inventing a SQL-only
  write path.
* Keep Arrow Flight as the preferred high-throughput ingress. Its optimization is Arrow-native
  schema/batch transport and direct batch validation, not bypassing authoritative semantics.
* Treat WAL-skip/direct commit as an explicit durability mode, not a hidden optimization. It is
  valid only when the target commit protocol provides equivalent crash recovery, idempotency,
  manifest atomicity, and repair source semantics.
* Correct legacy VectorOps naming/metrics so “direct” means direct segment/manifest commit only
  after that path exists. Until then, bulk-routed VectorOps writes are still WAL-backed.

=== Tenant / RLS Boundary

Tenant ownership is primarily a control-plane property, not a mandatory user-visible table column.
xCatalog owns workspace/account, namespace/schema, table ownership, policy boundary, and default
tenant context. Protocol sessions and service calls carry tenant context into planning and
execution; user DDL should not need to declare `tenant_id` unless the application deliberately
models tenant as business data.

Internal physical metadata may still carry tenant information for enforcement and pruning:

* table or namespace metadata when a physical table is dedicated to one tenant;
* segment/block metadata such as `tenant_id_hash` when all rows in a block share the same tenant;
* internal system fields such as `__tenant_id` only when a physical table/segment intentionally
  mixes tenants and row-level isolation must be enforced below scan/index iterators;
* WAL, CDC, audit, backup, and repair records so replay does not depend on a protocol session that
  no longer exists.

Rule: tenant/RLS fields are system context. They may be exposed through explicit introspection or
open-format projection columns, but they are not logical user columns by default. Physical layouts
should elide per-row tenant storage when the tenant is constant at table/segment scope and must
preserve enough internal metadata for engine-level RLS, snapshot repair, and projection rebuild.

== HLD: Compatibility And Retirement Plan

[source,mermaid]
----
flowchart TB
  Current["Current migration state"]
  Current --> Parser["SQL parser produces DmlStatement / CopyIntoPlan"]
  Parser --> Dml["DmlService"]
  Dml --> Store["CatalogRoutingTableRecordStore"]
  Store --> Compat["VectorOpsTableRecordStore\nfor canonical + legacy branches today"]
  Compat --> VectorOps["VectorOps"]

  Target["Target state"]
  Target --> Dml2["DmlService"]
  Dml2 --> Store2["CatalogRoutingTableRecordStore"]
  Store2 --> Direct["DirectWalTableRecordStore\nrelational/PAX/doc/graph/observability"]
  Store2 --> Compat2["VectorOpsTableRecordStore\nvector-only legacy migration"]
  Direct --> WAL["WAL / PAX / projections"]
  Compat2 --> VectorOps2["VectorOps facade\ncalls canonical store over time"]
----

Retirement gates:

1. Direct WAL/table-record writer supports insert/upsert/update/delete and primary-key lookup. ✓
2. Prior-version closure and tombstone visibility are tested below all protocol facades. ✓ (`services::record_store::tests`: `tombstone_is_invisible_to_scan_after_delete`, `prior_version_is_closed_after_update`, `re_insert_succeeds_after_delete_no_prior_version_interference`)
3. Catalog table scans can read from direct writer snapshots. ✓ (`tombstone_is_invisible_to_scan_after_delete` confirms scan visibility through `DirectWalTableRecordStore`)
4. Vector-bearing tables can write canonical records without `VectorOps` being the mutation owner. ✓ (`services::record_store::tests::vector_bearing_pax_table_writes_canonical_without_vector_ops`)
5. Legacy `LsmWriteOptimized` route is limited to explicitly cataloged compatibility tables. ✓ (`services::record_store::tests::table_record_route_uses_catalog_storage_specialization` — covers all 7 canonical PAX/relational/document/graph specializations and confirms only `LsmWriteOptimized` + `VectorAnn` route to `LegacyVectorCompatibility`)
6. `VectorOpsTableRecordStore` is removed from canonical branch construction.

== Implementation Tracker

[cols="1,1,3,2"]
|===
| Phase | Status | Deliverable | Primary files

| T0
| Done
| Parser and DML service lower VALUES DML into catalog-validated canonical records.
| `src/query/sql_frontend/parser.rs`, `src/services/dml/mod.rs`,
`crates/control/proximadb-catalog/src/relational.rs`

| T1
| Done
| `TableRecordStore` boundary, scan contract, xCatalog route selection, direct `RecordStorage`
adapter, and VectorOps compatibility adapter.
| `src/services/record_store.rs`, `src/services/mod.rs`

| T2
| Done
| `CopyIntoPlan`, `DmlWritePlanner`, `TableWriteRouter`, candidate paths, guards, rule costs.
| `src/query/table_write_plan.rs`

| T3
| Done
| `TableWriteExecutor` boundary, planned-only executor, native record-batch write executor, and
cursor-based source-reader contract.
| `src/query/table_write_executor.rs`, `src/services/dml/mod.rs`

| T4
| Partial
| `TableRecordStoreSourceReader` for catalog-table scans through `TableRecordStore::scan_records`.
Parser lowers only safe `INSERT INTO target SELECT * FROM source` into a catalog-table source;
pgwire full-table scans now call `DmlService::scan_table_records` and project catalog columns from
`ProximaRecord` rows through the compatibility store. Simple pgwire `WHERE` clauses with catalog
column comparisons, `IN`, `LIKE`, and `IS NULL` joined by `AND` are evaluated over the scan result
as a compatibility adapter step. OR predicates, joins, aggregates, full expression planning, and
direct PAX/record-storage scanners remain pending.
| `src/query/table_write_executor.rs`, `src/query/sql_frontend/parser.rs`, `src/services/dml/mod.rs`

| T5
| Partial
| `DirectWalTableRecordStore` appends `CanonicalOperation::RecordUpsert` / `RecordDelete` through
`TableWalAppender`, then applies visible state to canonical `RecordStorage`. `DmlService` exposes an
explicit direct-record-storage constructor that routes relational/PAX branches through the direct
writer and keeps VectorOps for legacy vector/LSM routes. `FramedTableWalAppender` now provides a
binary framed Layer 0 implementation with magic/version/codec/sequence/length/CRC headers and
MessagePack named-field payloads, complete-frame replay, torn-tail truncation, and complete-frame
corruption rejection. pgwire now has explicit direct-catalog constructor hooks and an optional
server-level `DirectPgwireWriteServices` bundle that carries canonical `RecordStorage` plus
`TableWalAppender` through connection startup while keeping default startup on the legacy-safe route.
`PostgresServerConfig.enable_direct_record_writes` now defaults to `true` — retirement gates 1–5 are
satisfied (gate 5 expanded to cover all 7 canonical specializations). Gate 6 (`VectorOpsTableRecordStore`
removed from canonical branch) is blocked on vector/LSM format migration. Remaining T5 work:
replacing the memtable with PAX/LSM-backed current state, segmented WAL manifests, version-chain
closing, and PAX snapshot/partition overwrite commits.
| `src/services/record_store.rs`, `src/services/canonical_wal.rs`, `src/services/dml/mod.rs`,
`src/services/record_memtable.rs`, `src/network/postgres/protocol.rs`, `src/network/postgres/mod.rs`,
`src/network/multi_server.rs`, `src/network/server_config.rs`, WAL/storage modules

| T6
| Pending
| DataFusion source reader: SQL/logical plan to Arrow batches to `ProximaRecord` batches.
| `src/datafusion/**`, `src/query/table_write_executor.rs`

| T7
| Pending
| External file/open-table readers and commit executors.
| `src/catalog/**`, `src/query/table_write_executor.rs`, open-format modules

| T8
| Partial
| Statistics-backed CBO and EXPLAIN output for selected/rejected candidate paths.
`TableWriteRouter` now consumes target/source xCatalog statistics when present and threads row/byte
estimates into route cost and explanation output. Remaining work is fuller column/cardinality stats,
staleness handling, and runtime feedback from executed writes.
| `src/query/table_write_plan.rs`, EXPLAIN modules, xCatalog stats

| T9
| Partial
| SQL DML conformance tests using `DirectWalTableRecordStore` + `FramedTableWalAppender`:
`direct_record_storage_insert_select_and_wal_replay` (INSERT/SELECT/WAL replay durability) and
`direct_record_storage_update_and_delete_conformance` (UPDATE label visibility + DELETE tombstone
invisibility) in `src/services/dml/mod.rs`. Remaining: pgwire wire-protocol SQL tests (external
TPC/BenchBase driver hitting port 5433), metadata/DDL round-trip, and OLAP/HTAP route validation.
| `src/services/dml/mod.rs`, `tools/pgwire/**`, `tests/**`, external benchmark harness

| T10
| Partial
| xCatalog route-knob plumbing: authority mode, workload profile, storage specialization,
freshness SLA, compute route, partitioning, isolation profile, and policy boundary must flow into
`RoutedExecutionPlan.route_metadata` and candidate-path selection. CREATE TABLE option ingestion
now normalizes these knobs into xCatalog metadata, and the internal information-schema extension
exposes them through `information_schema.table_routing`; pgwire/SQL introspection routes that view
through `CatalogIntrospectionService`; REST exposes the same shape at
`/api/v1/catalog/table_routing`. Remaining gRPC/proto work requires a catalog runtime port and
regenerated catalog contracts so root REST catalog logic is not duplicated in the API crate.
| `crates/control/proximadb-catalog/src/lib.rs`, `src/query/table_write_plan.rs`,
catalog DDL/introspection modules

| T11
| Partial
| Route rejection and `EXPLAIN` metadata: selected backend, rejected candidates, required guards,
policy boundary, projection freshness, authority mode, and estimated data movement. The write router
now returns selected/rejected paths and route metadata; `RoutedExecutionPlan` exposes a stable
JSON-serializable explanation and REST exposes planning-only route inspection at
`/api/v1/catalog/table_write/explain`; pgwire `EXPLAIN INSERT ... SELECT` now returns the same
planning-only JSON shape without executing the write. Route explanations now include estimated
source read bytes, target rewrite bytes, write bytes, and before-write target size from xCatalog
statistics when source/target stats are available. Remaining work is gRPC `EXPLAIN` integration,
broader SQL EXPLAIN formats, and richer estimates as catalog statistics mature.
| `src/query/table_write_plan.rs`, SQL/EXPLAIN modules, REST/gRPC explain handlers

| T12
| Pending
| PAX/Arrow local HTAP reader path: catalog-table scan reader over PAX/record storage, Arrow
`RecordBatch` source, tenant/time/min-max pruning, and DataFusion source-reader integration.
| `crates/storage/proximadb-block-format`, `crates/storage/proximadb-storage-common`,
`src/query/table_write_executor.rs`, `src/datafusion/**`

| T13
| Pending
| Open compute split path: Iceberg REST metadata and Arrow Flight tickets expose ProximaDB splits
with policy/authority guards for Spark/Trino/Flink/Ray delegation.
| `src/catalog/iceberg_rest_service.rs`, `src/network/rest/v1/iceberg_rest_catalog.rs`,
Arrow Flight modules, external compute adapters

| T14
| Partial
| Shared write-intent contract and write-lane router: `WalCurrentState`, `BulkAppendCommit`,
`OverwriteSnapshotCommit`, and `ProjectionOnly`. REST/gRPC/Arrow/pgwire must all lower into this
contract after their protocol-specific decoding/parsing.
Initial typed contract now exists in `src/services/write_intent.rs` with operation kind,
durability authority, isolation, projection freshness, catalog/tenant-context/idempotency hints, required
guards, and rejected-lane explanations. `DmlWritePlanner` / `TableWriteRouter` now derive
`WriteIntent` and `WriteLaneDecision` for table-to-table writes and expose the selected lane in
table-write route explanations. Row-level `DmlService` INSERT/UPSERT/UPDATE/DELETE now route a
`WriteIntent` before calling `TableRecordStore`, preserving `WalCurrentState` as the required
OLTP/MVCC lane. REST table-write EXPLAIN now accepts protocol-provided write-intent hints
(`tenant_id` as session/catalog tenant context, `actor`, `idempotency_key`, row/byte estimates, and
constraint/row-level flags) and the EXPLAIN payload returns the normalized `write_intent` plus
selected lane. pgwire now persists `SET proximadb.write.*` session hints and threads them into
planning-only `EXPLAIN INSERT ... SELECT`, sharing the same DML route explanation. Remaining work is
wiring gRPC/Arrow adapters and physical executors to consume `WriteIntent` before storage.
`NativeTableWriteExecutor` now treats lane decisions as execution gates: it commits only
`WalCurrentState` through `TableRecordStore` and rejects `BulkAppendCommit`,
`OverwriteSnapshotCommit`, and `ProjectionOnly` until their dedicated commit protocols exist.
| `src/services/write_intent.rs`, `src/services/dml/mod.rs`, `src/query/table_write_plan.rs`,
`src/services/record_store.rs`,
REST/gRPC/Arrow/pgwire adapters

| T15
| Pending
| Protocol fast-lane parity: REST/gRPC typed writes and Arrow Flight batch writes skip SQL syntax,
while pgwire lowers simple DML into the same record-batch executor. All surfaces share xCatalog
validation, RLS/tenant coercion, constraints, WAL/direct-commit policy, and projection freshness.
| `src/api_handlers/request_handlers.rs`, `src/network/grpc/v2/record_service.rs`,
`src/network/arrow_ipc/service.rs`, `src/network/postgres/protocol.rs`, DML executor modules

| T16
| Pending
| Arrow Flight bulk direct-commit path for append-only and overwrite loads: durable PAX/columnar
segment write, manifest/xCatalog snapshot commit, idempotency key, crash-recovery replay/repair,
and explicit rejection for upsert/update/delete/merge direct-commit attempts.
| `src/network/arrow_ipc/service.rs`, `src/services/operations/catalog_bulk_write.rs`,
PAX/manifest modules, xCatalog snapshot modules

| T17
| Partial
| VectorOps bulk-route cleanup: rename stale “direct write bypasses WAL” comments/metrics until a
real direct segment commit exists; keep current bulk path honest as WAL-backed fast batching.
Current comments/logs/metrics now describe the route as `BULK_WAL`; remaining work is renaming
legacy field/API names such as `use_direct_write` once downstream callers can absorb the change.
| `src/services/operations/vectors/legacy.rs`, `src/services/operations/bulk_write_router.rs`,
metrics/tests

| T18
| Pending
| Cross-surface write conformance: same insert/upsert/update/delete/merge semantics and visibility
through pgwire, REST, gRPC, Arrow Flight, and embedded mode for the same cataloged table.
| `tests/**`, `tools/pgwire/**`, Arrow Flight/gRPC/REST smoke scripts
|===

== Required Test Matrix

[cols="1,2,2"]
|===
| Surface | Required coverage | Notes

| pgwire / psql
| CREATE TABLE, INSERT VALUES, UPSERT, UPDATE by PK, DELETE by PK, INSERT SELECT parse/plan,
INSERT OVERWRITE parse/plan, metadata introspection, point SELECT, full-table SELECT, and simple
non-primary-key predicate SELECT smoke.
| `tools/pgwire/psql_compat_smoke.sh` currently verifies `pass=27 fail=0 known_gap=0`, including
session write-intent hints reaching EXPLAIN, simple non-PK comparison predicates, `IN`, `LIKE`, and
full-table scan returning the inserted row. Must continue improving command tags and
SQLSTATE-compatible errors where possible.

| SQLAlchemy
| Reflection smoke, insert/update/delete round trips, transaction-like behavior for supported
statements, parameter binding.
| Good compatibility proxy for real application clients.

| DML unit tests
| `CatalogRow` validation, mutation record shape, tombstone shape, update reconstruction,
primary-key errors.
| Should avoid server startup where possible.

| Routing unit tests
| OLTP native, OLAP DataFusion, distributed DataFusion, external Iceberg/Delta/Hudi delegated,
legacy vector route, legacy LSM route, WAL current-state, bulk append commit, overwrite snapshot
commit, projection-only rebuild.
| Include rejected-path reasons and durability-mode reasons.

| Executor unit tests
| planned-only guard validation, native record-batch commit, catalog-table scan batching, empty
batches, partial/failure handling, unsupported MERGE.
| Existing native executor tests cover successful native writes, scan batching, and unsupported MERGE.

| Integration tests
| pgwire DML through real catalog manager and storage writer once direct writer lands.
| Existing psql smoke covers the default compatibility route. Add coverage for
`DmlService::with_direct_record_storage`, `FramedTableWalAppender`, and the experimental
`enable_direct_record_writes` startup path.

| Arrow Flight write modes
| `DoPut` and `DoExchange` insert/upsert/delete with table descriptors, WAL mode, direct-mode
fallback/rejection, and future append-only direct commit.
| Direct mode must prove durable segment+manifest commit before it can skip WAL.

| REST/gRPC parity
| Same record batches written through REST/gRPC and pgwire produce the same canonical WAL entries,
current-state visibility, catalog stats, and projection freshness metadata.
| Confirms typed APIs skip SQL syntax but not shared write semantics.

| Benchmarks
| 100k row OLTP and OLAP smoke, then TPC-C-like/BenchBase and TPC-H/TPC-DS profiles.
| Benchmark claims must be gated until direct writer/source readers exist.
|===

== Open Design Decisions

1. Direct writer commit protocol:
   `DirectWalTableRecordStore` now writes canonical `RecordUpsert`/`RecordDelete` operations and
   returns insert conflicts before WAL append. The first production WAL shape is framed binary, not
   JSONL: headers carry magic, format version, codec id, sequence number, payload length, and CRC;
   payloads use MessagePack named-field encoding until a protobuf/Avro schema is promoted as the
   durable cross-language wire contract. Recovery truncates only a trailing partial frame; complete
   frame corruption is a hard error. Remaining protocol work: segmented manifests,
   version-chain closing, idempotency keys, snapshot/partition overwrite commits, and constraint
   helpers for primary key, unique, check, and foreign-key enforcement.

2. Source scan shape:
   `TableRecordSourceReader` is now record-batch-native with an executor-local cursor for native
   catalog-table paths. pgwire full-table SELECT currently uses the same `TableRecordStore`
   scan contract directly through `DmlService`; simple non-PK `AND` predicates over comparisons,
   `IN`, `LIKE`, and `IS NULL` are evaluated as a pgwire compatibility filter. Rich predicates and
   analytical scans still need logical-plan/DataFusion/PAX scanner integration. Decide whether
   DataFusion/open-format paths should expose Arrow batches directly before conversion into
   `ProximaRecord` batches.

3. Snapshot and branch context:
   Add explicit `TableWriteContext` to table-to-table execution so source snapshot, target branch,
   idempotency key, actor, tenant, and isolation travel together.

4. Constraint enforcement:
   Place primary key, unique, check, and foreign-key enforcement below all facades, likely in the
   direct table-record writer with catalog/index helpers.

5. Overwrite semantics:
   Specify table-level vs partition-level overwrite commits for Proxima-authoritative PAX/columnar
   tables and how old snapshots are retained.

6. EXPLAIN:
   Expose `DmlWritePlanner` and `TableWriteRouter` candidate paths, selected backend, guards, and
   rejected reasons through pgwire/REST explain surfaces.

7. Legacy retirement:
   Define objective gates for removing VectorOps from canonical relational branches and keeping it
   only as vector facade/query functionality.

8. WAL-skip direct commits:
   Decide the exact durable proof required before any path may skip canonical WAL. At minimum this
   needs atomic segment+manifest commit, idempotency/retry behavior, recovery replay or repair
   source, catalog snapshot isolation, and CDC/replication semantics. Until this is accepted, “direct”
   write requests must either use WAL or fail with a route-rejection reason.

== Non-Goals

* Do not make VectorOps, DataFusion, Polars, DuckDB, Spark, Trino, or an open-table format the
  internal semantic authority for ProximaDB record writes.
* Do not add protocol-specific DML semantics below pgwire that REST/gRPC/Arrow/embedded cannot
  share.
* Do not hide schema inference, type coercion, RLS, MVCC, or ownership semantics in source readers
  or compute engines.
* Do not physically delete old data during overwrite before snapshot, branch, retention, and CDC
  rules permit cleanup.
