= Convergence Session Handoff — TD-106 / TD-109 / TD-104
:toc: left
:toclevels: 2
Date: 2026-06-03
Branch: proximadb-embedding (HOT — interleaved with other agents)

[NOTE]
====
Resume handoff after a long convergence session. TD-106 (seam S3) and TD-109 are
CLOSED; TD-104 (seam S1) advanced to "all 4 protocols off the concrete root
handler" (S1+S2+S3). This doc is the source of truth for what shipped, what needs
a smoke-test, and the remaining ROOT-deletion work. Companions:
`TD104_HANDLER_CONVERGENCE_PLAN_2026_06_03.adoc` (full TD-104 design) and
`TD106_DOCUMENT_CANONICAL_MIGRATION_HANDOFF_2026_06_03.adoc` (TD-106).
====

== 0. Resume prompt (paste into the new session)

[source,text]
----
Continue convergence on ProximaDB, branch proximadb-embedding (HOT — commit each
slice individually with `git commit -o <explicit files>`; the linter reformats
unstaged files; parallel agents commit concurrently; verify with `git show --stat`).
FIRST READ: docs/12-design/CONVERGENCE_SESSION_HANDOFF_2026_06_03.adoc and
docs/12-design/TD104_HANDLER_CONVERGENCE_PLAN_2026_06_03.adoc, plus memory
project_td106_sqlvalue_migration_2026_06_03.md and
project_td104_handler_convergence_plan_2026_06_03.md.

DONE: TD-106 (document compute fully ProximaValue-native, document:SqlObject
deleted), TD-109 (json->proxima converters converged), TD-104 S1/S2/S3 (REST,
gRPC, pgwire, Arrow Flight all off the CONCRETE root UnifiedHandlers) — and gRPC +
Arrow-Flight smoke-tests PASSED on a running unified-mode server (local FS).
Smoke-test caught/fixed: a real build break (3c944044c, ProximaTree must import
unconditionally) and the arrow StructArray 'key' panic (69029d4ee, greened 2
arrow_ipc tests). The original session task list is COMPLETE; what follows is the
larger ROOT-deletion arc that emerged from TD-104.

NEXT, in priority order:
1. REST S1 smoke-test in multi-port + gRPC-disabled mode (api.unified_mode=false +
   grpc disabled) — the one config S1 fixes, still untested. Server bin:
   `cargo build -p proximadb-server`; grpcurl+reflection / pyarrow.flight clients work.
   (Optional foreign fix: codec::search_results_to_batch builds FixedSizeList(dim)
   even when search results carry no vectors -> Flight DoGet response try_new fails;
   pre-existing, breaks Flight vector-search responses; out of TD-104 routing scope.)
2. TD-104 S5 — build-time field-extraction injection: ~9 REST router-build sites
   read state.request_handlers.{vector_operations_service,document_service,
   event_log,observability_service} (rest/v1/handlers.rs:1207-1521) +
   request_handlers.graph_execution_service (rest/server.rs:312). Inject these
   services (all in SharedServices) into AppState at build so the REST router-build
   no longer reads ROOT's concrete fields. Boot wiring; same-instance swaps.
3. Remove the rest/v1/handlers.rs:1337 ports=None ROOT fallback once the
   legacy/cluster constructors (with_security_and_config) also wire api_handlers.
4. ROOT deletion (large): re-home UnifiedHandlers' ~30 methods' logic onto the
   concrete services as port impls; make Arrow's ports runtime-backed (impl
   RecordOpsPort on a runtime-accessible target + inject vector/collection
   services at boot instead of extracting from the passed UnifiedHandlers in
   ProximaFlightService::new); then delete src/api_handlers/request_handlers.rs
   UnifiedHandlers.

Reuse-first; cite the canonical surface or an ADR for any new struct/port. Verify
each slice: cargo check --lib then targeted cargo test --lib. The crate DENIES
unused imports (an unused import => "could not compile"). cargo check --lib SKIPS
#[cfg(test)] — run test-compile before declaring done.
----

== 1. Shipped this session (all committed, compile-verified)

=== TD-106 (seam S3) — CLOSED
Document modality compute is 100% `ProximaValue`/`ProximaTree`-native;
`DocumentRecord.document: SqlObject` deleted; `props: ProximaTree` is the sole
in-memory body. Commits: `58a4f4ac4` (S5 aggregation), `32d4436b3` (S6 dual-carry),
`39a68714e` (sync-fix), `f8f2e5e65` (S7a filter), `dfed61722` (S7b index+sort),
`a10695861` (S7c fulltext), `3ed3fe14b` (S7d update-mutation), `47d1186b4`
(S7e delete field), `2e5595a8f` (dead JsonPath engine removed), `13518b14f`
(index-value minor), `4be1f7958` (drop unwired AggregationExpression). Residual
`SqlObject` = edges only (proto wire, persistence `_document`, `$lookup` join,
AggregateResult, condition operands).

=== TD-109 — DONE
`aff26afca`: the 3 scattered `json->ProximaValue` copies (rest/v2/query.rs,
arrow_ipc/codec.rs, rest/websocket.rs) converged onto
`proximadb_records::conversions::json_to_proxima` (websocket gained correct
Array/Map/Null — was a stringify bug). Deferred (optional): relocate
`proxima_value_to_json`/`proxima_tree_to_json_map` into `proximadb-records`.

=== TD-104 (seam S1) — S1+S2+S3 SHIPPED: all 4 protocols off the concrete root handler
* **S1** `f141dcc5d` — REST: `RestServerPorts.api_handlers` unconditional
  (doc/graph/obs ports → `Option`); multi-port REST wires the runtime handler even
  with gRPC disabled.
* **S2** `7e18a483b` — gRPC: api_port switched from `services.request_handlers`
  (ROOT) to `services.api_handlers` (RUNTIME) at all 3 boot sites; gRPC v1 only
  ever used the 7 `ApiHandlersPort` methods.
* **pgwire** — already decomposed (never used the monolith).
* **S3a** `f10c7cbbe` — `RecordOpsPort` (insert/upsert/delete over
  `Vec<ProximaRecord>`) + relocated `BatchOperationResult`/`BatchOperationMetrics`
  to `proximadb-runtime` (root `src/services/operations/batch_result.rs` is a
  re-export shim; 8 consumers unchanged); added `proximadb-records` dep to runtime;
  impl `RecordOpsPort for UnifiedHandlers` (delegates to `handle_record_*`).
* **S3b** `20e41ad95` — `ProximaFlightService` holds `Arc<dyn ApiHandlersPort>` +
  `Arc<dyn RecordOpsPort>` + the vector-ops/collection services directly (not
  `Arc<UnifiedHandlers>`); `new()` is the boot adapter (extracts/casts from the
  passed `UnifiedHandlers`), so `ArrowFlightServer`/`multi_server` are unchanged.
  Verified: 131 Arrow tests pass unchanged (5 failures pre-existing onnx/StructArray).

== 2. Smoke-tests — gRPC + Arrow Flight PASSED (2026-06-04, unified mode, local FS)

Ran the built `proximadb-server` (unified mode, port 5678 multiplexing REST/gRPC/Flight)
against `/tmp/proximadb` (no S3/MinIO). Also caught + fixed a real build break first:
S7 used `ProximaTree` unconditionally but its import was `canonical-document-store`-gated
— the server's feature set (that flag off) failed to compile until the import was made
unconditional (`3c944044c`). `cargo check --lib` (default features) had hidden it.

* **gRPC (S2) — PASS.** `grpcurl` + reflection: `CollectionService.ListCollections`
  (`{}`), `CreateCollection` (id returned), `VectorService.VectorBatch` insert (2
  records, success), `VectorService.VectorSearch` (2 results, v1 score 1.0 exact / v2
  0.92). Routed gRPC → runtime `api_handlers` → `CollectionPort`/`VectorOpsPort`.
* **Arrow Flight (S3) — PASS.** Raw `pyarrow.flight` to `grpc://localhost:5678`:
  **DoPut** insert OK (2 records → `record_port.insert_record_batch`); **ListFlights**
  OK (count=0, correct — no flushed `.arrow` files; reached `collection_service`);
  **DoGet** search executed and `✅ WAL search completed: 2 results` in the server log
  (routed via `api_port.handle_vector_search_v1`). NOTE: the DoGet *response* then hit
  a **pre-existing, foreign** panic in the Arrow result encoder
  (`arrow-array-57.3.1 struct_array.rs:91: "Found unmasked nulls for non-nullable
  StructArray field 'key'"`) — the SAME bug as the 5 pre-existing `arrow_ipc` test
  failures (verified foreign earlier via stash-on-HEAD). It crashes the Flight
  search-response encoding (metadata struct), NOT the S3-changed routing/search.

=== Arrow Flight search-response encoding (foreign, out of TD-104 routing scope)
A chain of pre-existing bugs in the Flight DoGet *response* encoder (not the
S3-changed routing — the search executes; the log shows "WAL search completed: 2
results"):
* **FIXED `69029d4ee`** — metadata `StructArray` `key` field was non-nullable but
  encoders push `None` keys → arrow-array 57.3.1 panic ("unmasked nulls for
  non-nullable StructArray field 'key'"). Made `key` nullable at all 3 sites
  (schema + 2 builders); greened `arrow_ipc` codec tests (5 failures → 3) and
  stopped the Flight DoGet crash.
* **STILL OPEN (separate, pre-existing)** — `codec::search_results_to_batch`
  builds the `vector` column as a `FixedSizeList(dim)` unconditionally, but search
  results carry no vectors unless `include_fields.vector=true`, so it gets 0 floats
  vs `dim×N` expected → `RecordBatch::try_new` fails ("Failed to create RecordBatch
  from search results"). Fix: build the vector column null/safe when vectors are
  absent. (And the proto `VectorSearchRequest` ticket is parsed by strict
  prost-serde — `include_fields` JSON shape is finicky; the no-`include_fields`
  ticket parses fine.) These block Flight vector-search *responses* in general and
  predate TD-104; flag for the Arrow-encoder owner.

Still UNTESTED: REST (S1) in multi-port mode with gRPC disabled
(`api.unified_mode=false` + gRPC off) — the specific config S1 fixes.

== 3. What still ties to ROOT (`src/api_handlers/request_handlers.rs` UnifiedHandlers)
. **S5 build-time field extraction** — `rest/v1/handlers.rs:1207-1521` (AQL source
  registration, hybrid backend, memory store) + `rest/server.rs:312` read ROOT's
  concrete service fields. Inject the services (in `SharedServices`) at build.
. **`ports=None` fallback** — `rest/v1/handlers.rs:1337` (legacy/cluster
  constructors `with_security_and_config`).
. **Arrow ports backed by ROOT** — `ProximaFlightService::new` extracts/casts from
  the passed `UnifiedHandlers` (the runtime handler doesn't impl `RecordOpsPort`
  and lacks the concrete vector/collection service fields).
. **ROOT's business logic** — UnifiedHandlers still implements the port traits and
  owns the concrete services. ROOT deletion = re-home its ~30 methods onto the
  concrete services as port impls.

S4 (`execute_sql_frontend`) is MOOT — it's an internal helper of ROOT's
`execute_sql_v1` (request_handlers.rs:2309), bypassed by the runtime handler's
port-delegating path; it dies with ROOT.

== 4. Landmarks
* Root handler (logic): `src/api_handlers/request_handlers.rs` (`UnifiedHandlers`,
  impls `ApiHandlersPort`+`RecordOpsPort`; `handle_record_*` at 1021/1069/1145).
* Runtime handler (port facade): `crates/platform/proximadb-runtime/src/handlers.rs`.
* Ports: `crates/platform/proximadb-runtime/src/{port.rs,service_ports.rs,
  record_ops_port.rs}`; `batch_result.rs` (relocated result types).
* Boot: `src/network/multi_server.rs` (`start`/`start_unified`/`start_with_cluster`);
  `src/network/rest/server.rs` (`RestServerPorts`, `with_security_and_config_and_ports`).
* Arrow: `src/network/arrow_ipc/{service.rs,server.rs}`.

== 5. Lessons
* The crate DENIES unused imports (an unused `use` => "could not compile", shown
  only as a warning line — look past the summary).
* `cargo check --lib` SKIPS `#[cfg(test)]` and feature-gated code; run a
  test-compile (`cargo test --lib <path>`) before declaring a slice done.
* WAL-recovery tests (`test_canonical_document_wal_recovery_*`) fail in ISOLATION
  (global-manifest set-once 2nd-boot singleton) but pass in the full suite — a
  pre-existing flake, not a regression. arrow_ipc has 5 pre-existing failures
  (onnx-off embedding + an arrow `StructArray` test panic) — foreign.
* For deep multi-line field-extraction chains, a `perl -0777 -pi` collapse of
  `\n[ \t]*\.request_handlers(?=\n[ \t]*\.X)` is faster + safer than manual edits;
  the compiler then proves completeness (no `.request_handlers` left + types match).
* HOT branch: HEAD moves under you (other agents commit); `git commit -o` lands on
  whatever HEAD is. Your committed work survives linter reformat of the working tree.
