= Architecture Decision Records
:toc: left
:icons: font
:last-updated: 2026-06-30

[.lead]
This directory contains Architecture Decision Records (ADRs) governing ProximaDB's implementation across all open issues (#25-#61). Each ADR defines canonical contracts, interfaces, and file plans that implementation must follow.

== ADR Index

[cols="1,4,2,2"]
|===
| ADR | Title | Status | Governs

| link:ADR-001-capability-registry.adoc[ADR-001]
| Capability Registry Architecture
| Accepted
| SB-01→07, #25, #26, #30

| link:ADR-002-normalized-filter-candidate-contract.adoc[ADR-002]
| Normalized Filter and Candidate Contract
| Accepted
| SB-08→13, #57

| link:ADR-003-multimodel-plan-contract.adoc[ADR-003]
| MultiModelPlan v1 Contract
| Accepted
| SB-14→20, #29

| link:ADR-004-unified-explain-contract.adoc[ADR-004]
| Unified Explain Contract
| Accepted
| SB-17, SB-20

| link:ADR-005-document-graph-query-extensions.adoc[ADR-005]
| Document and Graph Query Extension Architecture
| Accepted
| #51→53, #58, #59

| link:ADR-006-infrastructure-platform-decisions.adoc[ADR-006]
| Infrastructure and Platform Decisions
| Accepted
| #55, #54, #60, #61, #28, #27, #56

| link:ADR-007-iceberg-rest-catalog-server.adoc[ADR-007]
| Iceberg REST Catalog Server in xCatalog
| Accepted
| Open-format catalog, Arrow Flight split planning

| link:ADR-008-oltp-catalog-backends.adoc[ADR-008]
| OLTP Catalog Backends in xCatalog
| Accepted
| PostgreSQL/MariaDB/SQLite metadata backends

| link:ADR-009-schema-modes-and-proximarecord-surfaces.adoc[ADR-009]
| Schema Modes and ProximaRecord-Centric API Surfaces
| Accepted
| REST/gRPC/Arrow/SQL/SDK schema-on-read and schema-on-write convergence

| link:ADR-014-vector-query-2-shape.adoc[ADR-014]
| Vector Query Two-Shape Boundary
| Accepted
| `VectorOpsPort` (proto-typed runtime port) vs `VectorQueryService` (Rust-typed orchestration contract) — deliberate, not duplication

| link:ADR-015-port-impl-location.adoc[ADR-015]
| Port Impl Location — Bare Concrete Service vs gRPC Wrapper
| Accepted
| `impl XxxPort` lives on the bare concrete service; the gRPC wrapper is a thin proto-conversion shim that delegates to the port

| link:ADR-016-port-user-context.adoc[ADR-016]
| `PortUserContext` — Boundary Type for `SecurityPort` Authentication
| Accepted
| Lossy projection of root-crate `UnifiedUserContext` so `SecurityPort::authenticate` can exist at the runtime boundary without dragging the typed permission graph into the runtime crate

| link:ADR-025-deletion-vectors-cold-path.adoc[ADR-025]
| Deletion Vectors for Cold-Path Deletes (Layered over MVCC)
| Proposed
| Cold-path delete mechanism (DV bitmap + immutable files + Iceberg snapshots); rejects redo/undo; retains MVCC for the hot delta

| link:ADR-026-pax-canonical-vector-path.adoc[ADR-026]
| PAX Stacked Quantization as the Canonical Vector Path (Retire Legacy Progressive Search)
| Proposed
| AXIS graph + PAX stacked quant (RaBitQ->SQ8->f32) is the one vector path; retire legacy progressive engines, fold their designs into PAX; PAX is the cross-modality internal columnar substrate (Parquet at the interop seam); configurable geometry + user/adaptive/learned tuning hierarchy. Tracked: TD-150..157

| link:ADR-027-unified-storage-and-metering-substrate.adoc[ADR-027]
| Unified Storage + Metering Substrate (PAX-as-one-internal-format, two-class trace, coalesced write-path, dual-sink metering)
| Proposed
| PAX is the single internal format (LSM writes it for all modalities; one reader/one SQL lowering/one I/O-trace; Parquet at the seam). Trace = two classes: billing (always-on, full-fidelity) vs perf/geometry (compile-time gateable). Write-path reuses SST/LSM (threshold-flush + compaction; Aurora/Neon/Turso-grounded; no per-query object writes). Metering dual-sunk: durable per-tenant in object store + external OTLP. Tracked: TD-158, TD-160..162

| link:ADR-028-index-policy-routing.adoc[ADR-028]
| Per-Collection Index-Policy Routing + Cost-Derived Exact-vs-ANN Auto-Default
| Proposed
| Paved road + escape hatch for cold/warm search routing: precedence per-query `SearchMode` > collection `IndexPolicy` (typed proto knob: mode/rehydrate/byte_budget/nprobe, fail-closed validity matrix) > cost-derived auto-default (byte budget = `f(storage_class)`, not a vector count). Cold HNSW is dominated by exact up to the byte budget; quantization-only collections auto-route to HELIX (env-gated default-OFF); fixes IVF Defect B (persist-after-insert) so the cheap cold-load path returns vectors. Tracked: TD-165; cost-model trace consumption TD-158/TD-159

| link:ADR-029-shared-codegraph-chunker-package.adoc[ADR-029]
| Shared Code→CPG Chunker Package (`victor-codegraph`) — one chunker, three consumers, zero duplication
| Proposed
| The tree-sitter code→symbol+relation chunker is duplicated across ProximaDB SDK (`code.py`) and Victor (`victor-coding`); extract it into one neutral Apache-2.0 PyPI package (`victor-codegraph`, deps = `victor-contracts` + tree-sitter) consumed by Victor (owner), ProximaDB SDK (optional extra, retires `code.py` to stay generic), and anvaiops (SaaS code-graph vertical, ADR-0017 F3). Adopts LlamaIndex `CodeSplitter` size-discipline + Victor fallback chain + ProximaDB taxonomy; emits the substrate-keystone `ProximaRecord` shape. Boundary-safe (policy-free). Tracked: TD-CG1..CG3

| link:ADR-030-unified-hotpath-trace-meter-seam.adoc[ADR-030]
| Unified Hot-Path Trace+Meter Emission Seam (one I/O boundary, two structurally-separate sinks)
| Proposed
| Implements ADR-027's two-class trace via the per-query `tokio::task_local!` `IO_TRACE` accumulator: it is fed at the `read_range` GET boundary (no tenant needed), drained ONCE at scope-close (where tenant+engine are known), and fans the same snapshot to THREE observers — route-cost + cache (gateable perf, → `avg_get_size` for the cost model + adaptive engine, TD-158) and a NEW billing observer (always-on) that closes the verified **KRU** read-meter (`record_task_execution_time`) gap. **KSU** (storage accrual) is explicitly scoped out as the sibling write-path/periodic gap. Same measured snapshot ⇒ cost-model bytes == billing bytes (no divergence). Honors ADR-027 non-entanglement (separate sinks; billing never gated; CI guard) and the open-core boundary (OSS emits neutral units; AnvaiOps prices them). Closes the cost model's INPUT side only — routing-loop closure is a separate ADR. Tracked: TD-158 + KRU; prereq TD-160

| link:ADR-031-stable-object-id-physical-key.adoc[ADR-031]
| Stable `object_id` as the Physical Collection Key — the **catalog-object surrogate identity** authority (name is a mutable attribute)
| Proposed
| Keys the canonical-WAL `collection_id`, the memtable partition, and storage paths on a stable immutable `u64` `object_id` instead of the mutable name — **rename-safe** + **structurally tenant-isolated** (drops the per-query `tenant_id` feed predicate, since the id is globally unique). The **authority for catalog-object surrogate identity** (account/namespace/table/column/index): the parallel `ADR-033-catalog-object-surrogate-identity` draft (PR #436) was closed — `033` was taken (primary-storage-format) and re-use fails the #441 `td-adr-unique` guard — and its decisions, the salvaged execution plan, and the ADR-024 identity addendum are folded here; `object_id` ≡ `object_oid`. Mixed-read-safe **O0–O4** cutover, default-OFF (relational O0–O2 merged). Execution plan: `CATALOG_OID_MIGRATION_2026_06_27.adoc` (TD-181). Tracked: ADR-031 O0–O4.

| link:ADR-032-durable-tenant-prefixed-object-store-catalog.adoc[ADR-032]
| Durable, Tenant-Prefixed Object-Store Catalog (the catalog as the serverless shared truth; structural isolation, not a per-query predicate)
| Proposed
| An audit found the catalog is not yet durable on object storage (cloud URLs cached to `/tmp`; now fail-closed via PR #410), its metadata bypasses the `DrPathBuilder` tenant prefix (the one non-isolated surface), and the service-layer per-query tenant predicate is inert in production (no `with_tenant_manager` caller) and is the wrong mechanism. Decision: persist `NativeCatalog` through the existing `FilesystemFactory` (cloud backends already write — a wiring job, not a new client), address catalog assets through `DrPathBuilder` (`data/{tenant}/{ns}/…`, Phase-5 `accounts/{account}/…`), record tenant via `create_namespace_for_tenant`, and move isolation to the network edge (explicit fail-closed `DeploymentMode`) + the path — retiring the inert predicate. Mixed-read-safe, marker-detected, default-OFF, migration tool. Operator catalog stays in AnvaiOps; object-store IAM is future work. Tracked: TD-CAT-1/2/3, TD-113, TD-119, TD-164

| link:ADR-033-primary-storage-format-strategy.adoc[ADR-033]
| Primary Storage Format Strategy — own the vertical (PAX) where it pays, keep Parquet at the interop seam
| Proposed
| An adversarially-verified survey (22 sources; Postgres/InnoDB/Oracle-HCC/Aurora/Neon + ClickHouse/Snowflake/BigQuery/Delta/Iceberg/Lance/Turbopuffer) settles the "why Parquet, not our own format" question: **no serious engine uses Parquet as primary** — all own a co-designed format and expose open formats only at the interop edge. But the win is **workload-specific**: custom formats beat Parquet **100×+ on random-access / vector / selective** queries and **< 2× on sequential OLAP scans**, and Parquet-as-primary is genuinely right for lakehouse/Iceberg interop. Decision: the **dual-path is vindicated** — keep Parquet as the relational-scan + interop primary (not demoted to export), and spend the PAX budget on the vector/random-access path via **object-storage round-trip minimization** (Turbopuffer's ≤3-round-trip budget; Parquet's footer amplification is the measured anti-pattern) and **format⇄index co-design** (the Lance pattern). Recasts **TD-162a-2**: its worth is unifying relational+vector storage, enabling the Native cost-router arm, and zone-map/projection pruning on selective queries — *not* chasing scan-throughput parity (already < 2×). Adds a **first-principles cost spine** (latency = dependency-DEPTH × RTT; **depth ≫ width ≫ bytes ≫ CPU**) and an **egress correction** (egress = the customer result only; object-store reads are internal; quantization is a residency/round-trip lever, not egress). Tracked: TD-162a-2

| link:ADR-034-objectstore-access-path-codesign.adoc[ADR-034]
| Object-Store Access-Path Co-Design — minimize dependency depth
| Proposed
| Applies ADR-033's cost spine to the *access path*. A map of the real PAX format found the layout sound but the read path reproduces Parquet's failure internally: a pruned ranged read is **`1+3K` serial dependent GETs** (segment-index → per-block footer → per-block col-meta → per-block body), and graph traversal pointer-chases cold object storage (no RAM topology). Records 8 derived principles (depth-first; predictive/cached metadata; large-outside/fine-inside; codes-only residency; **graph = the adversarial workload → topology in RAM + batched frontier**; criticality tiering; round-trip budget as a planner constraint; LSM with free delta detection) and six ROI-ordered gap-closure decisions: **TD-167** read-path depth collapse (zone-map summary in `SegmentIndex` + `read_ranges` body-batching → depth ~1-2), **TD-168** graph topology RAM index, **TD-169** segment code region, **TD-170** round-trip budget, **TD-171** criticality tiering, and TD-162a-2 (typed stripes, last — < 2× scan). All additive, mixed-read-safe, GET-count-harness + recall-ratchet gated. Tracked: TD-167..171, TD-162a-2

| link:ADR-035-per-tenant-system-catalog-cache.adoc[ADR-035]
| Per-Tenant System Catalog — two-group object layout + 3-tier read cache + provisioning lifecycle
| Proposed
| Resolving a collection's metadata costs **1+N+M object-store GETs** on a cold cache (`list_namespaces` + per-ns `list_tables` + per-table `get_table`), re-paid after every pod restart — the wrong bill on the hot path. Decision: (D1) split the per-tenant system catalog into two object-store groups — **METADATA** (read-heavy registry, keyed by global `u64` object_id since names collide across tenants: `_syscat/objects/{oid}.json` + a `name→oid` index) and **OPERATIONAL** (write-heavy append: `_metering`/`_trace`/`_billing`, TD-161). (D2) a **3-tier read path** — hot in-memory `TenantCache` (≤1 MB/tenant, 5-min TTL, corpus-version-stamped) → warm local disk (OS page cache) → canonical object store. (D3) a **provisioning lifecycle** — signup pre-creates a bare-minimum skeleton, user DDL appends system-managed rows. (D4) hydration triggers — pgwire session-ready warm + lazy warm-on-first-miss for the stateless edges. Composes existing `TenantCache`/`CorpusVersionRegistry`/`CacheInvalidationCoordinator`/`IdAllocator`. Full read/write/provision/pod-start/invalidation branch table. Mixed-read-safe; default-on read accelerator first. Tracked: TD-SC-1..5; depends on ADR-032/TD-CAT-2.

| link:ADR-036-azure-objectstore-orientation.adoc[ADR-036]
| Azure / Object-Store Orientation — one account, Blob endpoint, flat keys, HNS-off, access-tier as the cost lever
| Proposed
| Re-orients the code to Azure ground truth after the MVP work found we registered four schemes (`adls`/`abfs`/`az`/`azure`) as if distinct, titled the backend "ADLS Gen2", and **silently dropped** the per-object access tier (every cloud `write` ignored `FileOptions`). Truth: one Storage account, **Blob endpoint** (flat keys) + an optional DFS endpoint *only* under Hierarchical Namespace; **ADLS Gen2 = Blob + HNS**; our four schemes all resolve to the *same* flat Blob backend, so `adls`/`abfs` were misleading (never engaged ABFS/DFS/HNS). The **cost lever is the access tier** (Hot/Cool/Cold/Archive), a property of the PUT available on a **flat (HNS-off)** account — HNS is a cost add, not a benefit, for our flat-key/immutable/ranged-read workload. Decisions: run flat Blob HNS-off (default cold = Cool/Cold tier); plumb a **canonical cloud-neutral `ObjectAccessTier`** (`hot`/`cool`/`cold`/`archive`) through `FileOptions.storage_class` → `object_store` `Attribute::StorageClass` (Azure `x-ms-access-tier` + S3 `x-amz-storage-class`), default-safe on unset/typo; scheme honesty (`az`/`azure` canonical, `adls`/`abfs` documented Blob aliases + one-time INFO log). GCS + `ProximaObjectStore` tier deferred (need backend-aware mapping; not the MVP path). Additive, no flag-day, moves the **at-rest GB-month** cost term (not the round-trip count). Tracked: TD-173

| link:ADR-037-statistics-substrate-and-reference-mcp.adoc[ADR-037]
| Statistics Substrate & Reference MCP Surface — aggregate the zone maps once; explain, routing, and agents all read it
| Proposed
| The agent-facing catalog's *producer* half: an agent is context-bounded, not compute-bounded, so the engine (the only thing co-located with the data) must hand it the minimal sufficient statistic. Maintain *mergeable* streaming sketches (HLL / t-digest / count-min / reservoir) on the existing flush/compaction pass + the PAX zone-map min/max — **no new scan, no scan-to-answer `ANALYZE`** — and assemble a frozen v1 `StatisticsEnvelope` exposed on the introspection surface (units only — NO $/PII; those are the AnvaiOps boundary). Per-modality extractors (graph/vector/document/trace/timeseries) fill the envelope's blocks over existing engines; a thin **reference MCP** projects existing surfaces (list/describe/stats/explain/search/memory) unprivileged. Also feeds ADR-004's `estimated_selectivity`/`estimated_rows` slots. (Renumbered from ADR-031 — that number landed for stable-object-id.) Tracked: TD-174/175/176, TD-177..180 (block-stats spine)

| link:ADR-038-statistics-envelope-freshness-coherence.adoc[ADR-038]
| Statistics Envelope Freshness Coherence — per-source freshness, conservative floor (v1.1)
| Proposed
| v1.1 of the ADR-037 envelope: each modality/source block carries its **own** freshness watermark (it was produced at a different flush), and the envelope's top-level freshness is the **conservative floor** (oldest source) so a consumer never over-trusts a stale block. (Renumbered from ADR-032 — that number landed for the durable tenant-prefixed catalog.) Tracked: TD-174

| link:ADR-039-olap-engine-boundary-swappable-physical-backend.adoc[ADR-039]
| OLAP Engine Boundary — DataFusion is a swappable physical backend behind the shared `LogicalNode` seam
| Accepted
| Ratifies + freezes the (audited 2026-06-27) design that makes DataFusion swappable as a *backend*, not a rewrite: the SQL frontend emits ProximaDB's own engine-neutral `LogicalNode` IR (relational crates carry **zero** DataFusion deps); DataFusion lowers `LogicalNode → datafusion::LogicalPlan` inside `src/datafusion/**` only; results come back as Arrow → `RelationalRow`/`ProximaValue` (pgwire never sees a DataFusion type); scalar functions are defined **once** in `proximadb-functions` and merely *bound* per engine; DataFusion is an optional cargo feature with the native Volcano path as the floor. Invariants are **ratcheted** (a leakage grep + a no-DataFusion CI build) so the boundary can't erode, and the swap procedure is captured so it is never re-analyzed. Dispatch is `dyn`/enum at the **per-query** seam — coarse, so the runtime bears no per-row cost (no engine generics threaded through the pipeline). Tracked: TD-181

| link:ADR-040-conformance-accuracy-ratchet.adoc[ADR-040]
| Conformance ratchets are ACCURACY ratchets — differential cross-engine + anchored answers, not "executes cleanly"
| The pgwire conformance ratchets (TPC-H 22, TPC-DS 16, graph 10, doc 11, ts 10) count queries that **execute without error**, not queries that return **correct answers** — only ~1 query per suite has its result asserted (TPC-H verifies Q1; the other 21 are execution-only). So the ratchet measures the *demo*, not the *eval* (against mandates #10/#13). The dual-engine cost-trace harness (PR #455), running each query on native AND DataFusion, proved this lets wrong results pass: `document` `d04/d08/d10` return **0 rows** (JSON-path SELECT projection silently empty) while `count(*)` shows the table non-empty; `g04` returns 16 rows native vs 32 DataFusion; `d06` 0 vs 1. *Decision:* a query counts only when verified correct by (1) **differential cross-engine equivalence** (native ≡ DataFusion after canonicalization — disagreement fails CI; near-free, no hand-grading) and/or (2) **anchored expected answers** (required for one-engine-only queries; catches "both wrong the same way"). Ratchet counts *accurate* queries, not merely executed. External Postgres/DuckDB oracle deferred. Phased rollout, one suite per PR (document first — pins the known JSON bug), each filing the bugs it uncovers. Tracked: TD-182

| link:ADR-042-canonical-segment-statistics-contract.adoc[ADR-042]
| Canonical SegmentStatistics Contract — format-uniform stats across ProximaBlocks + PAX, aggregated not re-derived
| Proposed
| Fulfills ADR-037 Decision 1 ("measured once at the write boundary, read many") with a *real* aggregate-not-rederive path: define a format-neutral, **mergeable** per-segment `SegmentStatistics` view (per-column HLL distinct + t-digest quantiles + typed min/max + null/value counts; segment-level counts + freshness watermark) that **both** ProximaBlocks and PAX produce at flush, and have the registry/compaction **merge** it format-agnostically via the `SegmentFormat::detect` seam (no rescan) — replacing the TD-174 per-record re-observe shortcut. Load-bearing structural move: relocate the mergeable sketches to a foundation crate so the PAX producer (`proximadb-block-format`) and the main-crate aggregator share one impl. Notes the PAX constraint (user fields live in opaque `PROPS` msgpack → per-user-field stats need the TD-162a typed `user_columns` stripe) and a mixed-read-safe P1–P5 rollout. The single block-stats spine behind TD-177/178/179. Tracked: TD-180

| link:ADR-043-unified-relational-expression-evaluation.adoc[ADR-043]
| Unified relational expression evaluation — one cast lowering, one function definition per binding, fail-loud predicate contract
| Accepted
| TD-183 (document JSON-path) had to be "fixed" in four code paths because predicate/scalar evaluation is fragmented: **4 disagreeing cast mechanisms** (translator string-strip with a hard-coded suffix list that forgot `::int` and over-strips `::boolean`; a frontend `lower_expr` with **no `Cast` arm**; a partial native `cast_value`; a fuller-but-unreachable DataFusion matrix), **3 function "registries"** (canonical `builtins()`, an empty `NoFunctions` at the pushdown boundary, and JSON funcs defined **only** as DataFusion UDFs so native lacks them), and **2 error contracts** (operators propagate via `?`, but the storage-pushdown predicate is a `Fn(&[ProximaValue]) -> bool` closure that coerces eval errors to `false` → **silently drops rows** = a wrong answer that looks like success). *Decision:* three normative invariants — (1) **never silently drop on eval error** (type the pushdown closure `-> Result<bool, ExprError>`, propagate like `FilterExec`); (2) **one cast lowering owned by the typed frontend** (add the `Cast` arm, delete the translator strip list, fill `cast_value` gaps); (3) **one function definition per binding** (move JSON kernels into `proximadb-functions`; F2 auto-binds DataFusion; native gets them free; engine-only scalars prohibited except listed Arrow specials). SOLID for an OLTP+OLAP hybrid: fail-loud + single-definition let an engine that can't serve a query escalate (ADR-039) instead of returning a plausible wrong answer. Surfaced by the ADR-040 accuracy harness. Tracked: TD-186
| link:ADR-044-stable-symbol-oid-correlated-cpg.adoc[ADR-044]
| Stable, line-independent symbol object_id — the correlated-CPG join key
| Proposed
| Cross-product audit 2026-06-28: the symbol `oid` is minted **3 different ways** across proximaDB / victor-codegraph / anvaiops and all embed the line number in identity, so a line move churns a symbol's vector + node + row — breaking fuse-on-shared-oid and forcing full re-index. *Decision:* one canonical, **line-independent** symbol oid minted once in `victor-codegraph`'s adapter and consumed verbatim by the SDK, connector, and substrate; identity = structural coordinates (`repo‖language‖module_path‖FQN‖overload-discriminator`), with line/col and body-hash as metadata, not identity. Extends ADR-031's tenet (stable id, mutable coordinate is metadata) to the symbol layer ADR-031 explicitly excluded. Mixed-read-safe, default-OFF. Tracked: TD-CG

| link:ADR-045-pax-durable-core-vs-serving-metadata.adoc[ADR-045]
| PAX Durable-Open Core vs Rebuildable Serving Metadata
| Proposed
| Stratify the PAX segment into a **durable open core** (stripes + MVCC; ships 1:1 to Iceberg) vs a **rebuildable serving-metadata layer** (row-dir, ColumnMeta, bloom, deletion vector, zone summaries). The ProximaDB-native form of Lakebase's "separate durability from visibility" — needs no transcode seam because PAX already co-locates row-directory + stripes; co-location of filter+vector stripes is inviolable (the ANN cascade must keep working with no extra GET). Enables LTAP single-copy export + fast cold-start (regenerate serving metadata without re-reading vectors). Grounded in Aurora "log is the database" (SIGMOD'17), PAX page-internal orthogonality (VLDB'01), Iceberg 3-FS-op immutable files. Refines ADR-010 Layer-2. Tracked: TD-LTAP-1

| link:ADR-046-lsn-coherent-merge-read.adoc[ADR-046]
| LSN-Coherent Single-Copy Analytics Read (LTAP without CDC)
| Proposed
| Analytical read obtains the WAL LSN, reads the materialized Parquet/VIPER arm as-of-LSN, then merges the un-materialized WAL-tail delta (MVCC-visible) — a consistent fresh read from ONE governed copy with zero tx impact. The ProximaDB-native LTAP: unify at the storage layer (one WAL authority + one durable core) with specialized compute (ADR-039). Research found Lakebase's *shipping* analytics freshness (CDF) is actually CDC (a second ~15s-synced copy), so a true single-copy LSN-merge is genuinely better. Reuses ADR-020/033/039 + the existing `search_unflushed` merge. Small-table skip. Tracked: TD-LTAP-2

| link:ADR-050-statistics-fed-datafusion-route-selection.adoc[ADR-050]
| Statistics-fed cost-based routing into the DataFusion OLAP arm
| Proposed
| Activates ADR-039's *declared-but-inert* DataFusion arm and makes its planning cost-based. Two moves: (1) `ComputeScheduler::route_select` stops hardcoding `Native(Volcano)` and routes analytical scan/join/aggregate over Parquet-backed tables to `DataFusionLocal` above a cost crossover (`SET compute_engine` override; strong-freshness stays Volcano) — realizes TD-115; (2) `schema_inference` feeds real DataFusion `ColumnStatistics` by projecting the ADR-042 mergeable `SegmentStatistics` (min/max/distinct/null) instead of `new_unknown()` — cost-based planning with **zero new scan**, the ADR-042 spine's second consumer. Stats are freshness-bounded hints (ADR-038) and calibrated by observed cost (ADR-030/034); the swap invariant (ADR-039) is preserved. **Explicitly disclaims the typed-vector-search scan cost** (#634 → ADR-011/028 + TD-064/069/096), which never enters DataFusion. Tracked: TD-115, TD-DFR-1
|===

NOTE: ADR-010 through ADR-013 and ADR-048/ADR-049 exist in this directory but
are not yet indexed in the table above; that catalogue gap is separate cleanup work.

== Relationship to Design Resolution Doc

These ADRs formalize and extend the decisions in `docs/_internal/roadmap/implementation/active/E1_E3_OPEN_TICKET_DESIGN_RESOLUTION_2026_04_01.adoc`. The design resolution doc remains the authoritative source for E1-E3 sequencing and sprint board semantics. ADRs govern the implementation contracts.

== Issue Coverage

Every open issue (#25-#61) is governed by at least one ADR:

* **E1 (Capability Registry)**: ADR-001
* **E2 (Filtered ANN)**: ADR-002
* **E3 (Multi-Model Plan)**: ADR-003, ADR-004
* **Gap Closure**: ADR-005 (document/graph), ADR-006 (infrastructure)
* **Production Hardening**: ADR-001 (#25, #26, #30), ADR-006 (#27, #28, #55, #60, #61)
