= Architecture Decision Records
:toc: left
:icons: font
:last-updated: 2026-04-02

[.lead]
This directory contains Architecture Decision Records (ADRs) governing ProximaDB's implementation across all open issues (#25-#61). Each ADR defines canonical contracts, interfaces, and file plans that implementation must follow.

== ADR Index

[cols="1,4,2,2"]
|===
| ADR | Title | Status | Governs

| link:ADR-001-capability-registry.adoc[ADR-001]
| Capability Registry Architecture
| Accepted
| SB-01→07, #25, #26, #30

| link:ADR-002-normalized-filter-candidate-contract.adoc[ADR-002]
| Normalized Filter and Candidate Contract
| Accepted
| SB-08→13, #57

| link:ADR-003-multimodel-plan-contract.adoc[ADR-003]
| MultiModelPlan v1 Contract
| Accepted
| SB-14→20, #29

| link:ADR-004-unified-explain-contract.adoc[ADR-004]
| Unified Explain Contract
| Accepted
| SB-17, SB-20

| link:ADR-005-document-graph-query-extensions.adoc[ADR-005]
| Document and Graph Query Extension Architecture
| Accepted
| #51→53, #58, #59

| link:ADR-006-infrastructure-platform-decisions.adoc[ADR-006]
| Infrastructure and Platform Decisions
| Accepted
| #55, #54, #60, #61, #28, #27, #56

| link:ADR-007-iceberg-rest-catalog-server.adoc[ADR-007]
| Iceberg REST Catalog Server in xCatalog
| Accepted
| Open-format catalog, Arrow Flight split planning

| link:ADR-008-oltp-catalog-backends.adoc[ADR-008]
| OLTP Catalog Backends in xCatalog
| Accepted
| PostgreSQL/MariaDB/SQLite metadata backends

| link:ADR-009-schema-modes-and-proximarecord-surfaces.adoc[ADR-009]
| Schema Modes and ProximaRecord-Centric API Surfaces
| Accepted
| REST/gRPC/Arrow/SQL/SDK schema-on-read and schema-on-write convergence

| link:ADR-014-vector-query-2-shape.adoc[ADR-014]
| Vector Query Two-Shape Boundary
| Accepted
| `VectorOpsPort` (proto-typed runtime port) vs `VectorQueryService` (Rust-typed orchestration contract) — deliberate, not duplication

| link:ADR-015-port-impl-location.adoc[ADR-015]
| Port Impl Location — Bare Concrete Service vs gRPC Wrapper
| Accepted
| `impl XxxPort` lives on the bare concrete service; the gRPC wrapper is a thin proto-conversion shim that delegates to the port

| link:ADR-016-port-user-context.adoc[ADR-016]
| `PortUserContext` — Boundary Type for `SecurityPort` Authentication
| Accepted
| Lossy projection of root-crate `UnifiedUserContext` so `SecurityPort::authenticate` can exist at the runtime boundary without dragging the typed permission graph into the runtime crate

| link:ADR-025-deletion-vectors-cold-path.adoc[ADR-025]
| Deletion Vectors for Cold-Path Deletes (Layered over MVCC)
| Proposed
| Cold-path delete mechanism (DV bitmap + immutable files + Iceberg snapshots); rejects redo/undo; retains MVCC for the hot delta

| link:ADR-026-pax-canonical-vector-path.adoc[ADR-026]
| PAX Stacked Quantization as the Canonical Vector Path (Retire Legacy Progressive Search)
| Proposed
| AXIS graph + PAX stacked quant (RaBitQ->SQ8->f32) is the one vector path; retire legacy progressive engines, fold their designs into PAX; PAX is the cross-modality internal columnar substrate (Parquet at the interop seam); configurable geometry + user/adaptive/learned tuning hierarchy. Tracked: TD-150..157

| link:ADR-027-unified-storage-and-metering-substrate.adoc[ADR-027]
| Unified Storage + Metering Substrate (PAX-as-one-internal-format, two-class trace, coalesced write-path, dual-sink metering)
| Proposed
| PAX is the single internal format (LSM writes it for all modalities; one reader/one SQL lowering/one I/O-trace; Parquet at the seam). Trace = two classes: billing (always-on, full-fidelity) vs perf/geometry (compile-time gateable). Write-path reuses SST/LSM (threshold-flush + compaction; Aurora/Neon/Turso-grounded; no per-query object writes). Metering dual-sunk: durable per-tenant in object store + external OTLP. Tracked: TD-158, TD-160..162

| link:ADR-028-index-policy-routing.adoc[ADR-028]
| Per-Collection Index-Policy Routing + Cost-Derived Exact-vs-ANN Auto-Default
| Proposed
| Paved road + escape hatch for cold/warm search routing: precedence per-query `SearchMode` > collection `IndexPolicy` (typed proto knob: mode/rehydrate/byte_budget/nprobe, fail-closed validity matrix) > cost-derived auto-default (byte budget = `f(storage_class)`, not a vector count). Cold HNSW is dominated by exact up to the byte budget; quantization-only collections auto-route to HELIX (env-gated default-OFF); fixes IVF Defect B (persist-after-insert) so the cheap cold-load path returns vectors. Tracked: TD-165; cost-model trace consumption TD-158/TD-159

| link:ADR-029-shared-codegraph-chunker-package.adoc[ADR-029]
| Shared Code→CPG Chunker Package (`victor-codegraph`) — one chunker, three consumers, zero duplication
| Proposed
| The tree-sitter code→symbol+relation chunker is duplicated across ProximaDB SDK (`code.py`) and Victor (`victor-coding`); extract it into one neutral Apache-2.0 PyPI package (`victor-codegraph`, deps = `victor-contracts` + tree-sitter) consumed by Victor (owner), ProximaDB SDK (optional extra, retires `code.py` to stay generic), and anvaiops (SaaS code-graph vertical, ADR-0017 F3). Adopts LlamaIndex `CodeSplitter` size-discipline + Victor fallback chain + ProximaDB taxonomy; emits the substrate-keystone `ProximaRecord` shape. Boundary-safe (policy-free). Tracked: TD-CG1..CG3

| link:ADR-030-unified-hotpath-trace-meter-seam.adoc[ADR-030]
| Unified Hot-Path Trace+Meter Emission Seam (one I/O boundary, two structurally-separate sinks)
| Proposed
| Implements ADR-027's two-class trace via the per-query `tokio::task_local!` `IO_TRACE` accumulator: it is fed at the `read_range` GET boundary (no tenant needed), drained ONCE at scope-close (where tenant+engine are known), and fans the same snapshot to THREE observers — route-cost + cache (gateable perf, → `avg_get_size` for the cost model + adaptive engine, TD-158) and a NEW billing observer (always-on) that closes the verified **KRU** read-meter (`record_task_execution_time`) gap. **KSU** (storage accrual) is explicitly scoped out as the sibling write-path/periodic gap. Same measured snapshot ⇒ cost-model bytes == billing bytes (no divergence). Honors ADR-027 non-entanglement (separate sinks; billing never gated; CI guard) and the open-core boundary (OSS emits neutral units; AnvaiOps prices them). Closes the cost model's INPUT side only — routing-loop closure is a separate ADR. Tracked: TD-158 + KRU; prereq TD-160

| link:ADR-032-durable-tenant-prefixed-object-store-catalog.adoc[ADR-032]
| Durable, Tenant-Prefixed Object-Store Catalog (the catalog as the serverless shared truth; structural isolation, not a per-query predicate)
| Proposed
| An audit found the catalog is not yet durable on object storage (cloud URLs cached to `/tmp`; now fail-closed via PR #410), its metadata bypasses the `DrPathBuilder` tenant prefix (the one non-isolated surface), and the service-layer per-query tenant predicate is inert in production (no `with_tenant_manager` caller) and is the wrong mechanism. Decision: persist `NativeCatalog` through the existing `FilesystemFactory` (cloud backends already write — a wiring job, not a new client), address catalog assets through `DrPathBuilder` (`data/{tenant}/{ns}/…`, Phase-5 `accounts/{account}/…`), record tenant via `create_namespace_for_tenant`, and move isolation to the network edge (explicit fail-closed `DeploymentMode`) + the path — retiring the inert predicate. Mixed-read-safe, marker-detected, default-OFF, migration tool. Operator catalog stays in AnvaiOps; object-store IAM is future work. Tracked: TD-CAT-1/2/3, TD-113, TD-119, TD-164

| link:ADR-033-primary-storage-format-strategy.adoc[ADR-033]
| Primary Storage Format Strategy — own the vertical (PAX) where it pays, keep Parquet at the interop seam
| Proposed
| An adversarially-verified survey (22 sources; Postgres/InnoDB/Oracle-HCC/Aurora/Neon + ClickHouse/Snowflake/BigQuery/Delta/Iceberg/Lance/Turbopuffer) settles the "why Parquet, not our own format" question: **no serious engine uses Parquet as primary** — all own a co-designed format and expose open formats only at the interop edge. But the win is **workload-specific**: custom formats beat Parquet **100×+ on random-access / vector / selective** queries and **< 2× on sequential OLAP scans**, and Parquet-as-primary is genuinely right for lakehouse/Iceberg interop. Decision: the **dual-path is vindicated** — keep Parquet as the relational-scan + interop primary (not demoted to export), and spend the PAX budget on the vector/random-access path via **object-storage round-trip minimization** (Turbopuffer's ≤3-round-trip budget; Parquet's footer amplification is the measured anti-pattern) and **format⇄index co-design** (the Lance pattern). Recasts **TD-162a-2**: its worth is unifying relational+vector storage, enabling the Native cost-router arm, and zone-map/projection pruning on selective queries — *not* chasing scan-throughput parity (already < 2×). Adds a **first-principles cost spine** (latency = dependency-DEPTH × RTT; **depth ≫ width ≫ bytes ≫ CPU**) and an **egress correction** (egress = the customer result only; object-store reads are internal; quantization is a residency/round-trip lever, not egress). Tracked: TD-162a-2

| link:ADR-034-objectstore-access-path-codesign.adoc[ADR-034]
| Object-Store Access-Path Co-Design — minimize dependency depth
| Proposed
| Applies ADR-033's cost spine to the *access path*. A map of the real PAX format found the layout sound but the read path reproduces Parquet's failure internally: a pruned ranged read is **`1+3K` serial dependent GETs** (segment-index → per-block footer → per-block col-meta → per-block body), and graph traversal pointer-chases cold object storage (no RAM topology). Records 8 derived principles (depth-first; predictive/cached metadata; large-outside/fine-inside; codes-only residency; **graph = the adversarial workload → topology in RAM + batched frontier**; criticality tiering; round-trip budget as a planner constraint; LSM with free delta detection) and six ROI-ordered gap-closure decisions: **TD-167** read-path depth collapse (zone-map summary in `SegmentIndex` + `read_ranges` body-batching → depth ~1-2), **TD-168** graph topology RAM index, **TD-169** segment code region, **TD-170** round-trip budget, **TD-171** criticality tiering, and TD-162a-2 (typed stripes, last — < 2× scan). All additive, mixed-read-safe, GET-count-harness + recall-ratchet gated. Tracked: TD-167..171, TD-162a-2

| link:ADR-035-per-tenant-system-catalog-cache.adoc[ADR-035]
| Per-Tenant System Catalog — two-group object layout + 3-tier read cache + provisioning lifecycle
| Proposed
| Resolving a collection's metadata costs **1+N+M object-store GETs** on a cold cache (`list_namespaces` + per-ns `list_tables` + per-table `get_table`), re-paid after every pod restart — the wrong bill on the hot path. Decision: (D1) split the per-tenant system catalog into two object-store groups — **METADATA** (read-heavy registry, keyed by global `u64` object_id since names collide across tenants: `_syscat/objects/{oid}.json` + a `name→oid` index) and **OPERATIONAL** (write-heavy append: `_metering`/`_trace`/`_billing`, TD-161). (D2) a **3-tier read path** — hot in-memory `TenantCache` (≤1 MB/tenant, 5-min TTL, corpus-version-stamped) → warm local disk (OS page cache) → canonical object store. (D3) a **provisioning lifecycle** — signup pre-creates a bare-minimum skeleton, user DDL appends system-managed rows. (D4) hydration triggers — pgwire session-ready warm + lazy warm-on-first-miss for the stateless edges. Composes existing `TenantCache`/`CorpusVersionRegistry`/`CacheInvalidationCoordinator`/`IdAllocator`. Full read/write/provision/pod-start/invalidation branch table. Mixed-read-safe; default-on read accelerator first. Tracked: TD-SC-1..5; depends on ADR-032/TD-CAT-2.
|===

NOTE: ADR-010 through ADR-013 exist in this directory but are not yet
indexed in the table above; that catalogue gap is separate cleanup work.

== Relationship to Design Resolution Doc

These ADRs formalize and extend the decisions in `docs/_internal/roadmap/implementation/active/E1_E3_OPEN_TICKET_DESIGN_RESOLUTION_2026_04_01.adoc`. The design resolution doc remains the authoritative source for E1-E3 sequencing and sprint board semantics. ADRs govern the implementation contracts.

== Issue Coverage

Every open issue (#25-#61) is governed by at least one ADR:

* **E1 (Capability Registry)**: ADR-001
* **E2 (Filtered ANN)**: ADR-002
* **E3 (Multi-Model Plan)**: ADR-003, ADR-004
* **Gap Closure**: ADR-005 (document/graph), ADR-006 (infrastructure)
* **Production Hardening**: ADR-001 (#25, #26, #30), ADR-006 (#27, #28, #55, #60, #61)
