= Module Component LLD Map
:toc:
:toclevels: 3

Status: active baseline +
Date: 2026-05-22 +
Companion: `ARCHITECTURE_ATLAS_2026_05_22.adoc`

This document decomposes the ProximaDB workspace into LLD-level component groups. It is not a
replacement for ADRs or active implementation trackers; it is the connectedness map used to decide
where a change belongs.

== Legend

* Authority: whether the module owns durable truth, derived state, protocol adaptation, or control
  metadata.
* Inputs: primary upstream contracts.
* Outputs: downstream contracts or side effects.
* Consolidation gate: what must be checked before expanding this module.

== LLD Component Diagrams

=== Foundation To Authority

[source,mermaid]
----
flowchart TB
  Kernel["proximadb-kernel\nerrors, traits, base contracts"]
  DataModel["proximadb-data-model\ntyped shared model"]
  Proto["proximadb-proto\ngenerated wire artifacts"]
  Records["proximadb-records\nProximaRecord / ProximaValue"]
  Filters["proximadb-filter-expression\nnormalized predicates"]
  Catalog["proximadb-catalog\nxCatalog metadata and route knobs"]
  StorageCommon["proximadb-storage-common\nengine contracts"]
  BlockFormat["proximadb-block-format\nPAX/block layout"]

  Kernel --> DataModel
  DataModel --> Records
  Proto --> Records
  Filters --> Catalog
  Records --> Catalog
  Records --> StorageCommon
  StorageCommon --> BlockFormat
----

=== Protocol To Execution

[source,mermaid]
----
flowchart LR
  SDK["SDKs and embedded bindings"]
  API["proximadb-api\nREST / gRPC / pgwire / Flight"]
  Runtime["proximadb-runtime\ncomposition and resources"]
  Handlers["src/api_handlers\nrequest orchestration"]
  Services["src/services\nDDL / DML / record store / operations"]
  Planner["query crates + src/query\nplans, routes, EXPLAIN"]
  Catalog["xCatalog"]
  WAL["WAL / log / manifest"]

  SDK --> API
  API --> Runtime
  Runtime --> Handlers
  Handlers --> Planner
  Planner --> Catalog
  Planner --> Services
  Services --> WAL
----

=== Storage And Modality Projections

[source,mermaid]
----
flowchart TB
  RecordStore["record store\ncanonical read/write bridge"]
  WAL["WAL / current-state / manifest"]
  Blocks["PAX/block format"]
  Engines["src/storage engines\nSST / VIPER / NOVA / HELIX / RAPTOR / SWIFT"]
  Vector["vector modality\nANN/index projections"]
  Document["document modality\nJSON/path projections"]
  Graph["graph modality\ntopology projections"]
  Observability["observability modality\nlogs/metrics/traces rollups"]
  Queue["queue modality\nmessage durability"]

  RecordStore --> WAL
  WAL --> Blocks
  Blocks --> Engines
  Engines --> Vector
  Engines --> Document
  Engines --> Graph
  Engines --> Observability
  Queue --> RecordStore
----

=== SaaS Control Plane Boundary

[source,mermaid]
----
flowchart LR
  Tenant["tenant/org/project"]
  Auth["auth, RLS, policy"]
  Budget["budget guard and metering"]
  Audit["audit and request context"]
  API["core API adapters"]
  Catalog["xCatalog policy boundary"]
  Explain["EXPLAIN / trace metadata"]

  Tenant --> Auth
  Auth --> Budget
  Budget --> API
  API --> Catalog
  Catalog --> Explain
  API --> Audit
  Audit --> Budget
----

== Foundation Layer

[cols="1,2,2,2,2",options="header"]
|===
| Module | Components | Authority | Inputs | Outputs / Gates

| `proximadb-kernel`
| Error types, base traits, UUID/time/core utility contracts.
| Foundational API semantics; no product-specific state.
| Rust std, async/runtime-neutral traits.
| High PageRank. Keep minimal; no modality or platform dependencies.

| `proximadb-data-model`
| Core typed model structures shared by records, catalog, modalities, query.
| Canonical data semantics.
| Kernel contracts.
| Any breaking change requires SDK, query, record, catalog, and storage compatibility review.

| `proximadb-proto`
| Generated protobuf Rust bindings for v1/v2/cluster contracts.
| Wire contract artifact, generated only.
| `proto/`.
| Do not hand-edit. Regenerate stubs and update OpenAPI/SDKs together.

| `proximadb-records`
| `ProximaRecord`, `ProximaType`, `ProximaValue`, proto conversion bridges.
| Canonical durable record/value shape.
| Data model and proto.
| Legacy v1 DTOs remain edge adapters only.

| `proximadb-filter-expression`
| Predicate ASTs, normalized filters, search/query params.
| Shared predicate contract.
| Query parameters and protocol inputs.
| Keep query-algebra neutral; engines consume normalized form.

| Type crates
| Distance, encoding, compression, quantization type contracts.
| Stable enum/config surfaces.
| Engine/query configs.
| Prefer additive variants and explicit compatibility notes.

| Config/hardware/operator
| Runtime config, hardware capabilities, pipeline operator abstraction.
| Deployment-time and execution capability metadata.
| Environment/config files, CPU/GPU probes.
| No protocol-specific behavior here.
|===

== Control And Authority Layer

[cols="1,2,2,2,2",options="header"]
|===
| Module | Components | Authority | Inputs | Outputs / Gates

| `proximadb-catalog`
| Table schemas, collection metadata, statistics, system columns, catalog cache, relational profiles.
| xCatalog control metadata and route knobs.
| DDL, runtime registration, external catalog imports.
| Planner metadata: `authority_mode`, `workload_profile`, `storage_specialization`,
  `freshness_sla`, `compute_route`, policy boundary.

| Root `src/catalog`
| Budget guard, OLTP catalog adapters, tenant tier, compatibility facades.
| Transitional integration shell around catalog capabilities.
| Platform/runtime config and services.
| Should migrate stable contracts into `proximadb-catalog`; root stays adapter/composition.
|===

== Protocol And Platform Layer

[cols="1,2,2,2,2",options="header"]
|===
| Module | Components | Authority | Inputs | Outputs / Gates

| `proximadb-api`
| REST v1, gRPC v1, Arrow Flight service, pgwire session/server scaffolding.
| Protocol adaptation only.
| HTTP/gRPC/Flight/SQL wire payloads.
| Canonical request structs and typed errors. No independent storage writes.

| `proximadb-runtime`
| Runtime composition, resource wiring, handler registry.
| Process composition, not data authority.
| Config, services, platform resources.
| Wires modules without introducing hidden alternate routes.

| Root `src/network`
| Existing REST v1/v2/v3, pgwire, Arrow IPC, auth middleware, multi-server wiring.
| Transitional protocol implementation.
| Wire requests, tenant/auth context.
| Lower into handlers/services and shared write/query intent. Continue extraction into
  `proximadb-api` where stable.

| Root `src/api_handlers`
| Unified request handlers, SQL execution dispatch, record batch facade.
| Request orchestration; not durable authority.
| Protocol-normalized requests and tenant context.
| Calls services after schema, policy, and write-lane checks.
|===

== Query Layer

[cols="1,2,2,2,2",options="header"]
|===
| Module | Components | Authority | Inputs | Outputs / Gates

| `proximadb-query`
| Cross-model query runtime, planner integration, execution contracts.
| Query execution semantics.
| Modality query contracts, catalog, record contracts.
| Routed execution plans and result streams.

| `proximadb-multimodel-query`
| Vector/document/graph/observability join contract.
| Algebra-level multimodel semantics.
| Modality query crates.
| Keep independent of concrete storage engines.

| `proximadb-multimodel-plan`
| Plan model, filter expression integration, pipeline operators.
| Planner contract shape.
| Filter expressions and catalog metadata.
| EXPLAIN-visible plan fragments.

| Query modality crates
| `proximadb-vector-query`, `proximadb-document-query`, `proximadb-graph-query`,
  `proximadb-observability-query`.
| Modality-specific query contracts.
| User query clauses and protocol adapters.
| Shared query runtime inputs; avoid duplicate parser semantics.

| Query helper crates
| `proximadb-query-filter`, `proximadb-query-clauses`, `proximadb-query-fusion`,
  `proximadb-uql`, `proximadb-query-capability`.
| Filter/clauses/fusion/capability contracts.
| Parser/planner/service inputs.
| Capability snapshots and normalized filters.

| Root `src/query`
| SQL frontend, federated optimizer, cache, explain, repair, table write plan.
| Transitional query implementation and active experimentation.
| Protocols, services, catalog.
| Gradually move stable contracts to crates; keep experimental routes flagged and documented.
|===

== Service Layer

[cols="1,2,2,2,2",options="header"]
|===
| Module | Components | Authority | Inputs | Outputs / Gates

| `src/services/ddl`
| DDL statements and schema/table operations.
| Catalog mutation authority through xCatalog.
| SQL/REST/gRPC DDL requests.
| Catalog updates, schema validation outcomes.

| `src/services/dml`
| Insert/update/delete/upsert, row count/stat bumps, table write execution.
| Mutation orchestration into canonical write authority.
| DML statements, write intents, catalog profiles.
| WAL/current-state writes plus catalog stats. No protocol-specific durability shortcut.

| `src/services/record_store`
| Direct WAL table record store, memtable, scan/get/mutation requests.
| Canonical table record IO bridge.
| Table mutations and scans.
| WAL append/current-state visibility.

| `src/services/operations`
| Legacy vector operations service and compatibility paths.
| Compatibility facade only for legacy vector flows.
| v1/v2 record requests.
| Must route through canonical records for new durable behavior.

| Queue/embedding services
| Embedding drainer, queue-facing ingest.
| Async ingest orchestration; source data remains authoritative elsewhere until committed.
| Queue messages and ingest requests.
| Idempotency and backpressure must be explicit.
|===

== Storage Layer

[cols="1,2,2,2,2",options="header"]
|===
| Module | Components | Authority | Inputs | Outputs / Gates

| `proximadb-block-format`
| PAX/block layout, codec integration, record block format.
| Durable block layout contract.
| Records, codec strategy, storage metadata.
| Must remain open-format aware and backwards compatible.

| `proximadb-storage-common`
| Common storage interfaces and block-format adapters.
| Shared engine contract.
| Block format, records, runtime common.
| Storage engines consume, not fork, this contract.

| Root `src/storage`
| Engine implementations, WAL/recovery, cache, persistence, open-format paths.
| Transitional storage runtime.
| Services, record store, catalog routes.
| New engine behavior must declare authority mode, policy boundary, repair source, and maturity.

| Codecs/compression
| `proximadb-codec`, `proximadb-compression`, PAX codec LLD.
| Encoding/compression policy and implementations.
| PAX/record blocks, graph/json/vector stripes.
| EXPLAIN and xCatalog must expose codec decisions when user-visible.
|===

== Modality Layer

[cols="1,2,2,2,2",options="header"]
|===
| Module | Components | Authority | Inputs | Outputs / Gates

| `proximadb-vector`
| Vector record/query/index contracts and runtime helpers.
| Vector modality semantics, not independent durability.
| Records, vector query, filter contracts.
| ANN access methods and vector results.

| `proximadb-document`
| Document runtime, document query bridge, record projection.
| Document semantics.
| ProximaRecord and document query contract.
| JSON/document indexes are projections unless cataloged otherwise.

| `proximadb-graph`
| Graph core, graph query contracts, topology semantics.
| Graph modality semantics.
| ProximaRecord, graph query, graph topology projections.
| Graph topology durability must remain stacked on canonical records.

| `proximadb-observability`
| Metrics/traces/log modality, query filters, proto bridge.
| Observability modality semantics.
| Telemetry records and query contracts.
| Retention, compression, and external export are policy-driven.

| `proximadb-embedding`
| Embedding runtime helpers and queued ingest integration.
| Derived embedding materialization.
| Source records, model config, queue messages.
| Must preserve source provenance and idempotent replay.

| `proximadb-queue`
| Memory tier, disk tier, group commit, object archive scaffold, producer/consumer API.
| Queue message durability only; not table-record authority.
| Producer messages and topic config.
| Backpressure, sync mode, fsync, archive, and consumer leases must be observable.
|===

== SaaS And Operations Modules

[cols="1,2,2,2,2",options="header"]
|===
| Module | Components | Authority | Inputs | Outputs / Gates

| `src/security`, `proximadb-security`
| Auth service, JWT, RBAC/policy helpers.
| Access-control decisions.
| Tenant identity, tokens, policy config.
| Must execute before query/write planning.

| `src/network/middleware`
| Tenant context, auth middleware, request metadata.
| Request-scoped policy context.
| Protocol requests.
| Emits context consumed by handlers/services.

| `src/observability`, `proximadb-telemetry`
| Metering events, request context, traces, metrics, alerting.
| Operational evidence, not user data authority.
| Runtime/service events.
| Used by SaaS billing, SLO, audit, and debugging.

| `src/cluster`
| Cluster coordination and RPC.
| Distributed coordination metadata.
| Runtime node state and membership.
| Must not bypass catalog/WAL authority for data changes.

| `src/cdc`
| Change-data-capture connectors.
| Publication/subscription state.
| WAL/catalog/storage events.
| External sinks/sources require explicit authority mode.

| `deploy/`, `deployment/`, `helm/`, `k8s/`, `packaging/`
| Deployment and packaging assets.
| Operational rollout, not runtime semantics.
| Config and release artifacts.
| Keep examples secret-free and supported-surface accurate.
|===

== Clients And Bindings

[cols="1,2,2,2,2",options="header"]
|===
| Module | Components | Authority | Inputs | Outputs / Gates

| `clients/python`
| Unified Python SDK, REST/gRPC adapters, models, chunking, repository helpers.
| Client-side API ergonomics.
| OpenAPI/proto contracts.
| Must track supported surface and generated stubs.

| `clients/rust`
| Rust SDK crate.
| Client API.
| REST/gRPC/OpenAPI/proto contracts.
| Keep aligned with OpenAPI and server semantics.

| `clients/go`, TypeScript/Node, Java embedded, Python embedded
| SDKs and embedded/native bindings.
| Client API and embedded runtime access.
| Protocol contracts and FFI boundaries.
| Generated or bridge code should be clearly marked.

| `ui`
| React/TypeScript UI.
| Operator/user UX only.
| REST/API contracts.
| Should not encode hidden server behavior.
|===

== High-Risk Coupling Flags

* Foundation crates with high PageRank: `proximadb-kernel`, `proximadb-data-model`,
  `proximadb-proto`, `proximadb-query-filter`, and `proximadb-records`.
* Root crate still depends on 42 workspace crates. Treat it as integration shell and continue
  migrating stable ownership downward.
* Human-authored hotspots from Victor: `src/network/postgres/protocol.rs`,
  `src/query/federated/execution/mod.rs`, `src/services/dml/mod.rs`,
  `src/query/federated/optimizer/mod.rs`, `src/embedded/mod.rs`,
  `src/api_handlers/request_handlers.rs`, and selected large Python SDK files.
* Generated proto files dominate raw node counts and should be excluded from manual
  complexity refactor queues.
* Victor cross-file call centrality is not currently available; regenerate or fix Victor's
  cross-file resolution before using call PageRank as a release gate.

== Per-Change LLD Checklist

Every feature-level implementation should answer:

* Which module owns the durable or derived authority?
* Which protocol adapters lower into it?
* Which xCatalog knobs or policy boundaries does it consult?
* Which WAL/log/manifest behavior makes recovery possible?
* Which projection/index/cache paths are derived and rebuildable?
* Which EXPLAIN or trace metadata proves the route?
* Which supported-surface status applies: supported, beta, experimental, internal, or archived?

== Recursive src Module Decomposition

Companion to the crate/layer decomposition above. This part recurses into the `src/` runtime tree
one subsystem at a time, down to the level where further decomposition stops adding architectural
signal. Every diagram node maps to a real file or directory verified on 2026-05-28; legends cite the
paths. System-level context, use-case, and deployment views live in
`ARCHITECTURE_ATLAS_2026_05_22.adoc`. Critical-path sequence/activity flows are at the end of this
part ("Critical-Path Sequence And Activity Diagrams").

=== Network Layer (src/network)

Legend: `MultiServer`=multi_server.rs, `ServerBuilder`=server_builder.rs,
`SharedServices`=shared_services.rs, `UnifiedQueryHandler`=multi_protocol_handler.rs,
REST=rest/, gRPC=grpc/, pgwire=postgres/, Arrow Flight=arrow_ipc/, multiplex=multiplex/,
middleware=middleware/.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  Client["Client applications"]
  subgraph Orch["Server orchestration"]
    MS["MultiServer (multi_server.rs)"]
    SB["ServerBuilder (server_builder.rs)"]
  end
  subgraph MultiPort["Protocol servers (multi-port mode)"]
    REST["RestServer :5678"]
    GRPC["GrpcServer :5679"]
    PG["PostgresServer :5433"]
    AF["ArrowFlightServer :5680"]
  end
  subgraph Unified["Unified-port mode (default)"]
    TCP["TCP mux :5678"]
    PRouter["ProtocolRouter (multiplex/protocol_multiplexer.rs)"]
    MServ["MultiplexService (multiplex/service.rs)"]
  end
  subgraph MW["Middleware (middleware/)"]
    AUTH["auth · rate_limit · cors · timeout · tls · tenant · request_id · backpressure"]
  end
  subgraph Shared["Shared business logic"]
    SS["SharedServices (shared_services.rs)"]
    UQH["UnifiedQueryHandler (multi_protocol_handler.rs)"]
  end
  Client --> MS --> SB
  SB --> REST & GRPC & PG & AF
  SB --> TCP --> PRouter --> MServ
  REST & GRPC & PG & AF & MServ --> MW
  MW --> SS --> UQH --> Svc["Service layer"]
----

==== REST routes (rest/v1 vs rest/v2)

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  REST["RestServer :5678 (rest/server.rs)"]
  subgraph V1["v1 (rest/v1/)"]
    H["handlers.rs collections/vectors"]
    E["entities.rs (SKS)"]
    G["graph.rs"]
    D["document.rs"]
    HYB["hybrid.rs (BM25+vector)"]
    R["rank.rs / rank_profile.rs"]
    O["observability.rs"]
    AQL["aql.rs"]
    NL["nl.rs"]
    MM["multimodal_query.rs"]
    PIN["pinning.rs / affinity.rs"]
    ICE["iceberg_rest_catalog.rs"]
  end
  subgraph V2["v2 (rest/v2/)"]
    C["collections.rs"]
    SC["schema.rs"]
    REC["records.rs (ProximaRecord ops)"]
    Q["query.rs"]
    DIS["discovery.rs"]
  end
  REST --> V1 --> AppState["Shared handlers (AppState)"]
  REST --> V2 --> AppState
----

==== gRPC services (grpc/)

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  GRPC["GrpcServer :5679 (grpc/mod.rs)"]
  subgraph V1["v1 services"]
    ES["entity_service.rs"]
    VS["hybrid_search_service.rs"]
    GS["graph_service.rs"]
    DS["document_service.rs"]
    OS["observability_service.rs"]
    SS2["streaming_service.rs"]
    RS["rank_service.rs"]
  end
  subgraph V2["v2 services (grpc/v2/)"]
    PRS["ProximaRecordServiceImpl"]
  end
  GRPC --> V1 --> SH["UnifiedHandlers"]
  GRPC --> V2 --> SH
----

==== pgwire (postgres/) and Arrow Flight (arrow_ipc/)

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  PG["PostgresServer :5433 (postgres/)"]
  PROTO["protocol.rs (v3 parser/encoder)"]
  SESSION["session.rs"]
  TRANS["translator.rs"]
  PIPE["relational_pipeline.rs (algebra→planner→executor)"]
  PG --> PROTO --> SESSION --> TRANS --> PIPE --> XC["xCatalog DDL/DML routing"]

  AF["ArrowFlightServer :5680 (arrow_ipc/)"]
  SVC["service.rs (DoGet/DoPut)"]
  CODEC["codec.rs / multimodal_codec.rs"]
  FE["file_export.rs (SST→Parquet/IPC, SstArrowCache)"]
  AF --> SVC --> CODEC
  SVC --> FE
----

=== API Handlers And Services (src/api_handlers, src/services)

Legend: `UnifiedHandlers`=api_handlers/request_handlers.rs (proto-first facade serving REST+gRPC);
services are injected as `Arc<...>`. Paths: collection/manager.rs, operations/vectors/, dml/mod.rs,
ddl/mod.rs, search/streaming.rs, discovery/{service,job,executor}.rs, events/log.rs,
schema/evolution.rs, snapshot/coordinator.rs, document/service.rs, graph/service.rs.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
classDiagram
  class UnifiedHandlers {
    -collection_service: Arc~CollectionService~
    -vector_operations_service: Arc~VectorOperationsService~
    -document_service: Arc~DocumentService~
    -observability_service: Arc~ObservabilityService~
    -event_log: Option~Arc~EventLogEngine~~
    -graph_collection_service: Arc~GraphCollectionService~
    -graph_operations_service: Arc~GraphOperationsService~
    -graph_execution_service: Arc~dyn GraphExecutionService~
    -metrics_query_service: Option~Arc~MetricsQueryService~~
    -query_adapter: RwLock~Option~Arc~QueryFacadeAdapter~~~
    -dml_service: RwLock~Option~Arc~DmlService~~~
    -precision_resolver: OnceLock~Arc~CanonicalPrecisionResolver~~
    -collection_id_cache: CollectionIdCache
  }
----

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  UH["UnifiedHandlers"]
  subgraph Coll["Collection & metadata"]
    CS["CollectionService (collection/manager.rs)"]
    GCS["GraphCollectionService"]
  end
  subgraph Data["Data operations"]
    VOS["VectorOperationsService (operations/vectors/)"]
    DOS["DocumentService (document/service.rs)"]
    CBWS["CatalogBulkWriteService (operations/catalog_bulk_write.rs)"]
  end
  subgraph Lang["SQL / query"]
    DML["DmlService (dml/mod.rs)"]
    DDL["DdlService (ddl/mod.rs)"]
    SES["SchemaEvolutionService (schema/evolution.rs)"]
    SSrch["StreamingSearchService (search/streaming.rs)"]
  end
  subgraph Graph["Graph"]
    GOS["GraphOperationsService (graph/service.rs)"]
    GES["GraphExecutionService (trait)"]
  end
  subgraph Lifecycle["Discovery & snapshot"]
    DSV["DiscoveryService (discovery/service.rs)"]
    SP["SnapshotPublishCoordinator (snapshot/coordinator.rs)"]
  end
  UH --> CS & GCS & VOS & DOS & DML & DDL & SSrch & GOS & DSV
  GOS --> GES
  VOS --> CBWS
  DDL --> SES
  DSV --> SP
----

==== DML row routing (services/dml)

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  DML["DmlService"]
  TRR["TableRecordStore (routing abstraction)"]
  DWR["DirectWalTableRecordStore"]
  CRTS["CatalogRoutingTableRecordStore"]
  DML --> TRR
  TRR --> DWR
  TRR --> CRTS
----

=== Query Layer (src/query)

Legend: sql_frontend/{parser,lowering}.rs, query_router.rs, query_optimizer.rs, rl_planner/
(bandit/action/reward/state/experience), federated/{parser,optimizer,execution}, unified/
(decomposition/fusion/reranking/executor/uql), aql/, nl/, facade/{adapter,strategies}, explain.rs.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph LR
  REST["REST/gRPC/pgwire/SDK"] --> FACADE["UnifiedQueryFacade (facade/)"]
  FACADE --> VEC["VectorSearchStrategy"]
  FACADE --> SQLSTRAT["SqlStrategy"]
  FACADE --> GRAPH["GraphStrategy"]
  FACADE --> DOC["DocumentStrategy"]
  FACADE --> COL["ColumnarStrategy"]
  FACADE --> OBS["ObservabilityStrategy"]
  VEC & SQLSTRAT & GRAPH & DOC & COL & OBS --> ADAPTER["QueryFacadeAdapter (facade/adapter.rs)"]
----

==== SQL → router → optimizer → RL planner → federated executor

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph LR
  SQL["Raw SQL"] --> P["SqlFrontendParser (sql_frontend/parser.rs)"]
  P --> L["Lowering (sql_frontend/lowering.rs)"]
  L --> ROUTER["UnifiedQueryRouter (query_router.rs)"]
  ROUTER --> PLAN["MultiModelPlan v1 (multimodal/)"]
  PLAN --> OPT["UnifiedQueryOptimizer (query_optimizer.rs)"]
  OPT --> RL["RLPlanner (rl_planner/)"]
  RL --> ACT["ExecutionAction (rl_planner/action.rs)"]
  ACT --> FED["FederatedExecutor (federated/execution/)"]
  FED --> RES["Arrow RecordBatch stream"]
----

==== RL planner (Thompson sampling)

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  Q["Query context"] --> ST["StateEncoder (state.rs)"]
  ST --> BANDIT["ContextualBandit (bandit.rs) — Thompson sampling"]
  AS["ActionSpace (action.rs)"] --> BANDIT
  BANDIT --> SEL["Selected action"]
  SEL --> EXE["Execute"]
  EXE --> M["Metrics: latency/recall/throughput"]
  M --> RW["RewardCalculator (reward.rs)"]
  ST --> EB["ExperienceBuffer (experience.rs)"]
  SEL --> EB
  RW --> EB
  EB --> BANDIT
----

==== Federated SQL extensions and unified multi-model fusion

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  EXT["SQL ext: VECTOR_SEARCH() GRAPH_QUERY() DOCUMENT_QUERY() LOGS() METRICS()"]
  EXT --> FP["FederatedParser (federated/parser.rs)"]
  FP --> CO["CrossModelOptimizer (federated/optimizer/)"]
  CO --> FE["FederatedExecutor"]
  UQL["UQL (unified/uql.rs)"] --> DEC["QueryDecomposer (unified/decomposition.rs)"]
  DEC --> VC["Vector"] & DC["Document"] & GC["Graph"] & OC["Observability"]
  VC & DC & GC & OC --> FUS["ResultFuser (unified/fusion.rs): RRF/union/weighted"]
  FUS --> LF["LearnedFusion (unified/learned_fusion.rs)"]
  LF --> RR["CrossModalReranker (unified/reranking.rs)"]
----

==== NL (AV-SQL) → AQL with audit trail

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph LR
  NLQ["NL query"] --> RW["AgentRewriter (nl/)"]
  RW --> VG["AgentViewGenerator (nl/)"]
  VG --> CMP["AgentComposer (nl/)"]
  CMP --> AQL["AQL (aql/mod.rs)"]
  AQL --> AEX["AqlExecutor (aql/executor.rs)"]
  AEX --> AUD["AuditTrail (aql/): AuditOp/Frame/Outcome"]
----

=== Graph Layer (src/graph)

Legend: engines/{orion,pulsar,quasar}, generic_traversal.rs, service.rs (+ service_*.rs),
adjacency_projection.rs, canonical/, query/{parser,planner,executor,operators}, rag/
(engine_impls.rs), hybrid/{semantic_traversal,ranking}.rs. ORION is the in-memory+WAL production
engine; PULSAR (distributed) and QUASAR (hybrid tiering) are experimental / feature-gated.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  subgraph Svc["Service"]
    OPS["GraphOperationsService (service.rs)"]
    FACT["GraphEngineFactory"]
  end
  subgraph Eng["Engines (engines/)"]
    ORION["ORION (in-memory CSR + WAL)"]
    PULSAR["PULSAR (distributed, experimental)"]
    QUASAR["QUASAR (hybrid tiering, experimental)"]
  end
  subgraph Proj["Topology projections"]
    CANON["Canonical ProximaRecord nodes/edges"]
    ADJ["AdjacencyProjection (adjacency_projection.rs)"]
    CSR["CSR offsets+targets"]
  end
  subgraph Rag["Graph RAG (rag/)"]
    NR["NodeRetriever"]
    NF["NodeFilter (token budget)"]
    SG["SubgraphBuilder (k-hop BFS)"]
  end
  subgraph Qy["Query (query/)"]
    PARSER["parser.rs (Cypher/SQL)"]
    PLANNER["planner.rs"]
    EXEC["executor.rs"]
  end
  OPS --> FACT --> ORION & PULSAR & QUASAR
  ORION --> ADJ --> CANON
  ORION --> CSR
  NR --> NF --> SG --> ORION
  PARSER --> PLANNER --> EXEC --> ORION
----

==== ORION internals

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  subgraph Store["State (orion/)"]
    N["Nodes DashMap"]
    E["Edges DashMap"]
    CSRo["CSR out/in projections (index.rs)"]
  end
  subgraph Alg["Algorithms (orion/algorithms/)"]
    TR["traversal.rs BFS/DFS"]
    PF["pathfinding.rs Dijkstra/A*"]
    CN["centrality.rs PageRank"]
    CM["community.rs"]
  end
  subgraph Persist["Persistence (orion/persistence.rs, disk_storage.rs)"]
    SNAP["Snapshots + compression"]
    WALW["UnifiedWAL writer"]
    REC["Recovery replay"]
  end
  N & E --> CSRo
  TR --> CSRo
  PF --> TR
  N & E --> SNAP --> WALW --> REC
----

=== Catalog (xCatalog) Layer (src/catalog + crates/control/proximadb-catalog)

Legend: canonical `Catalog` trait lives in `crates/control/proximadb-catalog/src/lib.rs`;
`src/catalog/traits` re-exports it (shim). Factory impls: native.rs, hive.rs, iceberg.rs, oltp.rs
(always-on); glue.rs/polaris.rs/unity.rs/delta.rs (feature-gated). Subsystems: internal/ (registry,
enforcement, information_schema), federation/, iceberg_rest_service.rs, partition_pruning.rs,
segment_registry.rs, corpus_version*.rs, tenant_tier.rs, recall_probe.rs, budget_guard.rs,
tier_transition.rs, tier_recommendation.rs.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  CatMgr["CatalogManager (catalog/mod.rs)"]
  Traits["traits shim → proximadb_catalog::Catalog"]
  CatMgr --> Traits -.-> Crate["crates/control/proximadb-catalog (lib.rs)"]
  CatMgr --> Native["native.rs"]
  CatMgr --> Hive["hive.rs"]
  CatMgr --> Iceberg["iceberg.rs"]
  CatMgr --> Oltp["oltp.rs"]
  CatMgr --> Glue["glue.rs (feature: aws)"]
  CatMgr --> Polaris["polaris.rs (feature: polaris-catalog)"]
  CatMgr --> Unity["unity.rs (feature: unity-catalog)"]
  CatMgr --> Delta["delta.rs (feature: delta-lake)"]
----

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  subgraph Internal["internal/ (multi-model registry)"]
    Reg["InternalSchemaRegistry (registry.rs)"]
    Enf["ConstraintEnforcer (enforcement.rs)"]
    Info["InformationSchema (information_schema.rs)"]
  end
  subgraph Fed["federation/"]
    FC["FederatedCatalog"]
    EC["ExternalCatalog trait → Iceberg/Delta/Hudi/Hive/Glue/Unity/Polaris/LanceDB/DuckDB"]
  end
  subgraph Policy["Tenant policy & ops"]
    TT["tenant_tier.rs (Tier1..Tier5, budgets, ef caps)"]
    RP["recall_probe.rs (3-pass quant gate)"]
    BG["budget_guard.rs"]
    IRS["iceberg_rest_service.rs (Proxima-as-Iceberg-catalog)"]
    PP["partition_pruning.rs"]
    CV["corpus_version.rs (plan-cache invalidation)"]
  end
  Reg --> Info
  Reg --> Enf
  FC --> EC
  FC --> Reg
----

=== Index Layer / AXIS (src/index)

Legend: axis/management/{manager,analyzer,adaptive_engine,migration_engine,monitor}.rs,
axis/indexes/{hnsw_index,dual_store_ivf,lsh_index,annoy_index}.rs, axis/hmgi/
{registry,router,coordinator,detection,extraction}.rs, axis/integration/ (eventlog_consumer,
tiering_manager, memory_tracker), axis/storage/ (serialization, recovery), edr/, geo/,
axis/{tunnel,filterable_metadata}.rs.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  subgraph Core["AXIS coordination (management/)"]
    MGR["AxisManager (manager.rs)"]
    ADP["AdaptiveIndexEngine (adaptive_engine.rs)"]
    ANL["CollectionAnalyzer (analyzer.rs)"]
    MIG["MigrationEngine (migration_engine.rs)"]
    MON["AxisMonitor (monitor.rs)"]
  end
  subgraph Strat["Index strategies (indexes/)"]
    HNSW["HNSW (hnsw_index.rs)"]
    IVF["IVF (dual_store_ivf.rs)"]
    LSH["LSH (lsh_index.rs)"]
    ANNOY["Annoy (annoy_index.rs)"]
    FLAT["Flat (brute force)"]
  end
  MGR --> HNSW & IVF & LSH & ANNOY & FLAT
  ADP & ANL & MON --> MGR
  MIG --> MGR
----

==== HMGI (per-modality partitioning) and predicate-aware search

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  REG["HmgiRegistry (hmgi/registry.rs)"]
  ROUTER["HmgiRouter (hmgi/router.rs)"]
  COORD["HmgiQueryCoordinator (hmgi/coordinator.rs)"]
  DET["ModalityDetector (hmgi/detection.rs)"]
  EXT["ModalityExtractor (hmgi/extraction.rs)"]
  RING["ConsistentHashRing (hmgi/mod.rs)"]
  COORD --> ROUTER --> REG
  DET & EXT --> REG
  REG --> RING
  TUN["TunnelGate (tunnel.rs): Fetch/Tunnel/Skip"]
  FM["FilterableHnswMetadata (filterable_metadata.rs): tenant/RLS/TTL"]
  TUN --> FM --> HNSWn["HNSW nodes"]
----

==== EDR and geo

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph LR
  subgraph EDR["edr/"]
    EI["EdrIndex"]
    QE["QueryExpansion (expansion.rs)"]
    LI["LateInteractionScorer (scoring.rs)"]
    DSt["EdrDocumentStore (storage.rs)"]
  end
  subgraph Geo["geo/"]
    GH["GeoHash (geohash.rs)"]
    GI["GeoIndex (index.rs)"]
    GQ["GeoQuery (queries.rs)"]
  end
  QE --> EI
  LI --> EI
  DSt --> EI
  GH --> GI --> GQ
----

=== Compute Layer (src/compute)

Legend: distance_computation/engine.rs (UnifiedDistanceCompute, runtime SIMD),
quantization/quantization_engine.rs, gpu/distance.rs (Metal/CUDA/ROCm/OpenCL),
proximacodec/{codec,registry,wire_format}.rs, scheduler/mod.rs, provider/traits.rs,
pipeline_executor.rs.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  C["compute/mod.rs"]
  DC["distance_computation/ (engine.rs)"]
  Q["quantization/ (quantization_engine.rs)"]
  CO["proximacodec/ (codec.rs)"]
  G["gpu/ (distance.rs)"]
  S["scheduler/ (mod.rs)"]
  P["provider/ (traits.rs)"]
  PE["pipeline_executor.rs"]
  C --> DC & Q & CO & G & S & P & PE
  DC --> G
  Q --> DC
  CO --> Q
  S --> P
  PE --> P
----

==== Distance dispatch and quantization levels

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  Q["Query vector + DistanceMetric"] --> DC["UnifiedDistanceCompute (engine.rs)"]
  DC --> SEL{"Runtime SIMD level"}
  SEL --> A512["AVX512 (platform/avx512.rs)"]
  SEL --> A2["AVX2"]
  SEL --> NEON["NEON (distance_arm64_patch.rs)"]
  SEL --> SCAL["Scalar fallback"]
  DC --> QD["QuantizedDistanceCalculator (quantized.rs, int8_simd.rs)"]
  DC --> SP["Sparse kernels (sparse/)"]
  UQE["UnifiedQuantizationEngine (quantization_engine.rs)"] --> LV["Binary / Scalar(int8) / PQ / Uniform (types.rs)"]
  UQE --> GC["GlobalQuantizationCache (global_cache.rs)"]
----

=== Storage Layer (src/storage)

Legend: traits/mod.rs (UnifiedStorageEngine), trait_components/ (identity/reader/writer/compactor/
metrics/scan/lifecycle), engines/factory.rs + engine dirs (sst, viper, nova, swift, raptor, helix,
cedar, chrono, sequoia, titan, tst, eventlog), persistence/write_ahead_log/, persistence/filesystem/,
document/sdp.rs.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
classDiagram
  class UnifiedStorageEngine {
    +engine_name() str
    +strategy() StorageEngineStrategy
    +engine_type() StorageEngineType
    +do_flush(FlushParameters) FlushResult
    +do_compact(CompactionParameters) CompactionResult
    +vector_by_id(collection_id, base_path, vector_id) Option~ProximaRecord~
    +search_vectors_unified(StorageQueryContext) Vec~OptimizedSearchRecord~
    +ingest_sorted_segment(collection_id, base_path, records) SegmentIngestResult
    +create_scan(collection_id, strategy, config) ScanIterator
    +supports_atomic_operations() bool
  }
  UnifiedStorageEngine <|.. SstEngine
  UnifiedStorageEngine <|.. ViperEngine
  UnifiedStorageEngine <|.. NovaEngine
----

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  T["UnifiedStorageEngine trait (traits/mod.rs)"]
  F["StorageEngineFactory (engines/factory.rs)"]
  T --> F
  F --> E["Concrete engines (engines/*/): sst viper nova swift raptor helix cedar chrono sequoia titan tst eventlog"]
  T --> TC["trait_components/ (identity/reader/writer/compactor/metrics/scan/lifecycle)"]
  T --> WAL["persistence/write_ahead_log/"]
  T --> FS["persistence/filesystem/"]
  T --> SDP["document/sdp.rs"]
----

==== WAL and filesystem internals

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  WO["wal_operations.rs"] --> BS["batch_strategy.rs"]
  BS --> SER["serialization (bincode/proto/avro)"]
  SER --> DM["disk_manager.rs (fsync)"]
  DM --> FSb["filesystem backend"]
  MAN["manifest/ (segment tracking JSONL)"] --> DM
  DM --> RM["recovery_manager.rs (replay on startup)"]
  CC["compaction_coordinator.rs"] --> FC["flush_coordinator.rs"]

  FSroot["filesystem/mod.rs"] --> Local["local.rs"]
  FSroot --> S3["s3.rs"]
  FSroot --> AZ["azure.rs"]
  FSroot --> GCS["gcs.rs"]
  FSroot --> CACHE["caching_filesystem.rs (LRU + block cache)"]
  CACHE --> SIO["smart_io/ (prefetch)"]
----

=== Observability Layer (src/observability)

Legend: ingestion/ (http/otlp/syslog/cef_leef/ocsf/fluent adapters, parser.rs, buffer.rs),
query/ (logs.rs, metrics.rs, promql.rs, traces.rs, tantivy_log_index.rs), storage/ (partitioned.rs,
metrics.rs, traces.rs, rollup_persistence.rs), alerting/ (engine/rules/notifications/escalation/
history/persistence), trace_* (batcher/digest/fingerprint/retention/sampling), audit/schema_audit.rs,
metering_event.rs, rank_metrics.rs, route_explain.rs, tenant_label.rs.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  subgraph Ingest["Ingestion (ingestion/)"]
    AD["http/otlp/syslog/cef_leef/ocsf/fluent adapters"]
    BUF["RingBuffer (buffer.rs)"]
    PAR["LogParser (parser.rs)"]
  end
  ING["ObservabilityIngester"]
  subgraph Store["Storage (storage/)"]
    PART["PartitionedStorage (logs)"]
    MET["MetricStorage (time-series)"]
    TRC["TraceStorage (spans)"]
    ROL["rollup_persistence.rs"]
    WAL["UnifiedWAL durability"]
  end
  subgraph Qy["Query (query/)"]
    LQ["LogQueryBuilder"]
    MQ["MetricQueryBuilder"]
    PQL["PromQLExecutor (promql.rs)"]
    TAN["TantivyLogIndex (BM25)"]
  end
  subgraph Alert["Alerting (alerting/)"]
    ENG["AlertEngine"]
    NOT["NotificationManager (webhook/Slack/PagerDuty)"]
    ESC["escalation + history + persistence"]
  end
  AD --> BUF --> PAR --> ING
  ING --> PART & MET & TRC
  PART & MET & TRC --> WAL
  ROL --> MET
  PART --> LQ & TAN
  MET --> MQ & PQL
  ENG --> NOT --> ESC
----

==== Tracing → metering pipeline

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph LR
  SP["SearchPlanTrace (search_plan_trace.rs)"] --> RE["route_explain.rs"]
  TS["TraceStorage"] --> SAMP["trace_sampling.rs"]
  SAMP --> RET["trace_retention.rs"]
  RET --> BAT["trace_batcher.rs"]
  BAT --> DIG["trace_digest.rs (FNV-1a)"]
  DIG --> FP["trace_fingerprint.rs"]
  FP --> TL["tenant_label.rs (bounded cardinality)"]
  BAT --> ME["metering_event.rs (billing JSON)"]
  TL --> ME
----

=== CDC Layer (src/cdc)

Legend: event.rs (ChangeEvent/Operation), config.rs, connectors/ (postgres/mysql/mongodb —
experimental, feature `experimental-cdc-connectors`), sinks/ (kafka.rs, webhook.rs, MultiSink),
transform/ (schema.rs, filter.rs, embedding.rs), outbound/ (subscriber.rs, position.rs, router.rs,
dedup.rs, exactly_once.rs), coordinator.rs, offset.rs.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  subgraph Sources["Sources"]
    PG["postgres/ (experimental)"]
    MY["mysql/ (experimental)"]
    MO["mongodb/ (experimental)"]
    WS["WalSubscriber (outbound/subscriber.rs)"]
  end
  CE["ChangeEvent (event.rs)"]
  CO["CdcCoordinator (coordinator.rs)"]
  subgraph Tx["Transforms (transform/)"]
    SM["SchemaMapper (schema.rs)"]
    FR["FilterRuleSet (filter.rs)"]
    EM["EmbeddingPipeline (embedding.rs)"]
  end
  RT["EventRouter (outbound/router.rs)"]
  subgraph Sinks["Sinks (sinks/)"]
    KA["KafkaSink (kafka.rs)"]
    WH["WebhookSink (webhook.rs)"]
    MS["MultiSink"]
  end
  EO["ExactlyOnceManager (outbound/exactly_once.rs) + dedup.rs"]
  PG & MY & MO & WS --> CE --> CO
  CE --> SM & FR & EM --> RT
  RT --> KA & WH & MS
  KA & WH & MS --> EO
  WS --> POS["PositionTracker (outbound/position.rs) → OffsetStore (offset.rs)"]
----

=== Cluster Layer (src/cluster)

Legend: mod.rs (ClusterManager), consensus.rs (RaftConsensus), replication.rs, routing.rs, shard.rs,
distributed_ops.rs, primary_pod_registry.rs, cache_affinity.rs, rpc/ (traits.rs, grpc_client.rs,
grpc_server.rs, connection.rs, retry.rs, types.rs, error.rs).

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  CM["ClusterManager (mod.rs)"]
  RAFT["RaftConsensus (consensus.rs)"]
  MDS["MetadataService / NodeRegistry"]
  RS["RoutingService (routing.rs)"]
  SH["ShardManager (shard.rs)"]
  REP["EngineReplication (replication.rs)"]
  PPR["PrimaryPodRegistry (primary_pod_registry.rs)"]
  CA["CacheAffinityRegistry (cache_affinity.rs)"]
  CM --> RAFT & MDS & RS & SH
  RS --> SH
  CA --> PPR
  subgraph RPC["rpc/ abstraction"]
    CT["ConsensusTransport / ReplicationSink / SearchFanout / HealthChecker (traits.rs)"]
    GC["GrpcTransport (grpc_client.rs) + connection.rs + retry.rs"]
  end
  RAFT --> CT --> GC
  REP --> CT
----

=== Embedded Bindings Layer (src/embedded)

Legend: mod.rs (EmbeddedProximaDB), python.rs (PyO3), java.rs (JNI), nodejs.rs (NAPI-RS),
c_ffi.rs (C FFI), coordination/ (file_lock.rs, leader_election.rs), streaming.rs, agent_memory.rs.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  subgraph Lang["Language bindings (feature-gated)"]
    PY["Python PyO3 (python.rs)"]
    JV["Java JNI (java.rs)"]
    ND["Node NAPI (nodejs.rs)"]
    CF["C FFI (c_ffi.rs)"]
  end
  EDB["EmbeddedProximaDB (mod.rs) → SharedServices"]
  subgraph Coord["coordination/"]
    FL["FileLockManager (file_lock.rs)"]
    LE["LeaderElection (leader_election.rs)"]
  end
  CK["CheckpointManager (agent_memory.rs)"]
  STR["EmbeddedSearchIterator (streaming.rs)"]
  PY & JV & ND & CF --> EDB
  EDB --> FL & LE & CK & STR
----

=== Core Layer (src/core)

Legend: config.rs, service_types.rs, types/, search/ (filter_contract.rs,
metadata_filter_pushdown.rs, hybrid/, rank/), hardware_capabilities.rs, compression/, bloom/,
storage_layout.rs, memory/pool.rs, resilience/ (circuit_breaker.rs, retry.rs,
health_aggregator.rs), errors/core_error.rs, foundation/ (→ proximadb-kernel).

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
  subgraph CT["Config & types"]
    CFG["StorageConfig/SstConfig (config.rs)"]
    STyp["ServiceTypes (service_types.rs)"]
    TYP["ProximaType/TypedValue (types/)"]
  end
  subgraph SR["Search & filtering (search/)"]
    SM["search core (mod.rs)"]
    FC["FilterContract (filter_contract.rs)"]
    MF["MetadataFilterPushdown (metadata_filter_pushdown.rs)"]
    HY["hybrid/ fused ranking"]
    RK["rank/ RERANK SRF"]
  end
  subgraph HC["Hardware & compute"]
    HW["HardwareCapabilities (hardware_capabilities.rs)"]
    CMP["compression/"]
  end
  subgraph RES["Resilience (resilience/)"]
    CB["CircuitBreaker"]
    RP["RetryPolicy"]
    HA["HealthAggregator"]
  end
  ERR["ProximaDBError (errors/core_error.rs)"]
  FND["foundation/ → proximadb-kernel"]
  STyp --> CFG
  TYP --> STyp
  SM --> FC & MF & HY & RK
  HW --> CMP
  CB --> RP --> HA
  STyp --> ERR
  SM & TYP --> FND
----

== Critical-Path Sequence And Activity Diagrams

Traced through the live caller chain (verified 2026-05-28). These ground the "Runtime Data Flow"
summary in the atlas with method-level detail.

=== Write Path Sequence

Chain: `rest/v2/records.rs::insert_records` → `api_handlers/request_handlers.rs::
handle_record_batch_for_tenant` → `services/operations/vectors/legacy.rs::
insert_records_with_tenant_context` → `insert_batch_internal` → `insert_vectors_via_wal` →
`storage/persistence/write_ahead_log/mod.rs::write_vector_batch_native_arc_with_mode`.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
sequenceDiagram
  participant C as HTTP client
  participant R as REST v2 (records.rs)
  participant H as UnifiedHandlers (request_handlers.rs)
  participant V as VectorOpsService (legacy.rs)
  participant W as WALManager (write_ahead_log/mod.rs)
  participant M as Memtable
  participant F as Filesystem
  C->>R: POST /collections/{id}/records/batch
  R->>R: validate schema, build ProximaRecord(s)
  R->>H: handle_record_batch_for_tenant(batch)
  H->>H: resolve_collection_id, validate, enforce_wal_lane
  H->>H: coerce_records_to_canonical_precision
  H->>V: insert_records_with_tenant_context(...)
  V->>V: validate_tenant_access, ensure_tenant_on_records
  V->>V: insert_batch_internal → bulk_write_router.route_records
  V->>W: write_vector_batch_native_arc(collection_id, Arc<Vec<ProximaRecord>>)
  W->>M: add_vector_batch(collection_id, batch)
  M-->>W: sequences: Vec<u64>
  alt sync mode enabled
    W->>W: serialize_batch_with_v2_segment_header
    W->>F: write WAL segment + manifest, fsync
    F-->>W: Ok(path)
  end
  W-->>V: Ok(sequences)
  V-->>H: BatchOperationResult
  H-->>R: result + bump_row_count_stats
  R-->>C: InsertRecordsResponse(inserted, failed, ids)
----

=== Search Path Sequence

Chain: `rest/v2/records.rs::search_with_typed_filters` → `request_handlers.rs::
handle_record_search_for_tenant` → `legacy.rs::search_records_with_tenant_context` →
`unified_search_with_tenant_context` → `query_optimizer.rs::optimize_query` →
`legacy.rs::execute_unified_plan` → `execute_two_stage_search_with_mode` →
WAL (`search_unflushed_vectors`) + AXIS (`AxisManager::query`) + storage
(`search_vectors_unified`) + compute (SIMD distance).

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
sequenceDiagram
  participant C as HTTP client
  participant R as REST v2 (records.rs)
  participant H as UnifiedHandlers (request_handlers.rs)
  participant V as VectorOpsService (legacy.rs)
  participant O as QueryOptimizer (query_optimizer.rs)
  participant X as Executor (legacy.rs)
  participant W as WAL (search_unflushed_vectors)
  participant A as AxisManager (index/axis)
  participant S as StorageEngine (search_vectors_unified)
  participant K as Compute (distance)
  C->>R: POST /collections/{id}/search
  R->>H: handle_record_search_for_tenant(RichSearchRequest)
  H->>V: search_records_with_tenant_context(...)
  V->>V: validate_tenant_access, check query_cache
  V->>O: optimize_query(UnifiedQueryContext)
  O-->>V: UnifiedExecutionPlan
  V->>X: execute_unified_plan(...)
  X->>X: execute_two_stage_search_with_mode(ann_filtering_mode, selectivity)
  X->>W: search_unflushed_vectors (Stage 1)
  W-->>X: Vec<OptimizedSearchRecord>
  X->>A: query(HybridQuery) (Stage 2)
  A->>K: compute_similarity (AVX512/AVX2/NEON/scalar)
  K-->>A: scores
  A-->>X: Vec<OptimizedSearchRecord>
  opt need more (Stage 3)
    X->>S: search_vectors_unified(context)
    S->>K: distance_metric.compute (quant-aware)
    S-->>X: Vec<OptimizedSearchRecord>
  end
  X->>X: dedup (WAL>AXIS>Storage), drop tombstones, sort top_k
  X-->>V: results
  V->>V: validate_search_results_tenant_isolation, cache
  V-->>H: RichSearchResponse
  H-->>R: response
  R-->>C: TypedSearchResponse(results, total_found, latency_ms)
----

=== Write Path Activity (decision logic)

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
flowchart TD
  S([batch insert]) --> PP{primary-pod check}
  PP -->|misrouted| R421[return 421 Misdirected]
  PP -->|ok| SCH{schema valid?}
  SCH -->|no| ERRS[SCHEMA_VALIDATION_FAILED]
  SCH -->|yes| LANE{WAL lane allowed?}
  LANE -->|no| ERRL[WAL_LANE_REJECTED]
  LANE -->|yes| PREC[coerce canonical precision]
  PREC --> TEN{tenant access?}
  TEN -->|no| ERRT[UNAUTHORIZED]
  TEN -->|yes| ROUTE{bulk-write router}
  ROUTE -->|large| BULK[BULK_WAL path]
  ROUTE -->|small| STD[standard WAL path]
  BULK --> WMT[add to memtable]
  STD --> WMT
  WMT --> SYNC{sync mode?}
  SYNC -->|async| OK[BatchOperationResult success]
  SYNC -->|always/per-batch| SER[serialize + V2 segment header]
  SER --> FS[write segment + manifest, fsync]
  FS --> MET[emit precision metrics]
  MET --> OK
  OK --> E([InsertRecordsResponse])
----

=== Search Path Activity (multi-stage routing)

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
flowchart TD
  S([search request]) --> VAL[validate vector/top_k/filters]
  VAL --> TEN{tenant access?}
  TEN -->|no| ERR[UNAUTHORIZED]
  TEN -->|yes| CACHE{query cache hit? 5m TTL}
  CACHE -->|hit| RET[return cached]
  CACHE -->|miss| OPT[optimize_query → ExecutionPlan]
  OPT --> MODE{AnnFilteringMode}
  MODE --> TS[execute_two_stage_search_with_mode]
  TS --> ST1[Stage 1: WAL/memtable search_unflushed_vectors]
  ST1 --> ST2[Stage 2: AXIS HNSW query + SIMD distance]
  ST2 --> ENOUGH{>= candidates?}
  ENOUGH -->|yes| DED[dedup WAL>AXIS>Storage]
  ENOUGH -->|no| ST3[Stage 3: storage search_vectors_unified]
  ST3 --> DED
  DED --> TOMB[drop tombstones]
  TOMB --> SORT[sort by similarity, take top_k]
  SORT --> ISO[validate_search_results_tenant_isolation]
  ISO --> PUT[cache results]
  PUT --> E([TypedSearchResponse])
  RET --> E
----

== Recursive Decomposition — Completeness Note

Covered subsystems (one component map + recursive sub-diagrams each): network, api_handlers,
services, query, graph, catalog, index/AXIS, compute, storage, observability, cdc, cluster, embedded,
core — plus system-level use-case/deployment (atlas) and the two critical-path sequence/activity
flows above. Decomposition stops at the file/struct level where the next split would restate code
rather than architecture. Deeper, churning detail (per-algorithm internals, per-adapter wire formats)
is intentionally left to the source and the focused ADRs/LLDs referenced in `README.adoc`, to keep
this map stable against routine change.
