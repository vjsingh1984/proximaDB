// Copyright (C) 2026 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= Connectivity × Modality Completion Tracker — the shape, the seams, and the MVP path to write+read on every surface (SaaS + BYOC)
:toc: macro
:toclevels: 3

[cols="1,3", options="header"]
|===
| Field | Value
| Status | **Active planning authority** (2026-07-21). This is the single cross-surface completion map; the substrate execution detail lives in link:../10-quality/td/TD-WAL-2-multimodal-wal-convergence.adoc[TD-WAL-2], connectivity gaps in link:../10-quality/td/TD-CONN-1-surface-parity-gaps.adoc[TD-CONN-1].
| Owner | Vijaykumar Singh
| Decision inputs | Substrate-first sequencing; relational = durable single-statement DML now, ACID txns deferred (owner decision 2026-07-21).
| Relates to | ADR-020 (canonical WAL authority), ADR-027 (unified storage + metering substrate), ADR-069 (WAL local-disk tiered flush), ADR-053 (cross-modal query fabric), ADR-063 (object-store-native catalog restartability), ADR-036 (Azure object-store orientation), ADR-018 (pgwire SQL parity roadmap), TD-WAL-1/TD-WAL-2, TD-TENANT-1, `CODESIGN_DIMENSIONAL_ARCHITECTURE_2026_06_19.adoc`, `IN_PROCESS_EDGE_COLLAPSE_HLD_2026_06_19.adoc`, `SEARCH_SURFACE_CONTRACT_2026_06_24.adoc`.
| Evidence basis | Four verified reconnaissance sweeps (2026-07-21) of `src/network/`, `src/storage/engines/`, `src/storage/persistence/write_ahead_log/`, and `docs/12-design/`. File:line anchors below are read-confirmed unless marked (inferred).
|===

toc::[]

== 1. End goal (definition of done)

ProximaDB is **multimodal** (vector, document, graph, timeseries, relational) and **multifaceted**
(REST, gRPC, Arrow Flight, pgwire, MCP). "Complete" means, for every *tracked* modality and every
*canonical* surface:

. **Write and read both work** end-to-end through the engine's own router — never a test/product
  bypass to an engine.
. **Durable + crash-recoverable** on the one shared substrate (unified WAL → memtable → coalesced
  flush → recovery), with the *flushed* format free to be modality-optimized.
. **Metered per dimension** (KSU storage / KRU read / KIU compute / KOU outgress / KEU embedding),
  `TenantContext`-stamped, fail-closed.
. **Deployable two ways from one build**: **enterprise SaaS** (multi-tenant, structural isolation,
  per-tenant object-store paths) and **BYOC** (customer's own cloud account + object store, same
  binary via the in-process edge collapse).

Non-goal for this MVP: multi-statement ACID transactions (pgwire `BEGIN/COMMIT`), cross-region WAL
replication, and SQL-over-Arrow-Flight. These ride named follow-ups (§8).

== 2. Verified posture (2026-07-21)

Vector is the most mature modality; REST/gRPC the most mature connectivity. The gaps are asymmetric:
connectivity is nearly there; the *substrate under the non-vector modalities* is the real work.

.Modality maturity (write substrate → recovery → read)
[cols="1,2,2,1", options="header"]
|===
| Modality | Write substrate | Recovery | Read
| **Vector** | ✅ canonical WAL → LSM memtable → SST/ProximaBlocks (`services/canonical_wal.rs`, `storage/engines/sst/`) | ✅ unified `RecoveryManager` (`recovery_manager.rs`) | ✅ three-stage filter search + point-get
| **Document** | ⚠️ canonical route (gated) beside legacy `document_wal` fallback (`storage/document/service.rs`) | ⚠️ legacy replay; canonical replay gated | ✅ index-backed (B+tree/inverted/Tantivy)
| **Graph** | ⚠️ engine-internal WAL; canonical appender **default-OFF** (`graph/service.rs`) | ⚠️ ORION side-channel replay; canonical re-pop gated `PROXIMADB_GRAPH_CANONICAL_RECOVERY` | ✅ CSR traversal + fusion
| **Timeseries** | ❌ **isolated** engine WAL, not the canonical appender (`storage/engines/tst/`) | ⚠️ dedicated `TstWalRecovery` off its *own* WAL | ✅ OHLC / ASOF / partition-prune
| **Relational** | ⚠️ writes route through canonical/vector; no first-class path; no txns | ⚠️ indirect via canonical | ✅ native Volcano + DataFusion OLAP
|===

.Connectivity maturity (RW per modality)
[cols="1,2,2", options="header"]
|===
| Surface | Read | Write
| **REST v2** (`network/rest/v2/`) | vector, document, graph(fusion), timeseries, SQL-SELECT | vector, document, entity, timeseries — **graph writes absent** (gRPC/Flight only)
| **gRPC v2** (`network/grpc/v2/`) | vector, document, graph, fusion, SQL-SELECT | vector, document, graph, entity — **no timeseries service**
| **Arrow Flight** (`network/arrow_ipc/`) | vector, graph, file export | vector (bulk), graph (bulk) — no SQL
| **pgwire** (`network/postgres/`, :5433) | SELECT (vector + relational + federation) | full DML/DDL — **transactions unsupported**, COPY partial
| **MCP** (`platform/.../mcp`) | list/describe/stats/search/explain/event | **read-only + audit append** — checkpoint write deferred (TD-MCP-1)
|===

**Reconciliation of the 2026-07-21 review.** The review's "recovery replays vector batches only" is
precise about the *unified* `RecoveryManager` (`recovery_manager.rs:514-516` deserializes every WAL
batch into `Vec<ProximaRecord>` with **no dispatch on `UnifiedWALOperation` variant**), but graph and
timeseries do recover — via their own engine side-channels. The true defect is therefore **N parallel
recovery mechanisms instead of one dispatcher**, two of them gated OFF. That is the exact thing the
`ModalityRecoveryReplayer` seam (§4) collapses.

== 3. First principles — the two seams and one law

A record's journey is **identical across modalities**:

  surface → tenant-stamp → WAL(durable) → memtable(buffer) → flush(segment) → read(liveness-filtered) → recover → meter

The **only** legitimately modality-specific parts are: (a) the *flushed segment format* + its reader,
(b) the *post-flush index* (AXIS for vectors; adjacency IS the graph index; TS/doc index
incrementally), (c) the *query operators*. The **only** legitimately surface-specific parts are:
(a) the *wire encoding* (HTTP/JSON, HTTP2/proto, Arrow IPC, pgwire, JSON-RPC), (b) the *ergonomic
facade*. Everything else is shared.

That collapses the whole program to **two seams** — and the code is ~95% ready to *bend into* them
(no parallel reimplementation; see §4 for the confirmed bend points):

[cols="1,3", options="header"]
|===
| Seam | Shape
| **Storage seam** | One WAL (`UnifiedWALOperation`, already 8-variant), one memtable coordinator, one flush driver (ADR-069), one recovery dispatcher — with `ModalityBatch` + `ModalityMaterializer::do_flush` + `ModalityRecoveryReplayer` as the three trait/dispatch points behind which N modalities plug their format/index/replay.
| **Connectivity seam** | One canonical request envelope + one tenant-trust plane (`HeaderTrustPolicy`, TD-TENANT-1, already unified across all five surfaces) + one router (`ComputeScheduler` / `UnifiedQueryPort`, ADR-053) → N wire encoders. Surfaces are thin codecs over one core, not five stacks.
|===

**The law (made a mandatory contract in the rule seed, §7):** no modality may own a private WAL or a
private recovery path; no surface may own a private auth/tenant/routing path or a hand-rolled
request/response contract. New readers/writers emit the query-scoped I/O trace so the dimension they
touch is observable.

== 4. Extreme co-design — why unification is a cost lever, not cleanliness

Per `CODESIGN_DIMENSIONAL_ARCHITECTURE_2026_06_19.adoc`, we optimize across the storage↔compute↔network
boundary against the measured trace, toward each dimension's **dominant cost term** — for a cloud DB
that is **I/O round-trips + object-store operations + egress**, not CPU. The two seams each move a
dominant term:

* **One cross-modal flush driver → coalesced PUTs across modalities.** Today each modality that flushes
  does so on its own cadence; unifying under the ADR-069 driver lets N modalities' small segments
  batch into fewer, larger object PUTs — the Aurora/Neon/Turso "append→threshold-flush→compact"
  primitive ProximaDB already runs (ADR-027 Decision 3). Fewer PUTs = direct GB-op cost reduction and
  less read amplification/list cost. This is the small-files win applied cross-modality.
* **One recovery dispatcher → correct serverless restart on object storage.** ADR-063's
  catalog-driven restartability assumes every durable op is replayable from the one ledger; N
  side-channels break that assumption. Collapsing to `ModalityRecoveryReplayer` is what makes
  "no side RDBMS, restart from object store" actually hold for graph/TS/document.
* **One tenant/metering plane → structural isolation + accurate per-dimension billing.** Isolation is
  `DrPathBuilder`-structural, never a per-query predicate; metering lands KSU/KRU/KIU/KOU/KEU per
  tenant (ADR-027 Decision 4, dual-sink: durable per-tenant under `_metering/` + standard OTLP).

**Confirmed bend points (reuse-first, from the substrate sweep):**

[cols="2,2,2", options="header"]
|===
| Component | Current state | Minimal bend
| WAL enum `UnifiedWALOperation` | already 8 modality variants (`wal_operations.rs:34-88`) | ✅ none — the ledger is generic
| Flush policy + `AutoFlushDriver` | byte-based, modality-agnostic (`flush_policy.rs`, `auto_flush_driver.rs`) | ✅ none — already generic
| Size estimator | canonical `WALVectorBatch::estimate_records_size` (`wal_behavior.rs:70-84`) precision-aware; **duplicate** fp32-only in `BulkWriteRouter` (`bulk_write_router.rs:208-231`) | **V1**: point the router at the canonical fn
| `materialize_collection` | vector-hardcoded empty-embedding filter (`flush_materializer.rs:136-140`) | **V4**: lift into `ModalityMaterializer::do_flush`
| `RecoveryManager` replay | vector-only, no op-type dispatch (`recovery_manager.rs:514-516`) | **V2**: `ModalityRecoveryReplayer` dispatch on `UnifiedWALOperation`
| Memtable coordinator | `HashMap<coll → HashMap<batch → WALVectorBatch>>` (`wal_behavior.rs:154-160`) | **V4**: coordinate over `dyn ModalityBatch`
|===

== 5. Track M — modality completion (substrate-first)

Substrate work is tracked and sequenced in **TD-WAL-2** (slices V1–V6). This section is the roll-up +
per-modality definition-of-done; do not duplicate the slice detail here.

[cols="1,3,2,1", options="header"]
|===
| Item | MVP target (definition of done) | Reuse (no new build) | Blast
| **M0 substrate** | Size-accounting single-owner (V1) → `ModalityRecoveryReplayer` dispatch (V2) → `ModalityBatch` + `ModalityMaterializer::do_flush` + cross-modal coalesced flush (V4). One WAL, one recovery, one flush driver. | TD-WAL-2, ADR-020/027/069 | tiny→med
| **M1 Vector** | ✅ done — the reference template every modality converges to. | — | —
| **M2 Document** | Canonical cutover complete; legacy `document_wal` retired; recovery via V2 replayer; point-get filter pushdown. | ADR-055, TD-DOC-RETIRE-1, TD-DOC-CONV-1 | med
| **M3 Graph** | Mutations emit `GraphOp` to the shared WAL (or canonical appender promoted mandatory w/ fsync contract); canonical recovery flipped ON via V2; checkpoint truncation (TD-066c). Kills two-WAL split-brain. | TD-ORION-1, ADR-012, TD-WAL-2 V3 | med
| **M4 Timeseries** | TST writes route to the canonical appender (off its isolated WAL); recovery via V2; retention enforced. Gated on a named durability-requiring TS workload (V5 trigger). | TD-WAL-2 V5, TD-TS-1 | med
| **M5 Relational** | First-class durable single-statement DML on the substrate (INSERT/UPDATE/DELETE land in the WAL as canonical records); reads already prod. **ACID txns deferred** to ADR-018 Phase 3. | ADR-018, TD-REL-LOWER-*, TD-XMODAL-4 | large
|===

== 6. Track C — connectivity completion

[cols="1,3,2", options="header"]
|===
| Item | MVP target | Reuse / home
| **C0 envelope** | Canonical request envelope + tenant-trust plane (✅ unified via `HeaderTrustPolicy`); edge-collapse (`proximadb-edge`) for BYOC L7-in-process. | TD-TENANT-1, `IN_PROCESS_EDGE_COLLAPSE_HLD`
| **C1 REST** | Add graph-write endpoints for parity with gRPC; keep pgwire authoritative for SQL-write (documented policy, not a gap). | ADR-053, `SEARCH_SURFACE_CONTRACT`, **TD-CONN-1**
| **C2 gRPC** | Add a timeseries service (the one modality-surface gap); otherwise parity. | **TD-CONN-1**, TD-TS-1
| **C3 Arrow Flight** | Bulk RW parity for vector/graph (done); SQL-over-Flight deferred (§8). | —
| **C4 pgwire** | Durable single-statement DML parity (done) + COPY completion; transactions gate deferred to Phase 3. | ADR-018
| **C5 MCP** | Checkpoint/agent-state write (currently read-only + audit append). | TD-MCP-1
| **C6 SDKs** | Every SDK's REST transport generated from the OpenAPI spec (sync + async); merge-blocking codegen-drift gate. | TD-126 / TD-SDK, mandate #15
|===

== 7. Deployment shape (SaaS + BYOC — one build)

Both deployment modes reuse the same two seams; only the *edge* and *object-store binding* differ.

* **Enterprise SaaS**: multi-tenant, structural isolation via `DrPathBuilder` (`data/{tenant}/{namespace}/…`),
  per-dimension metering (dual-sink, ADR-027 Decision 4), object-store-native catalog restartability
  (ADR-063), per-object access tier as the at-rest cost lever (ADR-036 — Blob endpoint, flat keys,
  HNS-off; `ObjectAccessTier`/`FileOptions.storage_class`, never per-cloud strings at call sites).
* **BYOC**: same binary; the in-process edge collapse (`IN_PROCESS_EDGE_COLLAPSE_HLD`) folds
  reverse-proxy / LB / API-gateway into the process with one policy plane (TLS, authn, ABAC, tenant,
  rate-limit, trace), pointed at the customer's own object-store account. Metering still emits the
  neutral usage event; pricing stays an AnvaiOps concern (open-core boundary, ADR-067).

== 8. Sequencing (owner decision: substrate-first)

----
M0.V1 estimator dedup  ──►  M0.V2 recovery dispatch  ──►  M0.V3/V4 graph parity + ModalityMaterializer
   (this PR)                 (never let a modality write ops recovery can't replay)
        │                                                            │
        └── docs + rule-seed contract (this PR) ────────────────────┘
                                                                     ▼
                              M2 document cutover · M4 TS→canonical · M5 relational durable DML
                                                                     ▼
                              Track C gaps (C1 REST graph-write, C2 gRPC TS svc, C5 MCP checkpoint)
----

Rationale: substrate is the commonality, the critical-correctness gap, and the cost lever;
connectivity is already the mature axis. V2 (recovery) is load-bearing before any modality writes new
WAL ops — land the replay side first. Each item is a small-blast-radius, in-place change (nothing is
released; no back/forward-compat burden) tested in isolation before moving on.

**Deferred (named triggers):** ACID transactions (ADR-018 Phase 3); SQL-over-Arrow-Flight; TS WAL
(first durability-requiring TS workload, TD-WAL-2 V5); cross-region WAL replication.

== 9. Mandatory contract

The **Multimodal Substrate Law** and **Unified Connectivity Envelope** rules are added to
`docs/agent-rules/CLAUDE.md.seed` (the committed source; the local `CLAUDE.md` is gitignored) so they
bind every session. Summary: no private per-modality WAL/recovery; no private per-surface
auth/tenant/routing/contract; new readers/writers emit the query-scoped I/O trace; changes to a
storage format/reader/codec/cache state the dimensional cost term they move and justify it with a
measured per-query trace.
