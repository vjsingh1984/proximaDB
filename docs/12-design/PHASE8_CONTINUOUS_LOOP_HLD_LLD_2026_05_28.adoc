= Phase 8 HLD/LLD: Continuous Data Loop And Elastic Compute (CS/CD)
:toc: left
:toclevels: 4
:icons: font
:sectnums:

Status: Active feature specification (HLD/LLD) — not an accepted ADR +
Date: 2026-05-28 +
Scope: dial the Vector Lakebase alignment trajectory down to executable feature specifications.
HLD + LLD + task checklists + success criteria for six features (F1–F6), deep for F1/F2/F3/F5 and
outline for F4/F6. Tracks roadmap Phase 8 and TD-086..TD-091.

== Purpose

This document translates the vision-level recommendations in
`docs/12-design/VECTOR_LAKEBASE_ALIGNMENT_2026_05_28.adoc` (R1–R6) into concrete, executable feature
specs. It is *not* a new durability ADR and changes no accepted mandate. The codebase survey behind
this spec found that most of Phase 8 is *composition over primitives that already exist* — so every
feature below anchors to the specific existing type/file it reuses, per the reuse-first mandate in
`CLAUDE.md`.

Governing / referenced documents:

* `docs/12-design/VECTOR_LAKEBASE_ALIGNMENT_2026_05_28.adoc` — the trend analysis and scorecard this
  spec implements (R1→F1, R2→F2, R3→F3, R4→F4, R5→F5, R6→F6).
* `docs/00-product/VISION.adoc` — CS/CD design principle; 2028 serverless milestone.
* `docs/_internal/roadmap/STRATEGIC_ROADMAP.adoc` — Phase 8 rows this spec details.
* `docs/12-design/OPEN_FORMAT_CATALOG_2026_05_17.adoc` — authority modes, stacked durability.
* `docs/12-design/COMPETITIVE_OLTP_OLAP_MPP_TRAJECTORY_2026_05_20.adoc` — `compute_route`,
  `RoutedExecutionPlan`, route-once boundary.
* `docs/12-design/adr/ADR-004-unified-explain-contract.adoc` — every route decision must be
  EXPLAIN-disclosed.
* `docs/12-design/adr/ADR-010-pax-block-format.adoc` — PAX block format extended by F3.

Each feature ships **Experimental → Beta**, with its success criteria as the graduation gate.

== Shared HLD: the CS/CD control plane

ProximaDB already serves (vector / hybrid / cross-model retrieval). Phase 8 adds the *continuous*
half of the Vector Lakebase thesis: a flywheel where serving generates data and feedback, discovery
refines it, and improvements republish atomically back into serving.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph LR
    subgraph SERVE["Serving (exists today)"]
        Q["Retrieval<br/>vector / hybrid / cross-model"]
    end
    subgraph DISC["Discovery (F1)"]
        J["DiscoveryJob<br/>dedup / recluster / re-embed / quality"]
    end
    subgraph FOUND["One logical data foundation"]
        SNAP["Manifest snapshots + branches<br/>(GlobalCheckpoint, branch_id)"]
        IDX["Indexes<br/>(AXIS, BM25, cold-path tiers)"]
        EXT["External-resident data (F5)<br/>Iceberg / Lance / Parquet"]
    end

    Q -->|feedback + new data| SNAP
    SNAP -->|pinned snapshot| J
    J -->|rebuilt data + indexes| SNAP
    SNAP -->|atomic snapshot switch| Q
    EXT -.->|index-in-place| IDX
    IDX --> Q

    style SERVE fill:#e8f0fe,stroke:#2e5c8a,color:#000
    style DISC fill:#f0f7ff,stroke:#4a90e2,color:#000
    style FOUND fill:#f5f5f5,stroke:#999,color:#000
----

The six features map onto this control plane:

* **F1** is the loop itself (discovery job → atomic republish).
* **F2 / F3** make serving over object storage economical (cold start, point-read amplification) so
  compute can scale to zero between loop turns.
* **F5** extends the loop to cover external lake data without copying it.
* **F4** is the elastic compute substrate the loop runs on (offline-batch is the home for F1 jobs).
* **F6** adds Lance as an external substrate for F5.

=== Dependency graph and sequencing

[source]
----
F2 cold-path ─┐                        (independent; also closes TD-075)
F3 point-read ┤── enable ──▶ F1 discovery loop (keystone) ──uses──▶ F5 External Collection
              │                          │                              │
              └──────────────────────────┘                              └── F6 Lance (substrate)
F4 elastic compute (OfflineBatch hosts F1 jobs) ── BLOCKED ON ──▶ TD-077 transport
----

Recommended execution order: **F2 + F3 (near-term enablers) → F1 (keystone) → F5 → F4 (blocked on
TD-077) → F6 (lowest priority).**

=== Cross-cutting mandates (apply to every feature)

Per the `CLAUDE.md` generated-code and architecture mandates, every new route / reader / index in
this spec must, before default enablement:

1. Declare `authority_mode` (e.g. `InternalCanonical`, `ProjectionPublication`, `FederatedRead`) on
   its `CatalogStorageLayout` / `CatalogProjection`.
2. Declare freshness behavior and runtime `freshness_state`, plus the repair/rebuild source.
3. Declare policy boundary (engine-enforced vs external) and RLS behavior.
4. Surface its decision in unified EXPLAIN (ADR-004) and, where relevant, route-health.
5. Carry an explicit `support_status` (`experimental` → `beta` → `supported`).

== F1 — Continuous Discovery loop + atomic snapshot republish (TD-086) [DEEP]

=== HLD

A `DiscoveryJob` is a first-class cataloged asset with the lifecycle:
_schedule → pin snapshot → refine → build new indexes → register new projection → atomic publish →
serving switch_. Serving keeps reading the prior snapshot until the new data + indexes are ready;
the catalog then flips the active projection pointer atomically — no half-built index is ever
visible mid-run. This is the difference between "discovery as an external pipeline" and "discovery
as part of the same operational system."

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
sequenceDiagram
    participant C as Client / scheduler
    participant R as DiscoveryRegistry
    participant E as DiscoveryJobExecutor
    participant M as GlobalManifest
    participant X as xCatalog
    participant S as Serving

    C->>R: POST /discovery-jobs (kind=Dedup)
    R->>M: pin checkpoint (from_lsn..to_lsn)
    R->>X: register DiscoveryJob (Running), projection freshness=Updating
    R->>E: spawn
    E->>E: refine on pinned snapshot (branch_id=discovery_<id>)
    E->>E: rebuild indexes for refined set
    E->>X: register new CatalogStorageLayout (ProjectionPublication)
    Note over S: serving still reads OLD snapshot
    E->>X: atomic swap active pointer; freshness=Fresh; with_lineage(...)
    X-->>S: new snapshot now visible (atomic)
    E->>X: DiscoveryJob (Complete) + quality_metrics
----

Refinement kinds: `Dedup`, `Recluster`, `ReEmbed`, `QualityScan`, `TrajectoryAnalysis`.

=== LLD

New module `src/services/discovery/`:

* `job.rs` — `DiscoveryJob { job_id, collection_id, kind, status, snapshot_from_lsn,
  snapshot_to_lsn, refined_record_count, quality_metrics }`, plus `DiscoveryJobKind`,
  `DiscoveryJobStatus { Scheduled, Running, Complete, Failed }`, `DiscoveryJobResult`.
* `executor.rs` — `DiscoveryJobExecutor` (spawn + run the refinement passes).
* `registry.rs` — `DiscoveryRegistry` (track active jobs, dedupe, expose status).

Reuse map (each item anchored to an existing primitive):

[cols="1,3", options="header"]
|===
| Concern | Reuse

| Snapshot pinning
| `GlobalManifest::get_latest_checkpoint()` + `GlobalLsnAllocator::current()`
(`src/storage/persistence/write_ahead_log/manifest/types.rs`) → capture `from_lsn`/`to_lsn`.

| Freshness state machine
| `ProjectionFreshnessState` (`Fresh`/`Updating`/`Stale`/`RebuildRequired`/`ExternalSnapshotRegistered`/`Unavailable`)
+ `CatalogProjection::with_freshness_state()`, `with_lineage(source_range, last_included_position)`
(`crates/control/proximadb-catalog/src/lib.rs`).

| Atomic publish
| Register `CatalogStorageLayout::projection_publication(...)` + `CatalogProjection`; swap active
pointer via `Catalog::update_table`. For graph/branch collections, stage on
`branch_id = "discovery_<job_id>"` then `merge_branches` (`src/graph/merge.rs`, `MergePolicy`) as
the atomic commit.

| Background execution
| Mirror the `BackgroundMaintenanceManager` / `WALFlushCoordinator` spawn + per-collection status
pattern (`src/storage/persistence/write_ahead_log/background_manager.rs`,
`flush_coordinator.rs`).

| Refinement primitives
| Dedup via `compute/distance`; recluster via `UnifiedIvfIndex` k-means
(`src/index/axis/.../dual_store_ivf.rs`); re-embed hook; extend `src/automl/`
(`AutoMLCoordinator`, `OptimizationPipeline`, `WorkloadPattern::BatchProcessing`) as the offline arm
rather than introducing a new optimizer.

| Compute routing
| `ComputeBackend::Native` initially, `DataFusionDistributed` later
(`src/query/table_write_plan.rs`); read via
`ReadSource::CatalogTable { snapshot: SnapshotRef::SnapshotId(job_id) }`.

| Catalog asset
| `_system.discovery_jobs` registered as `CatalogTableSchema` with authority
`ExternalAuthoritative` (read-only job metadata).

| REST surface
| `POST /api/v2/collections/:id/discovery-jobs`, `GET .../discovery-jobs[/:job]` — reuse v2 handler
patterns (`src/network/rest/v2/`).
|===

=== Tasks

* [ ] *T1.1* `DiscoveryJob` / `DiscoveryJobKind` / `DiscoveryJobStatus` / `DiscoveryJobResult` types
  (`src/services/discovery/job.rs`). → TD-086 acceptance (1)
* [ ] *T1.2* Persist `discovery_jobs` as a cataloged asset (`proximadb-catalog`). → (1)
* [ ] *T1.3* Snapshot pin via `GlobalManifest` checkpoint LSN range. → (2)
* [ ] *T1.4* `DiscoveryJobExecutor` with dedup / recluster / re-embed / quality passes. → (3)
* [ ] *T1.5* Branch-staged write + atomic publish via freshness-state swap. → (3)(4)
* [ ] *T1.6* REST endpoints + handler wiring. → (1)
* [ ] *T1.7* Unified-EXPLAIN / route-health disclosure of republished snapshot. → (5)
* [ ] *T1.8* Integration test `tests/discovery_job_atomic_republish_test.rs`. → (6)

=== Success criteria

1. `DiscoveryJob` is cataloged and queryable.
2. A job pins an LSN snapshot range and runs without blocking serving.
3. Completion republishes new data + rebuilt indexes via an **atomic** snapshot switch — no
   half-built index ever visible mid-run.
4. `freshness_state` transition `Updating → Fresh` is observable in the catalog.
5. EXPLAIN discloses `authority_mode` + freshness + snapshot lineage for the republished projection.
6. Integration test proves the old snapshot is served throughout the run and the refined snapshot
   immediately after the switch.

_Maps to_: TD-086 · Roadmap Phase 8 / Continuous Discovery Loop.

== F2 — Scale-to-zero cold path: binary-first two-stage + IVF pruning (TD-087) [DEEP]

=== HLD

When compute scales to zero, the first query hits cold object storage. Instead of pulling the full
index (which can be hundreds of GB), load a compact 1-bit representation *first* (~1/30 the size),
serve a coarse two-stage search (binary filter → higher-fidelity rerank), IVF-prune to bound the
bytes touched per query, and lazily fetch the rerank tier. The recall envelope is gated and
disclosed.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph TB
    CQ["First query<br/>(cold, compute scaled from zero)"]
    L1["Load 1-bit binary tier first<br/>(~1/30 of full index)"]
    PR["IVF prune: nearest centroids<br/>(bounds bytes touched)"]
    S1["Stage 1: binary coarse filter"]
    S2["Stage 2: lazy-load rerank tier"]
    G{"RecallProbeGate<br/>open?"}
    FB["Fallback: non-quantized route<br/>(EXPLAIN degraded-route reason)"]
    OUT["Top-k with disclosed recall envelope"]

    CQ --> L1 --> PR --> G
    G -- open --> S1 --> S2 --> OUT
    G -- FAIL streak --> FB --> OUT

    style CQ fill:#e8f0fe,stroke:#2e5c8a,color:#000
    style OUT fill:#4a90e2,stroke:#2e5c8a,color:#000
    style FB fill:#f5f5f5,stroke:#999,color:#000
----

=== LLD

Reuse map:

[cols="1,3", options="header"]
|===
| Concern | Reuse

| Two-stage executor
| `UnifiedQuantizationEngine::calculate_progressive_distance()`
(`src/compute/quantization/quantization_engine.rs:324-401`) — already a selectivity cascade
(binary → INT8/PQ → FP32 rerank).

| 1-bit tier
| `BinaryQuantization` / `quantize_binary()` (same file, ~1062-1083; per-dimension sign, 32×
compression).

| Cold-load ordering (NEW)
| `ColdPathLoadPolicy` on `UniversalIndexStorage`
(`src/index/axis/storage/universal_index_storage.rs`) — fetch the binary tier before the full tier;
record the tier in `AxisSerializedIndexMetadata` (`.../serialization.rs`).

| Cluster pruning
| `UnifiedIvfIndex::find_nearest_centroids()` (`.../dual_store_ivf.rs:390-413`); the centroid store
is "inelastic" / always-resident — ideal for cold start.

| Gate wiring (also closes TD-075)
| Consult `RecallProbeGate::is_open` / a new `should_route_quantized()` at the quantized-route entry
in `AxisManager` (`src/index/axis/management/manager.rs`); FAIL streak → non-quantized fallback +
EXPLAIN degraded-route reason.
|===

=== Tasks

* [ ] *T2.1* Cold-path index profile: persist the binary tier + a metadata flag. → TD-087 (1)
* [ ] *T2.2* `ColdPathLoadPolicy`: binary-first load ordering in `UniversalIndexStorage`. → (1)
* [ ] *T2.3* Two-stage search wiring (binary coarse → rerank) + IVF prune. → (2)
* [ ] *T2.4* `RecallProbeGate` consulted at quantized-route entry in `AxisManager` (closes TD-075). → (3)
* [ ] *T2.5* Cold-start latency benchmark from object storage. → (4)

=== Success criteria

1. The first cold query loads ≤ ~1/30 of the full index bytes before serving begins.
2. Two-stage recall meets the configured floor, with the envelope disclosed per the TD-064 contract.
3. A gate FAIL streak forces a non-quantized fallback with a degraded-route reason in EXPLAIN.
4. Benchmark shows cold-start latency in the seconds class (not minutes) on an object-store-backed
   collection.

_Maps to_: TD-087 (and closes TD-075) · Roadmap Phase 8 / Elastic Compute And Scale-To-Zero.

== F3 — Point-read-optimized lake layout (I/O amplification) (TD-088) [DEEP]

=== HLD

A vector search returns IDs; serving then needs full records (text, metadata, permissions,
timestamps). Today the VIPER path deserializes a whole Parquet row group per point read — large I/O
amplification over object storage. Add a PAX **row-aligned column-group** mode so a point read
fetches only the needed columns' byte ranges, and evaluate the open **Vortex** format for
point-read-heavy collections. Serving (point reads) and discovery (scans) stay over *one logical
copy*.

=== LLD

Reuse map:

[cols="1,3", options="header"]
|===
| Concern | Reuse

| Block format
| `proximadb-block-format` (`crates/storage/proximadb-block-format/`). `BlockMode::Oltp` already
provides an O(log N) `RowDirectory::find_visible() -> row_index`; `ColumnMeta` already carries
`stripe_offset`/`stripe_len`. NEW: a column-group grouping so a point read reads only its group's
stripe byte ranges instead of all stripes.

| Fetch-path fix
| `VIPER internal_vector_by_id_with_path()` (`src/storage/engines/viper/engine.rs:1076+`) — replace
full-row-group deserialize with byte-range projection reads when backed by the PAX point-read
layout; keep the existing `CrossCacheOrchestrator` cache check.

| Vortex
| Evaluation spike only (no adoption commitment in this feature) — add behind an interoperability
shim and benchmark vs PAX-grouped and Parquet for both point reads and scans. Vortex is
Linux-Foundation, aligning with the open-format mandate.
|===

=== Tasks

* [ ] *T3.1* PAX column-group mode (writer + scanner). → TD-088 (1)
* [ ] *T3.2* Point-read byte-range projection in scanner (reuse `ColumnMeta.stripe_offset/len`). → (1)
* [ ] *T3.3* Wire VIPER / serving get-by-id to the point-read path. → (1)
* [ ] *T3.4* Vortex eval spike + benchmark. → (4)
* [ ] *T3.5* Micro-benchmark: bytes-downloaded-per-point-read (PAX-grouped vs Parquet). → (1)(2)

=== Success criteria

1. A point read fetches only requested columns' byte ranges — measured bytes downloaded ≪ a full
   row group.
2. Scan throughput is not regressed vs current PAX/Parquet.
3. One logical copy — no second physical store users must maintain.
4. The Vortex evaluation is documented with a go/no-go recommendation.

_Maps to_: TD-088 · Roadmap Phase 8 / Elastic Compute And Scale-To-Zero.

== F5 — External Collection: ProximaDB-owned indexes over external-resident data (TD-090) [DEEP]

=== HLD

Register an external lake table (Iceberg / Lance / Parquet) **un-copied and externally governed**;
ProximaDB builds and maintains vector + inverted (+ JSON) indexes over it that route through the same
retrieval path as native data. Index staleness vs source commits is declared; a source-commit
advance flips the projection to `RebuildRequired`. This lets F1's discovery loop cover lake data, not
just imported data. Principle (adopted verbatim): "One Data. One Index. No duplicated storage. No
dual-write pipelines."

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
graph LR
    EXT["External table (un-copied)<br/>Iceberg / Lance / Parquet"]
    REG["Register: authority=FederatedRead<br/>+ external ref + snapshot id"]
    RD["Source reader<br/>(catalog backend / arrow)"]
    VI["Build vector index<br/>(AxisVectorIndex over external snapshot)"]
    II["Build inverted index<br/>(FullTextIndex over external text)"]
    ST["Store indexes in ProximaDB<br/>(UniversalIndexStorage)"]
    RET["Retrieval: same path as native<br/>+ federated full-record fetch"]

    EXT --> REG --> RD --> VI --> ST
    RD --> II --> ST
    ST --> RET
    EXT -.->|full record on hit| RET

    style EXT fill:#f5f5f5,stroke:#999,color:#000
    style RET fill:#4a90e2,stroke:#2e5c8a,color:#000
----

=== LLD

Reuse map:

[cols="1,3", options="header"]
|===
| Concern | Reuse

| Authority
| `CatalogAuthorityMode::FederatedRead` / `ExternalAuthoritative` +
`CatalogStorageLayout::external_authoritative(...)` (`proximadb-catalog`); register external table
ref + snapshot id.

| Source reader
| Existing catalog backends (`src/catalog/iceberg.rs`; Parquet via arrow) feed the index build.

| External index (NEW)
| `ExternalCollectionIndex` decouples index build from local storage — today `AxisVectorIndex`
(`src/index/axis/index_factory.rs`) requires pre-ingested data. Add a remote source adapter that
reads vectors/records from the external snapshot, builds the index, stores it in ProximaDB
(`UniversalIndexStorage`), and never copies the source.

| Inverted index
| `FullTextIndex` / `build_index_from_chunks()`
(`src/storage/engines/core/formats/columnar/fulltext_index.rs`) over external text.

| Freshness
| `CatalogProjection` with `freshness_state` + `source_range` = external snapshot id; invalidation
policy flips to `RebuildRequired` when the source commit advances.

| Federated record fetch (gap today)
| Minimal retrieval-time path to return full records from the external source — reuse `src/query/`
distributed/federated patterns.
|===

=== Tasks

* [ ] *T5.1* ExternalCollection registration (authority + external ref + snapshot). → TD-090 (1)
* [ ] *T5.2* External source reader feeding the index build. → (2)
* [ ] *T5.3* Build vector index over external-resident data (decouple from local storage). → (2)
* [ ] *T5.4* Build inverted (BM25) index over external text (reuse `FullTextIndex`). → (2)
* [ ] *T5.5* Staleness vs source-commit + `RebuildRequired` on advance. → (4)
* [ ] *T5.6* Federated full-record fetch on retrieval. → (3)
* [ ] *T5.7* Integration test `tests/external_collection_index_in_place_test.rs`. → (6)

=== Success criteria

1. The external table is registered un-copied and remains externally governed.
2. ProximaDB-owned vector + inverted indexes are built over it.
3. Retrieval routes through the same path as native data and fetches full records from the source.
4. Index staleness vs source commits is declared; `RebuildRequired` triggers on source advance.
5. No duplicated storage / no dual-write.
6. Integration test passes (register external Parquet table → build index in place → search returns
   hits, no source copy).

_Maps to_: TD-090 · Roadmap Phase 8 / Continuous Discovery Loop. Gated for Lance corpora by F6.

== F4 — Elastic compute modes + resources-follow-data scheduler (TD-089) [OUTLINE]

=== HLD (outline)

Three compute modes over one logical store:

* `Long-running` — today's resident server (production serving, hot indexes preloaded).
* `OnDemand` — starts in seconds, minute-billed, for interactive / cold-data work.
* `OfflineBatch` — job-scoped, released on completion; the home for F1 discovery jobs.

A warm-pool scheduler attaches a pinned snapshot to prepared compute on request and releases it on
idle, while catalog/metadata stay queryable with **zero resident compute**. Built on
`ClusterManager` / `NodeInfo` (`src/cluster/`) by adding a `ComputeMode` and extending
`NodeInfo.capabilities` / `load` / heartbeat.

=== Dependency and deferral

BLOCKED on **TD-077** (distributed RPC transport / failover). Sequence after F1/F2/F3 make
scale-to-zero economical. This feature realizes the 2028 VISION "serverless deployment mode"
milestone.

=== Outline tasks

* [ ] `ComputeMode` enum + per-collection/per-request mode resolution.
* [ ] Warm-pool scheduler (attach pinned snapshot on request, release on idle).
* [ ] Metadata queryable with no resident compute.
* [ ] Cost/latency model documented.

=== Outline success criteria

1. Three modes operate over the same logical store.
2. Warm-pool attach-on-request / release-on-idle works.
3. Metadata is queryable with zero resident compute.
4. A cost/latency model is documented.

_Full LLD deferred to a follow-up spec once TD-077 lands._ _Maps to_: TD-089 · Roadmap Phase 8 /
Elastic Compute And Scale-To-Zero.

== F6 — Lance format interoperability (TD-091) [OUTLINE]

=== HLD (outline)

Add Lance as a read / import / external-index substrate alongside Iceberg/Delta/Hudi/Parquet (none
today — zero `lance` dependency in the workspace). A new catalog backend/reader plus the `lance`
crate dependency; gates F5 for Lance-resident corpora. Lowest priority of the six.

=== Outline tasks

* [ ] Add `lance` crate dependency (feature-gated).
* [ ] Lance reader (read path) + catalog registration as an external substrate.
* [ ] Import path (Lance → ProximaRecord) for the `ImportedSnapshot` authority mode.

=== Outline success criteria

1. Lance tables can be read / imported / used as an external-index substrate alongside the existing
   open formats.
2. F5 External Collection works over a Lance-resident corpus.

_Maps to_: TD-091 · Roadmap Phase 8 / Continuous Discovery Loop.

== Traceability

[cols="1,1,2,3", options="header"]
|===
| Feature | TD | Roadmap Phase 8 row | Reused primitives (anchors)

| F1 Discovery loop | TD-086 | Continuous Discovery Loop | GlobalManifest, ProjectionFreshnessState, merge.rs, CatalogStorageLayout, BackgroundMaintenanceManager, automl
| F2 Cold path | TD-087 (+TD-075) | Elastic Compute And Scale-To-Zero | calculate_progressive_distance, BinaryQuantization, UnifiedIvfIndex, UniversalIndexStorage, RecallProbeGate
| F3 Point-read | TD-088 | Elastic Compute And Scale-To-Zero | proximadb-block-format (RowDirectory/ColumnMeta), VIPER fetch path
| F5 External Collection | TD-090 | Continuous Discovery Loop | CatalogAuthorityMode, catalog backends, AxisVectorIndex, FullTextIndex, UniversalIndexStorage
| F4 Elastic compute | TD-089 | Elastic Compute And Scale-To-Zero | ClusterManager/NodeInfo (blocked on TD-077)
| F6 Lance | TD-091 | Continuous Discovery Loop | open-format catalog backends
|===

== Verification strategy

For the eventual implementation (not part of this spec task):

* *F1*: `tests/discovery_job_atomic_republish_test.rs` — insert → run a dedup job → assert serving
  reads the old snapshot during the run and the deduped snapshot after the atomic switch; assert the
  freshness transition and EXPLAIN lineage.
* *F2*: cold-start latency bench (object-store-backed) + recall-floor assertion + a gate-FAIL
  fallback test.
* *F3*: bytes-downloaded-per-point-read micro-benchmark (PAX-grouped vs Parquet) + a scan-throughput
  no-regression bench.
* *F5*: `tests/external_collection_index_in_place_test.rs` — register an external Parquet table,
  build the index, search, assert no source copy + `RebuildRequired` on source advance.
* *F4 / F6*: outline-level acceptance only; full verification deferred with their LLDs.

== Scope and non-goals of this specification

* This document is a specification only. It defines no durability contract and is not an ADR.
* F4 and F6 are intentionally outline-only; their full LLDs are deferred (F4 until TD-077 lands).
* No rebrand to "Vector Lakebase" — "The Context Database" remains the category (see the alignment
  doc). Phase 8 internalizes the continuous-loop and elastic-compute ideas without changing the
  category.

'''

_Author_: architecture, 2026-05-28. _Status_: feature specification (HLD/LLD), trajectory input.
_Derives from_: `VECTOR_LAKEBASE_ALIGNMENT_2026_05_28.adoc`. _Feeds_: implementation of TD-086..091.
