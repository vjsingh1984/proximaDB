= Architecture Overview
:toc: left
:toclevels: 2
:icons: font

[.lead]
ProximaDB is a multi‑model database that unifies vectors, documents, graphs, and observability data behind a single data plane.

NOTE: This developer-facing overview is a feeder of the apex
link:../12-design/SYSTEM_MAP_2026_05_30.adoc[SYSTEM_MAP_2026_05_30] single-pane-of-glass index. Use the
apex for the whole-system view; this doc stays focused on the high-level overview and request flows.

== System Diagram

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
flowchart TB
  subgraph API[API Layer]
    REST[REST]
    GRPC[gRPC]
    PG[Postgres Wire]
  end

  subgraph Services[Service Layer]
    VEC[Vector Ops]
    DOC[Document Ops]
    GRAPH[Graph Ops]
    OBS[Observability Ops]
    TS[Time-Series Ops]
    EVT[Event Store Ops]
    UQ[Unified Query]
  end

  subgraph DataPlane[Data Plane]
    WAL[Unified WAL]
    MEM[Memtables]
    ENG[Vector Engines]
    OBSSTORE["Observability Storage<br/>(partitioned logs, time-series, traces)"]
    TSENG["Time-Series Engine<br/>(TST, columnar, time-partitioned)"]
    EVTENG["Event Sourcing Engine<br/>(append-only, indexed)"]
    BM25["Hybrid Search<br/>(BM25 + Vector RRF)"]
  end

  subgraph ControlPlane[Control Plane]
    META[Catalog + Schemas]
    METRICS[Metrics + Tracing]
    SECURITY[Auth + RBAC]
  end

  API --> Services --> WAL --> MEM
  MEM --> ENG
  MEM --> OBSSTORE
  MEM --> TSENG
  MEM --> EVTENG
  Services --> ControlPlane
----

== Core Principles

* **Unified data plane**: vector, document, and observability writes share WAL semantics.
* **Pluggable engines**: storage engines are optimized for different workloads.
* **Model adapters**: each model (vector/document/graph/obs) has a dedicated service layer.
* **Developer‑first**: REST + gRPC are primary; pgwire is experimental.
* **Time-series-native**: first-class time-partitioned storage with ASOF joins and downsampling.
* **Event-sourcing-native**: append-only immutable event logs for compliance and audit trails.

== Request Flow (Write Path)

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
sequenceDiagram
  participant C as Client
  participant API as API Layer
  participant WAL as Unified WAL
  participant MEM as Memtable
  participant ENG as Storage Engine

  C->>API: Insert (vector/doc/log)
  API->>WAL: Append
  API->>MEM: Apply
  API-->>C: Ack
  MEM->>ENG: Flush/compact (async)
----

== Request Flow (Query Path)

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
sequenceDiagram
  participant C as Client
  participant UQ as Unified Query
  participant V as Vector Service
  participant D as Document Service
  participant G as Graph Service
  participant O as Observability

  C->>UQ: Multi‑model query
  UQ->>V: Vector search
  UQ->>D: Document filter
  UQ->>G: Graph traversal
  UQ->>O: Log/metric query
  UQ-->>C: Fused result
----

== Target 8-Model Architecture

ProximaDB's vision extends the current 4-model architecture (vector, graph, document, observability) to 8 models. Status reflects the v0.2 support tiers in link:../SUPPORTED_SURFACE.adoc[SUPPORTED_SURFACE.adoc] (Supported/Beta/Planned); the engines listed are the supported set (SWIFT/RAPTOR are experimental and omitted):

[cols="1,2,2,1", options="header"]
|===
| Model | Engine | Use Case | Status

| *Vector*
| SST, VIPER, HELIX, NOVA + AXIS (HNSW/IVF) indexes
| Similarity search, embeddings, RAG
| Supported

| *Graph*
| ORION graph runtime over relational/storage substrate
| Relationships, traversals, knowledge graphs
| Beta

| *Document*
| WAL-backed JSON store
| Structured data, metadata, state management
| Beta

| *Observability*
| Partitioned logs, time-series metrics, trace spans
| Monitoring, SIEM, audit
| Beta

| *Time-Series*
| TST (Time-Series Tree)
| OHLC bars, IoT sensors, metrics, streaming data
| Beta (engine complete; productization in progress)

| *Event Sourcing*
| Append-only WAL with indexed queries
| Audit trails, compliance, CDC, order history
| Beta (engine complete; productization in progress)

| *Relational*
| DataFusion-backed SQL tables
| Structured queries, joins, aggregations
| Foundation exists (full RDBMS parity post-MVP, TD-110)

| *Media*
| Multi-modal embeddings (CLIP, ImageBind)
| Image, audio, video search
| Planned

| *Agentic*
| RUBICON / AQL (Stonebraker Design)
| Auditable agentic plans, Text-to-AQL, traceability
| Research (P5)
|===

== Federated Query Engine

The FederatedQueryContext enables cross-model SQL queries with specialized extensions.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
flowchart TB
  subgraph Client[Client Layer]
    REST[REST /api/v2/query]
    GRPC[gRPC ProximaRecordService]
    PGWIRE[PostgreSQL Wire :5433]
  end

  subgraph Parser[FederatedParser]
    SQL[SQL Parser]
    EXT[Extension Detection]
    PLAN[Query Plan]
  end

  subgraph Optimizer[CrossModelOptimizer]
    COST[Cost Model]
    REORDER[Join Reordering]
    PUSH[Predicate Push]
  end

  subgraph Executor[FederatedExecutor]
    PAR[Parallel Execution]
    ARROW[Arrow RecordBatch]
  end

  subgraph Models[Data Models]
    VEC[Vector Engine]
    DOC[Document Service]
    GRAPH[Graph Engine]
    OBS[Observability Store]
  end

  Client --> Parser --> Optimizer --> Executor
  Executor --> Models
  VEC --> ARROW
  DOC --> ARROW
  GRAPH --> ARROW
  OBS --> ARROW
  ARROW --> Client
----

[NOTE]
====
Regardless of entry point, ranking is owned by a *single shared `FusionService`*. Vector
`/search`, hybrid dense+BM25 retrieval, and graph `fusion-search` all delegate to one fusion
engine (calibrated candidate-set fusion over the `oid`-keyed seam). Per-surface fusion is an
anti-pattern — each model service above is a *source of candidates*, not a ranker. See
link:hybrid-fusion.adoc[Hybrid Fusion] and
link:../12-design/SEARCH_SURFACE_CONTRACT_2026_06_24.adoc[Search Surface Contract].
====

=== SQL Extensions

ProximaDB extends SQL with functions that route to the appropriate data model:

|===
| Extension | Syntax | Purpose

| `VECTOR_SEARCH` | `VECTOR_SEARCH(collection, vector, top_k)` | Similarity search
| `<->` operator | `embedding <-> '[...]'::vector` | pgvector-compatible distance
| `GRAPH_QUERY` | `GRAPH_QUERY('MATCH (a)-[:REL]->(b) ...')` | Cypher traversal
| `DOCUMENT_QUERY` | `DOCUMENT_QUERY(collection, filter)` | JSON document query
| `LOGS` | `LOGS(namespace)` | Log query
| `METRICS` | `METRICS(namespace)` | Metric query
|===

=== Cross-Model Query Example

[source,sql]
----
-- Find products similar to user preferences, get reviews, check inventory via graph
SELECT u.name, v.product_id, v.score, d.review_text, g.in_stock
FROM users u
JOIN LATERAL VECTOR_SEARCH('products', u.preference_vector, 10) v ON true
JOIN LATERAL DOCUMENT_QUERY('reviews', 'product_id = "' || v.product_id || '"') d ON true
JOIN LATERAL GRAPH_QUERY('MATCH (p:Product {id: "' || v.product_id || '"})-[:STOCKED_IN]->(w:Warehouse) RETURN w.quantity > 0 as in_stock') g ON true;
----

=== Result Fusion Strategies

When combining results from multiple models, the engine supports:

* **Intersection** (AND) - Records must match all sub-queries
* **Union** (OR) - Records from any sub-query
* **Reciprocal Rank Fusion (RRF)** - Weighted rank combination
* **Ranked Fusion** - Custom weights per model

== Hybrid Search Architecture

Hybrid search combines BM25 keyword matching with vector similarity for improved retrieval accuracy.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
flowchart LR
    Q[Query] --> BM25[BM25 Index\n(tantivy)]
    Q --> VEC[Vector Search\n(engine)]
    BM25 --> RRF[Reciprocal Rank\nFusion]
    VEC --> RRF
    RRF --> RES[Ranked Results]
----

=== Fusion Strategies

* **Reciprocal Rank Fusion (RRF)**: Default. Combines ranked lists without score normalization. Robust across different score distributions.
* **Weighted Linear**: User-specified weights for BM25 vs vector scores. Useful when one signal is known to be more reliable.
* **Learned Fusion**: RL-based weight optimization using the existing Thompson Sampling planner.

=== Integration Points

* BM25 index maintained alongside vector index per collection
* Queries can specify `search_mode`: `vector_only`, `keyword_only`, `hybrid` (default)
* Filter predicates applied before fusion for efficiency

== CQRS and Event Sourcing Pattern

For workloads like algorithmic trading, ProximaDB supports Command Query Responsibility Segregation (CQRS) combined with Event Sourcing.

[source,mermaid]
----
%%{init: {"theme": "neutral"}}%%
flowchart TB
    subgraph Commands[Command Path]
        CMD[Write Command] --> VAL[Validation]
        VAL --> EVT[Event Store\n(append-only)]
        EVT --> PROJ[Projections]
    end

    subgraph Queries[Query Path]
        QRY[Read Query] --> CACHE[Cache Layer]
        CACHE --> MAT[Materialized Views]
        MAT --> IDX[Indexes]
    end

    PROJ --> MAT
    EVT --> CDC[CDC Stream]
----

=== Key Properties

* **Immutability**: Events are append-only. No updates or deletes. Required for MiFID II and SEC compliance.
* **Projections**: Materialized views are derived from the event stream. Can be rebuilt from scratch.
* **Temporal Queries**: Query state at any point in time by replaying events up to a timestamp.
* **CDC Integration**: Event stream can be consumed by external systems via CDC sinks (Kafka, webhooks).

== Current Implementation Notes

* **Phase 1 Complete**: SST is Supported; VIPER/HELIX/NOVA are Beta; SWIFT/RAPTOR are Experimental. WAL, APIs, quantization, and AXIS indexes are present at the tiers listed in the support contract.
* **Phase 2 Complete**: Cross-model queries, graph database (ORION), document store, observability.
* **Phase 3 In Progress**: External catalogs, streaming/CDC, distributed operations.
* **Phase 4 Foundations Beta**: Time-series (TST) and event-sourcing engines are complete; hybrid search (BM25) is wired. Product packaging, API parity, and at-scale validation are in progress -- these are Beta surfaces in v0.2.
* Document and observability services use WAL‑backed recovery.
* Unified query execution supports VECTOR_SEARCH, GRAPH_QUERY, DOCUMENT_QUERY, LOGS, METRICS.

== Stability Matrix

*Status* below reflects implementation stability in the working tree; the customer-facing
support tier (Supported/Beta/Experimental) is governed by
link:../SUPPORTED_SURFACE.adoc[SUPPORTED_SURFACE.adoc] and wins for any product claim.

|===
| Area | Phase | Status | Notes

| Vector engine + filters | P1 | ✅ Stable | 4 supported engines (SST/VIPER/HELIX/NOVA), AXIS (HNSW/IVF) indexes
| Document storage | P2 | ✅ Stable | WAL-backed, JSON path queries
| Graph traversal | P2 | ✅ Stable | ORION, WAL persistence
| Observability logs | P2 | ✅ Stable | WAL-backed, partitioned
| Observability metrics | P2 | ✅ Stable | Time-series aggregation
| Unified query | P2 | ✅ Stable | Cross-model SQL
| External catalogs | P3 | 🔄 In Progress | Iceberg, Delta Lake
| Distributed ops | P3 | 🔄 In Progress | Raft, sharding
| Hybrid search (BM25) | P4 | Beta | tantivy integration; Beta in v0.2
| Time-series engine | P4 | Beta | TST engine complete; productization in progress
| Event sourcing | P4 | Beta | Append-only WAL; productization in progress
| Agentic Intelligence | P5 | 📅 Planned | Research frontier (arXiv:2604)
|===

See link:vision.adoc[Vision Document (legacy)] and link:../00-product/VISION.adoc[Refined Vision Document] for phase definitions.
