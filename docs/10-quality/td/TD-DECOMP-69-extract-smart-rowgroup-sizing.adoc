// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-69: Move raptor smart_rowgroup_sizing into proximadb-engine-core

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export (1 shim + 1 dep add)
| Component | `crates/storage/proximadb-engine-core`; root `src/storage/engines/raptor/mod.rs`
|===

== What moved
`src/storage/engines/raptor/smart_rowgroup_sizing.rs` (32 tests: SmartRowGroupSizer,
BalancedMatrixTrinitySizing, OptimalRowGroupSize, CloudIOProfile, DataTemperatureTier,
CommonConfigurations — cloud rowgroup sizing heuristics for S3/GCS/ADLS/NVMe) → the EXISTING
storage crate `proximadb-engine-core`.

Phase 3 Step 1b quick win (plan: engine-level extraction). Fully hermetic leaf: ZERO `crate::`
refs, `Debug, Clone` derives only, deps `anyhow` + `proximadb-quantization-model` (foundation,
clean storage→foundation edge). All consumers live inside raptor (`rowgroup_manager`,
`consolidated_compactor`, `engine`, `mod`) and keep working through the shim.

Root `raptor/mod.rs`: `pub mod smart_rowgroup_sizing;` →
`pub use proximadb_engine_core::smart_rowgroup_sizing;` (the parent's `pub use
smart_rowgroup_sizing::{...}` re-export block is unchanged). Root Cargo.toml already carried
the engine-core dep — zero manifest churn. Engine-core grows 951 LOC / 32 tests; crate now
100 tests total.

Verified: engine-core standalone `--all-targets` ✓ (46s), root `cargo check -p proximadb --lib`
✓ (6m14s), boundary checker `--strict` No findings, `cargo fmt --check` ✓, nextest 100/100 ✓.
SIFT RUNS (storage_changes) under the 45m cap.
