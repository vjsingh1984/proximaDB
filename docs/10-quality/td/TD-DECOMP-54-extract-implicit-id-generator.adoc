// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-54: Extract implicit_id_generator → new proximadb-implicit-id-generator crate (horizontal)

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export
| Component | `crates/horizontal/proximadb-implicit-id-generator`; root `src/storage/engines/core/formats/columnar/parquet_write_engine/mod.rs`
|===

== What moved
`src/storage/engines/core/formats/columnar/parquet_write_engine/implicit_id_generator.rs` (5 tests:
`IdLessLookup` + `generate_implicit_id` for synthesizing record IDs) → new horizontal crate
`proximadb-implicit-id-generator`. Root `parquet_write_engine/mod.rs` re-exports it
(`pub use proximadb_implicit_id_generator::implicit_id_generator;`), so the existing
`pub use implicit_id_generator::IdLessLookup;` resolves unchanged (consumer: `examples_test.rs`).
Deps: anyhow only (zero `proximadb_*` deps, zero `crate::` climb-out, `super::` test-only). Verified:
standalone + clippy -D warnings + root lib all pass. SIFT recall ratchet RUNS (storage_changes) under
the 45m cap (#1555).
