// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-43: Extract connectors types → new proximadb-connectors-types crate (horizontal)

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export
| Component | `crates/horizontal/proximadb-connectors-types`; root `src/connectors/mod.rs`
|===

== What moved
`src/connectors/types.rs` (7 tests) → new horizontal crate `proximadb-connectors-types`.
Root `connectors/mod.rs` re-exports it (`pub use proximadb_connectors_types::types;`), so the
existing `pub use types::{ColumnStatistics, Statistics, TableInfo, TableStatistics, WriteResult};`
resolves unchanged for every connector impl (DuckDB, Spark, …). Deps: arrow + serde only
(zero `proximadb_*` deps, zero `crate::` climb-out, `super::` test-only). Verified: standalone +
clippy -D warnings + root lib all pass. SIFT recall ratchet SKIPS (connectors/** is not
storage_changes).
