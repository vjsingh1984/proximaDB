// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-56: Extract document compression → new proximadb-document-compression crate (horizontal)

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export
| Component | `crates/horizontal/proximadb-document-compression`; root `src/storage/document/storage/mod.rs`
|===

== What moved
`src/storage/document/storage/compression.rs` (5 tests: `DocumentCompressor` per-field codec selection
+ lz4 helpers) → new horizontal crate `proximadb-document-compression`. Root
`storage/document/storage/mod.rs` re-exports it (`pub use proximadb_document_compression::compression;`),
so the existing `pub use compression::DocumentCompressor;` resolves unchanged. Deps: anyhow + lz4_flex
(zero `proximadb_*` deps, zero `crate::` climb-out, `super::` test-only). Verified: standalone + clippy
-D warnings + root lib all pass. SIFT recall ratchet RUNS (storage_changes) under the 45m cap (#1555).
