// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-51: Move pgwire types into existing proximadb-pgwire-translator crate

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export (+2 crate-local deps)
| Component | `crates/horizontal/proximadb-pgwire-translator`; root `src/network/postgres/mod.rs`
|===

== What moved
`src/network/postgres/types.rs` (6 tests: pgwire type conversions) → the EXISTING horizontal crate
`proximadb-pgwire-translator` (TD-DECOMP-38), joining `translator`. Root `network/postgres/mod.rs`
re-exports it (`pub use proximadb_pgwire_translator::types;`), so `crate::network::postgres::types::*`
consumers resolve unchanged. Self-contained (0 `crate::`, 0 translator cross-refs). Deps: uuid + hex,
both added crate-locally (neither is in `[workspace.dependencies]` — same gotcha as TD-44/47).
NO new crate, NO root `Cargo.toml` wiring → no stacked-PR conflict. Verified: standalone + clippy
-D warnings + root lib all pass. SIFT recall ratchet SKIPS (network/** is not storage_changes).
