// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-68: Extract multimodel_codec → new proximadb-multimodel-codec crate (horizontal)

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export
| Component | `crates/horizontal/proximadb-multimodel-codec`; root `src/network/arrow_ipc/mod.rs`
|===

== What moved
`src/network/arrow_ipc/multimodel_codec.rs` (12 tests) → new horizontal crate
`proximadb-multimodel-codec`. Root `arrow_ipc/mod.rs` re-exports it. Deps: arrow-schema +
proximadb-catalog-schema (the TD-67 foundation crate — first consumer of the schema-type
extraction to benefit from the clean downward edge). Zero crate:: climb-out, zero sibling deps
(free functions only). SIFT SKIPS (network/** is not storage_changes).

== Phase 3 significance
First extraction to use `proximadb-catalog-schema` (foundation) instead of `proximadb-catalog`
(control) — proving the TD-67 schema-type extraction works as designed for horizontal consumers.
