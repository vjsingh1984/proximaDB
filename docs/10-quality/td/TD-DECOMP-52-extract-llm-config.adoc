// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-52: Extract llm config → new proximadb-llm-config crate (horizontal)

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export
| Component | `crates/horizontal/proximadb-llm-config`; root `src/llm/mod.rs`
|===

== What moved
`src/llm/config.rs` (5 tests: LLM/RAG/embedding-provider/semantic-cache configuration structs) → new
horizontal crate `proximadb-llm-config`. Root `llm/mod.rs` re-exports it (`pub use proximadb_llm_config::config;`),
so the existing `pub use config::{EmbeddingProvider, LLMConfig, RAGConfig, SemanticCacheConfig};` resolves
unchanged. Deps: `proximadb-config` only (zero `crate::` climb-out, `super::` test-only, zero external
crates — pure type defs over the config foundation crate). Verified: standalone + clippy -D warnings +
root lib all pass. SIFT recall ratchet SKIPS (llm/** is not storage_changes).
