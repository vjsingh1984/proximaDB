// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-OLAP-2: Wire the statistics substrate into the planner (real CBO + cardinality/selectivity)

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Open (first slice **landed 2026-07-04**: link:TD-DFR-1-columnstatistics-for-datafusion.adoc[TD-DFR-1] wired footer-derived `ColumnStatistics` + registry NDV *overlay* to the DataFusion provider). **A2 landed 2026-07-11**: the registry was designed but never POPULATED in production (the overlay read an empty registry ⇒ `distinct_count = Absent` ⇒ `EnforceDistribution` fell back to a single-partition, single-core hash-join build — the measured 100-700× gap vs DuckDB at SF=0.1). `publish_ndv_statistics` (`src/services/dml/mod.rs`) now folds the materialized `ProximaRecord`s into per-column HLL sketches at the write boundary (zero new scan — the records are already resident for the Parquet write) and publishes them keyed by table name — the SAME key `ObjectStoreParquetTable::statistics()` passes to `statistics_from_splits` as `collection` (and the empty-delta OLAP-merge fast path falls back to that reader, so the read-mostly benchmark path is covered). The reverted A1 `num_rows`-as-NDV proxy overestimated low-cardinality columns (`l_returnflag` NDV=2 → proxy=60K) and selected an exploding join plan at SF=0.01; the HLL carries the true per-column distinct count (±~2%). This TD continues with the ADR-042 generalization, native-planner cardinality, histograms, and stats invalidation.
| Priority | **P0a** — cheapest unlock in the ADR-052 execution order; feeds cost routing (ADR-050) and runtime filters (TD-OLAP-3). DIP: the planner consumes the ADR-042 `SegmentStatistics` port, never engine internals
| Severity | High — without stats the planner is blind; runtime filters (TD-OLAP-3) and cost routing (ADR-050) both depend on it
| Component | `src/core/statistics/{summary,sketches,registry}`, `src/datafusion/schema_inference.rs`, `src/query/query_optimizer.rs`, `src/query/federated/optimizer/`
| Governs | link:../../12-design/adr/ADR-052-analytics-execution-competitiveness-codesign.adoc[ADR-052] (invariant 3); closes the ADR-037/038 designed-only gap
| Relates to | ADR-037 (statistics substrate), ADR-038 (freshness coherence), ADR-050 D2 (DataFusion ColumnStatistics), ADR-042 (mergeable SegmentStatistics), TD-174/175
|===

== Why

ADR-037/038 specify a mergeable statistics envelope; `src/core/statistics/{summary,sketches}`
already implement min/max, NDV (HyperLogLog), TDigest quantiles, null-count, and a registry.
Its consumers today are the pgwire EXPLAIN selectivity surface (`relational_pipeline.rs` →
`statistics_registry().equality_selectivity`) and the federated optimizer
(`optimizer/{selectivity,plan_calibration}.rs`) — but none of it reaches **cost-based physical
planning**: `schema_inference.rs` returns `ColumnStatistics::new_unknown()` (its
`extract_statistics` has no call site; table providers default `statistics: None`), so the
DataFusion planner plans blind, and the native planner has no cardinality model. Snowflake's
signature posture is *"all statistics used for optimization are automatically maintained"* and
a no-indices Cascades CBO; Spark AQE/DPP depend on runtime stats. A competitive engine cannot
ship a statistics-blind planner.

== Rollout

. **Close ADR-050 D2 (= TD-DFR-1):** project the ADR-042 `SegmentStatistics`
  (min/max/distinct/null) into DataFusion `ColumnStatistics` in `schema_inference.rs`
  (replace `new_unknown()` and actually wire `extract_statistics` to the table providers) —
  zero new scan; the stats substrate's next consumer.
. **Persist + freshness:** stamp per-segment stats with the ADR-038 freshness watermark; merge
  up the segment→row-group→table hierarchy; invalidate on compaction / DV application.
. **Cardinality estimation:** feed NDV + TDigest histograms into the federated optimizer's
  selectivity/cardinality code (`src/query/federated/optimizer/`) so join ordering and filter
  costing use real distributions, not defaults.
. **Histograms for joins:** add equi-depth/equi-width histograms for join-column selectivity
  (today only TDigest quantiles exist).
. **Calibration:** wire the TD-158 observed-cost trace back as a cardinality correction ratio
  (infrastructure exists in the federated optimizer; tune it).

== Verification

* A plan-explain diff shows non-`unknown` cardinality on TPC-DS tables.
* Join-order changes measurably on skew-sensitive TPC-DS queries (Q72/Q95-class) once stats
  land, with conformance ratchets unchanged.
* Freshness: a write advances the watermark; a stale-stat query is flagged in EXPLAIN.
