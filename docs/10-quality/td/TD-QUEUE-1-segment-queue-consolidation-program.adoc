// Copyright (C) 2026 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-QUEUE-1: Consolidate the siloed EventLog/queue constructs onto one segment-based queuing primitive
:status: In Progress
:date: 2026-07-28
:dim: D1 (object storage — one prefix-LIST recovery per topic) + D4 (compute-per-modality)
:pillar: OE
:realizes: ADR-079 ; executes ADR-072 / TD-FOUNDATION-1
:relates-to: TD-EVENTLOG-1 (resolved by Q2/Q4), TD-AXIS-3 S2 (absorbed by Q3), ADR-078 (AXIS convergence), ADR-071 (WAL stays separate)

[cols="1,3"]
|===
| Field | Value

| Status
| **In Progress** — Q0 (reposition + audit) landing first. Each phase is an independently
  shippable PR; migrations are mixed-read-safe per mandate #8.

| Priority
| P2 — the silos are functional but three carry verified correctness defects (restart data
  loss, swapped-args no-op, false metric) and all five duplicate one primitive.

| Component
| `crates/horizontal/proximadb-queue/` (the primitive, repositioned from `crates/modalities/`),
  and the silos it absorbs: `src/index/axis/eventlog/`, `src/storage/engines/eventlog/`,
  `src/services/events/`.
|===

toc::[]

== 1. Problem

Five separate append-only log / queue implementations each re-roll sequence-counter + path
scheme + recovery scan + in-memory index on the raw `FileSystem`, and drift on correctness
(see ADR-079 §Context for the evidence). Meanwhile `proximadb-queue` already centralizes the
segment-based, per-topic-directory, producer/consumer, three-tier shape — **complete and
tested (74 tests)** — but was never adopted for internal use, which is precisely why the
silos exist.

== 2. Work

[cols="1,3,1"]
|===
| Slice | Scope | Status

| **Q0**
| **Reposition + audit (no behavior change).** Move `proximadb-queue` → `crates/horizontal/`
  (leaf; path-based layering auto-re-maps it). Update the 4 path refs (workspace `Cargo.toml`
  member + dep, `clients/python-queue-embedded/{pyproject.toml,README.md}`). Reconcile the
  stale "Phase 1B scaffold" docs with what the 74 tests prove (disk/object/recovery/leases
  all work). File ADR-079 + this TD.
| landing

| **Q1**
| **Group-scoped pub/sub (Kafka consumer-group semantics).** Key leases + offsets by
  `(group, topic, partition)` — `{root}/{topic}/{partition}/{group}/{lease,offset}.meta`.
  Exclusive within a group (competing consumers), independent across groups (fan-out). Tests:
  two groups each consume all messages on the same partition; one group's ack does not
  advance the other's offset; recovery resumes each group's cursor. Needed before Q2 (audit
  fan-out). On-disk format change ⇒ mandate #8 gating (low blast radius — no production
  consumers yet).
| next

| **Q2**
| **Migrate the audit log → `audit` topic.** Mixed-read-safe: read legacy `event_NNNN.bin`
  **and** `.qseg`; default-OFF. Fixes TD-EVENTLOG-1's restart data loss *structurally*
  (persisted sequence survives restart by construction). Regression: reconstruct over a
  `base_dir`, assert prior appends survive. Both `file://` and an object-store scheme.
| next

| **Q3**
| **Migrate AXIS coordination → `axis-index` topic.** Absorbs TD-AXIS-3 S2 — the shared-types
  extraction + queue-runtime retirement becomes "publish to the `axis-index` topic." The
  swapped-args no-op + false `successful_migrations` metric go away by removal. Recall
  ratchet unchanged (coordination, not indexing logic).
| next

| **Q4**
| **`agent-state` topic + `ConditionalKeyStore`.** MCP checkpoint → `agent-state` keyed by
  `agent-checkpoint:{tenant}:{ns}:{thread}` via `put_if_absent` (ADR-072). Resolves
  TD-EVENTLOG-1 end-state **and** the entity re-key in one move. Queue delivers ordered
  appends; ConditionalKeyStore serves point lookups.
| next
|===

== 3. Verification

Compile is not the gate here — the migrations change durability/recovery paths.

* **Per migrated topic (mandate #8):** round-trip (produce → consume → assert order + payload)
  + crash-recovery (produce → kill → reopen → resume → no loss/dup) on **both** `file://`
  and an object-store scheme (the `FileSystem::list` shape hazard already bit the audit-log
  recovery scan once).
* **Defect-becomes-regression tests:** (a) audit appends survive restart over the same
  `base_dir`; (b) AXIS compaction publishes once, no double-index; (c) MCP checkpoint
  get-by-thread returns persisted state after restart.
* Layering + workspace-boundary guards green after the move; `cargo build -p proximadb-server`
  (mandate #11); env-gate registry for any new `PROXIMADB_QUEUE_*` gate.
* Recall ratchet unaffected (queue is coordination).

== 4. Residual

The collection WAL stays separate (ADR-071) — it is the write-path durability floor, not the
async/coordination plane this primitive covers. The user-facing Queue *modality* is
unaffected (it shares the primitive; no client-visible API change).
