// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-V1SUNSET-2: VectorRecord → ProximaRecord — scoped first migration slice (ADR-024 Tier-3 kick-off)
:toc: macro
:icons: font
:last-updated: 2026-08-05

[cols="1,3", options="header"]
|===
| Field | Value

| Status | **Scoped (execution plan; implementation not started).** This TD is the *plan* for the first
safe slice — it migrates nothing. The first slice (S1) is internal-only and needs no flag; later
durable-path changes are default-OFF until baked.
| Severity | Medium — `VectorRecord` is a v1 proto *wire* type (`proximadb.v1.rs:2011`) **and** a legacy
durable format, so this is a link:../../12-design/adr/ADR-024-single-canonical-data-type-and-schema.adoc[ADR-024]
storage-format migration: mixed-read-safe, default-OFF for durable changes, round-trip + recall
tested (CLAUDE.md mandate #8). Recovery for S1 is a revert of an internal-only cluster.
| Component | Data model / storage: `VectorRecord` callers across `src/storage/engines/**`, `src/core/**`,
`src/index/**`, `src/services/**`; the v1↔v2 bridge
(`crates/foundation/proximadb-records/src/conversions.rs`); the legacy SST1 row format
(`src/storage/engines/sst/mod.rs` — `SstEntry`, retired 2026-08-05).
| Tracked-by | link:../../12-design/adr/ADR-024-single-canonical-data-type-and-schema.adoc[ADR-024] (the
canonical-type decision); link:../../12-design/V1_RETIREMENT_ROADMAP_2026_07_08.adoc[V1_RETIREMENT_ROADMAP]
Tier 3; the v1-proto ratchet (`scripts/check_v1_proto_usage.py`).
| Depends-on | The existing bidirectional conversion bridge (present — verified). The durable *write*
convergence to `ProximaRecord`-native PAX (ADR-049 M1-3, landed). Does **not** depend on the REST/gRPC
sunset (that is the sibling link:TD-V1SUNSET-1-rest-grpc-sunset-cutover.adoc[TD-V1SUNSET-1]); the two
are complementary, not sequenced.
|===

== Why this exists

`VectorRecord` is the **single largest remaining v1-proto slice** and the Tier-3 long pole the
retirement roadmap defers to ADR-024. The roadmap classifies it "deeply embedded … NOT disable-able"
and points here for the *first* migration step. This TD scopes that step: it classifies every
`VectorRecord` caller into internal-only (safe to migrate first) vs wire-format/persistent (needs the
mixed-read-safe gating), names a recommended first slice, and lands the reusable test harness — without
migrating any production code.

Measured on `develop` (2026-07-09):

[cols="1,2,2", options="header"]
|===
| Root | VectorRecord refs | Files

| `src`     | ~900 | 150 (~129 prod, ~21 test/bench)
| `tests`   | ~269 | 57
| `crates`  | ~59  | 14
| `clients` | Python v1 SDK + tests (Bucket F) | ~109
|===

`VectorRecord` is ~900 of the ~2248 `src` v1-proto ratchet references — roughly **40%** of the
remaining v1 metric, and the dominant lever for lowering TD-123's count.

`VectorRecord` is a `prost` v1 message (`id`, `vector: Vec<f32>`, flat
`metadata: HashMap<String, SqlValue>`, ms-granularity timestamps, `version`, `source`): **FP32-only**
(no quantization), flat metadata (no NF² property tree). The target `ProximaRecord` is richer — ns
timestamps, multi-tenancy/RLS, `ProximaTree` properties, multi-precision `EmbeddingCell`
(`Fp32`/`Fp16`/`Bf16`/`Int8Scalar`/`UInt8Scalar`), graph `edge`/`refs`. Because it is both a wire type
and a serialized durable form, it cannot be a big-bang edit.

== Current state (verified on `develop`, 2026-07-09)

The decisive finding — it reshapes the risk of the whole effort:

[cols="1,4", options="header"]
|===
| Layer | Status

| **WAL** | Already `ProximaRecord`-native (`wal_operations.rs`: `record: ProximaRecord` in
`VectorOperation`/`DocumentOperation`/`TimeSeriesOperation`). **Zero** `VectorRecord` in the WAL.

| **PAX durable format** (live flush default, ADR-049 M1-3) | **Already `ProximaRecord`-native.**
`crates/storage/proximadb-block-format/src/record.rs` is "ProximaRecord ↔ PAX column projection";
`writer.rs:305 write_pax_segment<I: Iterator<Item = ProximaRecord>>`; `flush/mod.rs` is entirely
`ProximaRecord`. New writes never serialize `VectorRecord`.

| **Legacy SST1 row format** (`SstEntry`, `.sst`) | ⚠️ **SUPERSEDED 2026-08-05 — `SstEntry` is RETIRED;
see <<reconciliation-2026-08-05>>.** Historical description: carried **prost-serialized `VectorRecord`**
(`mod.rs:481 pub struct SstEntry { record: VectorRecord, … }`, `serialize()`/`deserialize()` at
`mod.rs:551`/`:582`). The audit found **zero production callers**, so it was deleted. The durable
`VectorRecord` **read** surface still exists, but it is `compaction.rs` decoding `VectorRecord`
directly from stored blocks — it never went through `SstEntry`.

| **Conversion bridge** | **Bidirectional and complete**, in
`crates/foundation/proximadb-records/src/conversions.rs`: `From<&VectorRecord>`/`From<VectorRecord>` for
`ProximaRecord`; `From<&ProximaRecord>`/`From<ProximaRecord>` for `VectorRecord`; plus
`proxima_record_to_vector`. Lossy edges exist by design (see Constraints).

| **v2 proto** | Replaces `VectorRecord` with `ProximaRecord` (`proto/proximadb/v2/record.proto:385`). The
v2 wire is already `VectorRecord`-free.

| **Network exposure** | Per link:TD-V1SUNSET-1-rest-grpc-sunset-cutover.adoc[TD-V1SUNSET-1]: gRPC v1
services deleted; REST v1 gate (`PROXIMADB_REST_V1_COMPAT`) **default OFF** on `develop` — corrected
2026-08-05; this line previously read "default ON", but `parse_compat_flag` maps `None => false` since
`9c9b04669` (#814), so `/api/v1/*` already returns `410 Gone`. The
**deprecated** v1 surface is the *api-crate* `crates/platform/proximadb-api/src/{rest,grpc}/v1/`
(TD-V1SUNSET-1 owns its deletion); the root-crate `src/network/rest/v1/` is **live** `/api/v2` code.
v2/blended surfaces use the v1 `SearchVectorRecord` wrapper, not bare `VectorRecord`.
|===

**Implication:** the durable *write* side is already converged to `ProximaRecord`, and the
mixed-read-safe *read* mechanism already exists (legacy `.sst` decode → bridge → `ProximaRecord` on the
PAX/compaction path). What remains is **(a)** the in-memory domain model still threading `VectorRecord`
through ~129 prod files (pure ratchet debt, zero disk risk), and **(b)** a bounded legacy-read path
that must keep decoding `VectorRecord` until no legacy `.sst` segments remain.

== Classification — internal-only (migrate first) vs wire-format/persistent (gated)

[cols="2,4,1,3", options="header"]
|===
| Bucket | What | ~Refs | Gating / risk

| **A. Legacy durable** | ~~`SstEntry` SST1 row encode/decode~~ **(retired 2026-08-05 — was dead, OQ-1
resolved)**. What remains: compaction re-read, now at ~`compaction.rs:2995`, which prost-decodes
`VectorRecord` straight from stored block bytes. On-disk bytes *are* prost `VectorRecord`.
| 1 prod site | **Mixed-read-safe READ path.** No new write format. Retires as compaction ages legacy
`.sst` → PAX. Mandate #8 still applies to this remaining site.

| **B. In-memory plumbing** | Engines/analytics using `VectorRecord` purely as a transient working type:
`viper/pipeline.rs` (51), `sst/compaction.rs` (46), `axis/hmgi/detection.rs` (27), `nova/*`, `helix/*`,
`raptor/*`, `swift/*`, `tst/*`, `core/search`, `core/pca`, `record_store.rs`,
`services/operations/vectors/legacy.rs`, and ~115 more.
| ~900 `src` / 150 files | **SAFE to migrate FIRST.** No disk touched; convert via the bridge; each
slice drops the TD-123 ratchet. Default-OFF flag **not** required (internal).

| **C. Already migrated** | PAX format, WAL, and `ObjectStoreVectorRecordStore` (returns `ProximaRecord`;
only the *name* + doc-comments still say "VectorRecord").
| — | Verify; rename/doc-debt only. Not a format change.

| **D. v2-network shims** | `rest/v2/records.rs`, `grpc/v2/record_service.rs`, `arrow_ipc/codec.rs` (uses
`SearchVectorRecord`), `hybrid_search.rs`, and live root-crate `rest/v1/handlers.rs` / `rank_backend.rs`.
Convert at the boundary.
| handful | Boundary conversion; retires with the v1 proto. `SearchVectorRecord` is a sibling v1 type
(see roadmap §"not all v1-proto names are deprecated").

| **E. Deprecated v1 wire** | api-crate `proximadb-api/src/{rest,grpc}/v1/`.
| the v1 surface | **Owned by link:TD-V1SUNSET-1-rest-grpc-sunset-cutover.adoc[TD-V1SUNSET-1]** (REST
default-off flip + hard-delete). Not duplicated here; this migration *enables* the v1 proto retirement
that follows it.

| **F. SDK / clients** | Python v1 client + tests referencing `proximadb.v1`.
| ~109 py | Separate — spec-driven SDK codegen (TD-126). Not this migration.
|===

== The first slice (recommended)

**Goal:** prove the migration mechanism on the **smallest safe internal-only cluster** *and* land the
**round-trip + recall test harness** that every later slice reuses. **No durable-path change in S1.**

*Recommended cluster:* **`src/storage/engines/core/pca`** (`pipeline.rs` + `manager.rs`, ~19 refs). A
self-contained analytics subsystem whose API is `&[VectorRecord]` params/returns (`from_records`,
`train_model`, `cluster_vectors`, `check_drift`, `needs_retraining`) and which consumes **only the float
vectors** — it does not need the record envelope. Migration is clean: change the param/return to
`&[ProximaRecord]` (or directly `&[EmbeddingCell]` / `&[Vec<f32>]`), bridging at the call boundary.

*Alternatives:* if a coupling probe shows `core/pca` more entangled than it appears, pick another leaf.
`core/search/search_common.rs` (central, high blast radius) and `viper/pipeline.rs` (large) are
explicitly **not** slice-1 candidates.

**Deliverables:**

. **Round-trip + recall test (mandate #8 — the reusable harness).** In `proximadb-records`, round-trip
  `VectorRecord → ProximaRecord → VectorRecord` across the **lossy edges**: non-Fp32 embeddings
  (`Fp16`/`Bf16`/`Int8Scalar` → `Fp32` on the v1 side), ms↔ns timestamp conversion, and
  flat-`HashMap`↔`ProximaTree` metadata. Assert the `VectorRecord`-representable subset round-trips
  losslessly, and that a search over converted records holds **recall@k within tolerance of the f32
  baseline** (Quality-Ratchet mandate #10). This test is the gate every later slice re-runs.
. **Migrate `core/pca`** to `ProximaRecord` (or `EmbeddingCell`) params/returns, bridging at callers.
. **Measure** the TD-123 ratchet delta (`scripts/check_v1_proto_usage.py`) — expect a net decrease.
. **Ship S1 default-ON** (no flag — it touches no durable format); the round-trip/recall test *is* the
  safety gate.
. **Refresh the v1-proto baseline** (`--write docs/_internal/roadmap/V1_PROTO_USAGE_BASELINE.json`)
  only if the decrease is committed intentionally.

== Why internal-first (not the durable path first)

The durable *write* side is **already** `ProximaRecord` (PAX default-on); the mixed-read-safe *read*
path already decodes legacy `VectorRecord` and compaction bridges it forward. The gating infrastructure
**already exists**. The remaining debt is the in-memory domain model still carrying `VectorRecord`.
Retiring that first is **zero disk risk**, lowers the ratchet immediately, and is the prerequisite for
durable-path retirement (which can only complete once legacy `.sst` segments are aged out by
compaction). Migrating the durable read seam first would touch the highest-risk path for no ratchet gain.

== Phased roadmap

NOTE: Superseded 2026-08-06 by the measured surface map — see <<revised-roadmap>>. The original
sequencing is kept below for provenance. Its two errors: it nominated `core/pca` as the first
slice (OQ-2 shows it is mid-coupling, not lowest), and it carried an `S-durable` phase that has
no subject (there is no durable v1 read surface — see the RETRACTED warning above).

[.line-through]
. *S1 (this TD): internal-only cluster (`core/pca`) + reusable round-trip/recall harness.*
. *S2–Sn: fan internal migration out per engine/subsystem …*
. *S-durable: convert the legacy SST1 read seam …*
. *S-net / Sunset: as below (these two remain valid).*

[[revised-roadmap]]
=== Revised roadmap (measured 2026-08-06)

The surface is **575 real code refs / 103 files** — and `VectorRecord` is **already fully
evicted from the upper half of the system**: `src/query`, `graph`, `network`, `core`,
`datafusion`, `streaming`, `index` and every `crates/*-ports` crate are at **zero**. What
remains is essentially one subsystem, `src/storage/engines/*`, plus the raptor crates.

Those refs are not independent. They sit downstream of a **sandwich**: canonical records enter,
are **downgraded at flush** by five `.map(proxima_record_to_vector)` sites, live as
`VectorRecord` inside the engine, and are **upgraded again at read**. Everything between is
transient usage that exists only because the engine was handed a v1 record. So the slicing is
**vertical (per engine)**, not horizontal — and slices stay in disjoint files, so they
parallelise across worktrees.

. **S0 (this PR):** free deletions — dead traits `UniversalEngineOperations` (0 implementors)
  and `NovaOperations` (0 implementors), `helix/zone_maps.rs` (0 external references),
  `swift::build_blocks_from_records` (0 callers), and the dead `fast_parse_data_block` +
  commented-out `REMOVED_read_data_blocks_from_sstable` corpse in `compaction.rs`.
. **S1:** the reusable harness **alone** — a *composed* `VectorRecord → ProximaRecord →
  VectorRecord` round-trip test (none exists today; every current test is a one-hop spot
  check), asserting each documented lossy edge loses exactly what is documented; plus the
  OQ-4 `tenant_id` audit.
. **S2:** shared test fixtures (`tests/common/test_data.rs`,
  `tests/common/integration_test_helpers.rs`) — the generators feeding most of the ~231
  test-side refs; converting them unblocks the test tail in one move.
. **S3–S5:** vertical per-engine slices, ascending blast radius — HELIX, then NOVA, then
  VIPER/SWIFT/TST/RAPTOR — each removing its downgrade `.map()`, converting the now-mechanical
  interior, and removing its upgrade-at-read. `core/pca` follows, with its five façades and
  `tests/spatial_clustering_test.rs` budgeted.
. **S6:** cross-crate public API — `proximadb-raptor-engine::{scan_vectors_with_strategy,
  write_vectors}` and `proximadb-raptor-common::RowGroup.vectors` (contained: raptor is behind
  `experimental-engines`).
. **S7:** the remaining traits (`SearchableBlock`, `StorageEngineAdapter`) and the output-side
  downgrade described in <<s-durable-retired>>.
. **S-net:** migrate the v2-network shims (Bucket D) off `VectorRecord`/`SearchVectorRecord`.
. **Sunset:** after TD-V1SUNSET-1's REST/gRPC hard-delete, retire the v1 proto `VectorRecord`
  message (with TD-121/TD-123), once no live v1 caller remains.

Out of scope throughout: the generated prost wire types (`proximadb.streaming.v1`,
`proximadb.cluster.v1`) embed `Vec<VectorRecord>`. Per ADR-024 those are versioned wire edges —
changing them is a `.proto` + wire-compat decision, not a Rust refactor.

[[reconciliation-2026-08-05]]
== Reconciliation (2026-08-05) — audit vs `develop`, and the `SstEntry` retirement

Audited every decisive claim against `origin/develop` before acting. Unlike its
siblings TD-V1SUNSET-1 and -3, **this TD held up**: its central risk finding — that
a durable `VectorRecord` surface still exists, so this is a genuine ADR-024 /
mandate-#8 storage-format migration and not an in-memory type swap — is still
correct. Only locations and one network claim had drifted.

.Verified
[cols="3,1,3", options="header"]
|===
| Claim (2026-07-09) | Verdict | Detail (2026-08-05)

| WAL is `ProximaRecord`-native, **zero** `VectorRecord`
| ✅ holds
| The only `VectorRecord*` hits in the WAL are a `FlushDataSource::VectorRecords`
  variant whose payload is `Vec<ProximaRecord>` — a stale *name*, not the v1 type.

| PAX durable format is `ProximaRecord`-native
| ✅ holds
| Zero `VectorRecord` refs in `proximadb-block-format`.

| Conversion bridge bidirectional + complete
| ✅ holds
| Both `From` directions plus `proxima_record_to_vector` in `records::conversions`.

| `SstEntry` (`mod.rs:481`) is the **only** durable `VectorRecord` surface
| ⚠️ half-stale
| Struct had drifted to `:526` and was **dead** (no production caller). But the
  claim's *substance* survives elsewhere: `compaction.rs` still prost-decodes
  `VectorRecord` from stored block bytes on a live path.

| Compaction re-reads legacy at `compaction.rs:1804`
| ⚠️ line drift
| Now ~`:2995`, still live. This is the surviving durable read surface.

| REST v1 gate `PROXIMADB_REST_V1_COMPAT` **default ON**
| ❌ stale
| `None => false` (default **OFF**) since `9c9b04669` (#814); `/api/v1/*` 410s.
  Same stale claim TD-V1SUNSET-1 carried until it was corrected.

| Surface ~900 `src` refs / 150 files
| ⚠️ re-measured
| `src` 779/142 · `tests` 270/56 · `crates` 103/21 (grew — decomposition moved
  code into crates) · `clients` 512/109.
|===

.What this PR changed
`SstEntry` + its companion `SstMetadata` are **deleted** (159 lines), resolving
OQ-1 and executing the "retire if dead" half of S-durable early, because it is
pure subtraction with no format risk:

* No `SstEntry::` call site in `src/`, `crates/`, or `apps/`; no re-export.
* `SstMetadata` (the local `{is_tombstone, sequence_number, level}` one — distinct
  from `proximablocks::sst_metadata::SstMetadata` and from `SstMetadataStats`) was
  referenced only by `SstEntry`, so the two retire as one unit.
* Its only exercise was tests. Of the four test files touching it, **exactly one
  was compiled** — `tests/integration/sst_optimization_test.rs`, reachable via
  `[[test]] name = "integration"` → `tests/rust/mod.rs` → `#[path]` →
  `tests/integration/mod.rs:122`. Its `create_test_sst_record` fixture was rewritten
  to yield a `VectorRecord` (the surrounding tests exercise bytemuck vector
  serialization and ZSTD block compression, which are unaffected), and the three
  `SstEntry`-format-only tests were dropped.
* The other three were **never compiled** — `tests/integration/storage/compaction_config_test.rs`
  is explicitly commented out in its `mod.rs` as "outdated storage APIs", and
  `tests/rust/storage/` has no `mod.rs` at all, so both `test_expired_record_*.rs`
  were orphaned. Deleted as dead weight.

[WARNING]
====
**RETRACTED 2026-08-06 — the CAUTION printed here was wrong.**

It read: *"'no production callers' did not mean 'no durable surface'. The durable
read path survived `SstEntry`'s death because it lives in `compaction.rs`.
S-durable is still a mixed-read-safe change, and mandate #8 still applies to it."*

**There is no durable v1 read surface.** Verified directly on `develop`:

* `fast_parse_data_block` (`compaction.rs:2911`) carried `#[allow(dead_code)]` and had
  **zero callers** — the only two references in the whole repo were its own definition
  and a `debug!` format string. Its sole historical caller,
  `REMOVED_read_data_blocks_from_sstable`, was entirely inside a block comment.
* Its `BLK1` block magic appeared **nowhere else in the tree** — no writer, no reader,
  no router emitted or dispatched on it, so nothing could produce bytes it would accept.
* The **live** compaction read path was already fully v2:
  `read_all_records_from_file_unified` (`compaction.rs:3048`) returns
  `Vec<ProximaRecord>`; `.pax` inputs route to `segment_format::read_segment_records`,
  everything else to `SstQueryEngine::read_all_records_for_compaction` — both v2.

Both dead blocks are now **deleted** (this PR).

**Consequence: mandate #8 does not apply to this migration.** There is no legacy on-disk
`VectorRecord` decode to keep mixed-read-safe — so no magic byte, no env gate, no
default-OFF rollout, and **no `S-durable` phase** as this TD describes it. See
<<s-durable-retired>>.

How the error happened, recorded because the failure mode is the interesting part: having
nearly mis-inferred "no callers ⇒ no durable surface" for `SstEntry` and correctly caught
it, the opposite assumption was then made about the surviving decode — that it *must* be
live — without running the same caller check. **Verify liveness in both directions; a
`grep` that finds a decoder proves the code exists, not that anything calls it.**
====

[[s-durable-retired]]
=== S-durable is retired; the real remaining compaction seam points the other way

What actually remains in `compaction.rs` is not a v1→v2 *read* but a gratuitous **v2→v1
downgrade on the output side**, through the lossy bridge, serving consumers that already
take v2:

[source,rust]
----
// compaction.rs — `records` are ALREADY ProximaRecord (post-MvccResolver)
MergedVectorTracking::Enabled => records.iter()
    .filter(|r| !r.embeddings.is_empty())
    .map(VectorRecord::from)          // lossy downgrade, no v1 consumer
    .collect(),
----

Both consumers are already `&[ProximaRecord]`: `storage/common/axis_flush_hook.rs` and
`storage/persistence/write_ahead_log/compaction_axis_integration.rs`.

It is not merely redundant. `.to_fp32_owned()` force-promotes fp16/int8 to f32, colliding
with `SstCompactionTask::precision_hint`, whose whole purpose is coercing embedding
precision; and the ns→ms integer division truncates sub-millisecond ordering while
`MvccResolver` and the tombstone/expiry comparisons alongside it operate at **ns**
granularity. Fixing it means typing `merged_vectors` as `Vec<ProximaRecord>` and deleting
the `.map(VectorRecord::from)` — no production consumer changes.

== Open questions (resolve at slice execution — probe, don't assume)

* **OQ-1 — ✅ RESOLVED 2026-08-05: dead, and retired.** `SstEntry::serialize` was reachable from
  *nothing*: the whole `SstEntry` type had zero production callers (definition + impl in
  `sst/mod.rs` only; no re-export; no `SstEntry::` call site anywhere in `src/`, `crates/`, or
  `apps/`). Its companion `SstMetadata` was used solely by it. Both were deleted — this executed the
  "retire `SstEntry::serialize` if dead" branch of S-durable ahead of the rest of that slice, since
  it is pure subtraction. See <<reconciliation-2026-08-05>>.
* **OQ-2 — ✅ RESOLVED 2026-08-06: `core/pca` is NOT the lowest-coupling leaf, and its path in
  this TD is wrong.** The path is `src/storage/engines/core/pca`, not `src/core/pca`. It is
  mid-coupling, not lowest: its `&[VectorRecord]` methods are re-exported through **five**
  façades (`core/mod.rs`, `sst/pca_manager.rs`, `swift/pca_manager.rs`,
  `helix/pca_manager.rs`, `helix/pca_impl.rs`) and have live callers including the
  **out-of-crate** `tests/spatial_clustering_test.rs`, so a signature change breaks a test
  crate. Lower-coupling candidates, measured: `helix/zone_maps.rs` (zero references anywhere
  outside its own file — deleted in this PR rather than migrated),
  `core/ops/mod.rs::UniversalEngineOperations` (zero implementors — deleted),
  `helix/liquid_clustering.rs` (one caller), `tst/asof_join.rs` (two callers). The revised
  slice order puts the **whole HELIX clustering cluster** first and `core/pca` after it, with
  the five façades and the out-of-crate test explicitly budgeted.
* **OQ-4 (new):** Does any live path convert `VectorRecord → ProximaRecord` and then read
  `tenant_id`? The bridge sets it to `String::new()`. Under mandate #3 (isolation is
  structural, never a per-query predicate) that would be a tenant-isolation bug, not a
  cosmetic loss. Audit before migrating anything — this is now slice 1's first task.
* **OQ-3:** Does any live READ path surface **raw** `VectorRecord` to the compute layer
  (SQL/pgwire/DataFusion), or is it always bridged to `ProximaRecord` before compute? If always bridged,
  S-durable is cheap; if not, it must precede the v1 proto retirement.

== Risks

* **Lossy conversion silently dropping data.** `ProximaRecord` → `VectorRecord` collapses non-Fp32
  embeddings to `Fp32` and NF² properties to a flat map. Mitigation: the S1 round-trip/recall harness
  asserts the round-tripable subset and fails loud on loss outside tolerance (mandate #8/#10).
* **Touching the durable path by mistake.** S1 must stay out of `SstEntry`/`compaction.rs` decode. The
  classification table is the safeguard; the durable seam is S-durable, gated default-OFF.
* **Ratchet regression.** A slice that *adds* `VectorRecord` refs fails TD-123. Each slice refreshes
  the baseline only on an intentional, measured decrease.

== References

* link:../../12-design/adr/ADR-024-single-canonical-data-type-and-schema.adoc[ADR-024] — the canonical
type/schema decision this migrates toward.
* link:../../12-design/V1_RETIREMENT_ROADMAP_2026_07_08.adoc[V1_RETIREMENT_ROADMAP_2026_07_08] — Tier 3
names `VectorRecord` as the ADR-024 long pole.
* link:TD-V1SUNSET-1-rest-grpc-sunset-cutover.adoc[TD-V1SUNSET-1] — sibling: the REST/gRPC v1 sunset
cutover (Bucket E owner).
* `scripts/check_v1_proto_usage.py` + `docs/_internal/roadmap/V1_PROTO_USAGE_BASELINE.json` — the
downward-only v1-proto ratchet (CI-blocking).
