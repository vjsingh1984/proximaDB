// Copyright (C) 2026 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-TRACE-2: Structured io_trace ETL sink — memory ingress → pending JSONL+zstd → object-store → Iceberg warehouse

[cols="1,3", options="header"]
|===
| Field | Value
| Status | **S1 #1084, S2 #1087, S2b #1093, S3 #1133, S4 #1136 shipped; S4b background-compaction trigger shipped (this PR).** The sink seals immutable pending files before conditional create, verifies digest collisions, retries on startup, exposes bounded-delivery metrics, serializes each record as an ADR-066 D1 envelope (billing untouched / byte-identical meters), and an async compactor projects those envelopes into an idempotent Iceberg-managed star-schema Parquet warehouse with a source-retirement watermark, run on a background schedule (S4b). The full S1–S4b arc is complete; remaining is governance/tenant-root integration + a per-engine `compute_ms` satellite. Ingress remains best-effort observability, not billing/audit authority.
| Priority | P2 — leverage multiplier for fleet observability + cost analytics (which routes regress, which tenants pay the most GET round-trips, where the cost model mis-attributes). Not on any hot path; default-OFF.
| Component | `crates/modalities/proximadb-observability-engine` (`io_trace.rs` — a new `set_trace_observer` seam + spool writer), `crates/storage/proximadb-object-store` + `DrPathBuilder` (dispatch), the server config (`config.toml` `[observability.io_trace_sink]` + `Config` struct), a background seal/upload task, and (later) a JSONL→Parquet compactor.
| Evidence | #1084/#1087 exposed the delivery gap closed by #1093: immutable pending seal, UUID+sequence+digest identity, conditional create/verification, startup retry, hard pending cap, and delivery metrics. #1133 adds the versioned header + modality payload envelope with compatibility and billing-parity tests. #1136 adds deterministic Parquet projection, Iceberg snapshot publication, source-watermark ordering, and crash/retry/dedup/schema-evolution tests. Remaining evidence gaps are representative hot-path/lifecycle behavior, structural tenant/operator governance, and the S4b background trigger. `partition_by`/`retention_days` remain reserved; `IoTraceSnapshot` remains the in-memory source while the durable representation is now the envelope. ADR-066 §1/§6.
|===

== The finding

The external landing now exists. Records can disappear before sealing because ingress is deliberately
bounded and drop-capable; after #1093, however, sealed pending files are restart-retried with a
conditional-create/digest protocol and delivery metrics. #1133 now converts the flat in-memory
snapshot into the versioned durable envelope at the sink seam. #1136 adds the committed Iceberg-managed
warehouse; automated background scheduling and the governance/lifecycle tail remain open.

== Direction (per ADR-066)

Keep the separate, gated observer, but name the layers precisely: bounded **memory ingress** →
immutable local **pending segment** → conditional-create object PUT → Iceberg-managed Parquet
warehouse. The sink remains best-effort at ingress. Once a record is sealed, retry is idempotent and
crash-aware. Billing stays always-on/untouched (ADR-027).

== Slices

* **S1 — shipped #1084: sink skeleton + local sealing.** New `set_trace_observer` seam (mirroring
  `set_billing_observer` but SEPARATE); mint a stable `query_id` at `instrument()` entry (if none
  exists); a bounded memory queue and background JSONL+zstd sealer; config + env overrides;
  default-OFF. It serializes the existing flat snapshot on query completion. “Local spool” in the
  original slice name is not a durability claim.
* **S2 — shipped #1087: initial object-store dispatch (superseded by S2b delivery).** Background task sealed on
  `segment_bytes` OR `flush_interval_s` (whichever first), zstd-compress, PUT via
  `proximadb-object-store` under `DrPathBuilder` keys
  `_operator/io_trace/` with `ObjectAccessTier = Cold`; PUT failure wrote a local fallback.
  Compression/PUT remain off the query path. Tenant/date/host partitioning and retention are not
  implemented.
* **S2b — delivery hardening (shipped #1093).** Seal an immutable pending file before upload; startup retry;
  writer-incarnation UUID + monotonic sequence + content digest; conditional create and
  AlreadyExists digest verification; retire local pending only after verified PUT. Add counters/gauges
  for dropped records/bytes, seal/upload failures, retries, pending files/bytes, and last success.
  Bound pending bytes without deleting sealed files: at the cap, stop draining ingress and account
  subsequent ingress drops. Duplicate install must drain/reject rather than abort the prior worker;
  shutdown timeout state is observable.
  Reject unsupported compression/format config. Add operator ACL/encryption and structural tenant-mode
  integration tests. Keep the ingress delivery label best-effort.
* **S3 — shipped #1133: envelope refactor.** `TraceEnvelope { schema_version, writer_uuid,
  sequence, header, payload }` in `proximadb-observability-engine::trace_envelope` — a homogeneous
  `TraceHeader` + a `#[serde(tag="modality")]` `TracePayload` union (`Relational` / `VectorAnn` /
  `Embedding` / `Generic`, `#[serde(other)]` → `Other` for a newer writer's modality). Built from an
  `IoTraceSnapshot` at the durable-sink boundary (`schema_version` 1 → 2). **Billing is untouched** —
  the billing observer still reads the in-memory `IoTraceSnapshot` directly (`compute_ms`,
  `bytes_read`, `range_gets`, `egress_bytes`), so meter behavior is byte-identical *by construction*;
  the envelope is only the sink's serialization view (the stable seam). Every field is
  `#[serde(default)]` (additive back-compat); an unrecognized `modality` deserializes to `Other`
  (skippable). Payload holds only bounded operator keywords — no raw data/query/vector. Tested:
  billing-input parity, per-modality classification (+ ANN-precedence), envelope round-trip,
  unknown-modality skippability, missing-field tolerance, bounded top-level shape.
* **S4 — shipped #1136: Iceberg-managed Parquet warehouse.** `IoTraceWarehouse::compact`
  (`src/observability/io_trace_warehouse.rs`) reads the durable JSONL+zstd envelopes and projects
  them into a star schema — `trace_header` (fact) + `trace_relational` / `trace_exec` (`exec[]`
  flattened, one row per operator) / `trace_vector` / `trace_embedding` satellites — Parquet written
  via `record_batches_to_parquet_bytes` and committed through Iceberg
  (`publish_iceberg_snapshot`). **Idempotent + crash-safe**: each source segment maps to a
  deterministically named per-table Parquet file, and the snapshot enumerates all files under a
  table's data prefix, so a crash before the watermark advances re-writes the same files and
  re-publishes the same set — no duplicate rows. Records de-duplicate within a segment by
  `{writer_uuid, sequence}`; the source-retirement watermark (`_watermark.json`) is written only
  after the snapshot commits; `now_ms` is the snapshot id (unique per run). Tested: star-schema
  round-trip, within-segment dedup, watermark incremental-skip, crash/retry idempotency,
  schema-evolution tolerance (unknown modality + malformed line).
* **S4b — shipped (this PR): background compaction trigger.** `io_trace_warehouse::install/shutdown`
  spawn a background task that runs `compact` every `warehouse_compaction_interval_s` seconds (config
  under `[observability.io_trace_sink]`, env `PROXIMADB_IO_TRACE_SINK_WAREHOUSE_COMPACTION_INTERVAL_S`;
  `0`/unset = off, requires `object_store_uri`) and once more on shutdown, mirroring the sink worker
  (watch-shutdown + timeout; duplicate install rejected). Wired at `ProximaDB::new` / `shutdown`.
  Tested: install → duplicate-reject → graceful-shutdown final compaction lands the segment.
  Remaining follow-up: a per-engine `compute_ms` satellite.

== Resolved decisions / current caveats

* **Envelope migration ordering:** sink-first on the **flat** snapshot (S1–S2), envelope refactor later
  (S3). The durable stream does not block on the data-model change.
* **Identity:** `query_id` joins analytical rows; `{writer_uuid,sequence}` identifies ingestion events
  and prevents retry duplicates.
* **Object-size math:** `segment_bytes` is uncompressed JSONL bytes. Record compressed bytes and PUT
  count; tune from traces. Do not claim 4 MiB is a multipart part.
* **Retention/GC:** lifecycle may retire raw JSONL only after the S4 watermark covers it. The currently
  parsed `retention_days` is inert.
* **Isolation:** default operator stream is cross-tenant and operator-only. Tenant mode means one
  tenant per object under a tenant root, not a cosmetic key partition.

== Gate

Observe-only—no served-result change. In addition to existing JSONL round-trip, bounded-ingress, and
billing-never-gated tests, require:

* kill after local seal / before PUT, then restart → exactly one verified object and pending file retired;
* repeated PUT/AlreadyExists → digest match is no-op, mismatch fails closed;
* two writers with identical wall clock never collide; sequence/digest round-trip;
* object-store outage → pending metrics rise, recovery drains them; cap behavior is explicit;
* duplicate install and shutdown timeout never silently discard a sealed/pending segment;
* unsupported config rejected; active-vs-reserved config test;
* sink OFF/ON representative query p50/p95 + allocation budget, proving no filesystem/network/compress on query path;
* envelope unknown-payload/back-compat and billing input parity;
* S4 Iceberg crash/retry/dedup/schema-evolution/source-watermark round-trip;
* operator-only and tenant-root path/ACL integration tests.

== Non-goals

Recall@k / ranking / quality-eval (stays on the sampled `predicate_diagnostics` bus); OpenTelemetry
span export; a Kafka/Avro streaming bus (ADR-066 D2/D3 rejected Avro); any change to the billing meters.

== Sequencing

TD-TRACE-1 #1076 → S1 #1084 → S2 #1087 → S2b #1093 → S3 #1133 → S4 #1136 → **S4b trigger (this PR)**.
The S1–S4b core arc is complete; the unresolved lifecycle, governance, and operational gates remain
explicit above rather than being mistaken for unshipped core.
