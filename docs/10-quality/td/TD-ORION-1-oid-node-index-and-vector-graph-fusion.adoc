// Copyright (C) 2026 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-ORION-1: vector→graph match-then-traverse fusion — already implemented; residual is a relevance eval

[cols="1,3", options="header"]
|===
| Field | Value
| Status | **Resolved 2026-07-15 — the fusion is ALREADY implemented and wired (TD-045 Modular Graph RAG).** The original filing (2026-07-15) claimed it was blocked on building an ORION `oid`→node index; a source review disproved that premise (see below). The one genuine residual is a **relevance eval** (mandate #13) for this ranked/generated surface, tracked in <<_residual_the_eval>>.
| Priority | P3 (residual eval only; the capability itself ships today).
| Severity | Low — no missing capability; a test-quality (eval vs deterministic test) gap.
| Component | PARTIAL (corrected 2026-08-12): the pipeline `src/graph/rag/` (`RagPipeline`, `VectorNodeRetriever`, `KHopSubgraphBuilder`) is shipped, but the REST entry point was NOT mounted — `create_graph_router()` in that file was referenced nowhere, and the router actually nested at `handlers.rs:1694` is the `proximadb-api` one, which routes `/graphs/{id}/rag` to `not_implemented_handler`. The unmounted duplicate was removed 2026-08-12; the pipeline in `src/graph/rag/` remains. Graph-RAG is therefore NOT reachable over REST today. Residual: mount a RAG route with identity extraction built in, plus a graph-RAG eval harness under `tests/`.
| Governs | link:../../12-design/adr/ADR-061-per-collection-workload-profiles.adoc[ADR-061] (D4)
| Relates to | TD-045 (Modular Graph RAG — the implementation), ADR-044 (stable symbol `oid`), TD-WLP-5 (Churn read core), TD-WLP-8 (the amendment this split from)
| Pillar | correctness/eval (ranked graph-RAG relevance)
|===

toc::[]

== Why the original premise was wrong (verified 2026-07-15)

The filing assumed "ORION has no `oid`→node index, so match-then-traverse must build one first." Both
halves are false:

* **No index is needed — `NodeId` *is* the `oid`.** The SDK mints one id via `record_symbol_id()`
  (ADR-044, `graph/{repo}/node/{key}`) and uses it for BOTH the vector record id AND the graph node
  id (`clients/python/.../code_knowledge.py`: vector `id` at `:527`, `node_id` at `:561`, with the
  explicit comment "node_id below uses the same value"). ORION's `node_to_index`
  (`crates/modalities/proximadb-orion-engine/src/lib.rs`) is therefore already keyed by the `oid`; `get_outgoing_edges`/
  `get_neighbors` resolve an `oid` directly.
* **The fusion already exists and is wired.** `src/graph/rag/` implements the full pipeline
  (TD-045): `VectorNodeRetriever::retrieve` does a vector search and returns record `oid`s as seeds
  (`engine_impls.rs:63`, `.map(|r| r.oid)`); `KHopSubgraphBuilder::build` k-hop BFS-expands them via
  `get_outgoing_edges(oid, edge_type)`; `RagPipeline::run` composes retrieve → filter → prune → build.
  It is exposed as a REST endpoint **`POST /graphs/{graph_id}/rag`** (route
  the pipeline in `src/graph/rag/`). This *is* ADR-061 D4's match-then-traverse, but
  see the Component row: its REST entry point was an unmounted duplicate and has been
  removed, so the capability is not reachable over REST until a route is mounted.

So the vector→graph fusion capability is **shipped**, not blocked. ADR-061 D4's fusion intent is
satisfied by the `/rag` endpoint (a dedicated graph-RAG surface — cleaner than overloading the Churn
vector-search path).

[#_residual_the_eval]
== Residual: the relevance eval (mandate #13)

The only real gap. `tests/graph_rag_integration_test.rs::test_rag_pipeline_integration` exercises the
pipeline plumbing with a **mock retriever** — it is a deterministic test, not a relevance **eval**.
Per mandate #13 (evals for non-deterministic/ranked surfaces), graph-RAG retrieval needs an eval
suite with a real rubric covering **output** (are the returned neighbours the right symbols) and
**trajectory** (was the seed→expand path sound), gated on a threshold and versioned with the code.

This is the follow-up if we want the surface eval-gated. It is a distinct kind of work (eval
authoring over a small code-graph corpus), not a code-capability gap. File/scope on demand.

== Not doing (measure-don't-assert)

* No `oid`→node index (does not need to exist).
* No re-implementation of match-then-traverse (shipped as TD-045 / `/rag`).
* No Churn-search-seam integration unless a use case wants fusion *inside* vector search rather than
  via the dedicated `/rag` endpoint.
