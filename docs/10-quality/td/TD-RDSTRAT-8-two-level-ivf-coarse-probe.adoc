// Copyright (C) 2026 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-RDSTRAT-8: Persisted IVF probe directory for SST tier regions — bound the large-segment GET/byte cliff

[cols="1,3", options="header"]
|===
| Field | Value
| Status | **Core implemented and default ON (reconciled 2026-07-28).** #1128 added mixed-safe v3/A0 persistence, #1135 the selective reader, #1144/#1148 durable telemetry and SIFT gates, and #1153 the independently gated PR-C2 layout proof. The trained compaction writer and reader were promoted in #1210/#1234 after ledgered `nprobe_sweep_trained_1m` held recall `0.986-0.987` and improved the unprobed control (`81.4` vs `108.5` GETs/query; `92` vs `144` ms). #1227 closed the probe cache bypass and #1252 corrected TOML/config wiring. `PROXIMADB_PAX_WRITE_A0_TRAIN=0` and `PROXIMADB_PAX_READ_COARSE_PROBE=0` are kill-switches. PR-C2 prefix/split-cache/wider-coalescing controls remain default OFF.
| Priority | **P1 hardening at 10M/high dimension/multiple metrics and on production object stores.** These measurements refine calibration; they do not make the shipped 1M-trained path default OFF.
| Severity | Current capability with mixed-read fallback and kill-switches. Residual risk is scale/distribution calibration, not format rollout.
| Component | `src/storage/engines/sst/` — `segment_format.rs` (writer/reader), `block_cluster.rs` (clustering), `compaction.rs`/`compactor_impl.rs` (the only write path that clusters), `crates/storage/proximadb-storage-common/src/pax_block.rs` + `segment_layout.rs` (regions/footer), coalesce policy + `IopsBudget`. **NOVA is explicitly NOT the host** (rev 2).
| Governs | The large-segment choice between many ranged GETs and whole-region O(N) bytes, plus the unconditional Region-A scan bytes. Target: bounded probed cells and bytes per live segment; collection cost still multiplies by live segment count.
| Relates to | ADR-062/ADR-065 (regions; this extends the region layout with a coarse directory), TD-RDSTRAT-6 (measured baseline), TD-WLP-4b (PCA/sample-train/NEON — reused; its deferred "persist the PCA model" item becomes REQUIRED here), TD-WLP-9 (fail-closed recall gate pattern), AnvaiOps ADR-0044 (downstream gate).
| Pillar | COST (billing) / PERF (latency).
|===

== Current serving contract (2026-07-28)

The normative state is:

* compaction emits v3/A0 by default when it has a trainable clustered plan;
* the reader probes A0 by default when a v3 segment declares a non-empty A0
  extent, otherwise it uses the v1/full-region path;
* unset configuration uses
  `nprobe = ceil(sqrt(ivf_cells_total) * nprobe_multiplier)`, with multiplier
  `1.0`, minimum `3`, and no configured maximum beyond the cell count;
* `PROXIMADB_PAX_WRITE_A0_TRAIN=0` and
  `PROXIMADB_PAX_READ_COARSE_PROBE=0` disable the write and read sides;
* unknown/legacy layout, missing A0, or a safe fallback condition never assumes
  probing; the durable `ivf_*`, region-byte, fetch-round, and fallback telemetry
  proves which route ran.

The revision chronology below records how the implementation arrived here.
Any “default OFF” wording inside that chronology describes the landing state of
that revision, not the current default.

== Why (measured, with the current-reader asymptote corrected)

Compaction currently uses `cells ≈ N·dim / IOP_target`; cells therefore scale with N.
The writer uses those cells only to order rows, then discards the PCA model, centroids,
and run directory. Query-time Region A consequently ranks every row, and Region B plans
ranges from scattered survivor ordinals without knowing cell boundaries.

The rev-1 `GETs ∝ N` statement was incomplete. `rabitq_search_segment_coalesced` already
falls back to one whole-Region-B GET when planned survivor ranges would read at least the
whole region. Request count therefore does not grow without bound: the actual cliff is a
chooser between **more ranged GETs** and **O(N) whole-region bytes/NIC/cache footprint**.
Measured single-segment SIFT128 points still show the scale pressure:

[cols="3,1,1,1", options="header"]
|===
| N | k | GETs/q | notes
| 100 000 | 3 | 9 | k = N·128/4MiB
| 1 000 000 | 30 | 51 | 10× N → ~5.7× GETs (sub-linear; locality helps a little)
| ~10 000 000 (projected) | ~305 | do not extrapolate linearly past the whole-region fallback | requires measurement
|===

Measured and ruled out as complete fixes: **M** (100→10000 moves GETs only 8→10 — M is
primarily a bytes/recall lever) and **k tuning** (50–54 across k 8→128 in the recorded
protocol). A query-visible cell directory changes the access structure: rank a small
directory in RAM, then fetch only selected contiguous Region-A/B runs.

**The unconditional O(N) term:** Region A itself. Today the
RaBitQ region is fetched whole (one GET — fine at 1M ≈ 16–24 MB). At 10M that whole-region
fetch is ~160–240 MB *per cold query*: a bytes/latency/cache-footprint cliff that survivor-GET
fixes alone do not touch. Coarse-probe fixes **both** terms with one layout: probed cells
= contiguous sub-ranges of Region A ⇒ the scan reads `nprobe` sub-ranges, O(N·nprobe/k_c)
bytes, instead of all N codes.

**Rev-2.1 latency-model correction (waves, not sums).** Cold-latency figures derived as
sequential `GETs × RTT` (~1 s @1M, ~10 s @10M projected) are **sequential worst-case**. With
a bounded concurrent fetch pool the correct model is
`latency ≈ rounds × RTT + Σ ceil(GETs_in_round / pool) × RTT + compute`, where the stage
chain (Region A scan → survivor select → Region B fetch → rerank → Region D) is ~3
dependent rounds and GETs within a round are parallel. Re-derived: 1M/51 GETs at ~6
workers ≈ **~250–350 ms** cold; 10M single-level (~500 GETs) ≈ **~1.7–2 s** — better, but
still commercially unusable AND unchanged in **GET billing** (parallelism moves latency
only, never GET count). **No gate changes**; this TD remains the fix for both columns.

== Host decision: SST layout, not a different engine

Rev 1 proposed NOVA ("multi-level SuperBlock hierarchical stats, zone maps, progressive
search"). A code review falsified each substrate claim:

[cols="2,3,2", options="header"]
|===
| Claim (rev 1) | Reality on `develop` | Citation
| "SuperBlock hierarchical stats" | A ~140-line **stub**; types exported, never in the hot path | `nova/hierarchical_stats.rs`
| "progressive search with early termination" = coarse→fine | Staged **full-scan filtering** (Binary→INT8→PQ→F32); every stage touches all rows; no cell selection, no routing | `nova/progressive_search.rs`, `progressive_stages.rs`
| Zone maps as pruning substrate | Vector min/max/centroid selectivity *estimates* for cost-ordering; not block pruning in the hot path | `nova/zone_maps.rs`
| Object-store efficiency | Plain Parquet row-groups, one ranged GET each; **no region hoisting, no coalescing, no IopsBudget-aligned layout** — none of the ADR-062/065 machinery | `nova/nova_ranged_reader.rs`, `nova/operations/*`
| "users choose by scale" | Engine choice is **immutable per collection** — switching = full re-ingest. A forced engine migration at the 5–10M growth boundary is a product cliff | `query/query_router.rs`, `factory.rs:176–235`
|===

Meanwhile SST already carries ~90% of the needed substrate: `cluster_order_pca_ivf`
(PCA → k-means → Hilbert-ordered cells, `block_cluster.rs:153–286`), per-block
**centroid + radius** in the VOE directory (`object_economy_directory.rs:61–92` — a
proto-coarse-layer), the region layout + footer self-description, `ObjectRangeCoalescePolicy`
+ `IopsBudget`, sample-train + NEON `sq_l2` (TD-WLP-4b), the survivor cache, and the
compaction retrain hook (`retrain_pca_model_for_compaction`).

**Decision: persisted-cell probing is an SST compaction-time successor layout** (layout
version 2 / successor magic to today's `PXH1` v1), mixed-read-safe beside existing
segments. NOVA stays an analytics engine; if NOVA ever needs ANN
it should adopt the PAX-region logic from the shared crates (`proximadb-storage-common` is
already engine-neutral — SST and HELIX consume it; NOVA today consumes none of it), but that
is out of scope here. HNSW is implemented in AXIS (`index/axis/indexes/hnsw_index.rs`), but it
is an in-memory/warm index and does not supply a cold object-store PAX region layout. Persisted
graph traversal's dependent pointer-chasing remains a poor cold-store fit versus parallel IVF
cell ranges. Reuse the AXIS HNSW route for warm/churn profiles; this TD solves the immutable
SST cold-layout problem rather than claiming HNSW is absent.

**SWIFT considered and rejected (rev 2.1).** SWIFT is nearer SST in spirit than NOVA,
but it is marked *DEPRECATED — INCOMPLETE* by its own `mod.rs` (2026-04-03): behind the
`experimental-engines` flag, 41 tests, "30+ DEFERRED critical features", slated for removal
in v1.0, with a migration guide that itself says "use SST". Hardening SWIFT to production
would mean rebuilding exactly the substrate SST already has battle-tested (WAL/recovery,
compaction CAS, regions, coalescer, caches) — all cost, no unique capability. The
persisted-probe feature needs a **layout**, not an **engine**; any engine-level host (NOVA
or SWIFT) pays the full substrate rebuild. The SST successor layout stands.

Two placement constraints inherited from ADR-065 and shipped code:

* **Production coalesced flush stays cheap SQ8-Morton** (not the stale ADR-065 sign-bit
  claim). Learned PCA/IVF stays at compaction. The successor layout is written **only by
  compaction** — which is exactly where large
  corpora end up, and how existing collections upgrade with zero migration events.
* L0/`PXH1` segments keep the fetch-all read path; the per-segment chooser picks
  by layout version (the ADR-061/045 mixed-read pattern).

== Successor layout (extends ADR-065; one atomic object, unchanged invariants)

----
[header-prefix v2/PXH2: magic | version | A0/A/B/footer extents]
[Region A0: IVF probe directory — NEW, cached]
[Region A : RaBitQ codes      — existing compaction-cell order]
[Region B : SQ8 codes         — same order (PR3 / #1069)]
[Region D : row blocks        — same order; block boundaries never straddle a coarse cell; ADR-065 Region C/D remain separate gates]
[footer-index: block table | region table | A0 offset copy (optional section) — footer stays version 1, see §As-built delta]
----

Everything ADR-062/065 guarantees is preserved: one PUT flush, one-object compaction CAS,
manifest-level atomicity, self-describing footer, per-provider IopsBudget geometry.

The current header is `PXH1`, `SEG_LAYOUT_VERSION=1`, with explicit A/B/footer extents.
There is no deployed v2/v3 hierarchy. This TD must use the next supported version and keep
the `PXH1` reader; it must not reuse the old version while changing prefix bytes.

=== Region A0 — persisted compaction plan

[cols="2,2,3", options="header"]
|===
| Field | Type/size | Rationale
| `k` | u32 | existing IOP-derived compaction cell count
| `pca_model` | `n_comp` u16 + mean `dim×f32` + components `n_comp×dim×f32` | **the TD-WLP-4b deferred persistence, now required** — query-time coarse ranking needs the same projection the writer used. ~10×128×4 ≈ 5 KB at SIFT; ~10×1536×4 ≈ 61 KB at 1536d
| `centroids` | `k × n_comp × f32` | in PCA space, from the already-paid compaction clustering
| `radii` | `k × f32` | diagnostic/calibration input only until a metric-consistent bound is proven
| `rabitq_params` | versioned Region-A rank parameters | sub-range codes must be independently rankable; the reader cannot call `RaBitQRegion::from_bytes` on a partial payload and assume the whole-region header
| `sq8_params` | versioned Region-B min/scale | avoids a serialized metadata GET before B decode; must match the footer mirror
| per-cell entry ×k | `{row_begin,row_end: u64; a_off,a_len,b_off,b_len: u64; d_block_begin,d_block_end: u32}` | direct ranged addressing for existing regions; optional future regions extend a versioned entry
| `checksum, seed, trained_on` | u64s | determinism + diagnostics
|===

Directory size is derived from the actual IOP-derived `k`, not asserted as 120–300 KB.
Fetched once per segment and held by the byte-budgeted invariants cache. A cache miss is one
GET; eviction and many live segments mean it is not assumed permanently resident.

=== Write path (compaction only; reuse before invention)

1. Merge inputs (existing compaction flow).
2. Run the existing deterministic `cluster_plan_pca_ivf`: its current
   `k = clamp(N·dim / IOP_target, 2, 4096)` aligns an SQ8 cell with the backend read unit.
   `sqrt(N)` is rejected as the default: it optimizes an in-memory CPU/list-scan balance,
   not cloud range economics, and at SIFT1M would create ~1,000 ~128 KiB B-runs far below
   a 4–8 MiB IOP.
3. Extend `ClusterPlan` to retain the fitted PCA model, centroids, deterministic seed, and
   cell runs rather than training another quantizer. Assignment cost and ordering stay the
   work compaction already performs.
4. Emit Regions A/B in existing cell order and serialize A0. Row-region byte extents are
   exact without forcing row-block padding. Align D block boundaries only if measurement
   proves its over-read dominates the write amplification.
5. Write the successor header + regions + footer through the existing staged one-object
   `write_if_absent` publication and manifest advance.
6. **Spill (deferred):** boundary rows with `d_2nd/d_1st < τ` could get their
   **Region-A code only** duplicated
   into the runner-up cell. A spilled code must carry its original global row ordinal so a
   hit addresses the one Region-B/D row; implicit code-position→row mapping is no longer
   valid for that cell. The mapping bytes and duplicate-candidate dedup are charged in the
   gate. B/C/D are never duplicated. No spill environment gate is currently
   registered; file one only with an implementation/evidence PR.

=== Read path (query)

1. Load the successor prefix+A0 in one coalesced GET (cache-hit in steady state). Fetch the
   footer in the same parallel round when it is not cached; do not serialize A0→footer when
   D-block planning will need both. Avoid a separate metadata/HEAD by threading catalog/manifest
   object size into the reader where available; otherwise count the metadata request explicitly.
2. Project query with the persisted model and rank all `k` centroids in RAM.
3. Probe a configured/calibrated `nprobe` cells: ranged GETs into Region-A sub-ranges via
   `ObjectRangeCoalescePolicy`; adjacent physical cells may coalesce.
4. RaBitQ-rank within probed rows; for query result count `top_k`, survivor pool
   `M' = max(top_k·100, probed_rows·0.01, 1000)` (the adaptive-M law, scoped to probed
   rows). Deduplicate original row ordinals before Region-B fetch when spill is enabled.
5. Survivors → Region B ranged GETs (contiguous within probed cells), SQ8 rerank → top-k →
   Region D fetch. Survivor cache unchanged (keys are segment+query-shaped; A0 rides the
   invariants cache).
6. **io_trace additions:** per query emit `ivf_cells_total`, `ivf_cells_probed`,
   `probed_rows`, `fetch_rounds`, and `whole_region_fallback`, alongside existing GET/bytes
   and cache counters.

The former adaptive radius rule is rejected as a correctness guard. It compared a
PCA-space centroid/radius lower bound with a RaBitQ approximate distance, which is neither
the same metric nor the same error model; it also does not generalize to cosine/dot.
`nprobe` is therefore fixed by metric/dimension/distribution eval profiles in the first
implementation. Exact mode probes all cells. Adaptive escalation may return only after a
score-space-consistent bound is derived and separately recall-gated.

The budget is per segment. A collection with `F` live segments can pay roughly `F` times
the directory/A/B/D rounds, so GET-budget compaction remains part of the acceptance gate.

=== Compatibility / rollout

* Successor magic/layout version in the header-prefix; `PXH1` readers reject cleanly,
  and the new reader handles `PXH1` segments via the existing full-scan/fallback path.
  Per-segment dispatch is mandatory; mixed collections are normal during rollout.
* Current default **ON**: normal compaction emits v3/A0 and the reader probes it.
  Operators can independently disable write training or read probing with the
  canonical kill-switches above. Existing v1 segments need no migration event,
  engine change, or re-ingest.
* The retired `PROXIMADB_IVF2*` names are historical only; the environment-gate
  registry is authoritative for current names and defaults.

== Historical as-built delta (PR-A) — layout versioning + the rev-3 write-path reconciliation

*Write path reuses the existing IOP-derived plan (rev 3 — not a second `sqrt(N)`
quantizer).* An earlier PR-A cut trained a fresh coarse k-means at
`k_c = clamp(round(√N), 64, 4096)` over the fine IVF cells. The rev-3
first-principles reconciliation (this doc's body) replaced it:
`cluster_plan_ivf_probe` now persists the *same* plan `cluster_plan_pca_ivf`
already computes — IOP-derived `k = clamp(N·dim/IOP, 2, 4096)`, the
`ivf_projection_dims` law, `PROXIMADB_IVF_TRAIN_SAMPLE`, seed
`0x5041_585F_4956_4631`, 15 iterations — into Region A0 rather than discarding
it. Ranking those `k` centroids in RAM (≈305 at 10M, ≈3050 at 100M, capped
4096) already breaks GETs ∝ N; a `sqrt(N)` level bought nothing and would
fragment Region B into sub-IOP (~128 KiB) runs. The single-level (`PXH1`) f64
path is untouched (byte-stable); the shared row-ordering is extracted into
`order_rows_by_cell` so both plans stay identical to their inline form.

*Layout versioning lives in the header-prefix, not the footer.* Between rev 2 and
PR-A, ADR-065 PR3 (#1069) collapsed the footer v1/v2 split into a **single
version-1 footer** with an optional-sections framework whose unknown tags old
parsers skip (pinned by test). PR-A therefore does NOT introduce a footer v2/v3:

* `SegmentHeaderPrefix.version = 3` (`SEG_LAYOUT_VERSION_TWO_LEVEL`; byte value
  matches this TD's "v3" name, 2 intentionally skipped/reserved) selects the
  two-level layout; the v3 prefix is 72 B (`+[a0_off u64][a0_len u64]`).
  Readers fetch 72 B unconditionally (free within the same GET) and branch on
  the version byte.
* The footer gains an **additive** `SECTION_COARSE_DIRECTORY` section
  (`[a0_off][a0_len]` mirror). A sectionless footer stays byte-identical.
* A0 serde: `crates/storage/proximadb-storage-common/src/coarse_directory.rs`
  (`PXA0` magic, own version byte, FNV-1a checksum, fixed layout —
  `serialized_len` is exact so the writer computes all offsets in one pass).

*Write/read projection consistency (f32-persisted model).* The PCA model is
truncated to the f32 precision A0 persists **before** cell assignment: rows,
centroids, and radii are all computed with the same `project_with_model` kernel
the query path uses — no f64-train vs f32-read drift at cell boundaries.
Vectors stay f32 end-to-end; only dot-product accumulators widen transiently to
f64 (cancellation-safe accumulation, never stored).

*Read compatibility shipped in PR-A; the probe reader shipped in PR-B.* The
current binary parses v3 and probes A0 by default. Disabling the read probe
serves the same v3 segment through the existing whole-region scan (Regions A/B
are byte-identical in format; a full scan over cell-ordered rows is
order-agnostic). `read_segment_records` handles v3 (including the Region-B vector overlay),
so re-compaction/recovery of v3 segments never drops vectors. Version-1-only
binaries reject v3 at the prefix version check (fail-closed; contract pinned
by `v1_only_reader_rejects_v3_prefix`).

*Enabling fix — armed compaction of coalesced `.pax` was a silent no-op.* The
production-trigger route proof (below) caught that the compaction unified
reader predates the coalesced layout ("Invalid SSTable magic marker"): an
armed AppendBulk collection flushing coalesced RaBitQ L0s (default-on since
#1024) collected 0 records, hit the empty-merge early return, reported
`compaction_ran=true`, and left inputs unmerged — no data loss, but the entire
re-cluster arc (and this TD's write path) was dark on the real trigger.
`read_all_records_from_file_unified` now routes PAX segments through the
canonical `read_segment_records` inverse (mixed-read mandate: reuse the
existing inverse).

*Eval realization (the recall/GET gates must actually reach v3).* The SIFT
harness (`tests/sift_pax_recall_ratchet_test.rs`) drives **flush only**, and v3
is written **only** by `write_pax_segment_compacted` — so without explicit
wiring no eval would ever measure a v3 segment:

* The recall/GET ratchet reaches v3 via the **direct compaction-writer route**
  — the harness calls `write_pax_segment_compacted()` with write training enabled,
  byte-identical to the production compaction product (and/or the established
  `PROXIMADB_PAX_FLUSH_CLUSTER=ivf`-style eval-knob pattern for
  flush-reachable measurement). Production policy remains compaction-only and
  is now default ON.
* One **production-trigger route proof** exists as of PR-A:
  `tests/sst_ivf2_compaction_route_proof_test.rs` (mirrors the TD-WLP-7
  execution gate) — armed AppendBulk flush past L0 threshold ⇒ inline
  compaction emits a v3 segment with a parsable A0 covering every merged row,
  production search over it stays exact, and the flag-off control stays v1.
* **CI authority (PR-B):** wire the v3 recall/GET assertion into the qa-gate
  ANN-recall conformance ratchet tier (develop→qa MEDIUM — SIFT + release
  build are too heavy for the per-PR LIGHT tier). If the SIFT dataset is
  unavailable in that tier, a deterministic synthetic-corpus variant of the
  same gate runs unconditionally and the SIFT run stays `#[ignore]`d for the
  quiet-machine eval; record which one is the CI authority here when wired.

*PR-A landing surface (historical default-OFF state):* the probe plan reuses the
single-level IVF knobs — `PROXIMADB_IVF_K`
overrides the IOP-derived cell count, `PROXIMADB_IVF_TRAIN_SAMPLE` the
sample-train — seed `0x5041_585F_4956_4631`, 15 iterations, with **no separate
`sqrt(N)` surface** (`PROXIMADB_IVF2_KC` / `_TRAIN_SAMPLE` removed in the rev-3
reconciliation). Determinism, cell-extent exactness, cell-boundary block
padding, mixed-read, and the flag gates are unit/e2e tested (`pax_block.rs`,
`segment_layout.rs`, `coarse_directory.rs`, `block_cluster.rs`,
`segment_format.rs`, route-proof test). The canonical write kill-switch is now
`PROXIMADB_PAX_WRITE_A0_TRAIN`; retired `PROXIMADB_IVF2*` names must not be used.

== Historical PR-B delta (the A0 coarse-probe reader, rev 3.2)

*The probe reader.* `rabitq_search_segment_coalesced` gained a v3 branch
(initially gated OFF, now default ON): parse Region A0, project the query with the persisted f32 model,
**rank the `k_c` centroids in RAM**, select the `nprobe` nearest non-empty cells,
ranged-read ONLY those cells' Region-A code extents (`a_off/a_len`), and rank
that subset. The whole Region A is never fetched, so cold GETs/bytes drop from
O(N) toward O(nprobe). Survivors (global rows) then feed the **unchanged** SQ8
rerank + OID-fetch tail — because those steps are already survivor-driven and
ranged, fewer/cluster-local survivors shrink the Region-B/D reads for free.

* *Subset rank without the whole region.* New `coalesced_rabitq::rank_probed_rows`
  ranks a set of probed row-runs given the region header (one small ranged GET of
  `[rabitq_off, rabitq_off + region_header_len(dim))`). Probed rows are **always
  present** (A0 cells cover only usable rows), so a synthesized all-ones bitmap
  lets the canonical `parse_rabitq_codes` + `rank_candidates` decode/score each
  run unchanged — no hand-rolled decoder, no bitmap GET. Unit-gated
  (`rank_probed_rows_matches_region_rank`: one run over all rows reproduces
  `RaBitQRegion::rank` exactly; sub-runs restrict + map global indices).
* *Coalescing.* The PR-B baseline merges only strict physical adjacency
  (gap 0). PR-C2 adds a default-OFF, cost-aware range planner: it may physically
  over-read a bounded gap while passing only the selected cell slices to the
  ranker, so logical `nprobe`, rows, and results cannot change. Hilbert
  emission order already clusters near cells, so the nprobe nearest usually
  collapse to a few contiguous runs.
* *Cache chronology.* The initial probe deliberately bypassed the
  whole-Region-A invariants entry because its purpose is selective cell reads.
  #1227 later routed those probed Region-A ranges and the whole-Region-B
  fallback through `SurvivorRangeCache`; control metadata remains in the
  invariants surface. Recall is independent of cache residency.
* *Current configuration:* `PROXIMADB_PAX_READ_COARSE_PROBE` is a kill-switch;
  `PROXIMADB_PAX_READ_COARSE_NPROBE` is the explicit eval override. Unset uses
  the geometric TOML/default formula in the current serving contract above.
* *Gate.* `coarse_probe_holds_recall_and_cuts_bytes` (per-PR lib test, so it runs
  in the LIGHT tier) writes a v3 segment via the compaction writer, then asserts
  the probe **engaged** (`0 < cells_probed <= nprobe`), read **materially fewer
  bytes** than the whole-region baseline, and **held recall vs the full scan**
  (probe recall ≥ baseline − 0.1 on clustered data). Diagnostic counters ride
  `PROXIMADB_TRACE_GETS` (`drain_get_trace` / `drain_probe_trace`).
* *Completed follow-ups:* #1144 promoted `ivf_cells_total/probed`,
  `ivf_probed_rows`, `ivf_fetch_rounds`, and `ivf_whole_region_fallback` into
  durable io-trace/Prometheus surfaces; #1148 landed the SIFT ratchets/evidence.
  #1153 measured A0/header prefixing, split metadata caching, and Region-A
  coalescing; those component controls remain independently default OFF.
  #1210/#1234 promoted the trained writer/reader, #1227 closed the probe-path
  survivor-cache seam, and #1252 fixed authoritative TOML wiring. Production
  object-store and scale/high-dimension/multi-metric runs remain hardening.

== PR-C2 delta (trace-derived control and Region-A layout proof, rev 3.4)

PR-C1's full SIFT1M trace separated the v3 request budget into search control,
footer metadata, Region A, Region B, and Region D. It showed that the v3 probe
moved bytes but paid three small control reads per file and one Region-A read per
selected cell. PR-C2 tests three independently gated reader changes:

* a bounded prefix range supplies the prefix, A0, and RaBitQ parameters when
  their extents fit, without moving the fixed-offset header contract;
* small search-control/footer invariants may populate the byte-budgeted cache
  even though the probe never fetches the complete Region A; and
* `ObjectRangeCoalescePolicy` merges selected Region-A extents across a bounded
  gap, retaining exact selected slices so over-read cannot broaden ranking.

The release-profile 100-query SIFT1M proof retains the combined 64 KiB prefix +
split metadata cache + 1 MiB Region-A gap (4 MiB maximum range). Recall is
unchanged: `0.985` single-file (`nprobe=6`) and `0.982` four-file (`nprobe=3`).
GETs/query move `33→30` cold and `24→19` warm single-file; `75→65` cold and
`62→44` warm four-file. Under `20×GET + 5×MiB`, score improves 6.1–24.0%.
A 4 MiB gap is rejected as non-robust: it buys at most one additional four-file
warm GET while adding 3.59 MB/query and worsening the single-file cold tail.
See `RDSTRAT8_IVF2_LAYOUT_COALESCING_SIFT1M_2026_07_22.adoc` and evidence claim
`rdstrat8_ivf2_layout_coalescing_sift1m`.

The trace also identifies the next independent term: Region B still consumes
`22–42` GETs/query. Any successor work must optimize survivor-run geometry at
fixed recall/`nprobe`; file count remains the collection-level GET knob.

== Eval gates (ADR-062 pattern; eval doc in `docs/_internal/status/`)

[cols="3,2", options="header"]
|===
| Gate | Target
| SIFT1M recall@10 / GETs | ≥ 0.98 and materially below the `PXH1` baseline; report **per segment and per collection** with identical live-file geometry
| SIFT1M bytes/query | materially below baseline with Region-A and Region-B bytes reported separately; no unledgered fixed 15 MB assertion
| Whole-region fallback | frequency and bytes decrease materially; never silently disables the probed path
| 10M SIFT-shaped | measure, do not linearly project: recall, GETs, bytes, fetch rounds, cache footprint, and cold p50/p95 on a production backend or faithful emulator
| High-dim + metrics | representative 768/1536d corpora; L2, cosine, dot; each profile pins an `nprobe` recall knee
| Compaction overhead | bounded wall, peak memory, staging bytes, and write amplification vs current `cluster_plan_pca_ivf`
| Mixed/live files | `PXH1` + successor results merge correctly; collection-level budget holds at the configured L0/compaction file-count ceiling
|===

=== Bounded-parallel `read_ranges` (rev 2.1 — orthogonal work item, PR-B or standalone)

`FileSystem::read_ranges`' default impl is a **sequential loop** (TD-RDSTRAT-6: "the GET
win is the coalesce policy's"). Add a bounded concurrent executor: issue the ranges of a
round in parallel, completing into the decode pipeline. Sizing: the GETs themselves are
I/O-bound (async in-flight may exceed core count; the real ceilings are store throttles —
S3 ~5,500 GET/s/prefix, Azure per-partition limits — and NIC bandwidth); the
**~40%-of-cores bound applies to the decode-coupled completion work** (zstd, SQ8 parse,
distance kernels), with pure prefetch allowed a higher in-flight cap. Env:
`PROXIMADB_READ_RANGES_PARALLEL` (0 = sequential, default until measured),
`PROXIMADB_READ_RANGES_INFLIGHT` (prefetch cap). Benefits `PXH1` segments equally; lands
independently of the successor layout. Acceptance: SIFT1M cold p50 improves ≥2× at equal GETs;
io_trace gains `fetch_rounds` + `max_inflight` fields.

== Historical execution plan and remaining hardening

[cols="1,3,3", options="header"]
|===
| PR | Scope (files) | Acceptance
| **A — plan+format** | Extend `ClusterPlan` with the existing PCA model/centroids/runs; A0 serde; successor header/footer; no second k-means and no mandatory D-block padding | byte round-trip, deterministic segment, checked offsets, old/new dispatch, clean unsupported-version error, gate OFF emits byte-identical `PXH1`
| **B — reader, fixed nprobe** | successor branch in `rabitq_search_segment_coalesced`; pure sub-range RaBitQ-rank primitive; A0 rank → Region-A/B subreads; trace fields; exact mode probes all | sub-range rank parity with whole `RaBitQRegion`; SIFT100k parity/recall; mixed collection correct; corrupt/mismatched A0-footer fails closed; no whole-read masquerading as probed success
| **C — collection economics** | invariants/survivor cache interaction; bounded-parallel reads; multi-file compaction budget; measured `nprobe` profiles | SIFT1M representative cold/warm traces meet the table above; trace reconciles physical backend counters
| **D — scale hardening** | 10M + high-dim/multi-metric evals; optional spill only if measured; dated evidence + ledger | preserve or raise the current recall/cost ratchets; recalibrate defaults only from measured evidence
|===

== Open questions (narrowed from rev 1)

1. Does the existing `k = N·dim/IOP` remain optimal when Region A and B have different
   bytes/row? Derive the default from the combined measured probed bytes, then sweep around it.
2. Which model fields are minimally sufficient for stable query projection and future reader
   compatibility? Version the numeric contract and checksum the directory.
3. Spill necessity: only if representative recall misses at a commercially acceptable fixed
   `nprobe`; duplication must be charged to persisted bytes and compaction cost.
4. At 100M, does a second directory level beat larger cached A0 plus SIMD centroid rank? File
   only after the one-directory design is measured; do not pre-commit a hierarchy.
