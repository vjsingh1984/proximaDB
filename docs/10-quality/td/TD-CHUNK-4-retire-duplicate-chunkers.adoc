// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-CHUNK-4: Retire the duplicate chunkers — ~3,900 dead lines, deletable before any behaviour changes (ADR-091 D6, Phase 4)

[cols="1,3",options="header"]
|===
| Field | Value
| Status | **Open — filed 2026-08-17 with ADR-091.** The dead-code half needs no port and no suite and can start immediately; the live-code half is gated on TD-CHUNK-2's migration.
| Dim | D4 (compute-per-modality)
| Pillar | OE — secondary SUS
| Blocks | Nothing
| Depends on | Dead-code slices: nothing. Live-code slices: TD-CHUNK-2 (consumers must be on the port first).
|===

== The census, split by what it costs to remove

Verified by grepping for real importers, distinguishing product callers from tests. Victor's *live*
product chunking is only about **470 lines** in two paths; almost everything else is residue.

[cols="3,1,3",options="header"]
|===
| Target | Lines | Why it is safe, and what it costs
| Victor `victor/processing/native/chunking.py` | 258 | **Zero callers, including tests.** Free. Do this first.
| Victor `victor/framework/ingestion/` | 1,415 | **Zero importers package-wide.** A *verbatim fork* of victor-rag's chunker (differences are stripped comments and a `self.config → self._config` rename). Retire victor-rag's false docstring at the same time — it claims to wrap this module's `BaseChunker` and imports nothing from it; the refactor was announced in a docstring and never executed, in the opposite direction from reality.
| Victor `victor/core/chunking/` | 1,559 | Reachable only through a `register_chunker()` shim that is explicitly deprecated, emits a warning, and has **no registered entry points** anywhere — so external discovery is a permanent no-op. Note it is a behavioural *divergence* from the other two copies, not a verbatim one, so diff it before deleting rather than assuming equivalence.
| Victor `verticals/victor-coding/native/src/chunker.rs` | 530 | Duplicates the built Rust chunker and is **not even built** (`ModuleNotFoundError`), fronted by a backend registry with no reachable backend. Free.
| ProximaDB `crates/modalities/proximadb-embedding/src/chunker.rs` | 127 | **Zero callers** in the workspace. Its `ChunkStrategy::Heading` silently falls back to paragraph while being *persisted per-collection in the catalog* and exposed on the embedding-config surface — so the catalog can hold a policy nothing implements. Either delete, or implement it via TD-CHUNK-3's heading source.
| Victor `victor_coding/codebase/chunker.py` + `embeddings/chunker.py` | 1,962 | ~97% dead — only an 11-line `detect_language()` is live. **Gated:** the sweep also removes ~1,700 lines of tests, so it needs ADR-014 phase two verified first. Not a freebie; sequence last.
| ProximaDB `chunking_strategies/code.py` | 4,427 | Owned by **TD-CG2**, not this TD. Listed for completeness.
|===

Excluded deliberately: `build/lib/` copies (7 more chunking paths) and `.claude/worktrees/` (34 more
files) are stale build/worktree artefacts, not source. And `victor/agent/services/chunk_runtime.py` is
**not a text chunker** — it streams LLM response chunks. Pure name collision; leave it alone.

== The catalog-truthfulness item

Independent of deletion and worth doing regardless: stop persisting a `Heading` chunk-strategy policy that
the server does not implement. A per-collection policy that silently degrades is worse than an absent one,
because a caller reasonably assumes the catalog reflects behaviour. Either wire it (TD-CHUNK-3) or remove it
from the enum and the persisted config.

== Sequencing

. **Free deletions, any order, no port required:** `processing/native/chunking.py`,
  `framework/ingestion/`, the unbuilt `victor-coding` Rust chunker, ProximaDB's zero-caller
  `proximadb-embedding/chunker.rs`. ~2,330 lines. Retire the false victor-rag docstring with the second
  one.
. **`core/chunking/`** — diff against the other two copies first (it diverged), then retire with its
  deprecated shim. ~1,559 lines.
. **Catalog `Heading`** truthfulness fix.
. **`victor_coding/codebase/` sweep** — after ADR-014 phase two, accepting the ~1,700 test lines that go
  with it.

== Acceptance

Each deletion is its own commit with the grep that proves zero product importers quoted in the message —
so a reviewer can re-run the evidence rather than trust it. No behaviour change in steps 1-2 by
construction: nothing imports the deleted code. Full workspace build plus the TD-CHUNK-1 suite green after
each step.
