// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-58: Extract arrow block index → new proximadb-arrow-block-index crate (horizontal)

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export
| Component | `crates/horizontal/proximadb-arrow-block-index`; root `src/storage/engines/core/formats/arrow_block/mod.rs`
|===

== What moved
`src/storage/engines/core/formats/arrow_block/index.rs` (5 tests: `ArrowBlockIndex` +
`ArrowIndexEntry` block-level secondary index) → new horizontal crate `proximadb-arrow-block-index`.
Root `arrow_block/mod.rs` re-exports it (`pub use proximadb_arrow_block_index::index;`), so the
existing `pub use index::{ArrowBlockIndex, ArrowIndexEntry};` resolves unchanged. Deps: serde +
bytes + bincode (zero `proximadb_*` deps, zero `crate::` climb-out, `super::` test-only). NOTE:
`bytes` declared inline (`bytes = "1.5"`) — NOT in `[workspace.dependencies]`; serde + bincode are
workspace deps. Verified: standalone + clippy -D warnings + root lib all pass. SIFT recall ratchet
RUNS (storage_changes) under the 45m cap (#1555).
