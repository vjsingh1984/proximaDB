// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-46: Extract tcp_multiplexer → new proximadb-tcp-multiplexer crate (horizontal)

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export
| Component | `crates/horizontal/proximadb-tcp-multiplexer`; root `src/network/multiplex/mod.rs`
|===

== What moved
`src/network/multiplex/tcp_multiplexer.rs` (7 tests) → new horizontal crate `proximadb-tcp-multiplexer`.
Root `network/multiplex/mod.rs` re-exports it (`pub use proximadb_tcp_multiplexer::tcp_multiplexer;`),
so the existing `pub use tcp_multiplexer::{TcpMultiplexConfig, TcpMultiplexer, TcpProtocol};` resolves
unchanged for the unified-port server wiring. Deps: tokio + tracing (zero `proximadb_*` deps, zero
`crate::` climb-out, `super::` test-only). Carries the protocol-sniffing TCP accept loop that serves
REST + gRPC + Arrow Flight from one port. Verified: standalone + clippy -D warnings + root lib all
pass. SIFT recall ratchet SKIPS (network/** is not storage_changes).
