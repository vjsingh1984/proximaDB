// Copyright (C) 2026 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-SEARCH-3: Per-operation PAX reads are fragmented and mis-metered
:status: In Progress
:dim: D1
:pillar: PERF

[cols="1,3", options="header"]
|===
| Field | Value

| Status
| **In Progress.** The physical-operation counter and bounded prefix read are
implemented. A fixed-bed Azurite wire experiment proves that 24--32 MiB ranges
reduce physical GETs by 64--65% and that the Azure SDK does not split them.
The default-OFF exact-plan estimator now passes its exact-current BIGANN 1M
and 10M paired gates: wire GET/query fell 24.1% and 20.2% respectively with
identical aggregate recall, lower latency, and bounded bytes/RSS. The 3.3M
point reduced wire GET/query by 24.3% but exceeded the predeclared peak-RSS
ratio by 0.21 percentage points, and a serialized repeat reproduced that
shape-specific miss. Under bounded concurrency, however, both the 3.3M and
10M beds pass every gate at the concurrency-4 operating knee. No default is
promoted yet; paired DRAM/persistent-disk cache validation remains open.

| Priority
| **P1.** Operation count is a direct Hot-tier Blob cost and latency multiplier.
Byte amplification remains a separate latency, bandwidth, scanned-byte, and
possible egress concern.

| Component
| `src/storage/engines/sst/segment_format.rs`;
`src/storage/engines/sst/search/mod.rs`;
`crates/storage/proximadb-storage-filesystem-types/src/counting.rs`

| Trigger
| The SIFT1M investigation reported 205 GETs/query, but the benchmark and
counter were not first-principles sound: `read_ranges()` counted one logical
batch even when its default implementation issued one physical `read_range()`
per element. The original TD also assumed physical adjacencies that do not
exist in the v3 layout.
|===

== Cost model: operations and bytes are different axes

For the Azure Blob **flat Blob endpoint** used by active PAX segments, every
HTTP ranged GET is a read transaction. Hot-tier pricing has no separate
per-GB *data retrieval* fee, so a 72-byte and a 24-MiB range each contribute
one read operation. The exact transaction rate is region, redundancy, and
account dependent; `$0.005/10K` must not be described as a universal Azure
price.

That does **not** make bytes free:

* larger ranges consume network/NIC and memory bandwidth and can increase
  tail latency;
* cross-region/public transfer can incur bandwidth/egress charges;
* ProximaDB's own scanned-byte KRU term still meters bytes;
* Cool/Cold/Archive tiers add data-retrieval charges;
* hierarchical-namespace/Data Lake endpoints can have different transaction
  categorization, but ProximaDB does not route to them: `az://`, `azure://`,
  `adls://`, and `abfs://` are aliases for the same flat Blob backend. Path
  spelling therefore cannot select a different range-cost policy.

Therefore a merge is admissible only when the saved round trip is worth its
bounded gap over-read. Optimizing GET count without a byte cap merely moves
the cost.

== Actual v3 layout and dependency chain

The persisted order is:

[source,text]
----
[fixed header, 72 B]
[A0 coarse model + cell directory, variable]
[Region A: RaBitQ header + presence bitmap + cell-major codes]
[Region B: SQ8 params/codes]
[optional OID-position resolver]
[Region D blocks: OIDs, metadata, optional f32 stripes]
[footer index, variable][footer_len][magic]
----

The fixed header is at byte zero and mirrors all region offsets, including
`footer_off/footer_len`. This is deliberate: the reader can discover the
footer with the first ranged GET and does **not** need an object-store `HEAD`
plus tail seek.

The footer is variable because it contains the block table, schema/encoding
map, and optional resolver metadata that are known only after blocks are
written. Keeping it at the tail preserves streaming assembly and mixed-version
readability. Moving it beside the header would require a second-pass rewrite
or a bounded reserved hole and would not eliminate the need for the fixed
header. Duplicating the entire footer in a prefix would also make format
evolution and integrity reconciliation harder.

The efficient policy is:

* keep the fixed routing header at byte zero;
* keep the authoritative variable footer at the tail;
* retain both in `SegmentInvariantsCache` and persistent L2;
* populate them from writer-owned bytes only after atomic publication.

Thus header/footer placement is not the steady-state GET problem. A working
warm tier makes their steady-state object GET count zero.

== Correct adjacency analysis

The prior version made three incorrect assumptions.

. **A0 is not always “a few KB.”** Its exact size is
  `fixed + 4*dim + 4*ncomp*dim + 4*k*ncomp + 4*k + 72*k + checksum`.
  It grows with dimension and cell count.
. **A0 is not generally adjacent to the first selected cell.** Region A has
  its own header and presence bitmap, and the nearest probed cell may occur
  after codes for unselected cells. Joining them can require gap over-read.
. **The footer is not adjacent to the last Region-B survivor range.** The
  optional resolver and Region-D blocks lie between Region B and the footer.
  A `B+footer` merge would read most of Region D and is rejected.

Adjacent *selected cell extents* can still be coalesced, and non-adjacent
extents can be joined when both `max_gap_bytes` and `max_range_bytes` admit the
over-read. Logical selected slices remain unchanged, so recall is invariant.

== Implemented remediation

=== S1: physical GET accounting

`FileSystem::read_ranges(path, ranges)` now has an explicit plan/execute split
(#1649). `plan_read_ranges()` converts logical ranges into physical reads plus
an input-order slice mapping under an optional `RangeCoalescePolicy`; execution
issues exactly the physical plan and reconstructs the original logical result
vector. With no policy the plan is the byte-identical identity path.

`CountingFileSystem` delegates policy lookup through its decorator chain and
meters `plan.physical.len()`, not `ranges.len()`. It separately records
coalesced over-read bytes so logical bytes returned and physical bytes fetched
cannot be conflated. The counter remains a prediction from the same plan;
`io_trace.range_gets` and the Azurite request log are the independent leaf/wire
measurements required for promotion. The encryption wrapper remains a
documented exception because it can materialize one encrypted object and slice
locally.

The invariant is:

[source,text]
----
unencrypted current backends with a stable policy:
planned physical reads == counted range operations == leaf range_gets
----

A Rust function call named `read_ranges` is still not evidence of one cloud
request; the physical plan and leaf/wire observations are.

=== S2: bounded prefix read

On a cold invariants miss, the reader now fetches a bounded prefix:

* default: 1 MiB;
* `PROXIMADB_PAX_PREFIX_PREFETCH_BYTES=0`: kill switch;
* explicit values are clamped to 8 MiB.

The same byte buffer supplies the fixed header and, when fully covered, A0,
the RaBitQ control header, and early selected-cell ranges. No bytes are
reinterpreted: every consumer takes its original exact logical slice.

This speculative prefix is intentionally bounded. If A0 or a selected cell
does not fit, the reader performs the normal exact ranged read. The expected
cold saving is workload/layout dependent and must be measured; it is not
asserted as “one GET per segment” for every segment.

=== S3: existing selected-range coalescer

`plan_probe_cell_ranges()` remains the correct cell-data seam. It sorts extents
in file order and merges only under `ObjectRangeCoalescePolicy` gap/range
bounds. It never changes `nprobe`, selected rows, or ranking inputs.

Region A now derives its unset policy from the same backend IOP target as
Regions B/D: `max_gap = max(target/4, 64 KiB)` and
`max_range = target`. The Azure profile is therefore a 1 MiB maximum gap and
4 MiB maximum range; local SSD remains 256 KiB/1 MiB. The two registered env
mirrors can override either value, including `0` for exact-adjacency/unbounded
diagnostics.

The 4 MiB Azure value is a **conservative read-planner policy**, not a flat
Blob billing quantum and not a proven SDK split boundary. The previous source
comment that described a hard 4 MiB SDK boundary was unsupported and is
removed. `AzureBlobFileSystem::read_range()` invokes
`object_store.get_range()` once, but the application counter above that call
cannot observe an HTTP-client retry or internal split. A wire-boundary
measurement is therefore required before interpreting application GETs as
Azure transactions for ranges larger than 4 MiB.

=== S3a: isolate range-cap effects before choosing cell count

The completed 4 MiB factorial remains the controlled baseline. One exact
768-dimensional BGE point on its immutable one-segment `k_c=30`, projection
width 32 bed, at `nprobe=12`, top-10, and 1,000 diverse queries measured:

[cols="2,1", options="header"]
|===
| Metric | Value

| Cells probed | 12 / 30
| Recall@10 | 0.9836
| Rows probed/query | 203,093.762
| Region-A bytes/query | 8,305,348.376
| Region-B bytes/query | 150,304,273.920
| Total bytes/query | 170,313,605.378
| Application physical GET/query | 49.542
| Invariants hit ratio | 0.999
| Survivor hit ratio | 0.0562387
|===

At this geometry, average Region-B bytes per cell are about 12.5 MiB. The
current 4 MiB maximum can therefore turn one selected cell extent into roughly
3–4 application ranges. That makes every cell-size/GET conclusion conditional
on the planner cap: it does **not** prove that smaller cells are inherently
required to reduce Blob operations.

`scripts/bench/range_cap_sweep.py` now defines the query-only falsification
experiment. It reuses the exact bed and fixed query slice, holds `nprobe=12`
and `max_gap=1 MiB` constant, varies only `max_range={4,8,16,32} MiB`, and runs
top-10 and top-20 in a fresh process per point. It records:

* application GETs and bytes from ProximaDB;
* actual GET requests received at the Azurite HTTP boundary, including range
  lengths, so SDK splitting/retries are visible;
* recall, p50/p95, Region-A/B attribution, cache hit and resident bytes; and
* sampled server peak RSS.

Azurite proves request shape/count through the production Azure backend; its
localhost latency is not Azure service latency evidence. Promote a larger Hot
Blob default only when recall is identical, wire GETs fall materially, bytes
are bounded, p50/p95/RSS do not materially regress, and the wire/application
ratio proves one HTTP request per issued large range. Repeat only eligible caps
on the natural `k_c=90` bed; use `k_c=200` afterward to measure the independent
byte/selectivity trade-off.

The harness snapshots the release binary into each run directory before
starting any server process and verifies its SHA-256. Cargo atomically replaces
profile binaries after a rebuild; launching later points from the mutable
`target/` path could otherwise mix revisions while retaining the hash captured
at startup. A pre-existing snapshot with a different hash now fails closed.

=== S3b: measured wire range-cap knee

The fixed-bed falsification experiment ran with an exact release binary from
source revision `16712f1ac830556ebf3f62f41aabbb595ea838d9` (binary SHA-256
`ff7675af778c04f280cf57fdf8093a49e51ec390a026baecc38de981b7996500`).
Every point used the same one-segment 491,655-row, 768-dimensional `k_c=30`,
width-32 bed, `nprobe=12`, 1,000 queries, a fresh process/cache, a 1 MiB gap
cap, and the production Azure backend against Azurite. The isolated-port
configuration SHA-256 was
`f316a004ac4ff30a4114ba7d38662edf1845eded1b50ed5464423da854b88740`.

The clean, caffeinated 4-versus-32 MiB run measured:

[cols="1,1,1,1,1,1,1", options="header"]
|===
| Range cap / top-k | Recall | Wire GET/q | App GET/q | MB/q | p50 / p95 ms | Peak RSS GB

| 4 MiB / 10 | 0.9837 | 49.543 | 49.542 | 170.314 | 537.98 / 777.13 | 4.656
| 32 MiB / 10 | 0.9837 | 17.084 | 17.083 | 176.132 | 200.15 / 252.32 | 5.228
| 4 MiB / 20 | 0.9815 | 49.529 | 49.528 | 172.225 | 559.17 / 634.07 | 4.651
| 32 MiB / 20 | 0.9815 | 17.169 | 17.168 | 178.973 | 208.53 / 256.69 | 5.158
|===

The wire counter saw exactly one HTTP range request per application-issued
range at both caps. The extra approximately one wire GET per process is the
non-range object request, amortized over 1,000 queries. Thus neither the SDK
nor Azurite imposes a 4 MiB range split. At 32 MiB, GETs fell 65.3--65.5% and
bytes rose only 3.4--3.9%, but peak RSS rose 10.9--12.3%. Top-10 therefore
fails the experiment's predeclared `candidate/base <= 1.10` RSS gate; 32 MiB
is not eligible for promotion.

A focused 4-versus-24 MiB run then measured:

[cols="1,1,1,1,1,1,1", options="header"]
|===
| Range cap / top-k | Recall | Wire GET/q | App GET/q | MB/q | p50 / p95 ms | Peak RSS GB

| 4 MiB / 10 | 0.9840 | 49.543 | 49.542 | 170.314 | 610.76 / 1197.05 | 4.654
| 24 MiB / 10 | 0.9836 | 17.958 | 17.957 | 176.003 | 527.17 / 975.94 | 4.950
| 4 MiB / 20 | 0.98155 | 49.529 | 49.528 | 172.225 | 604.23 / 849.99 | 4.658
| 24 MiB / 20 | 0.98150 | 17.846 | 17.845 | 179.088 | 273.99 / 392.39 | 4.949
|===

This second run had unrelated host compilation and benchmark load. Its latency
is therefore retained for provenance but is not accepted as isolated latency
evidence. The operation shape, bytes, SDK ratio, and process RSS reproduced:
24 MiB reduced wire GETs 63.8--64.0%, bounded bytes to +3.3--4.0%, and bounded
RSS to +6.2--6.4%. It also crosses the `<=20 GET/query` economic threshold.

Repeated 4 MiB controls span 0.9835--0.9840 at top-10, so requiring exact
aggregate equality across fresh processes would mistake repeatability noise for
a quality regression. The promotion gate instead requires candidate recall to
remain at least 0.98 and permits at most 0.0005 absolute loss versus the paired
4 MiB control. On 1,000 top-10 queries that margin is five ground-truth hits out
of 10,000 and equals the observed control envelope; it does not lower the
quality ratchet. The 24 MiB point satisfies this aggregate gate.

Exact ordered result equality is diagnostic, not an acceptance requirement.
The benchmark now records per-query ordered-ID, ID-set, recall-hit, and
result-count hashes without retaining user IDs, plus total hit delta and counts
of queries that gained or lost hits. An exact-current-binary rerun must rule out
a systematic result-set shift and confirm the paired non-inferiority result
before the default can change.

These measurements invalidate two prior shortcuts: 4 MiB is neither a Blob
billing quantum nor an SDK request quantum, and the `k_c=90` GET minimum
measured under the 4 MiB cap is not yet a provider-independent geometry result.
Repeat the eligible cap on `k_c=90` at iso-cap before choosing cell count.

=== S3c: exact-current scale points and adaptive estimator contract

The exact-current release binary from source revision
`758fec2df1c995e8d909e4d9f45acf07b8d66394` (SHA-256
`2feac49987aff4bda157b2473387cb5e78c185bdf4d710f2ef0a56b236609f5a`)
closed the two immediate measurement gates through the production Azure backend
against Azurite. Each point used one immutable PAX v3 segment, 1,000 diverse
queries, a fresh process/cache, and one wire range request per application
range.

The 491,655-row, 768-dimensional, `k_c=90`, `nprobe=30` point measured 4 MiB
versus 24 MiB as follows:

[cols="1,1,1,1,1,1", options="header"]
|===
| top-k / cap | Recall | Wire GET/q | MiB/q | p50 / p95 ms | Peak RSS GiB

| 10 / 4 MiB | 0.9884 | 47.514 | 132.386 | 426.63 / 489.41 | 4.345
| 10 / 24 MiB | 0.9883 | 26.790 | 137.653 | 215.37 / 268.10 | 4.787
| 20 / 4 MiB | 0.9864 | 48.015 | 133.848 | 439.91 / 513.39 | 4.359
| 20 / 24 MiB | 0.9864 | 26.929 | 140.304 | 223.76 / 279.81 | 4.710
|===

At top-10, 24 MiB reduced GETs by 43.6% and halved p50 while adding 4.0%
bytes. Peak RSS was 10.176% above the baseline, missing the predeclared 10%
bound by 0.176 percentage points; this remains an honest gate failure rather
than being relabelled after the fact. Top-20 passed all declared gates.

The separate BIGANN 1M, 128-dimensional, `k_c=30` bed ran at its measured
top-10 quality knee, `nprobe=6`, and produced the cap curve below:

[cols="1,1,1,1,1", options="header"]
|===
| Cap | Recall@10 | Wire GET/q | MiB/q | p50 / p95 ms

| 4 MiB | 0.9800 | 10.077 | 28.56 | 106.78 / 144.62
| 8 MiB | 0.9800 | 8.177 | 33.11 | 76.30 / 104.03
| 12 MiB | 0.9800 | 7.796 | 33.69 | 75.06 / 103.63
| 16 MiB | 0.9800 | 7.657 | 33.71 | 74.64 / 103.67
| 24 MiB | 0.9800 | 7.647 | 33.71 | 75.79 / 104.16
|===

The result disproves a universal range cap. At 1M, 8 MiB is the latency knee,
16 MiB is the GET knee, and 24 MiB adds no material benefit. The 24 MiB point's
18.0% byte amplification is not an Azure Hot retrieval charge, but it is 5.15
MiB/query of additional NIC, cache, and memory traffic. At one million queries
that is about 5 TiB of extra same-region traffic. A single-query latency win
therefore needs a concurrency/RSS guard rather than an arbitrary requirement
that bytes be identical.

The implementation successor is a deterministic estimator at the shared
engine-core read-planning seam:

. Preserve explicit range/gap env overrides as the highest-precedence
  diagnostic contract.
. Under the existing default-OFF
  `PROXIMADB_STORAGE_READ_STRATEGY_CHOOSER`, enumerate provider-bounded caps:
  Azure 4/8/12/16/24 MiB, S3/GCS 8/16 MiB, and local
  1/2/3/4/6/8 MiB. This is read-only policy; writer block/cell geometry remains
  unchanged.
. Run the real Region-A/B/D coalescing state machine for each candidate. The
  resulting `(cold-miss planned GETs, planned bytes)` are exact for the selected
  cells/rows, so vector count and dimension need not match a benchmark lookup
  table. Subsequent DRAM/disk hits can only remove canonical-store requests;
  cache residency is measured separately rather than guessed by this planner.
. Score `20*GETs + 5*MiB` for active Hot/standard cloud objects, matching the
  neutral ADR-052 route-cost ordering. Reject candidates above 1.25x baseline
  bytes. For retrieval-billed Cool/Cold/Archive objects, raise the byte weight
  and tighten the guard to 1.10x. Local storage prices requests at zero and
  admits no byte amplification.
. Treat scores within 0.5% as the same knee and choose the smaller cap.
  This selects 16 rather than 24 MiB for the measured 1M curve.
. Emit baseline/chosen GETs, bytes, cap, gap, cost class, and candidate count.
  Unset gate or an invalid/empty decision fails closed to the existing fixed
  policy.

The benchmark beds calibrate the latency/RSS weights and validate the policy;
they are not a brittle table keyed by exact `(N, dimension, k_c)`. Once A0 has
selected cells, their actual byte extents make the cold-miss plan exact.
Regression/telemetry remains useful for concurrency latency and for choosing
the recall-safe `nprobe`, which is a separate quality problem.

This first estimator slice intentionally keeps the backend-derived gap fixed.
Enumerating larger gaps would be mechanically easy but would optimize an
unmeasured second variable that can add bystander bytes discontinuously. A
paired gap sweep and concurrency/RSS evidence are required before admitting
gap candidates.

The engine-core contract accepts a canonical `ObjectAccessTier`, but the PAX
search caller does not yet receive per-object tier metadata. This slice passes
`None`, which deliberately resolves active object-store segments as
Hot/standard. Plumbing actual Cool/Cold/Archive placement to this leaf is a
prerequisite before enabling adaptive reads on retrieval-billed tiers; path
aliases alone must never guess a billing tier.

=== S3d: exact-current adaptive 1M checkpoint

The release-server binary built from `4697205c2c1431f0f5a74a86c50119c8487a6259`
(SHA-256
`48a727e840fbf9858051ed65e888ddae103f9c9584d5775193391a3610445511`)
ran the immutable one-segment BIGANN 1M bed (`d=128`, `k_c=30`, `nprobe=6`)
for 1,000 top-10 queries. Each policy used a fresh process/cache and the
Azurite wire observer reconciled one HTTP range request per application range.

[cols="1,1,1,1,1,1", options="header"]
|===
| Policy | Recall@10 | Wire GET/q | MiB/q | p50 / p95 ms | Peak RSS GiB

| Fixed 4 MiB | 0.9800 | 10.077 | 28.56 | 99.93 / 133.49 | 4.348
| Exact adaptive | 0.9800 | 7.647 | 33.71 | 68.44 / 94.63 | 4.571
|===

The estimator admitted query-local caps through 24 MiB (the largest observed
wire range was 19.26 MiB), while the 0.5% knee rule prevents a larger cap from
winning when its request benefit is negligible. Relative to the fixed policy it
reduced wire
GETs by 24.11%, reduced p50 by 31.51% and p95 by 29.11%, increased bytes by
18.04%, and increased peak RSS by 5.15%. All predeclared gates passed. The
ordered result and result-set identities matched for all 1,000 queries; recall
hit totals were identical. This is sufficient to retain the implementation
behind its default-OFF gate, not sufficient to flip the default across shapes.

Raw external artifact:
`/Users/vijaysingh/proximadb-bench-data/runs/range-cap-adaptive-1m-4697205c2/matrix.json`
(SHA-256
`3e0f4484dc71c9e3e259ec01edaf3a459faa318c7ce8ba20cb65b5de137af718`).
Azurite proves operation shape/count and relative localhost latency; it is not
Azure service-latency evidence.

=== S3e: exact-current 3.3M and 10M checkpoints

The same immutable release-server binary and paired harness then measured two
larger, one-segment BIGANN beds. The 3.3M bed used `d=128`, `k_c=100`,
`nprobe=16`; the 10M bed used `d=128`, `k_c=305`, `nprobe=22`. Both ran 1,000
top-10 queries per policy with fresh processes and exact-scope ground truth.

[cols="1,1,1,1,1,1,1", options="header"]
|===
| Shape / policy | Recall@10 | Wire GET/q | MiB/q | p50 / p95 ms | Peak RSS GiB | Gate

| 3.3M fixed 4 MiB | 0.9817 | 33.360 | 85.21 | 296.39 / 372.96 | 4.342 | baseline
| 3.3M exact adaptive | 0.9817 | 25.258 | 90.95 | 203.66 / 282.51 | 4.785 | RSS miss
| 10M fixed 4 MiB | 0.9802 | 52.903 | 124.79 | 509.23 / 784.17 | 4.378 | baseline
| 10M exact adaptive | 0.9802 | 42.232 | 126.84 | 414.79 / 593.99 | 4.702 | pass
|===

At 3.3M the estimator reduced wire GETs by 24.29%, p50 by 31.29%, and
p95 by 24.25%, while increasing bytes by 6.73%. Its peak-RSS ratio was
1.10209 against the predeclared maximum 1.10, so this point is correctly
ineligible for promotion; the 0.2089-percentage-point excess is not rounded
away. A serialized repeat measured a 1.10229 ratio, confirming a stable
shape-specific serial memory cost rather than sampling variance. The fixed and
adaptive processes had identical survivor/invariants resident bytes in both
runs, localizing the extra roughly 455 MiB to transient range buffers or the
allocator rather than persistent cache occupancy.

The 3.3M full pair reported one result-set mismatch at query 233 despite
identical aggregate recall and recall-hit totals. A focused fresh-process
replay made fixed and adaptive produce the same ordered and set hashes for that
query (and the replayed fixed result matched the earlier adaptive result).
Therefore exact identity is retained as a diagnostic rather than
mis-attributed to the read-range policy; the predeclared bounded recall gate is
the correctness decision. Raw artifacts:

* `/Users/vijaysingh/proximadb-bench-data/runs/range-cap-adaptive-3m3-4697205c2/matrix.json`
  (SHA-256
  `71eb37c8f786842cc36cad62f372842ef825b32790bacee1d0ecc2213eaf1d7f`).
* `/Users/vijaysingh/proximadb-bench-data/runs/range-cap-adaptive-3m3-q233-4697205c2/matrix.json`
  (SHA-256
  `8c7ada84f4c0d6fbbc74acc5bd0231484adecc608d49831c4b23f8a611f4fb4e`).
* `/Users/vijaysingh/proximadb-bench-data/runs/range-cap-adaptive-3m3-repeat-4697205c2/matrix.json`
  (SHA-256
  `a166d20b68284b017a397666af88b62fd2683a30b7666472049d55510719d0d6`).

At 10M the estimator reduced wire GETs by 20.17%, p50 by 18.55%, and p95 by
24.25%, while increasing bytes by only 1.64% and peak RSS by 7.39%. All
predeclared gates passed and all 1,000 ordered results, result sets, and recall
hit counts matched. The largest observed adaptive wire range was 24 MiB and
Azurite again reconciled exactly one HTTP range request per application range.
Raw artifact:
`/Users/vijaysingh/proximadb-bench-data/runs/range-cap-adaptive-10m-4697205c2/matrix.json`
(SHA-256
`3066919cc24561d84bbda137c376cc9c75009eeae6c1472ab7d639e5ebaddd9d`).

These points establish that the exact estimator bends the operation curve
without changing IVF geometry or recall, but do not justify an unconditional
default. Paired DRAM/persistent-disk cache evidence remains the promotion gate.
As elsewhere in this TD, Azurite supports wire
operation shape/count and relative localhost latency, not Azure service
latency.

=== S3f: bounded-concurrency operating knee

Serial latency and RSS are not a sufficient serving decision. A closed-loop,
bounded-concurrency extension therefore replayed the same 1,000 distinct query
indices at configured concurrency 1, 4, 8, and 16. Every fixed/adaptive point
used a fresh server and cache state, and achieved its configured peak
concurrency. Adjacent policy pairs limited host drift; a 10M fixed/concurrency-8
attempt that observed an unrelated `rustc` process was rejected and rerun.

[cols="1,1,1,1,1,1,1,1", options="header"]
|===
| Shape / concurrency / policy | Recall@10 | QPS | Wire GET/q | MiB/q
| p50 / p95 ms | Peak RSS GiB | Paired gate

| 3.3M / 1 / fixed | 0.9817 | 1.98 | 33.360 | 85.21 | 481 / 821 | 4.352 | baseline
| 3.3M / 1 / adaptive | 0.9817 | 4.05 | 25.258 | 90.95 | 229 / 398 | 4.787 | RSS miss
| 3.3M / 4 / fixed | 0.9817 | 4.53 | 33.162 | 85.38 | 900 / 1,142 | 4.461 | baseline
| 3.3M / 4 / adaptive | 0.9817 | 7.40 | 24.742 | 90.72 | 547 / 719 | 4.816 | pass
| 3.3M / 8 / fixed | 0.9817 | 4.59 | 32.951 | 85.36 | 1,777 / 2,220 | 4.525 | baseline
| 3.3M / 8 / adaptive | 0.9817 | 7.63 | 24.360 | 90.34 | 1,058 / 1,381 | 4.920 | pass
| 3.3M / 16 / fixed | 0.9817 | 4.44 | 33.924 | 88.79 | 3,661 / 4,722 | 4.684 | baseline
| 3.3M / 16 / adaptive | 0.9817 | 7.56 | 24.637 | 91.46 | 2,152 / 2,729 | 5.047 | pass

| 10M / 1 / fixed | 0.9801 | 2.37 | 52.903 | 124.79 | 434 / 519 | 4.378 | baseline
| 10M / 1 / adaptive | 0.9803 | 2.91 | 42.232 | 126.84 | 338 / 476 | 4.702 | pass
| 10M / 4 / fixed | 0.9801 | 3.05 | 51.880 | 124.14 | 1,348 / 1,621 | 4.506 | baseline
| 10M / 4 / adaptive | 0.9804 | 4.36 | 41.170 | 126.11 | 919 / 1,226 | 4.793 | pass
| 10M / 8 / fixed | 0.9802 | 3.02 | 51.411 | 124.13 | 2,691 / 3,364 | 4.610 | baseline
| 10M / 8 / adaptive | 0.9803 | 4.33 | 40.735 | 125.81 | 1,876 / 2,391 | 4.855 | pass
| 10M / 16 / fixed | 0.9801 | 2.44 | 51.209 | 124.69 | 6,240 / 10,677 | 4.758 | baseline
| 10M / 16 / adaptive | 0.9804 | 2.42 | 39.967 | 125.69 | 6,564 / 9,280 | 5.060 | pass by declared gates; overloaded
|===

Concurrency 4 is the common operating knee. At 3.3M, adaptive ranges reduce
wire GETs by 25.39%, improve QPS by 63.50%, reduce p50/p95 by 39.21%/37.00%,
and use 7.96% more peak RSS with 6.25% more bytes. At 10M, they reduce wire
GETs by 20.64%, improve QPS by 42.64%, reduce p50/p95 by 31.88%/24.41%, and
use 6.37% more peak RSS with only 1.59% more bytes. Concurrency 8 provides no
material QPS gain over 4 and roughly doubles latency; concurrency 16 is
overload, most visibly at 10M where both policies fall below their
concurrency-4 throughput.

At the illustrative `$0.005/10K` Hot-Blob transaction rate, the
concurrency-4 operation component falls from `$16.58` to `$12.37` per million
queries at 3.3M and from `$25.94` to `$20.59` at 10M. This excludes bytes,
compute, cache, and tier-dependent transfer charges and therefore is not a
complete rate-card margin.

Raw external artifacts:

* `/Users/vijaysingh/proximadb-bench-data/runs/range-cap-concurrency-3m3-128bdd70d/matrix.json`
  (SHA-256
  `7dc0b3a11b1205b7981444630b388aee7172bee1adb6f226da530abd4d7144a6`).
* `/Users/vijaysingh/proximadb-bench-data/runs/range-cap-concurrency-10m-128bdd70d/matrix.json`
  (SHA-256
  `76c4c61975358d40ba88551397bb7aa7cbee83b19d4a928c870d17d24b47795f`).

The 10M adaptive points gained one to three aggregate recall hits and the 3.3M
points had identical aggregate recall. A few result identities varied across
fresh processes, consistent with the already isolated tie/nondeterminism
diagnostic rather than range-policy data loss; no paired point lost more than
the predeclared 0.0005 recall margin. These localhost results establish the
relative concurrency knee and wire operation shape, not Azure service QPS.

=== S4: remove the redundant PAX-as-SST1 magic probe

The first passing release run after the persistent-tier implementation reported
exactly 2 physical GETs/query and 16 bytes/query in the local-disk-warm phase
with exactly 2 PAX segments. The source was not PAX header/footer loading.
Two generic legacy-SST helpers could perform an 8-byte `SST1` magic probe for
every discovered file before returning “not a legacy SST” for PAX:
`load_sst_header_centroid()` during file pruning and
`segment_vector_count()` during the adaptive exact-route decision. Removing
the centroid probe reduced the object-cold total, but a release rerun retained
exactly 2 GETs/query and 16 bytes/query; that isolated the adaptive count probe.

Discovery already classifies current PAX segments by the durable `.pax` suffix,
and PAX stores its partition model in A0 rather than a legacy SST header.
Both helpers therefore return `None` for `.pax` before acquiring a filesystem
or reading bytes. Unknown and legacy paths retain the magic sniff, so
mixed-format reads are unchanged. Regression tests use a nonexistent `.pax`
object to prove both PAX branches perform no I/O and a nonexistent `.sst`
object to prove both fallbacks still probe.

Expected effect for the measured two-segment geometry is local-disk-warm
2→0 GETs/query and object-cold approximately 42→40. This expectation is not
promoted as evidence until the release benchmark is rerun.

=== S5: preserve recall before minimizing nprobe

The settled one-segment SIFT1M file has 30 persisted A0 cells. A controlled
release sweep found that the old geometric default
`ceil(sqrt(30) * 1.0) = 6` missed the recall@10 ratchet on independent
1,000-query slices. Raising nprobe while enabling the bounded Azure planner
showed that recall and operations can both satisfy their gates:

[cols="1,1,1,1,1,1"]
|===
| nprobe/profile | Query slice | recall@10 | GET/query | MB/query | p50

| 6, local planner
| 6000..6999
| 0.9795
| 26.69
| 24.66
| 23.91 ms

| 7, Azure planner
| 7000..7999
| 0.9790
| 11.00
| 33.35
| 26.79 ms

| 11, Azure planner
| 2000..2999
| 0.9822
| 15.90
| 48.82
| 36.90 ms

| 11, persistent-local warm
| 1000..1999
| 0.9803
| 0
| 0
| 47.72 ms
|===

These are diagnostic sweeps over the immutable acceptance segment, not the
final post-change evidence run. They justify changing the default multiplier
to `2.0` (11 cells at `k_c=30`) while preserving the canonical
`recall@10 >= 0.98` ratchet. The byte increase is explicit: Azure Hot read-op
COGS falls with fewer operations, but scanned bytes and latency remain
separate constraints.

== Cell-count / nprobe scale matrix

The writer's natural cell law is IOP-derived, not `sqrt(N)`:

----
k_c = clamp(floor(N_usable * dim / 4 MiB), 2, 4096)
----

For 128-dimensional SIFT this yields approximately:

[cols="1,1,2"]
|===
| Rows in one settled segment | Natural `k_c` | Role in the experiment

| 100 / 1K / 10K
| 2 (minimum clamp)
| Degenerate/fallback correctness; not an asymptotic data point.

| 100K
| 3
| Small natural geometry plus a forced-`k_c` calibration sweep.

| 1M
| 30
| Current acceptance and cost point.

| 3.3M
| 100 (measured)
| First scale-validation point above 1M; one PAX v3 L1 segment.

| 10M
| approximately 305
| Extrapolation gate before claiming one calibrated rule scales.
|===

The planned matrix therefore separates corpus scale from query confidence:

. SIFT100K prefixes at 100/1K/10K/100K validate training floors, clamps,
  empty cells, and exact fallback. At 100K, force
  `k_c={2,4,8,16,32}` and sweep absolute `nprobe=1..k_c` to map the geometry
  rather than repeatedly observing the two-cell floor.
. SIFT1M uses the natural 30-cell geometry and at least 1,000 independent
  queries per reported slice.
. BIGANN/SIFT10M uses natural 3.3M/10M points (measured 100 / expected
  approximately 305 cells) with the same selected policy; 10M is a validation
  set, not another place to retune.
. Every subset needs ground truth generated against exactly that corpus
  prefix. `sift1m_get_reduction.py` now rejects `--rows` when the declared
  ground-truth scope differs, preventing full-SIFT1M neighbors from being
  misreported as subset recall. `scripts/bench/sift_exact_groundtruth.py`
  generates exact L2 ivecs blockwise and emits a SHA-256 manifest for these
  subset runs.

For each point record cell row-count skew, empty-cell fraction, radius
distribution, training sample/cell, normalized `nprobe/k_c`, probed-row
fraction, Region A/B/D reads and bytes, recall, and latency. Query counts
(100/1K/10K) narrow confidence intervals; they do not define geometry.

The geometric multiplier remains a deterministic default. The likely
successor is query-adaptive stopping using the persisted centroid distances,
radii, and cell row counts: continue probing until an admissible lower bound
or covered-mass criterion proves another cell cannot improve top-k. That model
must first be calibrated by the matrix above.

== Measured 3.3M validation point (2026-07-31)

The first scale point above 1M is now measured, not projected. BIGANN/SIFT
3.3M, 128d L2, one trained PAX v3 L1 segment, natural `k_c=100`, release
arm64 binary, Azurite through the production Azure HTTP backend, 1,000 diverse
queries per point, fresh process and empty DRAM/result caches for every
`(nprobe, top_k)` pair:

[cols="1,1,1,1,1", options="header"]
|===
| Result size | Minimum passing nprobe | Recall | Physical GET/query | p50

| top-10 | 11 | 0.9800 | 24.478 | 256.04 ms
| top-20 | 12 | 0.9812 | 27.320 | 298.93 ms
|===

The current `ceil(2 * sqrt(k_c))` policy selects 20 probes. At top-20 it
measured recall 0.9959, 41.12 GET/query, and 452.19 ms p50. Selecting the
minimum passing 12 probes therefore removes 33.6% of physical GETs and 33.9%
of median latency while preserving the 0.98 ratchet. It still needs about a
27% operation reduction to reach 20 GET/query; bounded coalescing and
singleflight remain necessary.

The scale relationship is sublinear, not a fixed probe fraction. The joint
floors at natural cell counts are `3/3`, `5/10`, `7/30`, and `12/100` for
100K, 330K, 1M, and 3.3M. A four-scale fit predicts about 18 top-20 probes at
an expected 10M `k_c` near 305, but that is only a validation schedule until
10M is measured. See
link:../../_internal/status/BIGANN_NPROBE_GEOMETRY_100K_3M3_2026_07_31.adoc[the
dated evidence artifact] for the full curves, hashes, shape distribution, and
the single-segment 10M memory limitation.

== Partial 10M natural-knee evidence (2026-08-04)

A later spill-enabled run produced one 10M PAX v3 segment with natural
`k_c=305`. Five provenance-identical interruption-safe checkpoints contain 7
of 20 unique planned `(nprobe, top_k)` points. The measured top-10 physical-GET,
byte, p50, and p95 knees all select `nprobe=30`: recall 0.9744, 66.119 GET/query,
163.04 MB/query, and 558.37 ms local-Azurite p50. The best sampled recall is
0.9782 at nprobe 64, so the 0.98 target is explicitly unattained in the partial
curve. Top-20 has only one point and no defensible knee.

This changes the interpretation, not the quality ratchet. The economy knee is
an observed cost/recall bend; the quality floor is the first point attaining an
explicit target. They must not share one field, and neither is a production
default without its own policy. The prior four-scale quality fit remains useful
as a validation schedule but did not predict a passing 10M point. See
link:../../_internal/status/BIGANN_NPROBE_GEOMETRY_100K_10M_2026_08_04.adoc[the
five-scale partial evidence artifact] for source hashes, coverage, fits, charts,
and remaining measurement gates.

== Remaining inefficiencies

* The filesystem trait's generic `read_ranges()` is bounded-concurrent but
  still N physical GETs. Concurrency reduces latency waves, not billable
  operations.
* Concurrent identical misses can race through the cache and issue duplicate
  object GETs. Singleflight by immutable `(path, offset, len)` is the next
  operation-only reduction.
* Exact range keys miss when one request asks for a subrange of a cached parent.
  The write-time SQ8 seed now has an explicit parent-range lookup; the general
  cache can later grow interval containment if evidence warrants it.
* `nprobe` multiplies both Region-A and Region-B work. A fixed reduction failed
  the current recall ratchet; radius/row-aware adaptive stopping is the safer
  path to fewer cells.
* Segment count remains the outer multiplier. Coalescing does not substitute
  for higher-level consolidation.

== Verification gate

Close this TD only with a benchmark that records, for the same fully
materialized segment geometry:

. physical GETs/query by PAX region/tier;
. bytes/query;
. cold and local-disk-warm p50/p95;
. segment count and level distribution;
. recall@10 (must not regress below the ratchet);
. prefix disabled (`0`) versus 1 MiB versus a justified alternative.

Before interpreting finer `k_c` as GET-optimal, repeat the adaptive policy on
the settled 3.3M and 10M beds and measure its DRAM/persistent-disk cache
interaction. Keep any
resulting policy tier-aware: Hot flat Blob in the same region may prefer fewer,
larger requests; Cool/Cold/remote placement must jointly price bytes and
requests. The measured sweep does not authorize a production-default change.

The benchmark must wait for flush/compaction quiescence and prove that all
1M vectors are searchable from materialized segments before restarting.

== Related

link:TD-COMPACT-10-l2-segment-consolidation-gap.adoc[TD-COMPACT-10],
link:../../12-design/adr/ADR-080-vector-search-compaction-tuning.adoc[ADR-080],
link:../../12-design/adr/ADR-065-clustered-tier-regions-single-segment.adoc[ADR-065],
link:../../12-design/adr/ADR-052-analytics-execution-competitiveness-codesign.adoc[ADR-052].
