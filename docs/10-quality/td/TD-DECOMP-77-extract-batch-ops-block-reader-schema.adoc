// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-77: Zero-risk wave — batch_operations + block_reader + columnar_schema; delete dead toml_config

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — survey-corrected moves (refs-in-comments ≠ coupling)
| Component | `crates/storage/proximadb-engine-core/src/proximablocks`; root columnar/proximablocks mod.rs
|===

== What moved (Phase 5 PR A, ~2.9K LOC)
The Phase-5 survey re-ranked candidates by WHERE refs point, flipping several "blocked"
verdicts:

* `proximablocks/batch_operations.rs` (834 LOC, 3 tests) → engine-core. Its "3 quantization
  refs" were ALL in commented-out test code — zero live `crate::` refs; live imports are
  block_structures + index_structures (already engine-core) + runtime_common's VectorMemoryPool.
* `proximablocks/block_reader.rs` (710 LOC) → engine-core. Field swap
  `Arc<UnifiedCachingFilesystem>` → `Arc<dyn FileSystem>`: the reader calls exactly ONE
  method (`read()`, 5 sites), and `UnifiedCachingFilesystem` implements the `FileSystem`
  trait (caching_filesystem.rs:270), so the helix call site (`helix/mod.rs:2249`) coerces
  implicitly. Bloom ref → foundation.
* `columnar/columnar_schema.rs` (644 LOC, 0 tests) → engine-core as `columnar_schema_types`.
  Deps are `ColumnarFileMetadata` + `QuantizationConfig`, both already in engine-core's
  `columnar_config_types` (TD-76).

== Dead code deleted
`columnar/toml_config.rs` (689 LOC) — NO module declaration anywhere; `ColumnarTomlStorageConfig`
referenced by nothing. Never compiled. (Second catch of this class after TD-74's strategy.rs —
the columnar tree has more undeclared orphans than expected.)

== Verified
engine-core standalone `--all-targets` ✓ + nextest **157/157** ✓ (was 152); clippy
`--lib --bins -D warnings` ✓; root lib ✓; workspace `--tests` shape ✓; boundary `--strict`
No findings; fmt ✓. SIFT RUNS (45m cap).
