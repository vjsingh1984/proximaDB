// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-17: Extract query optimizer (plan_calibration + gls) → proximadb-query-fusion

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export
| Component | `crates/query/proximadb-query-fusion`; root `src/query/federated/optimizer/mod.rs`
| Relates to | [[TD-DECOMP-1]] (recipe); [[TD-DECOMP-16]] (audit pattern)
|===

== Why

`plan_calibration.rs` (20 tests) + `gls.rs` (9 tests) in `src/query/federated/optimizer/` are
fully standalone — zero `crate::` + zero `super::` refs. They move to the existing
`proximadb-query-fusion` crate (query tier). `selectivity.rs` (11 tests) stays root-side
(it depends on root optimizer types via `super::`).

== What moved (behavior-neutral)

* Moved: `plan_calibration.rs` (20t) + `gls.rs` (9t) = **29 tests**.
* `selectivity.rs` returned to root (super:: dependency on optimizer Predicate types).
* Root `src/query/federated/optimizer/mod.rs`: re-exports from crate.
* Deps: serde + serde_json (workspace).

== Follow-ups

* **Running total once merged: ~611 tests out of root** (582 + 29).
