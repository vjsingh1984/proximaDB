// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-62: Extract geo index → new proximadb-geo-spatial-index crate (horizontal)

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export (4 import rewrites)
| Component | `crates/horizontal/proximadb-geo-spatial-index`; root `src/index/geo/mod.rs`
|===

== What moved
`src/index/geo/index.rs` (10 tests: `GeoIndex` geohash-based spatial index — radius/bbox/nearest-k/polygon
search) → new horizontal crate `proximadb-geo-index`. Root `index/geo/mod.rs` re-exports it
(`pub use proximadb_geo_index::index;`), so the existing `pub use index::{GeoIndex, GeoIndexConfig,
GeoIndexEntry};` resolves unchanged. Deps: `proximadb-geo-types` + `proximadb-geohash` +
`proximadb-geo-queries` — all three sibling crates from TD-DECOMP-59/60/61. Four import rewrites
(`super::types/geohash/queries` → respective crate paths). This COMPLETES the index/geo subsystem
extraction: 4 files (types + geohash + queries + index = ~31 tests) fully decomposed via the
hub-and-dependents pattern. Verified: standalone + clippy -D warnings + root lib all pass. SIFT recall
ratchet SKIPS (index/** is not storage_changes).
