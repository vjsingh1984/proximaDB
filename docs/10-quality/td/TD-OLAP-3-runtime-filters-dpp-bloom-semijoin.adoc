// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-OLAP-3: Runtime filters — Dynamic Partition Pruning (DPP) + bloom semi-join

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Open (slices A–C **landed 2026-07-04**: mechanism RESOLVED — DataFusion 54 natively builds join runtime filters (min/max bounds + in-list over HashJoin build keys, `enable_join_dynamic_filter_pushdown` on by default), so the custom-wrapper fallback is NOT needed. Landed: (A) `supports_filters_pushdown` on `ObjectStoreParquetTable` — static WHERE predicates finally reach `scan()` and prune row-group splits (was dormant), default ON, correctness-neutral (Inexact keeps the FilterExec); (B) `ProximaScanExec` absorbs `DynamicFilterPhysicalExpr` in Post-phase pushdown and prunes splits at stream-open via a conservative PhysicalExpr→`ScalarPredicate` translator, gated `PROXIMADB_DF_RUNTIME_FILTER_PRUNE=1` default OFF; (C) `splits_total`/`splits_pruned` io_trace counters — the skip ratio lands in the TD-OLAP-4 ledger automatically. First evidence run **2026-07-04** (`docs/_internal/status/TPC_PERF_GATE_EVIDENCE_2026_07_04.adoc`): both gates stability-neutral (38/38, latency parity) but skip ratio 0% — the harness's UNIFORM synthetic data defeats zone-map pruning, so gates stay default-off; promotion prerequisite = re-run over CLUSTERED data (sort fact tables by date/key in the generator, or faithful dbgen). Evidence v2 **2026-07-04** (`TPC_PERF_GATE_EVIDENCE_V2_2026_07_04.adoc`) root-caused the full chain: date-literal translation + row-group geometry fixed; the last blocker is the materializer discarding row order — link:TD-OLAP-6-sort-on-materialize-cluster-keys.adoc[TD-OLAP-6]. Evidence v3 **2026-07-05** (post-TD-OLAP-6 sort): STATIC pruning delivers 19.2% TPC-H split skip (default-on); the runtime-filter gate measured ZERO incremental pruning — stays default-off. Absorption INSTRUMENTED 2026-07-05: delivery works (Post-phase reaches the scan, downcast + absorption OK) but snapshot() stayed the lit(true) placeholder; two correctness fixes landed (first-poll deferral, then per-split late evaluation per DataSourceExec semantics) — still zero measured gain. 2026-07-05 deep-dive CLOSED most of the chain: the join DOES update+complete the absorbed filter (gen=2, 'bounds AND IN(SET)' with tight windows — live shape pinned by unit test); the probe/build RACE was real and is fixed by a wait_complete() stream head (0ms when resolved, 10s bounded fallback); star facts need an explicit cluster_key (INT date surrogates defeat the DATE heuristic — harness now declares it) — with all of that, per-split pruning DOES skip splits (52 measured). Evidence v4 **2026-07-05** (`TPC_PERF_GATE_EVIDENCE_V4_2026_07_05.adoc`) RESOLVED the v3 frontier: the "bytes_read does not drop / second unpruned execution" hypothesis is DISPROVED — `execute_datafusion` runs once per statement (the 2–3× was a conformance-test artifact: value-correctness re-runs + 60-char prefix truncation). Real root cause: `ProximaScanExec` reports `UnknownPartitioning(num_cpus)` → DataFusion's `CoalescePartitionsExec` drives partitions on SPAWNED tokio tasks, and the `IO_TRACE` `task_local!` does not cross `tokio::spawn` — so 84/96 object-store GETs (the row-group reads pruning saves) recorded into nothing (probe-confirmed); counted `bytes_read` was only the in-scope plan-time stats read, constant regardless of pruning. This is the SAME mechanism behind the "record_splits lands outside scope" counter no-op. FIX (landed this PR): hold the trace behind `Arc<IoTrace>`, capture a handle via `io_trace::current_handle()` in scope (`TracingObjectStore::wrap`, `ProximaScanExec::build`), record through it on spawned tasks. VERIFIED: `bytes_read` now tracks pruning — Q1 (2/30 pruned) 8.77 MB vs Q6 (25/30 pruned) 4.61 MB, both scanning lineitem (byte-identical before the fix); aggregate TPC-H skip now a measured/attributed 12.7%. Evidence v5 **2026-07-06** (`TPC_PERF_GATE_EVIDENCE_V5_2026_07_06.adoc`) — first A/B taken through the FIXED meter — OVERTURNS the v3 "zero incremental" reading: Slice B (runtime join filter) DOES prune the fact table where Slice A structurally cannot. On the TPC-DS star queries (q3/q42/q52/q55, which filter `date_dim` not `store_sales`), static pushdown prunes 0/17 fact splits (2.58 MB) while A+B prunes 11–13/33 and reads 1.37–1.57 MB — a 39–47% fact-scan byte reduction, correct rows. TPC-H shows only ±1 KB jitter (its fact predicates are already in the WHERE, so A handles them). A and B are COMPLEMENTARY: A for direct fact predicates, B for dimension-only predicates reaching the fact via the join. Evidence v6 **2026-07-06** (`TPC_PERF_GATE_EVIDENCE_V6_2026_07_06.adoc`) **PROMOTED the gate to default-ON**, clearing v5's three caveats: (1) larger-scale — the star-join reduction reproduces at scale 0.01 (full 38-query suite, TPC-DS −10.9%, q42/q52/q55 −44.1%, q3 −29.9%) and holds at 0.05; (2) `wait_complete()` stall-safety — the full conformance suite runs gate-ON with no 10 s fallback stalls (sub-second TPC-DS walls, all 38 pass); (3) `splits_*` double-count — FIXED (execute-time `record_splits` now adds only the dynamic skip to `splits_pruned`; `splits_total` stays the true candidate count). `PROXIMADB_DF_RUNTIME_FILTER_PRUNE` now defaults ON (opt out with `=0`); Slice A + Slice B both default-ON and complementary. Bloom semi-join slice **landed 2026-07-06** (default-OFF, `PROXIMADB_DF_BLOOM_SEMIJOIN=1`): the object-store Parquet read leaf's in-list split-pruning (`filter_prunes_split`) collapses when the key set spans a split's [min,max] (the high-NDV semi-join case); the bloom branch materializes a split's distinct join-key set (projected column read, cached per file/row-group/column) and tests it against a low-fpr `proximadb-bloom` built over the in-list — pruning a split iff *none* of its keys can be in the list. Direction is load-bearing: the bloom is over the IN-LIST (dimension) keys and SPLIT keys are tested against it, NOT the reverse — testing thousands of in-list keys against a per-split bloom collapses to "never prune" (~fpr false-positives per split keep it). Zero false negatives (bloom over the in-list has none; a matching split key always reads present; the FilterExec above re-checks exactly). Reuses `proximadb-bloom` per the one-primitive-two-consumers ISP. REMAINING: partition-level DPP **split out to link:TD-OLAP-9-iceberg-partition-spec-unblocks-dpp.adoc[TD-OLAP-9]** (blocked on an Iceberg partition spec — tables are written unpartitioned today, `iceberg.rs:18`), faithful-scale datagen (TD-OLAP-4). **Bloom semi-join promoted default-ON 2026-07-08 (ADR-056 MVP-1):** `PROXIMADB_DF_BLOOM_SEMIJOIN` now defaults ON (opt out with `=0`); correctness-neutral (bloom over the in-list has no false negatives; the FilterExec above re-checks exactly), measured 30–57% `bytes_read` reduction on TPC-DS star joins. The qa-gate `bytes_read` ratchet is the verification — a regression fails CI.)
| Priority | **P2a** in the ADR-052 execution order — blocked by TD-OLAP-2 (P0a) stats for costing when to build a filter. ISP: one `proximadb-bloom` primitive, two consumers (ANN pre-filter ADR-011 + OLAP semi-join)
| Severity | High — the single largest warehouse-class lever; 5–50× on star-schema joins (Databricks AQE / Snowflake SOS)
| Component | `src/query/query_optimizer.rs`, `src/query/federated/{optimizer,execution}`, PAX/Iceberg scan pruning, `proximadb-bloom`
| Governs | link:../../12-design/adr/ADR-052-analytics-execution-competitiveness-codesign.adoc[ADR-052] (invariant 3)
| Relates to | ADR-025 (deletion vectors — companion scan-avoidance), ADR-050 (cost routing), TD-OLAP-1 (scan prune stack), TD-OLAP-2 (stats), ADR-011 (filtered-ANN — the same idea on the vector side)
|===

== Why

The dominant cloud-OLAP cost term is bytes scanned. The biggest lever for star/snowflake
schemas — TPC-DS's dominant shape — is *runtime* filtering: at hash-join build time, push the
dimension-table keys down to prune fact-table partitions (DPP) and inject a bloom filter the
probe side tests before fetching rows (bloom semi-join). This is Databricks AQE + Snowflake
Search Optimization Service territory; it is where warehouse-class engines get 5–50× and
naive fixed-plan engines cannot follow. ProximaDB's bloom primitive
(`proximadb-block-format/src/stripe.rs`) and exact-vs-ANN pre-filter (ADR-011/028) are the
same idea on the vector side — converge, don't duplicate.

== Rollout (default-off, trace-gated)

. **Bloom semi-join:** at HashJoin build, construct a bloom filter over build-side keys;
  inject it into the probe-side scan (`LogicalNode::Scan` predicate) so non-matching stripes
  are pruned *before* fetch. Reuse `proximadb-bloom`. **Mechanism (pinned):** since the join
  executes inside DataFusion (ADR-052 invariant 1), the filter rides DataFusion's own
  dynamic/runtime-filter machinery where the pinned version supports it; otherwise a custom
  `ExecutionPlan` wrapper around the join captures the build-side keys and injects the bloom
  into the probe-side scan — the fallback is ours to ship, not blocked on upstream.
  **LANDED (2026-07-06; promoted default-ON 2026-07-08 — ADR-056 MVP-1):** in the object-store
  Parquet read leaf, the split-level in-list prune (`filter_prunes_split`) now falls back from
  min/max to a bloom when an in-list's cardinality ≥ `PROXIMADB_DF_BLOOM_SEMIJOIN_MIN_KEYS`
  (default 256). Build a low-fpr (~6.6e-5) `proximadb-bloom` over the in-list keys once per
  query; materialize each surviving split's distinct join-key set via a projected column read
  (cached per file/row-group/column, capped at 1M distinct keys); prune a split iff every one of
  its keys misses the in-list bloom. A key-family tag (Int/Float/Str/Bool) stored with the split
  keys keeps build- and probe-side byte encodings identical, so a cross-type mismatch can never
  drop a real key. **Direction is load-bearing** — testing thousands of in-list keys against a
  per-split bloom collapses to "never prune" (≈fpr false positives per split keep it); testing a
  split's bounded key set against a low-fpr in-list bloom prunes reliably. Zero false negatives;
  the FilterExec above re-applies exact `IN` semantics.
. **Dynamic Partition Pruning:** for partitioned/Iceberg tables, defer fact-scan partition
  selection until the dimension filter's runtime values are known; prune partitions from the
  manifest list at runtime. **Split out to link:TD-OLAP-9-iceberg-partition-spec-unblocks-dpp.adoc[TD-OLAP-9]** — blocked
  today because Iceberg tables are written unpartitioned (partition spec id 0, empty struct;
  `crates/storage/proximadb-iceberg-engine/src/iceberg.rs:18`), so there is no partition list to
  prune. Partition-spec support must land first.
. **AQE hook:** insert a re-optimization point at pipeline breaks (post-build) so the runtime
  filter + observed cardinality can adjust the probe plan (coalesce/repartition, join swap).
  (Full AQE is larger; this TD delivers the runtime-filter slice first.) **Upstream watch:** DataFusion issue #23194 (Add AQE). The runtime-filter mechanism is already RESOLVED via DataFusion 54 native `enable_join_dynamic_filter_pushdown` (no local shim to retire); if/when full AQE (re-optimization at pipeline breaks, join swap, repartition coalesce) lands upstream, adopt it directly rather than building a local equivalent.
. **Trace + promotion:** instrument `bytes_scanned_before/after_filter` per query (TD-158 is
  **Resolved** — the trace substrate is available); promote only where the measured skip
  ratio justifies the bloom-construction cost.

== Verification

* TPC-DS star-join queries (e.g. Q72/Q95-class, store_sales ⋈ item/date_dim) show a measured
  bytes-scanned reduction in the TD-OLAP-4 ledger, conformance unchanged.
* The same bloom primitive serves ANN pre-filtering (ADR-011) and OLAP semi-join — one
  implementation, two consumers (converge, don't duplicate).
