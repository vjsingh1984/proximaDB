// Copyright (C) 2026 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-RDSTRAT-12: Bounded-parallel FileSystem read_ranges — complete the parallel read path

[cols="1,3", options="header"]
|===
| Field | Value
| Status | **Rewired + re-measured (2026-08-27)** — IVF probe cold tail now batches through `read_ranges_prefetch`; parity proven, mechanism proven; ≥2× acceptance requires a high-RTT backend rerun. See §3.
| Priority | **P2** — performance optimization for cold queries; not correctness-blocked.
| Severity | Feature-complete; gate reachable and proven fired by positive control.
| Component | `crates/storage/proximadb-storage-filesystem-types/src/lib.rs` — `read_ranges_buffered`, `FileSystem::read_ranges`, `FileSystem::read_ranges_prefetch`
| Governs | Parallelism of ranged GETs in the `FileSystem::read_ranges` path, which serves PAX segment reads (Region-A/B reads, footer fetches).
| Relates to | TD-RDSTRAT-8 (IVF coarse probe — this is the orthogonal work item from §Bounded-parallel `read_ranges`), TD-RDSTRAT-6 (binary tier I/O baseline)
| Pillar | PERF (latency)
|===

== Problem statement

TD-RDSTRAT-8 §"Bounded-parallel `read_ranges`" (rev 2.1) identifies that cold query latency follows a **waves-not-sums model**: `latency ≈ rounds × RTT + Σ ceil(GETs_in_round / pool) × RTT + compute`. The sequential `read_ranges` loop (1 GET at a time) makes the first term dominate. A bounded concurrent executor (issue ranges in parallel, complete into the decode pipeline) turns cold latency from `GETs × RTT` into `rounds × RTT`.

The TD specifies two env knobs, acceptance criteria, and io_trace additions.

== Current state (partial)

[cols="2,3", options="header"]
|===
| Item | Status
| `read_ranges_buffered` helper | ✅ **Implemented** (lines 477-492 of `proximadb-storage-filesystem-types/src/lib.rs`) — order-preserving, short-circuit-on-error, unit-tested
| `PROXIMADB_FS_READ_RANGES_PARALLEL` | ✅ **Implemented** — env gate registered, default 0 (sequential)
| `FileSystem::read_ranges` parallel path | ✅ **Implemented** (lines 620-636) — delegates to `read_ranges_buffered` when `PARALLEL > 1`
| `PROXIMADB_READ_RANGES_INFLIGHT` | ✅ **Implemented** (2026-08-26) — `FileSystem::read_ranges_prefetch()` method with INFLIGHT cap (lib.rs:780-851); zero production callers
| `fetch_rounds` + `max_inflight` in io_trace | ✅ **Implemented for general path** (2026-08-26) — `io_trace::record_read_ranges()` + `read_ranges_fetch_rounds` / `read_ranges_max_inflight` metrics (io_trace.rs:684-686, 887-891)
| SIFT1M cold p50 ≥2× improvement | ⚠️ **Rewired + re-measured** — parity & mechanism proven (positive control); −4.3% p50 on ≈0-RTT local bed; acceptance needs high-RTT rerun (see §3)
|===

== Remaining work

=== 1. Add `PROXIMADB_READ_RANGES_INFLIGHT` prefetch cap

The TD specifies **two independent bounds**:

* `PROXIMADB_READ_RANGES_PARALLEL` (renamed in code to `PROXIMADB_FS_READ_RANGES_PARALLEL`) — the **decode-bound** concurrency cap (~40% of cores)
* `PROXIMADB_READ_RANGES_INFLIGHT` — the **pure prefetch cap**, for I/O-bound work that doesn't decode (zstd, SQ8 parse, distance kernels)

Current implementation has one cap (`PARALLEL`). Add `INFLIGHT` as a separate env var that:

* Defaults to a higher value than `PARALLEL` (e.g., 2× `PARALLEL`, or a fixed sensible default like 32)
* Is parsed + cached in `read_ranges_buffered` or its caller
* Controls the `buffered(N)` concurrency when prefetch is safe (caller can annotate "pure prefetch" via a separate method or flag)

Note: The TD text is ambiguous on **how** the caller signals "pure prefetch". Two options:

* **Option A:** Add a `FileSystem::read_ranges_prefetch` method that uses `INFLIGHT` (only for metadata/footer prefetch paths)
* **Option B:** Pass a "decode cost" hint to `read_ranges`; use `INFLIGHT` when decode is cheap/none

Decision gate: file a sub-TD or ADR to resolve the API shape before implementing.

=== 2. Add `fetch_rounds` + `max_inflight` to general io_trace

Today `fetch_rounds` exists only for IVF probe metrics (`src/storage/engines/sst/metrics.rs:46`). The TD requires these fields for **general `read_ranges` telemetry**, so the concurrency benefits are observable across all paths (footer fetches, Region-A/B reads, not just IVF).

Required:

* Extend the `io_trace` mechanism with `fetch_rounds` (count of parallel rounds issued) and `max_inflight` (peak in-flight reads in a round)
* Emit in `read_ranges_buffered` or its caller
* Surface in Prometheus metrics (`proximadb_read_ranges_fetch_rounds_total`, `proximadb_read_ranges_max_inflight`)

=== 3. Collect SIFT1M evidence

**Status (2026-08-27): rewiring COMPLETE; acceptance NOT MET on local-disk bed — structurally unevaluable there; high-RTT rerun required.**

==== Round 1 (2026-08-26): hypothesis falsified — gate inert

Original A/B (`INFLIGHT=0` vs `=32`) was a null result because NOTHING reached
the gated function: `coarse_probe_survivors` issued per-fetch `fs.read_range`
in a loop, and `read_ranges_prefetch` had zero production callers
(`rdstrat12_sift1m_inflight_inert` ledger entry).

==== Round 2 (2026-08-27): cold tail rewired through the batch primitive

Adopted option 1. `coarse_probe_survivors` now classifies every probed-cell
fetch up front (region Arc → prefix → invariants → survivor-RAM residency via
new side-effect-free `peek_memory_exact`/`TenantCache::peek_memory`) and issues
ONE bounded-concurrent `fs.read_ranges_prefetch` round for the TRUE-COLD tail;
consume replays the original loaders/get_or_fetch so TD-CACHE-6 tracking,
LRU admission, counters and error text are unchanged. Sequential shape when
the cap resolves ≤1. An LRU race between peek and consume fails safe to the
baseline loader (no panics). A first draft gating batches on
`survivor_cache.is_none()` was dead code in production (cache always `Some`)
and was replaced before measuring.

==== Measured parity + verdict

Object-cold parity across arms: GETs/q 13.80 = , bytes/q 46.18MB = ,
cells/q 11.0 = , recall@100 0.9874 = . Latency deltas: object_cold p50
55.37→52.98ms (−4.3%), post_write −2.4%, warm-disk −3.5%.
Mechanism-fired positive control:
`probe_cold_tail_records_read_ranges_metrics_when_inflight_armed` proves the
batch path executes and records `Some{fetch_rounds,max_inflight}` when armed,
None when cache layers absorb everything (that fixture lesson is encoded in
the test: disarm `PROXIMADB_PAX_PREFIX_PREFETCH_BYTES` and gap coalescing or
nothing reaches the cold queue).

Acceptance ("cold p50 ≥2× at equal GETs") **cannot be met on this bed**: the
file:// backend carries ≈0 RTT, leaving no round-trip term for wave
parallelism to collapse. Required next step: rerun this pair against real
Azure/S3 or Azurite behind an artificial-latency proxy (per
CODESIGN_MEASURE_ARC_SIFT1M_AZURITE_2026_07_26) before any default-flip.

Evidence: `rdstrat12_sift1m_inflight_batch_v2` (ledger) +
`docs/_internal/status/RDSTRAT12_SIFT1M_INFLIGHT_BATCH_V2_2026_08_27.adoc`;
round-1 falsification preserved at `rdstrat12_sift1m_inflight_inert` +
`RDSTRAT12_SIFT1M_INFLIGHT_INERT_2026_08_26.adoc`.

=== 4. Update ENV_GATE_REGISTRY

✅ **DONE** — row exists at `docs/12-design/ENV_GATE_REGISTRY.adoc:107`
(semantic / unset-default / opt-in tier, owner TD-RDSTRAT-12).
== Implementation notes

* The core `read_ranges_buffered` contract is correct and unit-tested — do NOT change its behavior
* `INFLIGHT` is additive: when unset, behavior is unchanged (current `PARALLEL` cap still applies)
* io_trace additions should reuse the existing `drain_get_trace` / `drain_probe_trace` pattern from `src/storage/traces/`
* Benchmark: reuse the SIFT harness infrastructure from `tests/sift_pax_recall_ratchet_test.rs` or the SIFT1M eval runs

== Historical context

The TD-RDSTRAT-8 §"Bounded-parallel `read_ranges`" text:

```
Add a bounded concurrent executor: issue the ranges of a round in parallel, completing into the decode pipeline. Sizing: the GETs themselves are I/O-bound (async in-flight may exceed core count; the real ceilings are store throttles — S3 ~5,500 GET/s/prefix, Azure per-partition limits — and NIC bandwidth); the ~40%-of-cores bound applies to the decode-coupled completion work (zstd, SQ8 parse, distance kernels), with pure prefetch allowed a higher in-flight cap. Env: PROXIMADB_READ_RANGES_PARALLEL (0 = sequential, default until measured), PROXIMADB_READ_RANGES_INFLIGHT (prefetch cap). Benefits PXH1 segments equally; lands independently of the successor layout. Acceptance: SIFT1M cold p50 improves ≥2× at equal GETs; io_trace gains fetch_rounds + max_inflight fields.
```

Current implementation:
* `PROXIMADB_FS_READ_RANGES_PARALLEL` (note the `FS_` prefix) ✅
* `PROXIMADB_READ_RANGES_INFLIGHT` ❌
* `fetch_rounds` / `max_inflight` ❌
* SIFT1M evidence ❌
