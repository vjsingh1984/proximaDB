// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-30: Extract AXIS tunnel → new proximadb-axis-tunnel crate (horizontal)

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export
| Component | `crates/horizontal/proximadb-axis-tunnel`; root `src/index/axis/mod.rs`
| Relates to | [[TD-DECOMP-1]] (recipe); [[TD-DECOMP-28]] (catapult leaf)
|===

== Why

`src/index/axis/tunnel.rs` (**10 tests**) is the AXIS tunnel routing primitive. Truly self-contained:
zero `crate::` climb-out, zero real `super::`, **`std`-only** (no external deps at all!), **0 external
consumers**. Not feature-gated (AXIS is a default feature).

== What moved (behavior-neutral)

* `src/index/axis/tunnel.rs` → `crates/horizontal/proximadb-axis-tunnel/src/tunnel.rs`.
* Root `src/index/axis/mod.rs`: `pub mod tunnel;` → `pub use proximadb_axis_tunnel::tunnel;`.
* New horizontal-tier crate with **empty `[dependencies]`** (std-only). All 3 Cargo.toml entries wired.

== Follow-ups

* **Running total once merged: ~970 tests out of root** (960 + 10).
* Standalone crate `--all-targets` compiled in **0.31s** (std-only, no deps to compile).
