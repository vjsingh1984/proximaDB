= TD-DECOMP-34: Extract rank/search metrics → observability-engine (existing crate)

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export into existing crate
| Component | `crates/modalities/proximadb-observability-engine`; root `src/core/search/rank/mod.rs`
|===

== Why
`src/core/search/rank/metrics.rs` (13 tests, prometheus+kernel+rank_core deps) moves into the
existing observability-engine crate (which already has these deps from TD-DECOMP-2's rank_metrics
extraction). Zero root Cargo.toml changes.

== What moved
* `rank/metrics.rs` → `observability-engine/src/search_rank_metrics.rs` (renamed to avoid collision
  with the existing `rank_metrics.rs` from TD-DECOMP-2).
* Root `core/search/rank/mod.rs`: `pub mod metrics;` → `pub use proximadb_observability_engine::search_rank_metrics as metrics;`.
* Engine lib.rs: `pub mod search_rank_metrics;`.
