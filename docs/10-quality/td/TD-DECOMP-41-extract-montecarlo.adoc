// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-41: Extract montecarlo → new proximadb-montecarlo crate (horizontal)

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export
| Component | `crates/horizontal/proximadb-montecarlo`; root `src/compute/mod.rs`
|===

== What moved
`src/compute/montecarlo/mod.rs` (8 tests) → new horizontal crate `proximadb-montecarlo`
(module file `src/montecarlo.rs`). Root `compute/mod.rs` re-exports it
(`pub use proximadb_montecarlo::montecarlo;`), so the consumers
(`crate::compute::montecarlo::mc_price_batch_seq` / `black_scholes` in
`src/datafusion/udf.rs`) resolve unchanged. Deps: rayon + rand (zero `proximadb_*`
deps, zero `crate::` climb-out). Verified: standalone + root lib both pass.
Carries the Black-Scholes pricer + parallel Monte-Carlo path simulator.
