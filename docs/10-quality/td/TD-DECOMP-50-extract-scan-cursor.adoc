// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-50: Extract scan_cursor → new proximadb-scan-cursor crate (horizontal)

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export
| Component | `crates/horizontal/proximadb-scan-cursor`; root `src/services/mod.rs`
|===

== What moved
`src/services/scan_cursor.rs` (5 tests: paginated `ScanCursor` resumption state + base64
continuation-token codec) → new horizontal crate `proximadb-scan-cursor`. Root `services/mod.rs`
re-exports it (`pub use proximadb_scan_cursor::scan_cursor;`), so `crate::services::scan_cursor::*`
consumers resolve unchanged. Deps: proximadb-records + serde + thiserror + rmp-serde + base64
(zero `crate::` climb-out, `super::` test-only). NOTE: `base64` declared inline (`base64 = "0.22"`)
— NOT in `[workspace.dependencies]` (same gotcha as uuid/hex); the other four are workspace deps.
Verified: standalone + clippy -D warnings + root lib all pass. SIFT recall ratchet SKIPS
(services/** is not storage_changes).
