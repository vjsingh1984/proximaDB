// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-DECOMP-39: Extract geo_index → new proximadb-geo-index crate (horizontal)

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Landing
| Severity | Low — behavior-neutral re-export
| Component | `crates/horizontal/proximadb-geo-index`; root `src/storage/document/indexes/mod.rs`
|===

== What moved
`src/storage/document/indexes/geo_index.rs` (8 tests) → new horizontal crate `proximadb-geo-index`.
Root `storage/document/indexes/mod.rs` re-exports it (`pub use proximadb_geo_index::geo_index;`),
so the lone consumer (`use self::geo_index::GeoIndex;`) resolves unchanged. Deps: std + anyhow
(zero `proximadb_*` deps). Verified: standalone + root lib both pass. Carries the geohash
spatial index (`GeoPoint` + `GeoIndex`: point insert/update/remove + radius, bbox, nearest-k queries).
