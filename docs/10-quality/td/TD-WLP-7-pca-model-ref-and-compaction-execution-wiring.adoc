// Copyright (C) 2025 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-WLP-7: Compaction execution wiring — VOE/PCA sidecar proposal retired

[cols="1,3", options="header"]
|===
| Field | Value
| Status | **Resolved / partially superseded.** Part 1 execution wiring shipped in #1012 and remains the actuation seam reused by link:TD-RDSTRAT-9-collection-read-cost-envelope.adoc[TD-RDSTRAT-9]. Part 2 VOE emission + `pca_model_ref` is **retired—do not implement as filed**: its block-centroid-prune consumer is superseded by ADR-062/065. Persisting the same compaction plan now has a different in-segment consumer, Region A0 selective probing, owned by link:TD-RDSTRAT-8-two-level-ivf-coarse-probe.adoc[TD-RDSTRAT-8]; this does not revive the VOE sidecar.
| Priority | Resolved — Part 1 execution wiring shipped; current follow-ups are TD-RDSTRAT-8/9.
| Severity | Resolved / historical — background execution remains in use; VOE/PCA sidecar persistence is retired.
| Component | `src/storage/engines/sst/flush/coordinator.rs` (`post_flush_operations` — the historical "Compaction would be triggered here" stub); `src/storage/engines/sst/compaction.rs` (`schedule_compaction`/`perform_compaction_enhanced`); `src/storage/engines/sst/compactor_impl.rs` (`retrain_pca_model_for_compaction` — the persistence pattern to reuse); `object_economy_directory.rs` (`pca_model_ref` + `with_pca_model_ref`, currently write-only in tests)
| Governs | link:../../12-design/adr/ADR-061-per-collection-workload-profiles.adoc[ADR-061] (D3, arm-defaults amendment)
| Relates to | TD-WLP-4 (historical clustering slice), ADR-062/065 (current serving layout), link:TD-RDSTRAT-8-two-level-ivf-coarse-probe.adoc[TD-RDSTRAT-8] (current persisted-plan consumer), link:TD-RDSTRAT-9-collection-read-cost-envelope.adoc[TD-RDSTRAT-9] (current policy consumer), ADR-046 (LSN-coherent swap)
| Pillar | PERF (the armed default actually fires), REL (atomic swap read-consistency), OE (background execution observability)
|===

toc::[]

== Why

TD-WLP-4 landed the re-cluster mechanism (`write_pax_segment_compacted`, PCA+IVF ordering) and
armed compaction by default for `AppendBulk`, but two seams were deferred:

1. **Execution wiring.** `FlushCoordinator::post_flush_operations` still carries the historical
   stub — `compaction_triggered=true` feeds tiering accounting but never schedules the compactor.
   The armed default is only real end-to-end once the trigger schedules
   (`schedule_compaction` → `perform_compaction_enhanced`) in production code.
2. **Persisted model + directory ref.** [.line-through]#The WLP-4 write-time PCA is batch-local and discarded.
   Persist an `EnhancedPCAModel` at compaction (reuse `retrain_pca_model_for_compaction`'s
   `{collection_dir}/__model/pca_model.bin` pattern) and populate the VOE directory's
   `pca_model_ref` `ObjectRef`, so cold queries load the projection in one round trip and the
   read side can prune in PCA space (TD-WLP-6).# **← SUPERSEDED by ADR-062/TD-RDSTRAT-6.** The
   consumer (read-side block-centroid prune in PCA space) is the path ADR-062 retires; the VOE
   sidecar is replaced by the in-file footer-index. Any residual (persist the compaction cluster
   model) is re-specified as TD-RDSTRAT-8's in-segment Region-A0 contract; the old sidecar
   reference remains retired.

== Scope

NOTE: Only the **first** bullet (execution wiring, Part 1) shipped (#1012) and remains current. The
remaining bullets are **Part 2, superseded by ADR-062/065** (see Status) — do not
implement as filed.

* Wire post-flush triggering to actual background compaction execution — tokio-native (no
  non-daemon threads; deterministic-testable via a synchronous test hook), respecting
  `OrchestratorCompactionConfig::effective_for_collection_tags` and the WLP-2/4 arming cascade.
* [.line-through]#Persist the compaction-trained PCA model and set `pca_model_ref` (url/size/etag) in the VOE
  directory emission for compacted segments; compaction outputs must emit VOE directory entries
  (today only flush emits).# _(superseded — VOE sidecar replaced by ADR-062 footer-index)_
* [.line-through]#Read-consistency across the atomic segment swap during re-cluster (ADR-046 LSN-coherence) —
  the TD-WLP-4 gate item that rides on execution wiring.# _(carried by ADR-062 D6 atomicity: Iceberg-style fresh-name + manifest advance)_

== TDD gate

NOTE: Superseded along with Part 2 — the ADR-062 layout is verified by TD-RDSTRAT-6's own gates
(RaBitQ-scan recall at keep=100%, coalesced-GET count). Retained for provenance.

* [.line-through]#Integration: a flush that crosses the L0 threshold on an `AppendBulk` collection leads —
  without any manual compaction call — to a compacted `.pax` segment whose VOE entry carries
  populated centroids/radii and `pca_model_ref`; reads during the swap stay consistent.#
* [.line-through]#`Churn` collection under the same process: no compaction executes, no model persists.#

== Gating

Same cascade as TD-WLP-2/4: armed by default for `AppendBulk`, `compaction:off` opt-out,
`PROXIMADB_L0_COMPACTION_ENABLED` falsy = master kill-switch.
