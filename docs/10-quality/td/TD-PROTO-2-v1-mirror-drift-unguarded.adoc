// Copyright (C) 2026 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-PROTO-2: the hand-maintained v1 Rust mirror can drift from v1 .proto — and already has
:toc: macro

[cols="1,3", options="header"]
|===
| Field | Value
| Status | Implemented + ledger burned to ZERO in the same change (41 → 0)
| Severity | Medium — silent field/variant loss on binary decode; no guard existed
| Component | `crates/foundation/proximadb-proto/src/proto/proximadb.v1.rs` vs `proto/proximadb/v1/*.proto`
| Pillar | CORRECTNESS / TOOLING
|===

toc::[]

== Problem

The v1 proto package is NOT generated: `build.rs` regenerates only the
self-contained `proximadb.v2` package, and `proximadb.v1.rs` is hand-maintained
(the comment in build.rs says this is deliberate, to protect hand-maintained
serde impls on v1 types). Nothing in CI compiles the v1 `.proto` files against
the Rust mirror:

* `make proto-check` = `cargo check -p proximadb-proto` + Python contract
  tests — it checks the mirror *compiles*, not that it *matches* the `.proto`.
* The descriptor-byte-equality drift gate pairs only protos that have
  committed generated stubs — v1 has none.

Consequence: a field added to a v1 `.proto` but not to the mirror decodes
(binary wire) to `None`/default silently; a field added to the mirror but not
the proto is dead code. Both directions are invisible to every gate.

== Live instance (found by the #1724 fresh-agent review)

`proto/proximadb/v1/catalog.proto` declares `OltpCatalogConfig oltp = 17` in
the `CatalogConfig` oneof (fields 10-16 + 17) — while the hand-maintained
mirror's oneof covers tags 10-16 only and has no `OltpCatalogConfig` struct at
all. A binary-wire catalog config carrying field 17 decodes to
`config: None` today. (The live REST ingest path is JSON/serde, where the
mirror's serde derives are the authority, so the practical blast radius is a
binary-gRPC future — but that is exactly the direction the v2 push is going.)

The same review also corrected a mechanism claim this family had been making:
removing an enum value does NOT make prost reject the value on binary decode
(the `catalog_type` field is a raw `i32`; unknown values pass through), and
reserving a oneof field does NOT error on decode (unknown tags are skipped and
retained). Rejection for the retired Hive arm happens at the JSON/serde
boundary (unknown-variant hard error) — comments in
`proximadb.v1.rs` now say so.

== Candidate fix (pick one, measure maintainability first)

1. **Round-trip conformance test**: a small test that, when `protoc` is
   available, parses `proto/proximadb/v1/*.proto` descriptors and asserts the
   mirror's `#[prost]` tags/field names per message match (mirror→proto
   direction catches invented fields; proto→mirror direction catches missing
   ones). Skipped gracefully when protoc is absent (mirrors the v2 regen
   gate's env pattern).
2. **Generate the structural half**: keep hand-maintained serde, but generate
   the prost structure from the `.proto` into a check-in file and diff it in
   CI against the mirror's prost-derive surface (heavier; only if (1)
   proves insufficient).

== Implementation (2026-09-02)

Candidate fix **1** landed — option 2 (generating the structural half) was not
needed: the source-scan approach proved sufficient and stays hand-maintained
alongside the mirrors it guards.

* **Guard**: `crates/foundation/proximadb-proto/src/proto/v1_conformance.rs`
  (`#[cfg(test)]`, runs under CI's unit-test job which installs
  `protobuf-compiler`; skips gracefully when `protoc` is absent). It compiles
  `proto/proximadb/v1/*.proto` + `proto/proximadb/explain.proto` with `protoc
  --include_imports`, decodes the `FileDescriptorSet`, parses ALL five v1
  mirrors (`proximadb.v1`, `.cluster.v1`, `.streaming.v1`, `.explain.v1`,
  `.v1.ranking`) for their `#[prost]` attributes / derives / enum discriminants
  / name tables, and compares both directions per message: field names, tags,
  kinds, cardinality, map-ness, oneof member tags, enum wire-name →
  discriminant maps. Unparseable mirror syntax is a fail-loud anomaly, never a
  silent skip. Wire-compatible leniencies (documented in the file): a proto
  oneof may be modeled as same-tagged plain fields; message/enum Rust-name
  renames (RbacAuditEvent vs RBACAuditEvent) are not wire drift.
* **Ratchet**: current findings are diffed against the checked-in ledger
  `src/proto/v1_conformance_known_drift.json` — new drift fails, fixed drift
  fails until the ledger is shrunk (regen:
  `UPDATE_V1_DRIFT_SNAPSHOT=1 cargo test -p proximadb-proto --lib
  v1_conformance`). The ledger doubles as the burn-down backlog.
* **Live instances fixed in the same change**:
  ** `OltpCatalogConfig` message + `CatalogConfig` oneof arm `oltp = 17` +
    `CatalogType::{OltpPostgres,OltpMysql,OltpSqlite}` (10/11/12) — the filed
    instance. The REST construction arm declines loudly (TD-CAT-10 retirement)
    instead of constructing: the wire shape exists so binary configs decode
    losslessly, not to resurrect the adapter.
  ** `SqlValue` oneof arm `jsonb_value = 9` (bytes) + the custom serde
    (`serde_impls.rs`) round-trip + the ~20 exhaustive-match fan-out sites,
    each extending its local `BytesValue` convention.
* **First-run census**: the guard found 49 pre-existing findings; 8 were fixed
  above and the remaining **41 were burned down in the same change — the ledger
  now sits at zero** and any future finding is new drift:
  ** Never-mirrored messages/enums added to the mirror from their protos:
     the graph.proto PULSAR/QUASAR distributed-engine block (13 messages, 5
     enums incl. `GraphEngineType`), the collection_types.proto server-side
     embedding block (`EmbeddingConfig`/`EmbeddingRoute`/`ByoEndpoint`/
     `ByoAuth`/`MtlsCert`) plus `CollectionConfig.embedding_config` (tag 25)
     and `DocumentCollectionConfig.use_jsonb` (tag 8).
  ** `CollectionConfig.permitted_principals` was a real mirror bug: the prost
     attr said `message` over a `Vec<String>` (proto: `repeated string`) —
     fixed to `string`.
  ** The 6 mirror-only `assemble_*` messages (plus the whole generated
     `ContextService` client/server built on them) were **deleted**: absent
     from every proto, zero references anywhere in the workspace — dead wire
     types left behind when the protos dropped them (pre-GA no-back-compat
     applies). This is the guard's "invented types" direction doing its job.
  ** 3 of the "missing" messages were parser false-positives: one-line
     `pub struct X {}` units (prost's rendering of empty messages) — the
     guard now recognizes them.
* The guard caught a variant-name typo (`Polar` vs `Polaris`) introduced in
  this very PR before review did — the class of mistake it exists for.

A fresh-context adversarial review (2026-09-02) hardened the change; its
findings and dispositions:

**Round 2 (high effort, 2026-09-03) — 10 findings, all dispositioned:**

* **Writer emitted tag 8, not the declared tag 9** (MAJOR):
  `proxima_to_sql_value` encoded `ProximaValue::Jsonb` as `BytesValue`, so no
  write path ever produced the `JsonbValue` the canonical decode expects —
  the filter fix was unreachable. The writer now emits `JsonbValue`
  (mixed-read safe within the lineage: pre-PR segments carry tag-8 bytes that
  still decode via the legacy arm; the tag-9 variant did not exist before
  this PR). Pinned by `jsonb_writes_the_declared_tag9_variant`.
* **Six live wildcard fan-out misses** fixed (search-executor result metadata
  collapsed JSONB to `""`; nova string column, SST text-column support,
  document aggregation equality, document-block sort keys, timeseries tags
  dropped it) — each now decodes via the canonical representation.
* **Observability attribute filters**: the `JsonbValue` arm fed raw
  MessagePack through `from_utf8_lossy` (mojibake) into `key:value`
  matching, silently dropping matching logs — now decodes to compact JSON.
* **serde `jsonb_value` tagged form** now commits only for a SINGLE-KEY
  object whose value decodes — sibling keys can no longer vanish.
* **Guard: services were unguarded** — the exact drift class that motivated
  the TD (the deleted `ContextService` was found by luck, not by the gate).
  The guard now compares declared services (name + rpc set, snake-cased)
  against the tonic stubs, both directions. First run caught 7 real GraphService
  rpc gaps (the PULSAR/QUASAR messages were mirrored but their service
  methods were not) and the never-written `RankService` stub — all fixed by
  splicing in fresh `tonic-prost-build` output.
* **Guard: enum tables validated per-table** — a value must appear in every
  PRESENT name table; previously either table sufficed, letting a missing
  `from_str_name` arm hide behind a complete `as_str_name`.
* **One decode-or-binary helper** (`ProximaValue::from_jsonb_or_binary`)
  replaces the four copy-pasted match arms; the Arrow Flight codec renders
  compact JSON instead of a Debug byte dump; `MetadataValue` (which feeds
  equality) and its streaming.rs duplicate decode instead of emitting a
  `[jsonb]` placeholder.
* **Documented, not code** (mirror fidelity ≠ feature implementation):
  ** `CollectionConfig.embedding_config` (tag 25) and
     `DocumentCollectionConfig.use_jsonb` (tag 8) are contract-declared but
     feature-unimplemented — the proto fields pre-date this PR; the mirror
     now carries them faithfully. Wiring them is separate feature work.
  ** `permitted_principals`'s attr fix (`message` → `string`) changes the
     binary encoding of non-empty values — the OLD bytes were
     proto-nonconformant (prost wrapped each string in a StringValue
     submessage); JSON (the live REST authority) is unaffected, pre-GA
     no-back-compat applies.

**Round 3 (efficiency pass) — 6 findings, 4 fixed, 2 deferred with rationale:**

* Fixed: `full_precision_rank` no longer round-trips metadata through
  `serde_json` (the JSON detour decoded JSONB per candidate only to drop
  object/array payloads on the floor at the back-conversion) — the typed map
  is built directly from the proto items and the declared tag-9 variant
  passes through untouched; the document-block comparator gained a
  byte-equality fast path before the canonical JSON render; the malformed
  fallback in `jsonb_to_json_lossy` renders hex in ONE allocation; the dead
  `#[allow(dead_code)]` duplicate of the `MetadataValue` conversion in
  `streaming.rs` was deleted instead of extended.
* Deferred (accepted, tracked here): (a) `evaluate_filter_resolved` resolves
  each `Comparison` node independently, so a two-predicate range filter on
  one JSONB field decodes the same bytes twice per record — fixing it means
  per-record field-resolution caching across the resolver API (SST/HELIX/
  SWIFT callers); (b) the `SqlValue` → JSON arm still exists as ~7 local
  `sql_value_to_json` copies that should collapse into one shared converter
  beside `jsonb_to_json_lossy`. Both are behavior-neutral refactors — follow
  the repo's measure-first rule before touching the filter hot path.

**Round 4 (high effort, 2026-09-04) — 10 findings, 7 fixed, 3 dispositioned:**

* Fixed: the SECOND live writer — `proxima_value_to_sql_value`
  (search-types) served every engine search response with the legacy tag-8
  magic+JSON-text encoding, leaving the canonical path unreachable for
  reads; it now emits tag-9 MessagePack (pinned by a writer test), with the
  magic form still decoded for legacy results. pgwire's `sql_object_to_json`
  gained the JsonbValue arm (was silently null on the document-query
  surface). The observability tantivy `_all` index and the group-by key
  builder decode canonical JSON instead of `<jsonb:N>` byte-length
  placeholders (fulltext over JSONB attributes now matches; no legacy-index
  concern — the variant became producible in this change).
* Fixed: `full_precision_rank`'s rewrite restored the legacy placeholders
  for Bytes/Array/Object and the skip for unset-oneof keys, so output is
  unchanged on unchanged data except JSONB passes through typed.
* Fixed: the serde `{"jsonb_value": <base64>}` tagged form now also requires
  the decoded bytes to be VALID MessagePack (a genuine JSONB payload is, by
  construction) — generic single-key objects whose string merely
  base64-decodes fall through to ObjectValue. Residual ambiguity mirrors the
  pre-existing `bytes_value` tagged form.
* Fixed (guard): field/variant TYPE REFERENCES are now compared (enum attr
  refs and oneof payload refs vs the descriptor's type_name last segment) —
  a variant retargeted to the wrong struct/enum no longer passes on tag+kind
  equality. Also deleted the orphaned, not-in-module-tree
  `rest/wrappers.rs` copy, and aligned the MetadataItem lossy mapping to
  canonical JSON text.
* Dispositioned: (a) the filter seam renders legacy tag-8 JSONB as bytes —
  identical to pre-PR behavior (no regression); canonical filtering engages
  for tag-9 values; the writer comment now states this precisely instead of
  claiming full mixed-read equivalence; (b) the un-gated tag-9 writer flip
  follows the owner's pre-GA no-back-compat directive (replace in place,
  keep on-disk readability) — mixed-VERSION windows (rolling restart /
  rollback / Raft) drop tag-9 fields on old binaries, same as any pre-GA
  wire addition; (c) the one-shared-converter consolidation remains the
  tracked follow-up (deferred in round 3; round 4's concrete inconsistencies
  were fixed individually).

**Round 5 (independent rebase review, 2026-09-04) — 5 findings, all fixed:**

* The search-types writer's nominally unreachable encode-error fallback put
  legacy magic+JSON bytes under tag 9, violating tag 9's MessagePack contract
  and causing the new reader to return opaque binary. The fallback now uses
  its legacy tag-8 encoding, which the reader already supports.
* Three additional live output boundaries silently rendered tag-9 JSONB as
  null: multimodal `QueryRow` metadata, canonical REST observability log
  fields, and pgwire document JSON. Focused red/green tests now pin all three;
  the two row/document converters are exhaustive over nested arrays and
  objects instead of retaining wildcard null fallbacks.
* Round 4's field type-reference guard covered enums but still discarded the
  Rust type of ordinary `message` fields. A synthetic negative-control test
  reproduced the hole (`WrongMessage` at the correct tag/kind passed); the
  parser now records and compares normalized message targets as well. The
  full zero-drift descriptor census remains green.

**Round 6 (high effort, 2026-09-04) — 10 findings, 7 fixed, 3 dispositioned
(conventions angle clean):**

* Fixed: the last missed wildcard — the native Volcano vector-search
  executor's metadata→JSON match rendered JSONB as SQL NULL in projections.
  The SqlValue filter path now dot-traverses JSON(B) roots (`memory.type`
  resolves inside a JsonbValue document) exactly like its ProximaTree twin,
  pinned by `jsonb_dot_path_filtering_matches_the_proxima_tree_semantics`.
* Fixed (guard): plain MESSAGE-field payload types are now compared (the
  final Rust type identifier vs descriptor type_name; maps remain governed
  by their key/value wire-kind check); rpc STREAMING FLAVOR is compared (a hand-splice rendering a
  streaming rpc as unary fails the gate; client/bidi share the client-side
  `.streaming` bucket); and the gate can no longer silently disarm — under
  `REQUIRE_PROTOC=1` (now set by CI's unit lane) a missing protoc FAILS
  instead of skipping, keeping skip and pass distinguishable.
* Fixed (efficiency): document-block min/max stats track the running JSONB
  extremum by its rendered key (each document decodes once, not per
  comparison); `jsonb_to_json_string_lossy` is the named seam for the ~10
  string-only consumers (future direct MessagePack→text transcoder drops
  in there).
* Dispositioned: `full_precision_rank`'s two deliberate deltas vs the old
  serde detour (Int64 precision preserved; NaN/∞ → unset instead of 0) are
  now documented in the code — latent until that pipeline gains a live
  constructor; the RankService/GraphService stubs are contract surface —
  wiring them to the existing inherent impls (`rank_service.rs`) is tracked
  follow-up; `valid_jsonb_bytes`'s single-authority consolidation (kernel
  home) rides with the shared-converter follow-up.

**Round 6 (high effort, 2026-09-05) — 10 findings, 9 fixed (2
mutation-proven), 1 dispositioned:**

* **The guard advertised three protections that were dead or inert code** —
  proven by the reviewer's differential mutation testing and re-verified
  here after fixing: (a) the message-field type-ref check never fired
  because the wrapper stripper handled only bare `Option<`/`Vec<` while
  453/475 wrapped fields are fully qualified — now strips balanced angle
  brackets through any spelling, and a compiling wrong-struct mutation
  (`Vec<ShardStats>` → `Vec<NodeAccessStats>`) FAILS the gate; (b) the
  map key/value-kind comparison was unreachable (map descriptor kind is
  `message`, so the type-ref else-if always won) — now first in the chain;
  (c) the rpc streaming-flavor check no-oped for client-streaming rpcs
  (`.client_streaming` is not a superstring of `.streaming`) and its
  24-line lookahead window was smaller than real method bodies — needle
  added, window 100. Type-ref comparison normalizes both sides through
  snake() so casing renames are not drift.
* Fixed: full_precision_rank's round-5 disposition claimed NaN→unset but
  the code passes NumberValue through — the comment now states the code's
  actual behavior (pass-through; non-finite renders null only at the
  serde boundary, pre-existing). The results.rs writer fallback is now
  plain JSON-text bytes (the magic-prefixed fallback was undecodable under
  tag 9). Federated vector extraction traverses JSONB documents (was
  silently dropping/zeroing rows whose path crossed one). The SqlValue
  dot-traversal walks by reference (was clone-per-segment deep copies).
  Nova uses the jsonb_to_json_string_lossy seam (was the one bypass). The
  Python SDK's second decoder (grpc_sync) gained the jsonb_value branch so
  both SDK decoders agree.
* Dispositioned: the hand-rolled line scanner remains
  correct-only-by-enumeration of today's codegen shapes (where-clause
  impls, raw strings, block comments can desync it) — migrating the parser
  to `syn` or TD option 2's generate-and-diff is the tracked end state;
  grow a special case per new shape only until then.

**Round 7 (post-merge confirmation, 2026-09-05) — 10 findings, 7 fixed in
the follow-up PR, 3 deferred with rationale:**

* Fixed (guard): an enum whose wire-name tables are BOTH absent now FAILS
  LOUD instead of silently downgrading to discriminant-multiset comparison
  (prost always generates both tables — joint absence is drift, and the weak
  fallback would let a wire-name rename pass green). REQUIRE_PROTOC now
  gates the FILESYSTEM disarm causes too (absent repo root, unreadable proto
  tree) — a repo restructure can no longer silently disarm the gate on CI.
* Fixed (runtime): document-block path stats merge JSONB and scalar extrema
  with a deterministic cross-kind comparison instead of the JSONB extremum
  blindly overwriting the scalar one (pure columns keep exact semantics).
  The records-crate writer's encode-error fallback is the legacy tag-8
  magic encoding (matching search-types; plain JSON-text under tag 9
  violated the MessagePack contract). The graph seam converts JSONB to
  canonical JSON text like its array/object arms (raw MessagePack bytes
  rendered as AqlValue::Null). The two new SqlValue→JSON converter copies
  (pgwire protocol.rs, executor.rs) delegate to the shared
  arrow_conversion helper. The SqlValue dot-traversal walks ObjectValue
  roots by reference instead of materializing the whole owned tree.
* Deferred: (a) the `{"jsonb_value": …}` sniffed-tag ambiguity — rmp-serde
  accepts almost any short byte sequence, so the round-4 MessagePack-validity
  gate narrows little; the durable fix is an unambiguous tagged shape, which
  is a wire-visible REST change needing owner sign-off; (b) a shared
  big-stack test-runtime builder for the ~670 in-process-server tests
  (only 8 set stack sizes; production already runs 32 MiB) — separate
  test-infra PR; (c) filter+group-by on the same JSONB field renders twice
  per log — measure-first before plumbing pre-rendered keys.

**Round 8 (review of the round-7 PR, 2026-09-05) — 10 findings, all
addressed in the same PR:**

* Build-break (reproduced by the reviewer): the round-7 fail-loud guard
  passed a `&str` where `record` takes `String` — invisible to
  `cargo check`/`clippy --lib` because the module is `#[cfg(test)]`; CI's
  unit lane would have gone red. Fixed, and the one-sided gap closed
  properly: an empty name table (single OR joint) now fails loud. That
  stricter check surfaced REAL drift — explain.v1's three enums
  (QueryType/NodeType/ExplainFormat) never had `from_str_name` at all:
  JSON/enum-name decoding of explain types has never worked. The mirror
  gained the three impls.
* Mixed-kind extrema reverted to the conservative kind-segregated form:
  the lexicographic merge did not bound the column and made
  `might_match_range` false-prune reachable; recording the JSONB extremum
  (incomparable, never prunes) is the safe choice. A mixed-kind regression
  pins the no-false-prune behavior.
* The graph fix moved to the LIVE seam (`proxima_value_to_property_value`
  in adjacency_projection — the round-7 edit had landed in its
  `#[allow(dead_code)]` twin): Json/Jsonb map to canonical JSON text
  instead of `_ => None`.
* `JSONB_LEGACY_MAGIC` is now a single data-model authority used by both
  writers; the records-crate reader strips the magic prefix BEFORE
  msgpack-decoding (feeding it raw succeeds as Number(-1) — the
  recovers-losslessly claim was empirically false).
* The dot-traversal is a two-phase walk (SqlObject segments by reference,
  then the shared `json_get_path` for JSON(B) tails) — replacing the
  three-cursor form whose invariants were hand-enforced, and unifying the
  JSON walker this file previously maintained twice.

**Round 9 (partial — review agent rate-limited mid-run, 2026-09-05) —
1 empirically-confirmed finding, fixed:**

* The round-8 records-reader fix stripped the legacy magic but then
  msgpack-decoded the remaining bytes — the legacy payload after the magic
  is JSON TEXT (search-types' decoder had this right). Short payloads
  "decoded" as garbage numbers (`123` → Number(123)); documents degraded
  to Binary. The reader now decodes magic-stripped bytes with
  serde_json; only magic-free bytes take the MessagePack path. Regression
  tests pin both a structured document and the malformed-payload binary
  fallback.

**Round 10 (2026-09-05) — 6 findings, all fixed:**

* **Correctness** (6/8 angles + reviewer trace): the two-phase dot-traversal
  returned a scalar PARENT's value for a dotted field (`user.name` over
  `{"user": "admin"}` resolved to "admin") — admitting rows on
  `user.name = "admin"` and flipping IS NULL semantics on the GA engine
  filter paths (helix, swift, compiled-filter). The mid-path non-object arm
  now returns None (the leaf case is the post-loop match); pinned by
  `dotted_field_on_a_scalar_parent_never_resolves` for both root and nested
  scalar parents.
* The live graph seam dropped `ProximaValue::Map` via the wildcard one line
  below the new Json/Jsonb arm while the structurally identical Struct
  mapped to ObjectValue — Map now joins the Struct arm, with a direct test
  covering both Map and Jsonb projection.
* Deleted the dead `sql_value_to_property_value` /
  `property_value_to_sql_value` twins in graph_schema (the round-7 trap —
  re-armed by parity-editing instead of deleting).
* `json_get_path` takes an iterator (`once(segment).chain(segments)`,
  `tail.split('.')`) — zero allocations on the per-record hot path where
  both call sites previously collected Vecs.
* Guard: removed the now-unreachable empty-table `continue` and its stale
  PRESENT-table comment (absence fails loud above).
* Not re-fixed (tracked, per the standing consolidation disposition): the
  legacy magic+JSON-text codec body is still line-copied between records
  and search-types — it rides with the single-authority consolidation.

**Round 11 (2026-09-05) — 7 findings, all addressed:**

* Correctness: a nested field stored as `SqlValue{value: None}` (the wire
  form of a nested null) became unresolvable by the two-phase traversal —
  `a.b.c = null` silently dropped rows where the develop-era path matched.
  A present key with an unset oneof now resolves as a JSON null leaf, and
  `obj_to_json` includes None-valued children as null (restoring
  `a = {"b": {"c": null}}` equality). Pinned by
  `none_valued_child_resolves_to_json_null`.
* The v2 gRPC entity seam (`typed_value_to_property_value`) gained the
  `JsonValue` arm — the wildcard was still silently dropping JSON
  properties one file over from the round-8 fix.
* `json_get_path` is pub and the federated JSONB vector-path arm
  delegates to it (was a verbatim third copy of the walk).
* The mixed-column scalar min/max branch is guarded by
  `else if jsonb_min.is_none()` — the JSONB extremum wins by design, so
  the scalar stores were dead work inviting a future "fix" that would
  reintroduce the round-7 false-prune.
* The `JSONB_LEGACY_MAGIC` and records comments now say precisely what is
  shared (the magic bytes) and what is not (the codec bodies, tracked).
* The nested-null traversal no longer uses `.expect("checked")`; the
  production panic-policy applies even when a preceding guard proves the
  option is present, so the branch now matches `Option` explicitly.
* Tracked (unchanged disposition): the root-crate SqlValue→JSON helper's
  home (arrow_conversion) is unreachable from extracted crates; the
  foundation-level anchor (proximadb-records) is the consolidation target.

**Round 12 (2026-09-05) — 8 findings: 6 fixed, 2 tracked/dispositioned:**

* Correctness: the round-11 null fix was depth-inconsistent — a TOP-LEVEL
  unset oneof (`a = null`) still resolved as absent and dropped rows while
  the nested form matched. The fast path now maps an unset oneof to JSON
  null at every depth; pinned by
  `top_level_none_valued_field_resolves_to_json_null`.
* The `.expect("checked")` in the traversal is gone (no-expect mandate; the
  panic-policy guard scans only src/ so nothing machine-checked this
  crate) — restructured as a plain match.
* `obj_to_json` deleted — it duplicated `sql_val_to_json`'s ObjectValue arm
  (including the round-11 null-children change, re-derived by hand); the
  post-loop leaf lowers through the one shared function.
* The records BytesValue tail delegates to `from_jsonb_or_binary` (was a
  verbatim copy of the helper the adjacent arm already uses).
* Graph seam: a JSON null document stays the property model's null form —
  rendering it as the string "null" flipped null-equality to string
  equality.
* Tracked: (a) SqlValue-vs-ProximaTree object-literal equality divergence
  (`a.b = {"c":1}` matches one evaluator, not the other) — pre-existing on
  develop, needs a tree_get contract change in proximadb-records;
  (b) the canonical-JSON-text-as-StringValue policy is hand-encoded at two
  sibling graph seams — rides with the shared-converter consolidation.

**Round 13 (2026-09-05) — 7 findings: 6 fixed, 1 tracked:**

* Correctness (SQL three-valued logic): the round-12 null resolution let
  null-valued fields leak into range results (`rank < 10` admitted them
  while an absent field was excluded). Ordering comparisons now return
  false when either side is null; `= null` keeps matching (the pinned
  JSON-consistent choice for explicit null literals — documented in the
  test). Pinned by `null_valued_field_satisfies_no_range_predicate`.
* The third sibling seam: entity_to_node's direct-property loop
  wildcard-dropped JSON(B)/object/array flexible-metadata fields, making
  them unfilterable (matches_metadata_filter reads only direct
  properties). They now lower to canonical JSON text — with the literal
  `null` document mapping to the property model's null form (the round-12
  rule applied at this seam too).
* The v2 gRPC write seam: `json_value: "null"` no longer becomes the
  string "null" (same rule).
* The unset-oneof-is-null rule now lives in one wrapper
  (`sql_value_to_json`) at the top level instead of being re-derived by
  hand at four depths.
* Cleanup: duplicate disarm-contract comment removed; (tracked, unchanged)
  the Jsonb read-rendering round-trip asymmetry at the graph property
  seam (StringValue loses the JSON typing on read-modify-write) — needs a
  typed property or marker scheme, folded into the JSON-text-policy
  consolidation.

**Round 14 (2026-09-05) — 4 findings, all fixed:**

* `EntityNodeMapper` serialized `SqlObject`/`SqlArray` protobuf wrappers,
  producing strings such as `{"fields":{"rank":{"int64_value":7}}}`
  instead of the natural document `{"rank":7}`. The compatibility input
  now lowers through `SqlValue -> ProximaValue` before JSON projection.
* The same mapper's JSON-null helper returned `None`, but its caller treated
  that as "skip this field" rather than an unset `PropertyValue` oneof.
  JSON null is now retained as a present null property.
* The v2 entity seam compared raw JSON text to the exact spelling `"null"`;
  valid whitespace-equivalent nulls became strings and malformed JSON was
  accepted. It now parses through `typed_value_to_proxima` first.
* The null ordering guard covered four comparison operators but omitted
  `BETWEEN`; a null bound could therefore act as an open lower bound.
  `BETWEEN` now rejects null fields and either null bound.

* **SQL null semantics centralized**: the round-13 per-arm range guards
  were copy-pasted (missing Between) and left `!=`/`NOT IN` admitting
  null rows. One central rule now opens `compare_json_op`: a null on
  either side makes every comparison false, except IS NULL/IS NOT NULL
  and the pinned both-null `= null`. Covers Between, !=, NOT IN, and the
  security walker's delegation.
* Null-at-depth-1 (a null HEAD under a dotted path) now matches the
  depth-2 rule via a NullValue sentinel — `a.b = null` and
  `a.b IS NULL` agree, and the triple-nested child special-case
  collapsed to the same sentinel.
* graph_schema's Object/Array arms lower canonically via the shared
  `sql_val_to_json` (serde_json::to_string on the prost struct emitted
  the proto serde ENVELOPE — `{"fields":{…}}` — unmatchable by the
  exact-string entity filter and divergent from the JSONB arm).
* The v2 seam's null detection is parse-based (" null" is valid JSON
  text and was stored as a string property).
* The legacy vector-search executor no longer collapses nulls/bytes to
  String("") — nulls stay null, bytes stay binary, objects lower
  canonically.
* HELIX/SWIFT extraction keeps unset-oneof keys as JSON null instead of
  dropping them (per-seam null accounting).
* The last two hand-rolled root-crate REST converters (canonical
  handlers + document) delegate to the shared arrow_conversion helper —
  their bytes/NaN renderings had already drifted from it.
* Doc blocks disentangled (the payload-lowering contract sits on
  `sql_val_to_json` again).

**Round 15 (2026-09-05) — 10 findings: 8 fixed, 1 pre-addressed, 1
inherent:**

* Null-LITERAL semantics restored: `!= null` had become a zero-rows trap
  (the Mongo-style exclude-nulls idiom, live on the REST neq path) and
  NOT IN with a null literal rejected everything. The central rule now
  distinguishes a null literal (Equals/=null pinned; NotEquals/NotIn
  exclude nulls; In false) from a null FIELD (no value comparison
  matches).
* NOT IN with a null ELEMENT in the list now denies (SQL UNKNOWN ⇒
  deny) — including via the security walker's delegation.
* The stale in-file converter tests (bytes int-array, NaN→0) updated to
  the shared helper's rendering — the delegation would have gone red on
  CI's unit lane (the #[cfg(test)] invisibility class again).
* The executor's object/array arms delegate to the canonical converter
  (the hand-rolled JSON round-trip dropped null children, re-typed
  bytes, degraded floats; ArrayValue still hit the wildcard → "");
  the owned-bytes clone dropped.
* The v2 seam's unparseable-JSON fallback keeps the raw string verbatim
  (was double-serialized — JSON quoting the filter can never match).
* The dead outer NULL sentinel removed with an accurate comment (a null
  HEAD is untraversable; `a.b` over `{"a":null}` resolves to nothing,
  consistent with IS NULL).
* Pre-addressed by the parallel session: the guard's surplus wire-name
  key check (verified present and correctly keyed).
* Inherent (documented): a JSONB-null flexible-metadata field becomes
  an ABSENT v1-entity property (the v1 property map cannot express
  present-null) while v2/adjacency store present nulls — the v1 entity
  path's property map would need an Option<Value> shape change; tracked
  with the typed-property consolidation.

**Round 16 (2026-09-05) — 6 findings, all fixed:**

* Contains-null regression: the null-literal guard's catch-all denied
  `tags contains null` (develop matched null array elements
  structurally). A Contains carve-out restores array-membership-of-null.
* NotIn-with-null-literal now excludes null rows (was `true` — code,
  comment, and ledger disagreed; now consistent with NotEquals).
* The executor's entire SqlValue lowering delegates to the canonical
  converter (8 hand-copied arms had a nesting-dependent divergence:
  top-level legacy magic bytes stayed Binary while nested ones were
  sniffed).
* HELIX/SWIFT extraction delegates to the shared converter — their
  private helpers rendered bytes as hex and arrays/objects as
  placeholder strings, drifting per-surface; the dead helpers deleted.
* The Between arm's dead `!json_val.is_null()` conjunct dropped (the
  centralized guards return first; a comment now says so).

**Round 17 (2026-09-05) — 10 findings, all fixed:**

* Non-canonical key order: SqlObject.fields is a HashMap and serde_json's
  preserve_order is active (utoipa), so the graph-schema object/array
  property text varied per write — exact-string property filters flaked.
  Serialization now sorts keys recursively (canonical_json_string), and
  the arm uses the same shared converter the six delegated sites use (was
  the competing search-types twin — two-authority drift).
* The Jsonb arm rejects undecodable bytes (property dropped, develop's
  behavior) instead of storing quoted-hex junk.
* The v2 seam parses strictly and unwraps JSON string documents (a
  valid-JSON string stores its inner text, not a double-quoted spelling;
  malformed JSON is rejected, matching the sibling services'
  typed_value_to_proxima contract).
* The type-strict/security path routes null LITERALS through the
  centralized idioms before its class gate (Null is its own comparable
  class, so `!= null` deny-all'd on the RLS walkers while returning
  exclude-nulls everywhere else).
* parse_from_str_arm tolerates the qualified EnumName::Variant spelling
  (a correct arm parsed as an empty table and tripped the fail-loud
  guard).
* DocumentBlock::is_null recognizes the unset-oneof null wire form
  (null-count/selectivity and extrema exclusion).
* Null tests hoisted above the null-value guards (three encodings of
  one rule collapsed to one); the centralizing comment now states the
  actual carve-out table.

**Round 18 (2026-09-05) — 10 findings: 5 fixed, 5 tracked:**

* Fixed: NOT over a null-field value comparison now denies (SQL
  NOT(UNKNOWN)=UNKNOWN; the two-valued flip admitted null rows — a
  regression vs develop). Direct Comparison children only; deeper
  nestings keep the flip (double negation cancels; documented).
* Fixed: the v2 seam's malformed-JSON arm no longer stores a present
  value-less property (silent content destruction with an OK response) —
  the property is skipped; full write-rejection parity needs a Result
  signature threaded through upsert (tracked).
* Fixed: sorted-key canonical text applied at the remaining two
  JSON-to-StringValue seams (adjacency_projection, entity_service) —
  equal documents materialize as equal property text regardless of
  write path.
* Fixed: unreachable null-test arms' comment corrected; dead is_empty
  branches in helix/swift extraction dropped.

**Round 19 (2026-09-05) — 10 findings: 6 fixed, 4 tracked:**

* Fixed: NOT(UNKNOWN)=deny extended to BOTH strict evaluators (round 18
  patched only the permissive one — strict/legacy filter modes returned
  different result sets for the same filter+record) and to ABSENT
  fields (is_none_or — present-null and absent are both "null" per the
  file's own rule; they denied differently).
* Fixed: the v2 malformed-JSON fix made effective (return None from the
  fn — the round-18 arm only nulled the inner Option, which the fn tail
  re-wrapped into exactly the present value-less property the comment
  claimed eliminated).
* Fixed: json_text_property matches the PARSED Value::Null (was a
  serialized-text compare against "null" — renderer-dependent).
* Fixed: the AXIS eventlog consumer lowers structured metadata via the
  canonical JSON→ProximaValue mapping (the round-16 extraction contract
  change had been silently re-lowered to Null — tags changed value
  with no error).
* Fixed: the Not null-probe resolves once (probe only when the inner
  comparison denied — was double-resolution per record).
* Tracked: JSON string-document spelling divergence between the v2
  entity seam (unwrapped inner text) and adjacency/graph_schema
  (quoted canonical text); the extraction twin blocks (helix/swift,
  parity-edited three times); canonical_json_string's clone-per-write
  (in-place sort_keys pending); the two unconsolidated dot-walkers
  (auth oidc claim_at_path, plan_executor field_value).
* Tracked: evaluate_filter_proxima_strict decides IS NULL on the raw
  ProximaValue (Jsonb(Null) answers false there, true elsewhere —
  pre-existing; needs null normalization at the records boundary);
  corrupt JSONB nested inside ObjectValue renders quoted-hex while
  top-level is dropped (corrupt-input only); the executor delegation
  clones per field per row (owned-input entry point pending);
  canonical_json_string is one of three recursive key-sorts (one home
  pending); In with a null list element admits an array-field holding
  null (pre-existing, benign-direction, permissive path only).


* **The ledger must be `git add -f`-tracked**: `.gitignore`'s `*.json` silently
  swallowed it (now a negation beside the other ratchet baselines). Untracked,
  the guard would have panicked on every fresh checkout — CI included.
* **Map kinds now compared for real**: the descriptor's synthetic map-entry
  message supplies the key/value kinds (the first cut hardcoded string keys and
  never compared values).
* **Enum name tables validated independently**: `as_str_name` and
  `from_str_name` are each checked against the descriptor and cross-checked
  against each other, so a corrupted serialize table can no longer hide behind
  a correct decode table.
* **`jsonb_value` handling canonicalized**: every ProximaValue-producing site
  decodes tag-9 bytes with the canonical MessagePack codec
  (`ProximaValue::from_jsonb_slice`, the v2 canon), with Binary fallback — the
  AQL sources follow their local JSON heuristic (matching their BytesValue
  arms) instead of silently nulling via the `_` wildcard; the Python SDK's
  `_convert_from_sql_value` gained the `jsonb_value` arm instead of silently
  returning `None`.
* **serde `jsonb_value` key no longer hijacks the generic-object fallback**:
  the branch commits only when the base64 payload decodes; otherwise the
  pre-existing MVP-friendly generic-object path applies.
* **protoc probe moved before any filesystem access**, so the graceful skip
  holds even without the repo tree; string literals and trailing comments are
  masked in the tokenizer's brace counting so `format!("{…}")`-style lines
  cannot desync it.
* **Round-trip gate extended** (`tests/sql_value_roundtrip.rs`): JsonbValue
  now covered on both the binary (prost) and REST (custom serde, `jsonb_value`
  base64 key pinned) paths — the format-change gate mandate #8 requires.

Known divergence deliberately left to the backlog: the JSONB *stringifying*
surfaces (REST row builders, Arrow/observability stringifiers) each follow
their local BytesValue convention (hex / per-byte arrays / placeholders); see
the ledger + follow-ups.

== Not in scope

The v2 package already has both generation (`PROXIMADB_PROTO_REGEN=1`) and the
descriptor drift gate; TD-PROTO-1 (Python stub drift) is the stub-side analog.

== Related

* #1724 (Hive arm retirement — whose review surfaced this)
* TD-CAT-8 (Hive adapter retirement), TD-CAT-7.4 (OltpCatalog gating —
  owner context for the `oltp = 17` field)
