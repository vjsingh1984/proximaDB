= Pre-release Foundational Items — Live Tracker
:toc: left
:toclevels: 2
:icons: font
:last-updated: 2026-05-26
:audit-source: ../../12-design/DESIGN_DOC_STATUS_INDEX_2026_05_26.adoc
:plan-source: /Users/vijaysingh/.claude/plans/atomic-puzzling-meteor.md

[.lead]
**Live progress tracker for the 5-tier pre-release foundational work plan dated 2026-05-26. Add commit hash + verification note as each item lands.**

== Context

The 2026-05-26 design-doc audit
(`docs/12-design/DESIGN_DOC_STATUS_INDEX_2026_05_26.adoc`) made the
design-doc landscape honest. This tracker covers the next layer of
honesty work: dashboard ↔ code ↔ support-contract reconciliation and
the small set of genuine multi-tenant safety / durability gaps.

User-confirmed decisions (from plan AskUserQuestion 2026-05-26):

* T1.2 auth defaults: Option B — keep default off, add startup `warn!`. Defer Option A (forced-explicit) to v0.4.
* T3.2 hybrid retrieval gRPC: wire real BM25+vector backend (parity with REST).
* Tier 2 scope this session: write ADR-020 + land TD-065 (default-enable canonical-document-store). TD-066/067/068 stay open under the new ADR for follow-up sessions.
* Execution order: T0 → T1 → T2 → T3 → T4 sequential.

== Tier 0 — Dashboard honesty (zero-risk docs)

[cols="3,1,3,2", options="header"]
|===
| Item | Status | Commit | Verification

| T0.1 — Reconcile MASTER_FEATURE_DASHBOARD maturity rows with SUPPORTED_SURFACE; downgrade Documents/Graph/Hybrid/Observability/Unified-query/Time-series/Event-sourcing/Security from "Production Ready" to Beta; downgrade Distributed cluster from "Beta" to Experimental; add pgwire/SQL Beta row; add Caveat column citing controlling TD/ADR.
| Done
| (this session)
| `git diff docs/_internal/roadmap/MASTER_FEATURE_DASHBOARD.adoc` shows the new table.

| T0.2 — Create this tracker doc.
| Done
| (this session)
| File exists at `docs/_internal/status/PRE_RELEASE_FOUNDATIONS_2026_05_26.adoc`.
|===

== Tier 1 — Multi-tenant safety hardening

[cols="3,1,3,2", options="header"]
|===
| Item | Status | Commit | Verification

| T1.1 — Implement `rls_record_filter` on NOVA + HELIX engines, mirroring SST's `trait_impl.rs:303` pattern. Use the generic `RlsRecordPredicate` from `src/storage/traits/query.rs:21`. Skip SWIFT + RAPTOR (`experimental-engines` feature, not production-multi-tenant targets).
| Done
| (verified in current tree)
| NOVA implements `rls_record_filter` in `src/storage/engines/nova/engine.rs`; HELIX implements it in `src/storage/engines/helix/mod.rs`. Follow-up: add a narrow integration test for tenant A/B isolation on both engines.

| T1.2 — Add startup `warn!` log when production-mode server runs without auth (Option B). Keep `RestAuthConfig::default().enabled = false`. Touch the server-startup path that constructs `RestServerSecurityConfig` so the warning fires when `development_mode == false && auth.enabled == false`.
| Done
| (verified in current tree)
| `src/network/rest/server.rs` emits a `proximadb::security` warning when `development_mode == false && auth.enabled == false`.

| T1.3 — Document the `auth_middleware_unified_port` dormant status. Add a comment block at `src/network/auth/middleware.rs:168` confirming this is the parallel-shortcut state (ADR-016 step 3 of 4), and that step 4 (consumer migrations) is intentionally demand-blocked.
| Done
| (verified in current tree)
| `src/network/auth/middleware.rs` documents the dormant production status and ADR-016 step-4 deferral.
|===

== Tier 2 — Canonical WAL fragmentation (durability story)

[cols="3,1,3,2", options="header"]
|===
| Item | Status | Commit | Verification

| T2.1 — Write ADR-020: Canonical WAL Authority Model. Cover the four open TDs (065/066/067/068) — for each, document the decision (default-enable, demote, key contract, rewire-vs-retire).
| Done
| (this session)
| `docs/12-design/adr/ADR-020-canonical-wal-authority.adoc` exists with per-TD decisions: TD-065 default-enable; TD-066 demote ORION WAL to performance buffer; TD-067 OID-keyed projection contract; TD-068 retire QUASAR cold-tier in favor of canonical-record-backed tiering.

| T2.2 — Default-enable `canonical-document-store` feature (TD-065). Flip `Cargo.toml` `[features]` default set; preserve the opt-out flag for migration. Mark TD-065 Resolved in TECHNICAL_DEBT.adoc.
| Done
| (this session)
| `Cargo.toml:464` default set now includes `canonical-document-store`. TD-065 marked Resolved with cross-reference to ADR-020. Opt-out path: `default-features = false` + explicit feature selection.

| T2.3 — Deferred to follow-up session per user decision: TD-066 ORION canonical Checkpoint emission. Captured here so it doesn't disappear from the active tracker.
| Deferred
| —
| (Out of session scope.)
|===

== Tier 3 — Foundational ADR completion

[cols="3,1,3,2", options="header"]
|===
| Item | Status | Commit | Verification

| T3.1 — ADR-012 minimum-viable `merge_branch(source, target, conflict_policy)` for node + edge CRDT semantics. If embedding-merge case is intricate, document the deferral inline and ship node+edge for v0.2.
| Done (8 slices)
| Slices 1-8 below
| Slices 1-5 built the pure-logic merge pipeline (`walk_diff` / `classify_mutation` / `MergeReport` / `merge_branches` over canonical-WAL filter + merge-base discovery). Slice 2 added `ProximaRecord.branch_id` (V2 wire-visible). Slice 6 added `CatalogBranchMergePolicy` to `CatalogTableSchema` with ADR-012 defaults. Slice 7 (`0c87e4640`) landed durable WAL write-back via `write_back_merge` → `FramedTableWalAppender::append_operations` and wired the REST endpoint `POST /api/v1/collections/:collection/branches/:branch/merge` to honor `dry_run=false`, stamping `origin = "branch_merge:<a>:<b>"` on every merged upsert. Slice 8 (`2b46a4a16`) added `tests/graph_branch_merge_integration_test.rs` covering 5 durable-roundtrip cycles (LWW / Delete-wins / Add-wins union / unilateral / sequence monotonicity). Embedding-merge classification per Slice 5 covers `EmbeddingUpdate`, `LabelSet`, `PropsKey` pairwise — no deferral needed.

| T3.2 — Wire real BM25 + vector backend through the gRPC hybrid_search route. Target `crates/platform/proximadb-api/src/grpc/v1/hybrid.rs` (the new home; the root-crate `hybrid_search_service.rs` is a deprecated shim). Mirror the REST `/api/v1/hybrid/search` behavior.
| Slice 1 + 1b + filter/fusion unit coverage done
| (this session)
| **Slice 1** (`6a73ead7f`): added `fulltext_indexes: HybridFullTextIndexMap` to `SharedServices`; replaced 3 deprecated-shim wire-ups in `src/network/multi_server.rs` (L431/L853/L1233) with `RestHybridPortImpl`. **Slice 1b**: added `with_fulltext_indexes` builder to `AppState`; threaded `Option<FullTextIndexMap>` param through REST server builders; wired production sites to pass `shared_services.fulltext_indexes.clone()`. **Filter/fusion slice**: REST request translation now preserves filters + RRF/weighted fusion params; `RestHybridPortImpl` forwards filters into vector search and honors proto fusion params. REST handler and gRPC service tests verify filters/fusion params reach `HybridPort`. Verified with `cargo test -p proximadb-api rest::v1::hybrid::tests -- --nocapture`, `cargo test -p proximadb-api grpc::v1::hybrid::tests -- --nocapture`, and `cargo test -p proximadb --lib network::hybrid_search::tests -- --nocapture`. Remaining work: optional live server REST/gRPC smoke.
|===

== Tier 3.1 — ADR-012 graph branch merge orchestration

[cols="3,1,3,2", options="header"]
|===
| Item | Status | Commit | Verification

| T3.1 Slice 1 — WAL diff walker (pure logic, no WAL integration yet).
  Adds `MutationEvent` struct + `walk_diff(left, right) -> Vec<MergeConflict>`
  + `classify_mutation(base, branch_a, branch_b) -> MutationClass` to
  `src/graph/merge.rs`. Branch identity implicit in "which input list."
| Done
| (this commit)
| 13 tests passing (7 existing + 6 walk_diff/classify_mutation new). `cargo test --lib graph::merge`. Steps 1-2 (merge base discovery, WAL iteration) + step 5 (write back) require `branch_id` plumbing into `ProximaRecord` — future slices.

| T3.1 Slice 2 — `branch_id` field plumbing into `ProximaRecord`
| Done
| (this session)
| Verified in current tree: runtime `ProximaRecord.branch_id` exists in `crates/foundation/proximadb-records/src/lib.rs`; `ProximaRecordV2` carries it while legacy v1 bincode skips it. Covered by `branch_id_round_trips_through_v2_wire`, `branch_id_is_dropped_by_v1_bincode_path_serde_skip`, and `branch_id_none_default_for_v2_wire`.

| T3.1 Slice 3 — WAL iteration API filter by branch_id + LSN range
| Done
| (this commit)
| Added `filter_wal_by_branch_lsn(entries, branch_id, lsn_range) -> Vec<&CanonicalWalEntry>` to `crates/storage/proximadb-storage-common/src/wal_entry.rs`. Pure function; 8 tests covering empty input, LSN-range edges, branch-id matching, None-branch exclusion, RecordDelete/Checkpoint/CdcBarrier exclusion, combined LSN+branch filtering. Caller materializes input (e.g. via `recover_from_canonical_wal`). RecordDelete handled separately by the orchestrator in Slice 4+.

| T3.1 Slice 4 — Merge-base discovery (catalog `BranchMetadata` or walk-back)
| Done (pure walk-back)
| (this commit)
| Added `find_merge_base_lsn(entries, branch_a, branch_b) -> Option<u64>` to `crates/storage/proximadb-storage-common/src/wal_entry.rs`. Definition: `min(first_lsn(a), first_lsn(b)) - 1`. Assumes both branches share a common ancestor; asymmetric forks (branch_b forked from branch_a) are Slice 5+ scope. 8 tests covering empty input, missing branches, both-present, different first-LSNs, saturating_sub edges, non-upsert exclusion, simultaneous fork.

| T3.1 Slice 5 — `GraphBranchMerger` orchestrator
| Done (pure; WAL write-back deferred to Slice 6)
| (this session)
| `merge_branches(wal, branch_a, branch_b) -> Option<MergeReport>` now reconstructs latest base/left/right records from canonical WAL and applies pairwise `classify_mutation`, so branch conflicts distinguish `EmbeddingUpdate`, `LabelSet`, and `PropsKey` in addition to node/edge upsert/delete. Verification: `cargo test -p proximadb --lib graph::merge -- --nocapture` (31 passed).

| T3.1 Slice 6 — Catalog `branch_merge_policy` fields on `CatalogTableSchema`
| Done
| (this session)
| Added `CatalogBranchMergePolicy` + `CatalogBranchMergeResolution` to `crates/control/proximadb-catalog/src/lib.rs` and added `CatalogTableSchema.branch_merge_policy` with ADR-012 defaults. Legacy catalog JSON deserializes with default policy. Verified with `cargo test -p proximadb-catalog branch_merge_policy -- --nocapture`, `cargo test -p proximadb-catalog catalog_table_schema_serde_back_compat_with_pre_branch_policy_json -- --nocapture`, and `cargo check -p proximadb --lib`.

| T3.1 Slice 7 — REST `POST /collections/{name}/branches/{branch}/merge` endpoint
| Done (end-to-end after TD-072 fix)
| `0c87e4640` + TD-072 fix `996626595`
| REST route `POST /api/v1/collections/:collection/branches/:branch/merge` reads the canonical framed WAL from `data_dir/pgwire/canonical-records.wal`, filters by collection, and runs `merge_branches`. With `dry_run=true` the handler returns merge-base/events/conflicts/resolutions only. With `dry_run=false` it calls `write_back_merge`, which materializes resolutions as `CanonicalOperation::RecordUpsert` / `RecordDelete` entries through `FramedTableWalAppender::append_operations` and stamps `origin = "branch_merge:<source>:<target>"` on every merged upsert. Response carries a `write_back` block with `first_lsn` / `last_lsn` / `entries_written`. The Slice 7 endpoint code was correct from the start, but a latent bug from Slice 2 (`#[serde(skip)]` on `ProximaRecord.branch_id`) silently dropped the field through the canonical WAL — discovered by Slice 8 integration tests and fixed as TD-072. End-to-end durable branch-merge through the REST endpoint now works.

| T3.1 Slice 8 — Integration tests for full branch creation → divergent mutations → merge cycle
| Done (all 5 cycles pass after TD-072 fix)
| `2b46a4a16` (test code) → TD-072 fix `996626595` → un-ignore in this session
| `tests/graph_branch_merge_integration_test.rs` covers 5 durable WAL roundtrip cycles: LWW (later-append wins), Delete-wins (tombstone propagation), Add-wins (label union), unilateral mutations, and post-write-back sequence-number monotonicity. Each test seeds canonical WAL entries via `FramedTableWalAppender::append_operations`, runs `merge_branches` + `write_back_merge`, then re-reads via `read_entries_from_path` and asserts the merge entries carry `origin = "branch_merge:a:b"`. **First run on a clean build surfaced TD-072** (latent bug from Slice 2: `#[serde(skip)]` on `branch_id` silently dropped through the canonical WAL). Tests were briefly `#[ignore]`-d pending the fix; with TD-072 resolved (`996626595`) all 5 now pass without `--ignored`. Verified with `cargo test --test graph_branch_merge_integration_test`: 5 passed / 0 failed.

| T3.1 Slice 9 — REST-router integration tests for the branch-merge endpoint
| Done (5 cycles)
| `3452cf58a`
| Refactored `merge_graph_branch` in `src/network/rest/v1/handlers.rs` to extract a pure-logic `pub merge_graph_branch_inner(data_dir, collection, branch, request)` function; the production extractor-decorated handler is now a 7-line shim. `graph_branch_merge_wal_path` refit to take `&Path` instead of `&AppState`. Added `tests/graph_branch_merge_rest_integration_test.rs` (5 cycles) that mounts a minimal axum Router with `State<PathBuf>` on a test-local shim and drives the handler through `tower::ServiceExt::oneshot` with real HTTP `Request`/`Response`. Cycles: dry-run returns report without writing (asserts WAL unchanged); commit returns 200 with `write_back` block and the WAL grows by `entries_written` entries all carrying `origin = "branch_merge:a:b"`; 404 for missing WAL file; 400 for empty target_branch; 404 when WAL has no entries for the requested collection. Verified with `cargo test --test graph_branch_merge_rest_integration_test`: 5 passed / 0 failed. The "extract inner + test-local router" pattern is reusable — future REST endpoint integration tests can mirror it without forcing anyone to build `AppState::for_test`.
|===

== Tier 4b — Superficial-mock cleanup sweep (prior session)

[cols="3,1,3,2", options="header"]
|===
| Item | Status | Commit | Verification

| Item 2 — `entities.rs:310` text_query silent fallback → 400 reject
| Done
| (this commit)
| POST to `/api/v1/entities/search` with `query_text` set returns 400 `"text_query embedding not configured"` instead of silently using `query_vector`.

| Item 3 — `entities.rs:341` stale `// temporal_filter` comment removed
| Done
| (this commit)
| `SearchEntitiesRequest` has no `temporal_filter` field — the comment was misleading "we silently drop this" docs. Replaced with an explicit "this was never accepted" note.

| Item 4 — `graph.rs:290` inert `graph_id="default"` cleaned up
| Done
| (this commit)
| Changed to `String::new()` with a comment explaining the proto field is unused (engine uses the explicit `&str` arg). Not a behavior change — `traverse_with_overrides` already takes the URL-extracted graph_id correctly.

| Item 5 — `multimodal_query.rs:1488,1495` distributed metrics → TD-071
| Done
| (this session)
| `DistributedQueryCoordinator::execute_with_metadata` now returns plan/cache metadata, `DistributedQueryStrategy` preserves node/cache/subquery deltas in `ExecutionMetrics.extra`, and REST distributed query responses read `nodes_involved` + `cache_hits` from those structured metrics instead of hardcoded zeros.

| Item 6 — `/api/v1/experimental/hybrid/search` mock route removed
| Done
| (this commit)
| Route mount in `handlers.rs:1057` removed. Module file at `src/network/rest/v1/hybrid.rs` remains for reference but is no longer mounted. Customers redirected to production `/api/v1/hybrid/search` (now real BM25+vector per Slice 1+1b above).

| T3.3 — Audit `src/network/rest/v1/iceberg_rest_catalog.rs` per-endpoint completeness. Document gaps in ADR-007 `Status:` block if any handler is a stub; implement only if a customer-facing endpoint is missing.
| Done
| (this session)
| Audit complete: all standard Iceberg REST endpoints implemented. Single 501 (`/oauth/tokens`) is intentional opt-in (header-auth alternative). One internal `panic!` on type-coercion is defense-in-depth candidate, not a route handler. ADR-007 gained a `== Wiring (2026-05-26 audit)` section recording the finding.
|===

== Tier 4 — Production hardening polish

[cols="3,1,3,2", options="header"]
|===
| Item | Status | Commit | Verification

| T4.1 — Experimental engine startup guard. In the engine factory, when asked to create RAPTOR or SWIFT and the `experimental-engines` feature flag is absent, return a hard error at engine creation (not at first query). Add startup `warn!` if the flag IS compiled in for a production build.
| Done (already in place)
| (verified this session)
| Both already implemented in `src/storage/engines/factory.rs`: lines 181-208 emit `anyhow::bail!` for Swift/Raptor when feature is absent; lines 505/518 emit `tracing::warn!` when the feature IS compiled in. Verified by `tests/engine_safety_test.rs` (4 passing tests: SWIFT and RAPTOR blocked without feature; SST/VIPER/HELIX/NOVA always available).

| T4.2 — Proto compatibility CI gate. In CI, regenerate proto types and diff against committed types. Fail on non-additive changes (field removal, type change). Add `make proto-check` as the canonical local invocation.
| Done (CI gated; full Rust breaking-diff still TBD)
| (this session)
| `make proto-check PYTHON=/Users/vijaysingh/code/.venv/bin/python` passes locally: it runs `cargo check -p proximadb-proto` plus Python gRPC drift and OpenAPI contract tests, using `--confcutdir=tests/unit` so offline contract checks do not initialize the root test embedding model. **Now wired into CI** as the `proto-compat` job in `.github/workflows/ci.yml` (triggered when `proto/**`, Rust, or Python paths change), and added to the `ci-success` needs list. The job also runs `git diff --exit-code` on `crates/foundation/proximadb-proto/src/proto/` and `src/proto/` to catch hand-edited drift in committed generated sources. **Still deferred:** a true Rust regeneration-and-breaking-diff gate (e.g. via `buf breaking` against a committed descriptor image) — the current manual `scripts/regenerate-proto.sh` flow isn't deterministic enough for CI. That's tracked as a follow-up.
|===

| T4.3 — Dimension / NaN / Inf validation hardening on hot paths (insert + query). Audit entry points; reject NaN/Inf in quantization pipeline; add regression tests.
| Done
| (this session)
| `src/services/operations/vectors/legacy.rs` now validates insert records before WAL/bulk routing and validates search query vectors before cache lookup/execution. Insert validation rejects dimension mismatches and non-finite values across fp32/fp16/bf16 and scalar-quantized scale metadata; search validation rejects non-finite query values and dimension mismatches against collection config. Verified by `cargo check -p proximadb --lib` and `cargo test -p proximadb --lib services::operations::vectors::legacy::index_first_search_tests -- --nocapture` (18 passed, including four new validation regression tests). REST/gRPC response-shape tests remain useful follow-up coverage, not a blocker for TD-070 closure.
|===
|===

== Tier 5 — Demand-blocked, intentionally deferred

Captured here so they don't disappear from the planning surface. Don't implement until a real consumer surfaces.

* **ADR-016 step 4** (PortUserContext consumer migrations) — needs a real cross-crate consumer with port-typed need.
* **Task #72 cluster_port consumer wiring** — slot exists; no consumer yet.
* **ADR-018 pgwire Phase 2/3** — multi-session work per ADR's own roadmap.
* **TD-066 ORION canonical Checkpoint** — covered by ADR-020 but explicit work deferred to follow-up session.
* **TD-067 projection keys facade-vs-OID** — scoped by ADR-020; not pre-release-critical.
* **TD-068 QUASAR cold tier** — depends on ADR-020 + benchmark evidence; could go either way.

== Estimated effort + ship target

* Tier 0: 1-2 days (pure docs)  — completing this session
* Tier 1: 3-5 days (RLS impl + auth-defaults warn + middleware comment)
* Tier 2: 1-2 days (ADR-020 + TD-065 only; TD-066/067/068 deferred)
* Tier 3: 1-2 weeks (ADR-012 minimum-merge + hybrid gRPC parity + Iceberg endpoint check)
* Tier 4: 3-5 days (hardening polish)

**Total realistic v0.2 GA path**: ~5-7 weeks. Tier 0 ships first because it's the customer-trust surface. (Earlier drafts of this doc referred to "v0.3" — that was incorrect framing; v0.2 is the next release.)

== Cross-references

* Plan source: `/Users/vijaysingh/.claude/plans/atomic-puzzling-meteor.md`
* Design audit: `docs/12-design/DESIGN_DOC_STATUS_INDEX_2026_05_26.adoc`
* Consolidation plan: `docs/12-design/DOCUMENTATION_CONSOLIDATION_AND_ARCHIVE_PLAN_2026_05_22.adoc`
* Support contract: `docs/SUPPORTED_SURFACE.adoc` + `docs/12-design/SUPPORTED_SURFACES.adoc`
* Live debt: `docs/10-quality/TECHNICAL_DEBT.adoc`
* Sibling status trackers:
** `docs/_internal/status/PHASE9_REMAINING_2026_05_25.adoc`
** `docs/_internal/status/MULTIMODAL_MULTIMODEL_STATE_2026_05_25.adoc`
