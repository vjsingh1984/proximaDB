= MVP Release Readiness Reconciliation — Round 2 — 2026-05-28
:toc: left
:toclevels: 3
:icons: font
:last-updated: 2026-05-28

[.lead]
Follow-on audit after `MVP_RELEASE_READINESS_RECONCILIATION_2026_05_28.adoc`
closed its enumerated blockers (commit `ca4122cfa`). Round 1 focused on
documented gaps (scope claims, route-health, freshness, recall envelope,
v2 INSERT→SEARCH). Round 2 hunted for blockers and stale claims the first
pass didn't enumerate.

== What changed since Round 1

* `ca4122cfa` closed P0 #3 (v2 INSERT→SEARCH gap, four stacked root causes).
* All 9 Round-1 workstreams (WS1–WS9) landed.
* Background tests confirm `fp16_v2_insert_search_e2e` and `release_smoke_v2`
  pass on a fresh server with no pre-existing state.

== Round-2 blockers (new — fixed in this session)

[cols="1,3,2", options="header"]
|===
| Severity | Blocker | Fix

| P0
| `POST /api/v2/collections/{missing}/records/batch` returned HTTP 200 with
  `BatchOperationResult::failure` body — same silent-failure shape that hid
  the v2 INSERT→SEARCH gap. The REST handler at `src/network/rest/v2/records.rs:1133`
  wrapped the handler's `Ok(failure(NOT_FOUND))` and forwarded it as success.
| `src/network/rest/v2/records.rs` now maps the `NOT_FOUND` error_code onto
  `ApiError::NotFound`, which translates to HTTP 404 via the existing
  `errors/mod.rs:142` mapping. Regression test:
  `tests/release_smoke_v2.rs::rest_v2_insert_to_missing_collection_returns_404`.

| P1
| v2 search `top_k` had no upper bound — adversarial client could request
  unbounded result-buffer allocation (`src/network/rest/v2/records.rs:1369`
  only validated `top_k > 0`).
| Added a default cap of `10_000`, overridable via
  `PROXIMADB_MAX_SEARCH_TOP_K`. Above-cap requests return HTTP 400.
  Regression test:
  `tests/release_smoke_v2.rs::rest_v2_search_top_k_above_cap_returns_400`.

| P1
| Arrow Flight INSERT (`src/network/arrow_ipc/service.rs:834`) routed to
  `handle_record_insert_batch_for_tenant`, which was **missing the precision-
  coercion block** that `handle_record_batch_for_tenant` (the upsert path)
  already applied. Arrow Flight INSERT into an fp16 collection would land
  fp32 records, divergent from REST/gRPC v2 behaviour. Same class of bug as
  the Round-1 v2 metric gap.
| `src/api_handlers/request_handlers.rs` `handle_record_insert_batch_for_tenant`
  now applies the same precision-resolver coercion. Parity test deferred to
  TD-082.

|===

== Round-2 blockers (new — deferred to TD entries)

[cols="1,3,1", options="header"]
|===
| Severity | Blocker | Tracker

| P1
| `precision_resolver` is wired only when `catalog_manager.default_catalog()`
  succeeds (`src/database.rs:159`). In embedded test harnesses that don't
  register a default catalog, the resolver stays unwired and the fp16
  canonical-bytes metric is emitted under `precision="fp32"`. Surfaced this
  session when `rest_v2_insert_into_fp16_collection_accumulates_canonical_bytes_metric`
  failed after the WS3 fixes; the test is `#[ignore]`'d with a note. Production
  wires the resolver correctly.
| TD-080

| P1
| `tests/release_smoke_v2.rs` is Rust-only — no Python SDK equivalent. A
  regression in `clients/python/src/proximadb_sdk/protocols/rest_sync.py`
  insert→search round-trip would not be caught by `make release-check`. The
  existing Python unit test mocks HTTP rather than exercising the live path.
| TD-081

| P1
| `handle_record_insert_batch_for_tenant` and `handle_record_batch_for_tenant`
  now both have the precision-coercion block, but no test asserts they remain
  behaviourally equivalent under identical input. Another precision/validation
  block could regress only one in a future change.
| TD-082

| Medium
| Rust SDK (`clients/rust/src/collection.rs:621`) exposes v2 upsert only — no
  full INSERT with `props` / `text_fields`. Rust SDK users are constrained to
  v1-shape metadata dict.
| TD-083

| Medium
| PostgreSQL wire (`src/network/postgres/protocol.rs`) does not expose v2
  `props` / `text_fields` typed-field insert. pgvector users stay on v1-shape
  SQL.
| TD-084
|===

== Round-2 documentation cleanup landed this session

[cols="2,3", options="header"]
|===
| File | What changed

| `README.adoc` (root)
| Replaced "Industry-leading sub-10ms" header with YMMV-disclaimed reference
  numbers; added v0.2 positioning note pointing at `SUPPORTED_SURFACE`.

| `docs/INDEX.adoc`
| Phase 2 downgraded from "Complete" to "Core complete; modality projections
  Beta". Added explicit pointers to `SUPPORTED_SURFACE` and the reconciliation
  doc for v0.2 scope.

| `docs/README.md`
| Stale "v0.2.0 (February 2026)" replaced with "Pre-release (in development)
  v0.2.0 — narrow single-node cut targeting May 2026".

| `docs/03-api-reference/rest.adoc`
| Added "Not supported in v0.2" warning block listing optimistic/conditional/
  filter/patch/freshness-default; documented `top_k` cap; cross-link to
  `SUPPORTED_SURFACE`.

| `docs/05-concepts/storage-engines.adoc`
| Added warning: SWIFT and RAPTOR are gated behind `experimental-engines`
  Cargo feature; in v0.2 supported engines are SST/HELIX/VIPER/NOVA.

| `docs/00-product/PRD.adoc`
| Time-Series and Event Sourcing rows reclassified from "Planned (P0)" to
  "Beta (post-v0.2 P0)".
|===

== Round-2 documentation cleanup deferred to user discretion

`docs/_internal/enterprise/` and `docs/_internal/business/` contain
positioning copy ("85-96% cost reduction", "296% performance" competitor
claims) that survived Round 1 because the round 1 grep allowlisted internal
marketing paths. The audit recommends scrubbing or scoping these to
internal-only audiences before any external publication of the v0.2 release.
Out of scope for the engineering reconciliation; flagged for the product
owner.

== Verification

After all Round-2 fixes:

[source,bash]
----
# Existing release gate covers the new tests too:
make release-check

# Specifically the new R2 regressions:
cargo test --test release_smoke_v2 -- --test-threads=1 \
    rest_v2_insert_to_missing_collection_returns_404 \
    rest_v2_search_top_k_above_cap_returns_400

# Doc-claim check still green after R2-D edits:
make docs-claim-check
----

== Sequencing for further rounds

Round 3 (post-v0.2 cut) should address:

* TD-080: catalog bootstrap in embedded test harnesses.
* TD-081: Python SDK live INSERT→SEARCH coverage in CI.
* TD-082: handler-parity test for Arrow Flight INSERT vs. REST/gRPC UPSERT.
* TD-083 / TD-084: SDK and pgwire v2 feature exposure.
