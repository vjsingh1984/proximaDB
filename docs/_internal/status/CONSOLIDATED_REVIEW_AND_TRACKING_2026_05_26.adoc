= Consolidated Review And Tracking Audit - 2026-05-26
:toc: left
:toclevels: 3
:icons: font
:last-updated: 2026-05-26

[.lead]
Audit of current code, API surfaces, deployment shapes, and feature dashboards. This file is a reconciliation layer, not a new support contract.

== Executive Summary

ProximaDB has one clearly supported customer-safe path today: canonical ProximaRecord vector CRUD/search over REST v2 and gRPC v2. Most other product surfaces are implemented but correctly belong in Beta or Experimental until canonical WAL/xCatalog authority, API parity, route explanations, and production validation are finished.

The largest honesty risk is not missing code; it is proliferation:

* Dashboard summary rows have been refreshed, but detailed dashboard rows still contain stale "complete/planned" contradictions.
* API contracts exist in at least four layers: `docs/openapi/proximadb-openapi.yaml`, `src/network/rest/*`, `crates/platform/proximadb-api/src/rest/*`, and `src/api_handlers/*`.
* Deployment artifacts have a canonical Helm chart plus divergent infrastructure Helm, plain Kubernetes, enterprise release, systemd, Docker, and two Terraform shapes.
* The storage/query naming migration still carries both `multimodal` and `multimodel` paths.

The fastest path to a more honest release is to finish the small pre-release safety items first, quarantine stale dashboards/deployment manifests, then complete API parity and hot-path correctness gaps.

== Source-Of-Truth Order

Use this precedence when sources disagree:

[cols="2,4,3", options="header"]
|===
| Rank | Source | Role

| 1
| `docs/SUPPORTED_SURFACE.adoc`
| Customer support contract. Code presence alone must not promote a feature.

| 2
| `docs/10-quality/TECHNICAL_DEBT.adoc`
| Live debt/backlog register. Open and partial TD rows are actionable unless explicitly superseded.

| 3
| `docs/_internal/status/PRE_RELEASE_FOUNDATIONS_2026_05_26.adoc`
| Current release-closure tracker for T0-T5 pre-release work.

| 4
| `docs/12-design/DESIGN_DOC_STATUS_INDEX_2026_05_26.adoc`
| Design/ADR wiring status and consolidation queue.

| 5
| `docs/_internal/roadmap/STRATEGIC_ROADMAP.adoc`
| Sequencing and investment direction, not support tier.

| 6
| `docs/_internal/roadmap/MASTER_FEATURE_DASHBOARD.adoc`
| Implementation inventory only. Summary table is refreshed; detailed rows below it are not yet authoritative.

| 7
| `docs/_internal/roadmap/CAPABILITY_MATRIX.toml`
| Stale March 2026 capability snapshot. Keep only after refresh or archive.
|===

== Reconciled Capability Status

[cols="2,1,4,3", options="header"]
|===
| Area | Status | Evidence | Needed For Promotion

| Vector CRUD/search
| Supported
| `docs/SUPPORTED_SURFACE.adoc`; OpenAPI v2 record/search paths; gRPC `proximadb.v2.ProximaRecordService`.
| Maintain v2 contract tests and proto drift guardrails.

| REST v2 / gRPC v2 record APIs
| Supported core, partial query
| REST v2 collections/records/search are mounted in `src/network/rest/v2/mod.rs`. `/api/v2/query` rejects AQL text and accepts UQL/federated SQL only.
| Expand OpenAPI/runtime parity for query and agentic APIs or keep them outside published surface.

| REST/gRPC v1 APIs
| Deprecated compatibility
| v1 compatibility headers in `crates/platform/proximadb-api/src/rest/v1/mod.rs`; gRPC v1 deprecated metadata in `crates/platform/proximadb-api/src/grpc/v1/mod.rs`.
| Keep as adapters only; do not add new product claims on v1.

| Federated SQL / unified query
| Beta
| Supported-surface says strongest on function-backed sources; generic relational scans and remote distributed execution remain incomplete.
| Finish generic relational scans, route once per plan, expose authority/fallback in EXPLAIN.

| Document
| Beta
| Canonical-document-store is default-enabled, but per-call-site cleanup and path-filter depth remain open.
| Close ADR-020 follow-up cleanup and prove canonical WAL replay as the only durable authority.

| ORION graph
| Beta
| ORION operations usable; ADR-012 branch-merge remains partial; TD-066 tracks ORION WAL as parallel durable truth.
| Finish branch merge, demote ORION WAL to projection buffer/checkpoint, and complete declarative gRPC path.

| PULSAR / QUASAR
| Experimental / demotion candidates
| Supported-surface marks PULSAR/QUASAR non-canonical; TD-068 requires rewire-vs-retire decision for QUASAR cold tier.
| Require ADR + benchmark evidence before any promotion.

| Observability
| Beta
| Logs/metrics/traces exist, but canonical replay and operator workflow depth are incomplete.
| Normalize observability records through ProximaRecord/xCatalog and close SIEM/operator gaps.

| Hybrid retrieval
| Beta
| REST and production gRPC both use the port-backed `RestHybridPortImpl` and shared `HybridFullTextIndexMap`; filters and fusion params now flow through the port-backed path. TD-062 tracks planner-level hybrid/rerank surface.
| Finish end-to-end REST/gRPC parity coverage, EXPLAIN, and labeled-quality benchmarks.

| Time-series and event sourcing
| Beta
| Engines exist; support contract says packaging/API parity/scale validation still in progress.
| Publish canonical API surface and production validation.

| Security runtime
| Beta
| JWT/API key/mTLS/SSO hooks exist; RLS only wired in SST/VIPER; NOVA/HELIX pending.
| Finish T1.1/T1.2 and admin/status/revocation policy hardening.

| Distributed cluster execution
| Experimental
| Raft/sharding foundations exist; transport and remote execution remain partial.
| Real RPC transport, failover tests, route metrics, and supported deployment runbook.

| SWIFT / RAPTOR
| Deprecated research engines
| Supported-surface deprecates both; feature-gated behind `experimental-engines`.
| Prefer archive/removal over completion unless a real customer case appears.
|===

== Active Work Inventory

=== Pending / Open

[cols="2,4,1,1,3", options="header"]
|===
| ID | Item | Status | Effort | Notes

| T1.1
| NOVA + HELIX `rls_record_filter`
| Done
| Follow-up test only
| Current tree implements NOVA + HELIX filters; remaining value is a narrow tenant A/B integration test.

| T1.2
| Startup warning when production mode runs with auth disabled
| Done
| Complete
| `src/network/rest/server.rs` emits the production-without-auth warning.

| T1.3
| Document dormant `auth_middleware_unified_port`
| Done
| Complete
| Dormant ADR-016 middleware status is documented in `src/network/auth/middleware.rs`.

| T3.1 write-back + integration
| Graph branch merge completion
| In progress
| 1-2 weeks
| `branch_id` plumbing, pairwise pure merge classification, catalog `branch_merge_policy` metadata, and dry-run REST endpoint are present. Remaining: canonical WAL merge-fragment write-back and full branch-cycle integration tests.

| T3.2 remaining slices
| Hybrid gRPC parity
| Done except optional live smoke
| 1-2 weeks
| Shared index map, filters, fusion params, and REST/gRPC handler tests landed. Remaining only optional live server REST/gRPC smoke.

| T4.2
| Rust-side proto compatibility gate / `make proto-check`
| Done
| 2-5 days
| `make proto-check` added and verified with `PYTHON=/Users/vijaysingh/code/.venv/bin/python`; Python/OpenAPI drift guards remain in place.

| TD-005
| Multi-language SDKs
| Open
| Large
| Current clients exist but status is inconsistent across docs/dashboard.

| TD-024 / TD-025
| SWIFT / RAPTOR incomplete
| Open but deprecated
| Large
| Should be archive/remove candidates, not focus candidates.

| TD-029
| Code coverage below target
| In progress
| Large
| Coverage improved; more gains require integration harnesses.

| TD-030
| Integrated ibkrtrading validation
| Open
| 2-4 weeks
| Proof-of-production-readiness gap.

| TD-035
| Graph query executor bypass/stub debt
| Open
| 3-4 weeks
| Some later docs claim graph query improvements, so re-audit before work.

| TD-040
| Quantization-aware row-group pruning read side
| Partial
| 2-3 weeks
| Write-side stats landed; read-side pruning pending.

| TD-042
| Cache architecture over-complexity
| Open
| 1-2 weeks
| Good maintainability win, lower product impact.

| TD-052
| Proactive agentic memory retrieval
| Open speculative
| 6-8 weeks
| Keep future/research; do not roadmap as committed.

| TD-053 / TD-054
| Encoding gates and L-RAG lazy loading
| Open
| 2-6 weeks
| Research/frontier work; not release-critical.

| TD-060
| Storage layout authority / external format semantics
| Open, hooks landed
| 4-6 weeks
| Core architectural correctness for open formats.

| TD-061
| Optional relational capabilities
| Open
| 6-8 weeks
| Keys/FKs/checks/materialized views as cataloged opt-in semantics.

| TD-062
| Hybrid retrieval/reranking planner surface
| Open
| 6-10 weeks
| Planner/EXPLAIN convergence for lexical+dense+sparse+graph+rerank.

| TD-063
| Observability/SIEM convergence
| Open
| 8-12 weeks
| High strategic value but too large for immediate focus.

| TD-064
| Predicate-aware vector correctness and fallback reporting
| Open
| 6-8 weeks
| High correctness/security impact.

| TD-066 / TD-067 / TD-068
| Canonical WAL follow-ups for graph/projection/QUASAR
| Open
| 2-5 weeks each
| Governed by ADR-020.

| TD-070
| Insert/query dimension + NaN/Inf validation on hot paths
| Done
| Done
| Shared vector service validation rejects dimension mismatches and non-finite insert/search vectors before WAL/search execution; focused module tests pass.

| TD-071
| Distributed query metrics plumbing
| Done
| Done
| Coordinator metadata now flows through facade metrics into REST `nodes_involved` and `cache_hits`.
|===

=== Completed / Resolved Recently

* Dashboard summary honesty refresh (T0.1) and pre-release tracker creation (T0.2).
* ADR-020 canonical WAL authority model and default-enabled `canonical-document-store` (T2.1/T2.2, TD-065).
* ADR-007 Iceberg endpoint audit (T3.3).
* Experimental engine startup guard already verified (T4.1).
* Multiple graph branch merge pure-logic slices are done: diff walker, WAL branch/LSN filter, merge-base discovery, and pure orchestrator.

=== Deprecated / Archive Candidates

* SWIFT and RAPTOR should remain deprecated research engines; avoid investing completion work unless a specific user changes priority.
* REST/gRPC v1 are compatibility adapters and should not receive new product-only features.
* `src/storage/multimodal/` is a legacy twin of canonical `src/storage/multimodel/`.
* `deploy/k8s/release-1/*` claims enterprise release 1.0.0 behavior and should be archived or explicitly marked non-authoritative.
* `deploy/infrastructure/helm/proximadb` should be renamed to a cloud example or reconciled into `deploy/helm/proximadb`.
* `docs/_internal/roadmap/CAPABILITY_MATRIX.toml` is stale as of March 2026 and should be refreshed or archived.

== API Shape Audit

[cols="2,3,4,3", options="header"]
|===
| Surface | Current Shape | Findings | Action

| Published OpenAPI
| `docs/openapi/proximadb-openapi.yaml`
| v2-first and intentionally excludes v1. Covers health, collections, schema, records, search, query, explain.
| Keep as public SDK contract; add runtime drift checks.

| Runtime REST
| `src/network/rest/v2/*`, `src/network/rest/v1/*`, `src/network/rest/v3/*`
| v2 is mounted; v3 document ingest exists but is not in OpenAPI; v1 remains broad compatibility surface.
| Decide whether v3 is private/experimental or publish it. Keep new work on v2/v3 only.

| Platform API crate
| `crates/platform/proximadb-api/src/rest/*`
| Contains contract/router replicas and compatibility stubs. Some graph routes are `not_implemented_handler` here while root runtime has richer handlers.
| Treat as extracted contract layer; reconcile route parity against root runtime before claiming extraction complete.

| Legacy handler layer
| `src/api_handlers/*`
| `crates/platform/proximadb-api/src/rest.rs` still says TODO to move handlers from `src/network/rest` and `src/api_handlers`.
| Do not add new protocol code here except as temporary adapter.

| gRPC v2
| `proto/proximadb/v2/record.proto`, `src/network/grpc/v2/record_service.rs`
| Canonical record service exists and is registered.
| Add Rust-side proto drift/breaking gate.

| gRPC v1
| `proto/proximadb/v1/*`, `crates/platform/proximadb-api/src/grpc/v1/*`
| Compatibility only; some services return safe unimplemented/not configured if ports are missing.
| Keep v1 honest in docs and deprecate by metadata.

| Query APIs
| `/api/v2/query`, `/api/v1/unified/*`, pgwire
| `/api/v2/query` rejects AQL text; v1 unified remains broad and deprecated; pgwire parity is ADR-018 partial.
| Publish one canonical query matrix by language/surface.

| Dashboards
| React UI, static `src/network/rest/dashboard.html`, Rust monitoring dashboard module
| React UI displays fake enterprise version/readiness; static dashboard and Rust HTML dashboard overlap.
| Pick one product dashboard and one lightweight embedded health page.
|===

== Deployment Shape Audit

[cols="2,2,4,3", options="header"]
|===
| Path | Status | Findings | Action

| `deploy/helm/proximadb`
| Canonical
| Chart version/appVersion 0.2.0; values expose REST/gRPC/Arrow/metrics and sane single-node defaults.
| Keep as canonical Kubernetes chart.

| `deploy/docker`
| Canonical local/container
| Docker and compose assets are in the expected location.
| Keep and test against supported ports.

| `deploy/kubernetes`
| Stale plain manifests
| Deployment pinned to image/app version 0.1.4.
| Update to 0.2.0 or mark as legacy example.

| `deploy/k8s/release-1`
| Not honest for OSS support surface
| Enterprise 1.0.0 image and many env vars claim features that support docs mark Beta/Experimental.
| Archive or add a strong non-authoritative banner.

| `deploy/infrastructure/helm/proximadb`
| Divergent Helm variant
| Uses different image (`proximadb/proximadb-server`), replica defaults, values schema, and a monolithic template. Template has questionable `.Values` scoping and duplicate pod `securityContext`.
| Rename as AWS/cloud example after fixing, or fold useful values into canonical chart.

| `deploy/terraform/*`
| Generic cloud examples
| AWS/Azure/GCP roots plus reusable ProximaDB module.
| Keep as provider examples if validated.

| `deploy/infrastructure/terraform/*`
| AWS managed-service stack
| Separate VPC/EKS/env model with its own Helm values.
| Keep as AWS reference architecture, but point to canonical chart.

| `deploy/systemd/legacy-root`
| Legacy
| Correctly quarantined under legacy-root.
| Keep only for compatibility; do not duplicate new units here.

| `deploy/packaging/*`
| Packaging
| Package scripts/systemd/WiX are separate from deployment runtime.
| Keep, but validate version strings before release.
|===

== Dashboard / Tracking Audit

[cols="2,2,4,3", options="header"]
|===
| File | Status | Finding | Action

| `MASTER_FEATURE_DASHBOARD.adoc`
| Partially reconciled
| Maturity summary is honest as of 2026-05-26. Detailed feature rows still contradict it for SWIFT/RAPTOR, streaming/CDC, SDKs, time-series/event, and security.
| Reconcile or move detailed rows under "historical March baseline".

| `CAPABILITY_MATRIX.toml`
| Stale
| `as_of_date = "2026-03-02"` and conflicts with later TD/dashboard updates.
| Refresh from code/tests or archive.

| `TECHNICAL_DEBT.adoc`
| Authoritative backlog
| Useful but contains duplicate IDs (`TD-043`, `TD-044`) and resolved entries mixed with open entries.
| Normalize duplicate IDs and split resolved archive later.

| `PRE_RELEASE_FOUNDATIONS_2026_05_26.adoc`
| Active
| Good release tracker; should be the primary closure board.
| Keep current.

| React `ui/` dashboard
| Stale/fake claims
| Shows `v1.0.4 - Production Ready (99.99%)` and static uptime/status.
| Replace fake values with live health/version API data or mark mock/demo.

| `src/network/rest/dashboard.html`
| Duplicate embedded dashboard
| Static Chart.js page overlaps with Rust dashboard module and React UI.
| Keep as dev-only embedded page or remove after React UI is wired.
|===

== Top 10 Focus Candidates

Ranked by impact/effort ratio, with high-impact low-effort work first.

== Follow-up Remediation Pass

[cols="1,3,2,4", options="header"]
|===
| Rank | Candidate | Current status | Notes

| 1
| T1.2 production-mode auth-disabled startup warning
| Done / verified in current tree
| `src/network/rest/server.rs` emits the production-mode auth-disabled warning.

| 2
| Quarantine or fix stale deployment manifests
| Done for stale tags and release-1 caveat
| `deploy/kubernetes` now uses `0.2.0`; `deploy/k8s/release-1` files are marked historical/non-authoritative.

| 3
| Reconcile detailed rows in `MASTER_FEATURE_DASHBOARD.adoc`
| Done
| Unsupported/deprecated/experimental rows were downgraded or scoped to their real support tier.

| 4
| T1.3 document dormant `auth_middleware_unified_port`
| Done / verified in current tree
| `src/network/auth/middleware.rs` documents the dormant ADR-016 migration state.

| 5
| Finish `multimodal` -> `multimodel` consumer sweep
| Done / verified in current tree
| No active `storage::multimodal` module or import remains; similarly named query APIs are separate concepts.

| 6
| T1.1 RLS for NOVA + HELIX
| Done / verified in current tree
| NOVA and HELIX implement engine-level `rls_record_filter`; follow-up test coverage remains useful.

| 7
| TD-071 distributed query response metrics plumbing
| Done
| Coordinator execution metadata now flows through facade metrics into REST `nodes_involved` and `cache_hits`.

| 8
| T4.2 Rust-side proto compatibility gate / `make proto-check`
| Partial
| Local `make proto-check` target added and verified with `/Users/vijaysingh/code/.venv/bin/python`; gRPC drift baseline shrank for resolved graph RPCs. Deterministic generated-descriptor refresh/full CI breaking gate remains pending.

| 9
| TD-070 vector dimension + NaN/Inf validation on insert/query hot paths
| Done
| Shared vector service now rejects insert/query dimension mismatches and NaN/Inf before WAL/search execution. Verified with focused module tests; REST/gRPC response-shape tests remain follow-up coverage.

| 10
| T3.2 hybrid filter/fusion parity tests
| Handler/service parity covered; server smoke pending
| Production gRPC already uses `RestHybridPortImpl` with shared BM25 indexes. REST request translation now preserves filters + RRF/weighted fusion params; `RestHybridPortImpl` forwards filters to vector search and honors proto fusion params. REST handler and gRPC service tests now verify the shared request shape reaches `HybridPort`. Remaining work: optional live server REST/gRPC smoke.
|===

[cols="1,3,1,1,1,4", options="header"]
|===
| Rank | Candidate | Impact | Effort | Ratio | Why

| 1
| T1.2 production-mode auth-disabled startup warning
| High
| XS
| Very high
| Prevents unsafe deployments and keeps security claims honest with minimal code.

| 2
| Quarantine or fix stale deployment manifests
| High
| XS-S
| Very high
| `deploy/kubernetes` is pinned to 0.1.4 and `deploy/k8s/release-1` claims unsupported enterprise 1.0 behavior.

| 3
| Reconcile detailed rows in `MASTER_FEATURE_DASHBOARD.adoc`
| High
| S
| Very high
| Removes the main source of stale "complete/planned" contradictions.

| 4
| T1.3 document dormant `auth_middleware_unified_port`
| Medium
| XS
| Very high
| Cheap anti-proliferation fix; prevents another parallel auth layer.

| 5
| Finish `multimodal` -> `multimodel` consumer sweep and delete legacy module
| Medium-High
| S
| High
| Removes typo-twin storage/query surface and reduces future cognitive load.

| 6
| T1.1 RLS for NOVA + HELIX
| High
| S
| High
| Important multi-tenant safety gap; currently only SST/VIPER have engine-layer hooks.

| 7
| TD-071 distributed query response metrics plumbing
| Medium-High
| S
| High
| Fields and coordinator stats exist; response currently reports unwired zeros.

| 8
| T4.2 Rust-side proto compatibility gate / `make proto-check`
| High
| S
| High
| Protects v1/v2 API shapes from silent drift and complements existing Python/OpenAPI drift tests.

| 9
| TD-070 vector dimension + NaN/Inf validation on insert/query hot paths
| High
| M
| High
| Completed in this pass. Keep follow-up API response-shape tests on the validation backlog.

| 10
| T3.2 hybrid filter/fusion parity tests
| High
| M
| Medium-High
| Completed unit-level parity in this pass. Remaining Beta hardening is full REST/gRPC integration coverage and quality benchmarks.
|===

Next tier after these: ADR-012 graph branch merge completion, TD-066 ORION canonical WAL checkpointing, and TD-064 predicate-aware vector search correctness. Those are high-impact but larger and should follow the quick trust/safety wins above.

== Consolidation Rules

* New customer-facing claims must cite `docs/SUPPORTED_SURFACE.adoc`.
* New implementation inventory should update `MASTER_FEATURE_DASHBOARD.adoc`, but only after support tier is checked.
* New debt must be a TD row unless it is pure release closure, in which case add it to `PRE_RELEASE_FOUNDATIONS_2026_05_26.adoc`.
* New design work must be an ADR or a `docs/12-design/*` file and should be reflected in `DESIGN_DOC_STATUS_INDEX_2026_05_26.adoc`.
* New deployment examples must live under `deploy/` and must declare whether they are canonical, provider example, legacy, or experimental.
* Avoid adding new root-level dashboards or deployment roots.

== Verification Notes

This audit was performed by reading and grepping the repository on 2026-05-26. No build or test suite was run for this document.

Primary files inspected:

* `docs/SUPPORTED_SURFACE.adoc`
* `docs/12-design/SUPPORTED_SURFACES.adoc`
* `docs/10-quality/TECHNICAL_DEBT.adoc`
* `docs/_internal/status/PRE_RELEASE_FOUNDATIONS_2026_05_26.adoc`
* `docs/_internal/status/PHASE9_REMAINING_2026_05_25.adoc`
* `docs/_internal/status/MULTIMODAL_MULTIMODEL_STATE_2026_05_25.adoc`
* `docs/12-design/DESIGN_DOC_STATUS_INDEX_2026_05_26.adoc`
* `docs/_internal/roadmap/MASTER_FEATURE_DASHBOARD.adoc`
* `docs/_internal/roadmap/CAPABILITY_MATRIX.toml`
* `docs/openapi/proximadb-openapi.yaml`
* `proto/proximadb/v1/*`, `proto/proximadb/v2/record.proto`
* `src/network/rest/*`, `src/network/grpc/*`, `crates/platform/proximadb-api/src/*`
* `deploy/*`
* `ui/src/components/Dashboard.tsx`, `ui/src/types/api.ts`
