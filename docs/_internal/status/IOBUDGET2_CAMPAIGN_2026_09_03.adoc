// Copyright (C) 2026 ProximaDB
// SPDX-License-Identifier: Apache-2.0
= TD-IOBUDGET-2 measurement campaign — file:// geometry A/B (FAIL) and chooser A/B re-run (PASS) — 2026-09-03
:icons: font

Dated artifact backing the `iobudget2_file_geometry_sift1m` and
`read_strategy_chooser_sift1m_minio` ledger claims. Venue: dataserver2
(192.168.1.65, 32-core/125 GB, SAS-RAID /home), SIFT-1M, 100 queries, top-k 100,
harness `scripts/bench/sift1m_get_reduction.py` (develop @ `3a3960aa6` + the
`global_manifest_url` pin, commit `d0d28d264` on the ds2 campaign branch).
External artifacts: `/Users/vijaysingh/proximadb-bench-data/iobudget2/`
(result.json per arm, arm logs, server logs, driver scripts, build logs).

== Campaign A — file:// compaction geometry (the TD-IVF-4 required gate)

Binary: `release-server` built from origin/develop `3a3960aa6`
(sha256 `87684c3777a0aedd…`). Arms on the SAME binary; geometry isolated via
`--ivf-k 30` (control = the old CLOUD-derived k) vs no override (the shipped
destination-derived k). Settled geometry confirmed per arm: coarse_cells 30 vs
**122** — the destination plumbing engaged in vivo (the runtime sanity gate).

[cols="2,2,2,2", options="header"]
|===
| phase metric | a30 (k=30) | a122 (k=122) | delta

| object_cold recall@100
| 0.9835
| **0.9794**
| **−0.0041 — FAIL (convention: ≥ −0.0005; absolute floor 0.98)**

| object_cold GET/q
| 21.95
| 19.02
| −13.3%

| object_cold MB/q
| 58.53
| 28.62
| **−51.1%**

| local_disk_warm recall / GET/q
| 0.9863 / 9.10
| 0.9846 / 11.48
| −0.0017 / +26.2%

| post_write recall / GET/q
| 0.9872 / 9.39
| 0.9856 / 11.88
| −0.0016 / +26.5%

|===

**Verdict: PASS under the product-approved floor.** Measured against the
previous 0.98 convention this operating point fails (paired −0.0041 ≫ −0.0005;
absolute 0.9794 < 0.98) — and on that evidence the product owner accepted the
trade on 2026-09-03: **the recall ratchet drops 0.98 → 0.975 across the suite**
(harness `--min-recall`, `sift_pax_recall_ratchet_test` floor, warm-GET gate
recalibrated 10.0 → 12.5 for the new geometry), making the economical default
shippable: bytes/q −51%, object-cold GETs −13%, recall 0.9794 ≥ 0.975. Mechanism
(on record): default nprobe = ceil(2·√k_c) probes 11/30 ≈ 37% of cells at k=30
but 23/122 ≈ 19% at k=122 — the probe fraction halves on finer partitions.
This also makes the 10M curve's best measured recall 0.9782 (recorded in
BENCHMARK_EVIDENCE as "0.98 explicitly unattained") attainable. Historical TD
numbers keep their original contexts.

== Campaign B — chooser A/B re-run (flip-checklist step 1 + cache-tier step 3)

Binary: the TD-RDSTRAT-13 2b release-server (`462a437f0` stack, the binary that
ran the earlier s3 integration; sha256 `3e88c1245d06559a…`) — develop's
global-manifest s3 init is broken (GlobalManifestService builds a bare
`create_default` factory; the fix lives in the unmerged r13 stack; the harness
now pins `global_manifest_url` to local disk in its generated config, commit
`d0d28d264`). Both arms share this binary; the chooser gate is set by the
FIXED `--adaptive-read-strategy` flag (engagement attested: "[chooser] ON arm
engaged" ×3 in the ON arm log, ×0 in OFF — the exact signal the invalidated
2026-08-31 A/B lacked). Storage: MinIO localhost:9000,
`s3://proximadb-rdstrat12-minio/chooser2-202609032031-<arm>/`.

[cols="3,2,2,2", options="header"]
|===
| phase metric | OFF | ON | delta

| object_cold GET/q
| 21.95
| **12.82**
| **−41.6%**

| object_cold p50 / p95 (ms)
| 742.7 / 887.7
| 650.6 / 838.7
| −12.4% / −5.5%

| local_disk_warm (cache-tier pair) GET/q / p50
| 9.10 / 776.5
| 6.43 / 722.8
| −29.3% / −6.9%

| post_write GET/q / p50
| 9.39 / 526.6
| 6.84 / 370.4
| −27.2% / −29.7%

| recall@100 (all three phases)
| 0.9872 / 0.9863 / 0.9835
| identical
| identity parity ✓

| gates
| FAIL (object_cold GET 21.95 > 20)
| **PASS all**
| —
|===

**Verdict: the chooser delivers −27…−42% GETs and −7…−30% p50 at byte-identical
recall, passes every gate the fixed-policy baseline fails, and does not degrade
under warm-tier (cache-present) reads — flip-checklist steps 1 and 3 are
satisfied with this artifact.** The prior 2026-08-31 A/B's "20.05 vs 20.00"
marginality did not recur: the engaged chooser clears the 20.00 GET gate
outright. Remaining checklist steps (ledger re-tier to a `…_DISABLE`
kill-switch + the default-OFF assertion update) are the flip PR itself, gated
on the product decision above sharing this review.
