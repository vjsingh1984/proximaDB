= Warm-tier + flush-floor 1M evidence (2026-07-24) — the #1179 measurement set
:toc:

Dated artifact backing the BENCHMARK_EVIDENCE claims for the #1179 package
(TD-FLUSH-3 S1/S2 predicted-bytes floor, TD-CACHE-2 S1/S2a eviction+sharding,
TD-CACHE-4 warm-tier arming) and the TD-COMPACT-1 resolution. All runs: SIFT1M
(128d, Euclidean), local macOS arm64 (Apple Silicon, 64 GB), serialized runs,
release build, REST e2e through the multiplexed 5678 surface, paced streaming
ingest (1000/batch, 50 ms pacing), full-1M brute-force ground truth,
`PROXIMADB_PAX_COALESCED_RABITQ=1 PROXIMADB_PAX_VECTOR_QUANT=rabitq`,
config `/tmp/compact-bench.toml` shape (local file://, write_buffer 4096 MB).
Harness: `scripts/bench/sift_rest_warmtier_sweep.py`. Indicative dev-machine
runs, not an SLA.

== Run ladder (same box, same protocol, three builds)

|===
| | develop baseline (pre-#1179) | + cache S1/S2a | + flush floor (full #1179)

| L0 segments after 1M ingest | 10 | 10 | *2* (0 compactions needed)
| Recall@10 (200 q, full-1M GT) | 0.985–0.999 | 0.9850 | *0.9865*
| Cold c=1 QPS / mean | 3.9 / 258 ms | 5.6 / 179 ms | 6.2 / 161 ms
| Cold c=8 QPS / mean | 16.8 / 468 ms | 27.3 / 289 ms | 37.2 / 212 ms
| Cold c=16 QPS / mean (ceiling) | 18.2 / 861 ms | 27.6 / 571 ms | 38.6 / 407 ms
| Hot c=16 QPS / p50 | ~2,455 / 5.1 ms | 2,455 / 5.1 ms | *4,085 / 3.8 ms*
| Invariants cache hit rate | unarmed (0/0) | unarmed in that run | *99.9%* (1,838 hits / 2 misses)
| `proximadb_queries_total` | absent | absent | 1,120 (live)
|===

Interim datapoint (4-segment layout, before the AutoFlushDriver floor fix):
cold c=16 = 59.4 QPS — more files raise inter-file parallelism (#1161) while
costing ~2× the GETs; the 2-segment layout optimizes the billed dimension
(GETs) and cedes local-SSD cold latency until TD-SEARCH-2 S2 (intra-query
parallelism) lands.

== Notes

* Ingest wall-clock 88–93 s in all three configurations; floor's memtable
  residency peaks ~11 GB transient at 1M-scale ingest (documented in
  TD-FLUSH-3; bounded by capacity watermarks + RPO timer).
* Compaction health references: #1175 verification (9 compactions, 19.7 s
  total, mean 2.2 s, RSS 7.4 GB peak) posted on that PR from the same bed.
* Query-set discipline: recall/pre-settle sets disjoint from cold-sweep sets
  disjoint from hot-repeat sets (no result-cache contamination of cold
  numbers; hot pass measures the second round of a repeated set).
