= Vision-Aligned Sprint Roadmap (Multi-Model, Format-First)
:toc: left
:toclevels: 2
:icons: font

[.lead]
Sprint-level roadmap derived from link:../../../../concepts/vision.adoc[Vision]. This plan
focuses on unifying cross-model query, catalog-centric governance, deployment neutrality,
and operational simplicity.

== Assumptions

* Sprint length: 2 weeks
* Time horizon: 0-3 months (S1-6), 3-6 months (S7-12), 6-12 months (S13-24)
* Roadmap prioritizes SOLID design and extensibility for new engines and protocols.

== SOLID Design Guardrails (Apply Across All Milestones)

* **Single Responsibility**: isolate parse/plan/execute, scan/provider, catalog/governance.
* **Open/Closed**: add engines, protocols, and catalogs via interfaces and adapters.
* **Liskov Substitution**: all engine/protocol implementations must satisfy shared contracts.
* **Interface Segregation**: split read providers (scan/filter/project) and protocol handlers.
* **Dependency Inversion**: query and governance depend on abstractions, not concrete engines.

== Phase 0-3 Months (Sprints 1-6)

=== M1: Unified Cross-Model Planner and Executor (S1-S3)

*Owner modules:* `src/query/federated/mod.rs`, `src/query/unified/mod.rs`,
`src/query/unified_query_optimizer.rs`, `src/network/rest/v1/unified_query.rs`,
`src/api_handlers/unified_handlers.rs`

*Sprint plan:*

* **S1**: Inventory all query paths and define a single Planner/Executor interface.
* **S2**: Route all multi-model SQL (`VECTOR_SEARCH`, `GRAPH_QUERY`, `DOCUMENT_QUERY`, `LOGS`,
  `METRICS`) through the federated context.
* **S3**: Remove or feature-gate redundant paths; unify plan/metrics schema.

*Acceptance criteria:*

* All multi-model SQL endpoints execute via one planner/executor path.
* `/api/v1/unified/explain` returns a single plan format across REST and gRPC.
* Legacy query paths are disabled or isolated behind compile-time flags.

*Test checklist:*

* Unit tests: parser and optimizer in `src/query/federated/*`.
* Integration tests: REST + gRPC multi-model queries.
* Regression tests: vector-only, document-only, and graph-only queries.
* Explain plan smoke test: consistent plan schema and metrics.

=== M2: Dual Columnar Execution (Arrow In-Memory + I/O Range Scan) - Core (S2-S5)

*Owner modules:* `src/storage/engines/core/formats/columnar/columnar_query_engine/*`,
`src/storage/engines/core/formats/columnar/unified_compaction.rs`,
`src/query/unified_query_optimizer.rs`

*Sprint plan:*

* **S2**: Introduce `ColumnarReadProvider` and scan-policy abstraction.
* **S3**: Implement Arrow RecordBatch in-memory scan provider (protocol-optimized).
* **S4**: Implement range-pruned I/O scan provider (physics-optimized).
* **S5**: Planner selects provider; metrics track bytes scanned and provider choice.

*Acceptance criteria:*

* Two scan providers co-exist and are selectable via planner policy.
* Same query returns equivalent result sets across providers (order differences allowed).
* I/O scan performs pruning and emits bytes-scanned metrics.

*Test checklist:*

* Unit tests: scan provider selection and Arrow conversion.
* Integration tests: identical query results across providers.
* Performance tests: pruning effectiveness on clustered data.

=== M3: Graph API Parity (REST/gRPC) (S4-S6)

*Owner modules:* `src/api_handlers/unified_handlers.rs`, `src/graph/service.rs`,
`src/network/*`, `clients/python/*`

*Sprint plan:*

* **S4**: Define canonical response schema for graph operations.
* **S5**: Fix REST/gRPC schema mismatches and missing endpoints.
* **S6**: Update SDKs and docs to match canonical schema.

*Acceptance criteria:*

* REST and gRPC return identical logical response schemas.
* SDK integration tests pass without response-shape adapters.

*Test checklist:*

* Integration tests: `clients/python/tests/*` graph suite.
* Parity tests: golden responses for REST vs gRPC.

=== M4: Observability and Health Baseline (S5-S6)

*Owner modules:* `src/network/tests/metrics_tests.rs`, `src/monitoring/*`,
`src/services/operations/vectors.rs`

*Sprint plan:*

* **S5**: Fix Prometheus endpoint test; define minimal SLI set.
* **S6**: Ensure endpoints expose consistent metrics across models.

*Acceptance criteria:*

* Metrics endpoint passes test and includes `proximadb` namespace.
* Health checks reflect real service state (storage, WAL, query).

*Test checklist:*

* Metrics test passes and is not ignored.
* Smoke test: `/metrics`, `/health` return expected payloads.

== Phase 3-6 Months (Sprints 7-12)

=== M5: Catalog-Centric Governance v1 (S7-S9)

*Owner modules:* `src/catalog/*`, `src/catalog/federation/*`, `src/schema/*`, `src/query/*`

*Sprint plan:*

* **S7**: Apply missing feature gates for external catalogs; unify catalog interface.
* **S8**: Add lineage metadata hooks for vector/doc/graph operations.
* **S9**: Enforce schema validation via catalog on all query paths.

*Acceptance criteria:*

* External catalogs are feature-gated and discoverable via config.
* All models register schema + lineage metadata in shared catalog.
* Planner fails fast on schema violations.

*Test checklist:*

* Unit tests for catalog gating and schema validation.
* Integration test: cross-model query uses catalog resolution.

=== M6: Distributed Foundations (S9-S12)

*Owner modules:* `src/services/*`, `src/network/*`, `src/storage/*`,
`config/config.toml`

*Sprint plan:*

* **S9**: Wire sharding + routing for vector collections.
* **S10**: Implement replication for writes with basic consistency levels.
* **S11**: Add routing health checks and retry policy enforcement.
* **S12**: Stabilize distributed read/write behavior and update docs.

*Acceptance criteria:*

* 3-node cluster supports sharded collections.
* Replication honors configured consistency levels (One/Quorum/All).
* Routing health checks update node availability.

*Test checklist:*

* Integration tests: sharding, routing, replication on 3-node setup.
* Failure tests: node down, retry and read-replica routing.

=== M7: Backup and Restore v1 (S12)

*Owner modules:* `src/services/events/persistence.rs`,
`src/storage/engines/core/formats/proximablocks/*`, `docs/04-operations/*`

*Sprint plan:*

* **S12**: Snapshot + restore flow for vector/doc/graph WALs.

*Acceptance criteria:*

* Snapshot completes without data loss for all models.
* Restore brings services online with consistent state.

*Test checklist:*

* Backup/restore integration test on sample datasets.
* Corruption injection test (WAL segment loss) and recovery behavior.

== Phase 6-12 Months (Sprints 13-24)

=== M8: Arrow Flight Integration (S10-S12, then hardening S19-S22)

*Owner modules:* `src/network/*`, `src/storage/engines/core/formats/columnar/*`

*Sprint plan:*

* **S10-S12**: Arrow Flight endpoints backed by Arrow in-memory scans.
* **S19-S22**: Cost-based selection between Arrow scans and I/O range scans.

*Acceptance criteria:*

* Arrow Flight endpoints serve query results using RecordBatch scans.
* Planner auto-selects provider based on cost model and protocol.

*Test checklist:*

* Protocol tests: Arrow Flight query/response schema.
* Consistency tests: Arrow Flight vs REST parity.

=== M9: HA, Rebalancing, and Consistency Semantics (S13-S18)

*Owner modules:* `src/services/*`, `src/network/*`, `src/storage/*`

*Sprint plan:*

* **S13-S15**: HA behavior for node loss; failover routing.
* **S16-S18**: Rebalancing and shard movement with minimal disruption.

*Acceptance criteria:*

* Read/write availability maintained during node failures.
* Rebalancing completes without data loss or stale reads.

*Test checklist:*

* Failure injection tests (kill node, network partition).
* Rebalance tests with data integrity verification.

=== M10: Governance Enforcement Across Models (S17-S20)

*Owner modules:* `src/security/*`, `src/query/*`, `src/catalog/*`

*Sprint plan:*

* **S17-S20**: Enforce RBAC and policy checks at planner/executor boundary.

*Acceptance criteria:*

* Policies apply consistently across vector/doc/graph/obs.
* Unauthorized cross-model queries are blocked with clear errors.

*Test checklist:*

* Role-based allow/deny matrix tests.
* Cross-model query policy enforcement tests.

== Definition of Done (Applies to All Milestones)

* Feature flag is documented and defaults are safe.
* Tests pass in CI for affected modules.
* Docs updated in `docs/` and `docs/_internal/` as appropriate.
* Metrics cover the new feature path.

