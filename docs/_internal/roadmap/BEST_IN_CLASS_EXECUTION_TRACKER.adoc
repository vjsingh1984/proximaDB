= Best-in-Class Foundation Execution Tracker
:toc: left
:toclevels: 2
:last-updated: 2026-03-12
:last-audit: 2026-05-26
:last-reconciled: 2026-06-24

[NOTE]
====
*2026-06-24 reconciliation.* The *Workstreams* and *Milestone Gates* status columns below were
re-scored against `origin/develop` HEAD and the live
link:../../10-quality/TECHNICAL_DEBT.adoc[Technical Debt Register]; a 2026-06-24 block was appended
to the *Session Log* (the March-2026 entries are retained as historical baseline, per the note below).
Authority for live status: the Technical Debt Register (implementation) +
link:../../SUPPORTED_SURFACE.adoc[SUPPORTED_SURFACE.adoc] (support contract). The broader tracking
reconciliation is recorded in link:./RECONCILIATION_2026_06_24.adoc[RECONCILIATION_2026_06_24].
====

[.lead]
Operational execution tracker for closing foundation gaps and achieving best-in-class product quality (UX, robustness, and product-market fit).

[IMPORTANT]
====
**Staleness note (2026-05-26 audit)**: the workstream log below was last
updated 2026-03-14, ~2.5 months before this audit. Several
workstreams have meaningful progress since (notably Task #76
`collection_port` consumer chain, Task #69 / ADR-016 PortUserContext
foundation, Phase 9 platform extraction). For current truth use:

* `docs/_internal/status/PHASE9_REMAINING_2026_05_25.adoc` — Phase 9
  refactor state (live).
* `docs/_internal/status/MULTIMODAL_MULTIMODEL_STATE_2026_05_25.adoc` —
  multimodal migration state (live).
* `docs/12-design/DESIGN_DOC_STATUS_INDEX_2026_05_26.adoc` —
  design-doc-to-code wiring snapshot.

This tracker is preserved (not archived) because (a) the workstream log
is genuinely useful historical context for the March 2026 baseline,
and (b) several active implementation tickets still cite it
(`docs/_internal/roadmap/implementation/active/FOUNDATIONAL_MULTIMODEL_EXECUTION_2026.adoc`,
`docs/_internal/roadmap/implementation/active/TIERED_EXECUTION_REVIEW_2026_03_15.adoc`).
A future session that refreshes the log should retain the existing
entries and append a 2026-05 block, not start over.
====

== Scope

This tracker complements link:./STRATEGIC_ROADMAP.adoc[Strategic Roadmap] and converts strategy into session-level execution.

Primary objective:

* Establish one coherent, reliable, production-grade platform surface for vectors + documents + graphs + time-series + events.
* Maintain capability state in link:./CAPABILITY_MATRIX.toml[CAPABILITY_MATRIX.toml] and propagate to strategic docs.

== Status Legend

|===
| Status | Meaning

| Not Started
| Workstream has defined scope but no active implementation.

| In Progress
| Active implementation in current or recent sessions.

| Blocked
| Waiting on prerequisite design, dependency, or resourcing.

| Done
| Exit criteria met and verified by tests/docs.
|===

== Workstreams

|===
| ID | Workstream | Outcome | Status | Exit Criteria

| WS-0
| Truth Layer (Docs/Code/Roadmap Alignment)
| Capability claims are accurate and synchronized.
| In Progress → substantially complete (2026-06-24)
| Automated capability matrix generated in CI; strategic docs reference same source of truth. *2026-06-24:* TD register split into live tracker + closed archive, dashboard/roadmap/matrix reconciled, capability matrix validator green in CI, all surfaces point to the live register. Residual: full DESIGN_DOC_STATUS_INDEX table re-merge + 143-row legacy dashboard re-audit.

| WS-1
| Surface Coherence (One Production Path per Feature)
| No ambiguous API behavior for core features.
| In Progress
| Hybrid/vector/document/graph endpoints each have a single canonical production path. *2026-06-24:* legacy graph routes 308-deprecated (done); hybrid dead mock removed (TD-092); fusion seam converging all modalities onto one fuse-by-`oid` path (TD-136/137) and retiring the weak proto path (TD-143/146). Residual: finish the seam + remove the duplicate hybrid entry point.

| WS-2
| Reliability Layer (Panic + Failure Semantics)
| Panic risk is systematically reduced.
| Done (2026-06-24)
| Clippy `unwrap_used` policy enforced for production modules; panic hotspots triaged with burn-down tracking. *Met:* 0 unwrap + 0 expect in production (TD-007 resolved), `clippy::unwrap_used/expect_used` warns + CI panic-policy guard; root crate clippy-clean (TD-079).

| WS-3
| Capability Exposure Layer (Engine Parity)
| Implemented engines are consistently exposed across proto/server/embedded.
| In Progress
| TST/EventLog available in proto enums, API mappings, embedded API, and e2e tests. *2026-06-24:* TST/EventLog engines exposed (TD-009/010); gRPC v2 ProximaGraphService delivered (TD-122). Residual: Document/Entity v2 gRPC services (TD-122) + v1→v2 message migration (TD-123).

| WS-4
| Security Chain (Transport -> Identity -> AuthZ -> Audit)
| Security story is complete end-to-end.
| In Progress (2026-06-24)
| Client cert identity mapping implemented; documented security matrix; regression tests for mTLS auth flow. *2026-06-24:* mTLS code-complete incl. CN-to-user mapping (TD-006, Beta), RBAC, encryption-at-rest (TD-016), RLS engine hooks (SST/VIPER/NOVA/HELIX), per-repo RBAC (TD-134). Residual: security admin/status APIs + stronger online revocation (CRL/OCSP).

| WS-5
| Distributed Readiness (Feature-flagged Productization)
| Consensus is productized without destabilizing single-node.
| In Progress — Experimental (2026-06-24)
| Raft runtime path wired behind explicit feature gates with deterministic failover tests. *2026-06-24:* Raft internals, snapshots, shard planning, routing exist behind the `cluster` flag (TD-077, Experimental tier). Residual: real RPC transport + remote execution + deterministic failover suite.

| WS-6
| Developer Experience Layer
| Fast, low-friction onboarding with predictable behavior.
| Not Started
| 5-minute quickstart validated in CI; canonical examples for core use cases. *2026-06-24:* SDK breadth grew (Python/Rust/Go + JVM/Node embedded; TD-005/126 spec-driven codegen) but no CI-validated quickstart yet.

| WS-7
| PMF Layer (ICP-Focused Packaging)
| Clear differentiation for target customer segments.
| Not Started
| Two reference architectures with measurable outcomes and adoption feedback loop. *2026-06-24:* ibkrtrading dogfood evidence still open (TD-030).
|===

== Milestone Gates

|===
| Milestone | Target | Gate

| M1 Foundation Coherence
| 2026-Q2
| WS-0 and WS-1 exit criteria complete; no contradictory capability claims. *2026-06-24: largely met* — WS-0 substantially complete (tracking surfaces reconciled, validator in CI), WS-1 advanced (fusion-seam convergence in progress).

| M2 Production Robustness
| 2026-Q3
| WS-2, WS-3, WS-4 materially complete with test evidence. *2026-06-24: partially met* — WS-2 Done (panic-free + clippy-clean), WS-3 + WS-4 In Progress (v2 gRPC parity + security admin residual).

| M3 Scale + Adoption
| 2026-Q4
| WS-5, WS-6, WS-7 complete with reference implementations and user validation. *2026-06-24: early* — WS-5 Experimental (distributed transport residual, TD-077), WS-6/WS-7 not started.
|===

== Session Log

|===
| Date | Session Focus | Completed | Evidence | Next Session

| 2026-03-02
| Foundation kickoff: claim verification and API coherence
| Added persistent execution tracker; verified strategic-analysis claims vs codebase; canonicalized hybrid API routing (production vs experimental); corrected stale strategic claims; added capability matrix artifact + validator + CI job; added initial cross-doc drift checks; added experimental hybrid route tests.
| `docs/_internal/roadmap/BEST_IN_CLASS_EXECUTION_TRACKER.adoc`, `src/network/rest/v1/handlers.rs`, `src/network/rest/v1/hybrid.rs`, `docs/10-quality/proximadb-strategic-analysis.adoc`, `docs/_internal/roadmap/CAPABILITY_MATRIX.toml`, `scripts/validate_capability_matrix.py`, `.github/workflows/ci.yml`
| Create lightweight `AppState` test fixture to validate canonical `/api/v1/hybrid/search` route; unblock full test run after pre-existing `cargo test` compile errors in unrelated modules.

| 2026-03-02
| WS-1 canonical production route verification
| Added `AppState` test fixture with temp storage/config + canonical production route test for `POST /api/v1/hybrid/search` to assert handler-level `400` behavior (route exists) instead of `404`; re-ran matrix validator and `cargo check`.
| `src/network/rest/v1/handlers.rs`, `scripts/validate_capability_matrix.py`
| Resolve pre-existing lib-test compile blockers (`src/auth/sso/oidc.rs`, `src/auth/sso/saml.rs`, `src/operations/backup/mod.rs`, `src/catalog/mod.rs`) so targeted canonical route tests can run in CI/local.

| 2026-03-02
| Test baseline unblock + WS-1 hybrid route validation
| Fixed pre-existing lib-test compile blockers (OIDC/SAML Debug bounds, backup test return signatures, catalog assertion chain) and verified hybrid + vector route coherence tests now execute successfully.
| `src/auth/sso/oidc.rs`, `src/auth/sso/saml.rs`, `src/operations/backup/mod.rs`, `src/catalog/mod.rs`, `src/network/rest/v1/handlers.rs`, `src/network/rest/v1/hybrid.rs`
| Extend WS-1 canonical-route verification to document/graph path coherence and then move to WS-0 broader drift automation.

| 2026-03-02
| WS-1 document + graph canonical route coherence
| Added canonical production route tests for document and graph surfaces and validated all four core WS-1 paths now execute in tests (hybrid/vector/document/graph).
| `src/network/rest/v1/handlers.rs`, `cargo test test_document_index_canonical_production_route_returns_bad_request_not_not_found --lib`, `cargo test test_graph_shortest_path_canonical_production_route_returns_unprocessable_entity_not_not_found --lib`
| Decide and implement legacy graph endpoint deprecation/redirect policy (`/api/v1/graph/nodes`, `/api/v1/graph/edges`) to fully satisfy single-production-path exit criteria.

| 2026-03-02
| WS-1 legacy graph endpoint deprecation/redirect policy
| Implemented 308 redirect policy for legacy graph compatibility endpoints to canonical multi-graph routes with deprecation metadata headers (`Location`, `Deprecation`, `Sunset`, `X-ProximaDB-Canonical-Route`); added route tests for `/api/v1/graph/nodes` and `/api/v1/graph/edges`.
| `src/network/rest/v1/graph.rs`, `src/network/rest/v1/handlers.rs`, `cargo test test_graph_legacy_nodes_endpoint_redirects_to_canonical_multi_graph_route --lib`, `cargo test test_graph_legacy_edges_endpoint_redirects_to_canonical_multi_graph_route --lib`
| Extend WS-0 drift checks so strategic docs explicitly reflect legacy graph route deprecation and canonical route ownership.

| 2026-03-02
| WS-0 graph deprecation docs + drift enforcement
| Added explicit legacy-route migration examples with concrete old->new mappings and redirect headers in graph API docs; converted graph engine guide examples to canonical `/api/v1/graph/graphs/:graph_id/...` routes; extended capability-matrix validator with required/forbidden multi-document drift assertions to prevent regression to legacy route messaging.
| `docs/03-api-reference/graph.adoc`, `docs/06-internals/GRAPH_ENGINES_GUIDE.adoc`, `docs/_internal/roadmap/CAPABILITY_MATRIX.toml`, `scripts/validate_capability_matrix.py`
| Expand drift assertions to additional high-risk API surfaces (distributed graph engine messaging, security auth claims) and keep examples synchronized with router changes.

| 2026-03-02
| WS-0 drift expansion (distributed/security/roadmap messaging)
| Added drift assertions for distributed consensus roadmap wording, mTLS status claims, distributed graph engine messaging (PULSAR/QUASAR experimental status), and graph router comment synchronization; added capability matrix entry for distributed graph engine messaging.
| `scripts/validate_capability_matrix.py`, `docs/_internal/roadmap/CAPABILITY_MATRIX.toml`, `docs/_internal/roadmap/STRATEGIC_ROADMAP.adoc`, `docs/10-quality/proximadb-strategic-analysis.adoc`, `docs/06-internals/GRAPH_ENGINES_GUIDE.adoc`, `src/network/rest/v1/graph.rs`
| Start WS-2 panic policy rollout with concrete burn-down metrics and fail-fast guardrails in CI for production modules.

| 2026-03-03
| WS-2 panic-policy rollout kickoff (burn-down + staged guardrails)
| Replaced ad-hoc panic counter with deterministic policy script (`report`, `no-regression`, `module-guard`); established baseline artifact with module-level totals; added Makefile targets for local report/check/baseline refresh; wired staged CI jobs (stage-1 report, stage-2 no-regression, stage-3 critical-module guard with PR advisory mode).
| `scripts/count_panic_patterns.sh`, `docs/_internal/roadmap/PANIC_POLICY_BASELINE.json`, `Makefile`, `.github/workflows/ci.yml`
| Start burn-down in highest-risk modules (`src/network/rest`, `src/api_handlers`, `src/graph`, `src/query`) and ratchet stage-3 from PR advisory to PR-required after first reduction milestone.

| 2026-03-03
| WS-2 first burn-down milestone (REST/API zero + stage-3 required)
| Updated panic-policy scan scope to production-only by excluding `#[cfg(test)]` blocks; verified `src/network/rest` and `src/api_handlers` now burn down to zero production panic-pattern calls; refreshed baseline to 1,125 total; flipped stage-3 critical-module guard from PR-advisory to PR-required.
| `scripts/count_panic_patterns.sh`, `docs/_internal/roadmap/PANIC_POLICY_BASELINE.json`, `.github/workflows/ci.yml`, `docs/_internal/td007-panic-fixes-plan.md`
| Continue WS-2 burn-down on remaining hotspots (`src/storage`, `src/query`, `src/core`, `src/graph`) while keeping REST/API at zero.

| 2026-03-03
| WS-2 targeted burn-down (storage/query/core/graph pass)
| Reduced baseline from 1,125 to 808 by tightening production scanner scope (exclude test/benchmark/example files and parser-internal `self.expect(...)` false positives), replacing lock-poison `expect` calls in ORION engine/persistence with fallible lock handling, removing panic-prone sort comparisons in RAPTOR reader (`partial_cmp().expect(...)` -> `total_cmp`), and removing single-condition `unwrap()` sites in core search filter conversions.
| `scripts/count_panic_patterns.sh`, `src/graph/engines/orion/mod.rs`, `src/graph/engines/orion/persistence.rs`, `src/storage/engines/impls/raptor/consolidated_reader.rs`, `src/storage/encryption/key_manager.rs`, `src/core/search/mod.rs`, `docs/_internal/roadmap/PANIC_POLICY_BASELINE.json`, `cargo check --lib`
| Continue high-volume burn-down in storage/core while keeping `network_rest`/`api_handlers` pinned at zero under stage-3 CI guardrails.

| 2026-03-03
| WS-2 burn-down milestone (storage/query/core/graph target pass)
| Reduced baseline from 791 to 752 (−39) while keeping `network_rest`/`api_handlers` pinned at zero: converted HELIX metric registration from panic-on-init to optional/logged registration; removed panic paths in unified query decomposition (regex compile + clock handling); removed core validation unwraps via fallible regex initialization; drove `graph` module to zero by removing production `expect` defaults and doc-example unwrap patterns.
| `src/storage/engines/impls/helix/metrics.rs`, `src/query/unified/decomposition.rs`, `src/core/utils/validation.rs`, `src/graph/engines/pulsar/mod.rs`, `src/graph/query/pattern.rs`, `src/graph/engines/orion/algorithms/{pathfinding.rs,embeddings.rs,traits.rs}`, `docs/_internal/roadmap/PANIC_POLICY_BASELINE.json`, `docs/_internal/td007-panic-fixes-plan.md`, `make panic-policy-module-guard`, `cargo check --lib`
| Continue WS-2 on highest remaining production totals (`storage` 206, `core` 31), then close residual `query` tail to zero.

| 2026-03-03
| WS-2 burn-down milestone (storage expect-tail cleared + query zero)
| Cleared `storage` expect tail from 36 to 0 via lock-poison recovery, metadata downcast guards, and fallible Arrow builder handling; then closed `query` residual tail from 3 to 0. Refreshed panic-policy baseline to 713 total with critical surfaces pinned at zero (`network_rest`, `api_handlers`, `graph`, `query`).
| `src/storage/engines/impls/swift/id_index.rs`, `src/storage/engines/core/formats/columnar/serialization.rs`, `src/storage/engines/impls/raptor/writer.rs`, `src/storage/engines/impls/helix/hilbert_curve.rs`, `src/storage/{transaction_coordinator.rs,encryption/mod.rs,common/mmap_vectors.rs}`, `src/storage/document/indexes/{array_index.rs,path_index.rs}`, `src/storage/engines/core/formats/{columnar/nova_metadata.rs,proximablocks/swift_metadata.rs,proximablocks/sst_metadata.rs,columnar/parquet_write_engine/streaming_writer.rs}`, `src/storage/engines/impls/{sst/indexed_reader.rs,viper/indexed_reader.rs}`, `src/query/{execution/executor.rs,rl_planner/bandit.rs}`, `docs/_internal/roadmap/PANIC_POLICY_BASELINE.json`, `make panic-policy-module-guard`, `cargo check --lib`
| Continue WS-2 on unwrap-dominant tail (`storage` 170, `core` 31), then reduce `services` expect tail (3).

| 2026-03-12
| WS-3 TST exposure layer kickoff
| Added `TST` to the canonical storage-engine proto enum and checked-in Rust bindings; normalized embedded, REST v2, multiplex REST, Arrow Flight, and Rust SDK engine parsing to use canonical mappings; fixed stale REST v2 raw enum values; skipped default HNSW auto-provisioning for TST collections; added embedded parity coverage for `create_collection(..., "tst")`.
| `proto/proximadb/v1/vector_types.proto`, `crates/foundation/proximadb-proto/src/proto/proximadb.v1.rs`, `src/core/{conversions.rs,utils/validation.rs,storage/engines.rs,compact_enums.rs}`, `src/embedded/mod.rs`, `src/network/{rest/v2/collections.rs,multiplex/handlers/rest.rs,arrow_ipc/service.rs}`, `src/services/collection/manager.rs`, `src/storage/{background_flush_context.rs,engine_capabilities.rs,traits.rs}`, `src/storage/common/{compaction_utils.rs,engine_config.rs,flush_handler_trait.rs}`, `src/storage/engines/factory.rs`, `src/storage/persistence/write_ahead_log/flush_coordinator.rs`, `src/storage/trait_components/capabilities.rs`, `clients/rust/src/{collection.rs,error.rs}`, `tests/embedded_engine_parity_test.rs`
| Finish EventLog exposure semantics and replace temporary TST shared-compaction fallbacks with dedicated handlers/configs, then widen verification to targeted Rust tests and `make test-rust`.

| 2026-03-14
| WS-1/WS-0 foundational multi-model execution hardening kickoff
| Added a persistent active execution plan for foundational multi-model correctness; started canonical-path fixes for vector predicate propagation and document query execution completeness after fresh architecture/code review identified public-path semantic gaps.
| `docs/_internal/roadmap/implementation/active/FOUNDATIONAL_MULTIMODEL_EXECUTION_2026.adoc`, `src/query/facade/*`, `src/services/operations/vectors.rs`, `src/storage/document/*`, `src/api_handlers/unified_handlers.rs`
| Complete F1/F2 implementation, add regression coverage, then update capability claims and strategic docs from verified code state.

| 2026-03-14
| WS-1/WS-3 distributed execution foundation
| Registered the distributed strategy in the live unified facade, replaced empty local distributed subquery execution with real unified-executor dispatch, and added a conservative federated-extension translation layer so forced distributed queries now execute actual local vector/document/graph/observability components.
| `src/query/facade/strategies/distributed.rs`, `src/query/distributed/coordinator.rs`, `src/network/multi_server.rs`, `docs/_internal/roadmap/implementation/active/FOUNDATIONAL_MULTIMODEL_EXECUTION_2026.adoc`
| Finish compile/test verification, then move to correlated federated join execution and remote distributed semantics.

| 2026-03-14
| WS-1 function-backed correlated federated joins
| Replaced the federated nested-loop execution stub with live function-backed `LATERAL` execution, added per-row correlated vector extraction from serialized outer documents, and updated federated planning to preserve SQL source order plus correlation metadata for `LATERAL` plans.
| `src/query/federated/{execution/mod.rs,optimizer/mod.rs,mod.rs}`, `docs/_internal/roadmap/implementation/active/FOUNDATIONAL_MULTIMODEL_EXECUTION_2026.adoc`
| Finish targeted cargo verification, then choose between widening correlated-source support or closing the generic relational scan backend gap.

| 2026-03-14
| WS-3 remote distributed execution contract
| Replaced the remote distributed executor’s simulated empty-result behavior with a registered handler contract, made remote failures propagate instead of being silently dropped, and added focused remote executor regressions. This makes remote execution wiring honest before true transport is introduced.
| `src/query/distributed/{remote.rs,coordinator.rs,mod.rs}`, `docs/_internal/roadmap/implementation/active/FOUNDATIONAL_MULTIMODEL_EXECUTION_2026.adoc`
| Finish targeted cargo verification, then choose between the relational scan backend gap and real node-to-node transport for the next foundational execution slice.

| 2026-06-24
| Tracking-surface reconciliation (WS-0) + workstream re-score
| Reconciled every tech-debt / feature / vision tracking surface against `origin/develop` HEAD + the 2026-06-04/06-19 pivots: split the TD register into a live tracker + closed archive, overhauled MASTER_FEATURE_DASHBOARD, fully reconciled CAPABILITY_MATRIX.toml (validator green) + STRATEGIC_ROADMAP, rebuilt the DESIGN_DOC_STATUS_INDEX addendum, refreshed SYSTEM_MAP, and re-scored WS-0..WS-7 + M1/M2/M3 here. Status flips verified vs develop: TD-129/134/133/135 Resolved; TD-132 Partial.
| `docs/10-quality/TECHNICAL_DEBT.adoc`, `docs/_archive/quality/2026-06/TECHNICAL_DEBT_CLOSED_2026_06_24.adoc`, `docs/_internal/roadmap/{MASTER_FEATURE_DASHBOARD,STRATEGIC_ROADMAP,CAPABILITY_MATRIX.toml,RECONCILIATION_2026_06_24}.adoc`, `docs/12-design/{SYSTEM_MAP_2026_05_30,DESIGN_DOC_STATUS_INDEX_2026_05_26}.adoc`, `scripts/validate_capability_matrix.py` (green)
| Re-merge the DESIGN_DOC_STATUS_INDEX two tables; re-audit the 143-row frozen dashboard baseline against code; advance WS-1 fusion-seam convergence (TD-136/137).
|===

== Current Priority Queue

_Reconciled 2026-06-24._

1. *WS-1:* land the cross-modal fusion seam (TD-136 Fuser core + TD-137 GraphExpander/REST v2 fusion-search), then retire the duplicate hybrid/proto paths (TD-143/146) so each modality has exactly one production path.
2. *WS-3:* close gRPC v2 parity — Document/Entity v2 services (TD-122) — and advance the v1→v2 transport-agnostic-domain migration (TD-123) ahead of the 2026-06-30 v1 sunset (TD-121).
3. *WS-4:* finish the security admin/status plane + stronger online revocation (CRL/OCSP) to graduate mTLS from Beta (TD-006).
4. *WS-7:* publish the ibkrtrading dogfood validation evidence (TD-030).
5. *Co-design:* wire cost-based multi-engine dispatch (TD-115) and the per-query I/O trace substrate so routing/caching optimize the measured dominant cost term.
