= Compression Guide
:toc: left
:toclevels: 2

**SDK-driven compression for optimal storage efficiency.**

== Overview

ProximaDB compression is **100% SDK-controlled** - no server defaults. Configure compression per collection through the client SDK.

== Quick Start

=== Basic Compression
[source,python]
----
from proximadb import ProximaDBClient
from proximadb.models import CompressionConfig

client = ProximaDBClient("http://localhost:5678")

# Create collection with compression
collection = client.create_collection(
    name="compressed",
    dimension=1536,
    engine="viper",
    compression=CompressionConfig(
        algorithm="zstd",
        level=6
    )
)
----

== Unified Compression Architecture

ProximaDB features a **unified compression module** (`src/core/compression/mod.rs`) that provides consistent compression across all storage engines and contexts.

=== Supported Algorithms (13 Total)
[cols="1,1,1,1,1", options="header"]
|===
| Algorithm | Ratio | Speed | Context Support | Use Case

| **ZSTD**
| 2-10x
| Medium
| All contexts
| Balanced (default)

| **LZ4**
| 1.5-3x
| Fast
| All contexts
| Low latency

| **Snappy**
| 1.5-2x
| Very Fast
| All contexts + Parquet
| Real-time

| **Gzip**
| 2-8x
| Medium
| All contexts + Parquet
| Web compatibility

| **Brotli**
| 3-12x
| Slow
| All contexts + Parquet
| Maximum compression

| **Bzip2**
| 4-15x
| Very Slow
| SST + Vector only
| Archive storage

| **Deflate**
| 2-8x
| Medium
| SST + Vector only
| Standard compression

| **XZ/LZMA**
| 5-20x
| Very Slow
| SST + Vector only
| Archive storage

| **Zlib**
| 2-8x
| Medium
| SST + Vector only
| Legacy compatibility

| **LZO**
| 1.5-2.5x
| Very Fast
| All contexts + Parquet
| High-speed requirements

| **LZ4HC**
| 2-4x
| Medium
| SST + Vector only
| Balanced LZ4 variant

| **None**
| 1x
| Fastest
| All contexts
| Maximum speed
|===

=== Compression Contexts

The unified module supports three distinct compression contexts:

1. **SST Block Context**: Custom block compression with format markers
2. **Vector Serialization Context**: Headers and checksums for vector data
3. **Parquet Column Context**: Arrow WriterProperties integration (7 algorithms supported)

=== Compression Levels

**Level-aware algorithms:**
* **ZSTD**: 1-22 (default: 6) - Write-optimized (1-3), Balanced (4-9), Storage-optimized (10+)
* **Gzip**: 1-9 (default: 6) - Speed vs compression tradeoff
* **Brotli**: 1-11 (default: 4) - Quality vs speed balance
* **Bzip2**: 1-9 (default: 6) - Block size 100K-900K
* **Deflate**: 1-9 (default: 6) - Same as Gzip without wrapper
* **XZ/LZMA**: 1-9 (default: 6) - Extreme compression at higher levels
* **Zlib**: 1-9 (default: 6) - Standard deflate with checksum

**Fixed-level algorithms:**
* **LZ4/LZ4HC/LZO**: Single compression level optimized for speed
* **Snappy**: Single level optimized for speed/ratio balance

== Engine-Specific Features

=== SST Engine (Row-Based)
[source,python]
----
# SST with block compression
config = CompressionConfig(
    algorithm="zstd",
    level=6,
    block_size_mb=16,      # SST-specific
    dynamic_block_sizing=True
)
----

**Features**:
* Block-level compression
* Dynamic block sizing
* Bloom filter optimization
* Fast decompression

=== VIPER Engine (Columnar)
[source,python]
----
# VIPER with quantization
config = CompressionConfig(
    algorithm="lz4",
    level=1,
    enable_quantization=True,     # VIPER-specific
    quantization_type="int8",     # or "pq8", "pq4"
    normalization_method="mean"
)
----

**Features**:
* Column compression
* Dual storage (FP32 + quantized)
* 100% vector fidelity
* Two-stage search

== Quantization (VIPER Only)

=== Types
[cols="1,1,1,1", options="header"]
|===
| Type | Compression | Accuracy | Speed

| **INT8**
| 4:1
| High
| Fast

| **PQ8**
| 8-32:1
| Medium
| Medium

| **PQ4**
| 16-64:1
| Lower
| Slow
|===

=== Two-Stage Search
1. **Stage 1**: Search quantized vectors (fast)
2. **Stage 2**: Rerank with FP32 (accurate)
3. **Result**: 2x speed, 100% accuracy

== Advanced Configuration

=== Adaptive Compression
[source,python]
----
config = CompressionConfig(
    algorithm="zstd",
    level=6,
    adaptive=True,         # Auto-adjust level
    min_ratio=2.0         # Minimum compression ratio
)
----

=== Search Optimization
[source,python]
----
from proximadb.models import SearchOptimization

# Compression-aware search
optimization = SearchOptimization(
    enable_two_stage=True,
    prefer_compressed_search=True,
    decompression_budget_ms=100,
    use_decompression_cache=True
)

results = client.search_vectors(
    "compressed",
    query_vector,
    search_optimization=optimization
)
----

== Performance Impact

[cols="2,1,1,1", options="header"]
|===
| Operation | No Compression | LZ4 | ZSTD-6

| **Insert Speed**
| 10K vec/s
| 8.5K vec/s
| 6K vec/s

| **Search Latency**
| 0.5ms
| 0.7ms
| 1.2ms

| **Storage Size**
| 100GB
| 50GB
| 30GB

| **Cache Hit Rate**
| N/A
| 75%
| 75%
|===

== Best Practices

=== Choosing Compression
```
Write-Heavy → LZ4 or Snappy
Storage-Optimized → ZSTD level 9+
Analytics → VIPER + Quantization
Real-time → No compression or Snappy
```

=== Configuration Tips
1. **Start with ZSTD-6** for balanced performance
2. **Enable quantization** for VIPER analytics
3. **Use adaptive compression** for mixed workloads
4. **Monitor compression ratios** via metrics
5. **Size decompression cache** based on working set

== Monitoring

=== Compression Metrics
```python
# Get compression stats
stats = client.get_collection_stats("compressed")
print(f"Compression ratio: {stats['compression_ratio']:.2f}x")
print(f"Compressed size: {stats['compressed_size_mb']:.1f} MB")
print(f"Original size: {stats['original_size_mb']:.1f} MB")
```

=== Cache Performance
```python
# Monitor decompression cache
cache_stats = client.get_cache_stats()
print(f"Cache hit rate: {cache_stats['hit_rate']:.1%}")
print(f"Cache size: {cache_stats['size_mb']:.1f} MB")
```

== Migration Guide

=== Enable Compression
```python
# Update existing collection
client.update_collection(
    "my_collection",
    compression=CompressionConfig(
        algorithm="zstd",
        level=6
    )
)
```

=== Disable Compression
```python
# Remove compression
client.update_collection(
    "my_collection",
    compression=CompressionConfig(
        algorithm="none"
    )
)
```

---
**Next**: link:performance_reference.adoc[Performance] | link:developer/architecture.adoc[Architecture]