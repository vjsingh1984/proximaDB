= Technical Reference
:toc: left
:toclevels: 2

Comprehensive technical documentation for ProximaDB.

== API Reference

=== REST API

==== Collections

[source,http]
----
# Create collection
POST /collections
{
  "name": "my_collection",
  "dimension": 384,
  "engine": "viper"
}

# Get collection
GET /collections/{name}

# Delete collection
DELETE /collections/{name}

# List collections
GET /collections
----

==== Vectors

[source,http]
----
# Insert vectors
POST /collections/{name}/vectors
{
  "vectors": [
    {
      "id": "vec1",
      "vector": [0.1, 0.2, ...],
      "metadata": {"key": "value"}
    }
  ]
}

# Search vectors
POST /collections/{name}/search
{
  "vector": [0.1, 0.2, ...],
  "top_k": 10,
  "metadata_filter": {"key": "value"}
}

# Get vector
GET /collections/{name}/vectors/{id}

# Delete vector
DELETE /collections/{name}/vectors/{id}
----

=== gRPC API

[source,proto]
----
service ProximaDB {
  // Collection operations
  rpc CreateCollection(CreateCollectionRequest) returns (Collection);
  rpc GetCollection(GetCollectionRequest) returns (Collection);
  rpc DeleteCollection(DeleteCollectionRequest) returns (Empty);
  
  // Vector operations
  rpc InsertVectors(VectorBatchRequest) returns (VectorBatchResponse);
  rpc SearchVectors(VectorSearchRequest) returns (VectorSearchResponse);
  rpc GetVector(GetVectorRequest) returns (VectorRecord);
}
----

=== SQL Interface

[source,sql]
----
-- Create collection
CREATE COLLECTION my_collection (dimension=384, engine='viper');

-- Insert vectors
INSERT INTO my_collection (id, vector, metadata) 
VALUES ('vec1', [0.1, 0.2, ...], '{"key": "value"}');

-- Search vectors
SELECT id, metadata, similarity(vector, [0.1, 0.2, ...]) as score
FROM my_collection
WHERE metadata->>'key' = 'value'
ORDER BY score DESC
LIMIT 10;

-- Delete vectors
DELETE FROM my_collection WHERE id = 'vec1';
----

== Configuration Reference

=== Server Configuration

[source,toml]
----
[server]
rest_port = 5678                    # REST API port
grpc_port = 5679                    # gRPC API port
worker_threads = 16                 # Number of worker threads
max_connections = 1000              # Maximum concurrent connections
request_timeout_secs = 30           # Request timeout
log_level = "info"                  # Log level: debug/info/warn/error
----

=== Storage Configuration

[source,toml]
----
[storage]
data_dir = "/var/lib/proximadb"     # Data directory
engine = "dual"                     # Engine: sst/viper/dual

[storage.write_buffer]
memory_flush_size_bytes = 67108864  # 64MB per collection
write_buffer_size_mb = 2048         # 2GB global limit
serialization_strategy = "bincode"  # bincode/proto/avro

[storage.sst]
block_size = 16384                  # 16KB blocks
compaction_interval_secs = 300      # 5 minutes
max_files_before_compaction = 10
bloom_filter_bits_per_key = 10      # 1% FPR

[storage.viper]
row_group_size = 50000
compression_codec = "zstd"
compression_level = 6
enable_statistics = true
----

=== Cloud Storage

[source,toml]
----
# AWS S3
[storage.s3]
region = "us-east-1"
bucket = "proximadb-data"
use_iam_role = true
endpoint = ""  # Optional custom endpoint

# Azure Blob Storage
[storage.azure]
account_name = "proximadbstorage"
container = "data"
use_managed_identity = true

# Google Cloud Storage
[storage.gcs]
project_id = "my-project"
bucket = "proximadb-data"
use_workload_identity = true
----

== Distance Metrics

Supported distance metrics:

[cols="2,3,2"]
|===
|Metric |Description |Range

|cosine
|Cosine similarity
|[0, 2]

|euclidean
|L2 distance
|[0, ∞)

|dot_product
|Inner product
|(-∞, ∞)

|manhattan
|L1 distance
|[0, ∞)

|hamming
|Bit differences
|[0, n]

|jaccard
|Set similarity
|[0, 1]
|===

== Index Types

=== HNSW (Hierarchical Navigable Small World)
* Best for: High recall requirements
* Memory: O(M × N) where M is connections
* Build time: O(N log N)
* Search time: O(log N)

=== IVF (Inverted File)
* Best for: Large datasets
* Memory: O(N) + centroids
* Build time: O(N × K) clustering
* Search time: O(nprobe × N/K)

=== LSH (Locality Sensitive Hashing)
* Best for: Approximate matching
* Memory: O(L × N) where L is tables
* Build time: O(L × N)
* Search time: O(L)

=== Annoy (Approximate Nearest Neighbors)
* Best for: Memory-constrained
* Memory: O(N × n_trees)
* Build time: O(N × n_trees × log N)
* Search time: O(n_trees × log N)

== Error Codes

[cols="2,3,2"]
|===
|Code |Description |Action

|COLLECTION_NOT_FOUND
|Collection does not exist
|Create collection first

|DIMENSION_MISMATCH
|Vector dimension incorrect
|Check collection dimension

|INVALID_METADATA
|Metadata parsing failed
|Validate JSON format

|STORAGE_ERROR
|Storage operation failed
|Check disk space/permissions

|TIMEOUT
|Operation timed out
|Retry with backoff
|===

---

For implementation examples, see link:appendix_implementation_details.adoc[Implementation Details].