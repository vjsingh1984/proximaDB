= Documentation Alignment Plan (Q4 2025)
:toc: left
:toclevels: 2
:icons: font

[.lead]
Concise plan to align documentation with code, standardize diagrams, and remove duplication.

== Summary
- Align feature claims to current code (paths, capabilities, feature flags).
- Standardize Mermaid diagrams for clarity and consistency.
- Deduplicate repeated explanations; cross-link canonical tech docs.
- Refresh README tone to be accurate yet engaging.

== Code-Truth Alignment (Examples)
- Advanced optimizer lives at `src/core/search/integrated_search_optimization.rs`.
- Storage engine trait at `src/storage/traits.rs` (not under `engines/`).
- Python SDK tests reside in `clients/python/tests/`.
- GPU backends are feature-gated; document flags instead of implying default availability.

== Diagram Standardization
- Use `[source,mermaid]` blocks with `%%{init: {"theme": "default"}}%%`.
- Prefer simple flowcharts/graphs; avoid over-styled palettes.
- Label nodes with component + brief role (e.g., “SST – hybrid blocks”).

== Deduplication Strategy
- Replace repeated engine overviews with links to per-engine docs:
  `src/storage/engines/impls/*/README.adoc` and `docs/07-architecture/storage-layer.adoc`.
- Keep roadmap pages status-focused; link to technical deep dives.

== README Refinements
- Remove “Production Ready” badge; mark benchmarks as internal and hardware-dependent.
- Present cloud storage and GPU as optional features.

== Quick Wins (next PRs)
- Fix references in `docs/09-roadmap/implementation/Q1_2025_IMPLEMENTATION.adoc` (done).
- Update `docs/09-roadmap/README.adoc` key achievements (done).
- Add “Mermaid style” snippet to authoring checklist.
