= Performance Reference
:toc: left
:toclevels: 2

**Performance benchmarks and optimization guide.**

== Benchmarks

**Test Hardware**: AMD Ryzen 9 7950X, 32 cores, 157GB RAM, AVX-512

[cols="2,1,1,1", options="header"]
|===
| Operation | gRPC | REST | Speedup

| **Insert Throughput**
| 20,575 vec/s
| 6,035 vec/s
| 3.4x

| **Search Latency**
| 1.5ms avg
| 2.6ms avg
| 1.7x

| **Concurrent QPS**
| 1,770 QPS
| 842 QPS
| 2.1x

| **Storage Compression**
| 50-80%
| 50-80%
| Same
|===

== Engine Performance

=== SST Engine (Row-Based)
* **Best for**: Write-heavy, OLTP workloads
* **Insert**: 18K vec/s
* **Search**: 1.8ms avg
* **Metrics**: Manhattan fastest (6.75ms)

=== VIPER Engine (Columnar)
* **Best for**: Analytics, complex queries
* **Insert**: 20K vec/s
* **Search**: 1.5ms avg
* **Metrics**: Euclidean fastest (6.82ms)

== Optimization Guide

=== Protocol Selection
```
High Throughput → gRPC (3x faster)
Wide Compatibility → REST
Bulk Operations → gRPC + batch 500
Single Operations → Either protocol
```

=== Batch Sizes
* **gRPC**: 500 vectors/batch
* **REST**: 100 vectors/batch
* **SQL**: 50 queries/batch

=== Memory Configuration
[source,toml]
----
[performance]
vector_cache_mb = 512       # Vector cache
metadata_cache_mb = 128     # Metadata cache
bloom_filter_mb = 64        # 95% scan reduction
connection_pool_size = 100  # Connections
query_cache_size_mb = 256   # Query results
----

=== Compression Settings
[cols="1,1,1,1", options="header"]
|===
| Algorithm | Speed | Ratio | Use Case

| **ZSTD**
| Medium
| 3-5x
| Balanced (default)

| **LZ4**
| Fast
| 2-3x
| Low latency

| **Snappy**
| Very Fast
| 2x
| Real-time

| **None**
| Fastest
| 1x
| Maximum speed
|===

== Hardware Recommendations

=== CPU
* **Minimum**: 4 cores, 8GB RAM
* **Recommended**: 8+ cores, 32GB RAM
* **Enterprise**: 16+ cores, 64GB RAM

=== Storage
* **Type**: NVMe SSD (10x faster than HDD)
* **Space**: 10GB minimum + data
* **IOPS**: 10K+ recommended

=== GPU (Optional)
* **CUDA**: NVIDIA GPUs
* **ROCm**: AMD GPUs
* **MPS**: Apple Silicon
* **Benefit**: 2-5x distance computation speedup

== Performance Features

=== Bloom Filters
* **95% scan reduction** for metadata queries
* **Zero false negatives**
* **64MB handles millions of vectors**
* **Automatic activation**

=== Hardware Acceleration
* **Auto-detection** at startup
* **SIMD**: AVX-512, AVX2, NEON
* **GPU**: Distance computation offload
* **Zero-copy**: Proto-first architecture

=== Two-Stage Search
* **Stage 1**: Quantized candidates (fast)
* **Stage 2**: FP32 reranking (accurate)
* **Benefit**: 2x speed, 100% accuracy

== Monitoring

=== Built-in Metrics
```
GET /metrics  # Prometheus format
```

Key metrics:
* `insert_rate_per_sec`
* `search_latency_p50/p95/p99`
* `cache_hit_rate`
* `compression_ratio`

=== Performance Testing
```python
# Benchmark script
from proximadb import ProximaDBClient
import time

client = ProximaDBClient("http://localhost:5678")
vectors = generate_vectors(10000, 384)

start = time.time()
client.insert_vectors("test", vectors, batch_size=500)
elapsed = time.time() - start

print(f"Insert rate: {10000/elapsed:.0f} vec/s")
```

== Best Practices

1. **Use gRPC** for production workloads
2. **Enable compression** for storage savings
3. **Batch operations** for throughput
4. **Monitor metrics** continuously
5. **Size caches** based on working set

---
**Next**: link:compression_guide.adoc[Compression Guide] | link:developer/architecture.adoc[Architecture]