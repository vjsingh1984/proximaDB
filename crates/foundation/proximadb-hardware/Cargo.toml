[package]
name = "proximadb-hardware"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Hardware capability detection for ProximaDB — SIMD, CPU, memory"

[lib]
name = "proximadb_hardware"
path = "src/lib.rs"

[dependencies]
num_cpus = "1.16"
sysinfo = "0.30"
