[package]
name = "proximadb-functions"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Canonical cross-engine function contract + registry (scalar/aggregate/table) over ProximaValue — bound natively by the Volcano and DataFusion engines"

[lib]
name = "proximadb_functions"
path = "src/lib.rs"

[dependencies]
proximadb-data-model = { workspace = true }
proximadb-relational-types = { workspace = true }
dashmap = { workspace = true }
