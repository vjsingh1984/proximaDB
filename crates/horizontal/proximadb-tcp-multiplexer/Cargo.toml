[package]
name = "proximadb-tcp-multiplexer"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "TCP port multiplexer (serves REST + gRPC + Arrow Flight from one port via protocol sniffing) — extracted from root network/multiplex (TD-DECOMP-46)"

[lib]
name = "proximadb_tcp_multiplexer"
path = "src/lib.rs"

[dependencies]
tokio = { workspace = true }
tracing = { workspace = true }
