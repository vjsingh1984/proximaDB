[package]
name = "proximadb-multimodel-codec"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Multimodel Arrow schema codecs (document/graph/metric/log/trace/relational schema generation from catalog types) — extracted from root network/arrow_ipc (TD-DECOMP-68)"

[lib]
name = "proximadb_multimodel_codec"
path = "src/lib.rs"

[dependencies]
arrow-schema = { workspace = true, default-features = false }
proximadb-catalog-schema = { workspace = true }

[dev-dependencies]
proximadb-data-model = { workspace = true }
