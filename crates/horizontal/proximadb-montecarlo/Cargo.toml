[package]
name = "proximadb-montecarlo"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Monte-Carlo option-pricing kernel (Black-Scholes + parallel batch path simulation) — extracted from root compute (TD-DECOMP-41)"

[lib]
name = "proximadb_montecarlo"
path = "src/lib.rs"

[dependencies]
rayon = { workspace = true }
rand = { workspace = true }
