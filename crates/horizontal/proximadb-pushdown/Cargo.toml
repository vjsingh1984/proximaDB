[package]
name = "proximadb-pushdown"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Pushdown protocol types (predicate/projection/aggregate/vector-search/graph-traversal pushdown request+response) — extracted from root connectors (TD-DECOMP-47)"

[lib]
name = "proximadb_pushdown"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
hex = "0.4"
