[package]
name = "proximadb-geo-spatial-index"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Geospatial secondary index (GeoIndex — geohash-based spatial search) — extracted from root index/geo (TD-DECOMP-62)"

[lib]
name = "proximadb_geo_spatial_index"
path = "src/lib.rs"

[dependencies]
proximadb-geo-types = { workspace = true }
proximadb-geohash = { workspace = true }
proximadb-geo-queries = { workspace = true }
