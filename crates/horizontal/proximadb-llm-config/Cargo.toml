[package]
name = "proximadb-llm-config"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "LLM/RAG configuration types (embedding provider, semantic-cache settings) — extracted from root llm (TD-DECOMP-52)"

[lib]
name = "proximadb_llm_config"
path = "src/lib.rs"

[dependencies]
proximadb-config = { workspace = true }
