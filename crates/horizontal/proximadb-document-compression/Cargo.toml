[package]
name = "proximadb-document-compression"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Document field compression (DocumentCompressor — per-field codec selection) — extracted from root storage/document (TD-DECOMP-56)"

[lib]
name = "proximadb_document_compression"
path = "src/lib.rs"

[dependencies]
anyhow = { workspace = true }
lz4_flex = { workspace = true }
