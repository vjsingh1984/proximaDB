[package]
name = "proximadb"
version = "0.2.2"
edition = "2024"
autobenches = false
authors = ["Vijaykumar Singh <singhvjd@gmail.com>"]
description = "ProximaDB - Cloud-native vector database engineered for AI-first applications"
license = "Apache-2.0"
repository = "https://github.com/anvai-labs/proximaDB"
homepage = "https://github.com/anvai-labs/proximaDB"
keywords = ["vector", "database", "ai", "ml", "proximity", "similarity-search"]
categories = ["database-implementations", "science"]
rust-version = "1.88"

[lints]
workspace = true

[workspace]
resolver = "2"
members = [
    "apps/proximadb-ann-bench",
    "apps/proximadb-bench",
    "apps/proximadb-migrate",
    "apps/proximadb-server",
    "apps/proximadb-vectordb-compare",
    ".",
    "clients/rust",
    "crates/foundation/proximadb-cache",
    "crates/foundation/proximadb-compression-types",
    "crates/foundation/proximadb-concurrent",
    "crates/foundation/proximadb-config",
    "crates/foundation/proximadb-data-model",
    "crates/foundation/proximadb-graph-model",
    "crates/foundation/proximadb-graph-engine-traits",
    "crates/foundation/proximadb-distance-types",
    "crates/foundation/proximadb-encoding-types",
    "crates/foundation/proximadb-filter-expression",
    "crates/foundation/proximadb-hardware",
    "crates/foundation/proximadb-hardware-caps",
    "crates/foundation/proximadb-distance-kernel",
    "crates/foundation/proximadb-clustering-kernel",
    "crates/foundation/proximadb-memory-pool",
    "crates/foundation/proximadb-kernel",
    "crates/foundation/proximadb-interner",
    "crates/foundation/proximadb-bloom",
    "crates/foundation/proximadb-pipeline-operator",
    "crates/foundation/proximadb-proto",
    "crates/foundation/proximadb-quantization-types",
    "crates/foundation/proximadb-quantization-model",
    "crates/foundation/proximadb-index-types",
    "crates/foundation/proximadb-index-storage",
    "crates/foundation/proximadb-index-traits",
    "crates/foundation/proximadb-records",
    "crates/foundation/proximadb-search-types",
    "crates/foundation/proximadb-tenant",
    "crates/foundation/proximadb-relational-types",
    "crates/foundation/proximadb-functions",
    "crates/control/proximadb-abac",
    "crates/control/proximadb-catalog",
    "crates/control/proximadb-metrics",
    "crates/horizontal/proximadb-codec",
    "crates/horizontal/proximadb-compression",
    "crates/horizontal/proximadb-embedded-common",
    "crates/binding/proximadb-embedded",
    "crates/binding/proximadb-catalog-introspection",
    "crates/horizontal/proximadb-recall-bench",
    "crates/horizontal/proximadb-runtime-common",
    "crates/horizontal/proximadb-serialization",
    "crates/horizontal/proximadb-security",
    "crates/horizontal/proximadb-tls",
    "crates/horizontal/proximadb-telemetry",
    "crates/platform/proximadb-api",
    "crates/platform/proximadb-runtime",
    "crates/platform/proximadb-admin-ui",
    "crates/platform/proximadb-mcp",
    "crates/modalities/proximadb-graph",
    "crates/modalities/proximadb-vector",
    "crates/modalities/proximadb-quantization-kernel",
    "crates/modalities/proximadb-document",
    "crates/modalities/proximadb-orion-engine",
    "crates/modalities/proximadb-observability",
    "crates/modalities/proximadb-observability-engine",
    "crates/modalities/proximadb-embedding",
    "crates/horizontal/proximadb-queue",
    "crates/modalities/proximadb-ledger",
    "crates/modalities/proximadb-rank-core",
    "crates/modalities/proximadb-rank-features",
    "crates/modalities/proximadb-rank-expr",
    "crates/modalities/proximadb-rank-profile",
    "crates/modalities/proximadb-rank-onnx",
    "crates/storage/proximadb-engine-core",
    "crates/storage/proximadb-raptor-common",
    "crates/storage/proximadb-raptor-engine",
    "crates/storage/proximadb-storage-common",
    "crates/storage/proximadb-storage-ports",
    "crates/storage/proximadb-cks-local",
    "crates/storage/proximadb-cks-objectstore",
    "crates/storage/proximadb-storage-traits",
    "crates/storage/proximadb-storage-encryption",
    "crates/storage/proximadb-block-format",
    "crates/storage/proximadb-object-store",
    "crates/storage/proximadb-iceberg-engine",
    "crates/storage/proximadb-storage-kv",
    "crates/storage/proximadb-storage-operations",
    "crates/storage/proximadb-storage-optimization",
    "crates/storage/proximadb-storage-filesystem-types",
    "crates/storage/proximadb-storage-strategy",
    "crates/storage/proximadb-storage-tenant",
    "crates/storage/proximadb-storage-transaction",
    "crates/query/proximadb-document-query",
    "crates/query/proximadb-graph-arrow",
    "crates/query/proximadb-graph-query",
    "crates/query/proximadb-graph-subset",
    "crates/query/proximadb-multimodel-query",
    "crates/query/proximadb-multimodel-plan",
    "crates/query/proximadb-execution-contracts",
    "crates/query/proximadb-relational-algebra",
    "crates/query/proximadb-relational-reader",
    "crates/query/proximadb-relational-planner",
    "crates/query/proximadb-relational-executor",
    "crates/query/proximadb-relational-frontend",
    "crates/query/proximadb-relational-engine",
    "crates/contracts/quantization",
    "crates/query/proximadb-observability-query",
    "crates/query/proximadb-query",
    "crates/query/proximadb-query-capability",
    "crates/query/proximadb-query-clauses",
    "crates/query/proximadb-query-fusion",
    "crates/query/proximadb-query-filter",
    "crates/query/proximadb-uql",
    "crates/query/proximadb-vector-query",
    "crates/binding/proximadb-spark-jni",
    "crates/binding/proximadb-mongodb-parser",
]

[workspace.package]
version = "0.2.0"
edition = "2024"
rust-version = "1.88"
license = "Apache-2.0"
authors = ["Vijaykumar Singh <singhvjd@gmail.com>"]
repository = "https://github.com/anvai-labs/proximaDB"
homepage = "https://github.com/anvai-labs/proximaDB"

[workspace.lints.rust]
# Edition-2024 migration for x86_64 SIMD: many `#[target_feature] unsafe fn`s call
# intrinsics / memory ops directly. Keep the pre-2024 "unsafe fn body is an unsafe
# context" behavior workspace-wide; `unused_unsafe` covers the resulting redundancy
# of any explicit `unsafe {}` blocks remaining in those fns. (NEON/AVX SIMD modules.)
unsafe_op_in_unsafe_fn = "allow"
unused_unsafe = "allow"

[workspace.lints.clippy]
# x86_64 SIMD idiom (only ever compiled on x86_64 CI, so it accrued unlinted):
# internal `#[target_feature]` unsafe primitives whose safety contract is uniformly
# "caller ensures the CPU supports the feature", and explicit chunk-index loops that
# read clearer than iterator adapters. Sloppy lints (needless_return / assign_op) are
# fixed in-code, not allowed.
missing_safety_doc = "allow"
needless_range_loop = "allow"

[workspace.dependencies]
anyhow = "1.0"
# R-5b.1.3 workaround: an auto-refactor commit added
# crates/query/proximadb-relational-frontend referencing
# `sqlparser = { workspace = true }` without first declaring the
# dep at the workspace level. Same pin as the root `[dependencies]`
# entry.
sqlparser = { version = "0.59", features = ["visitor"] }
arrow = { version = "58", features = ["prettyprint"] }
arrow-array = { version = "58", default-features = false }
bincode = "1.3"
arrow-flight = { version = "58", features = ["flight-sql-experimental"] }
arrow-schema = { version = "58", default-features = false }
parquet = { version = "58", default-features = false, features = ["arrow", "object_store", "zstd", "lz4", "snap", "brotli", "flate2", "flate2-rust_backened"] }
object_store = "0.13"
async-trait = "0.1"
axum = { version = "0.8", features = ["json", "ws"] }
chrono = { version = "0.4.31", features = ["serde"] }
clap = { version = "4.0", features = ["derive"] }
hyper = { version = "1.0" }
proximadb-data-model = { path = "crates/foundation/proximadb-data-model" }
proximadb-graph-model = { path = "crates/foundation/proximadb-graph-model" }
proximadb-graph-engine-traits = { path = "crates/foundation/proximadb-graph-engine-traits" }
proximadb-config = { path = "crates/foundation/proximadb-config" }
proximadb-compression-types = { path = "crates/foundation/proximadb-compression-types" }
proximadb-hardware = { path = "crates/foundation/proximadb-hardware" }
proximadb-hardware-caps = { path = "crates/foundation/proximadb-hardware-caps" }
proximadb-distance-kernel = { path = "crates/foundation/proximadb-distance-kernel" }
proximadb-clustering-kernel = { path = "crates/foundation/proximadb-clustering-kernel" }
proximadb-concurrent = { path = "crates/foundation/proximadb-concurrent" }
proximadb-memory-pool = { path = "crates/foundation/proximadb-memory-pool" }
proximadb-kernel = { path = "crates/foundation/proximadb-kernel" }
proximadb-interner = { path = "crates/foundation/proximadb-interner" }
proximadb-bloom = { path = "crates/foundation/proximadb-bloom" }
proximadb-search-types = { path = "crates/foundation/proximadb-search-types" }
proximadb-tenant = { path = "crates/foundation/proximadb-tenant" }
proximadb-proto = { path = "crates/foundation/proximadb-proto" }
proximadb-cache = { path = "crates/foundation/proximadb-cache" }
proximadb-distance-types = { path = "crates/foundation/proximadb-distance-types" }
proximadb-filter-expression = { path = "crates/foundation/proximadb-filter-expression" }
proximadb-quantization-types = { path = "crates/foundation/proximadb-quantization-types" }
proximadb-quantization-model = { path = "crates/foundation/proximadb-quantization-model" }
proximadb-index-types = { path = "crates/foundation/proximadb-index-types" }
proximadb-index-storage = { path = "crates/foundation/proximadb-index-storage" }
proximadb-index-traits = { path = "crates/foundation/proximadb-index-traits" }
proximadb-records = { path = "crates/foundation/proximadb-records" }
proximadb-relational-types = { path = "crates/foundation/proximadb-relational-types" }
proximadb-functions = { path = "crates/foundation/proximadb-functions" }
proximadb-execution-contracts = { path = "crates/query/proximadb-execution-contracts" }
proximadb-relational-algebra = { path = "crates/query/proximadb-relational-algebra" }
proximadb-relational-reader = { path = "crates/query/proximadb-relational-reader" }
proximadb-relational-planner = { path = "crates/query/proximadb-relational-planner" }
proximadb-relational-executor = { path = "crates/query/proximadb-relational-executor" }
proximadb-relational-frontend = { path = "crates/query/proximadb-relational-frontend" }
proximadb-relational-engine = { path = "crates/query/proximadb-relational-engine" }
proximadb-abac = { path = "crates/control/proximadb-abac" }
proximadb-catalog = { path = "crates/control/proximadb-catalog" }
proximadb-metrics = { path = "crates/control/proximadb-metrics" }
arc-swap = "1.7"
dashmap = "6.1.0"
moka = { version = "0.12", features = ["future"] }
parking_lot = "0.12"
tracing = "0.1"
serde_json = "1.0"
sqlx = { version = "0.9", features = ["runtime-tokio", "tls-native-tls", "json", "chrono", "uuid"] }
prost = "0.14"
prost-types = "0.14"
futures = "0.3"
rand = { version = "0.8", features = ["small_rng"] }
rayon = "1.10"
regex = "1.10"
serde = { version = "1.0", features = ["derive"] }
# On-demand stack growth for the plan-tree recursions (planner lowering, executor
# build, native lowering) — grows the stack instead of overflowing on deeply
# nested plans (TD-EXEC-2 §1.a). Same primitive rustc/Servo use.
stacker = "0.1"
tempfile = "3.0"
thiserror = "1.0"
tokio = { version = "1.37", features = ["full"] }
tokio-stream = { version = "0.1", features = ["net"] }
tonic = { version = "0.14", features = ["gzip", "tls-native-roots"] }
toml = "0.8"
tower = { version = "0.5", features = ["limit"] }
tower-http = { version = "0.6", features = ["cors", "trace"] }
proximadb-vector = { path = "crates/modalities/proximadb-vector" }
proximadb-quantization-kernel = { path = "crates/modalities/proximadb-quantization-kernel" }
proximadb-document = { path = "crates/modalities/proximadb-document" }
proximadb-orion-engine = { path = "crates/modalities/proximadb-orion-engine" }
proximadb-observability = { path = "crates/modalities/proximadb-observability" }
proximadb-observability-engine = { path = "crates/modalities/proximadb-observability-engine" }
proximadb-embedding = { path = "crates/modalities/proximadb-embedding" }
proximadb-rank-core = { path = "crates/modalities/proximadb-rank-core" }
proximadb-rank-features = { path = "crates/modalities/proximadb-rank-features" }
proximadb-rank-expr = { path = "crates/modalities/proximadb-rank-expr" }
proximadb-rank-profile = { path = "crates/modalities/proximadb-rank-profile" }
proximadb-rank-onnx = { path = "crates/modalities/proximadb-rank-onnx" }
proximadb-api = { path = "crates/platform/proximadb-api" }
proximadb-admin-ui = { path = "crates/platform/proximadb-admin-ui" }
proximadb-codec = { path = "crates/horizontal/proximadb-codec" }
proximadb-compression = { path = "crates/horizontal/proximadb-compression" }
proximadb-runtime-common = { path = "crates/horizontal/proximadb-runtime-common" }
proximadb-serialization = { path = "crates/horizontal/proximadb-serialization" }
proximadb-tls = { path = "crates/horizontal/proximadb-tls" }
proximadb-storage-common = { path = "crates/storage/proximadb-storage-common" }
proximadb-storage-ports = { path = "crates/storage/proximadb-storage-ports" }
proximadb-storage-traits = { path = "crates/storage/proximadb-storage-traits" }
proximadb-storage-encryption = { path = "crates/storage/proximadb-storage-encryption" }
proximadb-block-format = { path = "crates/storage/proximadb-block-format" }
proximadb-iceberg-engine = { path = "crates/storage/proximadb-iceberg-engine" }
proximadb-storage-kv = { path = "crates/storage/proximadb-storage-kv" }
proximadb-storage-operations = { path = "crates/storage/proximadb-storage-operations" }
proximadb-storage-optimization = { path = "crates/storage/proximadb-storage-optimization" }
proximadb-storage-filesystem-types = { path = "crates/storage/proximadb-storage-filesystem-types" }
proximadb-storage-strategy = { path = "crates/storage/proximadb-storage-strategy" }
proximadb-storage-tenant = { path = "crates/storage/proximadb-storage-tenant" }
proximadb-storage-transaction = { path = "crates/storage/proximadb-storage-transaction" }
proximadb-query-capability = { path = "crates/query/proximadb-query-capability" }
crc32fast = "1.4"
rmp-serde = "1.1"
tonic-prost = "0.14"
# Compression libraries (workspace-inheritable for modality/horizontal crates)
brotli = "6.0"
bzip2 = "0.5"
flate2 = { version = "1.0", default-features = true }
lz4_flex = "0.11"
snap = "1.1"
xz2 = "0.1"
zstd = "0.13"

# Language bindings
pyo3 = { version = "0.28", features = ["extension-module", "abi3-py310"] }  # abi3 floor = CPython 3.10 (our minimum supported Python); one wheel (cp310-abi3) installs on 3.10+
numpy = { version = "0.28" }

# TurboQuant numerics (ADR-021, gated by `experimental-turboquant`).
# Kept here so future modality + benchmark consumers share a single version.
faer = { version = "0.20", default-features = false, features = ["std"] }
statrs = "0.17"

# Python embedded library (cdylib for PyO3)
[lib]
name = "proximadb"
# Default: rlib only (for testing and normal Rust usage)
# cdylib is produced via the pylib feature flag (for PyO3/FFI builds only)
# See ADR-006 for rationale: cdylib caused test modules to be silently excluded
crate-type = ["rlib"]



[dependencies]
# Core async runtime
tokio = { version = "1.37", features = ["full"] }
tokio-stream = { version = "0.1", features = ["net"] }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Sandhi neutral usage parsers (TD-SANDHI-2 / ADR-0047 D10a): single-sourced, fixture-proven
# (Sandhi TD-0001 W1) usage/cache-split extraction adopted by the generation LLM path — not a
# hand-rolled 4th extractor. `sandhi-core` is serde-only (no reqwest/tokio); dependency direction
# one-way (ProximaDB → Sandhi, ADR-060). Registry release since sandhi v0.1.1.
sandhi-core = "0.1"

# OpenAPI spec-from-code (TD-126 Phase 1). The `ToSchema`/`IntoParams`/`path`
# derives annotate the v2 REST DTOs + handlers so `docs/openapi/proximadb-openapi.yaml`
# is generated from the handler code (see src/network/rest/openapi.rs) with a CI
# drift gate, rather than hand-maintained. Derives are zero runtime cost; the
# aggregated document is only materialized by the generator/test path.
utoipa = { version = "5", features = ["axum_extras", "preserve_order", "yaml"] }
# YAML (de)serialization for the OpenAPI generator: parse the checked-in
# supplement fragment and render the merged document to canonical YAML.
serde_yaml = "0.9"
bincode.workspace = true
rmp-serde = "1.1"  # MessagePack serialization
bytemuck = { version = "1.14", features = ["derive"] }
proximadb-proto = { path = "crates/foundation/proximadb-proto" }
proximadb-document-query = { path = "crates/query/proximadb-document-query" }
proximadb-config = { path = "crates/foundation/proximadb-config" }
proximadb-compression-types = { path = "crates/foundation/proximadb-compression-types" }
proximadb-hardware = { path = "crates/foundation/proximadb-hardware" }
proximadb-hardware-caps = { path = "crates/foundation/proximadb-hardware-caps" }
proximadb-distance-kernel = { path = "crates/foundation/proximadb-distance-kernel" }
proximadb-clustering-kernel = { path = "crates/foundation/proximadb-clustering-kernel" }
proximadb-concurrent = { path = "crates/foundation/proximadb-concurrent" }
proximadb-data-model = { path = "crates/foundation/proximadb-data-model" }
proximadb-graph-model = { path = "crates/foundation/proximadb-graph-model" }
proximadb-graph-engine-traits = { path = "crates/foundation/proximadb-graph-engine-traits" }
proximadb-distance-types = { path = "crates/foundation/proximadb-distance-types" }
proximadb-relational-types = { path = "crates/foundation/proximadb-relational-types" }
proximadb-functions = { path = "crates/foundation/proximadb-functions" }
proximadb-execution-contracts = { path = "crates/query/proximadb-execution-contracts" }
proximadb-relational-algebra = { path = "crates/query/proximadb-relational-algebra" }
proximadb-relational-reader = { path = "crates/query/proximadb-relational-reader" }
proximadb-relational-planner = { path = "crates/query/proximadb-relational-planner" }
proximadb-relational-executor = { path = "crates/query/proximadb-relational-executor" }
proximadb-relational-frontend = { path = "crates/query/proximadb-relational-frontend" }
proximadb-relational-engine = { path = "crates/query/proximadb-relational-engine" }
proximadb-index-types = { path = "crates/foundation/proximadb-index-types" }
proximadb-index-storage = { path = "crates/foundation/proximadb-index-storage" }
proximadb-index-traits = { path = "crates/foundation/proximadb-index-traits" }
proximadb-records = { path = "crates/foundation/proximadb-records" }
proximadb-quantization-types = { path = "crates/foundation/proximadb-quantization-types" }
proximadb-quantization-model = { path = "crates/foundation/proximadb-quantization-model" }
proximadb-catalog = { path = "crates/control/proximadb-catalog" }
proximadb-abac = { path = "crates/control/proximadb-abac", optional = true }
proximadb-metrics = { path = "crates/control/proximadb-metrics" }
proximadb-api = { path = "crates/platform/proximadb-api" }
proximadb-admin-ui = { path = "crates/platform/proximadb-admin-ui" }
proximadb-mcp = { path = "crates/platform/proximadb-mcp" }
proximadb-runtime = { path = "crates/platform/proximadb-runtime" }
proximadb-codec = { path = "crates/horizontal/proximadb-codec" }
proximadb-compression = { path = "crates/horizontal/proximadb-compression" }
proximadb-embedded-common = { path = "crates/horizontal/proximadb-embedded-common" }
proximadb-runtime-common = { path = "crates/horizontal/proximadb-runtime-common" }
proximadb-serialization = { path = "crates/horizontal/proximadb-serialization" }
proximadb-security = { path = "crates/horizontal/proximadb-security" }
proximadb-tls = { path = "crates/horizontal/proximadb-tls" }
proximadb-storage-common = { path = "crates/storage/proximadb-storage-common" }
proximadb-storage-ports = { path = "crates/storage/proximadb-storage-ports" }
proximadb-storage-traits = { path = "crates/storage/proximadb-storage-traits" }
proximadb-storage-encryption = { path = "crates/storage/proximadb-storage-encryption" }
proximadb-block-format = { path = "crates/storage/proximadb-block-format" }
proximadb-object-store = { path = "crates/storage/proximadb-object-store" }
proximadb-iceberg-engine = { path = "crates/storage/proximadb-iceberg-engine" }
proximadb-storage-kv = { path = "crates/storage/proximadb-storage-kv" }
proximadb-storage-operations = { path = "crates/storage/proximadb-storage-operations" }
proximadb-storage-optimization = { path = "crates/storage/proximadb-storage-optimization" }
proximadb-storage-filesystem-types = { path = "crates/storage/proximadb-storage-filesystem-types" }
proximadb-storage-strategy = { path = "crates/storage/proximadb-storage-strategy" }
proximadb-storage-tenant = { path = "crates/storage/proximadb-storage-tenant" }
proximadb-storage-transaction = { path = "crates/storage/proximadb-storage-transaction" }
proximadb-telemetry = { path = "crates/horizontal/proximadb-telemetry" }
proximadb-filter-expression = { path = "crates/foundation/proximadb-filter-expression" }
proximadb-cache = { path = "crates/foundation/proximadb-cache" }
proximadb-document = { path = "crates/modalities/proximadb-document" }
proximadb-orion-engine = { workspace = true }
proximadb-graph = { path = "crates/modalities/proximadb-graph" }
proximadb-observability = { path = "crates/modalities/proximadb-observability" }
proximadb-observability-engine = { path = "crates/modalities/proximadb-observability-engine" }
# Optional: the experimental transactional Ledger modality (gated by `experimental-ledger`).
proximadb-ledger = { path = "crates/modalities/proximadb-ledger", optional = true }
# F2 (ADR-072 D3/D5): the generation-fenced local-WAL ConditionalKeyStore impl,
# wired into the relational insert path for atomic composite-PK uniqueness under
# the `oltp-integrity` feature. Optional → default builds are byte-for-byte unchanged.
proximadb-cks-local = { path = "crates/storage/proximadb-cks-local", optional = true }
proximadb-embedding = { path = "crates/modalities/proximadb-embedding" }
proximadb-queue = { path = "crates/horizontal/proximadb-queue" }
proximadb-rank-core = { path = "crates/modalities/proximadb-rank-core" }
proximadb-rank-features = { path = "crates/modalities/proximadb-rank-features" }
proximadb-rank-expr = { path = "crates/modalities/proximadb-rank-expr" }
proximadb-rank-profile = { path = "crates/modalities/proximadb-rank-profile" }
proximadb-rank-onnx = { path = "crates/modalities/proximadb-rank-onnx" }
proximadb-vector = { path = "crates/modalities/proximadb-vector" }
proximadb-quantization-kernel = { path = "crates/modalities/proximadb-quantization-kernel" }
proximadb-graph-arrow = { path = "crates/query/proximadb-graph-arrow" }
proximadb-graph-query = { path = "crates/query/proximadb-graph-query" }
proximadb-graph-subset = { path = "crates/query/proximadb-graph-subset" }
proximadb-multimodel-query = { path = "crates/query/proximadb-multimodel-query" }
proximadb-multimodel-plan = { path = "crates/query/proximadb-multimodel-plan" }
proximadb-observability-query = { path = "crates/query/proximadb-observability-query" }
proximadb-query = { path = "crates/query/proximadb-query" }
proximadb-pipeline-operator = { path = "crates/foundation/proximadb-pipeline-operator" }
proximadb-query-capability = { path = "crates/query/proximadb-query-capability" }
proximadb-query-clauses = { path = "crates/query/proximadb-query-clauses" }
proximadb-query-fusion = { path = "crates/query/proximadb-query-fusion" }
proximadb-query-filter = { path = "crates/query/proximadb-query-filter" }
proximadb-uql = { path = "crates/query/proximadb-uql" }
proximadb-vector-query = { path = "crates/query/proximadb-vector-query" }
# Extracted from src/services/catalog_introspection.rs (re-export shim keeps callers stable).
proximadb-catalog-introspection = { version = "0.2.0", path = "crates/binding/proximadb-catalog-introspection" }

# SQL Parsing
sqlparser = { version = "0.59", features = ["visitor"] }

# Async recursion support
async-recursion = "1.0"

# Checksums
crc32fast = "1.4"  # Used for SST codebook checksum

# System information
libc = "0.2"

# Data structures
once_cell = "1.19"
lazy_static = "1.5"

# Networking & gRPC
ipnet = "2.12" # CIDR longest-prefix client-locality classification (KOU, co-design D2)
tonic = { version = "0.14", features = ["gzip", "tls-native-roots"] }
tonic-prost = "0.14"  # Runtime codec for tonic-build 0.14+ generated code
tonic-health = "0.14"  # Standard grpc.health.v1.Health service
tonic-reflection = "0.14"  # Server reflection (grpc.reflection.v1alpha.ServerReflection)
prost = "0.14"
prost-types = { version = "0.14" }
hyper = "1.0"  # Unified with tonic 0.14 / axum 0.8
hyper-util = { version = "0.1", features = ["tokio", "server", "server-auto"] }  # Utilities for hyper 1.x (tonic compatibility)
http-body-util = "0.1"  # Body utilities for http 1.x (tonic compatibility)
tower = { version = "0.5", features = ["limit"] }
tower-http = { version = "0.6", features = ["cors", "trace", "fs", "compression-full", "decompression-full", "timeout"] }
url = "2.4"
reqwest = { version = "0.11", features = ["json"] }
lettre = "0.11"  # Email sending for alert notifications

# OTLP metering push (TD-161 dual-sink, ADR-027). OPTIONAL + default-OFF behind the
# `otlp-metering` feature: the durable per-tenant `_metering` writer is OSS-native (no
# deps); only the external OTLP push pulls these. Pinned to the 0.32 line, which is the
# first to resolve against the repo's own `tonic 0.14` / `prost 0.14` with NO duplicate
# gRPC stack (otlp <=0.30 drags in a second tonic 0.12-0.13 + prost 0.13). `grpc-tonic`
# reuses the existing tonic transport and avoids pulling reqwest 0.12.
opentelemetry = { version = "0.32", optional = true }
# `rt-tokio` + `experimental_metrics_periodicreader_with_async_runtime` gate
# `runtime::Tokio` and the async-runtime `PeriodicReader`, which drive export on the
# tokio runtime — REQUIRED with the grpc-tonic exporter (the default thread-based
# reader block_on's a tonic call with no reactor → runtime panic). The async-runtime
# feature transitively enables `experimental_async_runtime`.
opentelemetry_sdk = { version = "0.32", features = ["rt-tokio", "experimental_metrics_periodicreader_with_async_runtime", "metrics"], optional = true }
opentelemetry-otlp = { version = "0.32", default-features = false, features = ["grpc-tonic", "metrics"], optional = true }

# Storage & Memory Management
memmap2 = "0.9"
lru = "0.18"  # LRU cache for disk-backed graph storage
rocksdb = { version = "0.21", optional = true }  # Optional for metadata backend
tokio-postgres = { version = "0.7", optional = true }  # PostgreSQL client for CDC (experimental-cdc-connectors)
sqlx = { version = "0.9", features = ["runtime-tokio", "tls-native-tls", "json", "chrono", "uuid"], optional = true }  # OLTP catalog backends (oltp-catalog)
tempfile = "3.0"
rayon = "1.10"  # Parallel processing for search optimization
# Parquet and Arrow dependencies - using v57+ for improved performance and tonic 0.14 compatibility
parquet = { version = "58", default-features = false, features = ["arrow", "zstd", "lz4", "snap", "brotli", "flate2", "flate2-rust_backened"] }
# Arrow dependencies matching parquet v57
arrow = { version = "58", features = ["prettyprint"] }
arrow-array = { version = "58", default-features = false }
arrow-buffer = { version = "58", default-features = false }
arrow-schema = { version = "58", default-features = false }
arrow-data = { version = "58", default-features = false }
arrow-ipc = { version = "58", default-features = false, features = ["lz4", "zstd"] }
arrow-select = { version = "58", default-features = false }
arrow-ord = { version = "58", default-features = false }
arrow-flight = { version = "58", features = ["flight-sql-experimental"] }
arrow-cast = { version = "58", default-features = false }

# DataFusion for SQL and compute engine compatibility (Arrow 57 compatible)
datafusion = { version = "54.0", default-features = false, features = ["sql", "regex_expressions", "unicode_expressions", "datetime_expressions", "math_expressions"], optional = true }
datafusion-common = { version = "54.0", optional = true }
# ADR-059: DuckDB-Local as a plugin OLAP engine (in-process, behind the
# `duckdb` feature, default OFF). `bundled` builds libduckdb from source so no
# system DuckDB is required. Used by the perf-ledger harnesses for an
# in-process, io-traced DuckDB baseline (ADR-059 D1/D2).
duckdb = { version = "1.1", features = ["bundled", "parquet"], optional = true }

# Advanced compression for recovery-optimized WAL
lz4_flex = "0.11"        # Faster LZ4 implementation
snap = "1.1"             # Snappy compression
zstd = "0.13"            # Zstandard compression
flate2 = { version = "1.0", default-features = true }  # Gzip/Deflate/Zlib compression
brotli = "6.0"           # Brotli compression - Updated from 3.5
bzip2 = "0.5"            # Bzip2 compression - Updated from 0.4
xz2 = "0.1"              # XZ/LZMA2 compression

# System monitoring
sysinfo = "0.30"

# Avro serialization with schema evolution
apache-avro = "0.16"
bytes = "1.5"

# Cloud storage: the canonical object_store crate provides true byte-range
# get_range for S3 + Azure (the bandwidth-optimal ADR-023 cold-load primitive)
# + emulator support (MinIO / Azurite), replacing the pre-GA azure_storage_blobs
# (broken ranged GET) and the heavy aws-sdk-s3 tree. Already transitive (via
# parquet); promoted to a direct dep. Feature-gated via the aws/azure features.
object_store = { workspace = true }
# GCS stays on the google-cloud-storage SDK: object_store's GCS backend speaks
# the XML API, which fake-gcs-server (our test emulator) does not serve, so it
# is unverifiable locally; this SDK uses the JSON API (fake-gcs-compatible) and
# still issues true ranged object downloads (bandwidth-optimal). Feature `gcp`.
google-cloud-storage = { version = "0.15", optional = true }

# Vector operations & SIMD
nalgebra = { version = "0.32", features = ["serde-serialize"] }  # For PCA eigendecomposition, with serde for model persistence
half = "2.4"  # FP16 (float16) support for centroid quantization (50% storage reduction)

# CPU optimization
num_cpus = "1.16"
raw-cpuid = "11.0"  # CPU feature detection for x86_64

# Random number generation
rand.workspace = true
stacker.workspace = true
rand_chacha = "0.3"
rand_distr = "0.4"  # For generating realistic probability distributions

# Cryptographic hashing
sha2 = "0.10"
hkdf = "0.12"  # Key derivation function
aes-gcm = "0.10"  # AEAD encryption for field-level encryption

# TLS/mTLS certificate management
rcgen = "0.12"                   # Certificate generation
x509-parser = "0.16"             # Certificate parsing and validation
rustls = { version = "0.23" }    # TLS implementation (unified with axum-server 0.7)
rustls-pemfile = "2.0"           # PEM file parsing (CertificateDer/PrivateKeyDer)
tokio-rustls = "0.26"            # Async TLS streams (rustls 0.23)
webpki-roots = "0.25"            # Mozilla root certificates
axum-server = { version = "0.7", features = ["tls-rustls"] }  # Axum TLS support (hyper 1.0)
time = { version = "0.3", features = ["std"] }  # Time handling for certificates

# Logging & Monitoring
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
tracing-appender = "0.2"
prometheus = "0.14"
metrics = "0.21"

# Configuration
config = "0.15"
clap = { version = "4.0", features = ["derive"] }
toml = "0.8"

# Benchmarking (HTML reports available via `cargo bench`)

# Error handling
anyhow = "1.0"
thiserror = "1.0"
proximadb-kernel = { path = "crates/foundation/proximadb-kernel" }
proximadb-interner = { path = "crates/foundation/proximadb-interner" }
proximadb-bloom = { path = "crates/foundation/proximadb-bloom" }
proximadb-search-types = { path = "crates/foundation/proximadb-search-types" }
proximadb-tenant = { path = "crates/foundation/proximadb-tenant" }

# Utilities
uuid = { version = "1.0", features = ["v4", "serde"] }  # Required for JWT token generation
# v10 made the crypto backend a pluggable `CryptoProvider` (like rustls), with NO provider in
# default features — without one, every sign/verify panics at runtime (crypto/mod.rs:124). Enable
# `aws_lc_rs` (aws-lc-rs is already in the tree via rustls; `rust_crypto` would pull new rsa/p256/p384).
jsonwebtoken = { version = "10", features = ["aws_lc_rs"] }  # JWT token creation and validation
chrono = { version = "0.4.31", features = ["serde"] }  # Updated for Arrow 51 compatibility
chrono-tz = "0.10"  # Timezone support for log parsing
hex = "0.4"  # Hex encoding for bytea types
base64 = "0.22"  # Base64 encoding for encryption
ring = "0.17"  # Cryptographic operations for field-level encryption
crossbeam = "0.8"  # Lock-free ring buffers for observability ingestion

parking_lot = "0.12"
arc-swap = { workspace = true }
async-trait = "0.1"
futures = "0.3"
regex = "1.10"
nom = "7"
hostname = "0.3"  # Get hostname for leader election

# Multi-model storage (Document + Observability)
tantivy = "0.22"  # Full-text search for documents
jsonpath-rust = "0.5"  # JSON path queries for document filtering (actively maintained)
flume = "0.11"  # High-performance MPSC channels for observability

# PostgreSQL wire protocol compatibility
pgwire = "0.21"  # PostgreSQL wire protocol for pgvector migration

# REST API
axum = { version = "0.8", features = ["json", "ws"] }
urlencoding = "2.1.3"
dashmap = "6.1.0"
moka = { version = "0.12", features = ["future"] }

# Language bindings (optional, for embedded mode)
# Python bindings via PyO3
# Vendored OpenSSL for the embedded wheel build only (see the `python` feature).
# `openssl-sys` reaches the tree transitively (sqlx tls-native-tls + reqwest 0.11
# default-tls). The manylinux wheel container has no system openssl, so the
# maturin build fails at link time. Declaring openssl here with `vendored` and
# gating it under `python` makes only the wheel build compile OpenSSL from source
# (portable, cross-target-safe); the OSS server build never pulls this dep and
# keeps dynamic system openssl.
# Zero-copy NumPy array access (matches PyO3 version)
# Provides direct buffer access to numpy arrays without Python .tolist() conversion
# Java bindings via JNI
# Node.js bindings via NAPI-RS
proximadb-raptor-common = { version = "0.2.0", path = "crates/storage/proximadb-raptor-common" }
proximadb-raptor-engine = { version = "0.2.0", path = "crates/storage/proximadb-raptor-engine" }
proximadb-engine-core = { version = "0.2.0", path = "crates/storage/proximadb-engine-core" }
proximadb-mongodb-parser = { version = "0.2.0", path = "crates/binding/proximadb-mongodb-parser" }

# GPU acceleration dependencies (platform-specific)
[target.'cfg(all(target_os = "macos", target_arch = "aarch64"))'.dependencies]
metal = { version = "0.27", optional = true }  # Metal GPU acceleration for Apple Silicon

# Build dependencies
[build-dependencies]
tonic-build = "0.14"
tonic-prost-build = "0.14"  # Required for prost-based protobuf compilation in tonic-build 0.14+
apache-avro = "0.16"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tracing = "0.1"
tracing-subscriber = "0.3"

# Development dependencies
[dev-dependencies]
criterion = { version = "0.5", features = ["async_tokio"] }
proptest = "1.0"
# In-process gather of the default prometheus registry where the per-tenant
# consumption counters live (tests/documents_convergence_bake.rs measures the
# metering delta at the registry, not just the /metrics/prometheus endpoint —
# same pin as the root [dependencies] entry).
prometheus = "0.14"
# tempdir = "0.3"  # REMOVED: Deprecated since 2018, has TOCTOU vulnerability (RUSTSEC-2023-0018). Use tempfile instead.
env_logger = "0.10"
walkdir = "2"
# Real Postgres wire-protocol client for the pgwire transport e2e tests
# (tests/fp16_pgwire_e2e.rs). Same major as the optional CDC dep but
# dev-only so it doesn't affect production binary size.
tokio-postgres = "0.7"
# Rust SDK in client (REST) mode for SDK-level integration tests
# (tests/fp16_rust_sdk_e2e.rs). The SDK lives in clients/rust as a
# workspace member; dev-dep here so we can drive it against an
# in-process server fixture.
proximadb-sdk = { path = "clients/rust", default-features = false, features = ["client"] }
# OpenAPI contract gates for the Rust connectors (tests/connectors_openapi_contract.rs
# and tests/connectors_flight_contract.rs). httpmock is the same crate the SDK
# gate uses at clients/rust/tests/openapi_contract.rs; serde_yaml lets the
# gates load docs/openapi/proximadb-openapi.yaml directly.
httpmock = "0.7"
serde_yaml = "0.9"
# Test-only: integration tests use EmbeddedProximaDB from the embedded crate.
# Dev-deps don't create build cycles (test-mode only).
proximadb-embedded = { path = "crates/binding/proximadb-embedded" }

[features]
# Default feature set as of 2026-05-26 (ADR-020 Tier 2.2 / TD-065 default-enable).
# `canonical-document-store` flips on by default so the canonical WAL path is
# the durable authority for documents. Operators on the legacy non-canonical
# (in-memory-map-as-durable-source) path during one minor-release migration
# window can opt OUT via `default-features = false` + explicit feature selection.
# Plan: remove the feature flag entirely in v0.4 once the migration window closes.
default = ["sql_frontend", "graph-first-sks", "unified-facade-routing", "canonical-document-store", "datafusion-integration", "otlp-metering", "axis"]
# AXIS index engine (HNSW/IVF/RaBitQ ANN) + its recall-advisor / drift / hot-swap surfaces.
# Default ON = zero behavior change (the canonical ANN path). Gate OFF for PAX exact-scan-only
# builds (smaller binary, no AXIS boot/index-build cost); search then routes to the exact-scan
# path (TD-AXIS-4 Superseded; ADR-052). Pure cfg gate — AXIS lives in the root crate, no dep.
axis = []
# Optional surfaces; keep off by default to minimize binary size and public API surface
ai_endpoints = []
tenant_access = []
# Experimental storage engines (RAPTOR, SWIFT); disabled by default to prevent
# server panics from unimplemented code paths. Enable for development/testing only.
# RAPTOR fundamentally uses AXIS clustering, so this implies `axis`.
experimental-engines = ["proximadb-raptor-engine/experimental-engines", "axis"]
# Experimental transactional Ledger modality (ADR-071 / TD-LEDGER-1): the durable atomic-CAS
# KV + TTL-lease OLTP surface. Off by default; enables the durable backend + its gRPC service.
experimental-ledger = ["dep:proximadb-ledger", "proximadb-ledger/durable"]
# F2 (ADR-072 D3/D5): enable the fenced ConditionalKeyStore in the relational insert
# path. Off by default → the existing check-then-act PK-uniqueness probe runs unchanged;
# on, a LocalWalKeyStore is wired in and enforces composite-PK uniqueness atomically.
oltp-integrity = ["dep:proximadb-cks-local"]
# FA (TD-FOUNDATION-3): enable ABAC row-level enforcement. Off by default → the
# read primitives run unchanged; on, the compile bridge + admits_with_security
# filter rows by the subject's policy. The abac crate itself stays un-gated
# (a security substrate that never compiles is never tested).
abac-policy = ["dep:proximadb-abac", "proximadb-embedded/abac-policy"]
# F3v (TD-DELVEC-1 / ADR-072 D14): enable cold-tier deletion vectors. Off by
# default → the default flush path is byte-for-byte unchanged; on, PAX segment
# writes capture the OID→position resolver (footer region, WI-2c) so a
# cold-resident delete (WI-3b) can set a deletion-vector bit without rewriting
# the segment. Pure gate (no dep) — the storage-common primitives
# (VersionedDeletionVector, OidPositionResolver, footer region) are unconditional.
cold-deletion-vectors = []
# Experimental SIMD path; not enabled by default
simd-experimental = ["proximadb-codec/simd-experimental"]
# TD-160 / ADR-027 / ADR-030: gate the PERF-CLASS I/O-trace *emission* (the
# per-query structured `tracing` event in `io_trace::IoTrace::emit`). Default OFF
# → zero perf-emission cost in normal operation. The cheap core counters
# (range_gets/bytes_read/compute_ms) and ALL observers (route-cost, cache, and
# the always-on BILLING observer) stay live regardless — billing is never gated
# (ADR-027 non-entanglement; enforced by scripts/check_billing_never_gated.py).
io-trace = ["proximadb-observability-engine/io-trace"]
# TurboQuant data-oblivious quantizer + in-kernel allowlist (ADR-021).
# Gates the QuantizationLevel::TurboQuant variant in proximadb-vector + the
# TurboQuantVectorData struct in src/compute/distance_computation/quantized.rs.
# Tracked in docs/12-design/TURBOQUANT_LLD_2026_05_30.adoc; promoted to beta
# after P9 recall validation closes.
experimental-turboquant = [
    "proximadb-quantization-types/experimental-turboquant",
    "proximadb-quantization-model/experimental-turboquant",
    "proximadb-vector/experimental-turboquant",
    "proximadb-distance-kernel/experimental-turboquant",
    "proximadb-quantization-kernel/experimental-turboquant",
]
# AVX512 intrinsics (requires nightly Rust, disabled by default for CI compatibility)
avx512 = ["proximadb-codec/avx512"]
standalone = []
# Cluster mode - enables distributed consensus (Raft), replication, and health services
# Used for multi-node deployments with leader election and data replication.
# Forwards to proximadb-runtime/cluster so the `ClusterServerConfig` re-export and
# the `MultiServerConfig::cluster_config` field (both `#[cfg(feature = "cluster")]`
# in bootstrap_config.rs) actually compile when this feature is on.
cluster = ["proximadb-runtime/cluster"]
rocksdb = ["dep:rocksdb"]  # RocksDB metadata backend support
# Real ONNX-runtime embedding. Off by default (the `ort` runtime + model files
# are heavyweight and not needed for the default build). Forwards to
# proximadb-embedding/onnx so the in-process BGE embedder produces real vectors;
# without it the BGE route returns ModelUnavailable (synthetic fallback is
# forbidden in production paths). Tests that exercise real embedding are gated
# on this feature.
onnx = ["proximadb-embedding/onnx"]

# Cloud storage features (optional to avoid linking issues during testing)
aws = ["object_store/aws", "proximadb-catalog/aws"]
azure = ["object_store/azure"]
gcp = ["dep:google-cloud-storage"]

# TD-161 external OTLP metering push (default-OFF). Compiles the OTLP exporter that
# ships the per-tenant metering snapshot to a collector; runtime-gated additionally by
# PROXIMADB_OTLP_ENDPOINT so the binary is inert unless explicitly configured. The
# durable FileSystem `_metering` writer is ALWAYS compiled (OSS-native, no deps) — only
# this push path is feature-gated to keep the opentelemetry transitive surface out of
# default builds. Verify the server binary builds with AND without this feature (#11).
otlp-metering = ["dep:opentelemetry", "dep:opentelemetry_sdk", "dep:opentelemetry-otlp", "proximadb-observability-engine/otlp-metering"]

# ============================================================================
# OPTIONAL INTEGRATION FEATURES
# ============================================================================

# Network REST endpoints (HTTP/HTTPS server components)
network-rest = ["proximadb-tls/network-rest"]

# CDC Kafka integration (change data capture to Kafka)
# Note: rdkafka dependency would be added when enabling this feature
cdc-kafka = []

# LLM-driven semantic join modes (TD-049, arXiv:2510.08489 block-batched joins).
# When enabled, JoinType::Semantic { mode: LlmBlockBatch { ... } } is dispatched
# to an LLM-backed batched-prompt path. When disabled, only the cosine mode runs;
# the LlmBlockBatch mode returns a clear NotImplemented error so server logs
# attribute the failure correctly.
llm-joins = []

# Protobuf compilation (for distributed gRPC service generation)
compile_protobuf = []

# ============================================================================
# TEST CATEGORIZATION (Strategic CI/CD Gating)
# ============================================================================
# These feature flags categorize tests for efficient CI pipeline execution.
# Usage in CI:
#   - Fast CI: cargo test --lib (unit tests only, ~2-5s)
#   - Standard CI: cargo test (unit + integration, ~30-60s)
#   - Full CI: cargo test --features test-full (all tests, ~5-10min)
#
# Test Categories:
#   - test-unit: Unit tests in src/ (always run, fastest)
#   - test-integration: Integration tests in tests/ (run on PRs)
#   - test-graph: Graph database tests (test_graph_*, graph_*)
#   - test-storage: Storage engine tests (sst, helix, viper, nova, swift, raptor)
#   - test-query: Query engine tests (sql, unified_query, federated)
#   - test-perf: Performance benchmarks (slow, CI-gated)
#   - test-security: Security/penetration tests
#   - test-cloud: Cloud backend tests (requires credentials)
#
# Example CI usage:
#   cargo test --lib                           # Fast: unit tests only
#   cargo test --features test-integration     # Standard: integration tests
#   cargo test --features test-graph           # Graph-specific tests
#   cargo test --features test-full            # All tests

# Default test runs (lean - unit tests only in CI by default)
comprehensive_tests = []  # Legacy flag for comprehensive tests
perf_tests = []           # Legacy flag for performance tests

# Strategic test categories for CI/CD
test-unit = []        # Unit tests (always fast, always run)
test-integration = [] # Integration tests (tests/*_test.rs)
test-graph = []       # Graph database tests
test-storage = []     # Storage engine tests
test-query = []       # Query engine tests (SQL, unified, federated)
test-perf = []        # Performance/benchmark tests
test-security = []    # Security and penetration tests
test-cloud = ["aws", "azure", "gcp"]  # Cloud backend tests

# Composite test categories
test-quick = ["test-unit"]  # Fast CI (<1min)
test-standard = ["test-unit", "test-integration"]  # Standard CI (<5min)
test-full = ["test-unit", "test-integration", "test-graph", "test-storage", "test-query"]  # Full CI

# Cloud storage features (optional to avoid linking issues during testing)
cloud-full = ["aws", "azure", "gcp"]

# SQL frontend feature flag (default on). When disabled, the server uses legacy sql_engine path.
sql_frontend = []

# Unified facade routing feature flag.
# When enabled, routes all queries through UnifiedQueryFacade for consistent execution.
# When disabled, uses legacy direct handler paths for backward compatibility.
unified-facade-routing = []

# Canonical document CRUD path. When enabled and a RecordStore is injected,
# DocumentService writes/reads durable document state through ProximaRecord while
# keeping legacy v1 document API shapes at the service edge.
canonical-document-store = []

# SKS Graph-First Architecture (default on for production)
# When enabled, uses OrionBackedEntityStore for 3-6x better performance
# When disabled, falls back to legacy split storage
graph-first-sks = []

# DataFusion integration for compute engine compatibility
# DataFusion 51.x uses Arrow 57.x — versions are aligned as of 2026-04
datafusion-integration = ["dep:datafusion", "dep:datafusion-common"]
# ADR-059: in-process DuckDB OLAP engine (default OFF — adds the libduckdb C++
# build). Enables the perf-ledger harnesses to run DuckDB in-process (io-traced,
# same parquet/SQL) instead of the CLI subprocess.
duckdb = ["dep:duckdb"]

# Hardware acceleration features
gpu = ["metal", "proximadb-codec/gpu", "proximadb-hardware-caps/gpu", "proximadb-distance-kernel/gpu"]  # GPU acceleration support (CUDA, ROCm, MPS, OpenCL)
metal = ["dep:metal", "proximadb-codec/metal"]  # Enable Metal/MPS GPU support on macOS ARM64
# SIMD optimizations - ARM64 has NEON, not AVX
simd = []  # Placeholder - would need ARM NEON implementation
debug-filters = []  # Enable debug filtering for search operations

# ============================================================================
# CDC (CHANGE DATA CAPTURE) FEATURES
# ============================================================================
# CDC core is always available (event types, outbound CDC, sinks, transforms).
# Database connectors are experimental and require feature flags.

# EXPERIMENTAL: Native database CDC connectors (PostgreSQL, MySQL, MongoDB)
# WARNING: These connectors have partial implementations without network I/O.
# For production MySQL/MongoDB CDC, use Debezium with Kafka sink.
# PostgreSQL connector now has tokio-postgres connection layer (Issue #54).
experimental-cdc-connectors = ["dep:tokio-postgres"]

# ============================================================================
# EMBEDDED features moved to the proximadb-embedded crate (crates/binding/proximadb-embedded).
c_ffi = []  # Stub — the C-FFI bindings live in proximadb-embedded; kept here for the duckdb connector cfg.
python = []  # Stub — the python bindings live in proximadb-embedded; kept here for the windows CI.
# The server binary no longer compiles language bindings (Python/Java/Node.js/C-FFI).

# CPU optimizations - Disabled Intel MKL on ARM64
# cpu-optimized = ["intel-mkl"]
# intel-mkl = ["intel-mkl-src"]

# GPU acceleration (removed - was placeholder code)

# Enterprise catalog integrations (optional)
unity-catalog = ["proximadb-catalog/unity-catalog"]      # Databricks Unity Catalog integration
polaris-catalog = ["proximadb-catalog/polaris-catalog"]    # Apache Polaris (Iceberg REST Catalog) integration
delta-lake = ["proximadb-catalog/delta-lake"]         # Delta Lake table format support
enterprise-catalogs = ["unity-catalog", "polaris-catalog", "delta-lake"]

# OLTP catalog backends — metadata-only; record data stays in ProximaDB engines.
# Supports PostgreSQL / Neon / Supabase / CockroachDB (postgres wire),
# MariaDB / MySQL / TiDB (mysql wire), and embedded SQLite.
oltp-catalog = ["dep:sqlx"]
oltp-catalog-postgres = ["oltp-catalog", "sqlx/postgres"]
oltp-catalog-mysql    = ["oltp-catalog", "sqlx/mysql"]
oltp-catalog-sqlite   = ["oltp-catalog", "sqlx/sqlite"]
oltp-catalog-full     = ["oltp-catalog-postgres", "oltp-catalog-mysql", "oltp-catalog-sqlite"]

# TLS/mTLS support (enabled by default)
tls = []  # Enable TLS/mTLS certificate management and secure connections

# External libraries
# faiss-cpu = ["faiss"]  # Unused - no FAISS integration
# faiss-gpu = ["faiss"]  # Unused - no FAISS integration

[profile.release]
opt-level = 3
lto = "thin"          # Thin LTO: ~90% of full LTO perf, 3x faster compile
codegen-units = 6     # 60% of cores (6 of 10) for parallel LLVM codegen
debug = false
strip = false         # Keep symbols for profiling
incremental = false

# Test configuration
[[test]]
name = "integration"
path = "tests/rust/mod.rs"

# CDC integration tests — only built when cdc-kafka is enabled (cdc is feature-gated
# in src/lib.rs; these tests reference proximadb::cdc::*).
[[test]]
name = "cdc_e2e_test"
path = "tests/cdc_e2e_test.rs"
required-features = ["cdc-kafka"]

[[test]]
name = "cdc_integration_test"
path = "tests/cdc_integration_test.rs"
required-features = ["cdc-kafka"]

[[test]]
name = "cdc_rest_changefeed_e2e"
path = "tests/cdc_rest_changefeed_e2e.rs"
required-features = ["cdc-kafka"]

# Benchmark profile - optimized but allows faster iteration than full release
[profile.bench]
inherits = "release"
lto = "thin"  # Faster than "fat", still optimized
codegen-units = 4  # Balance between compile time and runtime performance
debug = false
strip = false  # Keep symbols for profiling tools (flamegraph, perf)
incremental = false

[[bench]]
name = "engine_performance_reporter"
harness = false
path = "benches/engine_performance_reporter.rs"

# Release build profile optimized for production
[profile.release-server]
inherits = "release"
lto = "fat"
codegen-units = 1
panic = "abort"
opt-level = 3
debug = false
strip = true

# Development build with tests
# IMPORTANT: dev and test profiles should match for artifact reuse
[profile.dev]
opt-level = 0
# `line-tables-only` keeps file:line in backtraces (panics still readable) but
# drops the heavy DWARF variable/type info. ~5-10x smaller .rlib/.dSYM, large
# link-time reduction on the ~80-crate workspace. Switch back to `true` only
# when stepping through variables in lldb.
debug = "line-tables-only"
overflow-checks = true
incremental = true
split-debuginfo = "unpacked"  # Faster linking, reusable debug info

# Test profile - must match dev exactly for artifact sharing
# Cargo ONLY reuses artifacts if profiles are identical
[profile.test]
opt-level = 0  # MUST match dev for artifact reuse (0, not 1!)
debug = "line-tables-only"  # MUST match dev (was `true`)
overflow-checks = true
incremental = true
split-debuginfo = "unpacked"

# Benchmark configuration - Organized by functional categories

# 1. COMPUTE & DISTANCE BENCHMARKS (01-03)
# Migrated to proximadb-bench binary
# [[bench]]
# name = "bench_01_core_distance"
# harness = false
# path = "benches/bench_01_core_distance.rs"

# Migrated to proximadb-bench binary
# [[bench]]
# name = "bench_02_hardware_simd"
# harness = false
# path = "benches/bench_02_hardware_simd.rs"

# Migrated to proximadb-bench binary (Arc optimizations already implemented)
# [[bench]]
# name = "bench_03_memory_vector"
# harness = false
# path = "benches/bench_03_memory_vector.rs"

# 2. STORAGE ENGINE BENCHMARKS (04 - Unified)
# Consolidated storage benchmark combining all storage tests:
# - Compression efficiency with immediate search testing
# - Engine lifecycle and creation overhead
# - Large-scale search performance
# - Insertion performance
[[bench]]
name = "bench_04_storage_unified"
harness = false
path = "benches/bench_04_storage_unified.rs"

[[bench]]
name = "bench_08_quantization_sst"
harness = false
path = "benches/bench_08_quantization_sst.rs"

[[bench]]
name = "bench_09_columnar_viper"
harness = false
path = "benches/bench_09_columnar_viper.rs"

# 3. SEARCH & QUERY BENCHMARKS (10)
[[bench]]
name = "bench_10_query_progressive"
harness = false
path = "benches/bench_10_query_progressive.rs"

[[bench]]
name = "bench_object_store_latency"
harness = false
path = "benches/bench_object_store_latency.rs"
# Co-design T3.2: measures the object-store round-trip latency floor (GET /
# ranged-GET / LIST). Defaults to in-process `memory://` (no network RTT — a
# harness smoke check); point BENCH_OBJECT_STORE_URL at s3:// (with --features
# aws) or file:// for real cloud-latency evidence.

# 4. UNIFIED & CROSS-ENGINE BENCHMARKS
# Note: Cross-engine comparisons now included in bench_storage_engines

# 5. SYSTEM & COMPREHENSIVE BENCHMARKS (12-13)
[[bench]]
name = "bench_12_system_optimization"
harness = false
path = "benches/bench_12_system_optimization.rs"

[[bench]]
name = "bench_13_complete_suite"
harness = false
path = "benches/bench_13_complete_suite.rs"

# Temporarily disabled benchmarks
# [[bench]]
# name = "vector_operations"
# harness = false
# path = "benches/vector_operations.rs"

# [[bench]]
# name = "metadata_lifecycle"
# harness = false
# path = "benches/metadata_lifecycle.rs"

# 6. GRAPH OPERATIONS BENCHMARKS
[[bench]]
name = "bench_14_graph_operations"
harness = false
path = "benches/bench_14_graph_operations.rs"

[[bench]]
name = "bench_graph_adjacency_vs_csr"
harness = false
path = "benches/bench_graph_adjacency_vs_csr.rs"

# 8. GRAPH ENGINE REPORTER (CSV)
[[bench]]
name = "graph_engine_reporter"
harness = false
path = "benches/graph_engine_reporter.rs"

[[bench]]
name = "bench_15_memory_pool"
harness = false
path = "benches/bench_15_memory_pool.rs"

[[bench]]
name = "bench_16_proximacodec_comprehensive"
harness = false
path = "benches/bench_16_proximacodec_comprehensive.rs"

[[bench]]
name = "bench_18_fp16_centroid_performance"
harness = false
path = "benches/bench_18_fp16_centroid_performance.rs"

[[bench]]
name = "bench_19_comprehensive_optimization"
harness = false
path = "benches/bench_19_comprehensive_optimization.rs"

[[bench]]
name = "bench_20_facade_routing"
harness = false
path = "benches/bench_20_facade_routing.rs"

[[bench]]
name = "bench_21_query_cache"
harness = false
path = "benches/bench_21_query_cache.rs"

[[bench]]
name = "bench_21_hybrid_search_fusion"
harness = false
path = "benches/bench_21_hybrid_search_fusion.rs"

[[bench]]
name = "bench_22_multimodal_queries"
harness = false
path = "benches/bench_22_multimodal_queries.rs"

# Disabled — rotted against refactored internal APIs (VectorOperationsService,
# GraphOperationsService::{create_graph,add_node,add_edge}, FilesystemFactory::new,
# GraphFusionParams); see TD-BENCH-1. Resurrect once those surfaces stabilize.
# [[bench]]
# name = "bench_cross_modal_fusion_e2e"
# harness = false
# path = "benches/bench_cross_modal_fusion_e2e.rs"

[[bench]]
name = "bench_23_codec_vector_base_xor"
harness = false
path = "benches/bench_23_codec_vector_base_xor.rs"

[[bench]]
name = "hmgi_benchmark"
harness = false
path = "benches/hmgi_benchmark.rs"

[[bench]]
name = "streaming_search_bench"
harness = false
path = "benches/streaming_search_bench.rs"

[[bench]]
name = "bench_vector_object_economy"
harness = false
path = "benches/bench_vector_object_economy.rs"

[[bench]]
name = "bench_vector_object_economy_e2e"
harness = false
path = "benches/bench_vector_object_economy_e2e.rs"

# Disabled — rotted: IndexEngine no longer exposes insert_record (downcast to
# AxisManager required); see TD-BENCH-1. Resurrect once that path stabilizes.
# [[bench]]
# name = "bench_warm_path_profile"
# harness = false
# path = "benches/bench_warm_path_profile.rs"

# Recall@k computation bench (criterion). NOTE: measures the recall-computation
# utility over synthetic vectors + simulated approximate results — it does NOT
# measure the ANN engine's end-to-end Recall@10 (that needs an index-vs-ground-truth
# bench). Registered so the file is build-checked and runnable.
[[bench]]
name = "bench_23_recall_at_k"
harness = false
path = "benches/bench_23_recall_at_k.rs"

# Real engine Recall@k (index-vs-ground-truth) — builds a real ANN index via the
# embedded engine and measures actual recall against a brute-force oracle, unlike
# bench_23 which times the recall computation over simulated results.
[[bench]]
name = "bench_24_recall_at_k_engine"
harness = false
path = "benches/bench_24_recall_at_k_engine.rs"

# TD-096 S2 / S1.5: per-search GET-count (SST vs HELIX) on the filesystem seam.
[[bench]]
name = "bench_25_search_get_count"
harness = false
path = "benches/bench_25_search_get_count.rs"

# ============================================================================
# BUILD OPTIMIZATION STRATEGY
# ============================================================================
#
# GOAL: Maximize artifact reuse across build, test, and bench targets
#
# KEY INSIGHT: Cargo reuses artifacts ONLY when profiles are IDENTICAL.
# Any difference in opt-level, debug, or other flags triggers full recompilation.
#
# PROFILE CONFIGURATION:
# 1. dev = test (opt-level=0, debug=true)
#    → cargo build and cargo test share the SAME artifacts
#    → Test-only code is compiled incrementally on top of build artifacts
#
# 2. bench (opt-level=3, thin LTO, 4 codegen units)
#    → Faster than full release (fat LTO, 1 codegen unit)
#    → Still well-optimized for performance measurements
#    → Keeps debug symbols for profiling tools (perf, flamegraph)
#
# 3. release (opt-level=3, LTO=true, 1 codegen unit)
#    → Full optimization for production
#
# 4. release-server (inherits release + strip=true)
#    → Maximum optimization, stripped for deployment
#
# WORKFLOW:
# Development cycle:
#   cargo build       # 2m37s (first time, opt-level=0)
#   cargo test        # ~5-10s (reuses build artifacts, incremental)
#   cargo test        # ~2-5s (subsequent runs, incremental)
#
# Performance testing:
#   cargo build --release     # ~3-4min (opt-level=3, full LTO)
#   cargo bench               # ~90s (thin LTO, reuses some artifacts)
#
# Production deployment:
#   cargo build --profile release-server  # ~4-5min (fat LTO, stripped)
#
# ARTIFACT REUSE MECHANICS:
# ✓ dev → test:  100% reuse (profiles identical)
# ✓ release → bench:  Partial reuse (different LTO strategy)
# ✗ dev → bench:  No reuse (different opt-level)
# ✗ test → release:  No reuse (different opt-level)
#
# BENEFITS:
# ✓ Fast test iterations (dev artifacts fully reused)
# ✓ Faster benchmarks (thin LTO vs fat LTO: ~50% compile time reduction)
# ✓ Incremental compilation works across cargo build/test
# ✓ Debug symbols in benchmarks for profiling
# ✓ Production builds fully optimized independently
#
# TRADE-OFFS:
# - Tests run at opt-level=0 (slower execution, but compilation is 10x faster)
# - If test execution speed matters, use: cargo test --release (slower compile)
# - Benchmarks must always use optimized builds (handled automatically)
#
# RECOMMENDED COMMANDS:
# Daily dev: cargo test               # Fast compile, adequate test speed
# Quick check: cargo test --lib       # Even faster (lib only)
# Specific test: cargo test test_name # Incremental, only compiles needed code
# Performance: cargo bench            # Optimized but faster than full release
# Production: cargo build --profile release-server  # Maximum optimization
# ============================================================================

# Dependencies optimization for faster compilation
# Optimize dependencies even in dev/test builds (they rarely change)
[profile.dev.package."*"]
opt-level = 2  # Optimize dependencies (arrow, parquet, etc.)
debug = false  # No debug info for dependencies (saves disk space)

[profile.test.package."*"]
opt-level = 2  # Must match dev for artifact reuse
debug = false

[profile.bench.package."*"]
opt-level = 3  # Full optimization for dependencies in benchmarks
debug = false

# Special handling for heavy dependencies
[profile.dev.package.arrow]
opt-level = 3  # Arrow is performance-critical

[profile.dev.package.parquet]
opt-level = 3  # Parquet is performance-critical

[profile.dev.package.arrow-array]
opt-level = 3

[profile.dev.package.arrow-buffer]
opt-level = 3

[profile.test.package.arrow]
opt-level = 3  # Must match dev

[profile.test.package.parquet]
opt-level = 3  # Must match dev

[profile.test.package.arrow-array]
opt-level = 3

[profile.test.package.arrow-buffer]
opt-level = 3
