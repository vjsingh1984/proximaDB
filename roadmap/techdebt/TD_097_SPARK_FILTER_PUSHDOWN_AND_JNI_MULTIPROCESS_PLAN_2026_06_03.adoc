= TD-097 Spark Filter Pushdown + Multi-Process JNI Safety
:toc: auto
:toclevels: 3
:status: RUST FOUNDATION SHIPPED; JVM DATASOURCE V2 + LIFECYCLE HARDENING POST-MVP

== Context

Two smaller TD-097 follow-ups landed in scope after the 2026-06-02 sprint:

. **Spark filter pushdown**: `spark_plan_input_partitions(emb, table, filters_json, num_partitions)` originally accepted `filters_json` but ignored it. The Rust-side foundation now lowers supported Spark predicates into the canonical scan filter path; JVM DataSource V2 reporting remains post-MVP.
. **Multi-process JNI safety**: `EMBEDDED: OnceLock<Arc<EmbeddedProximaDB>>` is process-level and set-once-strict. JVM class-loader reloads (Spark executor reuse, IDE hot-reload) leave the singleton pinned to its first data dir, with no teardown. Two JVM processes loading the cdylib share the OS-cached library but get independent ProximaDB instances, relying on the file-lock coordination in `src/embedded/coordination/`.

This plan separates the two — they ship independently — but they're grouped because both touch the JNI surface and both are smaller than a full TD on their own.

=== Current state (verified)

**Filter surface**: `spark_plan_input_partitions(emb, table, filters_json, num_partitions)` now lowers Spark filter JSON into the canonical `FilterExpression` where possible. The Rust foundation is real; the JVM DataSource V2 layer that would report pushed/residual filters to Spark is still missing and remains Beta/post-MVP.

**Scan predicate plumbing**: `EmbeddedProximaDB::scan_records_filtered` forwards the optional filter to `VectorOperationsService::scan_records_paginated`, where tenant/RLS predicates and pushed filters are applied before the limit. TD-099(3d) added the deduped, time-ordered scan index that makes repeated cursor-page drains efficient once warm.

**JNI singleton**: `crates/binding/proximadb-spark-jni/src/lib.rs` defines a process-local OnceLock. `initialize` sets it strictly. A diagnostic `JNI_OnUnload` hook now logs unload state and performs a best-effort flush when initialized, but there is still no `_reset` hook. Cross-process safety relies on `src/embedded/coordination/mod.rs` (`AccessMode::Exclusive` / `SharedRead` / `LeaderFollower`).

== Part 1 — Spark Filter Pushdown

[IMPORTANT]
.AS-BUILT 2026-06-03 (Rust-side foundation shipped)
====
Shipped as 2 commits — `a9786bb27` (lowering) + `d9187bf0c` (scan-apply) — deviating from the slice plan below:

* *F1 (lowering) — DONE*, but the IR target is the canonical `FilterExpression` (`proximadb-filter-expression`), reusing the existing `evaluate_filter_proxima` record-props evaluator — NOT a fold into a Spark-specific path. `lower_spark_filters_to_expression` lowers each TOP-LEVEL filter all-or-nothing and DROPS unrepresentable shapes (struct-path columns, `AlwaysTrue/False`, an `And/Or/Not` with any unrepresentable child) rather than silently mis-pushing.
* *F3 (record-level filter) — DONE*, via the TD-099(3d) scan predicate seam (`scan_records_paginated(filter)` → `evaluate_filter_proxima(f, &record.props)`, applied BEFORE the limit so cursor pagination stays correct). Threaded plan→partition→reader→`EmbeddedProximaDB::scan_records_filtered`. Filters evaluate against the record property tree (data columns); canonical fields are dropped.
* *F2 (plan-time partition pruning) — DEFERRED*: the planner emits a single-partition fallback, so there is nothing to prune until shard-aware partitioning lands.
* *F4 (docs) — DONE* (this note + `TECHNICAL_DEBT.adoc` + the `spark.rs` module doc).
* *Part 2 (JNI multi-process/lifecycle safety) — M1 DONE 2026-06-04*: `NativeProximaDB.initialize(dataDir)` now emits structured `tracing` diagnostics for first init, duplicate init, empty data dir, construction failure, and set-once races; `crates/binding/proximadb-spark-jni/README.md` documents the one-embedded-instance-per-JVM lifecycle, class-loader reload constraint, multi-process coordination boundary, and fork-per-class test guidance. *M2 DONE 2026-06-04 (diagnostic scope)*: `JNI_OnUnload` logs unload state and performs a best-effort `EmbeddedProximaDB::flush()` when initialized; it deliberately does not reset/take the singleton because JVM native-library unload is process-scoped and rare. *M3 DONE 2026-06-04*: `crates/binding/proximadb-spark-jni/tests/cross_process_smoke.sh` verifies that a second JVM cannot initialize concurrently against the same exclusive embedded data dir and that a later JVM initializes after the first exits. The future Gradle/JUnit harness can wrap this behavior, but the release-blocking coordination smoke is now scriptable.

CRITICAL open dependency: the JVM DataSource V2 layer (`SupportsPushDownFilters`/`ScanBuilder`/`pushedFilters`) DOES NOT EXIST — `clients/jvm/spark-connector` is a JNI smoke shim. So there is no production driver pushing `filters_json` yet, and the "Spark re-applies dropped filters" contract has no home until that JVM layer is built (a separate Java/Gradle effort, overlapping TD-097(4)). The Rust foundation is exact and tested (12 lowering + 4 e2e tests) and ready for that JVM layer to consume.
====

=== Design (as-built plus deferred seam)

Lower `SparkFilter` JSON → the canonical predicate IR → push into `scan_records_filtered`. Two key questions:

. **Which IR?** `FilterExpression` is now the canonical reusable predicate shape for this path. Reuse it; do not create a Spark-specific predicate IR.

. **Where does pruning happen?** Two layers:
   * *Record-level prune*: shipped. `scan_records_filtered` evaluates the pushed predicate before the page limit so cursor pagination stays correct.
   * *Split/partition-level prune*: deferred into the shared split-aware read planning seam, not a Spark-only `plan_partitions` tweak. See `roadmap/techdebt/TD_097_SHARD_AWARE_PARTITION_PLAN_2026_06_03.adoc`, which is now refined around `SplitSource`, `SplitPlanner`, `RoutedReadPlan`, DataFusion, and Spark as one consumer.

=== Slice F1 — JSON-to-IR lowering (1 commit)

Status: DONE 2026-06-03. The details below are retained as the historical slice contract.

Critical files:
* `src/connectors/spark.rs` — add `fn lower_spark_filters_to_expression(json: &str) → Result<Option<FilterExpression>, SparkError>`. Parses the array of `SparkFilter` JSON values, folds with AND, returns `None` if empty. Unsupported operators are silently dropped (Spark accepts pushdown rejection — it'll re-apply on its side).

TDD: 10 inline tests in `src/connectors/spark.rs::tests::lower_filters_*` — one per supported operator + edge cases (empty array → `None`, malformed JSON → `Err(SparkError::InvalidArgument)`, unknown variant → drop-and-warn).

=== Slice F2 — Plan-time partition pruning (1 commit)

Status: DEFERRED / SUPERSEDED BY SHARED SPLIT-AWARE READ PLAN.

Do not implement this as a Spark-only method signature change. The canonical seam is now the
read-route/split-generation path from the notebook/DataFusion blueprint:
`RoutedReadPlan` + `SplitSource::generate_splits` + `SplitPlanner::plan_splits`, consumed by
DataFusion first and Spark later.

Historical sketch: this section originally proposed adding `Option<&FilterExpression>` directly to
`EmbeddedProximaDB::plan_partitions` and writing Spark-specific partition-pruning tests. That is no
longer the desired LLD. New tests belong in the shared split-aware plan and should validate
`RoutedReadPlan`, `SplitSource`, `SplitPlanner`, DataFusion partitions, and Spark consumption of the
same split inventory.

=== Slice F3 — Record-level filter in scan path (1 commit)

Status: DONE 2026-06-03. The details below are retained as the historical slice contract.

Critical files:
* `src/services/operations/vectors/legacy.rs::scan_records_with_tenant_context` — accept `Option<&FilterExpression>`, evaluate per record before adding to the result vec.
* `src/embedded/mod.rs::scan_records` — accept the filter, forward.
* `src/connectors/spark.rs::SparkPartitionReader` — store the filter (deserialize once at reader creation from the partition's `filter_json` field, encoded by the planner into the partition serialization).

TDD: 2 critical e2e tests in `src/connectors/spark.rs::tests`:
* `spark_filter_pushdown_predicate_eq` — write 100 records with `tenant_id ∈ {A,B}` 50/50, plan with `tenant_id = 'A'`, drain; assert exactly 50 records, all tenant_id=A.
* `spark_filter_pushdown_predicate_in_set` — write 200 records across 4 tenants, plan with `tenant_id IN ('A','B')`, drain; assert exactly 100.

=== Slice F4 — Docs (1 commit)

* `docs/10-quality/TECHNICAL_DEBT.adoc` — mark TD-097 filter pushdown DONE.
* Update `src/connectors/spark.rs` module doc comment with a worked filter-pushdown example.

== Part 2 — Multi-Process / Class-Loader JNI Safety

=== Design

Three failure modes to harden against:

. **JVM class-loader reload**: Spark sometimes evicts and reloads classes (e.g. dynamic SQL, structured streaming hot-reload). The `OnceLock` outlives class reload because it lives in the OS-loaded cdylib, not the JVM's class-loaded state. A second `NativeProximaDB.initialize(dataDir)` after reload returns `false` (we set-once-strict). **Acceptable behavior**: log clearly, document the constraint.

. **Two-JVM-on-same-host**: each JVM loads its own cdylib instance (OS deduplicates the file on disk but each process gets its own data segment), then both call `EmbeddedProximaDB::new(dataDir)`. The data dir's file-lock coordination layer (`src/embedded/coordination/`) handles this — `AccessMode::Exclusive` prevents concurrent initialization; today this normally blocks the contender until the holder exits.

. **JVM-exit recovery**: a crashed JVM leaves the file lock; the next JVM should detect a stale lock and reclaim. Today's coordination layer handles this (verified — `LockHeartbeat` timeout).

=== Slice M1 — Document, log, and assert the JNI lifecycle (1 commit)

Critical files:
* `crates/binding/proximadb-spark-jni/src/lib.rs` — extend `initialize` to log a structured `tracing::info!` with `data_dir`, PID, and a one-line summary on first init; log `tracing::warn!` with both old and new `data_dir` on a rejected second init.
* `crates/binding/proximadb-spark-jni/README.md` — new doc with:
  - "One ProximaDB instance per JVM process" — explicit
  - Class-reload constraint — explicit
  - Multi-process behavior — file-lock coordination, recovery on JVM crash
  - "Tests fork-per-class" recommendation (link to TD-097(4) Gradle plan)

TDD: 2 inline tests in `crates/binding/proximadb-spark-jni/tests/`:
* `initialize_logs_first_init` (uses `tracing-test` to capture log lines)
* `initialize_logs_rejected_second_init` (calls initialize twice with different dirs; assert warning logged + return `false`)

=== Slice M2 — `JNI_OnUnload` graceful shutdown hook (1 commit)

When the cdylib is unloaded (JVM exit, never class reload — the cdylib survives class reload), call a graceful flush + close on the embedded DB. This is correctness-improving for cold-shutdown scenarios where Spark hits a `System.exit(0)` without explicit `commitWriter`.

Critical files:
* `crates/binding/proximadb-spark-jni/src/lib.rs` — add `extern "system" fn JNI_OnUnload(_vm: JavaVM, _: *mut c_void)`. Tries to `take` from the OnceLock (rare — `OnceLock::take` isn't available; use `Box::leak` pattern + a `AtomicPtr` or `OnceCell<Mutex<Option<Arc<…>>>>` if we want true unload semantics). Pragmatically: log a structured shutdown line + best-effort flush via a shared shutdown channel.

Note: `JNI_OnUnload` is only called when the cdylib is unloaded, which the JVM almost never does. Document this clearly in the README. Don't over-invest.

TDD: a single integration test in `crates/binding/proximadb-spark-jni/tests/` — invoking `JNI_OnUnload` directly (it's safe; we control the function) and asserting a "shutdown logged" trace line.

=== Slice M3 — Cross-process e2e smoke (1 commit)

Status: DONE 2026-06-04.

Critical files: a single bash test under `crates/binding/proximadb-spark-jni/tests/cross_process_smoke.sh` that forks two Java processes both calling `initialize(sharedDir)`. Expected: one initializes and holds Exclusive, the second cannot initialize concurrently (current behavior is to block on the embedded file lock; a future nonblocking rejection is also acceptable), and a third JVM initializes after the first exits.

This can later be wrapped by TD-097(4) (Gradle/JUnit), but it is intentionally scriptable before that harness exists.

== Verification

[source,sh]
----
# Part 1 — Filter pushdown
cargo test --lib connectors::spark::tests::lower_filters_                          # F1
cargo test --lib connectors::spark::tests::spark_filter_pushdown_                  # F3
# F2 split/partition pruning moved to the shared split-aware read plan:
# roadmap/techdebt/TD_097_SHARD_AWARE_PARTITION_PLAN_2026_06_03.adoc

# Part 2 — JNI lifecycle
cargo test -p proximadb-spark-jni initialize_logs_                                  # M1
cargo test -p proximadb-spark-jni jni_on_unload_                                    # M2
bash crates/binding/proximadb-spark-jni/tests/cross_process_smoke.sh                # M3

# Aggregate
cargo fmt && cargo clippy --lib -- -D warnings
cargo test --lib connectors::spark
cargo test -p proximadb-spark-jni
clients/jvm/spark-connector/run-smoke.sh
----

== Out of scope

* True JVM class-loader unload semantics (`JNI_OnLoad` re-entry on class reload) — Java reality is that `System.loadLibrary` is a no-op on reload, so there's no second `JNI_OnLoad`. The OnceLock survives class reload; documented behavior, not a fixable defect.
* Coordinated reset across JVMs (e.g. "kick the other JVM out of its Exclusive lock") — out of scope. Operator concern, not a connector concern.
* Filter pushdown for nested-struct columns (`StructField` paths) — `SparkFilter` already serializes them; lowering them into `FilterExpression` requires path-expression support that doesn't exist yet. Drop them in F1 (silent rejection is the spec-compliant behavior).
* Cost-based filter ordering (apply selective filters first) — not needed at our current scale; revisit when Spark's `Filter` count routinely exceeds 5+ per scan.

== Convergence summary (Convergence Gate)

* Single predicate IR: `FilterExpression` is reused for Spark filter lowering and record-scan predicates; no Spark-local IR was introduced.
* Split/partition pruning moves to the shared read-planning seam (`RoutedReadPlan` + `SplitSource` + `SplitPlanner`) so DataFusion/notebook/Spark/Flight consume one split contract.
* JNI lifecycle docs go in the binding crate's README — not in the Rust top-level docs, since the lifecycle is JNI-specific. Pyo3/NAPI/CFFI bindings have their own equivalent constraints, documented in their own READMEs.
