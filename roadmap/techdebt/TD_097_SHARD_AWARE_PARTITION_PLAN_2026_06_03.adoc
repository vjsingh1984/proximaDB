= TD-097 Split-Aware Read Planning for Spark, DataFusion, and Notebook
:toc: auto
:toclevels: 3
:status: POST-MVP / EXPERIMENTAL; REFINED 2026-06-04 AGAINST RUST-NATIVE MPP CANONICAL SEAM

Implementation update 2026-06-05: Slice A has an initial ProximaDB-side implementation anchor in
`src/query/read_route.rs`. It defines `RoutedReadPlan`, `ReadRouteExplanation`, split strategy,
policy boundary, freshness SLA, selected/rejected route metadata, and optional distributed placement
metadata for future Ballista-backed `DataFusionDistributed` execution.
`src/query/compute_scheduler.rs` can now materialize the typed plan, and pgwire `EXPLAIN SELECT`
includes the nested read-route payload. Runtime read behavior is unchanged.

[IMPORTANT]
.MVP BOUNDARY
====
This is not part of the v0.2 supported surface. The current single-partition
fallback is correct for MVP but not parallel. Split-aware Spark planning,
DataFusion partition parallelism, cursor-range splits, and shard/cluster-aware
read routing remain Beta/Experimental until they carry a routed read plan,
policy/freshness guards, EXPLAIN diagnostics, and no-overlap correctness tests.
====

[IMPORTANT]
.2026-06-04 CANONICALIZATION
====
This plan was originally Spark-specific. The active architecture has since
shifted to a Rust-native DataFusion/MPP-first direction:

* `docs/12-design/DATA_WAREHOUSE_AND_ENGINEERING_COURSE_CORRECTION_2026_06_04.adoc`
  makes Rust-native DataFusion/MPP the primary data-engineering route.
* `docs/12-design/DATAFUSION_BALLISTA_DISTRIBUTED_EXECUTION_2026_06_05.adoc`
  defines the ProximaDB-owned route contract and Ballista integration boundary.
* `docs/12-design/PROXIMA_NOTEBOOK_PSEUDO_DISTRIBUTED_BLUEPRINT_2026_06_04.adoc`
  defines the shared split-generation seam for notebook/DataFusion and Spark.
* `docs/12-design/COMPETITIVE_OLTP_OLAP_MPP_TRAJECTORY_2026_05_20.adoc`
  requires route-once planning with cataloged authority, freshness, policy, and
  rejected-route reasons.
* `docs/12-design/adr/ADR-004-unified-explain-contract.adoc` requires route
  diagnostics to be mechanically derived from the validated plan.

Therefore the objective is no longer "make Spark partition planning smarter" in
isolation. The objective is: build one split-aware read contract that feeds
DataFusion local/pseudo-distributed execution, embedded notebook scans, Arrow
Flight tickets, and the Spark JNI connector. Spark is an external/Beta consumer
of this seam, not the canonical execution engine.
====

== Vision

ProximaDB read parallelism should come from a shared, catalog-aware split
contract:

* xCatalog and the route planner decide whether a read is safe, fresh enough,
  and policy-compliant.
* A `RoutedReadPlan` (read-side sibling of the existing write
  `RoutedExecutionPlan`) records compute route, split count, pushed/residual
  predicates, authority mode, policy boundary, freshness SLA, and rejected
  routes for ADR-004 EXPLAIN.
* `SplitSource` emits `FileSplit` values with `SplitStatistics` and locality
  hints.
* `SplitPlanner` groups those splits into partitions for the selected backend.
* Leaf readers execute typed splits directly without per-row route
  recomputation.

The same split generator must serve DataFusion `ProximaScanExec`, Spark
`SparkInputPartition`, notebook Arrow readers, and future Arrow Flight split
tickets. No connector gets its own parallel-scan truth.

== Objective

Replace `EmbeddedProximaDB::plan_partitions`'s one
`FileSplit::whole_collection(collection)` fallback with a shared split-generation
path, while preserving the fallback whenever metadata or safety guards are
missing.

Success means:

* DataFusion and Spark consume the same `FileSplit` inventory.
* Split pruning uses canonical `FilterExpression`/DataFusion predicate lowering
  and `SplitStatistics`; unsupported predicates remain residual.
* Live reads do not double-count or gap records across partitions.
* Policy and tenant/RLS predicates are applied before data leaves the ProximaDB
  policy boundary.
* EXPLAIN shows selected route, partition count, split type, pushed/residual
  predicates, freshness state, and rejected split/route reasons.

== Non-Objectives

* Do not claim distributed execution maturity. Multi-split local execution is
  not a multi-node cluster.
* Do not make Spark the primary MPP route. Spark remains a Beta/external
  connector consumer.
* Do not add a connector-local `ScanFilter` or encode policy semantics only in a
  `collection://...?shard=...` string.
* Do not route per row or per record batch. Route once per plan/fragment/split.
* Do not create new durable authority, WAL semantics, or catalog state for
  splits.

== Current State (Verified)

`EmbeddedProximaDB::plan_partitions(collection, _num_partitions)` validates the
collection and returns exactly one Spark partition:

[source,rust]
----
Ok(vec![SparkInputPartition::from_splits(
    0,
    vec![FileSplit::whole_collection(collection)],
)])
----

`FileSplit`, `SplitStatistics`, `SplitLocality`, `SplitGenerator`, and
`SplitPlanner` are real in
`crates/storage/proximadb-storage-common/src/format_splits.rs`. They are not yet
fed by collection/storage metadata in the embedded scan path.

`scan_records_filtered(collection, cursor, limit, filter)` is real and pushes the
optional `FilterExpression` into `VectorOperationsService::scan_records_paginated`
before the limit. TD-099(3d) added a lazy, deduped, time-ordered in-memory scan
index for the unflushed WAL/memtable path, invalidated on every mutation and
rebuilt from authoritative memtable state on read.

The existing write-side `RoutedExecutionPlan` and `ComputeBackend` are real for
table-write planning. The read-side equivalent is still a planned seam in the
notebook/DataFusion blueprint.

== HLD

[source,text]
----
Protocol/API facade
  pgwire / REST / Flight / embedded notebook / Spark JNI
        |
        v
Logical planner / surface lowering
  SQL, notebook plan, Spark filters, API scan request
        |
        v
xCatalog + policy + freshness + stats
        |
        v
Route planner
  -> RoutedReadPlan { compute_route, authority_mode, policy_boundary,
                      freshness_sla, split_strategy, rejected_routes }
        |
        v
SplitSource::generate_splits(collection, target_partitions, filters, snapshot)
        |
        v
SplitPlanner::plan_splits(FileSplit[], target_partitions)
        |
        +--> DataFusion ProximaScanExec partitions
        +--> SparkInputPartition values
        +--> embedded/notebook Arrow readers
        +--> future Arrow Flight split tickets
----

Fallback order:

. Segment-backed splits when engine/catalog metadata is present (row groups,
  blocks, range files, or cataloged object-store manifests).
. Pinned cursor-range splits only when a stable read snapshot/range contract is
  available.
. `FileSplit::whole_collection` when split metadata or safety guards are absent.

== LLD Seams

=== Seam A: `SplitSource`

Add an opt-in split source in storage-common rather than forcing every
`UnifiedStorageEngine` implementation to grow a method at once.

[source,rust]
----
pub struct SplitRequest<'a> {
    pub collection: &'a str,
    pub target_partitions: usize,
    pub filter: Option<&'a FilterExpression>,
    pub snapshot: SnapshotRef,
    pub require_policy_enforced: bool,
}

#[async_trait::async_trait]
pub trait SplitSource: Send + Sync {
    async fn generate_splits(&self, request: SplitRequest<'_>) -> anyhow::Result<Vec<FileSplit>>;
}
----

The exact module/type names can evolve with the DataFusion blueprint, but the
ownership rule is fixed: split generation is a shared storage/query seam, not a
Spark connector helper.

=== Seam B: Split Types

Use existing `FileSplit` constructors where possible:

* `RowGroup` for VIPER/NOVA/RAPTOR Parquet row groups.
* `Block` for SST/PAX blocks when block metadata is available.
* `HilbertRange`, `SuperBlock`, and `ZOrderRange` where engines already expose
  such metadata.
* `ByteRange` only for real byte ranges or the current whole-collection
  fallback.

Add a `CursorRange` split only after defining a pinned-range contract:

[source,rust]
----
CursorRange {
    start: Option<ScanCursor>,
    end: Option<ScanCursor>,
    snapshot: SnapshotRef,
}
----

TD-099(3d)'s scan index makes range seeking cheap once warm, but it does not by
itself make parallel live cursor ranges safe under concurrent writes. Cursor
ranges require one of:

* a pinned snapshot/branch;
* immutable flushed segment inventory;
* or a read epoch that freezes the split boundary for the query.

Without one of those guards, fall back to the single whole-collection split.

=== Seam C: Reader Contract

Readers consume typed split metadata, not connector-local strings. The
`collection://` URI can remain a compatibility serialization format for Spark
JSON, but the Rust-side reader should parse it immediately into a typed split
plan and reject unknown split attributes explicitly.

Reader execution rules:

* tenant/RLS predicates are part of the pushed predicate before the limit;
* unsupported predicates are residual and must be visible in EXPLAIN;
* each split owns a disjoint range, row group, block, or segment inventory;
* a split reader must not silently broaden to whole-collection scan unless the
  routed plan records that fallback.

=== Seam D: `RoutedReadPlan` / EXPLAIN

Before split-aware reads become default-routable, define a read-side route plan
with at least:

* `compute_route`: `Native`, `DataFusionLocal`, `DataFusionDistributed`,
  `ExternalDelegated("spark")`, etc.
* `authority_mode`, `policy_boundary`, `freshness_sla`;
* `split_strategy`: `Segment`, `CursorRange`, `WholeCollection`;
* `partition_count`, estimated rows/bytes, and stats freshness;
* pushed filters, residual filters, and rejected pushdowns;
* rejected routes/split strategies with concrete reasons.

This is the read equivalent of the existing write `RoutedExecutionPlan` and is
governed by ADR-004.

== Slices

=== Slice A: Document and Type the Read-Route Contract

Status: PARTIAL 2026-06-05 — `src/query/read_route.rs` now carries the typed route/explain
contract and unit tests. `ComputeScheduler` materializes it, and pgwire `EXPLAIN SELECT` emits a
nested read-route payload. Split-aware execution paths still need to consume it.

Critical files:

* `src/query/read_route.rs` — add/extend `RoutedReadPlan` / `ReadRouteExplanation` using the same
  `ComputeBackend` vocabulary.
* `src/observability/route_explain.rs` / `src/query/explain.rs` — expose read
  route diagnostics in ADR-004 shape.
* This TD and the notebook blueprint — keep the seam named once.

Acceptance:

* Unit tests cover selected route, rejected route, split strategy, policy
  boundary, and freshness rendering.
* No execution path changes yet.

=== Slice B: Introduce `SplitSource` and Preserve Fallback

Critical files:

* `crates/storage/proximadb-storage-common/src/format_splits.rs` — add
  request/trait/types.
* `src/embedded/mod.rs::plan_partitions` — call the shared split source when
  present, else preserve `FileSplit::whole_collection`.
* `src/connectors/spark.rs` — consume the resulting splits without changing the
  Java shape.

Acceptance:

* Missing collection still errors.
* Collections with no split metadata still produce exactly one partition.
* Spark smoke remains green.

=== Slice C: Segment-Backed Splits for One Real Engine

Choose one engine with real segment metadata first, preferably a Parquet-backed
path already close to DataFusion/object-store execution.

Acceptance:

* A multi-segment fixture produces multiple `FileSplit` values with row/byte
  estimates.
* `SplitPlanner` balances splits by estimated cost.
* Filters that can be evaluated from `SplitStatistics` prune splits; residual
  filters are retained.

=== Slice D: DataFusion Scan Consumes Splits

Wire `ProximaDataFusionTable::scan` / `ProximaScanExec` to consume the same
planned splits. This is the primary route under the June 4 course correction.

Acceptance:

* DataFusion reads a real collection through N partitions.
* `explain()` shows `compute_route = DataFusionLocal`, split count, pushed and
  residual filters.
* Single-partition fallback remains correct.

=== Slice E: Spark Connector Consumes the Shared Split Contract

Keep this after DataFusion because Spark is no longer the primary execution
route. Reuse the same `SplitSource` output, serialized into `SparkInputPartition`
for JNI.

Acceptance:

* Spark JNI smoke still passes.
* A split-aware fixture drains all partitions with no duplicate record ids and
  no missing records.
* Java/Scala DataSource V2 remains separate post-MVP work; this slice only
  prepares the underlying split contract.

== Verification

[source,sh]
----
cargo test --lib query::read_route
cargo test -p proximadb-storage-common format_splits
cargo test --lib embedded::tests::plan_partitions
cargo test --lib datafusion::proxima_scan_exec
cargo test --lib connectors::spark::tests::spark_split_aware_read_

clients/jvm/spark-connector/run-smoke.sh
bash crates/binding/proximadb-spark-jni/tests/cross_process_smoke.sh
----

== Out of Scope

* JVM DataSource V2 `SupportsPushDownFilters` and residual-filter reporting.
* Gradle/JUnit migration for the Spark smoke harness.
* Multi-node worker scheduling and shuffle.
* Claiming Spark, SQL parity, or distributed execution as supported v0.2
  surfaces.
* External-authoritative open-table commits.

== Convergence Summary

* Split generation is a shared storage/query seam; Spark is only a consumer.
* xCatalog and routed read plans own authority, freshness, policy, route, and
  maturity decisions.
* `FileSplit` / `SplitPlanner` remain the common parallel-read physical
  contract.
* `scan_records_filtered` remains the canonical cursor-paginated record scan
  path until segment-native readers are wired.
* All fallback and rejection decisions must be visible in unified EXPLAIN.
