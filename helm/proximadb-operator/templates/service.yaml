apiVersion: v1
kind: Service
metadata:
  name: {{ include "proximadb-operator.fullname" . }}
  labels:
    {{- include "proximadb-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "proximadb-operator.selectorLabels" . | nindent 4 }}
