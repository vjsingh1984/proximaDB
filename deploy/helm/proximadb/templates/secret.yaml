{{- if .Values.proximadb.auth.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "proximadb.fullname" . }}-auth
  labels:
    {{- include "proximadb.labels" . | nindent 4 }}
type: Opaque
data:
  api-key: {{ .Values.proximadb.auth.apiKey | b64enc | quote }}
{{- end }}
